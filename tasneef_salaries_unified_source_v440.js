(function(){
'use strict';
if(window.__tasneefPayrollServerOnlyV474) return;
window.__tasneefPayrollServerOnlyV474=true;
const VERSION='V474 مصدر الرواتب من السيرفر فقط';
const $=id=>document.getElementById(id);
const S=v=>String(v??'').trim();
const N=v=>Number(String(v??'').replace(/,/g,''))||0;
const esc=v=>S(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=v=>S(v).toLowerCase().replace(/[إأآا]/g,'ا').replace(/[ىي]/g,'ي').replace(/ة/g,'ه').replace(/[ًٌٍَُِّْـ]/g,'').replace(/[^\p{L}\p{N}]+/gu,' ').replace(/\s+/g,' ').trim();
const money=v=>N(v).toLocaleString('en-US',{maximumFractionDigits:2});
const iso=v=>{if(!v)return'';const d=new Date(v);return Number.isNaN(d.getTime())?'':d.toISOString().slice(0,10)};
const today=()=>iso(new Date());
const monthRange=m=>{const [y,mo]=S(m).split('-').map(Number);if(!y||!mo)throw new Error('الشهر المحدد غير صالح');const last=new Date(y,mo,0).getDate();return{month:`${y}-${String(mo).padStart(2,'0')}`,start:`${y}-${String(mo).padStart(2,'0')}-01`,end:`${y}-${String(mo).padStart(2,'0')}-${String(last).padStart(2,'0')}`,days:last,fromIso:`${y}-${String(mo).padStart(2,'0')}-01T00:00:00+03:00`,toIso:`${y}-${String(mo).padStart(2,'0')}-${String(last).padStart(2,'0')}T23:59:59+03:00`};};
const technical=v=>{const t=S(v);return !t||/^[0-9a-f]{8}-[0-9a-f-]{27,}$/i.test(t)||/\b(select|insert|update|delete|sql|jwt|debug|console|function|undefined|null)\b/i.test(t)||(/[{}\[\]]/.test(t)&&t.length>40)||t.length>180};
const human=v=>technical(v)?'':S(v);
function activeStatusValue(v){const n=norm(v);return ['active','enabled','نشط','فعال','مفعل','يعمل'].includes(n);}
function inactiveStatusValue(v){const n=norm(v);return ['inactive','stopped','terminated','disabled','موقوف','متوقف','منتهي','منتهي الخدمه','منتهية الخدمة'].includes(n);}
function isExplicitlyActive(o){if(!o)return false;if(Object.prototype.hasOwnProperty.call(o,'is_active'))return o.is_active===true;if(Object.prototype.hasOwnProperty.call(o,'active'))return o.active===true;const st=field(o,['status','employee_status','work_status','account_status']);return st?activeStatusValue(st):false;}
function isApprovedAttendance(r){if(!r||r.deleted_at||r.is_deleted===true||r.is_cancelled===true||r.cancelled===true)return false;const st=norm(field(r,['status','attendance_status','approval_status','record_status']));if(st&&(['ملغي','مرفوض','غير معتمد','failed','cancelled','rejected'].some(x=>st.includes(norm(x)))))return false;if(Object.prototype.hasOwnProperty.call(r,'is_approved')&&r.is_approved===false)return false;const checkIn=timeOnly(field(r,['check_in','in_time','start_time','login_time']));return !!checkIn;}
const state={rows:[],employees:[],projects:[],users:[],assignments:[],attendance:[],daily:[],adjustments:[],sources:[],diagnostics:{},incomplete:false,loading:false};
let printBusy=false;
function msg(text,bad=false){const el=$('salaryMsg');if(el){el.textContent=text;el.className='msg '+(bad?'err':'');el.style.display='block';}console[bad?'error':'log']('[payroll-v474]',text);}
function sourceError(name,table,error){const e={name,table,ok:false,count:0,error:S(error?.message||error),code:S(error?.code||''),details:S(error?.details||'')};state.sources.push(e);console.error('[payroll source]',e);return e;}
async function fetchAll(table,queryBuilder){let rows=[],from=0,size=1000;while(true){let q=window.sb.from(table).select('*').range(from,from+size-1);if(queryBuilder)q=queryBuilder(q);const r=await q;if(r.error)throw r.error;const part=r.data||[];rows.push(...part);if(part.length<size)break;from+=size;if(from>100000)throw new Error(`تجاوز حد القراءة الآمن في ${table}`);}return rows;}
async function readFirst(name,tables,builder,{required=false,merge=false}={}){let all=[],used=[],errors=[];for(const table of tables){try{const rows=await fetchAll(table,builder);used.push(table);state.sources.push({name,table,ok:true,count:rows.length,error:''});if(merge){all.push(...rows);}else if(rows.length){return{rows,table,ok:true};}}catch(e){errors.push(sourceError(name,table,e));}}
if(merge&&all.length)return{rows:dedupeRaw(all),table:used.join(' + '),ok:true};
if(!merge&&used.length)return{rows:[],table:used[0],ok:true};
if(required)state.incomplete=true;return{rows:[],table:tables[0],ok:false,errors};}
function dedupeRaw(rows){const seen=new Set();return rows.filter(r=>{const k=S(r.id)||JSON.stringify(r);if(seen.has(k))return false;seen.add(k);return true;});}
function field(o,names){for(const n of names){const v=o?.[n];if(v!==undefined&&v!==null&&S(v)!=='')return v;}return'';}
function employeeAliases(e){return [...new Set([...(Array.isArray(e?.__aliases)?e.__aliases:[]),field(e,['id']),field(e,['worker_id']),field(e,['employee_id']),field(e,['app_worker_id']),field(e,['employee_code']),field(e,['worker_code']),field(e,['employee_ts_id']),field(e,['code'])].map(S).filter(Boolean))];}
function employeeKey(e){return S(e?.__canonical_id||field(e,['employee_id','worker_id','id','app_worker_id','employee_code','worker_code','employee_ts_id','code']));}
function employeeCode(e){return S(field(e,['employee_code','worker_code','employee_ts_id','code','id']));}
function employeeName(e){return human(field(e,['app_name','display_name','name','full_name','employee_name','worker_name','iqama_name','residency_name']))||'موظف بلا اسم';}
function iqamaName(e){return human(field(e,['iqama_name','residency_name','official_name','full_name','name']))||employeeName(e);}
function employeeRole(e){const raw=human(field(e,['job_title','position','employee_type','job_type','role','title']))||'عامل';const n=norm(raw);if(n.includes('مشرف')||n.includes('supervisor'))return'مشرف';if(n.includes('حارس')||n.includes('امن')||n.includes('guard'))return'حارس';if(n.includes('فني')||n.includes('صيانه')||n.includes('technician'))return'فني';if(n.includes('سائق')||n.includes('driver'))return'سائق';if(n.includes('ادار')||n.includes('مدير')||n.includes('محاسب')||n.includes('موارد')||n.includes('تشغيل'))return'إدارة وتشغيل';return raw==='عامل'?'عامل':raw;}
function isClient(e){const n=norm(field(e,['role','user_type','account_type']));return n==='client'||n.includes('عميل');}
function payrollEligibleProfile(e,range){if(!e||isClient(e)||e.deleted_at||e.is_deleted===true)return false;const start=iso(field(e,['work_start_date','start_date','hire_date','joining_date','service_start']));const end=iso(field(e,['work_end_date','end_date','termination_date','stop_date','service_end']));if(start&&start>range.end)return false;if(end&&end<range.start)return false;return !!(employeeKey(e)||employeeCode(e));}
function mergeEmployeeProfiles(sources,range){
 const priority={employees_master_v386:1,employees:2,workers:3,app_users:4};
 const records=[];
 for(const src of sources)for(const raw of src.rows)if(payrollEligibleProfile(raw,range))records.push({...raw,__source:src.table,__sourcePriority:priority[src.table]||99});
 const parent=records.map((_,i)=>i),rank=records.map(()=>0),aliasOwner=new Map();
 const find=i=>{while(parent[i]!==i){parent[i]=parent[parent[i]];i=parent[i];}return i;};
 const union=(a,b)=>{a=find(a);b=find(b);if(a===b)return;if(rank[a]<rank[b])[a,b]=[b,a];parent[b]=a;if(rank[a]===rank[b])rank[a]++;};
 records.forEach((r,i)=>{for(const a of employeeAliases(r)){if(aliasOwner.has(a))union(i,aliasOwner.get(a));else aliasOwner.set(a,i);}});
 const groups=new Map();records.forEach((r,i)=>{const root=find(i);if(!groups.has(root))groups.set(root,[]);groups.get(root).push(r);});
 const employees=[],aliases=new Map(),duplicateCanonicalIds=[];
 for(const group of groups.values()){
   group.sort((a,b)=>a.__sourcePriority-b.__sourcePriority);
   const merged={};for(let i=group.length-1;i>=0;i--)Object.assign(merged,group[i]);
   const allAliases=[...new Set(group.flatMap(employeeAliases))];
   const canonical=S(group.map(r=>field(r,['employee_id'])).find(Boolean)||group.map(r=>field(r,['worker_id'])).find(Boolean)||group.map(r=>field(r,['id'])).find(Boolean)||group.map(r=>field(r,['app_worker_id'])).find(Boolean)||group.map(r=>field(r,['employee_code','worker_code','employee_ts_id','code'])).find(Boolean));
   if(!canonical)continue;
   merged.__canonical_id=canonical;merged.__aliases=allAliases;merged.__source=group[0].__source;merged.__profile_count=group.length;
   employees.push(merged);allAliases.forEach(a=>aliases.set(a,canonical));aliases.set(canonical,canonical);
   if(group.length>1)duplicateCanonicalIds.push({canonical_employee_id:canonical,profile_records:group.length,aliases:allAliases});
 }
 return{employees,aliases,stats:{before:records.length,merged:records.length-employees.length,unique:employees.length,duplicateCanonicalIds}};
}
function dedupeAssignments(rows,range){const seen=new Set(),out=[];for(const a of rows){if(!assignmentOverlaps(a,range))continue;const k=[assignmentEmployeeRef(a),projectRef(a),supervisorRef(a),aStart(a,range),aEnd(a,range)].join('|');if(seen.has(k))continue;seen.add(k);out.push(a);}return out;}
function dedupeEmployeeEvents(rows,aliasMap,type){const seen=new Set(),out=[];for(const r of rows){const rawRef=attendanceEmployeeRef(r),canonical=aliasMap.get(rawRef)||rawRef,date=rowDate(r),shift=S(field(r,['shift_id','shift','period_id','work_period'])),id=S(r.id);const k=type==='attendance'?[canonical,date,shift,timeOnly(field(r,['check_in','in_time','start_time','login_time'])),timeOnly(field(r,['check_out','out_time','end_time','logout_time']))].join('|'):[canonical,date,shift,id||N(field(r,['work_minutes','total_minutes','minutes','actual_minutes']))].join('|');if(seen.has(k))continue;seen.add(k);out.push({...r,__canonical_employee_id:canonical});}return out;}
function supervisorSelection(details){const daysBySupervisor=new Map(),latestBySupervisor=new Map();for(const a of details){const sid=S(a.supervisor_id)||'none';if(!daysBySupervisor.has(sid))daysBySupervisor.set(sid,new Set());for(const d of dateList(a.start,a.end))daysBySupervisor.get(sid).add(d);const prev=latestBySupervisor.get(sid)||'';if(a.start>prev)latestBySupervisor.set(sid,a.start);}const ranked=[...daysBySupervisor.entries()].map(([id,set])=>({id,days:set.size,latest:latestBySupervisor.get(id)||''})).sort((a,b)=>b.days-a.days||b.latest.localeCompare(a.latest));return{primary:ranked[0]?.id==='none'?'':ranked[0]?.id||'',ranked,multiple:ranked.filter(x=>x.id!=='none').length>1,tie:ranked.length>1&&ranked[0].days===ranked[1].days};}
function assignmentEmployeeRef(a){return S(field(a,['worker_id','employee_id','app_worker_id','worker_employee_code','employee_code','worker_code','employee_ts_id']));}
function projectRef(a){return S(field(a,['project_id','assigned_project_id','app_project_id','project_code']));}
function supervisorRef(a){return S(field(a,['supervisor_id','app_supervisor_id','assigned_supervisor_id','supervisor_employee_code','supervisor_code']));}
function aStart(a,r){return iso(field(a,['start_date','assignment_start','assigned_from','from_date','assigned_at']))||r.start;}
function aEnd(a,r){return iso(field(a,['end_date','assignment_end','assigned_to','to_date','unassigned_at']))||r.end;}
function assignmentOverlaps(a,r){if(a.deleted_at||a.is_deleted===true)return false;if(norm(a.status).includes('ملغي')||norm(a.status).includes('محذوف'))return false;const mk=S(a.month_key).slice(0,7);if(mk)return mk===r.month;return aStart(a,r)<=r.end&&aEnd(a,r)>=r.start;}
function dateDiffInclusive(a,b){const x=new Date(a+'T12:00:00'),y=new Date(b+'T12:00:00');return Math.max(0,Math.floor((y-x)/86400000)+1);}
function projectByRef(ref){const r=S(ref);return state.projects.find(p=>[p.id,p.project_id,p.code,p.project_code].map(S).includes(r));}
function projectName(ref){const p=projectByRef(ref);return human(field(p,['name','project_name','title']))||'';}
function projectOperation(ref){const p=projectByRef(ref);return human(field(p,['operation_type','work_type','project_type','type']))||'';}
function isDaily(ref){const n=norm(projectOperation(ref));return n.includes('زياره')||n.includes('زيارة')||n.includes('daily')||n.includes('visit')||n.includes('جزئي');}
function userName(ref){const r=S(ref);const u=state.users.find(x=>[x.id,x.employee_id,x.worker_id,x.employee_code,x.code,x.username].map(S).includes(r));if(u)return human(field(u,['full_name','name','display_name','username']))||'';const e=state.employees.find(x=>employeeAliases(x).includes(r));return e?employeeName(e):'';}
function isExplicitlyStopped(o){if(!o)return false;if(Object.prototype.hasOwnProperty.call(o,'is_active')&&o.is_active===false)return true;if(Object.prototype.hasOwnProperty.call(o,'active')&&o.active===false)return true;return inactiveStatusValue(field(o,['status','employee_status','work_status','account_status']));}
function activeSupervisor(ref){const r=S(ref);if(!r)return null;const e=state.employees.find(x=>employeeAliases(x).includes(r));if(e)return isExplicitlyStopped(e)?null:e;const u=state.users.find(x=>[x.id,x.employee_id,x.worker_id,x.employee_code,x.code,x.username].map(S).includes(r));return u&&!isExplicitlyStopped(u)?u:null;}
function canonicalEmployeeFromRef(ref,aliasMap){const r=S(ref);if(!r)return'';return aliasMap.get(r)||'';}
function assignmentSourceRank(a){const s=S(a.__source_table);if(s==='monthly_distribution')return 1;if(s==='monthly_distribution_view')return 2;if(s==='worker_project_assignments')return 3;return 9;}
function resolveProjectHuman(ref){const p=projectByRef(ref);if(!p||isExplicitlyStopped(p))return null;return p;}
function employeeServiceStart(emp,range){const d=iso(field(emp,['work_start_date','hire_date','joining_date','service_start','start_date']));return d&&d>range.start?d:range.start;}
function employeeServiceEnd(emp,range){const d=iso(field(emp,['work_end_date','termination_date','stop_date','service_end','end_date']));return d&&d<range.end?d:range.end;}
function attendanceEmployeeRef(a){return S(field(a,['worker_id','employee_id','app_worker_id','user_id','employee_code','worker_code','employee_ts_id']));}
function rowDate(a){return iso(field(a,['attendance_date','work_date','record_date','log_date','date','check_in','created_at']));}
function timeOnly(v){if(!v)return'';const s=S(v);if(/^\d{2}:\d{2}/.test(s))return s.slice(0,5);const d=new Date(v);return Number.isNaN(d.getTime())?'':d.toLocaleTimeString('en-GB',{hour:'2-digit',minute:'2-digit',hour12:false});}
function minutesBetween(a,b){if(!a||!b)return 0;const cv=t=>{if(/^\d{2}:\d{2}/.test(t)){const [h,m]=t.split(':').map(Number);return h*60+m;}const d=new Date(t);return Number.isNaN(d.getTime())?null:d.getHours()*60+d.getMinutes();};let x=cv(a),y=cv(b);if(x==null||y==null)return 0;if(y<x)y+=1440;return Math.max(0,y-x);}
function requiredDailyMinutes(project,date){const d=new Date(date+'T12:00:00').getDay();if(d===5){if(project?.friday_is_off===true)return 0;const fm=N(field(project,['friday_required_minutes','friday_minutes','required_friday_minutes']));if(fm)return fm;const fh=N(field(project,['friday_required_hours','friday_hours']));if(fh)return fh*60;}const m=N(field(project,['required_daily_minutes','required_minutes','work_minutes']));if(m)return m;const h=N(field(project,['required_daily_hours','required_hours','daily_hours','work_hours']));if(h)return h*60;return isDaily(field(project,['id']))?180:540;}
function dateList(start,end){const out=[];let d=new Date(start+'T12:00:00'),z=new Date(end+'T12:00:00');while(d<=z){out.push(iso(d));d.setDate(d.getDate()+1);}return out;}
function findEmployeeByRef(ref,aliasMap){const key=aliasMap.get(S(ref));return key?state.employeeMap.get(key)||null:null;}
function financialFor(empId,month){const rows=state.adjustments.filter(x=>S(field(x,['worker_id','employee_id','entity_id']))===S(empId)&&(!x.salary_month||S(x.salary_month).slice(0,7)===month));const sum=types=>rows.filter(x=>types.includes(norm(field(x,['type','adjustment_type','category'])))).reduce((s,x)=>s+N(field(x,['amount','value'])),0);return{allowances:sum(['بدل','بدلات','allowance']),commissions:sum(['عموله','عمولة','حافز','حوافز','commission','bonus']),advances:sum(['سلفه','سلفة','سلف','advance']),deductions:sum(['خصم','خصومات','جزاء','deduction']),rounding:sum(['جبر الكسور','rounding']),rows};}
function approvedOverrides(empId,month){const rows=state.adjustments.filter(x=>S(field(x,['worker_id','employee_id','entity_id']))===S(empId)&&S(x.salary_month||x.month_key).slice(0,7)===month&&(x.is_approved===true||norm(x.status)==='معتمد'));const out={};for(const x of rows){const k=S(field(x,['field_name','target_field']));if(k)out[k]=field(x,['new_value','value','amount']);}return out;}
async function buildUnifiedPayroll(period){
 const range=monthRange(period);
 if(!window.sb)throw new Error('اتصال Supabase غير متاح');
 // لا Cache ولا fallback ولا دمج داخل الواجهة: استجابة واحدة نهائية من RPC السيرفر.
 state.rows=[];state.projects=[];state.assignments=[];state.attendance=[];state.daily=[];state.sources=[];state.diagnostics={};state.incomplete=true;
 const startedAt=Date.now();
 const response=await window.sb.rpc('get_unified_payroll_from_server',{p_month:range.month});
 if(response.error){
   state.sources=[{name:'مصدر الرواتب الموحد',table:'RPC get_unified_payroll_from_server',ok:false,count:0,error:S(response.error.message||response.error),code:S(response.error.code||'')}];
   throw response.error;
 }
 const payload=response.data;
 if(!payload||!Array.isArray(payload.rows))throw new Error('استجابة السيرفر لا تحتوي صفوف رواتب نهائية');
 const rows=payload.rows.map(r=>({
   ...r,
   worker_id:S(r.worker_id||r.canonical_employee_id),
   canonical_employee_id:S(r.canonical_employee_id||r.worker_id),
   supervisor_id:S(r.supervisor_id||''),
   project_ids:Array.isArray(r.project_ids)?r.project_ids.map(S):[],
   project_names:Array.isArray(r.project_names)?r.project_names:[],
   assignment_details:Array.isArray(r.assignment_details)?r.assignment_details:[],
   details:Array.isArray(r.details)?r.details:[],
   issues:Array.isArray(r.issues)?r.issues:[],
   base_salary:N(r.base_salary),allowances:N(r.allowances),gross_salary:N(r.gross_salary),period_salary:N(r.period_salary),
   commissions:N(r.commissions),other_deductions:N(r.other_deductions),absence_deduction:N(r.absence_deduction),deductions:N(r.deductions),
   rounding:N(r.rounding),advances:N(r.advances),net_salary:N(r.net_salary),work_days:N(r.work_days),required_work_days:N(r.required_work_days),
   present_days:N(r.present_days),absent_days:r.absent_days===null?null:N(r.absent_days),due_days:N(r.due_days),required_minutes:N(r.required_minutes),actual_minutes:N(r.actual_minutes)
 }));
 const ids=rows.map(r=>r.canonical_employee_id).filter(Boolean),unique=new Set(ids);
 if(ids.length!==unique.size)throw new Error('Duplicate payroll employees detected');
 const diagnostics={...(payload.diagnostics||{}),client_received_rows:rows.length,request_duration_ms:Date.now()-startedAt,source_function:'public.get_unified_payroll_from_server'};
 if(N(diagnostics.duplicate_ids_found)!==0)throw new Error('Duplicate payroll employees detected');
 if(N(diagnostics.final_payroll_rows)!==N(diagnostics.canonical_employee_ids))throw new Error('Payroll row count does not match canonical employees');
 state.rows=rows;
 state.projects=Array.isArray(payload.projects)?payload.projects:[];
 state.diagnostics=diagnostics;
 state.sources=[{name:'مصدر الرواتب الموحد',table:'RPC public.get_unified_payroll_from_server',ok:true,count:rows.length,error:''}];
 state.incomplete=payload.incomplete===true;
 window.__tasneefPayrollDiagnostics=diagnostics;
 console.table(diagnostics);
 return rows;
}
function currentRows(){let rows=[...state.rows];const sup=S($('salarySupervisor')?.value),proj=S($('salaryProject')?.value),op=S($('salaryOperation')?.value),st=S($('salaryStatus')?.value),q=norm($('salarySearch')?.value);if(sup)rows=rows.filter(r=>r.supervisor_id===sup);if(proj)rows=rows.filter(r=>r.project_ids.includes(proj));if(op)rows=rows.filter(r=>op==='زيارة يومية'?r.assignment_details.some(a=>isDaily(a.project_id)):r.assignment_details.some(a=>!isDaily(a.project_id)));if(st)rows=rows.filter(r=>r.status===st);if(q)rows=rows.filter(r=>norm([r.worker_name,r.residency_name,r.employee_code,r.iqama_number,r.supervisor_name,r.work_location,r.project_names.join(' '),r.notes].join(' ')).includes(q));return rows;}
function fillFilters(){const s=$('salarySupervisor'),p=$('salaryProject');if(s){const old=s.value;s.innerHTML='<option value="">كل المشرفين</option>'+[...new Map(state.rows.map(r=>[r.supervisor_id,r.supervisor_name])).entries()].filter(x=>x[0]).map(([id,n])=>`<option value="${esc(id)}">${esc(n)}</option>`).join('');s.value=old;}if(p){const old=p.value;p.innerHTML='<option value="">كل المشاريع</option>'+state.projects.map(x=>`<option value="${esc(S(x.id))}">${esc(human(field(x,['name','project_name','title']))||'مشروع')}</option>`).join('');p.value=old;}}
function groupRows(rows){const map=new Map();for(const r of rows){let key=r.supervisor_id||`special:${r.job_title}`;let name=r.supervisor_name;if(r.job_title==='مشرف'&&!r.supervisor_id){key='special:supervisors';name='المشرفون';}else if(r.job_title==='فني'&&!r.supervisor_id){key='special:technicians';name='الفنيون';}else if(r.job_title==='إدارة وتشغيل'&&!r.supervisor_id){key='special:operations';name='الإدارة والتشغيل';}else if(!r.supervisor_id){key='special:none';name='دون مشرف';}if(!map.has(key))map.set(key,{key,name,rows:[]});map.get(key).rows.push(r);}return [...map.values()];}
function rowHtml(r,i){const warn=r.issues.length?'salary-row-warning':'';return`<tr class="${warn}"><td>${i+1}</td><td>${esc(r.employee_code)}</td><td>${esc(r.month)}</td><td>${esc(r.residency_name)}</td><td>${esc(r.worker_name)}${r.multiple_supervisors?'<small>متعدد المشرفين</small>':''}</td><td>${esc(r.iqama_number)}</td><td>${esc(r.work_location)}${r.work_location==='متعدد'?'<small>'+esc(r.project_names.join('، '))+'</small>':''}</td><td>${esc(r.job_title)}</td><td>${esc(r.service_start)}</td><td>${esc(r.service_end)}</td><td>${r.work_days}</td><td class="${N(r.absent_days)>0?'salary-red':''}">${r.absent_days===null?'غير محتسب':r.absent_days}</td><td>${r.due_days}</td><td>${money(r.base_salary)}</td><td>${money(r.allowances)}</td><td>${money(r.gross_salary)}</td><td>${money(r.period_salary)}</td><td>${money(r.commissions)}</td><td class="${N(r.deductions)>0?'salary-red':''}">${money(r.deductions)}</td><td>${money(r.rounding)}</td><td>${money(r.advances)}</td><td>${money(r.net_salary)}</td><td>${esc(r.notes||r.issues.join('، '))}</td><td><button class="light" onclick="tasneefSalariesUnifiedV440.details('${esc(r.worker_id)}')">تفاصيل</button> <button class="light" onclick="tasneefSalariesUnifiedV440.edit('${esc(r.worker_id)}')">تعديل</button></td></tr>`;}
function render(){const rows=currentRows(),body=$('salaryBody');if(!body)return;let html='',i=0;for(const g of groupRows(rows)){html+=`<tr class="salary-group-row"><td colspan="24"><div><b>${esc(g.name)}</b><span>عدد الموظفين: ${g.rows.length}</span><span>صافي المجموعة: ${money(g.rows.reduce((s,r)=>s+r.net_salary,0))}</span></div></td></tr>`;for(const r of g.rows)html+=rowHtml(r,i++);}if(rows.length){const sum=k=>rows.reduce((s,r)=>s+N(r[k]),0);html+=`<tr class="salary-total-row"><td colspan="13">الإجمالي العام</td><td>${money(sum('base_salary'))}</td><td>${money(sum('allowances'))}</td><td>${money(sum('gross_salary'))}</td><td>${money(sum('period_salary'))}</td><td>${money(sum('commissions'))}</td><td>${money(sum('deductions'))}</td><td>${money(sum('rounding'))}</td><td>${money(sum('advances'))}</td><td>${money(sum('net_salary'))}</td><td colspan="2"></td></tr>`;}body.innerHTML=html||'<tr><td colspan="24">لا توجد بيانات مستحقة خلال الفترة.</td></tr>';const sum=k=>rows.reduce((s,r)=>s+N(r[k]),0),kp=$('salaryKpis');if(kp)kp.innerHTML=`<div class="kpi"><small>الموظفون</small><b>${rows.length}</b></div><div class="kpi"><small>الأساسي</small><b>${money(sum('base_salary'))}</b></div><div class="kpi"><small>البدلات</small><b>${money(sum('allowances'))}</b></div><div class="kpi"><small>الخصومات والسلف</small><b>${money(sum('deductions')+sum('advances'))}</b></div><div class="kpi"><small>الصافي</small><b>${money(sum('net_salary'))}</b></div><div class="kpi"><small>تحتاج مراجعة</small><b>${rows.filter(r=>r.issues.length).length}</b></div>`;const w=$('salaryDataWarning');if(w){w.style.display=state.incomplete?'block':'none';w.textContent=state.incomplete?'بيانات ناقصة — لا يمكن اعتماد الاحتساب. راجع تفاصيل المصادر.':'';}const a=$('salaryApproveBtn'),pr=$('salaryPrintBtn');if(a)a.disabled=state.incomplete;if(pr)pr.disabled=state.incomplete;}
async function load(){if(state.loading)return;state.loading=true;try{const month=$('salaryMonth')?.value||today().slice(0,7);msg('جاري بناء كشف الرواتب من المصدر الموحد...');await buildUnifiedPayroll(month);fillFilters();render();if(state.incomplete)msg('تم إيقاف الاعتماد لأن مصدرًا أساسيًا لم يكتمل.',true);else msg(`تم تحميل ${state.rows.length} موظفًا، ودمج ${state.diagnostics.duplicateRowsRemoved} سجلًا مكررًا.`);}catch(e){state.incomplete=true;msg('تعذر بناء كشف الرواتب: '+S(e.message||e),true);}finally{state.loading=false;}}
function details(id){const r=state.rows.find(x=>x.worker_id===id);if(!r)return;let m=$('payrollDetailModal');if(!m){m=document.createElement('div');m.id='payrollDetailModal';m.className='payroll-modal';document.body.appendChild(m);}m.innerHTML=`<div class="payroll-modal-box"><div class="table-head"><h3>تفاصيل ${esc(r.worker_name)}</h3><button class="light" onclick="this.closest('.payroll-modal').classList.remove('show')">إغلاق</button></div><div class="salary-note">المشرف الأساسي: ${esc(r.supervisor_name)} · مكان العمل: ${esc(r.work_location)}</div><h4>التوزيعات</h4><div class="table-wrap"><table><thead><tr><th>المشروع</th><th>المشرف</th><th>البداية</th><th>النهاية</th><th>الأيام</th><th>التشغيل</th></tr></thead><tbody>${r.assignment_details.map(a=>`<tr><td>${esc(a.project_name||'-')}</td><td>${esc(a.supervisor_name)}</td><td>${a.start}</td><td>${a.end}</td><td>${a.days}</td><td>${esc(a.operation_type)}</td></tr>`).join('')||'<tr><td colspan="6">لا توجد توزيعات</td></tr>'}</tbody></table></div><h4>التفاصيل اليومية</h4><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>المشروع</th><th>المشرف</th><th>الدخول</th><th>الخروج</th><th>المطلوب</th><th>الفعلي</th><th>الحالة</th></tr></thead><tbody>${r.details.map(d=>`<tr><td>${d.date}</td><td>${esc(d.project_name||'-')}</td><td>${esc(d.supervisor_name)}</td><td>${esc(d.check_in||'-')}</td><td>${esc(d.check_out||'-')}</td><td>${d.required_minutes}</td><td>${d.actual_minutes}</td><td>${esc(d.status)}</td></tr>`).join('')}</tbody></table></div></div>`;m.classList.add('show');}
function edit(id){const r=state.rows.find(x=>x.worker_id===id);if(!r)return;let m=$('payrollEditModal');if(!m){m=document.createElement('div');m.id='payrollEditModal';m.className='payroll-modal';document.body.appendChild(m);}m.innerHTML=`<div class="payroll-modal-box" style="max-width:900px;margin:auto"><div class="table-head"><h3>تعديل ${esc(r.worker_name)}</h3><button class="light" onclick="this.closest('.payroll-modal').classList.remove('show')">إغلاق</button></div><div class="filters"><input type="hidden" id="peId" value="${esc(id)}"><div><label>الراتب الأساسي</label><input type="number" id="peBase" value="${r.base_salary}"></div><div><label>البدلات</label><input type="number" id="peAllow" value="${r.allowances}"></div><div><label>الحوافز والعمولة</label><input type="number" id="peComm" value="${r.commissions}"></div><div><label>السلف</label><input type="number" id="peAdv" value="${r.advances}"></div><div><label>الخصومات</label><input type="number" id="peDed" value="${r.other_deductions}"></div><div><label>أيام الاستحقاق</label><input type="number" id="peDays" min="0" max="31" value="${r.due_days}"></div><div><label>مكان العمل المصحح</label><input id="peLocation" value="${esc(r.work_location)}"></div><div><label>المشرف المصحح</label><select id="peSupervisor"><option value="">دون تغيير</option>${[...new Map(state.rows.map(x=>[x.supervisor_id,x.supervisor_name])).entries()].filter(x=>x[0]).map(([x,n])=>`<option value="${esc(x)}">${esc(n)}</option>`).join('')}</select></div><div style="grid-column:1/-1"><label>الملاحظات</label><textarea id="peNotes">${esc(r.notes)}</textarea></div><div style="grid-column:1/-1"><label>سبب التعديل *</label><textarea id="peReason"></textarea></div></div><button onclick="tasneefSalariesUnifiedV440.saveEdit()">حفظ وإعادة الحساب</button></div>`;m.classList.add('show');}
async function saveEdit(){try{const id=S($('peId')?.value),r=state.rows.find(x=>x.worker_id===id);if(!r)throw new Error('الموظف غير موجود');const reason=S($('peReason')?.value);if(!reason)throw new Error('سبب التعديل إلزامي');const old={base_salary:r.base_salary,allowances:r.allowances,commissions:r.commissions,advances:r.advances,other_deductions:r.other_deductions,due_days:r.due_days,work_location:r.work_location,supervisor_id:r.supervisor_id,notes:r.notes};const next={base_salary:N($('peBase')?.value),allowances:N($('peAllow')?.value),commissions:N($('peComm')?.value),advances:N($('peAdv')?.value),other_deductions:N($('peDed')?.value),due_days:N($('peDays')?.value),work_location:human($('peLocation')?.value)||r.work_location,supervisor_id:S($('peSupervisor')?.value)||r.supervisor_id,notes:S($('peNotes')?.value)};const payload=Object.entries(next).map(([field_name,new_value])=>({worker_id:id,salary_month:r.month,field_name,old_value:String(old[field_name]??''),new_value:String(new_value??''),reason,status:'معتمد',is_approved:true,created_at:new Date().toISOString()}));let res=await window.sb.from('payroll_adjustments').insert(payload);if(res.error)throw res.error;Object.assign(r,next);r.supervisor_name=userName(r.supervisor_id)||r.supervisor_name;r.gross_salary=r.base_salary+r.allowances;r.period_salary=r.gross_salary/30*r.due_days;r.absence_deduction=(r.absent_days==null?0:(r.base_salary/30)*N(r.absent_days));r.deductions=r.other_deductions+r.absence_deduction;r.net_salary=r.period_salary+r.commissions+r.rounding-r.deductions-r.advances;render();$('payrollEditModal')?.classList.remove('show');msg('تم حفظ التعديل وتسجيل القيمة السابقة والجديدة.');}catch(e){msg('تعذر حفظ التعديل: '+S(e.message||e),true);}}
function sourceDetails(){let m=$('payrollSourceModal');if(!m){m=document.createElement('div');m.id='payrollSourceModal';m.className='payroll-modal';document.body.appendChild(m);}m.innerHTML=`<div class="payroll-modal-box"><div class="table-head"><h3>مصادر الرواتب والتشخيص</h3><button class="light" onclick="this.closest('.payroll-modal').classList.remove('show')">إغلاق</button></div><pre dir="ltr">${esc(JSON.stringify(state.diagnostics,null,2))}</pre><div class="table-wrap"><table><thead><tr><th>المصدر</th><th>الجدول</th><th>الحالة</th><th>السجلات</th><th>الخطأ</th></tr></thead><tbody>${state.sources.map(s=>`<tr><td>${esc(s.name)}</td><td>${esc(s.table)}</td><td>${s.ok?'نجاح':'فشل'}</td><td>${s.count}</td><td>${esc(s.error||'-')}</td></tr>`).join('')}</tbody></table></div></div>`;m.classList.add('show');}
async function save(approve){if(state.incomplete)return msg('لا يمكن حفظ أو اعتماد كشف ناقص.',true);try{const month=$('salaryMonth')?.value||today().slice(0,7),payload=currentRows().map(r=>({salary_month:month,entity_type:'employee',entity_id:r.worker_id,base_salary:r.base_salary,allowances:r.allowances,commissions:r.commissions,deductions:r.deductions,advances:r.advances,net_salary:r.net_salary,status:approve?'معتمد':'مسودة',is_approved:approve,approved_at:approve?new Date().toISOString():null,notes:r.notes,updated_at:new Date().toISOString()}));const res=await window.sb.from('monthly_salaries').upsert(payload,{onConflict:'salary_month,entity_type,entity_id'});if(res.error)throw res.error;msg(approve?'تم اعتماد الكشف':'تم حفظ المسودة');}catch(e){msg('تعذر الحفظ: '+S(e.message||e),true);}}
function printRows(){if(state.incomplete)return msg('الطباعة النهائية متوقفة حتى تكتمل المصادر.',true);if(printBusy)return;printBusy=true;try{const rows=JSON.parse(JSON.stringify(currentRows())),month=$('salaryMonth')?.value||'';const w=window.open('','_blank');if(!w)throw new Error('المتصفح منع نافذة الطباعة');const sum=k=>rows.reduce((s,r)=>s+N(r[k]),0);w.document.write(`<!doctype html><html dir="rtl"><head><meta charset="utf-8"><style>@page{size:A4 landscape;margin:6mm}body{font-family:Tahoma,Arial}.title{background:#07513f;color:#fff;text-align:center;padding:10px;font-weight:900;font-size:21px}.sub{text-align:center;padding:6px}table{border-collapse:collapse;width:100%;font-size:7px}th,td{border:1px solid #8ba39a;padding:3px;text-align:center;white-space:nowrap}th{background:#1f6f58;color:#fff}thead{display:table-header-group}tr{break-inside:avoid}.total td{background:#cfe8dc;font-weight:900}</style></head><body><div class="title">كشف الرواتب</div><div class="sub">شركة تصنيف لإدارة المرافق - الشهر: ${esc(month)}</div><table><thead><tr><th>#</th><th>الكود</th><th>الاسم</th><th>الإقامة</th><th>المشرف</th><th>مكان العمل</th><th>الوظيفة</th><th>البداية</th><th>النهاية</th><th>الأيام</th><th>الغياب</th><th>الاستحقاق</th><th>الأساسي</th><th>البدلات</th><th>الإجمالي</th><th>راتب الفترة</th><th>العمولة</th><th>الخصومات</th><th>السلف</th><th>الصافي</th><th>ملاحظات</th></tr></thead><tbody>${rows.map((r,i)=>`<tr><td>${i+1}</td><td>${esc(r.employee_code)}</td><td>${esc(r.worker_name)}</td><td>${esc(r.iqama_number)}</td><td>${esc(r.supervisor_name)}</td><td>${esc(r.work_location)}</td><td>${esc(r.job_title)}</td><td>${r.service_start}</td><td>${r.service_end}</td><td>${r.work_days}</td><td>${r.absent_days===null?'غير محتسب':r.absent_days}</td><td>${r.due_days}</td><td>${money(r.base_salary)}</td><td>${money(r.allowances)}</td><td>${money(r.gross_salary)}</td><td>${money(r.period_salary)}</td><td>${money(r.commissions)}</td><td>${money(r.deductions)}</td><td>${money(r.advances)}</td><td>${money(r.net_salary)}</td><td>${esc(r.notes||r.issues.join('، '))}</td></tr>`).join('')}<tr class="total"><td colspan="12">الإجمالي</td><td>${money(sum('base_salary'))}</td><td>${money(sum('allowances'))}</td><td>${money(sum('gross_salary'))}</td><td>${money(sum('period_salary'))}</td><td>${money(sum('commissions'))}</td><td>${money(sum('deductions'))}</td><td>${money(sum('advances'))}</td><td>${money(sum('net_salary'))}</td><td></td></tr></tbody></table><script>window.onload=()=>setTimeout(()=>window.print(),300)<\/script></body></html>`);w.document.close();setTimeout(()=>printBusy=false,1000);}catch(e){printBusy=false;msg('تعذر الطباعة: '+S(e.message||e),true);}}
function csv(){if(state.incomplete)return msg('التصدير متوقف حتى تكتمل المصادر.',true);const X=window.XLSX;if(!X)return msg('مكتبة Excel غير متاحة.',true);const rows=currentRows(),month=$('salaryMonth')?.value||'',headers=['رقم','أيدي الموظف','الشهر','اسم الموظف في الإقامة','اسم الموظف الحركي','رقم الإقامة','المشرف','مكان العمل','الوظيفة','بداية الخدمة','نهاية الخدمة','أيام العمل','أيام الغياب','الأيام المستحقة','قيمة الرواتب الأساسية','البدلات','الإجمالي','إجمالي الراتب على أيام الفترة','العمولات','الخصومات','جبر الكسور','خصم السلف','الصافي','ملاحظات'];const aoa=[['كشف الرواتب'],[`شركة تصنيف لإدارة المرافق - الشهر: ${month}`],headers];rows.forEach((r,i)=>aoa.push([i+1,r.employee_code,r.month,r.residency_name,r.worker_name,r.iqama_number,r.supervisor_name,r.work_location,r.job_title,r.service_start,r.service_end,r.work_days,r.absent_days,r.due_days,r.base_salary,r.allowances,r.gross_salary,r.period_salary,r.commissions,r.deductions,r.rounding,r.advances,r.net_salary,r.notes||r.issues.join('، ')]));const ws=X.utils.aoa_to_sheet(aoa),wb=X.utils.book_new();ws['!rtl']=true;X.utils.book_append_sheet(wb,ws,'مع العمولة');X.writeFile(wb,`كشف_الرواتب_${month}.xlsx`);}
function inject(){let sec=$('salaries');if(!sec){sec=document.createElement('section');sec.id='salaries';sec.className='page hidden';(document.querySelector('main.content')||document.body).appendChild(sec);}sec.innerHTML=`<style>.salary-excel-card{border:1px solid #b6c9c1;border-radius:14px;overflow:hidden;background:#fff}.salary-excel-title{background:#07513f;color:#fff;text-align:center;padding:14px;font-size:24px;font-weight:900}.salary-excel-sub{text-align:center;padding:8px;background:#edf6f2;font-weight:800;color:#07513f}.salary-table-wrap{max-height:650px;overflow:auto}.salary-table{min-width:3000px;border-collapse:collapse}.salary-table th{position:sticky;top:0;z-index:2;background:#1f6f58;color:#fff}.salary-table th,.salary-table td{border:1px solid #9fb4ac;padding:7px;text-align:center;white-space:nowrap}.salary-table tbody tr:nth-child(even):not(.salary-group-row):not(.salary-total-row){background:#f2f8f5}.salary-table td small{display:block;color:#6b7280}.salary-group-row td{background:#dfeee9!important;color:#064537;font-weight:900;text-align:right}.salary-group-row div{display:flex;gap:18px;flex-wrap:wrap}.salary-total-row td{background:#cfe8dc!important;color:#064537;font-weight:900}.salary-row-warning{background:#fff4f4!important}.salary-red{color:#b91c1c;font-weight:900}.salary-actions{display:flex;gap:8px;flex-wrap:wrap}.salary-note{background:#eef8f4;border:1px solid #dce6e2;border-radius:12px;padding:10px}.payroll-modal{position:fixed;inset:0;background:#0008;display:none;z-index:99999;padding:3vh 3vw}.payroll-modal.show{display:block}.payroll-modal-box{background:#fff;border-radius:16px;padding:16px;max-height:94vh;overflow:auto}</style><div class="salary-excel-card"><div class="salary-excel-title">كشف الرواتب</div><div class="salary-excel-sub">شركة تصنيف لإدارة المرافق - الشهر: <span id="salaryTitleMonth">${today().slice(0,7)}</span></div><div class="card" style="border:0;border-radius:0"><div class="table-head"><h2>الرواتب</h2><span class="badge green">${VERSION}</span></div><div class="salary-note">مصدر واحد فقط من السيرفر: public.get_unified_payroll_from_server. لا دمج ولا Cache للرواتب داخل الواجهة.</div><div id="salaryMsg" class="msg hidden"></div><div id="salaryDataWarning" class="msg err" style="display:none"></div><div class="filters"><div><label>الشهر</label><input type="month" id="salaryMonth" value="${today().slice(0,7)}" onchange="salaryTitleMonth.textContent=this.value;tasneefSalariesUnifiedV440.load()"></div><div><label>المشرف</label><select id="salarySupervisor" onchange="tasneefSalariesUnifiedV440.render()"><option value="">كل المشرفين</option></select></div><div><label>المشروع</label><select id="salaryProject" onchange="tasneefSalariesUnifiedV440.render()"><option value="">كل المشاريع</option></select></div><div><label>نوع التشغيل</label><select id="salaryOperation" onchange="tasneefSalariesUnifiedV440.render()"><option value="">الكل</option><option>دائم</option><option>زيارة يومية</option></select></div><div><label>حالة الراتب</label><select id="salaryStatus" onchange="tasneefSalariesUnifiedV440.render()"><option value="">الكل</option><option>مسودة</option><option>يحتاج مراجعة</option><option>بيانات ناقصة</option><option>معتمد</option></select></div><div><label>بحث</label><input id="salarySearch" oninput="tasneefSalariesUnifiedV440.render()" placeholder="الموظف، الكود، الإقامة، المشرف، المشروع"></div></div><div class="salary-actions"><button onclick="tasneefSalariesUnifiedV440.load()">إعادة الاحتساب</button><button class="light" onclick="tasneefSalariesUnifiedV440.save(false)">حفظ مسودة</button><button id="salaryApproveBtn" class="light" onclick="tasneefSalariesUnifiedV440.save(true)">اعتماد الكشف</button><button id="salaryPrintBtn" class="light" onclick="tasneefSalariesUnifiedV440.print()">طباعة</button><button class="light" onclick="tasneefSalariesUnifiedV440.csv()">تصدير Excel</button><button class="light" onclick="tasneefSalariesUnifiedV440.sourceDetails()">التشخيص والمصادر</button></div><div id="salaryKpis" class="kpis small"></div></div><div class="table-wrap salary-table-wrap"><table class="salary-table"><thead><tr><th>رقم</th><th>أيدي الموظف</th><th>الشهر</th><th>اسم الموظف في الإقامة</th><th>اسم الموظف الحركي</th><th>رقم الإقامة</th><th>مكان العمل</th><th>الوظيفة</th><th>بداية الخدمة</th><th>نهاية الخدمة</th><th>أيام العمل</th><th>أيام الغياب</th><th>الأيام المستحقة</th><th>قيمة الرواتب الأساسية</th><th>البدلات</th><th>الإجمالي</th><th>إجمالي الراتب على أيام الفترة</th><th>العمولات</th><th>الخصومات</th><th>جبر الكسور</th><th>خصم السلف</th><th>الصافي</th><th>ملاحظات</th><th>إجراء</th></tr></thead><tbody id="salaryBody"></tbody></table></div></div>`;}
const api={inject,load,buildUnifiedPayroll,render,details,edit,saveEdit,sourceDetails,save,print:printRows,csv};window.tasneefSalariesUnifiedV440=api;
const oldShow=window.showPage;window.showPage=function(page,btn){const x=oldShow?oldShow.apply(this,arguments):undefined;if(page==='salaries')setTimeout(()=>{inject();load();},60);return x;};
document.addEventListener('DOMContentLoaded',()=>setTimeout(inject,500));window.addEventListener('load',()=>setTimeout(inject,800));
})();
