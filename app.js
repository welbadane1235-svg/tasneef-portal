/* TASNEEF BUILD V230 - inventory schema safe + hide warehouse cost info - 2026-05-20 */
/* V305: Loading screen removed + fast mode */
(function(){
  window.__tasneefLoadingV154 = true;
  window.showTasneefLoading = function(){};
  window.hideTasneefLoading = function(){};
  window.withTasneefLoading = async function(promiseOrFn){
    return await (typeof promiseOrFn === 'function' ? promiseOrFn() : promiseOrFn);
  };
  function removeLoading(){
    try{
      document.querySelectorAll('#tasneefSplashV154,#tasneefLoadingV154,.tasneef-splash-v154,.tasneef-loading-v154').forEach(function(x){x.remove();});
      document.documentElement.classList.add('tasneef-fast-v305');
      document.body && document.body.classList.add('tasneef-fast-v305');
    }catch(e){}
  }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded', removeLoading); else removeLoading();
  window.addEventListener('load', removeLoading, {once:true});
  setTimeout(removeLoading, 50);
  setTimeout(removeLoading, 500);
})();

/* V358: lightweight performance guard. No stored data is changed. */
(function(){
  if(window.__tasneefPerfGuardV358) return;
  window.__tasneefPerfGuardV358 = true;
  const nativeSetInterval = window.setInterval.bind(window);
  window.setInterval = function(fn, delay, ...args){
    const ms = Number(delay) || 0;
    return nativeSetInterval(fn, ms > 0 && ms < 5000 ? 5000 : ms, ...args);
  };
})();

// Tasneef HTML App V86 - Contract Services Cloud View Fix
const SUPABASE_URL = "https://zmjdqiswytxlbfgnfjfv.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_ADsAC5MtBCusDgX62c8NaQ_LyyuTPeb";
const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
// V167: expose Supabase client for late-loaded fixes and feature wrappers.
window.sb = sb;
const $ = id => document.getElementById(id);
const today = () => { const d=new Date(); return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0'); };
const nowTime = () => new Date().toTimeString().slice(0,5);
const session = () => JSON.parse(localStorage.getItem('tasneef_user') || 'null');
const setSession = u => localStorage.setItem('tasneef_user', JSON.stringify(u));
const clearSession = () => localStorage.removeItem('tasneef_user');
const fmt = d => d ? new Date(d).toLocaleString('ar-SA') : '-';
const timeOnly = d => d ? new Date(d).toLocaleTimeString('ar-SA',{hour:'2-digit',minute:'2-digit'}) : '-';
const minsToText = m => { m=Number(m||0); const h=Math.floor(m/60), mm=m%60; return `${h}:${String(mm).padStart(2,'0')}`; };
function num(v){ const n = Number(String(v ?? 0).replace(/,/g,'').trim()); return Number.isFinite(n) ? n : 0; }
function safeQtyEn(v){ return num(v).toLocaleString('en-US',{maximumFractionDigits:2}); }
function money(v){ return `${num(v).toLocaleString('ar-SA', {minimumFractionDigits:2, maximumFractionDigits:2})} ر.س`; }
window.num = num;
window.money = money;
const esc = s => String(s ?? '').replace(/[&<>"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
let data = { users:[], supervisors:[], projects:[], workers:[], attendance:[], logs:[], tickets:[], contractServices:[], financeContracts:[], financeInvoices:[], financeReceipts:[], financeExpenses:[], inventoryItems:[], inventoryMovements:[], inventoryRequests:[] };
function msg(text, type='ok'){ const el=$('globalMsg')||$('loginMsg'); if(!el) return; el.className='msg '+(type==='err'?'err':''); el.textContent=text; el.classList.remove('hidden'); setTimeout(()=>el.classList.add('hidden'),4000); }
function playAppSound(type){ try{ const files={checkin:'sounds/checkin.wav', checkout:'sounds/checkout.wav', ticket:'sounds/ticket.wav'}; const src=files[type]; if(!src) return; const a=new Audio(src); a.volume=0.75; a.play().catch(()=>{}); }catch(e){} }
function roleHomeUrl(role){ return ['admin','general_manager','financial_manager','operations_manager','warehouse_manager'].includes(role) ? 'admin.html' : (role==='technician' ? 'technician.html' : 'supervisor.html'); }
function isAdminAreaRole(role){ return ['admin','general_manager','financial_manager','operations_manager','warehouse_manager'].includes(role); }
function requireRole(role){ const u=session(); if(!u){ location.href='index.html'; return null; } if(role==='admin' && isAdminAreaRole(u.role)) return u; if(role && u.role!==role){ location.href = roleHomeUrl(u.role); return null; } return u; }
async function login(){
  const username=$('loginUsername').value.trim(), password=$('loginPassword').value.trim();
  if(!username||!password) return msg('أدخل اسم المستخدم وكلمة المرور','err');
  if(username==='admin' && password==='123456'){
    setSession({id:1,full_name:'مدير النظام',username:'admin',role:'admin',is_active:true});
    location.href='admin.html'; return;
  }
  const {data:u,error}=await sb.from('app_users').select('*').eq('username',username).eq('password',password).eq('is_active',true).maybeSingle();
  if(error||!u) return msg(error?.message || 'بيانات الدخول غير صحيحة','err');
  setSession(u); location.href = roleHomeUrl(u.role);
}
function logout(){ clearSession(); location.href='index.html'; }
async function loadAll(){
  const [users, projects, workers, attendance, logs, tickets] = await Promise.all([
    sb.from('app_users').select('*').order('id'),
    sb.from('projects').select('*').order('id'),
    sb.from('workers').select('*').order('id'),
    sb.from('attendance').select('*').order('attendance_date',{ascending:false}),
    sb.from('time_logs').select('*').order('check_in',{ascending:false}),
    sb.from('tickets').select('*').order('created_at',{ascending:false})
  ]);

  let contractServices = await sb.from('contract_services').select('*').order('id', { ascending: false });

  for(const r of [users,projects,workers,attendance,logs,tickets,contractServices]) if(r.error) console.warn(r.error.message);
  data.users = users.data || [];
  data.supervisors = data.users.filter(u=>u.role==='supervisor' && u.is_active!==false);
  data.technicians = data.users.filter(u=>u.role==='technician' && u.is_active!==false);
  data.projects = projects.data || [];
  data.workers = workers.data || [];
  data.attendance = attendance.data || [];
  data.logs = logs.data || [];
  data.tickets = tickets.data || [];
  data.contractServices = contractServices.data || [];
  data.contractServicesError = contractServices.error ? contractServices.error.message : '';
}
function fillSelect(id, rows, label='name', allLabel=null, value='id'){ const el=$(id); if(!el) return; el.innerHTML = (allLabel!==null?`<option value="">${allLabel}</option>`:'') + rows.map(r=>`<option value="${r[value]}">${esc(r[label]||r.full_name||r.username)}</option>`).join(''); }
function userRoleLabel(role){ return ({admin:'مدير عام',general_manager:'مدير عام',financial_manager:'مدير مالي',operations_manager:'مدير تشغيلي',warehouse_manager:'مدير مخازن',technician:'فني',supervisor:'مشرف'}[role] || role || '-'); }
function approvalRoleForUser(){ const u=session()||{}; return u.role==='general_manager' ? 'general' : (u.role==='admin' ? 'general' : (u.role==='financial_manager' ? 'finance' : (u.role==='operations_manager' ? 'ops' : (u.role==='warehouse_manager' ? 'warehouse' : '')))); }
function supervisorName(id){ return data.users.find(u=>String(u.id)===String(id))?.full_name || data.supervisors.find(u=>String(u.id)===String(id))?.full_name || '-'; }
function projectName(id){ return data.projects.find(p=>String(p.id)===String(id))?.name || '-'; }
function workerName(id){ return data.workers.find(w=>String(w.id)===String(id))?.name || '-'; }
function getProjectSupervisorId(pid){ return data.projects.find(p=>String(p.id)===String(pid))?.supervisor_id || ''; }
function dateTime(date, time){ return date && time ? new Date(`${date}T${time}:00`).toISOString() : null; }
function minutesBetween(a,b){ if(!a||!b) return 0; return Math.max(0, Math.round((new Date(b)-new Date(a))/60000)); }
function logActualMinutes(l){ const saved = Number(l.duration_minutes); if(Number.isFinite(saved) && saved > 0) return saved; return minutesBetween(l.check_in, l.check_out); }
function logRequiredMinutes(l){ const logDate = l.log_date || String(l.check_in||'').slice(0,10); const current = l.project_id ? requiredMinutesForLog(l.project_id, logDate) : 0; if(current > 0) return current; const saved = Number(l.required_minutes); if(Number.isFinite(saved) && saved > 0) return saved; return 0; }
async function initAdmin(){ requireRole('admin'); await refreshAll(); }
async function refreshAll(){ await loadAll(); hydrateForms(); renderAll(); }
function hydrateForms(){
  const sups = data.supervisors; const pros = data.projects; const workers = data.workers;
  ['logSupervisor','dailySupervisor','projectSupervisor','projectFilterSupervisor','projectManageSupervisor','workerSupervisor','workerFilterSupervisor','attendanceSupervisor','attendanceFilterSupervisor','ticketSupervisor','monthlySupervisor'].forEach(id=>fillSelect(id,sups,'full_name','الكل'));
  fillSelect('manageWorkerSelect', workers, 'name', 'اختر العامل');
  ['logProject','dailyProject','attendanceProject','ticketProject','workerProject','workerFilterProject'].forEach(id=>fillSelect(id,pros,'name','الكل'));
  fillSelect('projectSupervisor',sups,'full_name','بدون مشرف'); fillSelect('workerSupervisor',sups,'full_name','بدون مشرف'); fillSelect('attendanceWorker',workers,'name','اختر العامل');
  fillSelect('serviceFilterProject', pros, 'name', 'كل المشاريع');
  fillSelect('serviceFilterSupervisor', sups, 'full_name', 'كل المشرفين');
  hydrateServiceTypes();
  financeHydrateForms();
  if($('logDate')&&!$('logDate').value) $('logDate').value=today(); if($('dailyDate')&&!$('dailyDate').value) $('dailyDate').value=today(); if($('attendanceDate')&&!$('attendanceDate').value) $('attendanceDate').value=today(); if($('attendanceFilterDate')&&!$('attendanceFilterDate').value) $('attendanceFilterDate').value=today(); if($('monthlyMonth')&&!$('monthlyMonth').value) $('monthlyMonth').value=today().slice(0,7);
}
function renderAll(){ renderDashboard(); renderTimeLogs(); renderUsers(); renderProjects(); renderWorkers(); renderAttendance(); renderMonthly(); renderTickets(); renderAlerts(); renderContractServices(); }
function showPage(id, btn){ document.querySelectorAll('.page').forEach(p=>p.classList.add('hidden')); $(id)?.classList.remove('hidden'); document.querySelectorAll('.nav').forEach(n=>n.classList.remove('active')); btn?.classList.add('active'); renderAll(); if(id==='contracts') showContractsSubTab('services'); }
function renderDashboard(){ if(!$('kpiUsers')) return; $('kpiUsers').textContent=data.users.length; $('kpiProjects').textContent=data.projects.length; $('kpiWorkers').textContent=data.workers.length; $('kpiTodayLogs').textContent=data.logs.filter(l=>String(l.visit_type||'')!=='technician_attendance' && (l.log_date||String(l.check_in||'').slice(0,10))===today()).length; const div=$('todaySummary'); if(div) div.innerHTML = data.supervisors.map(s=>{ const logs=data.logs.filter(l=>String(l.supervisor_id)===String(s.id)&&(l.log_date||String(l.check_in||'').slice(0,10))===today()); const mins=logs.reduce((a,l)=>a+(l.duration_minutes||minutesBetween(l.check_in,l.check_out)),0); return `<div class="summary-item"><b>${esc(s.full_name)}</b><br>عدد التسجيلات: ${logs.length}<br>إجمالي الوقت: ${minsToText(mins)}</div>`; }).join('') || '<div class="summary-item">لا توجد تسجيلات اليوم</div>'; }
function setNow(id){ $(id).value=nowTime(); }
function clearLogForm(){ ['logId','logIn','logOut','logTravel','logNotes'].forEach(id=>{ if($(id)) $(id).value=id==='logTravel'?'0':''; }); if($('logDate')) $('logDate').value=today(); if($('logVisitType')) $('logVisitType').value='surface'; $('logFormTitle') && ($('logFormTitle').textContent='تسجيل دخول / خروج'); }
function findProject(id){ return data.projects.find(p=>String(p.id)===String(id)); }
function requiredMinutesForLog(projectId, dateStr){ const p=findProject(projectId); if(!p) return 0; return projectRequiredMinutes(p, dateStr); }
function calculateTravelMinutes(supervisorId, dateStr, checkInIso, currentId=null){
  if(!supervisorId || !dateStr || !checkInIso) return 0;
  const checkIn = new Date(checkInIso);
  const previous = data.logs
    .filter(l => String(l.supervisor_id)===String(supervisorId)
      && String(l.id)!==String(currentId||'')
      && (l.log_date || String(l.check_in||'').slice(0,10))===dateStr
      && l.check_out
      && new Date(l.check_out) <= checkIn)
    .sort((a,b)=> new Date(b.check_out) - new Date(a.check_out))[0];
  if(!previous) return 0;
  return Math.max(0, Math.round((checkIn - new Date(previous.check_out))/60000));
}
function monthDays(year, monthIndex){ return new Date(year, monthIndex+1, 0).getDate(); }
function projectRequiredMonthlyMinutes(projectId, monthStr){
  const p=findProject(projectId); if(!p || !monthStr) return 0;
  if((p.operation_type||'')==='as_needed') return 0;
  const parts=monthStr.split('-').map(Number), y=parts[0], m=parts[1];
  let total=0; const days=monthDays(y,m-1);
  for(let d=1; d<=days; d++){
    const ds=y+'-'+String(m).padStart(2,'0')+'-'+String(d).padStart(2,'0');
    total += projectRequiredMinutes(p, ds);
  }
  return total;
}
function performanceStatus(percent, required){
  percent = Number(percent || 0);
  if(!required) return {text:'غير محدد', cls:'amber'};

  // 90% إلى 110% = ممتاز
  // 70% إلى 89% أو 111% إلى 140% = جيد
  // أقل من 70% أو أكثر من 140% = ضعيف
  if(percent >= 90 && percent <= 110) return {text:'ممتاز', cls:'green'};
  if((percent >= 70 && percent < 90) || (percent > 110 && percent <= 140)) return {text:'جيد', cls:'amber'};
  return {text:'ضعيف', cls:'red'};
}
function percentText(v){ return (Math.round(Number(v||0)*10)/10).toFixed(1)+'%'; }
function calcTimeStatus(actualMinutes, requiredMinutes){ const diff = Number(actualMinutes||0) - Number(requiredMinutes||0); if(!requiredMinutes) return {diff, status:'unknown', text:'غير محدد', cls:'amber'}; if(diff > 5) return {diff, status:'over_time', text:'زيادة', cls:'red'}; if(diff < -5) return {diff, status:'under_time', text:'ناقص', cls:'amber'}; return {diff, status:'within_time', text:'ضمن الوقت', cls:'green'}; }
function timeStatusText(s){ return s==='over_time'?'زيادة':(s==='under_time'?'ناقص':(s==='within_time'?'ضمن الوقت':'غير محدد')); }
function timeStatusClass(s){ return s==='over_time'?'red':(s==='under_time'?'amber':(s==='within_time'?'green':'amber')); }
function diffText(m){ m=Number(m||0); return (m>0?'+':'') + m + ' دقيقة'; }
function logDateText(l){ return l.log_date || String(l.check_in || l.created_at || today()).slice(0,10); }
function logWorkersNames(l){
  const pid=String(l.project_id||''), sid=String(l.supervisor_id||'');
  const rows=(data.workers||[]).filter(w =>
    (pid && String(workerProjectId(w)||'')===pid) ||
    (!pid && sid && String(workerSupId(w)||'')===sid)
  ).filter(w => w.status !== 'inactive');
  return rows.map(w=>w.name).filter(Boolean).join('، ') || '-';
}
function buildLogWhatsAppMessage(l, type){
  const isOut = type==='out' || !!l.check_out;
  return [
    isOut ? 'تم تسجيل خروج' : 'تم تسجيل دخول',
    '',
    'اسم المشروع: ' + projectName(l.project_id),
    'نوع التنظيف: ' + visitTypeText(l.visit_type),
    'اسم العمال: ' + logWorkersNames(l),
    'التاريخ: ' + logDateText(l),
    'الوقت: ' + (isOut ? timeOnly(l.check_out) : timeOnly(l.check_in))
  ].join('\n');
}
function copyWhatsappText(text){
  try{ if(navigator.clipboard && navigator.clipboard.writeText) return navigator.clipboard.writeText(text).catch(()=>{}); }catch(e){}
  try{ const ta=document.createElement('textarea'); ta.value=text; ta.style.position='fixed'; ta.style.opacity='0'; document.body.appendChild(ta); ta.select(); document.execCommand('copy'); ta.remove(); }catch(e){}
  return Promise.resolve();
}
function openWhatsappText(text){ window.open('https://wa.me/?text=' + encodeURIComponent(text), '_blank'); }
window.sendLogWhatsapp = function(id, type){
  const l=(data.logs||[]).find(x=>String(x.id)===String(id));
  if(!l) return msg('لم يتم العثور على سجل الدخول والخروج','err');
  const msgText=buildLogWhatsAppMessage(l, type || (l.check_out?'out':'in'));
  copyWhatsappText(msgText).finally(()=>{ openWhatsappText(msgText); msg('تم تجهيز رسالة الواتساب'); });
};
function sendLogWhatsappFromRow(row, type){
  if(!row) return;
  const msgText=buildLogWhatsAppMessage(row, type || (row.check_out?'out':'in'));
  copyWhatsappText(msgText).finally(()=>openWhatsappText(msgText));
}
function logWhatsappButtons(l){
  if(l.check_out){ return `<button class="small" style="background:#128C7E;color:#fff" onclick="sendLogWhatsapp(${l.id},'out')">واتساب خروج</button>`; }
  return `<button class="small" style="background:#128C7E;color:#fff" onclick="sendLogWhatsapp(${l.id},'in')">واتساب دخول</button>`;
}
function onLogProjectChange(){ const pid=$('logProject')?.value; const p=findProject(pid); if(p && $('logVisitType')) $('logVisitType').value=p.visit_type_default||'surface'; if(p && $('logSupervisor') && !$('logSupervisor').value) $('logSupervisor').value=p.supervisor_id||''; }
async function saveTimeLog(){ const u=session(); const id=$('logId')?.value; const date=$('logDate')?.value || today(); let sup=$('logSupervisor')?.value || (u.role==='supervisor'?u.id:''); const project=$('logProject')?.value; if(!sup && project) sup=getProjectSupervisorId(project); const check_in=dateTime(date,$('logIn')?.value), check_out=dateTime(date,$('logOut')?.value); if(!project||!check_in) return msg('المشروع ووقت الدخول مطلوبان','err'); const actual=minutesBetween(check_in,check_out); const required=requiredMinutesForLog(project,date); const ts=calcTimeStatus(actual,required); const autoTravel=calculateTravelMinutes(sup,date,check_in,id); const row={user_id:u.id, supervisor_id:Number(sup)||null, project_id:Number(project), check_in, check_out, log_date:date, duration_minutes:actual, travel_minutes:autoTravel, visit_type:$('logVisitType')?.value||findProject(project)?.visit_type_default||'surface', required_minutes:required, time_difference_minutes:ts.diff, time_status:ts.status, notes:$('logNotes')?.value||''}; let res = id ? await sb.from('time_logs').update(row).eq('id',id).select('*').maybeSingle() : await sb.from('time_logs').insert(row).select('*').single(); if(res.error) return msg(res.error.message,'err'); const savedRow = res.data ? res.data : Object.assign({id:Number(id)||0}, row); playAppSound(check_out ? 'checkout' : 'checkin'); msg('تم حفظ التسجيل وحساب حالة الوقت ووقت التنقل تلقائياً'); sendLogWhatsappFromRow(savedRow, check_out ? 'out' : 'in'); clearLogForm(); await refreshAll(); }
function filterLogs(){ let rows=[...data.logs].filter(l=>String(l.visit_type||'')!=='technician_attendance'); const d=$('dailyDate')?.value, s=$('dailySupervisor')?.value, p=$('dailyProject')?.value, q=($('dailySearch')?.value||'').trim(); if(d) rows=rows.filter(l=>(l.log_date||String(l.check_in||'').slice(0,10))===d); if(s) rows=rows.filter(l=>String(l.supervisor_id)===String(s)); if(p) rows=rows.filter(l=>String(l.project_id)===String(p)); if(q) rows=rows.filter(l=>[supervisorName(l.supervisor_id),projectName(l.project_id),visitTypeText(l.visit_type),timeStatusText(l.time_status),l.notes].join(' ').includes(q)); return rows; }
function renderTimeLogs(){ const body=$('logsBody'); if(!body) return; const isSupervisorPage = !document.getElementById('daily'); const rows=filterLogs(); body.innerHTML = rows.map(l=>{ const logDate=l.log_date||String(l.check_in||'').slice(0,10); const actual=Number(l.duration_minutes||minutesBetween(l.check_in,l.check_out)); const required=logRequiredMinutes(l); const diff=(l.time_difference_minutes!==null&&l.time_difference_minutes!==undefined)?Number(l.time_difference_minutes):(actual-required); const status=l.time_status||calcTimeStatus(actual,required).status; const badge=`<span class="badge ${timeStatusClass(status)}">${timeStatusText(status)}</span>`; if(isSupervisorPage){ return `<tr><td>${esc(projectName(l.project_id))}</td><td>${visitTypeText(l.visit_type)}</td><td>${timeOnly(l.check_in)}</td><td>${timeOnly(l.check_out)}</td><td>${minsToText(required)}</td><td>${minsToText(actual)}</td><td>${badge}</td><td class="row-actions">${logWhatsappButtons(l)}</td></tr>`; } return `<tr><td>${esc(logDate)}</td><td>${esc(supervisorName(l.supervisor_id))}</td><td>${esc(projectName(l.project_id))}</td><td>${visitTypeText(l.visit_type)}</td><td>${timeOnly(l.check_in)}</td><td>${timeOnly(l.check_out)}</td><td>${minsToText(required)}</td><td>${minsToText(actual)}</td><td>${diffText(diff)}</td><td>${badge}</td><td>${l.travel_minutes||0}</td><td>${esc(l.notes)}</td><td class="row-actions">${logWhatsappButtons(l)}</td><td class="row-actions"><button onclick="editTimeLog(${l.id})">تعديل</button><button class="danger" onclick="deleteRow('time_logs',${l.id})">حذف</button></td></tr>`; }).join('') || (isSupervisorPage?'<tr><td colspan="8">لا توجد بيانات</td></tr>':'<tr><td colspan="14">لا توجد بيانات</td></tr>'); }
function editTimeLog(id){ const l=data.logs.find(x=>x.id===id); if(!l) return; $('logId').value=l.id; $('logDate').value=l.log_date||String(l.check_in||'').slice(0,10); if($('logSupervisor')) $('logSupervisor').value=l.supervisor_id||''; $('logProject').value=l.project_id||''; if($('logVisitType')) $('logVisitType').value=l.visit_type||findProject(l.project_id)?.visit_type_default||'surface'; $('logIn').value=l.check_in?new Date(l.check_in).toTimeString().slice(0,5):''; $('logOut').value=l.check_out?new Date(l.check_out).toTimeString().slice(0,5):''; $('logTravel').value=l.travel_minutes||0; $('logNotes').value=l.notes||''; $('logFormTitle') && ($('logFormTitle').textContent='تعديل تسجيل'); window.scrollTo({top:0,behavior:'smooth'}); }
async function deleteRow(table,id){ if(!confirm('تأكيد الحذف؟')) return; const {error}=await sb.from(table).delete().eq('id',id); if(error) return msg(error.message,'err'); msg('تم الحذف'); await refreshAll(); }
function clearUserForm(){ ['userId','userFullName','userUsername','userPassword'].forEach(id=>$(id)&&($(id).value='')); if($('userRole')) $('userRole').value='supervisor'; if($('userActive')) $('userActive').value='true'; $('userFormTitle')&&($('userFormTitle').textContent='إضافة مستخدم'); }
async function saveUser(){ const id=$('userId').value; const row={full_name:$('userFullName').value.trim(), username:$('userUsername').value.trim(), password:$('userPassword').value.trim()||'123456', role:$('userRole').value, is_active:$('userActive').value==='true'}; if(!row.full_name||!row.username) return msg('الاسم واسم المستخدم مطلوبان','err'); const res=id?await sb.from('app_users').update(row).eq('id',id):await sb.from('app_users').insert(row); if(res.error) return msg(res.error.message,'err'); msg('تم حفظ المستخدم'); clearUserForm(); await refreshAll(); }
function renderUsers(){ const b=$('usersBody'); if(!b) return; b.innerHTML=data.users.map(u=>`<tr><td>${esc(u.full_name)}</td><td>${esc(u.username)}</td><td><span class="badge">${esc(userRoleLabel(u.role))}</span></td><td><span class="badge ${u.is_active?'green':'red'}">${u.is_active?'نشط':'موقوف'}</span></td><td class="row-actions"><button onclick="editUser(${u.id})">تعديل</button><button class="danger" onclick="deleteRow('app_users',${u.id})">حذف</button></td></tr>`).join(''); }
function editUser(id){ const u=data.users.find(x=>x.id===id); if(!u)return; $('userId').value=u.id; $('userFullName').value=u.full_name||''; $('userUsername').value=u.username||''; $('userPassword').value=u.password||''; $('userRole').value=u.role; $('userActive').value=String(u.is_active!==false); $('userFormTitle').textContent='تعديل مستخدم'; }
function projectOperationText(t){ return t==='full_time'?'دوام كامل':(t==='as_needed'?'حسب الحاجة':'زيارة يومية'); }
function visitTypeText(t){ return t==='deep'?'نظافة عميقة':'نظافة سطحية'; }
function projectRequiredMinutes(p, dateStr){ const day=dateStr?new Date(dateStr+'T00:00:00').getDay():null; return day===5?Number(p.friday_minutes??90):Number(p.required_daily_minutes??180); }
function clearProjectForm(){ ['projectId','projectName','projectLocation','projectNotes'].forEach(id=>$(id)&&($(id).value='')); if($('projectSupervisor')) $('projectSupervisor').value=''; if($('projectStatus')) $('projectStatus').value='active'; if($('projectRequiredDaily')) $('projectRequiredDaily').value=180; if($('projectFridayMinutes')) $('projectFridayMinutes').value=90; if($('projectOperationType')) $('projectOperationType').value='daily_visit'; if($('projectVisitDefault')) $('projectVisitDefault').value='surface'; $('projectFormTitle')&&($('projectFormTitle').textContent='إضافة مشروع'); $('projectSaveBtn')&&($('projectSaveBtn').textContent='حفظ المشروع'); }
async function saveProject(){ const id=$('projectId').value; const row={name:$('projectName').value.trim(), location:$('projectLocation').value.trim(), supervisor_id:Number($('projectSupervisor').value)||null, required_daily_minutes:Number($('projectRequiredDaily')?.value||180), friday_minutes:Number($('projectFridayMinutes')?.value||90), operation_type:$('projectOperationType')?.value||'daily_visit', visit_type_default:$('projectVisitDefault')?.value||'surface', status:$('projectStatus').value, notes:$('projectNotes').value}; if(!row.name) return msg('اسم المشروع مطلوب','err'); const res=id?await sb.from('projects').update(row).eq('id',id):await sb.from('projects').insert(row); if(res.error) return msg(res.error.message,'err'); msg(id?'تم تحديث المشروع':'تم حفظ المشروع'); clearProjectForm(); await refreshAll(); }
function renderProjects(){ const b=$('projectsBody'); if(!b) return; const q=($('projectSearch')?.value||'').trim(), sid=$('projectFilterSupervisor')?.value, st=$('projectFilterStatus')?.value; let rows=data.projects; if(q) rows=rows.filter(p=>[p.name,p.location,supervisorName(p.supervisor_id),p.notes].join(' ').includes(q)); if(sid) rows=rows.filter(p=>String(p.supervisor_id)===String(sid)); if(st) rows=rows.filter(p=>(p.status||'active')===st); b.innerHTML=rows.map(p=>`<tr><td><b>${esc(p.name)}</b><br><small>${esc(p.location||'')}</small></td><td>${esc(supervisorName(p.supervisor_id))}</td><td>${minsToText(p.required_daily_minutes??180)}</td><td>${minsToText(p.friday_minutes??90)}</td><td>${projectOperationText(p.operation_type)}</td><td>${visitTypeText(p.visit_type_default)}</td><td><span class="badge ${p.status==='inactive'?'red':'green'}">${p.status==='inactive'?'متوقف':'نشط'}</span></td><td class="row-actions"><button onclick="editProject(${p.id})">تعديل</button><button class="light" onclick="openProjectManager(${p.id})">إدارة المشروع</button><button class="light" onclick="toggleProjectStatus(${p.id})">${p.status==='inactive'?'تفعيل':'إيقاف'}</button><button class="danger" onclick="deleteRow('projects',${p.id})">حذف</button></td></tr>`).join('')||'<tr><td colspan="8">لا توجد بيانات</td></tr>'; renderProjectManager(); }
function editProject(id){ const p=data.projects.find(x=>x.id===id); if(!p)return; $('projectId').value=p.id; $('projectName').value=p.name||''; $('projectLocation').value=p.location||''; $('projectSupervisor').value=p.supervisor_id||''; $('projectStatus').value=p.status||'active'; $('projectNotes').value=p.notes||''; if($('projectRequiredDaily')) $('projectRequiredDaily').value=p.required_daily_minutes??180; if($('projectFridayMinutes')) $('projectFridayMinutes').value=p.friday_minutes??90; if($('projectOperationType')) $('projectOperationType').value=p.operation_type||'daily_visit'; if($('projectVisitDefault')) $('projectVisitDefault').value=p.visit_type_default||'surface'; $('projectFormTitle').textContent='تعديل مشروع'; $('projectSaveBtn')&&($('projectSaveBtn').textContent='تحديث المشروع'); }
async function toggleProjectStatus(id){ const p=data.projects.find(x=>x.id===id); if(!p)return; const next=p.status==='inactive'?'active':'inactive'; const {error}=await sb.from('projects').update({status:next}).eq('id',id); if(error) return msg(error.message,'err'); msg(next==='active'?'تم تفعيل المشروع':'تم إيقاف المشروع'); await refreshAll(); }
function openProjectManager(id){ const p=data.projects.find(x=>x.id===id); if(!p)return; $('manageProjectId').value=id; $('projectManagerCard')?.classList.remove('hidden'); $('projectManagerTitle').textContent=`إدارة المشروع: ${p.name}`; if($('projectManageSupervisor')) $('projectManageSupervisor').value=p.supervisor_id||''; renderProjectManager(); setTimeout(()=>$('projectManagerCard')?.scrollIntoView({behavior:'smooth',block:'start'}),50); }
function closeProjectManager(){ $('projectManagerCard')?.classList.add('hidden'); if($('manageProjectId')) $('manageProjectId').value=''; }
function renderProjectManager(){ const b=$('projectWorkersBody'); if(!b) return; const pid=$('manageProjectId')?.value; if(!pid){ b.innerHTML='<tr><td colspan="5">اختر مشروع من زر إدارة المشروع</td></tr>'; return; } const rows=data.workers.filter(w=>String(workerProjectId(w))===String(pid)); b.innerHTML=rows.map(w=>`<tr><td>${esc(w.name)}</td><td>${esc(supervisorName(workerSupId(w)))}</td><td><span class="badge ${w.worker_type==='support'?'amber':'green'}">${workerTypeText(w.worker_type)}</span></td><td><span class="badge ${w.status==='inactive'?'red':'green'}">${w.status==='inactive'?'موقوف':'نشط'}</span></td><td class="row-actions"><button class="danger" onclick="removeWorkerFromProject(${w.id})">إزالة من المشروع</button></td></tr>`).join('')||'<tr><td colspan="5">لا يوجد عمال مرتبطون بهذا المشروع</td></tr>'; }
async function saveProjectManagerSupervisor(){ const pid=Number($('manageProjectId')?.value), sid=Number($('projectManageSupervisor')?.value)||null; if(!pid) return msg('اختر المشروع أولاً','err'); const {error}=await sb.from('projects').update({supervisor_id:sid}).eq('id',pid); if(error) return msg(error.message,'err'); await sb.from('workers').update({supervisor_id:sid, app_supervisor_id:sid}).eq('project_id',pid); msg('تم ربط المشرف بالمشروع وتحديث عمال المشروع'); await refreshAll(); openProjectManager(pid); }
async function addExistingWorkerToProject(){ const pid=Number($('manageProjectId')?.value), wid=Number($('manageWorkerSelect')?.value), type=$('manageWorkerType')?.value||'primary'; if(!pid||!wid) return msg('اختر المشروع والعامل','err'); const p=data.projects.find(x=>x.id===pid); const sid=p?.supervisor_id||null; const {error}=await sb.from('workers').update({project_id:pid, supervisor_id:sid, app_supervisor_id:sid, worker_type:type}).eq('id',wid); if(error) return msg(error.message,'err'); msg('تم ربط العامل بالمشروع'); await refreshAll(); openProjectManager(pid); }
async function removeWorkerFromProject(wid){ if(!confirm('إزالة العامل من هذا المشروع؟')) return; const pid=Number($('manageProjectId')?.value); const {error}=await sb.from('workers').update({project_id:null}).eq('id',wid); if(error) return msg(error.message,'err'); msg('تمت إزالة العامل من المشروع'); await refreshAll(); if(pid) openProjectManager(pid); }
async function addWorkerInsideProject(){ const pid=Number($('manageProjectId')?.value); if(!pid) return msg('اختر المشروع أولاً','err'); const name=$('manageNewWorkerName')?.value.trim(); if(!name) return msg('اسم العامل مطلوب','err'); const p=data.projects.find(x=>x.id===pid); const sid=p?.supervisor_id||null; const row={name, phone:$('manageNewWorkerPhone')?.value.trim()||'', salary:Number($('manageNewWorkerSalary')?.value||1500), supervisor_id:sid, app_supervisor_id:sid, project_id:pid, worker_type:$('manageNewWorkerType')?.value||'primary', status:'active'}; const {error}=await sb.from('workers').insert(row); if(error) return msg(error.message,'err'); ['manageNewWorkerName','manageNewWorkerPhone'].forEach(id=>$(id)&&($(id).value='')); if($('manageNewWorkerSalary')) $('manageNewWorkerSalary').value=1500; msg('تم إضافة العامل وربطه بالمشروع'); await refreshAll(); openProjectManager(pid); }
function clearWorkerForm(){
  ['workerId','workerName','workerPhone','workerNotes'].forEach(id=>$(id)&&($(id).value=''));
  if($('workerSalary')) $('workerSalary').value=1500;
  if($('workerSupervisor')) $('workerSupervisor').value='';
  if($('workerProject')) $('workerProject').value='';
  if($('workerType')) $('workerType').value='primary';
  if($('workerStatus')) $('workerStatus').value='active';
  $('workerFormTitle')&&($('workerFormTitle').textContent='إضافة عامل');
  $('workerSaveBtn')&&($('workerSaveBtn').textContent='حفظ العامل');
  $('workerCancelBtn')&&$('workerCancelBtn').classList.add('hidden');
}
function workerSupId(w){ return w.app_supervisor_id || w.supervisor_id; }
function workerProjectId(w){ return w.project_id || w.assigned_project_id || ''; }
function workerTypeText(t){ return t==='support' ? 'بديل / مساند' : 'أساسي'; }
function onWorkerSupervisorChange(){
  const sid=$('workerSupervisor')?.value;
  const el=$('workerProject');
  if(!el) return;
  let rows=data.projects;
  if(sid) rows=rows.filter(p=>String(p.supervisor_id)===String(sid));
  fillSelect('workerProject', rows, 'name', 'اختر المشروع');
}
async function saveWorker(){
  const id=$('workerId').value;
  const supId=Number($('workerSupervisor').value)||null;
  const projectId=Number($('workerProject')?.value)||null;
  const row={
    name:$('workerName').value.trim(),
    phone:$('workerPhone').value.trim(),
    salary:Number($('workerSalary').value||1500),
    supervisor_id:supId,
    app_supervisor_id:supId,
    project_id:projectId,
    worker_type:$('workerType')?.value||'primary',
    status:$('workerStatus').value,
    notes:$('workerNotes').value
  };
  if(!row.name) return msg('اسم العامل مطلوب','err');
  if(!row.supervisor_id) return msg('اختر المشرف المسؤول عن العامل','err');
  const res=id?await sb.from('workers').update(row).eq('id',id):await sb.from('workers').insert(row);
  if(res.error) return msg(res.error.message,'err');
  msg(id?'تم تحديث العامل':'تم حفظ العامل');
  clearWorkerForm();
  await refreshAll();
}
function renderWorkers(){
  const b=$('workersBody'); if(!b) return;
  const s=$('workerFilterSupervisor')?.value, p=$('workerFilterProject')?.value, st=$('workerFilterStatus')?.value, tp=$('workerFilterType')?.value, q=($('workerSearch')?.value||'').trim();
  let rows=data.workers;
  if(s) rows=rows.filter(w=>String(workerSupId(w))===String(s));
  if(p) rows=rows.filter(w=>String(workerProjectId(w))===String(p));
  if(st) rows=rows.filter(w=>(w.status||'active')===st);
  if(tp) rows=rows.filter(w=>(w.worker_type||'primary')===tp);
  if(q) rows=rows.filter(w=>[w.name,w.phone,supervisorName(workerSupId(w)),projectName(workerProjectId(w)),w.notes].join(' ').includes(q));
  b.innerHTML=rows.map(w=>`<tr><td>${esc(w.name)}</td><td>${esc(supervisorName(workerSupId(w)))}</td><td>${esc(projectName(workerProjectId(w)))}</td><td><span class="badge ${w.worker_type==='support'?'amber':'green'}">${workerTypeText(w.worker_type)}</span></td><td>${esc(w.phone)}</td><td>${w.salary||0}</td><td><span class="badge ${w.status==='inactive'?'red':'green'}">${w.status==='inactive'?'موقوف':'نشط'}</span></td><td>${esc(w.notes||'-')}</td><td class="row-actions"><button onclick="editWorker(${w.id})">تعديل</button><button class="light" onclick="toggleWorkerStatus(${w.id})">${w.status==='inactive'?'تفعيل':'إيقاف'}</button><button class="danger" onclick="deleteRow('workers',${w.id})">حذف</button></td></tr>`).join('')||'<tr><td colspan="9">لا توجد بيانات</td></tr>';
}
function editWorker(id){
  const w=data.workers.find(x=>x.id===id); if(!w)return;
  $('workerId').value=w.id; $('workerName').value=w.name||''; $('workerPhone').value=w.phone||''; $('workerSalary').value=w.salary||1500;
  $('workerSupervisor').value=workerSupId(w)||''; onWorkerSupervisorChange();
  if($('workerProject')) $('workerProject').value=workerProjectId(w)||'';
  if($('workerType')) $('workerType').value=w.worker_type||'primary';
  $('workerStatus').value=w.status||'active'; $('workerNotes').value=w.notes||'';
  $('workerFormTitle').textContent='تعديل عامل';
  $('workerSaveBtn')&&($('workerSaveBtn').textContent='تحديث العامل');
  $('workerCancelBtn')&&$('workerCancelBtn').classList.remove('hidden');
  showPage('workers', document.querySelector('.nav[onclick*="workers"]'));
  window.scrollTo({top:0,behavior:'smooth'});
}
async function toggleWorkerStatus(id){
  const w=data.workers.find(x=>x.id===id); if(!w) return;
  const next=w.status==='inactive'?'active':'inactive';
  const {error}=await sb.from('workers').update({status:next}).eq('id',id);
  if(error) return msg(error.message,'err');
  msg(next==='active'?'تم تفعيل العامل':'تم إيقاف العامل');
  await refreshAll();
}
function clearAttendanceForm(){ ['attendanceId','attendanceNotes'].forEach(id=>$(id)&&($(id).value='')); if($('attendanceDate')) $('attendanceDate').value=today(); if($('attendanceStatus')) $('attendanceStatus').value='present'; $('attendanceFormTitle')&&($('attendanceFormTitle').textContent='تسجيل حضور / غياب'); }
async function saveAttendance(){ const id=$('attendanceId').value; const row={attendance_date:$('attendanceDate').value||today(), worker_id:Number($('attendanceWorker').value), supervisor_id:Number($('attendanceSupervisor').value)||null, project_id:Number($('attendanceProject').value)||null, status:$('attendanceStatus').value, notes:$('attendanceNotes').value, created_by:session()?.id||null}; if(!row.worker_id) return msg('اختر العامل','err'); const res=id?await sb.from('attendance').update(row).eq('id',id):await sb.from('attendance').upsert(row,{onConflict:'attendance_date,worker_id'}); if(res.error) return msg(res.error.message,'err'); msg('تم حفظ الحضور'); clearAttendanceForm(); await refreshAll(); }
function renderAttendance(){ const b=$('attendanceBody'); if(!b) return; const d=$('attendanceFilterDate')?.value, s=$('attendanceFilterSupervisor')?.value, q=($('attendanceSearch')?.value||'').trim(); let rows=data.attendance; if(d) rows=rows.filter(a=>a.attendance_date===d); if(s) rows=rows.filter(a=>String(a.supervisor_id)===String(s)); if(q) rows=rows.filter(a=>[workerName(a.worker_id),supervisorName(a.supervisor_id),projectName(a.project_id),a.notes].join(' ').includes(q)); b.innerHTML=rows.map(a=>`<tr><td>${a.attendance_date}</td><td>${esc(workerName(a.worker_id))}</td><td>${esc(supervisorName(a.supervisor_id))}</td><td>${esc(projectName(a.project_id))}</td><td><span class="badge ${a.status==='present'?'green':'red'}">${a.status==='present'?'حاضر':'غائب'}</span></td><td>${esc(a.notes)}</td><td class="row-actions"><button onclick="editAttendance(${a.id})">تعديل</button><button class="danger" onclick="deleteRow('attendance',${a.id})">حذف</button></td></tr>`).join('')||'<tr><td colspan="7">لا توجد بيانات</td></tr>'; renderAttendanceWorkersQuick(); }
function editAttendance(id){ const a=data.attendance.find(x=>x.id===id); if(!a)return; $('attendanceId').value=a.id; $('attendanceDate').value=a.attendance_date; $('attendanceSupervisor').value=a.supervisor_id||''; $('attendanceProject').value=a.project_id||''; $('attendanceWorker').value=a.worker_id||''; $('attendanceStatus').value=a.status; $('attendanceNotes').value=a.notes||''; $('attendanceFormTitle').textContent='تعديل حضور'; }
function renderAttendanceWorkersQuick(){ const div=$('attendanceQuick'); if(!div) return; const sid=$('attendanceSupervisor')?.value; if(!sid){ div.innerHTML=''; return; } const ws=data.workers.filter(w=>String(workerSupId(w))===String(sid)); div.innerHTML=ws.map(w=>`<div class="quick-item"><b>${esc(w.name)}</b><div><button onclick="quickAttendance(${w.id},'present')">حاضر</button> <button class="danger" onclick="quickAttendance(${w.id},'absent')">غائب</button></div></div>`).join(''); }
async function quickAttendance(workerId,status){ $('attendanceWorker').value=workerId; $('attendanceStatus').value=status; await saveAttendance(); }
function renderMonthly(){
  const body=$('monthlyBody');
  if(!body) return;
  const month=$('monthlyMonth')?.value || today().slice(0,7);
  const sid=$('monthlySupervisor')?.value;
  let rows=data.logs.filter(l=>{
    const d = l.log_date || String(l.check_in||'').slice(0,10);
    return d.slice(0,7)===month;
  });
  if(sid) rows=rows.filter(l=>String(l.supervisor_id)===String(sid));
  const map={};
  rows.forEach(l=>{
    const k=(l.supervisor_id||'')+'_'+(l.project_id||'');
    if(!map[k]) map[k]={s:l.supervisor_id,p:l.project_id,c:0,m:0,req:0,t:0};
    const actual = logActualMinutes(l);
    const required = logRequiredMinutes(l);
    map[k].c++;
    map[k].m += actual;
    map[k].req += required;
    map[k].t += Number(l.travel_minutes||0);
  });
  const vals=Object.values(map).map(r=>{
    r.percent = r.req ? (r.m/r.req)*100 : 0;
    r.perf = performanceStatus(r.percent,r.req);
    return r;
  });
  body.innerHTML=vals.map(r=>`<tr><td>${esc(supervisorName(r.s))}</td><td>${esc(projectName(r.p))}</td><td>${r.c}</td><td>${minsToText(r.req)}</td><td>${minsToText(r.m)}</td><td>${r.t} دقيقة</td><td><span class="badge ${r.perf.cls}">${percentText(r.percent)}</span></td><td><span class="badge ${r.perf.cls}">${r.perf.text}</span></td></tr>`).join('')||'<tr><td colspan="8">لا توجد بيانات</td></tr>';
  const total=vals.reduce((a,r)=>a+r.m,0), required=vals.reduce((a,r)=>a+r.req,0), travel=vals.reduce((a,r)=>a+r.t,0), pct=required?total/required*100:0;
  const perf=performanceStatus(pct,required);
  if($('monthlySummary')) $('monthlySummary').innerHTML=`<div class="kpi"><small>عدد التسجيلات</small><b>${rows.length}</b></div><div class="kpi"><small>الساعات المطلوبة</small><b>${minsToText(required)}</b></div><div class="kpi"><small>الساعات الفعلية</small><b>${minsToText(total)}</b></div><div class="kpi"><small>وقت الانتقال</small><b>${travel} دقيقة</b></div><div class="kpi"><small>نسبة العمل</small><b>${percentText(pct)}</b></div><div class="kpi"><small>حالة الأداء</small><b><span class="badge ${perf.cls}">${perf.text}</span></b></div>`;
}
function clearTicketForm(){ ['ticketId','ticketTitle','ticketDescription'].forEach(id=>$(id)&&($(id).value='')); if($('ticketStatus')) $('ticketStatus').value='open'; if($('ticketPriority')) $('ticketPriority').value='normal'; $('ticketFormTitle')&&($('ticketFormTitle').textContent='إضافة تكت'); }
async function saveTicket(){ const u=session(); const row={project_id:Number($('ticketProject').value)||null, supervisor_id:Number($('ticketSupervisor')?.value || (u.role==='supervisor'?u.id:''))||null, title:$('ticketTitle').value.trim(), description:$('ticketDescription').value, priority:$('ticketPriority').value, status:$('ticketStatus')?.value || 'open'}; if(!row.title) return msg('عنوان التكت مطلوب','err'); if(row.status==='closed') row.closed_at=new Date().toISOString(); const id=$('ticketId')?.value; const res=id?await sb.from('tickets').update(row).eq('id',id):await sb.from('tickets').insert(row); if(res.error) return msg(res.error.message,'err'); playAppSound('ticket'); msg('تم حفظ التكت'); clearTicketForm(); await refreshAll(); }
function renderTickets(){ const b=$('ticketsBody'); if(!b) return; const st=$('ticketFilterStatus')?.value, q=($('ticketSearch')?.value||'').trim(); let rows=data.tickets; if(st) rows=rows.filter(t=>t.status===st); if(q) rows=rows.filter(t=>[t.title,t.description,projectName(t.project_id),supervisorName(t.supervisor_id)].join(' ').includes(q)); b.innerHTML=rows.map(t=>`<tr><td>${esc(projectName(t.project_id))}</td><td>${esc(supervisorName(t.supervisor_id))}</td><td>${esc(t.title)}</td><td><span class="badge ${t.priority==='high'?'red':'amber'}">${t.priority||'normal'}</span></td><td><span class="badge ${t.status==='closed'?'green':'red'}">${t.status==='closed'?'مغلق':'مفتوح'}</span></td><td class="row-actions"><button onclick="editTicket(${t.id})">تعديل</button><button class="danger" onclick="deleteRow('tickets',${t.id})">حذف</button></td></tr>`).join('')||'<tr><td colspan="6">لا توجد بيانات</td></tr>'; }
function editTicket(id){ const t=data.tickets.find(x=>x.id===id); if(!t)return; $('ticketId').value=t.id; $('ticketProject').value=t.project_id||''; if($('ticketSupervisor')) $('ticketSupervisor').value=t.supervisor_id||''; $('ticketTitle').value=t.title||''; $('ticketPriority').value=t.priority||'normal'; if($('ticketStatus')) $('ticketStatus').value=t.status||'open'; $('ticketDescription').value=t.description||''; $('ticketFormTitle')&&($('ticketFormTitle').textContent='تعديل تكت'); }
function renderAlerts(){ const div=$('alertsList'); if(!div) return; const alerts=[]; data.projects.filter(p=>!p.supervisor_id).forEach(p=>alerts.push(['warn',`مشروع بدون مشرف: ${p.name}`])); data.workers.filter(w=>!workerSupId(w)).forEach(w=>alerts.push(['warn',`عامل بدون مشرف: ${w.name}`])); data.logs.filter(l=>!l.check_out).forEach(l=>alerts.push(['danger',`تسجيل دخول بدون خروج: ${projectName(l.project_id)} - ${supervisorName(l.supervisor_id)}`])); data.tickets.filter(t=>t.status==='open').forEach(t=>alerts.push(['warn',`تكت مفتوح: ${t.title} - ${projectName(t.project_id)}`])); div.innerHTML=alerts.map(a=>`<div class="alert-item ${a[0]}">${esc(a[1])}</div>`).join('')||'<div class="alert-item">لا توجد تنبيهات حالياً</div>'; }
function toCSV(rows){ if(!rows.length) return ''; const keys=Object.keys(rows[0]); return [keys.join(','),...rows.map(r=>keys.map(k=>'"'+String(r[k]??'').replace(/"/g,'""')+'"').join(','))].join('\n'); }
function download(name,text){ const blob=new Blob([text],{type:'text/csv;charset=utf-8'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=name; a.click(); URL.revokeObjectURL(a.href); }
async function exportTable(table){ const {data:rows,error}=await sb.from(table).select('*'); if(error) return msg(error.message,'err'); download(`${table}.csv`, toCSV(rows||[])); }
function exportMonthlyCSV(){ const rows=[...document.querySelectorAll('#monthlyBody tr')].map(tr=>[...tr.children].map(td=>td.textContent)); const csv=['المشرف,المشروع,عدد السجلات,الساعات المطلوبة,الساعات الفعلية,وقت الانتقال,نسبة العمل,حالة الأداء',...rows.map(r=>r.map(x=>'"'+x+'"').join(','))].join('\n'); download('monthly.csv',csv); }
async function initSupervisor(){ const u=requireRole('supervisor'); if(!u) return; await loadAll(); data.projects=data.projects.filter(p=>String(p.supervisor_id)===String(u.id)); data.workers=data.workers.filter(w=>String(workerSupId(w))===String(u.id)); data.logs=data.logs.filter(l=>String(l.supervisor_id)===String(u.id)); const supProjectIds = new Set(data.projects.map(p=>String(p.id))); data.tickets=data.tickets.filter(t=>String(t.supervisor_id)===String(u.id) || String(t.created_by)===String(u.id) || supProjectIds.has(String(t.project_id))); $('supTitle').textContent=`لوحة المشرف - ${u.full_name}`; fillSelect('logProject',data.projects,'name','اختر المشروع'); fillSelect('attendanceProject',data.projects,'name','اختر المشروع'); fillSelect('ticketProject',data.projects,'name','اختر المشروع'); if($('logDate')) $('logDate').value=today(); if($('attendanceDate')) $('attendanceDate').value=today(); renderSupervisorAttendanceList(); renderTimeLogs(); await supervisorInventoryLoad(); }
async function supervisorCheckIn(){ if(!$('logProject').value) return msg('اختر المشروع','err'); $('logDate').value=today(); $('logIn').value=nowTime(); $('logOut').value=''; await saveTimeLog(); await initSupervisor(); }
async function supervisorCheckOut(){ const u=session(); const pid=$('logProject').value; const open=data.logs.find(l=>String(l.project_id)===String(pid)&&!l.check_out); if(open) editTimeLog(open.id); $('logOut').value=nowTime(); await saveTimeLog(); await initSupervisor(); }
function renderSupervisorAttendanceList(){ const div=$('supervisorAttendanceList'); if(!div) return; div.innerHTML=data.workers.map(w=>`<div class="quick-item"><b>${esc(w.name)}</b><select data-worker="${w.id}"><option value="present">حاضر</option><option value="absent">غائب</option></select></div>`).join('')||'<div class="quick-item">لا يوجد عمال مرتبطين بك</div>'; }
async function saveSupervisorAttendance(){ const u=session(); const date=$('attendanceDate').value||today(), project=Number($('attendanceProject').value)||null; const rows=[...document.querySelectorAll('#supervisorAttendanceList select')].map(s=>({attendance_date:date, worker_id:Number(s.dataset.worker), supervisor_id:u.id, project_id:project, status:s.value, created_by:u.id})); if(!rows.length) return; const {error}=await sb.from('attendance').upsert(rows,{onConflict:'attendance_date,worker_id'}); if(error) return msg(error.message,'err'); msg('تم حفظ التحضير'); await initSupervisor(); }

/* ===== Tasneef emergency navigation/button stabilizer ===== */
(function(){
  function runSafe(name){
    try { if (typeof window[name] === 'function') window[name](); }
    catch(e){ console.error('Error in '+name, e); }
  }

  const renderNames = ['renderDashboard','renderTimeLogs','renderUsers','renderProjects','renderWorkers','renderAttendance','renderMonthly','renderTickets','renderAlerts','renderTasneefAssistant'];
  window.renderAll = function(){ renderNames.forEach(runSafe); };

  window.showPage = function(id, btn){
    document.querySelectorAll('.page').forEach(p => p.classList.add('hidden'));
    const page = document.getElementById(id);
    if(page) page.classList.remove('hidden');
    document.querySelectorAll('.nav').forEach(n => n.classList.remove('active'));
    if(btn) btn.classList.add('active');
    try { window.renderAll(); } catch(e){ console.error('renderAll failed', e); }
  };

  const oldInitAdmin = window.initAdmin;
  window.initAdmin = async function(){
    try {
      if (typeof requireRole === 'function') requireRole('admin');
      if (typeof refreshAll === 'function') await refreshAll();
    } catch(e){
      console.error('initAdmin failed', e);
      try { window.renderAll(); } catch(_e){}
    }
  };

  document.addEventListener('DOMContentLoaded', function(){
    document.querySelectorAll('.side .nav:not(.danger)').forEach(function(btn){
      const txt = (btn.textContent || '').trim();
      const map = {
        'لوحة التحكم':'dashboard',
        'التسجيلات اليومية':'daily',
        'إدارة المستخدمين':'users',
        'المشاريع':'projects',
        'العمال':'workers',
        'الحضور والغياب':'attendance',
        'الأوقات الشهرية':'monthly',
        'التكتات':'tickets',
        'التنبيهات':'alerts',
        'مساعد تصنيف':'assistant',
        'التصدير':'export'
      };
      const old = btn.getAttribute('onclick') || '';
      const m = old.match(/showPage\('([^']+)'/);
      const id = (m && m[1]) || map[txt];
      if(id){
        btn.onclick = function(ev){ ev.preventDefault(); window.showPage(id, btn); return false; };
      }
    });
  });
})();

/* ===== V10: Monthly attendance matrix ===== */
(function(){
  function daysInMonth(monthStr){
    if(!monthStr || !monthStr.includes('-')) monthStr = today().slice(0,7);
    const parts = monthStr.split('-').map(Number);
    return new Date(parts[0], parts[1], 0).getDate();
  }
  function monthDate(monthStr, day){ return monthStr + '-' + String(day).padStart(2,'0'); }
  function setSelectOptionsSafe(id, rows, labelFn, allLabel){
    const el = $(id); if(!el) return;
    const old = el.value;
    el.innerHTML = `<option value="">${allLabel||'الكل'}</option>` + rows.map(r=>`<option value="${r.id}">${esc(labelFn(r))}</option>`).join('');
    if([...el.options].some(o=>o.value===old)) el.value = old;
  }
  function ensureAttendanceMatrixFilters(){
    const monthEl = $('attendanceMatrixMonth');
    if(monthEl && !monthEl.value) monthEl.value = today().slice(0,7);
    setSelectOptionsSafe('attendanceMatrixSupervisor', data.supervisors||[], s=>s.full_name||s.username, 'كل المشرفين');
    const sid = $('attendanceMatrixSupervisor')?.value;
    let projects = data.projects || [];
    if(sid) projects = projects.filter(p=>String(p.supervisor_id)===String(sid));
    setSelectOptionsSafe('attendanceMatrixProject', projects, p=>p.name, 'كل المشاريع');
  }
  function workerMatchesMatrix(w, sid, pid, q){
    if(sid && String(workerSupId(w))!==String(sid)) return false;
    if(pid && String(workerProjectId(w))!==String(pid)) return false;
    if(q && !String(w.name||'').includes(q)) return false;
    return true;
  }
  window.renderAttendanceMonthly = function(){
    if(!$('attendanceMatrixBody')) return;
    ensureAttendanceMatrixFilters();
    const month = $('attendanceMatrixMonth')?.value || today().slice(0,7);
    const sid = $('attendanceMatrixSupervisor')?.value || '';
    const pid = $('attendanceMatrixProject')?.value || '';
    const q = ($('attendanceMatrixSearch')?.value || '').trim();
    const days = daysInMonth(month);
    const workers = (data.workers||[]).filter(w=>workerMatchesMatrix(w,sid,pid,q));
    const head = $('attendanceMatrixHead');
    if(head){
      const dayHeads = Array.from({length:days},(_,i)=>`<th>${i+1}</th>`).join('');
      head.innerHTML = `<tr><th>العامل</th><th>المشرف</th><th>المشروع</th>${dayHeads}<th>حضور</th><th>غياب</th><th>النسبة</th></tr>`;
    }
    let totalPresent=0,totalAbsent=0;
    const body = $('attendanceMatrixBody');
    body.innerHTML = workers.map(w=>{
      let present=0, absent=0;
      const cells = [];
      for(let d=1; d<=days; d++){
        const ds = monthDate(month,d);
        const rec = (data.attendance||[]).find(a=>String(a.worker_id)===String(w.id) && a.attendance_date===ds && (!pid || String(a.project_id||workerProjectId(w))===String(pid)));
        if(rec?.status==='present'){ present++; cells.push(`<td title="حاضر"><span class="att-cell att-present">ح</span></td>`); }
        else if(rec?.status==='absent'){ absent++; cells.push(`<td title="غائب"><span class="att-cell att-absent">غ</span></td>`); }
        else cells.push(`<td title="لم يسجل"><span class="att-cell att-empty">-</span></td>`);
      }
      totalPresent += present; totalAbsent += absent;
      const pct = (present+absent) ? (present/(present+absent))*100 : 0;
      const cls = pct>=90?'green':(pct>=70?'amber':'red');
      return `<tr><td><b>${esc(w.name)}</b><div class="att-summary">${esc(workerTypeText(w.worker_type||'primary'))}</div></td><td>${esc(supervisorName(workerSupId(w)))}</td><td>${esc(projectName(workerProjectId(w)))}</td>${cells.join('')}<td><span class="badge green">${present}</span></td><td><span class="badge red">${absent}</span></td><td><span class="badge ${cls}">${pct.toFixed(1)}%</span></td></tr>`;
    }).join('') || `<tr><td colspan="${days+6}">لا يوجد عمال حسب الفلاتر المختارة</td></tr>`;
    const recorded = totalPresent + totalAbsent;
    const pct = recorded ? (totalPresent/recorded)*100 : 0;
    if($('attendanceMatrixSummary')){
      $('attendanceMatrixSummary').innerHTML = `<div class="kpi"><small>عدد العمال</small><b>${workers.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalPresent}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalAbsent}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`;
    }
  };
  window.exportAttendanceMatrixCSV = function(){
    const month = $('attendanceMatrixMonth')?.value || today().slice(0,7);
    const rows=[...document.querySelectorAll('#attendanceMatrixBody tr')].map(tr=>[...tr.children].map(td=>`"${td.textContent.trim().replace(/"/g,'""')}"`).join(','));
    const heads=[...document.querySelectorAll('#attendanceMatrixHead th')].map(th=>`"${th.textContent.trim()}"`).join(',');
    download(`attendance-${month}.csv`, [heads,...rows].join('\n'));
  };
  const oldRenderAttendance = window.renderAttendance;
  window.renderAttendance = function(){
    try{ if(typeof oldRenderAttendance==='function') oldRenderAttendance(); }catch(e){ console.error('old renderAttendance failed',e); }
    try{ window.renderAttendanceMonthly(); }catch(e){ console.error('renderAttendanceMonthly failed',e); }
  };
})();

/* ===== V10.1: Fix attendance monthly filters and empty results ===== */
(function(){
  function daysInMonthFixed(monthStr){
    if(!monthStr || !monthStr.includes('-')) monthStr = today().slice(0,7);
    const [y,m] = monthStr.split('-').map(Number);
    return new Date(y, m, 0).getDate();
  }
  function monthDateFixed(monthStr, day){ return `${monthStr}-${String(day).padStart(2,'0')}`; }
  function monthOfAttendance(a){ return String(a.attendance_date || '').slice(0,7); }
  function safeOptionText(v){ return esc(v || '-'); }
  function setOptionsKeepValue(id, rows, labelFn, allLabel){
    const el = $(id); if(!el) return '';
    const old = el.value || '';
    const seen = new Set();
    const opts = [];
    (rows||[]).forEach(r=>{
      if(!r || r.id===undefined || r.id===null) return;
      const val = String(r.id);
      if(seen.has(val)) return;
      seen.add(val);
      opts.push(`<option value="${esc(val)}">${safeOptionText(labelFn(r))}</option>`);
    });
    el.innerHTML = `<option value="">${allLabel || 'الكل'}</option>` + opts.join('');
    if([...el.options].some(o=>o.value===old)) el.value = old;
    else el.value = '';
    return el.value;
  }
  function projectMatchesSupervisorForAttendance(project, sid, month){
    if(!sid) return true;
    if(String(project.supervisor_id||'') === String(sid)) return true;
    const hasWorker = (data.workers||[]).some(w => String(workerProjectId(w)||'')===String(project.id) && String(workerSupId(w)||'')===String(sid));
    if(hasWorker) return true;
    return (data.attendance||[]).some(a => monthOfAttendance(a)===month && String(a.project_id||'')===String(project.id) && String(a.supervisor_id||'')===String(sid));
  }
  function workerMatchesAttendanceMatrixFixed(w, sid, pid, q, month){
    const wid = String(w.id);
    const wsid = String(workerSupId(w)||'');
    const wpid = String(workerProjectId(w)||'');
    const workerAttend = (data.attendance||[]).filter(a=>monthOfAttendance(a)===month && String(a.worker_id)===wid);
    if(sid){
      const projectSupervisorMatch = wpid && String((data.projects||[]).find(p=>String(p.id)===wpid)?.supervisor_id||'')===String(sid);
      const attendanceSupervisorMatch = workerAttend.some(a=>String(a.supervisor_id||'')===String(sid));
      if(wsid!==String(sid) && !projectSupervisorMatch && !attendanceSupervisorMatch) return false;
    }
    if(pid){
      const attendanceProjectMatch = workerAttend.some(a=>String(a.project_id||'')===String(pid));
      if(wpid!==String(pid) && !attendanceProjectMatch) return false;
    }
    if(q && !String(w.name||'').includes(q)) return false;
    return true;
  }
  function recordForDay(w, ds, sid, pid){
    const rows = (data.attendance||[]).filter(a=>String(a.worker_id)===String(w.id) && a.attendance_date===ds);
    return rows.find(a=>(!sid || String(a.supervisor_id||workerSupId(w)||'')===String(sid)) && (!pid || String(a.project_id||workerProjectId(w)||'')===String(pid))) || null;
  }
  window.renderAttendanceMonthly = function(){
    const body = $('attendanceMatrixBody');
    if(!body) return;

    const monthEl = $('attendanceMatrixMonth');
    if(monthEl && !monthEl.value) monthEl.value = today().slice(0,7);
    const month = monthEl?.value || today().slice(0,7);

    const supervisorRows = (data.supervisors && data.supervisors.length ? data.supervisors : (data.users||[]).filter(u=>u.role==='supervisor'));
    const sid = setOptionsKeepValue('attendanceMatrixSupervisor', supervisorRows, s=>s.full_name || s.username, 'كل المشرفين');

    const projectRows = (data.projects||[]).filter(p=>projectMatchesSupervisorForAttendance(p, sid, month));
    const pid = setOptionsKeepValue('attendanceMatrixProject', projectRows, p=>p.name, 'كل المشاريع');

    const q = ($('attendanceMatrixSearch')?.value || '').trim();
    const days = daysInMonthFixed(month);
    const workers = (data.workers||[]).filter(w=>workerMatchesAttendanceMatrixFixed(w, sid, pid, q, month));

    const head = $('attendanceMatrixHead');
    if(head){
      const dayHeads = Array.from({length:days},(_,i)=>`<th>${i+1}</th>`).join('');
      head.innerHTML = `<tr><th>العامل</th><th>المشرف</th><th>المشروع</th>${dayHeads}<th>حضور</th><th>غياب</th><th>النسبة</th></tr>`;
    }

    let totalPresent=0, totalAbsent=0;
    body.innerHTML = workers.map(w=>{
      let present=0, absent=0;
      const cells=[];
      for(let d=1; d<=days; d++){
        const ds = monthDateFixed(month,d);
        const rec = recordForDay(w, ds, sid, pid);
        if(rec && rec.status==='present'){
          present++; cells.push(`<td title="حاضر"><span class="att-cell att-present">ح</span></td>`);
        } else if(rec && rec.status==='absent'){
          absent++; cells.push(`<td title="غائب"><span class="att-cell att-absent">غ</span></td>`);
        } else {
          cells.push(`<td title="لم يسجل"><span class="att-cell att-empty">-</span></td>`);
        }
      }
      totalPresent += present; totalAbsent += absent;
      const pct = (present+absent) ? (present/(present+absent))*100 : 0;
      const cls = pct>=90 ? 'green' : (pct>=70 ? 'amber' : 'red');
      const projectId = pid || workerProjectId(w);
      const supervisorId = sid || workerSupId(w) || (projectId ? (data.projects||[]).find(p=>String(p.id)===String(projectId))?.supervisor_id : '');
      return `<tr><td><b>${esc(w.name)}</b><div class="att-summary">${esc(workerTypeText(w.worker_type||'primary'))}</div></td><td>${esc(supervisorName(supervisorId))}</td><td>${esc(projectName(projectId))}</td>${cells.join('')}<td><span class="badge green">${present}</span></td><td><span class="badge red">${absent}</span></td><td><span class="badge ${cls}">${pct.toFixed(1)}%</span></td></tr>`;
    }).join('') || `<tr><td colspan="${days+6}">لا يوجد عمال حسب الفلاتر المختارة</td></tr>`;

    const recorded = totalPresent + totalAbsent;
    const pct = recorded ? (totalPresent/recorded)*100 : 0;
    if($('attendanceMatrixSummary')){
      $('attendanceMatrixSummary').innerHTML = `<div class="kpi"><small>عدد العمال</small><b>${workers.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalPresent}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalAbsent}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`;
    }
  };

  const oldShowPageV101 = window.showPage;
  window.showPage = function(id, btn){
    if(typeof oldShowPageV101 === 'function') oldShowPageV101(id, btn);
    if(id === 'attendance') setTimeout(()=>window.renderAttendanceMonthly && window.renderAttendanceMonthly(), 0);
  };
})();


/* ===== V14: Ticket receiving, closing details, duration and colors ===== */
(function(){
  function ticketNo(t){ return t.ticket_number || ('T-' + String(t.id||0).padStart(4,'0')); }
  function ticketStatusLabel(status){ return status==='closed'?'مغلق':(status==='processing'?'تحت المعالجة':'مفتوح'); }
  function ticketPriorityLabel(p){ return p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي')); }
  function shortDesc(s){ s=String(s||''); return s.length>90 ? esc(s.slice(0,90))+'…' : esc(s||'-'); }
  function currentName(){ const u=session(); return (u && (u.full_name || u.username)) || 'غير محدد'; }
  function parseDate(v){ const d = v ? new Date(v) : null; return d && !isNaN(d) ? d : null; }
  function minutesBetween(a,b){ const da=parseDate(a), db=parseDate(b); if(!da || !db) return 0; return Math.max(0, Math.round((db-da)/60000)); }
  function durationLabel(min){ min=Number(min||0); if(!min) return '0د'; const d=Math.floor(min/1440), h=Math.floor((min%1440)/60), m=min%60; const parts=[]; if(d) parts.push(d+'ي'); if(h) parts.push(h+'س'); if(m||!parts.length) parts.push(m+'د'); return parts.join(' '); }
  function openMinutes(t){ if(t.status==='closed') return Number(t.open_duration_minutes||0) || minutesBetween(t.created_at, t.closed_at); return minutesBetween(t.created_at, new Date().toISOString()); }
  function ticketRowClass(t){ if(t.status==='closed') return 'ticket-row-closed'; if(t.status==='processing') return 'ticket-row-processing'; if(t.priority==='urgent'||t.priority==='high') return 'ticket-row-urgent'; return 'ticket-row-normal'; }
  function statusBadge(t){ const cls=t.status==='closed'?'green':(t.status==='processing'?'amber':((t.priority==='urgent'||t.priority==='high')?'red':'pink')); return `<span class="badge ${cls}">${ticketStatusLabel(t.status)}</span>`; }
  function priorityBadge(t){ const cls=t.priority==='urgent'?'red':(t.priority==='high'?'amber':'pink'); return `<span class="badge ${cls}">${ticketPriorityLabel(t.priority)}</span>`; }
  function askCloserName(){ const name=prompt('اسم الشخص الذي أغلق التكت\nاكتب اسم الفني أو المشرف', currentName()); return (name||'').trim(); }
  function askClosureNote(){ const note=prompt('كيف تم إغلاق التكت؟\nاكتب طريقة الحل أو الإجراء المنفذ'); return (note||'').trim(); }

  window.toggleTicketClosureBox = function(){
    const box=$('ticketClosureBox'); if(!box) return;
    const isClosed=($('ticketStatus')?.value||'')==='closed';
    box.classList.toggle('hidden', !isClosed);
    if(isClosed && $('ticketClosedByName') && !$('ticketClosedByName').value) $('ticketClosedByName').value=currentName();
  };

  window.clearTicketForm = function(){ ['ticketId','ticketTitle','ticketDescription','ticketClosureNote','ticketClosedByName'].forEach(id=>{ if($(id)) $(id).value=''; }); if($('ticketStatus')) $('ticketStatus').value='open'; if($('ticketPriority')) $('ticketPriority').value='normal'; if($('ticketFormTitle')) $('ticketFormTitle').textContent='إضافة تكت'; toggleTicketClosureBox(); };

  window.saveTicket = async function(){
    const u=session(); if(!u) return msg('سجّل الدخول أولاً','err');
    const title=($('ticketTitle')?.value||'').trim(); if(!title) return msg('عنوان التكت مطلوب','err');
    const status=$('ticketStatus')?.value || 'open';
    const row={project_id:Number($('ticketProject')?.value)||null, supervisor_id:Number($('ticketSupervisor')?.value || (u.role==='supervisor'?u.id:''))||null, created_by:u.id, title, description:$('ticketDescription')?.value || '', priority:$('ticketPriority')?.value || 'normal', status, updated_at:new Date().toISOString()};
    const id=$('ticketId')?.value;
    if(status==='closed'){
      const existing=id?(data.tickets||[]).find(x=>String(x.id)===String(id)):null;
      const note=(($('ticketClosureNote')?.value)||'').trim() || askClosureNote(); if(!note) return msg('لا يمكن إغلاق التكت بدون ذكر كيف تم الإغلاق','err');
      const closer=(($('ticketClosedByName')?.value)||'').trim() || askCloserName(); if(!closer) return msg('اكتب اسم من أغلق التكت','err');
      const now=new Date().toISOString(); row.closed_at=now; row.closed_by=u.id; row.closed_by_name=closer; row.closure_note=note; row.open_duration_minutes=existing?minutesBetween(existing.created_at,now):0; row.processing_duration_minutes=existing?.claimed_at?minutesBetween(existing.claimed_at,now):0;
      if(existing && !existing.claimed_at){ row.claimed_by=u.id; row.claimed_by_name=closer; row.claimed_at=now; }
    }else{ row.closed_at=null; row.closed_by=null; row.closed_by_name=null; row.closure_note=null; row.open_duration_minutes=null; row.processing_duration_minutes=null; }
    let res; if(id){ res=await sb.from('tickets').update(row).eq('id',id).select('*').maybeSingle(); }else{ res=await sb.from('tickets').insert(row).select('*').single(); if(!res.error&&res.data&&!res.data.ticket_number){ const tn='T-'+String(res.data.id).padStart(4,'0'); await sb.from('tickets').update({ticket_number:tn}).eq('id',res.data.id); } }
    if(res.error) return msg(res.error.message,'err'); playAppSound('ticket'); msg(id?'تم تحديث التكت':'تم حفظ التكت'); clearTicketForm(); if(u.role==='supervisor') await window.initSupervisor(); else await refreshAll();
  };

  window.renderTickets = function(){
    const adminBody=$('ticketsBody'), supBody=$('supTicketsBody'); if(!adminBody&&!supBody) return; let rows=[...(data.tickets||[])];
    if(adminBody){ const st=$('ticketFilterStatus')?.value||'', q=($('ticketSearch')?.value||'').trim().toLowerCase(); let list=rows; if(st) list=list.filter(t=>t.status===st); if(q) list=list.filter(t=>[ticketNo(t),t.title,t.description,projectName(t.project_id),supervisorName(t.supervisor_id),ticketStatusLabel(t.status),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q)); adminBody.innerHTML=list.map(t=>`<tr class="${ticketRowClass(t)}"><td><b>${esc(ticketNo(t))}</b></td><td>${esc(projectName(t.project_id))}</td><td>${esc(supervisorName(t.supervisor_id))}</td><td>${esc(t.title)}</td><td style="white-space:normal;min-width:220px">${shortDesc(t.description)}</td><td>${priorityBadge(t)}</td><td>${statusBadge(t)}</td><td>${fmt(t.created_at)}</td><td>${esc(durationLabel(openMinutes(t)))}</td><td>${esc(t.claimed_by_name||'-')}<br><small>${t.claimed_at?fmt(t.claimed_at):''}</small></td><td>${esc(t.closed_by_name||'-')}<br><small>${t.closed_at?fmt(t.closed_at):''}</small></td><td style="white-space:normal;min-width:220px">${shortDesc(t.closure_note)}</td><td class="row-actions"><button onclick="editTicket(${t.id})">تعديل</button>${t.status==='closed'?`<button class="light" onclick="setTicketStatus(${t.id},'open')">إعادة فتح</button>`:`${t.status!=='processing'?`<button class="light" onclick="claimTicket(${t.id})">استلام</button>`:''}<button onclick="closeTicket(${t.id})">إغلاق</button>`}<button class="danger" onclick="deleteRow('tickets',${t.id})">حذف</button></td></tr>`).join('')||'<tr><td colspan="13">لا توجد تكتات</td></tr>'; }
    if(supBody){ const st=$('supTicketFilterStatus')?.value||'', pid=$('supTicketFilterProject')?.value||'', q=($('supTicketSearch')?.value||'').trim().toLowerCase(); let list=rows; if(pid) list=list.filter(t=>String(t.project_id)===String(pid)); if(st) list=list.filter(t=>t.status===st); if(q) list=list.filter(t=>[ticketNo(t),t.title,t.description,projectName(t.project_id),ticketStatusLabel(t.status),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q)); supBody.innerHTML=list.map(t=>`<tr class="${ticketRowClass(t)}"><td><b>${esc(ticketNo(t))}</b></td><td>${esc(projectName(t.project_id))}</td><td>${esc(t.title)}</td><td style="white-space:normal;min-width:180px">${shortDesc(t.description)}</td><td>${priorityBadge(t)}</td><td>${statusBadge(t)}</td><td>${esc(durationLabel(openMinutes(t)))}</td><td>${esc(t.claimed_by_name||'-')}</td><td>${esc(t.closed_by_name||'-')}</td><td style="white-space:normal;min-width:180px">${shortDesc(t.closure_note)}</td><td class="row-actions"><button onclick="editTicket(${t.id})">تعديل</button>${t.status==='closed'?`<button class="light" onclick="setTicketStatus(${t.id},'open')">إعادة فتح</button>`:`${t.status!=='processing'?`<button class="light" onclick="claimTicket(${t.id})">استلام</button>`:''}<button onclick="closeTicket(${t.id})">إغلاق</button>`}</td></tr>`).join('')||'<tr><td colspan="11">لا توجد تكتات</td></tr>'; }
  };

  window.editTicket = function(id){ const t=(data.tickets||[]).find(x=>String(x.id)===String(id)); if(!t)return; if($('ticketId')) $('ticketId').value=t.id; if($('ticketProject')) $('ticketProject').value=t.project_id||''; if($('ticketSupervisor')) $('ticketSupervisor').value=t.supervisor_id||''; if($('ticketTitle')) $('ticketTitle').value=t.title||''; if($('ticketPriority')) $('ticketPriority').value=t.priority||'normal'; if($('ticketStatus')) $('ticketStatus').value=t.status||'open'; if($('ticketDescription')) $('ticketDescription').value=t.description||''; if($('ticketClosedByName')) $('ticketClosedByName').value=t.closed_by_name||''; if($('ticketClosureNote')) $('ticketClosureNote').value=t.closure_note||''; if($('ticketFormTitle')) $('ticketFormTitle').textContent='تعديل تكت '+ticketNo(t); toggleTicketClosureBox(); window.scrollTo({top:0,behavior:'smooth'}); };

  window.claimTicket = async function(id){ const u=session(); if(!u)return msg('سجّل الدخول أولاً','err'); const t=(data.tickets||[]).find(x=>String(x.id)===String(id)); if(!t)return msg('التكت غير موجود','err'); if(t.status==='closed')return msg('التكت مغلق','err'); const now=new Date().toISOString(), name=currentName(); const {error}=await sb.from('tickets').update({status:'processing',claimed_by:u.id,claimed_by_name:name,claimed_at:now,updated_at:now}).eq('id',id); if(error)return msg(error.message,'err'); msg('تم استلام التكت بواسطة '+name); if(u?.role==='supervisor') await window.initSupervisor(); else await refreshAll(); };

  window.closeTicket = async function(id){ const u=session(); if(!u)return msg('سجّل الدخول أولاً','err'); const t=(data.tickets||[]).find(x=>String(x.id)===String(id)); if(!t)return msg('التكت غير موجود','err'); if(t.status==='closed')return msg('التكت مغلق بالفعل','err'); const note=askClosureNote(); if(!note)return msg('لا يمكن إغلاق التكت بدون ذكر كيف تم الإغلاق','err'); const closer=askCloserName(); if(!closer)return msg('اكتب اسم من أغلق التكت','err'); const now=new Date().toISOString(); const row={status:'closed',closed_at:now,closed_by:u.id,closed_by_name:closer,closure_note:note,open_duration_minutes:minutesBetween(t.created_at,now),processing_duration_minutes:t.claimed_at?minutesBetween(t.claimed_at,now):null,updated_at:now}; if(!t.claimed_at){ row.claimed_by=u.id; row.claimed_by_name=closer; row.claimed_at=now; } const {error}=await sb.from('tickets').update(row).eq('id',id); if(error)return msg(error.message,'err'); playAppSound('ticket'); msg('تم إغلاق التكت وحفظ طريقة الإغلاق'); if(u?.role==='supervisor') await window.initSupervisor(); else await refreshAll(); };

  window.setTicketStatus = async function(id,status){ if(status==='closed') return closeTicket(id); if(status==='processing') return claimTicket(id); const row={status,updated_at:new Date().toISOString()}; if(status==='open'){ row.closed_at=null; row.closed_by=null; row.closed_by_name=null; row.closure_note=null; row.open_duration_minutes=null; row.processing_duration_minutes=null; } const {error}=await sb.from('tickets').update(row).eq('id',id); if(error)return msg(error.message,'err'); msg(status==='open'?'تم إعادة فتح التكت':'تم تحديث حالة التكت'); const u=session(); if(u?.role==='supervisor') await window.initSupervisor(); else await refreshAll(); };

  const oldInitSupervisorV14 = window.initSupervisor;
  window.initSupervisor = async function(){ await oldInitSupervisorV14(); if($('supTicketFilterProject')) fillSelect('supTicketFilterProject', data.projects||[], 'name', 'كل المشاريع'); renderTickets(); };
})();

/* ===== V13: Supervisor ticket live notifications, badge, and auto refresh ===== */
(function(){
  let ticketWatchTimer = null;
  let lastSeenTicketId = 0;
  let watchStarted = false;

  function ticketNoV13(t){ return t.ticket_number || ('T-' + String(t.id||0).padStart(4,'0')); }

  function ensureTicketNoticeUI(){
    if(!document.getElementById('supervisorTicketNotice')){
      const box = document.createElement('div');
      box.id = 'supervisorTicketNotice';
      box.style.cssText = 'display:none;position:fixed;left:14px;right:14px;top:14px;z-index:99999;background:#0A4033;color:#fff;border-radius:16px;padding:13px 15px;box-shadow:0 12px 30px rgba(0,0,0,.22);font-family:Tahoma,Arial,sans-serif;line-height:1.7;';
      document.body.appendChild(box);
    }
    const ticketBtn = [...document.querySelectorAll('.sup-tab')].find(b => (b.textContent||'').includes('التكتات'));
    if(ticketBtn && !ticketBtn.querySelector('#supTicketOpenCount')){
      ticketBtn.innerHTML = 'التكتات <span id="supTicketOpenCount" style="display:inline-flex;align-items:center;justify-content:center;min-width:22px;height:22px;border-radius:999px;background:#b83232;color:#fff;font-size:12px;margin-inline-start:6px;padding:0 6px;">0</span>';
    }
  }

  function supervisorTicketScope(rows){
    const u = session();
    if(!u || u.role !== 'supervisor') return rows || [];
    const pids = new Set((data.projects||[]).map(p => String(p.id)));
    return (rows||[]).filter(t =>
      String(t.supervisor_id||'') === String(u.id) ||
      String(t.created_by||'') === String(u.id) ||
      pids.has(String(t.project_id||''))
    );
  }

  function updateSupervisorTicketBadge(){
    ensureTicketNoticeUI();
    const el = document.getElementById('supTicketOpenCount');
    if(!el) return;
    const openCount = (data.tickets||[]).filter(t => t.status !== 'closed').length;
    el.textContent = openCount;
    el.style.display = openCount > 0 ? 'inline-flex' : 'none';
  }

  function showSupervisorTicketNotice(t){
    ensureTicketNoticeUI();
    const box = document.getElementById('supervisorTicketNotice');
    if(!box) return;
    box.innerHTML = `<b>تكت جديد وصل</b><br>${ticketNoV13(t)} - ${esc(projectName(t.project_id))}<br>${esc(t.title || '')}`;
    box.style.display = 'block';
    clearTimeout(window.__ticketNoticeTimeout);
    window.__ticketNoticeTimeout = setTimeout(()=>{ box.style.display='none'; }, 8000);
  }

  async function pollSupervisorTickets(silent=false){
    const u = session();
    if(!u || u.role !== 'supervisor') return;
    try{
      const {data:rows,error}=await sb.from('tickets').select('*').order('created_at',{ascending:false});
      if(error) return console.warn(error.message);
      const scoped = supervisorTicketScope(rows||[]);
      const newestId = scoped.reduce((m,t)=>Math.max(m, Number(t.id||0)), 0);
      const newTickets = scoped.filter(t => Number(t.id||0) > Number(lastSeenTicketId||0));

      data.tickets = scoped;
      if(typeof renderTickets === 'function') renderTickets();
      if(typeof renderSupervisorDailySummary === 'function') renderSupervisorDailySummary();
      updateSupervisorTicketBadge();

      if(!silent && watchStarted && newTickets.length){
        const latest = newTickets.sort((a,b)=>Number(b.id||0)-Number(a.id||0))[0];
        showSupervisorTicketNotice(latest);
        try{ playAppSound('ticket'); }catch(e){}
      }
      if(newestId > lastSeenTicketId) lastSeenTicketId = newestId;
      watchStarted = true;
    }catch(e){ console.warn('ticket watch failed', e); }
  }

  function startSupervisorTicketWatcher(){
    const u=session();
    if(!u || u.role !== 'supervisor') return;
    ensureTicketNoticeUI();
    updateSupervisorTicketBadge();
    const currentMax = (data.tickets||[]).reduce((m,t)=>Math.max(m, Number(t.id||0)), 0);
    if(!lastSeenTicketId) lastSeenTicketId = currentMax;
    watchStarted = true;
    if(ticketWatchTimer) clearInterval(ticketWatchTimer);
    ticketWatchTimer = setInterval(()=>pollSupervisorTickets(false), 20000);
  }

  const oldRenderTicketsV13 = window.renderTickets;
  window.renderTickets = function(){
    if(typeof oldRenderTicketsV13 === 'function') oldRenderTicketsV13();
    updateSupervisorTicketBadge();
  };

  const oldShowSupervisorWindowV13 = window.showSupervisorWindow;
  window.showSupervisorWindow = function(id, btn){
    if(typeof oldShowSupervisorWindowV13 === 'function') oldShowSupervisorWindowV13(id, btn);
    if(id === 'supTickets'){
      pollSupervisorTickets(true);
    }
  };

  const oldInitSupervisorV13 = window.initSupervisor;
  window.initSupervisor = async function(){
    await oldInitSupervisorV13();
    ensureTicketNoticeUI();
    updateSupervisorTicketBadge();
    startSupervisorTicketWatcher();
  };
})();

/* ===== V15: Technician ticket workspace ===== */
(function(){
  function tNo(t){ return t.ticket_number || ('T-' + String(t.id||0).padStart(4,'0')); }
  function statusLabel(status){ return status==='closed'?'مغلق':(status==='processing'?'تحت المعالجة':'مفتوح'); }
  function priorityLabel(p){ return p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي')); }
  function shortText(s, n=80){ s=String(s||''); return s.length>n ? esc(s.slice(0,n))+'…' : esc(s||'-'); }
  function d(v){ const x=v?new Date(v):null; return x&&!isNaN(x)?x:null; }
  function between(a,b){ const da=d(a), db=d(b); if(!da||!db)return 0; return Math.max(0, Math.round((db-da)/60000)); }
  function dur(min){ min=Number(min||0); if(!min)return '0د'; const day=Math.floor(min/1440), h=Math.floor((min%1440)/60), m=min%60; const arr=[]; if(day)arr.push(day+'ي'); if(h)arr.push(h+'س'); if(m||!arr.length)arr.push(m+'د'); return arr.join(' '); }
  function openMins(t){ return t.status==='closed' ? (Number(t.open_duration_minutes||0)||between(t.created_at,t.closed_at)) : between(t.created_at,new Date().toISOString()); }
  function rowClass(t){ if(t.status==='closed') return 'ticket-row-closed'; if(t.status==='processing') return 'ticket-row-processing'; if(t.priority==='urgent'||t.priority==='high') return 'ticket-row-urgent'; return 'ticket-row-normal'; }
  function badge(t){ const cls=t.status==='closed'?'green':(t.status==='processing'?'amber':((t.priority==='urgent'||t.priority==='high')?'red':'pink')); return `<span class="badge ${cls}">${statusLabel(t.status)}</span>`; }
  function pri(t){ const cls=t.priority==='urgent'?'red':(t.priority==='high'?'amber':'pink'); return `<span class="badge ${cls}">${priorityLabel(t.priority)}</span>`; }
  function currentTechName(){ const u=session(); return (u && (u.full_name || u.username)) || 'فني'; }
  function filterRows(kind){
    const u=session(); if(!u) return [];
    let rows=[...(data.tickets||[])];
    const q=($('techTicketSearch')?.value||'').trim().toLowerCase();
    const st=$('techTicketStatus')?.value||'';
    if(st) rows=rows.filter(t=>t.status===st);
    if(q) rows=rows.filter(t=>[tNo(t),t.title,t.description,projectName(t.project_id),statusLabel(t.status),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));
    if(kind==='open') rows=rows.filter(t=>t.status!=='closed' && !t.claimed_by);
    if(kind==='mine') rows=rows.filter(t=>String(t.claimed_by||'')===String(u.id) && t.status!=='closed');
    if(kind==='done') rows=rows.filter(t=>String(t.closed_by||'')===String(u.id) || (t.status==='closed' && String(t.closed_by_name||'')===String(currentTechName())));
    return rows.sort((a,b)=>new Date(b.created_at||0)-new Date(a.created_at||0));
  }
  function renderTechList(kind, bodyId){
    const b=$(bodyId); if(!b) return;
    const rows=filterRows(kind);
    b.innerHTML = rows.map(t=>`<tr class="${rowClass(t)}"><td><b>${esc(tNo(t))}</b></td><td>${esc(projectName(t.project_id))}</td><td>${esc(t.title||'-')}</td><td style="white-space:normal;min-width:180px">${shortText(t.description)}</td><td>${pri(t)}</td><td>${badge(t)}</td><td>${esc(dur(openMins(t)))}</td><td>${esc(t.claimed_by_name||'-')}<br><small>${t.claimed_at?fmt(t.claimed_at):''}</small></td><td>${esc(t.closed_by_name||'-')}<br><small>${t.closed_at?fmt(t.closed_at):''}</small></td><td style="white-space:normal;min-width:180px">${shortText(t.closure_note)}</td><td class="row-actions">${t.status==='closed'?'':`${!t.claimed_by?`<button onclick="techClaimTicket(${t.id})">استلام</button>`:''}<button onclick="techCloseTicket(${t.id})">إغلاق</button>`}</td></tr>`).join('') || '<tr><td colspan="11">لا توجد تكتات</td></tr>';
  }
  window.renderTechnicianTickets = function(){ renderTechList('open','techOpenTicketsBody'); renderTechList('mine','techMyTicketsBody'); renderTechList('done','techDoneTicketsBody'); updateTechKpis(); };
  function updateTechKpis(){ const u=session(); if(!$('techOpenCount')) return; $('techOpenCount').textContent=(data.tickets||[]).filter(t=>t.status!=='closed'&&!t.claimed_by).length; $('techMineCount').textContent=(data.tickets||[]).filter(t=>String(t.claimed_by||'')===String(u?.id||'')&&t.status!=='closed').length; $('techDoneCount').textContent=(data.tickets||[]).filter(t=>String(t.closed_by||'')===String(u?.id||'')).length; }
  window.showTechWindow = function(id, btn){ document.querySelectorAll('.tech-page').forEach(p=>p.classList.remove('active')); $(id)?.classList.add('active'); document.querySelectorAll('.tech-tab').forEach(b=>b.classList.remove('active')); btn?.classList.add('active'); renderTechnicianTickets(); };
  window.initTechnician = async function(){ const u=requireRole('technician'); if(!u) return; await loadAll(); if($('techTitle')) $('techTitle').textContent='لوحة الفني - '+(u.full_name||u.username); renderTechnicianTickets(); setInterval(async()=>{ await loadAll(); renderTechnicianTickets(); }, 20000); };
  window.techClaimTicket = async function(id){ const u=session(); if(!u) return msg('سجل الدخول أولاً','err'); const t=(data.tickets||[]).find(x=>String(x.id)===String(id)); if(!t) return msg('التكت غير موجود','err'); if(t.status==='closed') return msg('التكت مغلق','err'); if(t.claimed_by && String(t.claimed_by)!==String(u.id)) return msg('هذا التكت مستلم بواسطة '+(t.claimed_by_name||'شخص آخر'),'err'); const now=new Date().toISOString(); const name=currentTechName(); const {error}=await sb.from('tickets').update({status:'processing',claimed_by:u.id,claimed_by_name:name,claimed_at:t.claimed_at||now,updated_at:now}).eq('id',id); if(error) return msg(error.message,'err'); playAppSound('ticket'); msg('تم استلام التكت بواسطة '+name); await loadAll(); renderTechnicianTickets(); };
  window.techCloseTicket = async function(id){ const u=session(); if(!u) return msg('سجل الدخول أولاً','err'); const t=(data.tickets||[]).find(x=>String(x.id)===String(id)); if(!t) return msg('التكت غير موجود','err'); if(t.status==='closed') return msg('التكت مغلق بالفعل','err'); const note=prompt('كيف تم إغلاق التكت؟\nاكتب الإجراء المنفذ بالتفصيل'); if(!note || !note.trim()) return msg('لا يمكن إغلاق التكت بدون ذكر كيف تم الإغلاق','err'); const now=new Date().toISOString(); const name=currentTechName(); const row={status:'closed',closed_at:now,closed_by:u.id,closed_by_name:name,closure_note:note.trim(),open_duration_minutes:between(t.created_at,now),processing_duration_minutes:t.claimed_at?between(t.claimed_at,now):null,updated_at:now}; if(!t.claimed_at){ row.claimed_by=u.id; row.claimed_by_name=name; row.claimed_at=now; } const {error}=await sb.from('tickets').update(row).eq('id',id); if(error) return msg(error.message,'err'); playAppSound('ticket'); msg('تم إغلاق التكت وحفظ طريقة الإغلاق'); await loadAll(); renderTechnicianTickets(); };
})();

/* ===== V15.1: Supervisor window buttons fix ===== */
window.showSupervisorWindow = function(id, btn){
  document.querySelectorAll('.sup-page').forEach(p => p.classList.remove('active'));
  const page = document.getElementById(id);
  if(page) page.classList.add('active');
  document.querySelectorAll('.sup-tab').forEach(b => b.classList.remove('active'));
  if(btn) btn.classList.add('active');
  if(id === 'supTickets' && typeof renderTickets === 'function') renderTickets();
  if(id === 'supSummary' && typeof renderSupervisorDailySummary === 'function') renderSupervisorDailySummary();
};

/* ===== V15.2: Fix supervisor daily summary ===== */
(function(){
  function parseDateSafe(v){
    if(!v) return null;
    const d = new Date(v);
    return isNaN(d) ? null : d;
  }
  function dateOfLog(l){ return l.log_date || String(l.check_in || '').slice(0,10) || ''; }
  function sameDate(a,b){ return String(a||'') === String(b||''); }
  function addDays(dateObj, days){ const d = new Date(dateObj); d.setDate(d.getDate()+days); return d; }
  function localDateStr(d){
    const y=d.getFullYear();
    const m=String(d.getMonth()+1).padStart(2,'0');
    const day=String(d.getDate()).padStart(2,'0');
    return `${y}-${m}-${day}`;
  }
  function supervisorSummaryRange(){
    const mode = document.getElementById('summaryRange')?.value || 'today';
    const custom = document.getElementById('summaryDate')?.value;
    const now = new Date();
    const todayStr = localDateStr(now);
    if(mode === 'custom'){
      const d = custom || todayStr;
      return {start:d, end:d, label:d};
    }
    if(mode === 'yesterday'){
      const d = localDateStr(addDays(now,-1));
      return {start:d, end:d, label:'أمس'};
    }
    if(mode === 'week'){
      const start = localDateStr(addDays(now,-6));
      return {start, end:todayStr, label:'آخر 7 أيام'};
    }
    return {start:todayStr, end:todayStr, label:'اليوم'};
  }
  function inRangeDate(ds, range){ return ds && ds >= range.start && ds <= range.end; }
  function ticketStatusClosed(t){ return (t.status || 'open') === 'closed'; }
  function durationText(mins){
    mins = Number(mins || 0);
    const h = Math.floor(mins / 60), m = mins % 60;
    if(h && m) return `${h}س ${m}د`;
    if(h) return `${h}س`;
    return `${m}د`;
  }
  function summaryCard(label, value){
    return `<div class="summary-card-mini"><small>${esc(label)}</small><b>${esc(value)}</b></div>`;
  }
  window.renderSupervisorDailySummary = function(){
    const cards = document.getElementById('supervisorSummaryCards');
    const body = document.getElementById('supervisorSummaryBody');
    if(!cards && !body) return;
    const u = session && session();
    const range = supervisorSummaryRange();
    if(document.getElementById('summaryRange')?.value !== 'custom' && document.getElementById('summaryDate')){
      // Keep custom field available but do not override it every time.
    }

    const logs = (data.logs || []).filter(l => {
      const ds = dateOfLog(l);
      const bySupervisor = !u || !l.supervisor_id || String(l.supervisor_id) === String(u.id);
      return bySupervisor && inRangeDate(ds, range);
    });
    const workMinutes = logs.reduce((sum,l)=>{
      const saved = Number(l.duration_minutes || 0);
      const calc = (typeof minutesBetween === 'function') ? minutesBetween(l.check_in, l.check_out) : 0;
      return sum + (saved > 0 ? saved : calc);
    },0);
    const travelMinutes = logs.reduce((sum,l)=> sum + Number(l.travel_minutes || 0), 0);

    const attendance = (data.attendance || []).filter(a => {
      const ds = a.attendance_date || String(a.created_at || '').slice(0,10);
      const bySupervisor = !u || !a.supervisor_id || String(a.supervisor_id) === String(u.id);
      return bySupervisor && inRangeDate(ds, range);
    });
    const present = attendance.filter(a => a.status === 'present').length;
    const absent = attendance.filter(a => a.status === 'absent').length;

    const tickets = (data.tickets || []).filter(t => {
      const ds = String(t.created_at || '').slice(0,10);
      return inRangeDate(ds, range);
    });
    const openTickets = tickets.filter(t => !ticketStatusClosed(t)).length;
    const closedTickets = tickets.filter(ticketStatusClosed).length;

    if(cards){
      cards.innerHTML = [
        summaryCard('عدد التسجيلات', logs.length),
        summaryCard('ساعات العمل', durationText(workMinutes)),
        summaryCard('وقت التنقل', durationText(travelMinutes)),
        summaryCard('الحضور', present),
        summaryCard('الغياب', absent),
        summaryCard('التكتات المفتوحة', openTickets),
        summaryCard('التكتات المغلقة', closedTickets),
        summaryCard('الفترة', range.label)
      ].join('');
    }
    if(body){
      const rows = [
        ['عدد تسجيلات الدخول والخروج', logs.length],
        ['إجمالي ساعات العمل', durationText(workMinutes)],
        ['إجمالي وقت التنقل', durationText(travelMinutes)],
        ['عدد الحضور', present],
        ['عدد الغياب', absent],
        ['التكتات المفتوحة', openTickets],
        ['التكتات المغلقة', closedTickets]
      ];
      body.innerHTML = rows.map(r=>`<tr><td>${esc(r[0])}</td><td>${esc(r[1])}</td></tr>`).join('');
    }
  };

  const prevShowSupervisorWindowV152 = window.showSupervisorWindow;
  window.showSupervisorWindow = function(id, btn){
    if(typeof prevShowSupervisorWindowV152 === 'function') prevShowSupervisorWindowV152(id, btn);
    if(id === 'supSummary') window.renderSupervisorDailySummary();
  };

  const prevInitSupervisorV152 = window.initSupervisor;
  window.initSupervisor = async function(){
    if(typeof prevInitSupervisorV152 === 'function') await prevInitSupervisorV152();
    if(document.getElementById('supSummary')?.classList.contains('active')) window.renderSupervisorDailySummary();
  };
})();

/* V18 عقود المشاريع وتنبيهات نهاية العقد */
(function(){
  const $safe = (id)=>document.getElementById(id);
  function isoDate(d){ if(!d) return ''; return String(d).slice(0,10); }
  function parseDateOnly(s){ if(!s) return null; const parts=String(s).slice(0,10).split('-').map(Number); if(parts.length!==3||!parts[0]) return null; return new Date(parts[0], parts[1]-1, parts[2]); }
  function daysLeft(end){ const e=parseDateOnly(end); if(!e) return null; const t=new Date(); const today=new Date(t.getFullYear(),t.getMonth(),t.getDate()); return Math.ceil((e-today)/86400000); }
  function contractInfo(p){
    const d=daysLeft(p.contract_end);
    if(d===null) return {key:'missing', text:'بيانات ناقصة', cls:'amber', days:'-'};
    if(d < 0) return {key:'expired', text:'منتهي', cls:'red', days:'منتهي'};
    if(d <= 30) return {key:'soon', text:'قريب الانتهاء', cls:'amber', days:d + ' يوم'};
    return {key:'active', text:'نشط', cls:'green', days:d + ' يوم'};
  }
  function esc2(x){ try{return typeof esc==='function'?esc(x):String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));}catch(_){return x||'';} }
  function badgeContract(p){ const c=contractInfo(p); return `<span class="badge ${c.cls}">${c.text}</span>`; }


  async function recalcProjectDailyLogs(projectId, projectRow){
    projectId = Number(projectId || 0);
    if(!projectId || !projectRow) return {updated:0, failed:0};
    const q = await sb.from('time_logs').select('*').eq('project_id', projectId);
    if(q.error){ console.warn(q.error.message); return {updated:0, failed:1}; }
    const logs = q.data || [];
    let updated = 0, failed = 0;
    for(const l of logs){
      const logDate = (l.log_date || String(l.check_in||'').slice(0,10));
      const day = logDate ? new Date(logDate+'T00:00:00').getDay() : null;
      const required = day === 5 ? Number(projectRow.friday_minutes || 0) : Number(projectRow.required_daily_minutes || 0);
      const actual = Number(l.duration_minutes || minutesBetween(l.check_in, l.check_out));
      const ts = calcTimeStatus(actual, required);
      const upd = {
        required_minutes: required,
        duration_minutes: actual,
        time_difference_minutes: ts.diff,
        time_status: ts.status
      };
      const r = await sb.from('time_logs').update(upd).eq('id', l.id);
      if(r.error){ failed++; console.warn(r.error.message); } else updated++;
    }
    return {updated, failed};
  }

  const oldSaveProject = window.saveProject;
  window.saveProject = async function(){
    const id=$safe('projectId')?.value;
    const row={
      name:$safe('projectName')?.value?.trim(),
      location:$safe('projectLocation')?.value?.trim(),
      supervisor_id:Number($safe('projectSupervisor')?.value)||null,
      buildings_count:Number($safe('projectBuildingsCount')?.value)||0,
      units_count:Number($safe('projectUnitsCount')?.value)||0,
      contract_start:$safe('projectContractStart')?.value||null,
      contract_end:$safe('projectContractEnd')?.value||null,
      required_daily_minutes:Number($safe('projectRequiredDaily')?.value||180),
      friday_minutes:Number($safe('projectFridayMinutes')?.value||90),
      operation_type:$safe('projectOperationType')?.value||'daily_visit',
      visit_type_default:$safe('projectVisitDefault')?.value||'surface',
      status:$safe('projectStatus')?.value||'active',
      notes:$safe('projectNotes')?.value||''
    };
    if(!row.name) return msg('اسم المشروع مطلوب','err');
    const res=id?await sb.from('projects').update(row).eq('id',id):await sb.from('projects').insert(row);
    if(res.error) return msg(res.error.message,'err');
    if(id){
      const rr = await recalcProjectDailyLogs(id, row);
      msg('تم تحديث المشروع وتحديث الوقت المطلوب في السجلات اليومية' + (rr.updated ? ` (${rr.updated} سجل)` : ''));
    } else {
      msg('تم حفظ المشروع');
    }
    if(typeof clearProjectForm==='function') clearProjectForm();
    await refreshAll();
  };

  const oldEditProject = window.editProject;
  window.editProject = function(id){
    const p=(data.projects||[]).find(x=>String(x.id)===String(id));
    if(!p) return;
    if(typeof oldEditProject==='function') oldEditProject(id);
    if($safe('projectBuildingsCount')) $safe('projectBuildingsCount').value=p.buildings_count||0;
    if($safe('projectUnitsCount')) $safe('projectUnitsCount').value=p.units_count||0;
    if($safe('projectContractStart')) $safe('projectContractStart').value=isoDate(p.contract_start);
    if($safe('projectContractEnd')) $safe('projectContractEnd').value=isoDate(p.contract_end);
  };

  const oldClearProjectForm = window.clearProjectForm;
  window.clearProjectForm = function(){
    if(typeof oldClearProjectForm==='function') oldClearProjectForm();
    if($safe('projectBuildingsCount')) $safe('projectBuildingsCount').value=0;
    if($safe('projectUnitsCount')) $safe('projectUnitsCount').value=0;
    if($safe('projectContractStart')) $safe('projectContractStart').value='';
    if($safe('projectContractEnd')) $safe('projectContractEnd').value='';
  };

  window.renderContractAlerts = function(){
    const projects=data.projects||[];
    const relevant=projects.map(p=>({p,c:contractInfo(p)})).filter(x=>['soon','expired','missing'].includes(x.c.key));
    const html = relevant.sort((a,b)=>{
      const da=daysLeft(a.p.contract_end); const db=daysLeft(b.p.contract_end);
      return (da??99999)-(db??99999);
    }).map(x=>`<div class="alert-item ${x.c.key==='expired'?'danger':'warn'}"><b>${esc2(x.p.name)}</b><br>نهاية العقد: ${esc2(isoDate(x.p.contract_end)||'-')}<br>المتبقي: ${x.c.days} - ${x.c.text}</div>`).join('') || '<div class="alert-item">لا توجد عقود قريبة الانتهاء خلال 30 يوم</div>';
    if($safe('contractDashboardAlerts')) $safe('contractDashboardAlerts').innerHTML=html;
    if($safe('contractsAlertsList')) $safe('contractsAlertsList').innerHTML=html;
  };

  window.renderContracts = function(){
    const body=$safe('contractsBody');
    if(!body) return;
    const q=($safe('contractSearch')?.value||'').trim();
    const st=$safe('contractFilterStatus')?.value||'';
    let rows=[...(data.projects||[])];
    if(q) rows=rows.filter(p=>String(p.name||'').includes(q));
    if(st) rows=rows.filter(p=>contractInfo(p).key===st);
    rows.sort((a,b)=>{ const da=daysLeft(a.contract_end); const db=daysLeft(b.contract_end); return (da??999999)-(db??999999); });
    body.innerHTML=rows.map(p=>{ const c=contractInfo(p); return `<tr><td><b>${esc2(p.name)}</b></td><td>${p.buildings_count||0}</td><td>${p.units_count||0}</td><td>${esc2(isoDate(p.contract_start)||'-')}</td><td>${esc2(isoDate(p.contract_end)||'-')}</td><td>${c.days}</td><td><span class="badge ${c.cls}">${c.text}</span></td><td><button onclick="showPage('projects', document.querySelector(\`.nav[onclick*=projects]\`)); setTimeout(()=>editProject(${p.id}),50)">تعديل</button></td></tr>`; }).join('') || '<tr><td colspan="8">لا توجد بيانات</td></tr>';
    if($safe('contractsActiveCount')) $safe('contractsActiveCount').textContent=(data.projects||[]).filter(p=>contractInfo(p).key==='active').length;
    if($safe('contractsSoonCount')) $safe('contractsSoonCount').textContent=(data.projects||[]).filter(p=>contractInfo(p).key==='soon').length;
    if($safe('contractsExpiredCount')) $safe('contractsExpiredCount').textContent=(data.projects||[]).filter(p=>contractInfo(p).key==='expired').length;
    if($safe('contractsMissingCount')) $safe('contractsMissingCount').textContent=(data.projects||[]).filter(p=>contractInfo(p).key==='missing').length;
  };

  window.renderProjects = function(){
    const b=$safe('projectsBody'); if(!b) return;
    const q=($safe('projectSearch')?.value||'').trim(), sid=$safe('projectFilterSupervisor')?.value, st=$safe('projectFilterStatus')?.value;
    let rows=data.projects||[];
    if(q) rows=rows.filter(p=>[p.name,p.location,supervisorName(p.supervisor_id),p.notes].join(' ').includes(q));
    if(sid) rows=rows.filter(p=>String(p.supervisor_id)===String(sid));
    if(st) rows=rows.filter(p=>(p.status||'active')===st);
    b.innerHTML=rows.map(p=>{ const c=contractInfo(p); return `<tr><td><b>${esc2(p.name)}</b><br><small>${esc2(p.location||'')}</small></td><td>${esc2(supervisorName(p.supervisor_id))}</td><td>${p.buildings_count||0}</td><td>${p.units_count||0}</td><td>${esc2(isoDate(p.contract_end)||'-')}</td><td>${c.days}</td><td><span class="badge ${c.cls}">${c.text}</span></td><td>${minsToText(p.required_daily_minutes??180)}</td><td><span class="badge ${p.status==='inactive'?'red':'green'}">${p.status==='inactive'?'متوقف':'نشط'}</span></td><td class="row-actions"><button onclick="editProject(${p.id})">تعديل</button><button class="light" onclick="openProjectManager(${p.id})">إدارة المشروع</button><button class="light" onclick="toggleProjectStatus(${p.id})">${p.status==='inactive'?'تفعيل':'إيقاف'}</button><button class="danger" onclick="deleteRow('projects',${p.id})">حذف</button></td></tr>`; }).join('')||'<tr><td colspan="10">لا توجد بيانات</td></tr>';
    if(typeof renderProjectManager==='function') renderProjectManager();
    renderContracts(); renderContractAlerts();
  };

  const oldRenderDashboard = window.renderDashboard;
  window.renderDashboard = function(){ if(typeof oldRenderDashboard==='function') oldRenderDashboard(); renderContractAlerts(); };
  const oldRenderAll = window.renderAll;
  window.renderAll = function(){ if(typeof oldRenderAll==='function') oldRenderAll(); renderContracts(); renderContractAlerts(); };
})();

/* ===== V42: Clear WhatsApp column for tickets ===== */
(function(){
  function _e(v){return (typeof esc==='function'?esc(v):String(v??''));}
  function _no(t){return t.ticket_number || ('T-' + String(t.id||0).padStart(4,'0'));}
  function _status(s){return s==='closed'?'مغلق':(s==='processing'?'تحت المعالجة':'مفتوح');}
  function _priority(p){return p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي'));}
  function _short(s,n=90){s=String(s||'');return s.length>n?_e(s.slice(0,n))+'…':_e(s||'-');}
  function _date(v){const d=v?new Date(v):new Date();const x=isNaN(d)?new Date():d;return x.getFullYear()+'-'+String(x.getMonth()+1).padStart(2,'0')+'-'+String(x.getDate()).padStart(2,'0');}
  function _time(v){const d=v?new Date(v):new Date();const x=isNaN(d)?new Date():d;let h=x.getHours();const m=String(x.getMinutes()).padStart(2,'0');const ap=h>=12?'م':'ص';h=h%12||12;return String(h).padStart(2,'0')+':'+m+' '+ap;}
  function _between(a,b){const da=a?new Date(a):null,db=b?new Date(b):null;if(!da||!db||isNaN(da)||isNaN(db))return 0;return Math.max(0,Math.round((db-da)/60000));}
  function _dur(min){min=Number(min||0);if(!min)return '0د';const d=Math.floor(min/1440),h=Math.floor((min%1440)/60),m=min%60;const p=[];if(d)p.push(d+'ي');if(h)p.push(h+'س');if(m||!p.length)p.push(m+'د');return p.join(' ');}
  function _openMins(t){return t.status==='closed'?(Number(t.open_duration_minutes||0)||_between(t.created_at,t.closed_at)):_between(t.created_at,new Date().toISOString());}
  function _row(t){if(t.status==='closed')return 'ticket-row-closed';if(t.status==='processing')return 'ticket-row-processing';if(t.priority==='urgent'||t.priority==='high')return 'ticket-row-urgent';return 'ticket-row-normal';}
  function _sb(t){const cls=t.status==='closed'?'green':(t.status==='processing'?'amber':((t.priority==='urgent'||t.priority==='high')?'red':'pink'));return `<span class="badge ${cls}">${_status(t.status)}</span>`;}
  function _pb(t){const cls=t.priority==='urgent'?'red':(t.priority==='high'?'amber':'pink');return `<span class="badge ${cls}">${_priority(t.priority)}</span>`;}
  function _proj(t){return (typeof projectName==='function'?projectName(t.project_id):'') || t.project_name || '-';}
  function _problem(t){return t.description || t.title || '-';}
  window.buildTicketWhatsAppTextV42=function(t){const closed=t.status==='closed';const v=closed?(t.closed_at||t.updated_at):t.created_at;return [(closed?'تم إغلاق التكت':'تم تسجيل تكت جديد'),'','اسم المشروع: '+_proj(t),'رقم التكت: '+_no(t),'وصف المشكلة: '+_problem(t),'حالة المشكلة: '+(closed?'مغلق':_status(t.status)),'التاريخ: '+_date(v),'الوقت: '+_time(v)].join('\n');};
  function _copy(text){if(navigator.clipboard&&navigator.clipboard.writeText)return navigator.clipboard.writeText(text).catch(()=>{});const ta=document.createElement('textarea');ta.value=text;ta.style.position='fixed';ta.style.opacity='0';document.body.appendChild(ta);ta.select();try{document.execCommand('copy');}catch(e){}ta.remove();return Promise.resolve();}
  window.sendTicketWhatsApp=function(id){const t=(data.tickets||[]).find(x=>String(x.id)===String(id));if(!t)return msg('التكت غير موجود','err');const text=window.buildTicketWhatsAppTextV42(t);_copy(text).finally(()=>{window.open('https://wa.me/?text='+encodeURIComponent(text),'_blank');msg(t.status==='closed'?'تم تجهيز رسالة إغلاق التكت للواتساب':'تم تجهيز رسالة فتح التكت للواتساب');});};
  function _wab(t){return `<button class="wa-ticket-btn-v42" onclick="sendTicketWhatsApp(${t.id})">واتساب<br><small>${t.status==='closed'?'إغلاق التكت':'فتح التكت'}</small></button>`;}
  window.renderTickets=function(){const adminBody=$('ticketsBody'),supBody=$('supTicketsBody');if(!adminBody&&!supBody)return;const rows=[...(data.tickets||[])];
    if(adminBody){const st=$('ticketFilterStatus')?.value||'',q=($('ticketSearch')?.value||'').trim().toLowerCase();let list=rows;if(st)list=list.filter(t=>t.status===st);if(q)list=list.filter(t=>[_no(t),t.title,t.description,_proj(t),supervisorName(t.supervisor_id),_status(t.status),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));adminBody.innerHTML=list.map(t=>`<tr class="${_row(t)}"><td><b>${_e(_no(t))}</b></td><td>${_e(_proj(t))}</td><td>${_e(supervisorName(t.supervisor_id))}</td><td>${_e(t.title||'-')}</td><td style="white-space:normal;min-width:220px">${_short(t.description)}</td><td>${_pb(t)}</td><td>${_sb(t)}</td><td>${fmt(t.created_at)}</td><td>${_e(_dur(_openMins(t)))}</td><td>${_e(t.claimed_by_name||'-')}<br><small>${t.claimed_at?fmt(t.claimed_at):''}</small></td><td>${_e(t.closed_by_name||'-')}<br><small>${t.closed_at?fmt(t.closed_at):''}</small></td><td style="white-space:normal;min-width:220px">${_short(t.closure_note)}</td><td class="whatsapp-col">${_wab(t)}</td><td class="row-actions"><button onclick="editTicket(${t.id})">تعديل</button>${t.status==='closed'?`<button class="light" onclick="setTicketStatus(${t.id},'open')">إعادة فتح</button>`:`${t.status!=='processing'?`<button class="light" onclick="claimTicket(${t.id})">استلام</button>`:''}<button onclick="closeTicket(${t.id})">إغلاق</button>`}<button class="danger" onclick="deleteRow('tickets',${t.id})">حذف</button></td></tr>`).join('')||'<tr><td colspan="14">لا توجد تكتات</td></tr>';}
    if(supBody){const st=$('supTicketFilterStatus')?.value||'',pid=$('supTicketFilterProject')?.value||'',q=($('supTicketSearch')?.value||'').trim().toLowerCase();let list=rows;if(pid)list=list.filter(t=>String(t.project_id)===String(pid));if(st)list=list.filter(t=>t.status===st);if(q)list=list.filter(t=>[_no(t),t.title,t.description,_proj(t),_status(t.status),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));supBody.innerHTML=list.map(t=>`<tr class="${_row(t)}"><td><b>${_e(_no(t))}</b></td><td>${_e(_proj(t))}</td><td>${_e(t.title||'-')}</td><td style="white-space:normal;min-width:180px">${_short(t.description)}</td><td>${_pb(t)}</td><td>${_sb(t)}</td><td>${_e(_dur(_openMins(t)))}</td><td>${_e(t.claimed_by_name||'-')}</td><td>${_e(t.closed_by_name||'-')}</td><td style="white-space:normal;min-width:180px">${_short(t.closure_note)}</td><td class="whatsapp-col">${_wab(t)}</td><td class="row-actions"><button onclick="editTicket(${t.id})">تعديل</button>${t.status==='closed'?`<button class="light" onclick="setTicketStatus(${t.id},'open')">إعادة فتح</button>`:`${t.status!=='processing'?`<button class="light" onclick="claimTicket(${t.id})">استلام</button>`:''}<button onclick="closeTicket(${t.id})">إغلاق</button>`}</td></tr>`).join('')||'<tr><td colspan="12">لا توجد تكتات</td></tr>';}
  };
  const css=document.createElement('style');css.textContent='.wa-ticket-btn-v42{background:#128C7E!important;color:white!important;border:0!important;border-radius:10px!important;padding:8px 10px!important;line-height:1.25!important;min-width:110px!important;font-weight:700!important}.wa-ticket-btn-v42 small{font-weight:500;font-size:10px;color:white!important}.whatsapp-col{white-space:nowrap;text-align:center!important}';document.head.appendChild(css);
})();

/* ===== V43: Force WhatsApp button in supervisor tickets too ===== */
(function(){
  function _safeTicketNo(t){return t.ticket_number || ('T-' + String(t.id||0).padStart(4,'0'));}
  function _statusLabel43(s){return s==='closed'?'مغلق':(s==='processing'?'تحت المعالجة':'مفتوح');}
  function _date43(v){try{const d=v?new Date(v):new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');}catch(e){return '';}}
  function _time43(v){try{const d=v?new Date(v):new Date();return d.toLocaleTimeString('ar-SA',{hour:'2-digit',minute:'2-digit'});}catch(e){return '';}}
  function _project43(t){try{return projectName(t.project_id)||t.project_name||'-';}catch(e){return t.project_name||'-';}}
  function _msg43(t){const closed=t.status==='closed';const dt=closed?(t.closed_at||t.updated_at||t.created_at):(t.created_at||t.updated_at);return [closed?'تم إغلاق التكت':'تم تسجيل تكت جديد','',`اسم المشروع: ${_project43(t)}`,`رقم التكت: ${_safeTicketNo(t)}`,`وصف المشكلة: ${t.description||t.title||'-'}`,`حالة المشكلة: ${closed?'مغلق':_statusLabel43(t.status)}`,`التاريخ: ${_date43(dt)}`,`الوقت: ${_time43(dt)}`].join('\n');}
  window.sendTicketWhatsAppV43=function(id){const t=(data.tickets||[]).find(x=>String(x.id)===String(id)); if(!t){ if(typeof msg==='function') msg('التكت غير موجود','err'); return; } const txt=_msg43(t); const go=function(){window.open('https://wa.me/?text='+encodeURIComponent(txt),'_blank'); if(typeof msg==='function') msg(t.status==='closed'?'تم تجهيز رسالة إغلاق التكت للواتساب':'تم تجهيز رسالة فتح التكت للواتساب');}; if(navigator.clipboard&&navigator.clipboard.writeText) navigator.clipboard.writeText(txt).finally(go); else go();};
  function _btn43(t){return `<button type="button" class="wa-ticket-btn-v43" onclick="sendTicketWhatsAppV43(${t.id})">واتساب<br><small>${t.status==='closed'?'إغلاق التكت':'فتح التكت'}</small></button>`;}
  function forceSupervisorWhatsApp43(){
    const body=document.getElementById('supTicketsBody'); if(!body) return;
    const rows=Array.from(body.querySelectorAll('tr')); if(!rows.length) return;
    rows.forEach(function(tr,i){
      if((tr.innerText||'').includes('لا توجد')) return;
      if(tr.querySelector('.wa-ticket-btn-v43') || tr.querySelector('.wa-ticket-btn-v42')) return;
      const first=tr.cells&&tr.cells[0]?tr.cells[0].innerText.trim():'';
      let t=(data.tickets||[]).find(function(x){return _safeTicketNo(x)===first;});
      if(!t) t=(data.tickets||[])[i];
      if(!t) return;
      const target=tr.cells[10] || tr.cells[tr.cells.length-1];
      if(target){ target.innerHTML=_btn43(t); target.classList.add('whatsapp-col'); }
    });
  }
  const oldRenderTickets43=window.renderTickets;
  window.renderTickets=function(){ if(typeof oldRenderTickets43==='function') oldRenderTickets43.apply(this,arguments); setTimeout(forceSupervisorWhatsApp43,80); setTimeout(forceSupervisorWhatsApp43,400); };
  const oldShowSupervisorWindow43=window.showSupervisorWindow;
  window.showSupervisorWindow=function(id,btn){ if(typeof oldShowSupervisorWindow43==='function') oldShowSupervisorWindow43.apply(this,arguments); if(id==='supTickets') setTimeout(forceSupervisorWhatsApp43,700); };
  window.addEventListener('load',function(){setTimeout(forceSupervisorWhatsApp43,900);setTimeout(forceSupervisorWhatsApp43,1800);});
  const css=document.createElement('style');css.textContent='.wa-ticket-btn-v43{background:#128C7E!important;color:white!important;border:0!important;border-radius:10px!important;padding:8px 10px!important;line-height:1.25!important;min-width:110px!important;font-weight:700!important;cursor:pointer!important}.wa-ticket-btn-v43 small{font-weight:500;font-size:10px;color:white!important}.whatsapp-col{white-space:nowrap;text-align:center!important}';document.head.appendChild(css);
})();


/* ===== V45: FINAL FIX - Supervisor ticket WhatsApp button ===== */
(function(){
  function _v45Esc(v){return String(v??'').replace(/[&<>"']/g,function(m){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m];});}
  function _v45No(t){return t.ticket_number || ('T-' + String(t.id||0).padStart(4,'0'));}
  function _v45Status(s){return s==='closed'?'مغلق':(s==='processing'?'تحت المعالجة':'مفتوح');}
  function _v45Priority(p){return p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي'));}
  function _v45Project(t){try{return projectName(t.project_id)||t.project_name||'-';}catch(e){return t.project_name||'-';}}
  function _v45Short(s,n){s=String(s||'-');return _v45Esc(s.length>(n||90)?s.slice(0,(n||90))+'…':s);}
  function _v45Date(v){try{const d=v?new Date(v):new Date();if(isNaN(d))return '-';return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');}catch(e){return '-';}}
  function _v45Time(v){try{const d=v?new Date(v):new Date();if(isNaN(d))return '-';let h=d.getHours();const m=String(d.getMinutes()).padStart(2,'0');const ap=h>=12?'م':'ص';h=h%12||12;return String(h).padStart(2,'0')+':'+m+' '+ap;}catch(e){return '-';}}
  function _v45Mins(a,b){const da=a?new Date(a):null,db=b?new Date(b):new Date();if(!da||isNaN(da)||isNaN(db))return 0;return Math.max(0,Math.round((db-da)/60000));}
  function _v45Dur(m){m=Number(m||0);if(!m)return '0د';const d=Math.floor(m/1440),h=Math.floor((m%1440)/60),mm=m%60;let arr=[];if(d)arr.push(d+'ي');if(h)arr.push(h+'س');if(mm||!arr.length)arr.push(mm+'د');return arr.join(' ');}
  function _v45OpenM(t){return t.status==='closed'?(Number(t.open_duration_minutes||0)||_v45Mins(t.created_at,t.closed_at)):_v45Mins(t.created_at,new Date().toISOString());}
  function _v45Row(t){if(t.status==='closed')return 'ticket-row-closed';if(t.status==='processing')return 'ticket-row-processing';if(t.priority==='urgent'||t.priority==='high')return 'ticket-row-urgent';return 'ticket-row-normal';}
  function _v45Sb(t){const cls=t.status==='closed'?'green':(t.status==='processing'?'amber':((t.priority==='urgent'||t.priority==='high')?'red':'pink'));return '<span class="badge '+cls+'">'+_v45Status(t.status)+'</span>';}
  function _v45Pb(t){const cls=t.priority==='urgent'?'red':(t.priority==='high'?'amber':'pink');return '<span class="badge '+cls+'">'+_v45Priority(t.priority)+'</span>';}
  function _v45Text(t){const closed=t.status==='closed';const v=closed?(t.closed_at||t.updated_at||t.created_at):(t.created_at||t.updated_at);return [closed?'تم إغلاق التكت':'تم تسجيل تكت جديد','','اسم المشروع: '+_v45Project(t),'رقم التكت: '+_v45No(t),'وصف المشكلة: '+(t.description||t.title||'-'),'حالة المشكلة: '+(closed?'مغلق':_v45Status(t.status)),'التاريخ: '+_v45Date(v),'الوقت: '+_v45Time(v)].join('\n');}
  window.sendSupervisorTicketWhatsAppV45=function(id){const t=(data.tickets||[]).find(x=>String(x.id)===String(id));if(!t){if(typeof msg==='function')msg('التكت غير موجود','err');return;}const text=_v45Text(t);const go=()=>{window.open('https://wa.me/?text='+encodeURIComponent(text),'_blank');if(typeof msg==='function')msg(t.status==='closed'?'تم تجهيز رسالة إغلاق التكت للواتساب':'تم تجهيز رسالة فتح التكت للواتساب');};if(navigator.clipboard&&navigator.clipboard.writeText)navigator.clipboard.writeText(text).finally(go);else go();};
  function _v45Btn(t){return '<button type="button" class="wa-ticket-btn-v45" onclick="sendSupervisorTicketWhatsAppV45('+t.id+')">واتساب<br><small>'+(t.status==='closed'?'إغلاق التكت':'فتح التكت')+'</small></button>';}
  function renderSupervisorTicketsV45(){
    const body=document.getElementById('supTicketsBody'); if(!body) return false;
    let list=[...(data.tickets||[])];
    const st=document.getElementById('supTicketFilterStatus')?.value||'';
    const pid=document.getElementById('supTicketFilterProject')?.value||'';
    const q=(document.getElementById('supTicketSearch')?.value||'').trim().toLowerCase();
    if(pid) list=list.filter(t=>String(t.project_id)===String(pid));
    if(st) list=list.filter(t=>t.status===st);
    if(q) list=list.filter(t=>[_v45No(t),t.title,t.description,_v45Project(t),_v45Status(t.status),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));
    body.innerHTML=list.map(t=>'<tr class="'+_v45Row(t)+'"><td><b>'+_v45Esc(_v45No(t))+'</b></td><td>'+_v45Esc(_v45Project(t))+'</td><td>'+_v45Esc(t.title||'-')+'</td><td style="white-space:normal;min-width:180px">'+_v45Short(t.description,90)+'</td><td>'+_v45Pb(t)+'</td><td>'+_v45Sb(t)+'</td><td>'+_v45Esc(_v45Dur(_v45OpenM(t)))+'</td><td>'+_v45Esc(t.claimed_by_name||'-')+'</td><td>'+_v45Esc(t.closed_by_name||'-')+'</td><td style="white-space:normal;min-width:180px">'+_v45Short(t.closure_note,90)+'</td><td class="whatsapp-col">'+_v45Btn(t)+'</td><td class="row-actions"><button onclick="editTicket('+t.id+')">تعديل</button>'+(t.status==='closed'?'<button class="light" onclick="setTicketStatus('+t.id+',\'open\')">إعادة فتح</button>':((t.status!=='processing'?'<button class="light" onclick="claimTicket('+t.id+')">استلام</button>':'')+'<button onclick="closeTicket('+t.id+')">إغلاق</button>'))+'</td></tr>').join('') || '<tr><td colspan="12">لا توجد تكتات</td></tr>';
    return true;
  }
  const oldRenderTicketsV45=window.renderTickets;
  window.renderTickets=function(){ if(document.getElementById('supTicketsBody')) return renderSupervisorTicketsV45(); if(typeof oldRenderTicketsV45==='function') return oldRenderTicketsV45.apply(this,arguments); };
  const oldShowSupervisorWindowV45=window.showSupervisorWindow;
  window.showSupervisorWindow=function(id,btn){ if(typeof oldShowSupervisorWindowV45==='function') oldShowSupervisorWindowV45.apply(this,arguments); if(id==='supTickets'){setTimeout(renderSupervisorTicketsV45,50);setTimeout(renderSupervisorTicketsV45,300);} };
  const oldInitSupervisorV45=window.initSupervisor;
  window.initSupervisor=async function(){ if(typeof oldInitSupervisorV45==='function') await oldInitSupervisorV45.apply(this,arguments); setTimeout(renderSupervisorTicketsV45,80); setTimeout(renderSupervisorTicketsV45,600); };
  window.renderSupervisorTicketsV45=renderSupervisorTicketsV45;
  const css=document.createElement('style');css.textContent='.wa-ticket-btn-v45{background:#128C7E!important;color:#fff!important;border:0!important;border-radius:10px!important;padding:8px 10px!important;line-height:1.25!important;min-width:110px!important;font-weight:700!important;cursor:pointer!important}.wa-ticket-btn-v45 small{font-weight:500!important;font-size:10px!important;color:#fff!important}.whatsapp-col{text-align:center!important;white-space:nowrap!important}';document.head.appendChild(css);
  window.addEventListener('load',function(){setTimeout(renderSupervisorTicketsV45,1000);setTimeout(renderSupervisorTicketsV45,2200);});
})();


/* ===== V46: Technician can create tickets + WhatsApp buttons ===== */
(function(){
  function _e(v){ return (typeof esc==='function') ? esc(v) : String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])); }
  function _tNo(t){ return t.ticket_number || ('T-' + String(t.id||0).padStart(4,'0')); }
  function _statusLabel(status){ return status==='closed'?'مغلق':(status==='processing'?'تحت المعالجة':'مفتوح'); }
  function _priorityLabel(p){ return p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي')); }
  function _short(s,n=80){ s=String(s||''); return s.length>n ? _e(s.slice(0,n))+'…' : _e(s||'-'); }
  function _dateObj(v){ const x=v?new Date(v):null; return x&&!isNaN(x)?x:null; }
  function _between(a,b){ const da=_dateObj(a), db=_dateObj(b); if(!da||!db)return 0; return Math.max(0, Math.round((db-da)/60000)); }
  function _dur(min){ min=Number(min||0); if(!min)return '0د'; const day=Math.floor(min/1440), h=Math.floor((min%1440)/60), m=min%60; const arr=[]; if(day)arr.push(day+'ي'); if(h)arr.push(h+'س'); if(m||!arr.length)arr.push(m+'د'); return arr.join(' '); }
  function _openMins(t){ return t.status==='closed' ? (Number(t.open_duration_minutes||0)||_between(t.created_at,t.closed_at)) : _between(t.created_at,new Date().toISOString()); }
  function _rowClass(t){ if(t.status==='closed') return 'ticket-row-closed'; if(t.status==='processing') return 'ticket-row-processing'; if(t.priority==='urgent'||t.priority==='high') return 'ticket-row-urgent'; return 'ticket-row-normal'; }
  function _badge(t){ const cls=t.status==='closed'?'green':(t.status==='processing'?'amber':((t.priority==='urgent'||t.priority==='high')?'red':'pink')); return `<span class="badge ${cls}">${_statusLabel(t.status)}</span>`; }
  function _pri(t){ const cls=t.priority==='urgent'?'red':(t.priority==='high'?'amber':'pink'); return `<span class="badge ${cls}">${_priorityLabel(t.priority)}</span>`; }
  function _currentTechName(){ const u=session(); return (u && (u.full_name || u.username)) || 'فني'; }
  function _projectName(id){ return (typeof projectName==='function') ? projectName(id) : ((data.projects||[]).find(p=>String(p.id)===String(id))?.name || '-'); }
  function _fmt(v){ return (typeof fmt==='function') ? fmt(v) : (v ? new Date(v).toLocaleString('ar-SA') : ''); }
  function _waBtn(t){
    const fn = window.sendTicketWhatsAppV43 || window.sendTicketWhatsApp;
    if(!fn) return '-';
    return `<button type="button" class="wa-ticket-btn-v46" onclick="${fn===window.sendTicketWhatsAppV43?'sendTicketWhatsAppV43':'sendTicketWhatsApp'}(${t.id})">واتساب<br><small>${t.status==='closed'?'إغلاق التكت':'فتح التكت'}</small></button>`;
  }
  function _filterTechRows(kind){
    const u=session(); if(!u) return [];
    let rows=[...(data.tickets||[])];
    const q=($('techTicketSearch')?.value||'').trim().toLowerCase();
    const st=$('techTicketStatus')?.value||'';
    if(st) rows=rows.filter(t=>t.status===st);
    if(q) rows=rows.filter(t=>[_tNo(t),t.title,t.description,_projectName(t.project_id),_statusLabel(t.status),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));
    if(kind==='open') rows=rows.filter(t=>t.status!=='closed' && !t.claimed_by);
    if(kind==='mine') rows=rows.filter(t=>String(t.claimed_by||'')===String(u.id) && t.status!=='closed');
    if(kind==='done') rows=rows.filter(t=>String(t.closed_by||'')===String(u.id) || (t.status==='closed' && String(t.closed_by_name||'')===String(_currentTechName())));
    return rows.sort((a,b)=>new Date(b.created_at||0)-new Date(a.created_at||0));
  }
  function _renderTechList(kind, bodyId){
    const b=$(bodyId); if(!b) return;
    const rows=_filterTechRows(kind);
    b.innerHTML = rows.map(t=>`<tr class="${_rowClass(t)}"><td><b>${_e(_tNo(t))}</b></td><td>${_e(_projectName(t.project_id))}</td><td>${_e(t.title||'-')}</td><td style="white-space:normal;min-width:180px">${_short(t.description)}</td><td>${_pri(t)}</td><td>${_badge(t)}</td><td>${_e(_dur(_openMins(t)))}</td><td>${_e(t.claimed_by_name||'-')}<br><small>${t.claimed_at?_fmt(t.claimed_at):''}</small></td><td>${_e(t.closed_by_name||'-')}<br><small>${t.closed_at?_fmt(t.closed_at):''}</small></td><td style="white-space:normal;min-width:180px">${_short(t.closure_note)}</td><td class="whatsapp-col">${_waBtn(t)}</td><td class="row-actions">${t.status==='closed'?'':`${!t.claimed_by?`<button onclick="techClaimTicket(${t.id})">استلام</button>`:''}<button onclick="techCloseTicket(${t.id})">إغلاق</button>`}</td></tr>`).join('') || '<tr><td colspan="12">لا توجد تكتات</td></tr>';
  }
  function _updateTechKpisV46(){ const u=session(); if(!$('techOpenCount')) return; $('techOpenCount').textContent=(data.tickets||[]).filter(t=>t.status!=='closed'&&!t.claimed_by).length; $('techMineCount').textContent=(data.tickets||[]).filter(t=>String(t.claimed_by||'')===String(u?.id||'')&&t.status!=='closed').length; $('techDoneCount').textContent=(data.tickets||[]).filter(t=>String(t.closed_by||'')===String(u?.id||'')).length; }
  window.renderTechnicianTickets = function(){ _renderTechList('open','techOpenTicketsBody'); _renderTechList('mine','techMyTicketsBody'); _renderTechList('done','techDoneTicketsBody'); _updateTechKpisV46(); };
  window.clearTechnicianTicketForm = function(){
    if($('techNewTicketProject')) $('techNewTicketProject').value='';
    if($('techNewTicketPriority')) $('techNewTicketPriority').value='normal';
    if($('techNewTicketTitle')) $('techNewTicketTitle').value='';
    if($('techNewTicketDescription')) $('techNewTicketDescription').value='';
  };
  window.saveTechnicianTicket = async function(){
    const u=session(); if(!u) return msg('سجل الدخول أولاً','err');
    const projectId=Number($('techNewTicketProject')?.value)||null;
    const title=($('techNewTicketTitle')?.value||'').trim();
    const description=($('techNewTicketDescription')?.value||'').trim();
    const priority=$('techNewTicketPriority')?.value || 'normal';
    if(!projectId) return msg('اختر المشروع','err');
    if(!title) return msg('عنوان المشكلة مطلوب','err');
    const proj=(data.projects||[]).find(p=>String(p.id)===String(projectId));
    const now=new Date().toISOString();
    const row={
      project_id: projectId,
      supervisor_id: Number(proj?.supervisor_id)||null,
      created_by: u.id,
      title,
      description,
      priority,
      status:'open',
      updated_at: now
    };
    const res=await sb.from('tickets').insert(row).select('*').single();
    if(res.error) return msg(res.error.message,'err');
    if(res.data && !res.data.ticket_number){
      const tn='T-'+String(res.data.id).padStart(4,'0');
      await sb.from('tickets').update({ticket_number:tn}).eq('id',res.data.id);
      res.data.ticket_number=tn;
    }
    playAppSound('ticket');
    msg('تم رفع التكت بنجاح');
    clearTechnicianTicketForm();
    await loadAll();
    renderTechnicianTickets();
    // تجهيز رسالة واتساب بعد رفع التكت مباشرة
    setTimeout(()=>{ try{ (window.sendTicketWhatsAppV43||window.sendTicketWhatsApp)?.(res.data.id); }catch(e){} }, 300);
  };
  const _oldInitTech = window.initTechnician;
  window.initTechnician = async function(){
    const u=requireRole('technician'); if(!u) return;
    await loadAll();
    if($('techTitle')) $('techTitle').textContent='لوحة الفني - '+(u.full_name||u.username);
    if($('techNewTicketProject') && typeof fillSelect==='function') fillSelect('techNewTicketProject', data.projects||[], 'name', 'اختر المشروع');
    renderTechnicianTickets();
    if(!window.__techAutoRefreshV46){
      window.__techAutoRefreshV46=setInterval(async()=>{ await loadAll(); if($('techNewTicketProject') && typeof fillSelect==='function' && !$('techNewTicketProject').options.length) fillSelect('techNewTicketProject', data.projects||[], 'name', 'اختر المشروع'); renderTechnicianTickets(); }, 20000);
    }
  };
  const css=document.createElement('style');
  css.textContent='.wa-ticket-btn-v46{background:#128C7E!important;color:#fff!important;border:0!important;border-radius:10px!important;padding:8px 10px!important;line-height:1.25!important;min-width:105px!important;font-weight:700!important}.wa-ticket-btn-v46 small{font-size:10px;color:#fff!important}.whatsapp-col{text-align:center!important;white-space:nowrap}.grid.two{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}@media(max-width:760px){.grid.two{grid-template-columns:1fr}}';
  document.head.appendChild(css);
})();

/* ===== V47: Recalculate daily time logs when project required time changes ===== */

/* ===== V52: Professional daily manager PDF report ===== */
function reportEscV52(v){ return String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
function reportDateLabelV52(dateStr){
  if(!dateStr) return '-';
  try { return new Date(dateStr+'T00:00:00').toLocaleDateString('ar-SA',{weekday:'long',year:'numeric',month:'long',day:'numeric'}); }
  catch(e){ return dateStr; }
}
function reportWorkersForProjectV52(projectId){
  const names = new Set();
  (data.workers||[]).forEach(w=>{
    const pid = (typeof workerProjectId==='function') ? workerProjectId(w) : (w.project_id || w.assigned_project_id || '');
    if(String(pid)===String(projectId) && (w.status||'active')!=='inactive') names.add(w.name);
  });
  return [...names].filter(Boolean).join('، ') || '-';
}
function reportTimeStatusValueV52(log, actual, required){
  const diff = actual - required;
  if(diff < -5) return 'under_time';
  if(diff > 5) return 'over_time';
  return 'within_time';
}
function reportStatusArabicV52(status){
  if(status==='under_time' || status==='ناقص وقت') return 'ناقص وقت';
  if(status==='over_time' || status==='زيادة وقت') return 'زيادة وقت';
  if(status==='within_time' || status==='ضمن الوقت') return 'ضمن الوقت';
  return (typeof timeStatusText==='function' ? timeStatusText(status) : (status||'-'));
}
function exportDailyManagerPDF(){
  const rows = (typeof filterLogs==='function' ? filterLogs() : (data.logs||[])).slice().sort((a,b)=>{
    const pa = projectName(a.project_id), pb = projectName(b.project_id);
    if(pa!==pb) return pa.localeCompare(pb,'ar');
    return new Date(a.check_in||a.log_date) - new Date(b.check_in||b.log_date);
  });
  const selectedDate = $('dailyDate')?.value || today();
  const selectedSupervisor = $('dailySupervisor')?.value ? supervisorName($('dailySupervisor').value) : 'الكل';
  const selectedProject = $('dailyProject')?.value ? projectName($('dailyProject').value) : 'الكل';
  if(!rows.length){ msg('لا توجد بيانات لتصدير التقرير اليومي','err'); return; }

  let totalActual=0, totalRequired=0, under=0, over=0, within=0;
  const tableRows = rows.map((l,idx)=>{
    const actual = Number(l.duration_minutes || minutesBetween(l.check_in,l.check_out));
    const required = Number((typeof logRequiredMinutes==='function' ? logRequiredMinutes(l) : l.required_minutes) || 0);
    const diff = actual - required;
    const status = reportTimeStatusValueV52(l, actual, required);
    totalActual += actual; totalRequired += required;
    if(status==='under_time') under++; else if(status==='over_time') over++; else within++;
    const cls = status==='under_time'?'bad':(status==='over_time'?'warn':'ok');
    const workers = reportWorkersForProjectV52(l.project_id);
    return `<tr>
      <td>${idx+1}</td>
      <td>${reportEscV52(supervisorName(l.supervisor_id))}</td>
      <td>${reportEscV52(projectName(l.project_id))}</td>
      <td>${reportEscV52(workers)}</td>
      <td>${reportEscV52(typeof visitTypeText==='function'?visitTypeText(l.visit_type):'')}</td>
      <td>${reportEscV52(timeOnly(l.check_in))}</td>
      <td>${reportEscV52(timeOnly(l.check_out))}</td>
      <td>${reportEscV52(minsToText(required))}</td>
      <td>${reportEscV52(minsToText(actual))}</td>
      <td>${reportEscV52(typeof diffText==='function'?diffText(diff):minsToText(Math.abs(diff)))}</td>
      <td><span class="pill ${cls}">${reportStatusArabicV52(status)}</span></td>
      <td>${reportEscV52(l.notes||'')}</td>
    </tr>`;
  }).join('');
  const totalDiff = totalActual-totalRequired;
  const generatedAt = new Date().toLocaleString('ar-SA');
  const html = `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير يومي للمدير</title>
  <style>
    @page{size:A4 landscape;margin:12mm}
    *{box-sizing:border-box} body{font-family:Tahoma,Arial,sans-serif;color:#173b33;margin:0;background:#fff;font-size:12px}
    .header{display:flex;justify-content:space-between;align-items:flex-start;border-bottom:3px solid #0b5b4a;padding-bottom:12px;margin-bottom:14px}
    .brand{font-size:22px;font-weight:800;color:#06483b}.sub{font-size:12px;color:#667;margin-top:4px}.title{text-align:left}.title h1{margin:0;font-size:20px;color:#0b5b4a}.title .date{margin-top:6px;color:#444}
    .meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:12px 0}.box{border:1px solid #d8e4df;border-radius:12px;padding:10px;background:#f8fbfa}.box b{display:block;color:#0b5b4a;margin-bottom:4px}
    .kpis{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin:12px 0}.kpi{border-radius:14px;padding:10px;background:#eef7f4;border:1px solid #d4e8e1;text-align:center}.kpi strong{display:block;font-size:18px;color:#063f34}.kpi span{color:#596b66;font-size:11px}
    table{width:100%;border-collapse:collapse;margin-top:10px} th{background:#0b5b4a;color:#fff;padding:8px;border:1px solid #0b5b4a;font-size:11px}td{padding:7px;border:1px solid #dfe8e4;vertical-align:top;text-align:center}tbody tr:nth-child(even){background:#fafdfc}.pill{display:inline-block;padding:4px 8px;border-radius:999px;font-weight:700;font-size:11px}.ok{background:#e6f6ec;color:#116b32}.warn{background:#fff4d8;color:#8a5b00}.bad{background:#ffe5e5;color:#9c1d1d}
    .footer{margin-top:14px;display:flex;justify-content:space-between;color:#666;font-size:11px;border-top:1px solid #dfe8e4;padding-top:8px}.sign{margin-top:18px;display:grid;grid-template-columns:1fr 1fr;gap:20px}.sign div{height:55px;border:1px dashed #b9c9c3;border-radius:10px;padding:8px;color:#666}
    @media print{.no-print{display:none!important} body{print-color-adjust:exact;-webkit-print-color-adjust:exact}}
  </style></head><body>
    <div class="header"><div><div class="brand">شركة تصنيف لإدارة المرافق</div><div class="sub">تقرير التشغيل اليومي للإدارة</div></div><div class="title"><h1>تقرير يومي للمدير</h1><div class="date">تاريخ التقرير: ${reportEscV52(reportDateLabelV52(selectedDate))}</div><div class="sub">وقت التصدير: ${reportEscV52(generatedAt)}</div></div></div>
    <div class="meta"><div class="box"><b>المشرف</b>${reportEscV52(selectedSupervisor)}</div><div class="box"><b>المشروع</b>${reportEscV52(selectedProject)}</div><div class="box"><b>عدد السجلات</b>${rows.length}</div><div class="box"><b>معد التقرير</b>لوحة الإدارة</div></div>
    <div class="kpis"><div class="kpi"><strong>${reportEscV52(minsToText(totalActual))}</strong><span>إجمالي الوقت الفعلي</span></div><div class="kpi"><strong>${reportEscV52(minsToText(totalRequired))}</strong><span>إجمالي الوقت المطلوب</span></div><div class="kpi"><strong>${reportEscV52((totalDiff>=0?'زيادة ':'نقص ')+minsToText(Math.abs(totalDiff)))}</strong><span>فرق الوقت</span></div><div class="kpi"><strong>${within}</strong><span>ضمن الوقت</span></div><div class="kpi"><strong>${over} / ${under}</strong><span>زيادة / نقص</span></div></div>
    <table><thead><tr><th>#</th><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>نوع الزيارة</th><th>الدخول</th><th>الخروج</th><th>المطلوب</th><th>الفعلي</th><th>الفرق</th><th>الحالة</th><th>ملاحظات</th></tr></thead><tbody>${tableRows}</tbody></table>
    <div class="sign"><div>اعتماد مدير التشغيل:</div><div>ملاحظات المدير:</div></div>
    <div class="footer"><span>شركة تصنيف لإدارة المرافق</span><span>هذا التقرير مولّد آليًا من نظام التشغيل</span></div>
    <script>window.onload=function(){setTimeout(function(){window.print()},400)}</script>
  </body></html>`;
  const w = window.open('', '_blank');
  if(!w){ msg('المتصفح منع فتح نافذة التقرير. اسمح بالنوافذ المنبثقة','err'); return; }
  w.document.open(); w.document.write(html); w.document.close();
}

/* ===== V56: unique worker count + WhatsApp daily report ===== */
function tasneefNormNameV56(v){return String(v||'').trim().replace(/[أإآ]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه').replace(/\s+/g,' ')}
function uniqueWorkersCountV56(){const s=new Set();(data.workers||[]).forEach(w=>{if(String(w.status||'active').toLowerCase()==='deleted')return;const n=tasneefNormNameV56(w.name);if(n)s.add(n)});return s.size}
(function(){const old=window.renderDashboard;window.renderDashboard=function(){if(typeof old==='function')old();const k=$('kpiWorkers');if(k)k.textContent=uniqueWorkersCountV56()}})();
function uniqueWorkersForProjectTextV56(projectId){const m=new Map();(data.workers||[]).forEach(w=>{const st=String(w.status||'active').toLowerCase();if(st==='deleted'||st==='inactive')return;const pid=(typeof workerProjectId==='function')?workerProjectId(w):(w.project_id||w.assigned_project_id||'');if(String(pid)!==String(projectId))return;const key=tasneefNormNameV56(w.name);if(key&&!m.has(key))m.set(key,String(w.name||'').trim())});return [...m.values()].join('، ')||'-'}
function dailyReportRowsV56(){return (typeof filterLogs==='function'?filterLogs():(data.logs||[])).slice().sort((a,b)=>{const pa=projectName(a.project_id),pb=projectName(b.project_id);if(pa!==pb)return pa.localeCompare(pb,'ar');return new Date(a.check_in||a.log_date)-new Date(b.check_in||b.log_date)})}
function dailyReportWhatsappTextV56(){const rows=dailyReportRowsV56();const date=$('dailyDate')?.value||today();const sup=$('dailySupervisor')?.value?supervisorName($('dailySupervisor').value):'الكل';const proj=$('dailyProject')?.value?projectName($('dailyProject').value):'الكل';let actualTotal=0,reqTotal=0,under=0,over=0,within=0;const lines=['تقرير التشغيل اليومي','شركة تصنيف','المسؤول: وائل شاكر','التاريخ: '+(typeof reportDateLabelV52==='function'?reportDateLabelV52(date):date),'المشرف: '+sup,'المشروع: '+proj,'عدد السجلات: '+rows.length,''];rows.forEach(l=>{const actual=Number(l.duration_minutes||minutesBetween(l.check_in,l.check_out));const req=Number((typeof logRequiredMinutes==='function'?logRequiredMinutes(l):l.required_minutes)||0);const diff=actual-req;actualTotal+=actual;reqTotal+=req;if(diff<-5)under++;else if(diff>5)over++;else within++});lines.push('الملخص:');lines.push('إجمالي الوقت الفعلي: '+minsToText(actualTotal));lines.push('إجمالي الوقت المطلوب: '+minsToText(reqTotal));lines.push('فرق الوقت: '+((actualTotal-reqTotal)>=0?'زيادة ':'نقص ')+minsToText(Math.abs(actualTotal-reqTotal)));lines.push('ضمن الوقت: '+within+' | زيادة: '+over+' | نقص: '+under);lines.push('');lines.push('تفاصيل السجلات:');rows.slice(0,40).forEach((l,i)=>{const actual=Number(l.duration_minutes||minutesBetween(l.check_in,l.check_out));const req=Number((typeof logRequiredMinutes==='function'?logRequiredMinutes(l):l.required_minutes)||0);const diff=actual-req;const st=diff<-5?'ناقص وقت':diff>5?'زيادة وقت':'ضمن الوقت';lines.push((i+1)+') '+projectName(l.project_id)+' | '+supervisorName(l.supervisor_id)+' | العمال: '+uniqueWorkersForProjectTextV56(l.project_id)+' | '+timeOnly(l.check_in)+' - '+timeOnly(l.check_out)+' | الفعلي '+minsToText(actual)+' | المطلوب '+minsToText(req)+' | '+st)});if(rows.length>40)lines.push('... وباقي السجلات موجودة في تقرير PDF.');lines.push('');lines.push('ملاحظة: احفظ التقرير PDF من زر الطباعة ثم أرفقه في الواتساب.');return lines.join('\n')}
function copyTextV56(text){if(navigator.clipboard&&navigator.clipboard.writeText)return navigator.clipboard.writeText(text);const ta=document.createElement('textarea');ta.value=text;ta.style.position='fixed';ta.style.opacity='0';document.body.appendChild(ta);ta.select();document.execCommand('copy');ta.remove();return Promise.resolve()}
function sendDailyManagerWhatsapp(){const rows=dailyReportRowsV56();if(!rows.length){msg('لا توجد بيانات لإرسال التقرير','err');return}const text=dailyReportWhatsappTextV56();copyTextV56(text).finally(()=>{window.open('https://wa.me/?text='+encodeURIComponent(text),'_blank');msg('تم تجهيز رسالة التقرير للواتساب. احفظ PDF من زر الطباعة وأرفقه في الواتساب.')})}

/* ===== V57: Monthly print/PDF report without records count ===== */
function monthLabelV57(month){try{const a=String(month||'').split('-').map(Number);return new Date(a[0],a[1]-1,1).toLocaleDateString('ar-SA',{year:'numeric',month:'long'});}catch(e){return month||'-'}}
function monthlyDiffTextV57(mins){mins=Number(mins||0);if(mins>5)return 'زيادة '+minsToText(Math.abs(mins));if(mins<-5)return 'نقص '+minsToText(Math.abs(mins));return 'ضمن الوقت'}
function monthlyReportRowsV57(){
  const month=$('monthlyMonth')?.value||today().slice(0,7), sid=$('monthlySupervisor')?.value||'';
  let logs=(data.logs||[]).filter(l=>{const d=l.log_date||String(l.check_in||'').slice(0,10);return d&&d.slice(0,7)===month});
  if(sid) logs=logs.filter(l=>String(l.supervisor_id)===String(sid));
  const map=new Map();
  logs.forEach(l=>{const k=(l.supervisor_id||'')+'_'+(l.project_id||'');if(!map.has(k))map.set(k,{s:l.supervisor_id,p:l.project_id,a:0,r:0});const x=map.get(k);x.a+=Number((typeof logActualMinutes==='function'?logActualMinutes(l):(l.duration_minutes||minutesBetween(l.check_in,l.check_out)))||0);x.r+=Number((typeof logRequiredMinutes==='function'?logRequiredMinutes(l):l.required_minutes)||0)});
  return [...map.values()].sort((a,b)=>{const s=supervisorName(a.s).localeCompare(supervisorName(b.s),'ar');return s||projectName(a.p).localeCompare(projectName(b.p),'ar')}).map(x=>{const diff=x.a-x.r;let st='غير محدد',cls='neutral';if(x.r>0){if(diff<-5){st='ناقص وقت';cls='bad'}else if(diff>5){st='زيادة وقت';cls='warn'}else{st='ضمن الوقت';cls='ok'}}return {...x,diff,st,cls,workers:(typeof uniqueWorkersForProjectTextV56==='function'?uniqueWorkersForProjectTextV56(x.p):'-')}})
}
function printMonthlyReportV57(){
  const rows=monthlyReportRowsV57(); if(!rows.length){msg('لا توجد بيانات في الأوقات الشهرية للطباعة','err');return}
  const month=$('monthlyMonth')?.value||today().slice(0,7), sup=$('monthlySupervisor')?.value?supervisorName($('monthlySupervisor').value):'الكل';
  let ta=0,tr=0,within=0,over=0,under=0;
  const trs=rows.map((r,i)=>{ta+=r.a;tr+=r.r;if(r.st==='ضمن الوقت')within++;else if(r.st==='زيادة وقت')over++;else if(r.st==='ناقص وقت')under++;return `<tr><td>${i+1}</td><td>${reportEscV52(supervisorName(r.s))}</td><td>${reportEscV52(projectName(r.p))}</td><td>${reportEscV52(r.workers||'-')}</td><td>${reportEscV52(minsToText(r.a))}</td><td>${reportEscV52(minsToText(r.r))}</td><td>${reportEscV52(monthlyDiffTextV57(r.diff))}</td><td><span class="pill ${r.cls}">${reportEscV52(r.st)}</span></td></tr>`}).join('');
  const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>@page{size:A4 landscape;margin:10mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;margin:0;color:#163d34;font-size:12px}.page{border:2px solid #0a5a49;border-radius:18px;padding:14px;min-height:100vh;background:linear-gradient(135deg,rgba(10,90,73,.06),#fff 35%,rgba(191,156,86,.06))}.top{display:flex;justify-content:space-between;align-items:center;border-bottom:4px solid #0a5a49;padding-bottom:14px;margin-bottom:14px}.brand{display:flex;align-items:center;gap:12px}.logo{width:60px;height:60px;border:3px solid #c7a24d;border-radius:50%;display:grid;place-items:center;font-weight:900;color:#0a5a49}.brand h2,.title h1{margin:0;color:#0a5a49}.brand small,.title p{color:#6a766f}.title{text-align:left}.title h1{font-size:28px}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:12px 0}.box,.kpi,.panel{background:white;border:1px solid #d9e6e1;border-radius:16px;padding:11px;box-shadow:0 8px 18px rgba(0,0,0,.04)}.box b{display:block;color:#0a5a49;margin-bottom:5px}.box span{font-size:15px;font-weight:700}table{width:100%;border-collapse:separate;border-spacing:0;border-radius:16px;overflow:hidden;box-shadow:0 10px 24px rgba(0,0,0,.06)}th{background:#0a5a49;color:white;padding:10px}td{padding:9px;border-bottom:1px solid #e2ece8;text-align:center;background:#fff}tbody tr:nth-child(even) td{background:#f7fbfa}.pill{border-radius:999px;padding:5px 10px;font-weight:800;display:inline-block}.ok{background:#e5f6ec;color:#107338}.warn{background:#fff3d6;color:#8a5c00}.bad{background:#ffe5e5;color:#9d2020}.neutral{background:#edf1f4;color:#52616b}.section{text-align:center;margin:16px 0 12px}.section span{display:inline-block;background:#0a5a49;color:#fff;border:2px solid #c7a24d;border-radius:999px;padding:8px 36px;font-weight:900}.kpis{display:grid;grid-template-columns:repeat(6,1fr);gap:9px}.kpi{text-align:center}.kpi strong{display:block;color:#0a5a49;font-size:18px}.kpi span{font-size:11px;color:#6a766f}.bottom{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:14px}.panel h3{margin:0 0 10px;color:#0a5a49}.line{height:24px;border-bottom:1px dashed #afbeb8}.footer{text-align:center;margin-top:12px;color:#697a73;border-top:1px solid #dce8e3;padding-top:8px}@media print{body{print-color-adjust:exact;-webkit-print-color-adjust:exact}.page{border-radius:0}}</style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h2>شركة تصنيف</h2><small>إدارة المرافق والتشغيل</small></div></div><div class="title"><h1>تقرير الأوقات الشهرية</h1><p>المسؤول: وائل شاكر</p></div></div><div class="meta"><div class="box"><b>الشهر</b><span>${reportEscV52(monthLabelV57(month))}</span></div><div class="box"><b>المشرف</b><span>${reportEscV52(sup)}</span></div><div class="box"><b>عدد المشاريع</b><span>${rows.length}</span></div><div class="box"><b>وقت التصدير</b><span>${reportEscV52(new Date().toLocaleString('ar-SA'))}</span></div></div><table><thead><tr><th>#</th><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>إجمالي الوقت الفعلي</th><th>إجمالي الوقت المطلوب</th><th>فرق الوقت</th><th>حالة الوقت</th></tr></thead><tbody>${trs}</tbody></table><div class="section"><span>ملخص التقرير</span></div><div class="kpis"><div class="kpi"><strong>${reportEscV52(minsToText(ta))}</strong><span>إجمالي الساعات الفعلية</span></div><div class="kpi"><strong>${reportEscV52(minsToText(tr))}</strong><span>إجمالي الساعات المطلوبة</span></div><div class="kpi"><strong>${reportEscV52(monthlyDiffTextV57(ta-tr))}</strong><span>إجمالي فرق الوقت</span></div><div class="kpi"><strong>${within}</strong><span>ضمن الوقت</span></div><div class="kpi"><strong>${over}</strong><span>زيادة وقت</span></div><div class="kpi"><strong>${under}</strong><span>ناقص وقت</span></div></div><div class="bottom"><div class="panel"><h3>ملاحظات المدير</h3><div class="line"></div><div class="line"></div></div><div class="panel"><h3>اعتماد مدير التشغيل</h3><p>الاسم: وائل شاكر</p><div class="line">التوقيع:</div></div></div><div class="footer">هذا التقرير مولّد آليًا من نظام تصنيف — يحفظ PDF من نافذة الطباعة</div><script>window.onload=function(){setTimeout(function(){window.print()},400)}</script></div></body></html>`;
  const w=window.open('','_blank'); if(!w){msg('المتصفح منع فتح نافذة التقرير. اسمح بالنوافذ المنبثقة','err');return} w.document.open();w.document.write(html);w.document.close();
}

/* ===== V58: Monthly print like supervisor blocks with work percentage ===== */
function monthlyPercentClassV58(p){
  p=Number(p||0);
  if(!isFinite(p) || p<=0) return 'neutral';
  if(p>=95 && p<=105) return 'ok';
  if(p>105) return 'warn';
  return 'bad';
}
function monthlyStatusTextV58(p, req){
  p=Number(p||0); req=Number(req||0);
  if(!req) return 'غير محدد';
  if(p>=95 && p<=105) return 'ضمن الوقت';
  if(p>105) return 'زيادة وقت';
  return 'ناقص وقت';
}
function monthlyReportRowsV58(){
  const month=$('monthlyMonth')?.value||today().slice(0,7), sid=$('monthlySupervisor')?.value||'';
  let logs=(data.logs||[]).filter(l=>{const d=l.log_date||String(l.check_in||'').slice(0,10);return d&&d.slice(0,7)===month});
  if(sid) logs=logs.filter(l=>String(l.supervisor_id)===String(sid));
  const map=new Map();
  logs.forEach(l=>{
    const k=(l.supervisor_id||'')+'_'+(l.project_id||'');
    if(!map.has(k)) map.set(k,{s:l.supervisor_id,p:l.project_id,a:0,r:0,t:0});
    const x=map.get(k);
    x.a+=Number((typeof logActualMinutes==='function'?logActualMinutes(l):(l.duration_minutes||minutesBetween(l.check_in,l.check_out)))||0);
    x.r+=Number((typeof logRequiredMinutes==='function'?logRequiredMinutes(l):l.required_minutes)||0);
    x.t+=Number(l.travel_minutes||0);
  });
  return [...map.values()].sort((a,b)=>{const s=supervisorName(a.s).localeCompare(supervisorName(b.s),'ar');return s||projectName(a.p).localeCompare(projectName(b.p),'ar')}).map(x=>{
    const percent=x.r ? (x.a/x.r*100) : 0;
    const diff=x.a-x.r;
    const cls=monthlyPercentClassV58(percent);
    const st=monthlyStatusTextV58(percent,x.r);
    return {...x,percent,diff,cls,st,workers:(typeof uniqueWorkersForProjectTextV56==='function'?uniqueWorkersForProjectTextV56(x.p):'-')};
  });
}
function supervisorWorkersForMonthlyV58(supervisorId, rows){
  const names=new Set();
  rows.filter(r=>String(r.s)===String(supervisorId)).forEach(r=>{
    String(r.workers||'').split(/[،,]/).map(x=>x.trim()).filter(Boolean).forEach(n=>{if(n!=='-') names.add(n)});
  });
  if(!names.size){
    (data.workers||[]).filter(w=>String(workerSupId(w))===String(supervisorId)).forEach(w=>{if(w.name) names.add(w.name)});
  }
  return [...names].sort((a,b)=>a.localeCompare(b,'ar')).join('، ') || '-';
}
function printMonthlyReportV57(){
  const rows=monthlyReportRowsV58();
  if(!rows.length){msg('لا توجد بيانات في الأوقات الشهرية للطباعة','err');return}
  const month=$('monthlyMonth')?.value||today().slice(0,7), sup=$('monthlySupervisor')?.value?supervisorName($('monthlySupervisor').value):'الكل';
  let actualTotal=0, requiredTotal=0, within=0, over=0, under=0;
  rows.forEach(r=>{actualTotal+=r.a; requiredTotal+=r.r; if(r.st==='ضمن الوقت') within++; else if(r.st==='زيادة وقت') over++; else if(r.st==='ناقص وقت') under++;});
  const totalPct=requiredTotal ? actualTotal/requiredTotal*100 : 0;
  const groups=new Map();
  rows.forEach(r=>{const sid=String(r.s||''); if(!groups.has(sid)) groups.set(sid,[]); groups.get(sid).push(r);});
  const groupCards=[...groups.entries()].map(([sid,items])=>{
    const sActual=items.reduce((a,r)=>a+r.a,0), sReq=items.reduce((a,r)=>a+r.r,0), sPct=sReq?sActual/sReq*100:0;
    const projects=items.map(r=>`<tr><td class="pname">${reportEscV52(projectName(r.p))}</td><td>${reportEscV52(minsToText(r.a))}</td><td><span class="percent ${r.cls}">${reportEscV52(percentText(r.percent))}</span></td></tr>`).join('');
    return `<div class="super-card"><div class="super-head"><b>${reportEscV52(supervisorName(sid))}</b><span>${reportEscV52(percentText(sPct))}</span></div><table class="mini"><thead><tr><th>المشروع</th><th>الفعلي</th><th>نسبة العمل</th></tr></thead><tbody>${projects}</tbody></table><div class="workers"><b>أسماء العمال</b><p>${reportEscV52(supervisorWorkersForMonthlyV58(sid,rows))}</p></div></div>`;
  }).join('');
  const detailRows=rows.map(r=>`<tr><td>${reportEscV52(supervisorName(r.s))}</td><td>${reportEscV52(projectName(r.p))}</td><td>${reportEscV52(r.workers||'-')}</td><td>${reportEscV52(minsToText(r.a))}</td><td>${reportEscV52(minsToText(r.r))}</td><td>${reportEscV52(monthlyDiffTextV57(r.diff))}</td><td><span class="percent ${r.cls}">${reportEscV52(percentText(r.percent))}</span></td><td><span class="pill ${r.cls}">${reportEscV52(r.st)}</span></td></tr>`).join('');
  const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>
  @page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{margin:0;font-family:Tahoma,Arial,sans-serif;color:#123d32;background:#fff;font-size:11px}.page{min-height:100vh;padding:14px;background:radial-gradient(circle at top left,rgba(10,90,73,.10),transparent 32%),linear-gradient(135deg,#fff 0%,#fff 62%,rgba(199,162,77,.08));border:2px solid #0a5a49}.top{display:flex;align-items:center;justify-content:space-between;border-bottom:3px solid #0a5a49;padding-bottom:10px;margin-bottom:10px}.brand{display:flex;align-items:center;gap:10px}.logo{width:54px;height:54px;border-radius:50%;border:3px solid #c7a24d;display:grid;place-items:center;font-weight:900;color:#0a5a49}.brand h2{margin:0;font-size:19px;color:#0a5a49}.title{text-align:left}.title h1{margin:0;font-size:28px;color:#0a5a49}.title p{margin:4px 0 0;color:#68766e}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:10px 0}.box{background:#fff;border:1px solid #d9e6e1;border-radius:13px;padding:9px;text-align:center}.box b{display:block;color:#63756d;font-size:10px}.box strong{font-size:16px;color:#0a5a49}.section{margin:12px 0 8px;text-align:center}.section span{display:inline-block;background:#0a5a49;color:#fff;border:2px solid #c7a24d;border-radius:999px;padding:7px 40px;font-weight:900}.super-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.super-card{background:#fff;border:2px solid #0a5a49;border-radius:10px;overflow:hidden;min-height:245px}.super-head{display:flex;justify-content:space-between;align-items:center;background:#f7fbfa;border-bottom:1px solid #d9e6e1;padding:7px 9px;color:#0a5a49}.super-head b{font-size:13px}.super-head span{font-weight:900}.mini,.details{width:100%;border-collapse:collapse}.mini th{background:#0a5a49;color:#fff;padding:6px;font-size:10px}.mini td{border-bottom:1px solid #e5eeee;padding:5px;text-align:center}.pname{text-align:right!important;font-weight:700}.workers{padding:8px;text-align:center}.workers b{display:block;color:#0a5a49;margin-bottom:5px}.workers p{margin:0;line-height:1.7;color:#243b34}.details{margin-top:8px;border-radius:10px;overflow:hidden}.details th{background:#0a5a49;color:#fff;padding:7px}.details td{border:1px solid #e0e8e5;padding:6px;text-align:center}.percent,.pill{display:inline-block;border-radius:999px;padding:3px 8px;font-weight:900}.ok{background:#e5f6ec;color:#107338}.warn{background:#fff3d6;color:#8a5c00}.bad{background:#ffe5e5;color:#9d2020}.neutral{background:#edf1f4;color:#52616b}.kpis{display:grid;grid-template-columns:repeat(6,1fr);gap:8px;margin-top:8px}.kpi{background:#fff;border:1px solid #d9e6e1;border-radius:13px;padding:9px;text-align:center}.kpi strong{display:block;color:#0a5a49;font-size:16px}.kpi span{font-size:10px;color:#68766e}.bottom{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:10px}.panel{background:#fff;border:1px solid #d9e6e1;border-radius:12px;padding:9px}.panel h3{margin:0 0 8px;color:#0a5a49}.line{height:22px;border-bottom:1px dashed #afbeb8}.footer{text-align:center;margin-top:8px;color:#6a766f}.avoid{break-inside:avoid}@media print{body{print-color-adjust:exact;-webkit-print-color-adjust:exact}.page{border-radius:0}.super-card,.kpi,.panel{break-inside:avoid}}
  </style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h2>شركة تصنيف</h2><small>إدارة المرافق والتشغيل</small></div></div><div class="title"><h1>تقرير الأوقات الشهرية</h1><p>المسؤول: وائل شاكر</p></div></div><div class="meta"><div class="box"><b>الشهر</b><strong>${reportEscV52(monthLabelV57(month))}</strong></div><div class="box"><b>المشرف</b><strong>${reportEscV52(sup)}</strong></div><div class="box"><b>عدد المشاريع</b><strong>${rows.length}</strong></div><div class="box"><b>نسبة العمل الإجمالية</b><strong>${reportEscV52(percentText(totalPct))}</strong></div></div><div class="section"><span>ملخص المشرفين والمشاريع</span></div><div class="super-grid avoid">${groupCards}</div><div class="section"><span>تفاصيل الأوقات الشهرية</span></div><table class="details"><thead><tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الوقت الفعلي</th><th>الوقت المطلوب</th><th>الفرق</th><th>نسبة العمل</th><th>حالة الوقت</th></tr></thead><tbody>${detailRows}</tbody></table><div class="section"><span>ملخص التقرير</span></div><div class="kpis"><div class="kpi"><strong>${reportEscV52(minsToText(actualTotal))}</strong><span>إجمالي الوقت الفعلي</span></div><div class="kpi"><strong>${reportEscV52(minsToText(requiredTotal))}</strong><span>إجمالي الوقت المطلوب</span></div><div class="kpi"><strong>${reportEscV52(monthlyDiffTextV57(actualTotal-requiredTotal))}</strong><span>إجمالي فرق الوقت</span></div><div class="kpi"><strong>${reportEscV52(percentText(totalPct))}</strong><span>نسبة العمل</span></div><div class="kpi"><strong>${over}</strong><span>زيادة وقت</span></div><div class="kpi"><strong>${under}</strong><span>ناقص وقت</span></div></div><div class="bottom"><div class="panel"><h3>ملاحظات المدير</h3><div class="line"></div><div class="line"></div></div><div class="panel"><h3>اعتماد مدير التشغيل</h3><p>الاسم: وائل شاكر</p><div class="line">التوقيع:</div></div></div><div class="footer">هذا التقرير مولّد آليًا من نظام تصنيف — يحفظ PDF من نافذة الطباعة</div><script>window.onload=function(){setTimeout(function(){window.print()},400)}</script></div></body></html>`;
  const w=window.open('','_blank'); if(!w){msg('المتصفح منع فتح نافذة التقرير. اسمح بالنوافذ المنبثقة','err');return} w.document.open();w.document.write(html);w.document.close();
}

/* ===== V59: Monthly work percentage formula like Excel =====
   نسبة العمل = وقت المشروع الفعلي ÷ إجمالي وقت نفس المشرف × 100
   نسبة الالتزام = الوقت الفعلي ÷ الوقت المطلوب × 100
   حالة الوقت = حسب فرق الدقائق ±5
*/
function monthlyTimeClassV59(diff, required){
  if(!Number(required||0)) return 'neutral';
  diff=Number(diff||0);
  if(diff < -5) return 'bad';
  if(diff > 5) return 'warn';
  return 'ok';
}
function monthlyTimeStatusV59(diff, required){
  if(!Number(required||0)) return 'غير محدد';
  diff=Number(diff||0);
  if(diff < -5) return 'ناقص وقت';
  if(diff > 5) return 'زيادة وقت';
  return 'ضمن الوقت';
}
function monthlyCommitmentClassV59(percent, required){
  if(!Number(required||0)) return 'neutral';
  percent=Number(percent||0);
  if(percent>=95 && percent<=105) return 'ok';
  if(percent>105) return 'warn';
  return 'bad';
}
function monthlyBaseRowsV59(){
  const month=$('monthlyMonth')?.value||today().slice(0,7), sid=$('monthlySupervisor')?.value||'';
  let logs=(data.logs||[]).filter(l=>{const d=l.log_date||String(l.check_in||'').slice(0,10);return d&&d.slice(0,7)===month});
  if(sid) logs=logs.filter(l=>String(l.supervisor_id)===String(sid));
  const map=new Map();
  logs.forEach(l=>{
    const k=(l.supervisor_id||'')+'_'+(l.project_id||'');
    if(!map.has(k)) map.set(k,{s:l.supervisor_id,p:l.project_id,c:0,a:0,r:0,t:0});
    const x=map.get(k);
    x.c++;
    x.a+=Number((typeof logActualMinutes==='function'?logActualMinutes(l):(l.duration_minutes||minutesBetween(l.check_in,l.check_out)))||0);
    x.r+=Number((typeof logRequiredMinutes==='function'?logRequiredMinutes(l):l.required_minutes)||0);
    x.t+=Number(l.travel_minutes||0);
  });
  const vals=[...map.values()];
  const supTotals={};
  vals.forEach(r=>{ const s=String(r.s||''); supTotals[s]=(supTotals[s]||0)+Number(r.a||0); });
  return vals.map(r=>{
    const supTotal=supTotals[String(r.s||'')]||0;
    const workPercent=supTotal ? (r.a/supTotal*100) : 0;
    const commitmentPercent=r.r ? (r.a/r.r*100) : 0;
    const diff=r.a-r.r;
    const cls=monthlyTimeClassV59(diff,r.r);
    const st=monthlyTimeStatusV59(diff,r.r);
    const ccls=monthlyCommitmentClassV59(commitmentPercent,r.r);
    return {...r,supTotal,workPercent,commitmentPercent,diff,cls,st,ccls,workers:(typeof uniqueWorkersForProjectTextV56==='function'?uniqueWorkersForProjectTextV56(r.p):'-')};
  }).sort((a,b)=>{const s=supervisorName(a.s).localeCompare(supervisorName(b.s),'ar');return s||projectName(a.p).localeCompare(projectName(b.p),'ar')});
}
function renderMonthly(){
  const body=$('monthlyBody');
  if(!body) return;
  const vals=monthlyBaseRowsV59();
  body.innerHTML=vals.map(r=>`<tr><td>${esc(supervisorName(r.s))}</td><td>${esc(projectName(r.p))}</td><td>${r.c}</td><td>${minsToText(r.r)}</td><td>${minsToText(r.a)}</td><td>${r.t} دقيقة</td><td><span class="badge green">${percentText(r.workPercent)}</span></td><td><span class="badge ${r.ccls}">${percentText(r.commitmentPercent)}</span></td><td><span class="badge ${r.cls}">${r.st}</span></td></tr>`).join('')||'<tr><td colspan="9">لا توجد بيانات</td></tr>';
  const total=vals.reduce((a,r)=>a+r.a,0), required=vals.reduce((a,r)=>a+r.r,0), travel=vals.reduce((a,r)=>a+r.t,0), commitment=required?total/required*100:0;
  const diff=total-required, cls=monthlyTimeClassV59(diff,required), status=monthlyTimeStatusV59(diff,required);
  if($('monthlySummary')) $('monthlySummary').innerHTML=`<div class="kpi"><small>الساعات المطلوبة</small><b>${minsToText(required)}</b></div><div class="kpi"><small>الساعات الفعلية</small><b>${minsToText(total)}</b></div><div class="kpi"><small>فرق الوقت</small><b>${monthlyDiffTextV57(diff)}</b></div><div class="kpi"><small>وقت الانتقال</small><b>${travel} دقيقة</b></div><div class="kpi"><small>نسبة الالتزام</small><b>${percentText(commitment)}</b></div><div class="kpi"><small>حالة الأداء</small><b><span class="badge ${cls}">${status}</span></b></div>`;
}
function exportMonthlyCSV(){
  const rows=[...document.querySelectorAll('#monthlyBody tr')].map(tr=>[...tr.children].map(td=>td.textContent.trim()));
  const csv=['المشرف,المشروع,عدد السجلات,الساعات المطلوبة,الساعات الفعلية,وقت الانتقال,نسبة العمل,نسبة الالتزام,حالة الوقت',...rows.map(r=>r.map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))].join('\n');
  download('monthly.csv',csv);
}
function monthlyReportRowsV58(){ return monthlyBaseRowsV59(); }
function supervisorWorkersForMonthlyV58(supervisorId, rows){
  const names=new Set();
  rows.filter(r=>String(r.s)===String(supervisorId)).forEach(r=>{
    String(r.workers||'').split(/[،,]/).map(x=>x.trim()).filter(Boolean).forEach(n=>{if(n!=='-') names.add(n)});
  });
  if(!names.size){
    (data.workers||[]).filter(w=>String(workerSupId(w))===String(supervisorId)).forEach(w=>{if(w.name) names.add(w.name)});
  }
  return [...names].sort((a,b)=>a.localeCompare(b,'ar')).join('، ') || '-';
}
function printMonthlyReportV57(){
  const rows=monthlyBaseRowsV59();
  if(!rows.length){msg('لا توجد بيانات في الأوقات الشهرية للطباعة','err');return}
  const month=$('monthlyMonth')?.value||today().slice(0,7), sup=$('monthlySupervisor')?.value?supervisorName($('monthlySupervisor').value):'الكل';
  let actualTotal=0, requiredTotal=0, within=0, over=0, under=0;
  rows.forEach(r=>{actualTotal+=r.a; requiredTotal+=r.r; if(r.st==='ضمن الوقت') within++; else if(r.st==='زيادة وقت') over++; else if(r.st==='ناقص وقت') under++;});
  const commitmentTotal=requiredTotal ? actualTotal/requiredTotal*100 : 0;
  const groups=new Map();
  rows.forEach(r=>{const sid=String(r.s||''); if(!groups.has(sid)) groups.set(sid,[]); groups.get(sid).push(r);});
  const groupCards=[...groups.entries()].map(([sid,items])=>{
    const sActual=items.reduce((a,r)=>a+r.a,0), sReq=items.reduce((a,r)=>a+r.r,0), sCommit=sReq?sActual/sReq*100:0;
    const projects=items.map(r=>`<tr><td class="pname">${reportEscV52(projectName(r.p))}</td><td>${reportEscV52(minsToText(r.a))}</td><td><span class="percent ok">${reportEscV52(percentText(r.workPercent))}</span></td></tr>`).join('');
    return `<div class="super-card"><div class="super-head"><b>${reportEscV52(supervisorName(sid))}</b><span>${reportEscV52(minsToText(sActual))}</span></div><table class="mini"><thead><tr><th>المشروع</th><th>الفعلي</th><th>نسبة العمل</th></tr></thead><tbody>${projects}</tbody></table><div class="workers"><b>أسماء العمال</b><p>${reportEscV52(supervisorWorkersForMonthlyV58(sid,rows))}</p></div><div class="commitment">نسبة الالتزام: <b>${reportEscV52(percentText(sCommit))}</b></div></div>`;
  }).join('');
  const detailRows=rows.map(r=>`<tr><td>${reportEscV52(supervisorName(r.s))}</td><td>${reportEscV52(projectName(r.p))}</td><td>${reportEscV52(r.workers||'-')}</td><td>${reportEscV52(minsToText(r.a))}</td><td>${reportEscV52(minsToText(r.r))}</td><td>${reportEscV52(monthlyDiffTextV57(r.diff))}</td><td><span class="percent ok">${reportEscV52(percentText(r.workPercent))}</span></td><td><span class="percent ${r.ccls}">${reportEscV52(percentText(r.commitmentPercent))}</span></td><td><span class="pill ${r.cls}">${reportEscV52(r.st)}</span></td></tr>`).join('');
  const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{margin:0;font-family:Tahoma,Arial,sans-serif;color:#123d32;background:#fff;font-size:11px}.page{min-height:100vh;padding:14px;background:radial-gradient(circle at top left,rgba(10,90,73,.10),transparent 32%),linear-gradient(135deg,#fff 0%,#fff 62%,rgba(199,162,77,.08));border:2px solid #0a5a49}.top{display:flex;align-items:center;justify-content:space-between;border-bottom:3px solid #0a5a49;padding-bottom:10px;margin-bottom:10px}.brand{display:flex;align-items:center;gap:10px}.logo{width:54px;height:54px;border-radius:50%;border:3px solid #c7a24d;display:grid;place-items:center;font-weight:900;color:#0a5a49}.brand h2{margin:0;font-size:19px;color:#0a5a49}.title{text-align:left}.title h1{margin:0;font-size:28px;color:#0a5a49}.title p{margin:4px 0 0;color:#68766e}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:10px 0}.box{background:#fff;border:1px solid #d9e6e1;border-radius:13px;padding:9px;text-align:center}.box b{display:block;color:#63756d;font-size:10px}.box strong{font-size:16px;color:#0a5a49}.section{margin:12px 0 8px;text-align:center}.section span{display:inline-block;background:#0a5a49;color:#fff;border:2px solid #c7a24d;border-radius:999px;padding:7px 40px;font-weight:900}.super-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.super-card{background:#fff;border:2px solid #0a5a49;border-radius:10px;overflow:hidden;min-height:245px}.super-head{display:flex;justify-content:space-between;align-items:center;background:#f7fbfa;border-bottom:1px solid #d9e6e1;padding:7px 9px;color:#0a5a49}.super-head b{font-size:13px}.super-head span{font-weight:900}.mini,.details{width:100%;border-collapse:collapse}.mini th{background:#0a5a49;color:#fff;padding:6px;font-size:10px}.mini td{border-bottom:1px solid #e5eeee;padding:5px;text-align:center}.pname{text-align:right!important;font-weight:700}.workers{padding:8px;text-align:center}.workers b{display:block;color:#0a5a49;margin-bottom:5px}.workers p{margin:0;line-height:1.7;color:#243b34}.commitment{padding:7px;text-align:center;border-top:1px solid #e5eeee;color:#0a5a49}.details{margin-top:8px;border-radius:10px;overflow:hidden}.details th{background:#0a5a49;color:#fff;padding:7px}.details td{border:1px solid #e0e8e5;padding:6px;text-align:center}.percent,.pill{display:inline-block;border-radius:999px;padding:3px 8px;font-weight:900}.ok{background:#e5f6ec;color:#107338}.warn{background:#fff3d6;color:#8a5c00}.bad{background:#ffe5e5;color:#9d2020}.neutral{background:#edf1f4;color:#52616b}.kpis{display:grid;grid-template-columns:repeat(6,1fr);gap:8px;margin-top:8px}.kpi{background:#fff;border:1px solid #d9e6e1;border-radius:13px;padding:9px;text-align:center}.kpi strong{display:block;color:#0a5a49;font-size:16px}.kpi span{font-size:10px;color:#68766e}.bottom{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:10px}.panel{background:#fff;border:1px solid #d9e6e1;border-radius:12px;padding:9px}.panel h3{margin:0 0 8px;color:#0a5a49}.line{height:22px;border-bottom:1px dashed #afbeb8}.footer{text-align:center;margin-top:8px;color:#6a766f}.avoid{break-inside:avoid}@media print{body{print-color-adjust:exact;-webkit-print-color-adjust:exact}.page{border-radius:0}.super-card,.kpi,.panel{break-inside:avoid}}</style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h2>شركة تصنيف</h2><small>إدارة المرافق والتشغيل</small></div></div><div class="title"><h1>تقرير الأوقات الشهرية</h1><p>المسؤول: وائل شاكر</p></div></div><div class="meta"><div class="box"><b>الشهر</b><strong>${reportEscV52(monthLabelV57(month))}</strong></div><div class="box"><b>المشرف</b><strong>${reportEscV52(sup)}</strong></div><div class="box"><b>عدد المشاريع</b><strong>${rows.length}</strong></div><div class="box"><b>نسبة الالتزام الإجمالية</b><strong>${reportEscV52(percentText(commitmentTotal))}</strong></div></div><div class="section"><span>ملخص المشرفين والمشاريع</span></div><div class="super-grid avoid">${groupCards}</div><div class="section"><span>تفاصيل الأوقات الشهرية</span></div><table class="details"><thead><tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الوقت الفعلي</th><th>الوقت المطلوب</th><th>الفرق</th><th>نسبة العمل</th><th>نسبة الالتزام</th><th>حالة الوقت</th></tr></thead><tbody>${detailRows}</tbody></table><div class="section"><span>ملخص التقرير</span></div><div class="kpis"><div class="kpi"><strong>${reportEscV52(minsToText(actualTotal))}</strong><span>إجمالي الوقت الفعلي</span></div><div class="kpi"><strong>${reportEscV52(minsToText(requiredTotal))}</strong><span>إجمالي الوقت المطلوب</span></div><div class="kpi"><strong>${reportEscV52(monthlyDiffTextV57(actualTotal-requiredTotal))}</strong><span>إجمالي فرق الوقت</span></div><div class="kpi"><strong>${reportEscV52(percentText(commitmentTotal))}</strong><span>نسبة الالتزام</span></div><div class="kpi"><strong>${over}</strong><span>زيادة وقت</span></div><div class="kpi"><strong>${under}</strong><span>ناقص وقت</span></div></div><div class="bottom"><div class="panel"><h3>ملاحظات المدير</h3><div class="line"></div><div class="line"></div></div><div class="panel"><h3>اعتماد مدير التشغيل</h3><p>الاسم: وائل شاكر</p><div class="line">التوقيع:</div></div></div><div class="footer">ملاحظة: نسبة العمل = وقت المشروع ÷ إجمالي وقت المشرف. نسبة الالتزام = الوقت الفعلي ÷ الوقت المطلوب.</div><script>window.onload=function(){setTimeout(function(){window.print()},400)}</script></div></body></html>`;
  const w=window.open('','_blank'); if(!w){msg('المتصفح منع فتح نافذة التقرير. اسمح بالنوافذ المنبثقة','err');return} w.document.open();w.document.write(html);w.document.close();
}

/* ===== V60: Correct monthly percentages and unique workers count ===== */
function tasneefNormNameV60(name){return String(name||'').trim().replace(/[أإآ]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه').replace(/\s+/g,' ')}
function uniqueWorkersCountV60(){const set=new Set();(data.workers||[]).forEach(w=>{const st=String(w.status||'active').toLowerCase();if(st==='deleted')return;const key=tasneefNormNameV60(w.name);if(key)set.add(key)});return set.size}
function uniqueWorkersForProjectTextV60(projectId){const m=new Map();(data.workers||[]).forEach(w=>{const st=String(w.status||'active').toLowerCase();if(st==='deleted'||st==='inactive')return;const pid=(typeof workerProjectId==='function')?workerProjectId(w):(w.project_id||w.assigned_project_id||'');if(String(pid)!==String(projectId))return;const key=tasneefNormNameV60(w.name);if(key&&!m.has(key))m.set(key,String(w.name||'').trim())});return [...m.values()].join('، ')||'-'}
function renderDashboard(){if(!$('kpiUsers'))return;$('kpiUsers').textContent=data.users.length;$('kpiProjects').textContent=data.projects.length;$('kpiWorkers').textContent=uniqueWorkersCountV60();$('kpiTodayLogs').textContent=data.logs.filter(l=>String(l.visit_type||'')!=='technician_attendance' && (l.log_date||String(l.check_in||'').slice(0,10))===today()).length;const div=$('todaySummary');if(div)div.innerHTML=data.supervisors.map(s=>{const logs=data.logs.filter(l=>String(l.supervisor_id)===String(s.id)&&(l.log_date||String(l.check_in||'').slice(0,10))===today());const mins=logs.reduce((a,l)=>a+(Number(l.duration_minutes)||minutesBetween(l.check_in,l.check_out)),0);return `<div class="summary-item"><b>${esc(s.full_name)}</b><br>عدد التسجيلات: ${logs.length}<br>إجمالي الوقت: ${minsToText(mins)}</div>`}).join('')||'<div class="summary-item">لا توجد تسجيلات اليوم</div>'}
function monthlyStatusFromDiffV60(diff,required){if(!Number(required||0))return{text:'غير محدد',cls:'neutral'};diff=Number(diff||0);if(diff<-5)return{text:'ناقص وقت',cls:'bad'};if(diff>5)return{text:'زيادة وقت',cls:'warn'};return{text:'ضمن الوقت',cls:'ok'}}
function monthlyCommitmentClassV60(percent,required){if(!Number(required||0))return'neutral';percent=Number(percent||0);if(percent>=95&&percent<=105)return'ok';if(percent>105)return'warn';return'bad'}
function monthlyRowsV60(){const month=$('monthlyMonth')?.value||today().slice(0,7);const sid=$('monthlySupervisor')?.value;let logs=(data.logs||[]).filter(l=>{const d=l.log_date||String(l.check_in||'').slice(0,10);return d&&d.slice(0,7)===month});if(sid)logs=logs.filter(l=>String(l.supervisor_id)===String(sid));const map=new Map();logs.forEach(l=>{const k=String(l.supervisor_id||'')+'_'+String(l.project_id||'');if(!map.has(k))map.set(k,{s:l.supervisor_id,p:l.project_id,a:0,r:0,t:0});const x=map.get(k);x.a+=Number((typeof logActualMinutes==='function'?logActualMinutes(l):(l.duration_minutes||minutesBetween(l.check_in,l.check_out)))||0);x.r+=Number((typeof logRequiredMinutes==='function'?logRequiredMinutes(l):l.required_minutes)||0);x.t+=Number(l.travel_minutes||0)});const vals=[...map.values()];const supTotals={};vals.forEach(r=>{const s=String(r.s||'');supTotals[s]=(supTotals[s]||0)+Number(r.a||0)});return vals.map(r=>{const supTotal=supTotals[String(r.s||'')]||0;const workPercent=supTotal?(r.a/supTotal*100):0;const commitmentPercent=r.r?(r.a/r.r*100):0;const diff=r.a-r.r;const st=monthlyStatusFromDiffV60(diff,r.r);return{...r,supTotal,workers:uniqueWorkersForProjectTextV60(r.p),workPercent,commitmentPercent,ccls:monthlyCommitmentClassV60(commitmentPercent,r.r),diff,st:st.text,cls:st.cls}}).sort((a,b)=>{const s=supervisorName(a.s).localeCompare(supervisorName(b.s),'ar');return s||projectName(a.p).localeCompare(projectName(b.p),'ar')})}
function renderMonthly(){const body=$('monthlyBody');if(!body)return;const table=body.closest('table');if(table&&table.tHead)table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الساعات المطلوبة</th><th>الساعات الفعلية</th><th>وقت الانتقال</th><th>نسبة العمل</th><th>نسبة الالتزام</th><th>حالة الأداء</th></tr>';const vals=monthlyRowsV60();body.innerHTML=vals.map(r=>`<tr><td>${esc(supervisorName(r.s))}</td><td>${esc(projectName(r.p))}</td><td>${esc(r.workers)}</td><td>${minsToText(r.r)}</td><td>${minsToText(r.a)}</td><td>${r.t} دقيقة</td><td><span class="badge green">${percentText(r.workPercent)}</span></td><td><span class="badge ${r.ccls}">${percentText(r.commitmentPercent)}</span></td><td><span class="badge ${r.cls}">${r.st}</span></td></tr>`).join('')||'<tr><td colspan="9">لا توجد بيانات</td></tr>';const total=vals.reduce((a,r)=>a+r.a,0),required=vals.reduce((a,r)=>a+r.r,0),travel=vals.reduce((a,r)=>a+r.t,0),commitment=required?total/required*100:0;const diff=total-required,st=monthlyStatusFromDiffV60(diff,required);if($('monthlySummary'))$('monthlySummary').innerHTML=`<div class="kpi"><small>الساعات المطلوبة</small><b>${minsToText(required)}</b></div><div class="kpi"><small>الساعات الفعلية</small><b>${minsToText(total)}</b></div><div class="kpi"><small>فرق الوقت</small><b>${monthlyDiffTextV57(diff)}</b></div><div class="kpi"><small>وقت الانتقال</small><b>${travel} دقيقة</b></div><div class="kpi"><small>نسبة الالتزام</small><b>${percentText(commitment)}</b></div><div class="kpi"><small>حالة الأداء</small><b><span class="badge ${st.cls}">${st.text}</span></b></div>`}
function exportMonthlyCSV(){const rows=[...document.querySelectorAll('#monthlyBody tr')].map(tr=>[...tr.children].map(td=>td.textContent.trim()));const csv=['المشرف,المشروع,أسماء العمال,الساعات المطلوبة,الساعات الفعلية,وقت الانتقال,نسبة العمل,نسبة الالتزام,حالة الأداء',...rows.map(r=>r.map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))].join('\n');download('monthly.csv',csv)}
function monthlyBaseRowsV59(){return monthlyRowsV60()}
function monthlyReportRowsV58(){return monthlyRowsV60()}

/* ===== V72: User permissions + camera photo for supervisor check-in/out + WhatsApp ===== */
(function(){
  'use strict';

  const PERMISSIONS_V72 = [
    ['can_time_logs','تسجيل دخول / خروج'],
    ['can_journey','رحلة التشغيل اليومية'],
    ['can_attendance','تسجيل الحضور'],
    ['can_tickets','التكتات'],
    ['can_reports','الملخص والتقارير'],
    ['can_monthly','الأوقات الشهرية'],
    ['can_edit_time_logs','تعديل السجلات اليومية'],
    ['can_manage_users','إدارة المستخدمين'],
    ['can_manage_workers','إدارة العمال'],
  ];

  function roleDefaults(role){
    if(role === 'admin') return Object.fromEntries(PERMISSIONS_V72.map(p=>[p[0], true]));
    if(role === 'technician') return {can_tickets:true, can_inventory_requests:true};
    if(role === 'warehouse_manager') return {can_expenses_inventory:true, can_inventory_requests:true, can_manage_inventory:true};
    if(role === 'operations_manager') return {can_time_logs:true,can_journey:true,can_attendance:true,can_tickets:true,can_reports:true,can_monthly:true,can_expenses_inventory:true,can_inventory_requests:true,can_manage_inventory:false,can_edit_time_logs:true,can_manage_users:false,can_manage_workers:true};
    if(role === 'financial_manager') return {can_reports:true,can_expenses_inventory:true,can_inventory_requests:true,can_manage_inventory:false};
    if(role === 'general_manager') return Object.fromEntries(PERMISSIONS_V72.map(p=>[p[0], true]));
    return {
      can_time_logs:true,
      can_journey:true,
      can_attendance:true,
      can_tickets:true,
      can_reports:true,
      can_monthly:false,
      can_edit_time_logs:false,
      can_manage_users:false,
      can_manage_workers:false,
      can_expenses_inventory:false,
      can_inventory_requests:true,
      can_manage_inventory:false
    };
  }
  function getPerms(user){
    user = user || (typeof session === 'function' ? session() : null) || {};
    let base = roleDefaults(user.role);
    let extra = user.permissions || {};
    if(typeof extra === 'string'){
      try{ extra = JSON.parse(extra || '{}'); }catch(e){ extra = {}; }
    }
    return Object.assign({}, base, extra || {});
  }
  window.getUserPermissionsV72 = getPerms;

  function injectPermissionsBox(){
    if(window.__tasneefUnifiedPermissionsOnly) return;
    const formTitle = document.getElementById('userFormTitle');
    if(!formTitle) return;
    const active = document.getElementById('userActive');
    if(!active) return;
    let box = document.getElementById('userPermissionsBoxV72');
    const html = '<label>الصلاحيات</label><div class="perm-grid-v72">' + PERMISSIONS_V72.map(([key,label]) =>
      `<label class="perm-item-v72"><input type="checkbox" id="perm_${key}" data-perm="${key}"> <span>${label}</span></label>`
    ).join('') + '</div><div class="footer-note">تحدد هذه الصلاحيات ما يظهر للمستخدم داخل التطبيق. مدير عام يحصل على كل الصلاحيات افتراضيًا.</div>';
    if(!box){
      box = document.createElement('div');
      box.id = 'userPermissionsBoxV72';
      box.className = 'perm-box-v72';
      active.parentElement.insertBefore(box, active.nextSibling);
    }
    const needsRefresh = !box.querySelector('#perm_can_expenses_inventory') || !box.querySelector('#perm_can_inventory_requests') || !box.querySelector('#perm_can_manage_inventory') || !box.textContent.includes('مدير مخازن') === false;
    if(needsRefresh || !box.querySelector('.perm-grid-v72')) box.innerHTML = html;
    const role = document.getElementById('userRole');
    if(role && !role.dataset.permHookedV112){ role.dataset.permHookedV112='1'; role.addEventListener('change', ()=>setPermInputs(roleDefaults(role.value))); }
    setPermInputs(roleDefaults(role?.value || 'supervisor'));
  }
  function readPermInputs(){
    const out = {};
    PERMISSIONS_V72.forEach(([key])=>{
      const el = document.getElementById('perm_'+key);
      if(el) out[key] = !!el.checked;
    });
    return out;
  }
  function setPermInputs(perms){
    perms = perms || {};
    PERMISSIONS_V72.forEach(([key])=>{
      const el = document.getElementById('perm_'+key);
      if(el) el.checked = !!perms[key];
    });
  }
  function permissionsText(perms){
    perms = perms || {};
    const labels = PERMISSIONS_V72.filter(([k])=>perms[k]).map(x=>x[1]);
    return labels.length ? labels.join('، ') : '-';
  }

  const oldHydrate = window.hydrateForms;
  window.hydrateForms = function(){
    if(typeof oldHydrate === 'function') oldHydrate.apply(this, arguments);
    injectPermissionsBox();
  };

  window.saveUser = async function(){
    const id=$('userId')?.value;
    const row={
      full_name:$('userFullName')?.value.trim(),
      username:$('userUsername')?.value.trim(),
      password:$('userPassword')?.value.trim()||'123456',
      role:$('userRole')?.value,
      is_active:$('userActive')?.value==='true',
      permissions: readPermInputs()
    };
    if(!row.full_name||!row.username) return msg('الاسم واسم المستخدم مطلوبان','err');
    let res = id ? await sb.from('app_users').update(row).eq('id',id) : await sb.from('app_users').insert(row);
    if(res.error && String(res.error.message||'').includes('permissions')){
      const safeRow = Object.assign({}, row); delete safeRow.permissions;
      res = id ? await sb.from('app_users').update(safeRow).eq('id',id) : await sb.from('app_users').insert(safeRow);
      if(!res.error) msg('تم حفظ المستخدم، لكن عمود الصلاحيات غير موجود. شغّل ملف schema_update_v72_permissions_camera.sql','err');
    }
    if(res.error) return msg(res.error.message,'err');
    msg('تم حفظ المستخدم والصلاحيات');
    if(typeof clearUserForm === 'function') clearUserForm();
    await refreshAll();
    injectPermissionsBox();
  };

  window.renderUsers = function(){
    const b=$('usersBody'); if(!b) return;
    const table=b.closest('table');
    if(table && table.tHead){
      table.tHead.innerHTML='<tr><th>الاسم</th><th>المستخدم</th><th>الدور</th><th>الحالة</th><th>الصلاحيات</th><th>إجراء</th></tr>';
    }
    b.innerHTML=(data.users||[]).map(u=>{
      const perms = getPerms(u);
      return `<tr><td>${esc(u.full_name)}</td><td>${esc(u.username)}</td><td><span class="badge">${esc(userRoleLabel(u.role))}</span></td><td><span class="badge ${u.is_active?'green':'red'}">${u.is_active?'نشط':'موقوف'}</span></td><td style="white-space:normal;min-width:220px">${esc(permissionsText(perms))}</td><td class="row-actions"><button onclick="editUser(${u.id})">تعديل</button><button class="danger" onclick="deleteRow('app_users',${u.id})">حذف</button></td></tr>`;
    }).join('') || '<tr><td colspan="6">لا يوجد مستخدمون</td></tr>';
  };

  window.editUser = function(id){
    const u=(data.users||[]).find(x=>String(x.id)===String(id)); if(!u)return;
    injectPermissionsBox();
    $('userId').value=u.id; $('userFullName').value=u.full_name||''; $('userUsername').value=u.username||''; $('userPassword').value=u.password||''; $('userRole').value=u.role; $('userActive').value=String(u.is_active!==false); $('userFormTitle').textContent='تعديل مستخدم';
    setPermInputs(getPerms(u));
  };

  const oldClearUser = window.clearUserForm;
  window.clearUserForm = function(){
    if(typeof oldClearUser === 'function') oldClearUser.apply(this, arguments);
    injectPermissionsBox();
    setPermInputs(roleDefaults(document.getElementById('userRole')?.value || 'supervisor'));
  };

  function applyCurrentPermissions(){
    const u = (typeof session === 'function') ? session() : null;
    if(!u) return;
    const p = getPerms(u);
    if(u.role === 'admin'){
      const navMap = [
        ['users','can_manage_users'], ['workers','can_manage_workers'], ['daily','can_time_logs'], ['attendance','can_attendance'], ['monthly','can_monthly'], ['tickets','can_tickets'], ['financeDashboard','can_expenses_inventory']
      ];
      navMap.forEach(([page,perm])=>{
        const btn=[...document.querySelectorAll('.nav')].find(b=>String(b.getAttribute('onclick')||'').includes(`'${page}'`));
        if(btn && p[perm]===false) btn.classList.add('hidden');
      });
    }
    if(u.role === 'supervisor'){
      const tabRules = [
        ['supLogs','can_time_logs'], ['supAttendance','can_attendance'], ['supTickets','can_tickets'], ['supSummary','can_reports']
      ];
      tabRules.forEach(([page,perm])=>{
        const btn=[...document.querySelectorAll('.sup-tab')].find(b=>String(b.getAttribute('onclick')||'').includes(`'${page}'`));
        if(btn && p[perm]===false) btn.classList.add('hidden');
      });
    }
  }
  window.addEventListener('load', ()=>setTimeout(applyCurrentPermissions, 700));

  function ensureCameraStyles(){
    if(document.getElementById('cameraStylesV72')) return;
    const st=document.createElement('style'); st.id='cameraStylesV72';
    st.textContent = `
      .cam-overlay-v72{position:fixed;inset:0;background:rgba(0,0,0,.78);z-index:999999;display:grid;place-items:center;padding:16px;direction:rtl}
      .cam-box-v72{width:min(560px,96vw);background:#fff;border-radius:22px;padding:14px;border:1px solid #dce6e2;box-shadow:0 20px 60px rgba(0,0,0,.35)}
      .cam-box-v72 h3{margin:0 0 10px;color:#0A4033}.cam-video-v72{width:100%;max-height:58vh;background:#111;border-radius:16px;object-fit:cover}
      .cam-actions-v72{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}.cam-actions-v72 button{flex:1}.cam-cancel-v72{background:#b83232!important}.perm-box-v72{margin-top:12px;border:1px solid var(--line,#dce6e2);background:#fbfdfc;border-radius:14px;padding:12px}.perm-grid-v72{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.perm-item-v72{display:flex!important;align-items:center;gap:8px;margin:0!important;color:#10231d!important;font-weight:600!important}.perm-item-v72 input{width:auto!important}.footer-note{font-size:12px;color:#60706a;line-height:1.7;margin-top:8px}
      @media(max-width:640px){.perm-grid-v72{grid-template-columns:1fr}.cam-actions-v72{flex-direction:column}}
    `;
    document.head.appendChild(st);
  }
  function nowArabicLabel(){ return new Date().toLocaleString('ar-SA', {dateStyle:'medium', timeStyle:'short'}); }
  function dataUrlToFile(dataUrl, filename){
    const arr = dataUrl.split(','), mime = (arr[0].match(/:(.*?);/)||[])[1] || 'image/jpeg';
    const bstr = atob(arr[1]); let n=bstr.length; const u8=new Uint8Array(n);
    while(n--) u8[n]=bstr.charCodeAt(n);
    return new File([u8], filename, {type:mime});
  }
  async function capturePhotoV72(info){
    ensureCameraStyles();
    if(!navigator.mediaDevices || !navigator.mediaDevices.getUserMedia){
      msg('الكاميرا غير مدعومة في هذا المتصفح','err');
      return null;
    }
    return new Promise(async(resolve)=>{
      let stream=null;
      const overlay=document.createElement('div'); overlay.className='cam-overlay-v72';
      overlay.innerHTML=`<div class="cam-box-v72"><h3>${info.title}</h3><video class="cam-video-v72" autoplay playsinline muted></video><div class="cam-actions-v72"><button type="button" id="camShotV72">التقاط الصورة</button><button type="button" id="camSwitchV73" style="background:#0d6b56!important">تبديل الكاميرا</button><button type="button" class="cam-cancel-v72" id="camCancelV72">إلغاء</button></div><div class="footer-note">سيحاول التطبيق فتح الكاميرا الخلفية أولًا. إذا لم تعمل، اضغط تبديل الكاميرا أو تأكد أن الصفحة تعمل على HTTPS.</div></div>`;
      document.body.appendChild(overlay);
      const video=overlay.querySelector('video');
      function close(val){ try{ stream && stream.getTracks().forEach(t=>t.stop()); }catch(e){} overlay.remove(); resolve(val); }
      overlay.querySelector('#camCancelV72').onclick=()=>close(null);
      let preferBackCameraV73 = true;
      async function startCameraV73(){
        try{ stream && stream.getTracks().forEach(t=>t.stop()); }catch(e){}
        stream = null;
        const tries = preferBackCameraV73
          ? [
              {video:{facingMode:{exact:'environment'}}, audio:false},
              {video:{facingMode:{ideal:'environment'}}, audio:false},
              {video:true, audio:false}
            ]
          : [
              {video:{facingMode:{exact:'user'}}, audio:false},
              {video:{facingMode:{ideal:'user'}}, audio:false},
              {video:true, audio:false}
            ];
        let lastErr = null;
        for(const constraints of tries){
          try{
            stream = await navigator.mediaDevices.getUserMedia(constraints);
            video.srcObject = stream;
            try{ await video.play(); }catch(e){}
            return true;
          }catch(err){ lastErr = err; }
        }
        msg('تعذر فتح الكاميرا: '+((lastErr && lastErr.message) || lastErr || 'تحقق من صلاحيات الكاميرا'),'err');
        return false;
      }
      const switchBtnV73 = overlay.querySelector('#camSwitchV73');
      if(switchBtnV73){
        switchBtnV73.onclick = async()=>{
          preferBackCameraV73 = !preferBackCameraV73;
          switchBtnV73.textContent = preferBackCameraV73 ? 'تبديل الكاميرا' : 'الرجوع للخلفية';
          await startCameraV73();
        };
      }
      overlay.querySelector('#camShotV72').onclick=()=>{
        const w=640, h=480;
        const canvas=document.createElement('canvas'); canvas.width=w; canvas.height=h;
        const ctx=canvas.getContext('2d');
        ctx.fillStyle='#111'; ctx.fillRect(0,0,w,h);
        try{
          const vw=video.videoWidth||w, vh=video.videoHeight||h;
          const scale=Math.max(w/vw,h/vh), dw=vw*scale, dh=vh*scale, dx=(w-dw)/2, dy=(h-dh)/2;
          ctx.drawImage(video,dx,dy,dw,dh);
        }catch(e){}
        const lines=[info.title, 'المشرف: '+info.supervisorName, 'المشروع: '+info.projectName, 'الوقت: '+nowArabicLabel()];
        ctx.font='bold 24px Tahoma, Arial';
        const pad=16, lineH=34, boxH=pad*2+lines.length*lineH;
        ctx.fillStyle='rgba(10,64,51,.86)'; ctx.fillRect(0,h-boxH,w,boxH);
        ctx.fillStyle='#fff'; ctx.textAlign='right'; ctx.direction='rtl';
        lines.forEach((line,i)=>ctx.fillText(line,w-pad,h-boxH+pad+26+i*lineH));
        close(canvas.toDataURL('image/jpeg',0.72));
      };
      try{
        const ok = await startCameraV73();
        if(!ok) close(null);
      }catch(err){
        msg('تعذر فتح الكاميرا: '+(err.message||err),'err');
        close(null);
      }
    });
  }
  function workersForSupervisorText(supervisorId){
    const map=new Map();
    const projects=(data.projects||[]).filter(p=>String(p.supervisor_id||'')===String(supervisorId||''));
    const pids=new Set(projects.map(p=>String(p.id)));
    (data.workers||[]).forEach(w=>{
      const pid = (typeof workerProjectId === 'function') ? workerProjectId(w) : (w.project_id || w.assigned_project_id || '');
      if(!pids.has(String(pid))) return;
      const name=String(w.name||'').trim(); if(!name) return;
      const key=(typeof tasneefNormNameV60==='function'?tasneefNormNameV60(name):name.replace(/\s+/g,' '));
      if(!map.has(key)) map.set(key,name);
    });
    return [...map.values()].join('، ') || '-';
  }
  function attendanceWhatsappTextV72(type, row){
    const title = type==='out' ? 'انصراف المشرف وعماله' : 'حضور المشرف وعماله';
    const lines=[title,'','المشرف: '+supervisorName(row.supervisor_id),'المشروع: '+projectName(row.project_id),'نوع الزيارة: '+(typeof visitTypeText==='function'?visitTypeText(row.visit_type):row.visit_type||'-'),'العمال: '+workersForSupervisorText(row.supervisor_id),'التاريخ: '+(row.log_date||today()),(type==='out'?'وقت الخروج: ':'وقت الدخول: ')+timeOnly(type==='out'?row.check_out:row.check_in),'','شركة تصنيف لإدارة المرافق'];
    return lines.join('\n');
  }
  async function sharePhotoAndWhatsappV72(dataUrl, text, filename){
    try{
      if(dataUrl && navigator.canShare && navigator.share){
        const file=dataUrlToFile(dataUrl, filename||'tasneef-attendance.jpg');
        if(navigator.canShare({files:[file]})){
          await navigator.share({title:'تصنيف', text, files:[file]});
          return;
        }
      }
    }catch(e){}
    try{ await navigator.clipboard?.writeText(text); }catch(e){}
    const url='https://wa.me/?text='+encodeURIComponent(text + (dataUrl?'\n\nملاحظة: تم التقاط الصورة داخل التطبيق. إذا لم تُرفق تلقائيًا، احفظها من شاشة التصوير وأرفقها يدويًا.':''));
    window.open(url,'_blank');
  }
  async function saveTimeLogWithPhotoV72(type, photoDataUrl){
    const u=session();
    const id=$('logId')?.value;
    const date=$('logDate')?.value || today();
    let sup=$('logSupervisor')?.value || (u.role==='supervisor'?u.id:'');
    const project=$('logProject')?.value;
    if(!sup && project) sup=getProjectSupervisorId(project);
    const check_in=dateTime(date,$('logIn')?.value), check_out=dateTime(date,$('logOut')?.value);
    if(!project||!check_in) return {error:'المشروع ووقت الدخول مطلوبان'};
    const actual=minutesBetween(check_in,check_out);
    const required=requiredMinutesForLog(project,date);
    const ts=calcTimeStatus(actual,required);
    const autoTravel=calculateTravelMinutes(sup,date,check_in,id);
    const row={user_id:u.id, supervisor_id:Number(sup)||null, project_id:Number(project), check_in, check_out, log_date:date, duration_minutes:actual, travel_minutes:autoTravel, visit_type:$('logVisitType')?.value||findProject(project)?.visit_type_default||'surface', required_minutes:required, time_difference_minutes:ts.diff, time_status:ts.status, notes:$('logNotes')?.value||''};
    if(photoDataUrl){
      if(type==='out'){ row.check_out_photo=photoDataUrl; row.check_out_photo_at=new Date().toISOString(); }
      else { row.check_in_photo=photoDataUrl; row.check_in_photo_at=new Date().toISOString(); }
    }
    let res = id ? await sb.from('time_logs').update(row).eq('id',id).select('*').maybeSingle() : await sb.from('time_logs').insert(row).select('*').single();
    if(res.error && String(res.error.message||'').includes('photo')){
      delete row.check_in_photo; delete row.check_out_photo; delete row.check_in_photo_at; delete row.check_out_photo_at;
      res = id ? await sb.from('time_logs').update(row).eq('id',id).select('*').maybeSingle() : await sb.from('time_logs').insert(row).select('*').single();
      if(!res.error) msg('تم الحفظ، لكن أعمدة الصور غير موجودة. شغّل ملف schema_update_v72_permissions_camera.sql','err');
    }
    if(res.error) return {error:res.error.message};
    const savedRow = res.data ? res.data : Object.assign({id:Number(id)||0}, row);
    return {row:savedRow};
  }

  window.supervisorCheckIn = async function(){
    const u=session();
    if(!$('logProject')?.value) return msg('اختر المشروع','err');
    const pName=projectName($('logProject').value);
    const photo=await capturePhotoV72({title:'صورة حضور / دخول', supervisorName:u?.full_name||u?.username||'مشرف', projectName:pName});
    if(!photo) return msg('تم إلغاء التصوير، لم يتم تسجيل الدخول','err');
    $('logDate').value=today(); $('logIn').value=nowTime(); $('logOut').value='';
    const saved=await saveTimeLogWithPhotoV72('in', photo);
    if(saved.error) return msg(saved.error,'err');
    playAppSound('checkin'); msg('تم تسجيل الدخول بالصورة');
    const text=attendanceWhatsappTextV72('in', saved.row);
    await sharePhotoAndWhatsappV72(photo, text, 'tasneef-checkin-'+Date.now()+'.jpg');
    clearLogForm(); await initSupervisor();
  };
  window.supervisorCheckOut = async function(){
    const u=session(); const pid=$('logProject')?.value;
    if(!pid) return msg('اختر المشروع','err');
    const todayStr=today();
    const open=(data.logs||[]).filter(l=>String(l.project_id)===String(pid)&&String(l.supervisor_id)===String(u.id)&&(!l.check_out)&&(l.log_date||String(l.check_in||'').slice(0,10))===todayStr).sort((a,b)=>new Date(b.check_in)-new Date(a.check_in))[0];
    if(open) editTimeLog(open.id); else { $('logDate').value=todayStr; $('logIn').value=$('logIn').value||nowTime(); }
    const pName=projectName(pid);
    const photo=await capturePhotoV72({title:'صورة انصراف / خروج', supervisorName:u?.full_name||u?.username||'مشرف', projectName:pName});
    if(!photo) return msg('تم إلغاء التصوير، لم يتم تسجيل الخروج','err');
    $('logOut').value=nowTime();
    const saved=await saveTimeLogWithPhotoV72('out', photo);
    if(saved.error) return msg(saved.error,'err');
    playAppSound('checkout'); msg('تم تسجيل الخروج بالصورة');
    const text=attendanceWhatsappTextV72('out', saved.row);
    await sharePhotoAndWhatsappV72(photo, text, 'tasneef-checkout-'+Date.now()+'.jpg');
    clearLogForm(); await initSupervisor();
  };

  // تطبيق أولي بعد التحميل
  setTimeout(()=>{ try{ injectPermissionsBox(); applyCurrentPermissions(); }catch(e){} }, 1200);
})();

/* ===== V82: Monthly required total by unique visit days + V81 clarity/CSV BOM ===== */
(function(){
  'use strict';

  function normMinsV81(v){
    v = Number(v || 0);
    return Number.isFinite(v) ? v : 0;
  }
  function projectDailyRequiredMinutesV81(projectId){
    const p = (typeof findProject === 'function') ? findProject(projectId) : (data.projects||[]).find(x=>String(x.id)===String(projectId));
    return normMinsV81(p?.required_daily_minutes ?? 0);
  }
  function projectFridayRequiredMinutesV81(projectId){
    const p = (typeof findProject === 'function') ? findProject(projectId) : (data.projects||[]).find(x=>String(x.id)===String(projectId));
    return normMinsV81(p?.friday_minutes ?? 0);
  }
  function dailyRequiredTextV81(projectId){
    const daily = projectDailyRequiredMinutesV81(projectId);
    const fri = projectFridayRequiredMinutesV81(projectId);
    if(fri && daily && fri !== daily) return `${minsToText(daily)} / الجمعة ${minsToText(fri)}`;
    if(daily) return minsToText(daily);
    return 'غير محدد';
  }

  // Keep the same aggregation logic, but add daily required text separately from monthly total required.
  window.monthlyRowsV60 = function(){
    const month = $('monthlyMonth')?.value || today().slice(0,7);
    const sid = $('monthlySupervisor')?.value;
    let logs = (data.logs||[]).filter(l=>{
      const d = l.log_date || String(l.check_in||'').slice(0,10);
      return d && d.slice(0,7) === month;
    });
    if(sid) logs = logs.filter(l=>String(l.supervisor_id) === String(sid));

    const map = new Map();
    logs.forEach(l=>{
      const k = String(l.supervisor_id||'') + '_' + String(l.project_id||'');
      if(!map.has(k)) map.set(k,{s:l.supervisor_id,p:l.project_id,a:0,r:0,t:0, days:new Set(), reqByDay:{}});
      const x = map.get(k);
      const logDate = l.log_date || String(l.check_in||'').slice(0,10);
      const isCompleted = !!(l.check_in && l.check_out);
      x.a += Number((typeof logActualMinutes==='function' ? logActualMinutes(l) : (l.duration_minutes || minutesBetween(l.check_in,l.check_out))) || 0);
      // V82: إجمالي الوقت المطلوب يحسب حسب عدد أيام الزيارة الفعلية، وليس عدد السجلات المكررة.
      // إذا وُجد أكثر من سجل لنفس المشروع في نفس اليوم، يحسب الوقت المطلوب مرة واحدة فقط لذلك اليوم.
      if(isCompleted && logDate){
        x.days.add(logDate);
        if(x.reqByDay[logDate] === undefined){
          x.reqByDay[logDate] = Number((typeof logRequiredMinutes==='function' ? logRequiredMinutes(l) : l.required_minutes) || 0);
        }
      }
      x.t += Number(l.travel_minutes || 0);
    });
    map.forEach(x=>{
      x.uniqueDays = x.days ? x.days.size : 0;
      x.r = Object.values(x.reqByDay || {}).reduce((a,v)=>a+Number(v||0),0);
    });

    const vals=[...map.values()];
    const supTotals={};
    vals.forEach(r=>{ const s=String(r.s||''); supTotals[s]=(supTotals[s]||0)+Number(r.a||0); });

    return vals.map(r=>{
      const supTotal = supTotals[String(r.s||'')] || 0;
      const workPercent = supTotal ? (r.a/supTotal*100) : 0;
      const commitmentPercent = r.r ? (r.a/r.r*100) : 0;
      const diff = r.a-r.r;
      const st = monthlyStatusFromDiffV60(diff,r.r);
      return {
        ...r,
        supTotal,
        workers: (typeof uniqueWorkersForProjectTextV60==='function' ? uniqueWorkersForProjectTextV60(r.p) : '-'),
        dailyReqText: dailyRequiredTextV81(r.p),
        dailyReqMinutes: projectDailyRequiredMinutesV81(r.p),
        workPercent,
        commitmentPercent,
        ccls: monthlyCommitmentClassV60(commitmentPercent,r.r),
        diff,
        st: st.text,
        cls: st.cls
      };
    }).sort((a,b)=>{
      const s = supervisorName(a.s).localeCompare(supervisorName(b.s),'ar');
      return s || projectName(a.p).localeCompare(projectName(b.p),'ar');
    });
  };

  window.monthlyBaseRowsV59 = function(){ return monthlyRowsV60(); };
  window.monthlyReportRowsV58 = function(){ return monthlyRowsV60(); };

  window.renderMonthly = function(){
    const body = $('monthlyBody');
    if(!body) return;
    const table = body.closest('table');
    if(table && table.tHead){
      table.tHead.innerHTML = '<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الوقت اليومي المطلوب</th><th>إجمالي الوقت المطلوب</th><th>الساعات الفعلية</th><th>وقت الانتقال</th><th>نسبة العمل</th><th>نسبة الالتزام</th><th>حالة الأداء</th></tr>';
    }
    const vals = monthlyRowsV60();
    body.innerHTML = vals.map(r=>`<tr><td>${esc(supervisorName(r.s))}</td><td>${esc(projectName(r.p))}</td><td>${esc(r.workers)}</td><td>${esc(r.dailyReqText)}</td><td>${minsToText(r.r)}</td><td>${minsToText(r.a)}</td><td>${r.t} دقيقة</td><td><span class="badge green">${percentText(r.workPercent)}</span></td><td><span class="badge ${r.ccls}">${percentText(r.commitmentPercent)}</span></td><td><span class="badge ${r.cls}">${r.st}</span></td></tr>`).join('') || '<tr><td colspan="10">لا توجد بيانات</td></tr>';

    const total = vals.reduce((a,r)=>a+r.a,0), required = vals.reduce((a,r)=>a+r.r,0), travel = vals.reduce((a,r)=>a+r.t,0), commitment = required ? total/required*100 : 0;
    const diff = total-required, st = monthlyStatusFromDiffV60(diff,required);
    if($('monthlySummary')) $('monthlySummary').innerHTML = `<div class="kpi"><small>إجمالي الوقت المطلوب</small><b>${minsToText(required)}</b></div><div class="kpi"><small>الساعات الفعلية</small><b>${minsToText(total)}</b></div><div class="kpi"><small>فرق الوقت</small><b>${monthlyDiffTextV57(diff)}</b></div><div class="kpi"><small>وقت الانتقال</small><b>${travel} دقيقة</b></div><div class="kpi"><small>نسبة الالتزام</small><b>${percentText(commitment)}</b></div><div class="kpi"><small>حالة الأداء</small><b><span class="badge ${st.cls}">${st.text}</span></b></div>`;
  };

  // CSV with UTF-8 BOM so Arabic opens correctly in Excel.
  window.download = function(name,text){
    const blob = new Blob(['\uFEFF' + String(text||'')], {type:'text/csv;charset=utf-8;'});
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = name;
    a.click();
    URL.revokeObjectURL(a.href);
  };

  window.exportMonthlyCSV = function(){
    const rows=[...document.querySelectorAll('#monthlyBody tr')].map(tr=>[...tr.children].map(td=>td.textContent.trim()));
    const csv=['المشرف,المشروع,أسماء العمال,الوقت اليومي المطلوب,إجمالي الوقت المطلوب,الساعات الفعلية,وقت الانتقال,نسبة العمل,نسبة الالتزام,حالة الأداء',...rows.map(r=>r.map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))].join('\n');
    download('monthly.csv',csv);
  };

  window.printMonthlyReportV57 = function(){
    const rows = monthlyRowsV60();
    if(!rows.length){ msg('لا توجد بيانات في الأوقات الشهرية للطباعة','err'); return; }
    const month = $('monthlyMonth')?.value || today().slice(0,7), sup = $('monthlySupervisor')?.value ? supervisorName($('monthlySupervisor').value) : 'الكل';
    let actualTotal=0, requiredTotal=0, within=0, over=0, under=0;
    rows.forEach(r=>{ actualTotal+=r.a; requiredTotal+=r.r; if(r.st==='ضمن الوقت') within++; else if(r.st==='زيادة وقت') over++; else if(r.st==='ناقص وقت') under++; });
    const commitmentTotal = requiredTotal ? actualTotal/requiredTotal*100 : 0;
    const groups = new Map();
    rows.forEach(r=>{ const sid=String(r.s||''); if(!groups.has(sid)) groups.set(sid,[]); groups.get(sid).push(r); });
    const groupCards=[...groups.entries()].map(([sid,items])=>{
      const sActual=items.reduce((a,r)=>a+r.a,0), sReq=items.reduce((a,r)=>a+r.r,0), sCommit=sReq?sActual/sReq*100:0;
      const projects=items.map(r=>`<tr><td class="pname">${reportEscV52(projectName(r.p))}</td><td>${reportEscV52(r.dailyReqText)}</td><td>${reportEscV52(minsToText(r.r))}</td><td>${reportEscV52(minsToText(r.a))}</td><td><span class="percent ok">${reportEscV52(percentText(r.workPercent))}</span></td></tr>`).join('');
      return `<div class="super-card"><div class="super-head"><b>${reportEscV52(supervisorName(sid))}</b><span>${reportEscV52(minsToText(sActual))}</span></div><table class="mini"><thead><tr><th>المشروع</th><th>اليومي</th><th>المطلوب</th><th>الفعلي</th><th>نسبة العمل</th></tr></thead><tbody>${projects}</tbody></table><div class="workers"><b>أسماء العمال</b><p>${reportEscV52(supervisorWorkersForMonthlyV58(sid,rows))}</p></div><div class="commitment">نسبة الالتزام: <b>${reportEscV52(percentText(sCommit))}</b></div></div>`;
    }).join('');
    const detailRows=rows.map(r=>`<tr><td>${reportEscV52(supervisorName(r.s))}</td><td>${reportEscV52(projectName(r.p))}</td><td>${reportEscV52(r.workers||'-')}</td><td>${reportEscV52(r.dailyReqText)}</td><td>${reportEscV52(minsToText(r.r))}</td><td>${reportEscV52(minsToText(r.a))}</td><td>${reportEscV52(monthlyDiffTextV57(r.diff))}</td><td><span class="percent ok">${reportEscV52(percentText(r.workPercent))}</span></td><td><span class="percent ${r.ccls}">${reportEscV52(percentText(r.commitmentPercent))}</span></td><td><span class="pill ${r.cls}">${reportEscV52(r.st)}</span></td></tr>`).join('');
    const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{margin:0;font-family:Tahoma,Arial,sans-serif;color:#123d32;background:#fff;font-size:11px}.page{min-height:100vh;padding:14px;background:radial-gradient(circle at top left,rgba(10,90,73,.10),transparent 32%),linear-gradient(135deg,#fff 0%,#fff 62%,rgba(199,162,77,.08));border:2px solid #0a5a49}.top{display:flex;align-items:center;justify-content:space-between;border-bottom:3px solid #0a5a49;padding-bottom:10px;margin-bottom:10px}.brand{display:flex;align-items:center;gap:10px}.logo{width:54px;height:54px;border-radius:50%;border:3px solid #c7a24d;display:grid;place-items:center;font-weight:900;color:#0a5a49}.brand h2{margin:0;font-size:19px;color:#0a5a49}.title{text-align:left}.title h1{margin:0;font-size:28px;color:#0a5a49}.title p{margin:4px 0 0;color:#68766e}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:10px 0}.box{background:#fff;border:1px solid #d9e6e1;border-radius:13px;padding:9px;text-align:center}.box b{display:block;color:#63756d;font-size:10px}.box strong{font-size:16px;color:#0a5a49}.section{margin:12px 0 8px;text-align:center}.section span{display:inline-block;background:#0a5a49;color:#fff;border:2px solid #c7a24d;border-radius:999px;padding:7px 40px;font-weight:900}.super-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.super-card{background:#fff;border:2px solid #0a5a49;border-radius:10px;overflow:hidden;min-height:220px}.super-head{display:flex;justify-content:space-between;align-items:center;background:#f7fbfa;border-bottom:1px solid #d9e6e1;padding:7px 9px;color:#0a5a49}.mini,.details{width:100%;border-collapse:collapse}.mini th{background:#0a5a49;color:#fff;padding:5px;font-size:9px}.mini td{border-bottom:1px solid #e5eeee;padding:4px;text-align:center}.pname{text-align:right!important;font-weight:700}.workers{padding:7px;text-align:center}.workers b{display:block;color:#0a5a49;margin-bottom:4px}.workers p{margin:0;line-height:1.6;color:#243b34}.commitment{padding:6px;text-align:center;border-top:1px solid #e5eeee;color:#0a5a49}.details{margin-top:8px;border-radius:10px;overflow:hidden}.details th{background:#0a5a49;color:#fff;padding:6px}.details td{border:1px solid #e0e8e5;padding:5px;text-align:center}.percent,.pill{display:inline-block;border-radius:999px;padding:3px 8px;font-weight:900}.ok{background:#e5f6ec;color:#107338}.warn{background:#fff3d6;color:#8a5c00}.bad{background:#ffe5e5;color:#9d2020}.neutral{background:#edf1f4;color:#52616b}.kpis{display:grid;grid-template-columns:repeat(6,1fr);gap:8px;margin-top:8px}.kpi{background:#fff;border:1px solid #d9e6e1;border-radius:13px;padding:9px;text-align:center}.kpi strong{display:block;color:#0a5a49;font-size:16px}.kpi span{font-size:10px;color:#68766e}.bottom{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:10px}.panel{background:#fff;border:1px solid #d9e6e1;border-radius:12px;padding:9px}.panel h3{margin:0 0 8px;color:#0a5a49}.line{height:22px;border-bottom:1px dashed #afbeb8}.footer{text-align:center;margin-top:8px;color:#6a766f}.avoid{break-inside:avoid}@media print{body{print-color-adjust:exact;-webkit-print-color-adjust:exact}.page{border-radius:0}.super-card,.kpi,.panel{break-inside:avoid}}</style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h2>شركة تصنيف</h2><small>إدارة المرافق والتشغيل</small></div></div><div class="title"><h1>تقرير الأوقات الشهرية</h1><p>المسؤول: وائل شاكر</p></div></div><div class="meta"><div class="box"><b>الشهر</b><strong>${reportEscV52(monthLabelV57(month))}</strong></div><div class="box"><b>المشرف</b><strong>${reportEscV52(sup)}</strong></div><div class="box"><b>عدد المشاريع</b><strong>${rows.length}</strong></div><div class="box"><b>نسبة الالتزام الإجمالية</b><strong>${reportEscV52(percentText(commitmentTotal))}</strong></div></div><div class="section"><span>ملخص المشرفين والمشاريع</span></div><div class="super-grid avoid">${groupCards}</div><div class="section"><span>تفاصيل الأوقات الشهرية</span></div><table class="details"><thead><tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الوقت اليومي المطلوب</th><th>إجمالي الوقت المطلوب</th><th>الوقت الفعلي</th><th>الفرق</th><th>نسبة العمل</th><th>نسبة الالتزام</th><th>حالة الوقت</th></tr></thead><tbody>${detailRows}</tbody></table><div class="section"><span>ملخص التقرير</span></div><div class="kpis"><div class="kpi"><strong>${reportEscV52(minsToText(actualTotal))}</strong><span>إجمالي الوقت الفعلي</span></div><div class="kpi"><strong>${reportEscV52(minsToText(requiredTotal))}</strong><span>إجمالي الوقت المطلوب</span></div><div class="kpi"><strong>${reportEscV52(monthlyDiffTextV57(actualTotal-requiredTotal))}</strong><span>إجمالي فرق الوقت</span></div><div class="kpi"><strong>${reportEscV52(percentText(commitmentTotal))}</strong><span>نسبة الالتزام</span></div><div class="kpi"><strong>${over}</strong><span>زيادة وقت</span></div><div class="kpi"><strong>${under}</strong><span>ناقص وقت</span></div></div><div class="bottom"><div class="panel"><h3>ملاحظات المدير</h3><div class="line"></div><div class="line"></div></div><div class="panel"><h3>اعتماد مدير التشغيل</h3><p>الاسم: وائل شاكر</p><div class="line">التوقيع:</div></div></div><div class="footer">ملاحظة: الوقت اليومي المطلوب هو وقت المشروع في اليوم الواحد. إجمالي الوقت المطلوب = اليومي × عدد أيام الزيارة الفعلية في الفترة. نسبة العمل = وقت المشروع ÷ إجمالي وقت المشرف. نسبة الالتزام = الفعلي ÷ إجمالي المطلوب.</div><script>window.onload=function(){setTimeout(function(){window.print()},400)}</script></div></body></html>`;
    const w=window.open('','_blank');
    if(!w){ msg('المتصفح منع فتح نافذة التقرير. اسمح بالنوافذ المنبثقة','err'); return; }
    w.document.open(); w.document.write(html); w.document.close();
  };

  setTimeout(()=>{ try{ if($('monthlyBody')) renderMonthly(); }catch(e){} }, 800);
})();


/* ===== V84: Internal Tasneef Assistant - no API ===== */
(function(){
  let assistantMessages = [];
  let assistantLastReply = '';
  function normAr(s){return String(s||'').toLowerCase().replace(/[أإآ]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه').replace(/\s+/g,' ').trim();}
  function safe(s){return (typeof esc==='function'?esc(s):String(s??''));}
  function getProjectByText(text){
    const n=normAr(text);
    return (data.projects||[]).find(p=>n.includes(normAr(p.name))) || null;
  }
  function getSupervisorByText(text){
    const n=normAr(text);
    return (data.supervisors||data.users||[]).find(u=>u.role==='supervisor' && n.includes(normAr(u.full_name||u.username))) || null;
  }
  function arDate(d=new Date()){try{return d.toLocaleDateString('ar-SA',{year:'numeric',month:'2-digit',day:'2-digit'})}catch(e){return today();}}
  function projectWorkersText(projectId){
    const rows=(data.workers||[]).filter(w=>String(workerProjectId?.(w)||w.project_id||'')===String(projectId));
    const names=[...new Set(rows.map(w=>w.name).filter(Boolean))];
    return names.length?names.join('، '):'حسب فريق المشروع';
  }
  function makeReport(q){
    const p=getProjectByText(q); const project=p?.name || 'اسم المشروع';
    const n=normAr(q);
    let title='تقرير تنفيذ أعمال'; let body='تم تنفيذ الأعمال المطلوبة بالمشروع وفق الخطة التشغيلية المعتمدة، مع متابعة جودة التنفيذ والتأكد من نظافة الموقع بعد الانتهاء.';
    if(n.includes('مكافحه')||n.includes('حشرات')||n.includes('مبيد')){title='تقرير مكافحة الحشرات';body='تم تنفيذ أعمال مكافحة الحشرات في مرافق المشروع، مع التركيز على الممرات والمداخل ومناطق الخدمات، وتمت المتابعة للتأكد من سلامة التنفيذ.';}
    else if(n.includes('خزان')||n.includes('خزانات')){title='تقرير تنظيف الخزانات';body='تم تنفيذ أعمال تنظيف وغسيل الخزانات حسب الإجراءات التشغيلية، مع العناية بمراحل التنظيف والشطف والتأكد من جاهزية الخزانات بعد الانتهاء.';}
    else if(n.includes('بيسمنت')||n.includes('قبو')){title='تقرير تنظيف البيسمنت';body='تم تنفيذ أعمال تنظيف البيسمنت وإزالة الأتربة والمخلفات، مع ترتيب الموقع وتحسين مستوى النظافة العام.';}
    else if(n.includes('سطح')||n.includes('اسطح')){title='تقرير تنظيف الأسطح';body='تم تنفيذ أعمال تنظيف الأسطح وإزالة الأتربة والمخلفات، مع التأكد من نظافة مجاري التصريف والمناطق المحيطة.';}
    else if(n.includes('صيان')){title='تقرير صيانة';body='تمت مباشرة أعمال الصيانة المطلوبة ومعالجة الملاحظة حسب المتاح في الموقع، وسيتم متابعة الحالة لضمان عدم تكرار المشكلة.';}
    return `السلام عليكم ورحمة الله وبركاته\n\n${title}\n\nالمشروع: ${project}\nالتاريخ: ${arDate()}\n\n${body}\n\nوتفضلوا بقبول فائق التحية والتقدير\nشركة تصنيف لإدارة المرافق`;
  }
  function makeMessage(q){
    const p=getProjectByText(q); const project=p?.name || 'اسم المشروع'; const n=normAr(q);
    if(n.includes('خزان')||n.includes('خزانات')) return `السلام عليكم ورحمة الله وبركاته\n\nالسادة سكان / ملاك مشروع ${project} المحترمين\n\nنفيدكم بأنه سيتم تنفيذ أعمال غسيل الخزانات حسب الموعد المحدد، ونأمل من الجميع أخذ العلم باحتمالية تأثر المياه خلال فترة العمل.\n\nشاكرين لكم تعاونكم.\nشركة تصنيف لإدارة المرافق`;
    if(n.includes('حشرات')||n.includes('مبيد')) return `السلام عليكم ورحمة الله وبركاته\n\nالسادة سكان / ملاك مشروع ${project} المحترمين\n\nنفيدكم بأنه سيتم تنفيذ أعمال مكافحة الحشرات في المشروع، ونأمل إغلاق النوافذ وإبعاد الأغراض الشخصية عن مناطق الرش قدر الإمكان.\n\nشاكرين لكم تعاونكم.\nشركة تصنيف لإدارة المرافق`;
    if(n.includes('اعتماد')) return `السلام عليكم ورحمة الله وبركاته\n\nمرفق لسيادتكم طلب اعتماد بخصوص مشروع ${project}.\nنأمل الاطلاع والتوجيه بما ترونه مناسبًا حتى نتمكن من استكمال الإجراء.\n\nوتفضلوا بقبول فائق التحية والتقدير\nشركة تصنيف لإدارة المرافق`;
    return `السلام عليكم ورحمة الله وبركاته\n\nبخصوص مشروع ${project}:\nنفيدكم بأنه تم متابعة الملاحظة، وسيتم اتخاذ اللازم حسب الخطة التشغيلية.\n\nشركة تصنيف لإدارة المرافق`;
  }
  function openLogsReply(){
    const d=today(); const rows=(data.logs||[]).filter(l=>(l.log_date||String(l.check_in||'').slice(0,10))===d && l.check_in && !l.check_out);
    if(!rows.length) return 'لا توجد سجلات مفتوحة اليوم. جميع السجلات الحالية مكتملة أو لا توجد بيانات مفتوحة.';
    return 'السجلات المفتوحة اليوم:\n\n'+rows.map((l,i)=>`${i+1}. المشرف: ${supervisorName(l.supervisor_id)}\nالمشروع: ${projectName(l.project_id)}\nوقت الدخول: ${timeOnly(l.check_in)}`).join('\n\n');
  }
  function weakMonthlyReply(){
    const month=($('monthlyMonth')?.value)||today().slice(0,7); const start=month+'-01'; const end=month+'-31';
    const groups={};
    (data.logs||[]).filter(l=>{const d=l.log_date||String(l.check_in||'').slice(0,10);return d>=start&&d<=end&&l.check_in&&l.check_out;}).forEach(l=>{
      const key=[l.supervisor_id,l.project_id].join('|'); if(!groups[key]) groups[key]={sid:l.supervisor_id,pid:l.project_id,actual:0,requiredDays:new Set(),dailyReq:0};
      const d=l.log_date||String(l.check_in||'').slice(0,10); groups[key].actual += logActualMinutes(l); groups[key].requiredDays.add(d); groups[key].dailyReq = logRequiredMinutes(l)||groups[key].dailyReq;
    });
    const weak=Object.values(groups).map(g=>{const req=(g.dailyReq||0)*g.requiredDays.size; const pct=req?g.actual/req*100:0; return {...g,req,pct};}).filter(g=>g.req && g.pct<90).sort((a,b)=>a.pct-b.pct).slice(0,12);
    if(!weak.length) return `لا توجد مشاريع ناقصة وقت في شهر ${month} حسب البيانات الحالية.`;
    return `المشاريع الناقصة وقت في شهر ${month}:\n\n`+weak.map((g,i)=>`${i+1}. ${projectName(g.pid)} — ${supervisorName(g.sid)}\nالفعلي: ${minsToText(g.actual)} / المطلوب: ${minsToText(g.req)} — الالتزام: ${g.pct.toFixed(1)}%`).join('\n\n');
  }
  function dailySummaryReply(){
    const d=today(); const rows=(data.logs||[]).filter(l=>(l.log_date||String(l.check_in||'').slice(0,10))===d);
    const done=rows.filter(l=>l.check_in&&l.check_out); const open=rows.filter(l=>l.check_in&&!l.check_out); const mins=done.reduce((a,l)=>a+logActualMinutes(l),0);
    return `ملخص تشغيل اليوم ${d}:\n\nعدد التسجيلات: ${rows.length}\nالسجلات المكتملة: ${done.length}\nالسجلات المفتوحة: ${open.length}\nإجمالي ساعات العمل: ${minsToText(mins)}\n\n${open.length?'يوجد سجلات مفتوحة تحتاج متابعة.':'لا توجد سجلات مفتوحة حاليًا.'}`;
  }
  function designNotice(q){
    const p=getProjectByText(q); const project=p?.name||'اسم المشروع'; const n=normAr(q); let title='إشعار هام'; let line='نرجو من الجميع الالتزام بما ورد في هذا الإشعار.';
    if(n.includes('قمامه')||n.includes('نفايات')){title='موعد جمع النفايات'; line='موعد جمع النفايات اليومي: 4:00 عصرًا. نأمل إخراج النفايات في الوقت المحدد.';}
    else if(n.includes('سيارات')||n.includes('ابعاد')){title='تنبيه إبعاد السيارات'; line='نأمل إبعاد السيارات عن منطقة العمل خلال الفترة المحددة لتسهيل تنفيذ الأعمال.';}
    else if(n.includes('خزان')){title='إشعار غسيل الخزانات'; line='سيتم تنفيذ أعمال غسيل الخزانات حسب الموعد المحدد، ونأمل أخذ الاحتياطات اللازمة.';}
    return `${title}\n\nالمشروع: ${project}\n\n${line}\n\nشركة تصنيف لإدارة المرافق`;
  }
  function fallbackReply(){return 'أقدر أساعدك في:\n1. كتابة تقرير تشغيل أو تنظيف أو صيانة\n2. كتابة رسالة واتساب أو خطاب\n3. عرض السجلات المفتوحة اليوم\n4. تحليل المشاريع الناقصة وقت هذا الشهر\n5. إنشاء نص إعلان جاهز للطباعة\n\nاكتب طلبك مثل: اكتب تقرير مكافحة حشرات لمشروع صفاء 50';}
  function answer(q){
    const n=normAr(q);
    if(n.includes('سجلات مفتوحه')||n.includes('بدون خروج')||n.includes('لم يسجل خروج')) return openLogsReply();
    if(n.includes('ناقص') && (n.includes('وقت')||n.includes('شهر'))) return weakMonthlyReply();
    if(n.includes('ملخص')||n.includes('تحليل اليوم')||n.includes('تشغيل اليوم')) return dailySummaryReply();
    if(n.includes('تقرير')) return makeReport(q);
    if(n.includes('رساله')||n.includes('واتساب')||n.includes('خطاب')) return makeMessage(q);
    if(n.includes('تصميم')||n.includes('اعلان')||n.includes('اشعار')||n.includes('تنبيه')) return designNotice(q);
    return fallbackReply();
  }
  function addMsg(role,text){assistantMessages.push({role,text}); assistantLastReply=role==='bot'?text:assistantLastReply; renderTasneefAssistant();}
  window.renderTasneefAssistant=function(){
    const box=$('assistantChat'); if(!box) return;
    if(!assistantMessages.length){box.innerHTML=`<div class="assistant-empty">اكتب طلبك أو اختر أحد الأزرار السريعة. هذا المساعد يعمل بدون API وبدون تكلفة.</div>`; return;}
    box.innerHTML=assistantMessages.map(m=>`<div class="assistant-msg ${m.role==='user'?'user':'bot'}"><b>${m.role==='user'?'أنت':'مساعد تصنيف'}</b><pre>${safe(m.text)}</pre></div>`).join('');
    box.scrollTop=box.scrollHeight;
  };
  window.assistantSend=function(){const input=$('assistantInput'); const q=(input?.value||'').trim(); if(!q) return msg('اكتب طلبك أولًا','err'); addMsg('user',q); input.value=''; addMsg('bot',answer(q));};
  window.assistantQuick=function(q){const input=$('assistantInput'); if(input) input.value=q; window.assistantSend();};
  window.assistantCopyLast=function(){if(!assistantLastReply)return msg('لا يوجد رد لنسخه','err'); (navigator.clipboard?.writeText(assistantLastReply)||Promise.resolve()).then(()=>msg('تم نسخ الرد'));};
  window.assistantWhatsappLast=function(){if(!assistantLastReply)return msg('لا يوجد رد لإرساله','err'); try{navigator.clipboard?.writeText(assistantLastReply)}catch(e){} window.open('https://wa.me/?text='+encodeURIComponent(assistantLastReply),'_blank');};
  window.assistantPrintLast=function(){if(!assistantLastReply)return msg('لا يوجد رد للطباعة','err'); const w=window.open('','_blank'); w.document.write(`<!doctype html><html dir="rtl" lang="ar"><head><meta charset="utf-8"><title>مساعد تصنيف</title><style>body{font-family:Tahoma,Arial;padding:30px;line-height:1.9;white-space:pre-wrap;color:#0A4033}</style></head><body>${safe(assistantLastReply)}<script>window.onload=()=>setTimeout(()=>print(),300)<\/script></body></html>`); w.document.close();};
  window.assistantClear=function(){assistantMessages=[]; assistantLastReply=''; renderTasneefAssistant();};
})();



/* ===== V89 Manual Contract Services - Clean Rebuild ===== */
function showContractsSubTab(tab){
  const contracts=$('contractsSubTab'), services=$('servicesSubTab');
  if(contracts) contracts.classList.toggle('hidden', tab!=='contracts');
  if(services) services.classList.toggle('hidden', tab!=='services');
  $('contractsTabBtn')?.classList.toggle('light', tab!=='contracts');
  $('servicesTabBtn')?.classList.toggle('light', tab!=='services');
  if(tab==='services') renderContractServices();
}
function normalizeArV89(s){return String(s||'').trim().replace(/[أإآ]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه').replace(/\s+/g,' ')}
function csProjectObj(s){
  const pid=s?.project_id;
  if(pid){const p=data.projects.find(x=>String(x.id)===String(pid)); if(p) return p;}
  const n=normalizeArV89(s?.project_name||'');
  if(!n) return null;
  return data.projects.find(p=>normalizeArV89(p.name)===n || normalizeArV89(p.name).includes(n) || n.includes(normalizeArV89(p.name))) || null;
}
function csProjectName(s){return csProjectObj(s)?.name || s?.project_name || '-'}
function csSupervisorId(s){return s?.supervisor_id || csProjectObj(s)?.supervisor_id || null}
function csSupervisorName(s){return s?.supervisor_name || supervisorName(csSupervisorId(s)) || '-'}
function csServiceName(s){return s?.service_name || '-'}
function csServiceType(s){return s?.service_type || s?.service_name || 'أخرى'}
function csFrequency(s){return s?.frequency || 'مرة واحدة'}
function csVisits(s){return Number(s?.visit_count||1)||1}
function csDone(s){return Number(s?.executed_count||0)||0}
function csRemaining(s){const rem=s?.remaining_count; if(rem!==undefined&&rem!==null&&String(rem)!=='') return Math.max(Number(rem)||0,0); return Math.max(csVisits(s)-csDone(s),0)}
function csLastDate(s){return isoDate(s?.last_execution_date||'')}
function csDueDate(s){return isoDate(s?.next_due_date||'')}
function csExecutor(s){return s?.executor_name || '-'}
function csNotes(s){return s?.notes || ''}
function csStatusKey(s){
  const explicit=normalizeArV89(s?.status||'');
  const due=csDueDate(s); const t=today();
  if(explicit.includes('خارج')) return 'out';
  if(explicit.includes('متوقف')) return 'stopped';
  if(explicit.includes('منجز')) return 'done';
  if(explicit.includes('متاخر')) return 'late';
  if(explicit.includes('قريب')) return 'soon';
  if(explicit.includes('مستحق')) return 'due';
  if(explicit.includes('مراجعه') || explicit.includes('غير منفذ')) return 'review';
  if(due && due<t) return 'late';
  if(due){ const diff=Math.round((new Date(due+'T00:00:00')-new Date(t+'T00:00:00'))/86400000); if(diff>=0 && diff<=7) return 'soon'; }
  if(csRemaining(s)<=0 && csDone(s)>0) return 'done';
  return 'due';
}
function csStatusText(key){return {done:'منجزة',due:'مستحقة',soon:'قريبة',late:'متأخرة',review:'مراجعة / غير منفذة',out:'خارج العقد',stopped:'متوقفة'}[key]||'مستحقة'}
function csBadgeClass(key){return key==='done'?'green':key==='late'?'red':key==='soon'?'amber':key==='out'?'neutral':key==='stopped'?'neutral':'amber'}
function serviceById(id){return (data.contractServices||[]).find(s=>String(s.id)===String(id));}
function hydrateServiceTypes(){
  const el=$('serviceFilterType'); if(!el) return;
  const current=el.value;
  const types=[...new Set((data.contractServices||[]).map(csServiceType).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'ar'));
  el.innerHTML='<option value="">كل أنواع الخدمة</option>'+types.map(t=>`<option value="${esc(t)}">${esc(t)}</option>`).join('');
  el.value=current;
}
function getFilteredContractServices(){
  let rows=[...(data.contractServices||[])];
  const q=normalizeArV89($('serviceSearch')?.value||'');
  const pid=$('serviceFilterProject')?.value||'';
  const sid=$('serviceFilterSupervisor')?.value||'';
  const st=$('serviceFilterStatus')?.value||'';
  const typ=$('serviceFilterType')?.value||'';
  const d=$('serviceFilterDate')?.value||'';
  if(q) rows=rows.filter(s=>normalizeArV89([csProjectName(s),csSupervisorName(s),csServiceName(s),csServiceType(s),csNotes(s)].join(' ')).includes(q));
  if(pid) rows=rows.filter(s=>String(csProjectObj(s)?.id||s.project_id||'')===String(pid));
  if(sid) rows=rows.filter(s=>String(csSupervisorId(s)||'')===String(sid));
  if(st) rows=rows.filter(s=>csStatusKey(s)===st);
  if(typ) rows=rows.filter(s=>csServiceType(s)===typ);
  if(d) rows=rows.filter(s=>csDueDate(s)===d || csLastDate(s)===d);
  return rows.sort((a,b)=>String(csDueDate(a)||'9999').localeCompare(String(csDueDate(b)||'9999')) || String(csProjectName(a)).localeCompare(String(csProjectName(b)),'ar'));
}
function renderContractServices(){
  if(!$('contractServicesBody')) return;
  hydrateServiceTypes();
  financeHydrateForms();
  const rows=getFilteredContractServices();
  const all=data.contractServices||[];
  const counts={total:all.length,due:0,soon:0,done:0,late:0,review:0};
  all.forEach(s=>{const k=csStatusKey(s); if(k==='done')counts.done++; else if(k==='late')counts.late++; else if(k==='soon')counts.soon++; else if(k==='review')counts.review++; else if(k==='due')counts.due++;});
  if($('servicesTotalCount')) $('servicesTotalCount').textContent=counts.total;
  if($('servicesDueCount')) $('servicesDueCount').textContent=counts.due;
  if($('servicesDoneCount')) $('servicesDoneCount').textContent=counts.done;
  if($('servicesSoonCount')) $('servicesSoonCount').textContent=counts.soon;
  if($('servicesLateCount')) $('servicesLateCount').textContent=counts.late;
  if($('servicesReviewCount')) $('servicesReviewCount').textContent=counts.review;
  const emptyMsg = data.contractServicesError ? ('تعذر تحميل الخدمات: '+esc(data.contractServicesError)) : 'لا توجد خدمات بعد. اضغط إضافة خدمة وأدخلها يدويًا.';
  $('contractServicesBody').innerHTML = rows.slice(0,500).map(s=>{
    const k=csStatusKey(s);
    return `<tr>
      <td><b>${esc(csProjectName(s))}</b></td>
      <td>${esc(csSupervisorName(s))}</td>
      <td><b>${esc(csServiceName(s))}</b></td>
      <td>${esc(csServiceType(s))}</td>
      <td>${esc(csFrequency(s))}</td>
      <td>${csVisits(s)}</td>
      <td>${esc(csExecutor(s))}</td>
      <td>${csDone(s)}</td>
      <td>${csRemaining(s)}</td>
      <td>${esc(csLastDate(s)||'-')}</td>
      <td>${esc(csDueDate(s)||'-')}</td>
      <td><span class="badge ${csBadgeClass(k)}">${csStatusText(k)}</span></td>
      <td>${esc(csNotes(s)||'-')}</td>
      <td class="row-actions"><button onclick="executeContractService(${Number(s.id)||0})">تسجيل تنفيذ</button><button class="light" onclick="editContractService(${Number(s.id)||0})">تعديل</button><button class="light" onclick="whatsappContractService(${Number(s.id)||0})">واتساب</button></td>
    </tr>`;
  }).join('') || `<tr><td colspan="14">${emptyMsg}</td></tr>`;
  renderSmartServicesList();
  showSupervisorServicesPreview(false);
}
function resetServiceFilters(){['serviceSearch','serviceFilterProject','serviceFilterSupervisor','serviceFilterStatus','serviceFilterType','serviceFilterDate'].forEach(id=>{if($(id))$(id).value=''}); renderContractServices();}
function projectPromptMatch(project){
  const p=data.projects.find(x=>String(x.id)===String(project)) || data.projects.find(x=>normalizeArV89(x.name)===normalizeArV89(project)) || data.projects.find(x=>normalizeArV89(x.name).includes(normalizeArV89(project)) || normalizeArV89(project).includes(normalizeArV89(x.name)));
  return p||null;
}
async function openNewContractService(){
  const project=prompt('اسم المشروع كما هو في التطبيق'); if(!project) return;
  const p=projectPromptMatch(project); if(!p) return msg('لم أجد المشروع. اكتب الاسم كما يظهر في التطبيق.','err');
  const service=prompt('اسم الخدمة مثال: غسيل خزانات علوية / مكافحة حشرات'); if(!service) return;
  const type=prompt('نوع الخدمة', service) || service;
  const freq=prompt('التكرار: سنوي / ربع سنوي / شهري / عند الاحتياج', 'سنوي') || 'سنوي';
  const visits=Number(prompt('عدد الزيارات المطلوبة خلال العقد', '1')||1);
  const due=prompt('تاريخ الاستحقاق القادم YYYY-MM-DD', today()) || null;
  const notes=prompt('ملاحظات', '') || '';
  const sid=p.supervisor_id||null; const supName=supervisorName(sid)||'';
  const row={project_id:p.id, project_name:p.name, supervisor_id:sid, supervisor_name:supName, service_name:service, service_type:type, frequency:freq, visit_count:visits, executed_count:0, remaining_count:visits, last_execution_date:null, next_due_date:due, status:'مستحقة', executor_name:'', notes, created_at:new Date().toISOString(), updated_at:new Date().toISOString()};
  const {error}=await sb.from('contract_services').insert(row);
  if(error) return msg(error.message,'err');
  resetServiceFilters();
  if($('serviceSearch')) $('serviceSearch').value=service;
  msg('تمت إضافة الخدمة بنجاح');
  await refreshAll();
}
async function editContractService(id){
  const s=serviceById(id); if(!s) return msg('الخدمة غير موجودة','err');
  const service=prompt('اسم الخدمة', csServiceName(s)); if(!service) return;
  const type=prompt('نوع الخدمة', csServiceType(s)) || service;
  const freq=prompt('التكرار', csFrequency(s)) || 'مرة واحدة';
  const visits=Number(prompt('عدد الزيارات المطلوبة', String(csVisits(s)||1))||1);
  const executed=Number(prompt('عدد الزيارات المنفذة', String(csDone(s)||0))||0);
  const due=prompt('تاريخ الاستحقاق القادم YYYY-MM-DD', csDueDate(s)||'') || null;
  const last=prompt('آخر تنفيذ YYYY-MM-DD', csLastDate(s)||'') || null;
  const status=prompt('الحالة: مستحقة / قريبة / متأخرة / منجزة / مراجعة / خارج العقد', csStatusText(csStatusKey(s))) || csStatusText(csStatusKey(s));
  const executor=prompt('المنفذ / الشركة المنفذة', csExecutor(s)==='-'?'':csExecutor(s)) || '';
  const notes=prompt('ملاحظات', csNotes(s)||'') || '';
  const remaining=Math.max(visits-executed,0);
  const {error}=await sb.from('contract_services').update({service_name:service, service_type:type, frequency:freq, visit_count:visits, executed_count:executed, remaining_count:remaining, next_due_date:due, last_execution_date:last, status, executor_name:executor, notes, updated_at:new Date().toISOString()}).eq('id',id);
  if(error) return msg(error.message,'err');
  msg('تم تعديل الخدمة'); await refreshAll();
}
async function executeContractService(id){
  const s=serviceById(id); if(!s) return msg('الخدمة غير موجودة','err');
  const d=prompt('تاريخ التنفيذ', today()); if(!d) return;
  const executor=prompt('المنفذ / الشركة المنفذة', csExecutor(s)==='-'?'':csExecutor(s)) || '';
  const notes=prompt('ملاحظات التنفيذ', csNotes(s)||'') || '';
  const done=(csDone(s)||0)+1; const visits=Math.max(csVisits(s)||1, done); const remaining=Math.max(visits-done,0);
  let next=null;
  if(remaining>0){ next=prompt('تاريخ الاستحقاق القادم للزيارة التالية YYYY-MM-DD', csDueDate(s)||'') || null; }
  const st=remaining>0?'مستحقة':'منجزة';
  const {error}=await sb.from('contract_services').update({last_execution_date:d, executed_count:done, remaining_count:remaining, executor_name:executor, notes, status:st, next_due_date:next, updated_at:new Date().toISOString()}).eq('id',id);
  if(error) return msg(error.message,'err');
  msg('تم تسجيل تنفيذ الخدمة'); await refreshAll();
}
function whatsappContractService(id){
  const s=serviceById(id); if(!s) return msg('الخدمة غير موجودة','err');
  const txt=`السلام عليكم ورحمة الله وبركاته\n\nتقرير خدمة تعاقدية\nالمشروع: ${csProjectName(s)}\nالمشرف: ${csSupervisorName(s)}\nالخدمة: ${csServiceName(s)}\nالحالة: ${csStatusText(csStatusKey(s))}\nعدد الزيارات: ${csVisits(s)}\nالمنفذ: ${csDone(s)}\nالمتبقي: ${csRemaining(s)}\nآخر تنفيذ: ${csLastDate(s)||'-'}\nالاستحقاق القادم: ${csDueDate(s)||'-'}\nملاحظات: ${csNotes(s)||'-'}\n\nشركة تصنيف لإدارة المرافق`;
  window.open('https://wa.me/?text='+encodeURIComponent(txt),'_blank');
}
function renderSmartServicesList(){
  const el=$('smartServicesList'); if(!el) return;
  const rows=(data.contractServices||[]).filter(s=>!['done','out','stopped'].includes(csStatusKey(s))).sort((a,b)=>{const order={late:0,due:1,soon:2,review:3}; return (order[csStatusKey(a)]??9)-(order[csStatusKey(b)]??9);}).slice(0,8);
  el.innerHTML=rows.map((s,i)=>{const dd=csDueDate(s)||new Date(Date.now()+(i+1)*86400000).toISOString().slice(0,10); const k=csStatusKey(s); return `<div class="quick-item"><span><b>${esc(csServiceName(s))}</b><br><small>${esc(csProjectName(s))} — ${esc(dd)}</small></span><span class="badge ${csBadgeClass(k)}">${csStatusText(k)}</span></div>`}).join('') || '<div class="quick-item">لا توجد خدمات تحتاج جدولة</div>';
}
function showSupervisorServicesPreview(scroll=true){
  const el=$('supervisorServicesPreview'); if(!el) return;
  const sid=$('serviceFilterSupervisor')?.value || (data.supervisors[0]?.id||'');
  const rows=(data.contractServices||[]).filter(s=>String(csSupervisorId(s)||'')===String(sid) && !['done','out','stopped'].includes(csStatusKey(s))).slice(0,6);
  el.innerHTML=rows.map(s=>`<div class="quick-item"><span><b>${esc(csServiceName(s))}</b><br><small>${esc(csProjectName(s))} — ${esc(csDueDate(s)||'-')}</small></span><span class="badge ${csBadgeClass(csStatusKey(s))}">${csStatusText(csStatusKey(s))}</span></div>`).join('') || '<div class="quick-item">لا توجد خدمات مخصصة للمشرف المختار</div>';
  if(scroll) el.scrollIntoView({behavior:'smooth',block:'nearest'});
}
async function smartScheduleContractServices(){
  const rows=(data.contractServices||[]).filter(s=>!['done','out','stopped'].includes(csStatusKey(s))).sort((a,b)=>{const order={late:0,due:1,soon:2,review:3}; return (order[csStatusKey(a)]??9)-(order[csStatusKey(b)]??9);}).slice(0,20);
  if(!rows.length) return msg('لا توجد خدمات غير منفذة للجدولة');
  let base=new Date(); let updates=[];
  rows.forEach((s,i)=>{ const d=new Date(base.getTime()+(i+1)*86400000); const ds=d.toISOString().slice(0,10); updates.push(sb.from('contract_services').update({next_due_date:ds, status:csStatusKey(s)==='late'?'متأخرة':'مستحقة', updated_at:new Date().toISOString()}).eq('id',s.id)); });
  const results=await Promise.all(updates); const err=results.find(r=>r.error)?.error; if(err) return msg(err.message,'err'); msg('تمت الجدولة الذكية للخدمات غير المنفذة'); await refreshAll();
}
function exportContractServicesCSV(){
  const rows=getFilteredContractServices();
  const header=['المشروع','المشرف','الخدمة','نوع الخدمة','التكرار','عدد الزيارات','المنفذ','المنفذ فعليًا','المتبقي','آخر تنفيذ','الاستحقاق القادم','الحالة','ملاحظات'];
  const lines=[header,...rows.map(s=>[csProjectName(s),csSupervisorName(s),csServiceName(s),csServiceType(s),csFrequency(s),csVisits(s),csExecutor(s),csDone(s),csRemaining(s),csLastDate(s)||'',csDueDate(s)||'',csStatusText(csStatusKey(s)),csNotes(s)])];
  const csv='\uFEFF'+lines.map(r=>r.map(c=>'"'+String(c??'').replace(/"/g,'""')+'"').join(',')).join('\n');
  const blob=new Blob([csv],{type:'text/csv;charset=utf-8;'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='contract_services_manual.csv'; a.click(); URL.revokeObjectURL(a.href);
}

/* ===== V91: جذر منع التهنيج - تحميل كسول ومحدود للسجلات ===== */
(function(){
  const LOG_COLS = 'id,user_id,supervisor_id,project_id,check_in,check_out,duration_minutes,travel_minutes,visit_type,required_minutes,time_difference_minutes,time_status,notes,created_at,updated_at,log_date';
  const loadedLogRanges = new Set();
  const inflightRanges = new Set();
  const oldRenderMonthly = window.renderMonthly || (typeof renderMonthly==='function'?renderMonthly:null);

  function iso(d){ return d.toISOString().slice(0,10); }
  function addDays(dateStr, days){ const d=new Date(dateStr+'T00:00:00'); d.setDate(d.getDate()+days); return iso(d); }
  function monthStart(month){ return (month || today().slice(0,7)) + '-01'; }
  function monthEnd(month){ const [y,m]=(month || today().slice(0,7)).split('-').map(Number); return iso(new Date(y, m, 0)); }
  function currentMonth(){ return today().slice(0,7); }
  function mergeLogs(rows){
    const map = new Map((data.logs||[]).map(l=>[String(l.id), l]));
    (rows||[]).forEach(l=>{ if(l && l.id!=null) map.set(String(l.id), l); });
    data.logs = [...map.values()].sort((a,b)=>String(b.check_in||b.created_at||'').localeCompare(String(a.check_in||a.created_at||'')));
  }
  async function safeQuery(name, builder, fallback=[]){
    try{
      const res = await builder;
      if(res.error){ console.warn('تعذر تحميل '+name+':', res.error.message); return {data:fallback,error:res.error}; }
      return {data:res.data||fallback,error:null};
    }catch(e){ console.warn('تعذر تحميل '+name+':', e?.message||e); return {data:fallback,error:e}; }
  }

  window.fetchTimeLogsRangeV91 = async function(start, end, options={}){
    if(!start || !end) return [];
    const key = `${start}_${end}_${options.supervisorId||'all'}`;
    if(loadedLogRanges.has(key)) return data.logs || [];
    if(inflightRanges.has(key)) return data.logs || [];
    inflightRanges.add(key);
    try{
      let q = sb.from('time_logs')
        .select(LOG_COLS)
        .gte('log_date', start)
        .lte('log_date', end)
        .order('id', {ascending:false})
        .limit(options.limit || 2000);
      if(options.supervisorId) q = q.eq('supervisor_id', options.supervisorId);
      const res = await q;
      if(res.error){
        console.warn('تعذر تحميل سجلات الفترة:', res.error.message);
        data.logsLoadError = res.error.message;
        return data.logs || [];
      }
      mergeLogs(res.data || []);
      loadedLogRanges.add(key);
      data.logsLoadError = '';
      return data.logs || [];
    }catch(e){
      console.warn('تعذر تحميل سجلات الفترة:', e?.message||e);
      data.logsLoadError = e?.message || String(e);
      return data.logs || [];
    }finally{
      inflightRanges.delete(key);
    }
  };

  window.loadAll = async function(){
    const d=today();
    const cm=currentMonth();
    const start=monthStart(cm);
    const end=monthEnd(cm);

    const usersP = safeQuery('المستخدمين', sb.from('app_users').select('*').order('id'));
    const projectsP = safeQuery('المشاريع', sb.from('projects').select('*').order('id'));
    const workersP = safeQuery('العمال', sb.from('workers').select('*').order('id'));
    const attendanceP = safeQuery('الحضور', sb.from('attendance').select('*').gte('attendance_date', addDays(d,-45)).order('attendance_date',{ascending:false}).limit(2000));
    const ticketsP = safeQuery('التكتات', sb.from('tickets').select('*').order('id',{ascending:false}).limit(1000));
    const servicesP = safeQuery('الخدمات', sb.from('contract_services').select('*').order('id',{ascending:false}).limit(1000));

    const [users, projects, workers, attendance, tickets, contractServices] = await Promise.all([usersP,projectsP,workersP,attendanceP,ticketsP,servicesP]);

    data.users = users.data || [];
    data.supervisors = data.users.filter(u=>u.role==='supervisor' && u.is_active!==false);
    data.technicians = data.users.filter(u=>u.role==='technician' && u.is_active!==false);
    data.projects = projects.data || [];
    data.workers = workers.data || [];
    data.attendance = attendance.data || [];
    data.tickets = tickets.data || [];
    data.contractServices = contractServices.data || [];
    data.contractServicesError = contractServices.error ? contractServices.error.message : '';

    // تحميل السجلات منفصلًا حتى لا يوقف التطبيق لو فشل time_logs
    await window.fetchTimeLogsRangeV91(start, end, {limit: 2000});
  };

  window.refreshAll = async function(){
    try{ await window.loadAll(); }
    catch(e){ console.error('loadAll V91 failed', e); msg && msg('تعذر تحميل بعض البيانات، لكن التطبيق سيستمر بالعمل','err'); }
    try{ hydrateForms(); }catch(e){ console.warn('hydrateForms failed', e); }
    try{ renderAll(); }catch(e){ console.warn('renderAll failed', e); }
  };

  window.renderMonthly = function(){
    const body=$('monthlyBody');
    const month=$('monthlyMonth')?.value || today().slice(0,7);
    const start=monthStart(month), end=monthEnd(month);
    const key=`${start}_${end}_all`;
    if(body && !loadedLogRanges.has(key) && !inflightRanges.has(key)){
      body.innerHTML='<tr><td colspan="12">جاري تحميل سجلات الشهر من السحابة...</td></tr>';
      window.fetchTimeLogsRangeV91(start,end,{limit:3000}).then(()=>{
        try{ if(oldRenderMonthly) oldRenderMonthly(); }catch(e){ console.warn(e); }
      });
      return;
    }
    if(oldRenderMonthly) return oldRenderMonthly();
  };

  window.exportTable = async function(table){
    let rows=[]; let error=null;
    if(table==='time_logs'){
      const month=$('monthlyMonth')?.value || today().slice(0,7);
      const start=monthStart(month), end=monthEnd(month);
      const res=await sb.from('time_logs').select(LOG_COLS).gte('log_date',start).lte('log_date',end).order('id',{ascending:false}).limit(5000);
      rows=res.data||[]; error=res.error;
    }else{
      const res=await sb.from(table).select('*').limit(5000);
      rows=res.data||[]; error=res.error;
    }
    if(error) return msg(error.message,'err');
    download(`${table}.csv`, toCSV(rows||[]));
  };

  window.reloadTodayLogsV91 = async function(){
    const d=today();
    loadedLogRanges.delete(`${d}_${d}_all`);
    await window.fetchTimeLogsRangeV91(d,d,{limit:1000});
    renderAll();
  };

  console.log('V91 performance guard loaded: time_logs lazy loading enabled');
})();

/* ===================== V95 Premium Client Reports - Clean Build ===================== */
const PREMIUM_SERVICE_TYPES = ['مكافحة الحشرات','غسيل الخزانات','تنظيف الأسطح','تنظيف البيسمنت','تنظيف المناور','تنظيف المكيفات','صيانة كهرباء','صيانة سباكة','صيانة مصعد','أعمال زراعة','شريحة مخصصة','أخرى'];
let premiumServicesState = [];
let premiumWithoutMode = false;

function reportStatusText(s){ return s==='published'?'معتمد ومنشور':(s==='draft'?'مسودة':'غير منشور'); }
function reportStatusClass(s){ return s==='published'?'green':(s==='draft'?'amber':'red'); }
function ratingClass(v){ return v==='يحتاج تحسين'?'red':(v?'green':'amber'); }
function genReportToken(){ return 'r_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,10); }
function genReportNo(){ const d=new Date(); return `TR-${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}${String(d.getDate()).padStart(2,'0')}-${String(Date.now()).slice(-5)}`; }
function clientReportUrl(token){ const base = location.href.replace(/admin\.html.*$/,''); return `${base}client-report.html?token=${encodeURIComponent(token)}`; }
function normalizePhoneForWa(p){ p=String(p||'').replace(/[^0-9]/g,''); if(!p) return ''; if(p.startsWith('05')) p='966'+p.slice(1); if(p.startsWith('5')) p='966'+p; return p; }
function defaultReportSummary(){ return 'تم تنفيذ الأعمال الموضحة أدناه ضمن خطة التشغيل المعتمدة للمشروع، مع توثيق مراحل التنفيذ بالصور قبل وأثناء وبعد، بهدف رفع جودة المرافق المشتركة والمحافظة على بيئة آمنة ونظيفة للسكان.'; }

async function loadPremiumReportsOnly(showMessage=false){
  const [reports, services, ratings] = await Promise.all([
    sb.from('client_reports').select('*').order('created_at',{ascending:false}),
    sb.from('client_report_services').select('*').order('sort_order',{ascending:true}),
    sb.from('client_service_ratings').select('*').order('created_at',{ascending:false})
  ]);
  data.clientReports = reports.data || [];
  data.clientReportServices = services.data || [];
  data.clientServiceRatings = ratings.data || [];
  data.clientReportsError = reports.error?.message || services.error?.message || ratings.error?.message || '';
  if(showMessage) msg(data.clientReportsError ? 'تأكد من تشغيل ملف SQL الخاص بالتقارير في Supabase' : 'تم تحديث التقارير', data.clientReportsError?'err':'ok');
  renderPremiumReports();
  renderClientRatings();
}

const __v95OldLoadAll = loadAll;
loadAll = async function(){
  await __v95OldLoadAll();
  try{ await loadPremiumReportsOnly(false); }catch(e){ console.warn('Premium reports not ready', e.message); data.clientReports=[]; data.clientReportServices=[]; data.clientServiceRatings=[]; }
};
const __v95OldHydrateForms = hydrateForms;
hydrateForms = function(){
  __v95OldHydrateForms();
  fillSelect('premiumReportProject', data.projects||[], 'name', 'اختر المشروع');
  fillSelect('premiumReportFilterProject', data.projects||[], 'name', 'كل المشاريع');
  if($('premiumReportDate') && !$('premiumReportDate').value) $('premiumReportDate').value = today();
  if($('premiumReportFilterMonth') && !$('premiumReportFilterMonth').value) $('premiumReportFilterMonth').value = today().slice(0,7);
};
const __v95OldRenderAll = renderAll;
renderAll = function(){
  __v95OldRenderAll();
  renderPremiumReports();
  renderClientRatings();
};

function syncPremiumProjectInfo(){
  const pid=$('premiumReportProject')?.value; const p=(data.projects||[]).find(x=>String(x.id)===String(pid));
  if(!p) return;
  if(!$('premiumReportTitle').value) $('premiumReportTitle').value = `تقرير خدمات مشروع ${p.name}`;
  if(!$('premiumSummary').value) $('premiumSummary').value = defaultReportSummary();
}
function clearPremiumReportForm(){
  ['premiumReportId','premiumReportTitle','premiumChairmanName','premiumChairmanPhone'].forEach(id=>$(id)&&($(id).value=''));
  if($('premiumReportProject')) $('premiumReportProject').value='';
  if($('premiumReportDate')) $('premiumReportDate').value=today();
  if($('premiumReportType')) $('premiumReportType').value='تقرير خدمات';
  if($('premiumSummary')) $('premiumSummary').value=defaultReportSummary();
  premiumServicesState=[]; addPremiumService('مكافحة الحشرات'); renderPremiumServicesEditor();
  $('premiumReportFormTitle')&&($('premiumReportFormTitle').textContent='إنشاء تقرير جديد');
}
function makeService(type=''){
  const t=type||'مكافحة الحشرات';
  return {service_type:t,title:t,service_description:'',scope_work:'',notes:'',before_images:[],during_images:[],after_images:[], source:'admin'};
}
function addPremiumService(type=''){
  premiumServicesState.push(makeService(type));
  renderPremiumServicesEditor();
  setTimeout(()=>{ document.querySelectorAll('.service-editor-card')[premiumServicesState.length-1]?.scrollIntoView({behavior:'smooth',block:'center'}); },50);
}
function addSelectedPremiumService(){ addPremiumService($('premiumAddServiceType')?.value || 'مكافحة الحشرات'); }
function addCustomPremiumSlide(){
  const title = prompt('اكتب اسم الشريحة التي تريد إضافتها في التقرير:');
  if(!title) return;
  premiumServicesState.push({service_type:'شريحة مخصصة', title:title.trim(), service_description:'', scope_work:'', notes:'', before_images:[], during_images:[], after_images:[], source:'admin'});
  renderPremiumServicesEditor();
  setTimeout(()=>{ document.querySelectorAll('.service-editor-card')[premiumServicesState.length-1]?.scrollIntoView({behavior:'smooth',block:'center'}); },50);
}
function countServiceImages(s){ return (s.before_images||[]).length + (s.during_images||[]).length + (s.after_images||[]).length; }
function movePremiumService(i,dir){ const j=i+dir; if(j<0||j>=premiumServicesState.length) return; [premiumServicesState[i],premiumServicesState[j]]=[premiumServicesState[j],premiumServicesState[i]]; renderPremiumServicesEditor(); }

function removePremiumService(i){ if(!confirm('حذف هذه الخدمة من التقرير؟')) return; premiumServicesState.splice(i,1); renderPremiumServicesEditor(); }
function setPremiumServiceField(i, field, value){ if(premiumServicesState[i]) premiumServicesState[i][field]=value; }
function serviceTypeOptions(selected){ return PREMIUM_SERVICE_TYPES.map(t=>`<option ${t===selected?'selected':''}>${esc(t)}</option>`).join(''); }
function renderPremiumServicesEditor(){
  const box=$('premiumServicesEditor'); if(!box) return;
  if(!premiumServicesState.length) premiumServicesState=[makeService('مكافحة الحشرات')];
  box.innerHTML = premiumServicesState.map((s,i)=>`
    <div class="service-editor-card">
      <div class="service-editor-head">
        <div class="service-title-line"><span class="service-number">${i+1}</span><div><b>${esc(s.title||s.service_type||'خدمة')}</b><div class="service-counts"><span class="service-chip">قبل: ${(s.before_images||[]).length}</span><span class="service-chip">أثناء: ${(s.during_images||[]).length}</span><span class="service-chip">بعد: ${(s.after_images||[]).length}</span><span class="service-chip">الإجمالي: ${countServiceImages(s)}</span></div></div></div>
        <div class="row-actions"><button class="light" onclick="movePremiumService(${i},-1)">↑</button><button class="light" onclick="movePremiumService(${i},1)">↓</button><button class="danger" onclick="removePremiumService(${i})">حذف</button></div>
      </div>
      <div class="split"><div><label>نوع الخدمة</label><select onchange="setPremiumServiceField(${i},'service_type',this.value); if(!premiumServicesState[${i}].title || PREMIUM_SERVICE_TYPES.includes(premiumServicesState[${i}].title)) setPremiumServiceField(${i},'title',this.value); renderPremiumServicesEditor()">${serviceTypeOptions(s.service_type)}</select></div><div><label>عنوان الشريحة الظاهر للعميل</label><input value="${esc(s.title||'')}" oninput="setPremiumServiceField(${i},'title',this.value)"></div></div>
      <label>النص التوضيحي للشريحة</label><textarea oninput="setPremiumServiceField(${i},'service_description',this.value)" placeholder="وصف مختصر للخدمة يظهر داخل التقرير">${esc(s.service_description||'')}</textarea>
      <label>نطاق العمل / محتوى الشريحة</label><textarea oninput="setPremiumServiceField(${i},'scope_work',this.value)" placeholder="مثال: الممرات، المداخل، المناور، غرف الخدمات">${esc(s.scope_work||'')}</textarea>
      <div class="stage-grid">
        ${renderStageBox(i,'before_images','قبل التنفيذ','صور توضح حالة الموقع قبل العمل',s.before_images)}
        ${renderStageBox(i,'during_images','أثناء التنفيذ','صور توثيق تنفيذ الخدمة',s.during_images)}
        ${renderStageBox(i,'after_images','بعد التنفيذ','صور النتيجة النهائية',s.after_images)}
      </div>
      <label>ملاحظات الإدارة على الشريحة</label><textarea oninput="setPremiumServiceField(${i},'notes',this.value)" placeholder="اختياري ولا يظهر إذا تركته فارغًا">${esc(s.notes||'')}</textarea>
    </div>`).join('');
}
function renderStageBox(i, field, title, hint, imgs=[]){
  imgs=imgs||[];
  return `<div class="stage-box"><h4>${title}</h4><small>${hint}</small><label class="stage-upload">+ اختيار صور ${title}<input type="file" accept="image/*" multiple onchange="handlePremiumImages(${i},'${field}',this)"></label><div class="img-previews">${imgs.map((im,j)=>`<div class="preview-img"><img src="${im.data||im.url||''}" alt=""><button onclick="removePremiumImage(${i},'${field}',${j})">×</button></div>`).join('')}</div><small>${imgs.length ? imgs.length + ' صورة مضافة' : 'لن يظهر هذا القسم للعميل إذا لم تضف صورًا'}</small></div>`;
}
function removePremiumImage(i,field,j){ premiumServicesState[i]?.[field]?.splice(j,1); renderPremiumServicesEditor(); }
async function handlePremiumImages(i, field, input){
  const files=[...(input.files||[])]; if(!files.length) return;
  msg('جاري تجهيز الصور...');
  for(const f of files){
    try{ const data=await compressImageToDataUrl(f, 1400, 0.82); premiumServicesState[i][field].push({name:f.name,type:'image/jpeg',data}); }
    catch(e){ console.warn(e); }
  }
  renderPremiumServicesEditor(); msg('تمت إضافة الصور');
}
function compressImageToDataUrl(file, max=1400, quality=.82){
  return new Promise((resolve,reject)=>{
    const fr=new FileReader(); fr.onerror=reject; fr.onload=()=>{
      const img=new Image(); img.onerror=reject; img.onload=()=>{
        let w=img.width,h=img.height; const scale=Math.min(1,max/Math.max(w,h)); w=Math.round(w*scale); h=Math.round(h*scale);
        const canvas=document.createElement('canvas'); canvas.width=w; canvas.height=h; const ctx=canvas.getContext('2d'); ctx.fillStyle='#fff'; ctx.fillRect(0,0,w,h); ctx.drawImage(img,0,0,w,h);
        resolve(canvas.toDataURL('image/jpeg',quality));
      }; img.src=fr.result;
    }; fr.readAsDataURL(file);
  });
}
function collectPremiumReportBase(status){
  const pid=Number($('premiumReportProject')?.value)||null; const p=(data.projects||[]).find(x=>x.id===pid);
  if(!pid) throw new Error('اختر المشروع أولاً');
  if(!premiumServicesState.length) throw new Error('أضف خدمة واحدة على الأقل');
  return {
    project_id:pid, project_name:p?.name||'', chairman_name:$('premiumChairmanName')?.value.trim()||'', chairman_phone:$('premiumChairmanPhone')?.value.trim()||'',
    title:$('premiumReportTitle')?.value.trim()||`تقرير خدمات مشروع ${p?.name||''}`,
    report_type:$('premiumReportType')?.value||'تقرير خدمات', report_date:$('premiumReportDate')?.value||today(), executive_summary:$('premiumSummary')?.value.trim()||defaultReportSummary(),
    status, published_at: status==='published' ? new Date().toISOString() : null
  };
}
async function savePremiumReport(status='draft'){
  try{
    const btn = event?.target;
    if(btn){ btn.disabled=true; btn.dataset.oldText=btn.innerHTML; btn.innerHTML='جاري الحفظ...'; }

    const rawId=($('premiumReportId')?.value||'').trim();
    const isUuid=v=>/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(v||'');
    const id=isUuid(rawId)?rawId:'';
    const base=collectPremiumReportBase(status);
    let reportId='';
    let publicToken='';

    if(id){
      // نقرأ التقرير من قاعدة البيانات نفسها حتى لا نعتمد على كاش المتصفح
      const {data:current,error:readErr}=await sb.from('client_reports').select('id,public_token').eq('id',id).maybeSingle();
      if(readErr) throw readErr;
      if(current?.id){
        publicToken = current.public_token || genReportToken();
        const row={...base, public_token: status==='published'?publicToken:current.public_token, updated_at:new Date().toISOString()};
        const {data:updated,error}=await sb.from('client_reports').update(row).eq('id',id).select('id,public_token').single();
        if(error) throw error;
        reportId=updated.id;
        publicToken=updated.public_token||publicToken;
      }
    }

    // إذا كان رقم التقرير الموجود في الشاشة قديمًا أو غير موجود، ننشئ تقريرًا جديدًا بدل إدخال خدمات على تقرير غير موجود
    if(!reportId){
      publicToken=genReportToken();
      const row={...base, report_no:genReportNo(), public_token: status==='published'?publicToken:null};
      const {data:ins,error}=await sb.from('client_reports').insert(row).select('id,public_token').single();
      if(error) throw error;
      reportId=ins.id;
      publicToken=ins.public_token||publicToken;
      if($('premiumReportId')) $('premiumReportId').value=reportId;
    }

    // مهم: لا نحفظ الخدمات إلا بعد التأكد أن التقرير الرئيسي محفوظ فعليًا في قاعدة البيانات
    const {error:delErr}=await sb.from('client_report_services').delete().eq('report_id',reportId);
    if(delErr) throw delErr;

    const serviceRows=premiumServicesState.map((s,i)=>({
      report_id:reportId,
      sort_order:i+1,
      service_type:s.service_type||s.title||'خدمة',
      title:s.title||s.service_type||'خدمة',
      service_description:s.service_description||'',
      scope_work:s.scope_work||'',
      notes:s.notes||'',
      before_images:Array.isArray(s.before_images)?s.before_images:[],
      during_images:Array.isArray(s.during_images)?s.during_images:[],
      after_images:Array.isArray(s.after_images)?s.after_images:[]
    }));

    if(serviceRows.length){
      const {error:se}=await sb.from('client_report_services').insert(serviceRows);
      if(se) throw se;
    }

    await loadPremiumReportsOnly(false);
    msg(status==='published'?'تم اعتماد ونشر التقرير':'تم حفظ التقرير كمسودة');

    if(status==='published'){
      const url=clientReportUrl(publicToken);
      const phone=normalizePhoneForWa(base.chairman_phone);
      const text=`السيد / رئيس جمعية ${base.project_name} المحترم\n\nتم نشر ${base.title}.\nللاطلاع على التقرير:\n${url}\n\nشركة تصنيف لإدارة المرافق\n920015589`;
      if(phone && confirm('تم النشر. هل تريد فتح رسالة واتساب الآن؟')) window.open(`https://wa.me/${phone}?text=${encodeURIComponent(text)}`,'_blank');
    }
  }catch(e){
    console.error('savePremiumReport failed:', e);
    msg(e.message||String(e),'err');
  }finally{
    const btn = event?.target;
    if(btn){ btn.disabled=false; if(btn.dataset.oldText) btn.innerHTML=btn.dataset.oldText; }
  }
}
function getReportServices(reportId){ return (data.clientReportServices||[]).filter(s=>String(s.report_id)===String(reportId)).sort((a,b)=>(a.sort_order||0)-(b.sort_order||0)); }
function reportRatings(reportId){ return (data.clientServiceRatings||[]).filter(r=>String(r.report_id)===String(reportId)); }
function avgRatingLabel(reportId){ const rs=reportRatings(reportId); if(!rs.length) return 'لم يتم التقييم'; if(rs.some(r=>r.rating==='يحتاج تحسين')) return 'يحتاج تحسين'; return rs[0].rating || 'تم التقييم'; }
function serviceNamesForReport(reportId){ return getReportServices(reportId).map(s=>s.service_type||s.title).filter(Boolean); }
function reportMatchesService(reportId, service){ if(!service) return true; return serviceNamesForReport(reportId).some(n=>String(n).includes(service)); }
function renderPremiumReports(){
  const body=$('premiumReportsBody'); if(!body) return;
  const reports=data.clientReports||[], services=data.clientReportServices||[], ratings=data.clientServiceRatings||[];
  const month=$('premiumReportFilterMonth')?.value || today().slice(0,7);
  const mReports=reports.filter(r=>String(r.report_date||'').startsWith(month));
  if($('reportsTotalKpi')) $('reportsTotalKpi').textContent=reports.length;
  if($('reportsMonthKpi')) $('reportsMonthKpi').textContent=mReports.length;
  if($('reportsDraftKpi')) $('reportsDraftKpi').textContent=reports.filter(r=>r.status==='draft').length;
  if($('reportsPublishedKpi')) $('reportsPublishedKpi').textContent=reports.filter(r=>r.status==='published').length;
  if($('reportsFollowKpi')) $('reportsFollowKpi').textContent=ratings.filter(r=>r.rating==='يحتاج تحسين'||r.followup_requested).length;
  renderProjectsWithoutReports();
  const q=($('premiumReportSearch')?.value||'').trim(), pid=$('premiumReportFilterProject')?.value, st=$('premiumReportFilterStatus')?.value, sv=$('premiumReportFilterService')?.value, rt=$('premiumReportFilterRating')?.value;
  let rows=[...reports];
  if(q) rows=rows.filter(r=>[r.report_no,r.project_name,r.title,r.report_type,serviceNamesForReport(r.id).join(' ')].join(' ').includes(q));
  if(pid) rows=rows.filter(r=>String(r.project_id)===String(pid));
  if(st) rows=rows.filter(r=>(r.status||'unpublished')===st);
  if(month) rows=rows.filter(r=>String(r.report_date||'').startsWith(month));
  if(sv) rows=rows.filter(r=>reportMatchesService(r.id,sv));
  if(rt){ rows=rows.filter(r=> rt==='none' ? !reportRatings(r.id).length : reportRatings(r.id).some(x=>x.rating===rt)); }
  body.innerHTML = rows.map(r=>{
    const names=serviceNamesForReport(r.id), rating=avgRatingLabel(r.id), url=r.public_token?clientReportUrl(r.public_token):'';
    return `<tr><td><b>${esc(r.report_no||r.id)}</b><br><small>${esc(r.title||'')}</small></td><td>${esc(r.report_date||'')}</td><td>${esc(r.project_name||projectName(r.project_id))}</td><td>${names.map(n=>`<span class="service-chip">${esc(n)}</span>`).join('')||'-'}</td><td><span class="badge ${reportStatusClass(r.status)}">${reportStatusText(r.status)}</span></td><td><span class="badge ${ratingClass(rating)}">${esc(rating)}</span></td><td>${url?`<div class="client-copy">${esc(url)}</div><button class="light" onclick="copyText('${encodeURIComponent(url)}')">نسخ</button>`:'غير منشور'}</td><td class="row-actions"><button onclick="editPremiumReport('${r.id}')">تعديل</button>${r.status==='published'?`<button class="light" onclick="openClientReport('${r.public_token}')">عرض</button><button class="light" onclick="sendPremiumWhatsapp('${r.id}')">واتساب</button>`:''}<button class="danger" onclick="deletePremiumReport('${r.id}')">حذف</button></td></tr>`;
  }).join('') || `<tr><td colspan="8">${data.clientReportsError?'شغّل ملف SQL الخاص بالتقارير في Supabase':'لا توجد تقارير'}</td></tr>`;
}
function copyText(enc){ const t=decodeURIComponent(enc); navigator.clipboard?.writeText(t); msg('تم نسخ الرابط'); }
function openClientReport(token){ window.open(clientReportUrl(token),'_blank'); }
function sendPremiumWhatsapp(id){
  const r=(data.clientReports||[]).find(x=>String(x.id)===String(id)); if(!r||!r.public_token) return msg('التقرير غير منشور','err');
  const phone=normalizePhoneForWa(r.chairman_phone); if(!phone) return msg('لا يوجد رقم واتساب لهذا التقرير','err');
  const text=`السيد / رئيس جمعية ${r.project_name} المحترم\n\nتم نشر ${r.title}.\nللاطلاع على التقرير:\n${clientReportUrl(r.public_token)}\n\nشركة تصنيف لإدارة المرافق\n920015589`;
  window.open(`https://wa.me/${phone}?text=${encodeURIComponent(text)}`,'_blank');
}
function previewPremiumReport(){
  const id=$('premiumReportId')?.value; const r=(data.clientReports||[]).find(x=>String(x.id)===String(id));
  if(r?.public_token && r.status==='published') return openClientReport(r.public_token);
  msg('احفظ وانشر التقرير أولاً لفتح صفحة العميل','err');
}
async function editPremiumReport(id){
  const r=(data.clientReports||[]).find(x=>String(x.id)===String(id)); if(!r) return;
  $('premiumReportId').value=r.id; $('premiumReportProject').value=r.project_id||''; $('premiumReportDate').value=r.report_date||today(); $('premiumReportTitle').value=r.title||''; $('premiumReportType').value=r.report_type||'تقرير خدمات'; $('premiumChairmanName').value=r.chairman_name||''; $('premiumChairmanPhone').value=r.chairman_phone||''; $('premiumSummary').value=r.executive_summary||defaultReportSummary();
  premiumServicesState=getReportServices(id).map(s=>({service_type:s.service_type,title:s.title,service_description:s.service_description,scope_work:s.scope_work,notes:s.notes,before_images:s.before_images||[],during_images:s.during_images||[],after_images:s.after_images||[]}));
  if(!premiumServicesState.length) premiumServicesState=[makeService()];
  renderPremiumServicesEditor(); $('premiumReportFormTitle').textContent='تعديل تقرير'; $('premiumReportFormCard')?.scrollIntoView({behavior:'smooth'});
}
async function deletePremiumReport(id){ if(!confirm('حذف التقرير وكل خدماته وتقييماته؟')) return; const {error}=await sb.from('client_reports').delete().eq('id',id); if(error) return msg(error.message,'err'); msg('تم حذف التقرير'); await loadPremiumReportsOnly(false); }
function resetPremiumReportFilters(){ ['premiumReportSearch','premiumReportFilterProject','premiumReportFilterService','premiumReportFilterStatus','premiumReportFilterRating'].forEach(id=>$(id)&&($(id).value='')); if($('premiumReportFilterMonth')) $('premiumReportFilterMonth').value=today().slice(0,7); premiumWithoutMode=false; $('projectsWithoutReportsBox')?.classList.add('hidden'); renderPremiumReports(); }
function toggleProjectsWithoutReports(){ premiumWithoutMode=!premiumWithoutMode; renderProjectsWithoutReports(); }
function renderProjectsWithoutReports(){
  const box=$('projectsWithoutReportsBox'); if(!box) return; if(!premiumWithoutMode){ box.classList.add('hidden'); return; }
  const month=$('premiumReportFilterMonth')?.value || today().slice(0,7);
  const has=new Set((data.clientReports||[]).filter(r=>String(r.report_date||'').startsWith(month)).map(r=>String(r.project_id)));
  const rows=(data.projects||[]).filter(p=>!has.has(String(p.id)) && (p.status||'active')!=='inactive');
  box.className='card'; box.innerHTML=`<h2>المشاريع بدون تقارير في ${month}</h2><div class="no-reports-box">${rows.map(p=>`<div class="no-report-item"><b>${esc(p.name)}</b><br><small>لا يوجد تقرير لهذا الشهر</small><br><span class="mini-link" onclick="clearPremiumReportForm(); $('premiumReportProject').value='${p.id}'; syncPremiumProjectInfo(); $('premiumReportFormCard').scrollIntoView({behavior:'smooth'})">إنشاء تقرير</span></div>`).join('')||'<div class="no-report-item">كل المشاريع لديها تقارير هذا الشهر</div>'}</div>`;
}
function renderClientRatings(){
  const body=$('clientRatingsBody'); if(!body) return;
  const q=($('ratingSearch')?.value||'').trim(), v=$('ratingFilterValue')?.value, f=$('ratingFilterFollow')?.value;
  let rows=[...(data.clientServiceRatings||[])];
  if(v) rows=rows.filter(r=>r.rating===v); if(f==='yes') rows=rows.filter(r=>r.followup_requested); if(f==='low') rows=rows.filter(r=>r.rating==='يحتاج تحسين');
  if(q) rows=rows.filter(r=>[r.project_name,r.report_title,r.service_title,r.comment].join(' ').includes(q));
  body.innerHTML=rows.map(r=>`<tr class="${r.rating==='يحتاج تحسين'?'low-rating':''}"><td>${fmt(r.created_at)}</td><td>${esc(r.project_name||'-')}</td><td>${esc(r.report_title||'-')}</td><td>${esc(r.service_title||'-')}</td><td><span class="badge ${ratingClass(r.rating)}">${esc(r.rating||'-')}</span></td><td>${esc(r.comment||'-')}</td><td>${r.followup_requested?'نعم':'لا'}</td></tr>`).join('')||'<tr><td colspan="7">لا توجد تقييمات حتى الآن</td></tr>';
}
function exportPremiumReportsCSV(){
  const rows=(data.clientReports||[]).map(r=>({report_no:r.report_no, date:r.report_date, project:r.project_name, title:r.title, status:reportStatusText(r.status), services:serviceNamesForReport(r.id).join(' | '), rating:avgRatingLabel(r.id)}));
  const csv='رقم التقرير,التاريخ,المشروع,العنوان,الحالة,الخدمات,التقييم\n'+rows.map(r=>[r.report_no,r.date,r.project,r.title,r.status,r.services,r.rating].map(x=>`"${String(x||'').replace(/"/g,'""')}"`).join(',')).join('\n');
  const blob=new Blob([csv],{type:'text/csv;charset=utf-8;'}), a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='client_reports.csv'; a.click();
}



/* ===== V108 Supervisor Inventory Requests ===== */
let supInventoryRequestLines = [];
function supInventoryItemLabel(i){ return `${esc(i.name)} ${v118ProductCode(i)?'('+esc(v118ProductCode(i))+')':''} - المتوفر ${safeQtyEn(i.quantity)} ${esc(i.unit||'')}`; }
function renderSupInventoryRequestLines(){
  const b=$('supInventoryRequestLinesBody'); if(!b) return;
  b.innerHTML=supInventoryRequestLines.map((l,idx)=>{
    const it=(data.inventoryItems||[]).find(i=>String(i.id)===String(l.item_id))||{};
    return `<tr><td>${esc(v118ProductCode(it)||l.product_code||'')}</td><td>${esc(l.item_name||it.name||'')}</td><td>${safeQtyEn(l.available)}</td><td>${safeQtyEn(l.quantity)}</td><td><button class="danger mini" type="button" onclick="supRemoveInventoryLine(${idx})">حذف</button></td></tr>`;
  }).join('') || '<tr><td colspan="5">لم تتم إضافة أصناف بعد</td></tr>';
}
function supRemoveInventoryLine(idx){ supInventoryRequestLines.splice(idx,1); renderSupInventoryRequestLines(); }
function supAddInventoryLine(){
  const itemId=$('supInventoryRequestItem')?.value;
  const qty=num($('supInventoryRequestQty')?.value);
  if(!itemId) return msg('اختر الصنف','err');
  if(qty<=0) return msg('أدخل الكمية المطلوبة','err');
  const item=(data.inventoryItems||[]).find(i=>String(i.id)===String(itemId));
  if(!item) return msg('الصنف غير موجود','err');
  const existing=supInventoryRequestLines.find(l=>String(l.item_id)===String(itemId));
  if(existing){ existing.quantity=num(existing.quantity)+qty; existing.available=num(item.quantity); }
  else supInventoryRequestLines.push({item_id:Number(itemId), product_code:v118ProductCode(item), item_name:item.name||'', quantity:qty, available:num(item.quantity), unit_cost:num(item.unit_cost)});
  if($('supInventoryRequestItem')) $('supInventoryRequestItem').value='';
  if($('supInventoryRequestQty')) $('supInventoryRequestQty').value='';
  renderSupInventoryRequestLines();
}

async function supervisorInventoryLoad(){
  try{
    const u=session()||{};
    const [items,reqs]=await Promise.all([
      sb.from('inventory_items').select('*').order('name',{ascending:true}),
      sb.from('inventory_requests').select('*').order('created_at',{ascending:false})
    ]);
    data.inventoryItems=items.data||[];
    data.inventoryRequests=(reqs.data||[]).filter(r=>String(r.supervisor_id)===String(u.id));
    fillSelect('supInventoryRequestProject', data.projects, 'name', 'اختر المشروع');
    const itemEl=$('supInventoryRequestItem'); if(itemEl) itemEl.innerHTML='<option value="">اختر الصنف</option>'+data.inventoryItems.map(i=>`<option value="${i.id}">${esc(i.name)} ${v118ProductCode(i)?'('+esc(v118ProductCode(i))+')':''} - المتوفر ${num(i.quantity)} ${esc(i.unit||'')}</option>`).join(''); renderSupInventoryRequestLines();
    if($('supInventoryRequestDate') && !$('supInventoryRequestDate').value) $('supInventoryRequestDate').value=today();
    renderSupervisorInventoryRequests();
  }catch(e){ console.warn(e); }
}
async function supervisorSaveInventoryRequest(btn){
  try{
    if(btn) btn.disabled=true;
    const u=session()||{};
    const pid=$('supInventoryRequestProject')?.value;
    if(!pid) throw new Error('اختر المشروع');
    if(!supInventoryRequestLines.length) throw new Error('أضف صنف واحد على الأقل للطلب');
    const path=inventoryGetApprovalPath(); const first=path[0]||'warehouse';
    const firstLine=supInventoryRequestLines[0]||{};
    const row={
      item_id:Number(firstLine.item_id),
      item_name:firstLine.item_name||'',
      quantity:supInventoryRequestLines.reduce((a,l)=>a+num(l.quantity),0),
      request_items:supInventoryRequestLines, items:supInventoryRequestLines,
      project_id:Number(pid),project_name:projectName(pid),
      supervisor_id:u.id,supervisor_name:u.full_name||u.username||'',
      request_date:$('supInventoryRequestDate')?.value||today(),
      reason:$('supInventoryRequestReason')?.value||'',notes:$('supInventoryRequestNotes')?.value||'',
      status:'pending_'+first,current_step:first,approval_path:path,approval_log:[]
    };
    const {error}=await sb.from('inventory_requests').insert(row); if(error) throw error;
    msg('تم إرسال طلب الصرف للإدارة');
    supInventoryRequestLines=[]; renderSupInventoryRequestLines();
    ['supInventoryRequestItem','supInventoryRequestQty','supInventoryRequestReason','supInventoryRequestNotes'].forEach(id=>$(id)&&($(id).value='')); if($('supInventoryRequestProject')) $('supInventoryRequestProject').value='';
    await supervisorInventoryLoad();
  }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
}
function renderSupervisorInventoryRequests(){
  const b=$('supInventoryRequestsBody'); if(!b) return;
  b.innerHTML=(data.inventoryRequests||[]).map(r=>{ const lines=v118LineItems(r); const items=lines.map(l=>`${esc(l.product_code||'')} ${esc(l.item_name)} × ${safeQtyEn(l.quantity)}`).join('<br>')||esc(r.item_name||''); const total=lines.reduce((a,l)=>a+num(l.quantity),0)||num(r.quantity); return `<tr><td>${esc(r.request_date||'')}</td><td>${esc(r.project_name||projectName(r.project_id))}</td><td><b>${items}</b></td><td>${safeQtyEn(total)}</td><td>${esc(r.reason||'-')}</td><td><span class="badge ${inventoryRequestStatusClass(r.status)}">${inventoryRequestStatusText(r.status)}</span><br><small>${inventoryRequestNextRole(r.status)}</small></td></tr>`; }).join('')||'<tr><td colspan="6">لا توجد طلبات صرف</td></tr>';
}

// Initialize report form when the admin page is ready
setTimeout(()=>{ if($('premiumReportDate') && !premiumServicesState.length){ clearPremiumReportForm(); } }, 800);


/* ===== V103 Finance Core - Administrative Accounting ===== */
let financeLoaded = false;
let financeCurrentTab = 'overview';
function financeMonthOptions(){ const el=$('financeMonthFilter'); if(!el || el.dataset.ready) return; const now=new Date(); let opts='<option value="">كل الأشهر</option>'; for(let i=0;i<18;i++){ const d=new Date(now.getFullYear(), now.getMonth()-i, 1); const val=d.toISOString().slice(0,7); opts+=`<option value="${val}">${val}</option>`; } el.innerHTML=opts; el.value=today().slice(0,7); el.dataset.ready='1'; }
function financeHydrateForms(){
  if(!$('financeDashboard')) return;
  financeMonthOptions();
  const pros=(data.projects||[]).map(p=>({id:p.id,name:p.name}));
  ['financeProjectFilter','financeExpenseProject','inventoryMovementProject','inventoryRequestProject'].forEach(id=>fillSelect(id, pros, 'name', id==='inventoryMovementProject'?'بدون مشروع':'كل المشاريع'));
  inventoryFillItemSelect(); inventoryFillRequestSelect(); financeFillSupervisorSelect(); inventoryLoadApprovalSettings();
  if($('financeExpenseDate') && !$('financeExpenseDate').value) $('financeExpenseDate').value=today();
  if($('inventoryMovementDate') && !$('inventoryMovementDate').value) $('inventoryMovementDate').value=today();
  if($('inventoryRequestDate') && !$('inventoryRequestDate').value) $('inventoryRequestDate').value=today();
}
async function financeLoadAll(showMessage=false){
  try{
    const [expenses,items,movements,requests] = await Promise.all([
      sb.from('finance_expenses').select('*').order('expense_date',{ascending:false}),
      sb.from('inventory_items').select('*').order('name',{ascending:true}),
      sb.from('inventory_movements').select('*').order('movement_date',{ascending:false}),
      sb.from('inventory_requests').select('*').order('created_at',{ascending:false})
    ]);
    for(const r of [expenses,items,movements,requests]) if(r.error) console.warn('expenses/inventory:', r.error.message);
    data.financeExpenses=expenses.data||[];
    data.inventoryItems=items.data||[];
    data.inventoryMovements=movements.data||[];
    data.inventoryRequests=requests.data||[];
    data.financeError = expenses.error||items.error||movements.error||requests.error ? 'شغّل ملف schema_update_v111_warehouse_manager_permissions.sql في Supabase' : '';
    financeLoaded=true; financeHydrateForms(); financeRenderAll(); if(showMessage) msg(data.financeError||'تم تحديث المصروفات والمخزون');
  }catch(e){ console.error(e); data.financeError='شغّل ملف schema_update_v111_warehouse_manager_permissions.sql في Supabase'; financeRenderAll(); if(showMessage) msg(data.financeError,'err'); }
}
function financeShowTab(tab,btn){ financeCurrentTab=tab; document.querySelectorAll('.finance-tab-page').forEach(x=>x.classList.add('hidden')); $('financeTab'+tab[0].toUpperCase()+tab.slice(1))?.classList.remove('hidden'); document.querySelectorAll('.finance-tab').forEach(x=>x.classList.remove('active')); btn?.classList.add('active'); if(!financeLoaded) financeLoadAll(); financeRenderAll(); }
function financeProjectName(id){ return projectName(id); }
function financeFilterRows(rows,dateField='expense_date'){
  const q=($('financeSearch')?.value||'').trim(), pid=$('financeProjectFilter')?.value, mon=$('financeMonthFilter')?.value;
  let out=[...rows];
  if(pid) out=out.filter(r=>String(r.project_id)===String(pid));
  if(mon) out=out.filter(r=>String(r[dateField]||'').slice(0,7)===mon);
  if(q) out=out.filter(r=>[r.project_name,financeProjectName(r.project_id),r.supplier,r.category,r.notes,r.name,r.item_name,r.receiver,r.reason].join(' ').includes(q));
  return out;
}
function financeRenderAll(){ if(!$('financeDashboard')) return; financeRenderKpis(); financeRenderRecent(); financeRenderExpenses(); inventoryRenderItems(); inventoryRenderMovements(); inventoryRenderRequests(); financeRenderReports(); }
function financeRenderKpis(){
  const expenses=financeFilterRows(data.financeExpenses||[],'expense_date').filter(e=>!$('financeExpenseTypeFilter')?.value || e.category===$('financeExpenseTypeFilter').value);
  const expTotal=expenses.reduce((a,e)=>a+num(e.total),0);
  const low=(data.inventoryItems||[]).filter(i=>num(i.quantity)<=num(i.min_quantity));
  $('finKpiExpenses')&&($('finKpiExpenses').textContent=money(expTotal));
  $('finKpiExpenseCount')&&($('finKpiExpenseCount').textContent=expenses.length);
  $('finKpiItems')&&($('finKpiItems').textContent=(data.inventoryItems||[]).length);
  $('finKpiLowStock')&&($('finKpiLowStock').textContent=low.length);
  $('finKpiMovements')&&($('finKpiMovements').textContent=(data.inventoryMovements||[]).length);
}
function financeRenderRecent(){
  const be=$('finRecentExpenses'); if(be){ const rows=financeFilterRows(data.financeExpenses||[],'expense_date').slice(0,8); be.innerHTML=rows.map(e=>`<tr><td>${esc(e.expense_date||'')}</td><td>${esc(e.project_name||financeProjectName(e.project_id))}</td><td>${esc(e.category||'')}</td><td>${money(e.total)}</td></tr>`).join('')||`<tr><td colspan="4">${data.financeError||'لا توجد مصروفات'}</td></tr>`; }
  const lb=$('finLowStockBody'); if(lb){ const rows=(data.inventoryItems||[]).filter(i=>num(i.quantity)<=num(i.min_quantity)).slice(0,8); lb.innerHTML=rows.map(i=>`<tr><td><b>${esc(i.name)}</b></td><td>${esc(i.category||'')}</td><td><span class="badge red">${num(i.quantity)} ${esc(i.unit||'')}</span></td><td>${num(i.min_quantity)}</td></tr>`).join('')||'<tr><td colspan="4">لا توجد أصناف منخفضة</td></tr>'; }
}
function financeCalcExpenseTotal(){ const safeNum = window.num || function(v){ const n=Number(String(v ?? 0).replace(/,/g,'').trim()); return Number.isFinite(n)?n:0; }; const sub=safeNum(document.getElementById('financeExpenseSubtotal')?.value), vat=safeNum(document.getElementById('financeExpenseVat')?.value); const totalEl=document.getElementById('financeExpenseTotal'); if(totalEl) totalEl.value=(sub+vat).toFixed(2); }
async function financeSaveExpense(btn){ try{ if(btn) btn.disabled=true; const pid=$('financeExpenseProject')?.value; if(!pid) throw new Error('اختر المشروع'); const row={project_id:Number(pid),project_name:projectName(pid),category:$('financeExpenseCategory')?.value||'مصروف عام',expense_date:$('financeExpenseDate')?.value||today(),subtotal:num($('financeExpenseSubtotal')?.value),vat:num($('financeExpenseVat')?.value),total:num($('financeExpenseTotal')?.value),supplier:$('financeExpenseSupplier')?.value||'',payment_method:$('financeExpenseMethod')?.value||'تحويل',notes:$('financeExpenseNotes')?.value||''}; const id=$('financeExpenseId')?.value; const res=id?await sb.from('finance_expenses').update(row).eq('id',id):await sb.from('finance_expenses').insert(row); if(res.error) throw res.error; msg('تم حفظ المصروف'); financeClearExpenseForm(); await financeLoadAll(); }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; } }
function financeClearExpenseForm(){ ['financeExpenseId','financeExpenseSubtotal','financeExpenseVat','financeExpenseTotal','financeExpenseSupplier','financeExpenseNotes'].forEach(id=>$(id)&&($(id).value='')); if($('financeExpenseDate')) $('financeExpenseDate').value=today(); }
function financeRenderExpenses(){ const b=$('financeExpensesBody'); if(!b) return; let rows=financeFilterRows(data.financeExpenses||[],'expense_date'); const cat=$('financeExpenseTypeFilter')?.value; if(cat) rows=rows.filter(e=>e.category===cat); b.innerHTML=rows.map(e=>`<tr><td>${esc(e.expense_date||'')}</td><td>${esc(e.project_name||financeProjectName(e.project_id))}</td><td>${esc(e.category||'')}</td><td>${esc(e.supplier||'-')}</td><td>${money(e.total)}</td><td>${esc(e.payment_method||'')}</td><td class="row-actions"><button onclick="financeEditExpense('${e.id}')">تعديل</button><button class="danger" onclick="financeDelete('finance_expenses','${e.id}')">حذف</button></td></tr>`).join('')||`<tr><td colspan="7">${data.financeError||'لا توجد مصروفات'}</td></tr>`; }
function financeEditExpense(id){ const e=(data.financeExpenses||[]).find(x=>String(x.id)===String(id)); if(!e) return; $('financeExpenseId').value=e.id; $('financeExpenseProject').value=e.project_id||''; $('financeExpenseCategory').value=e.category||'مصروف عام'; $('financeExpenseDate').value=e.expense_date||today(); $('financeExpenseSubtotal').value=e.subtotal||0; $('financeExpenseVat').value=e.vat||0; $('financeExpenseTotal').value=e.total||0; $('financeExpenseSupplier').value=e.supplier||''; $('financeExpenseMethod').value=e.payment_method||'تحويل'; $('financeExpenseNotes').value=e.notes||''; financeShowTab('expenses',document.querySelectorAll('.finance-tab')[1]); $('financeExpenseId')?.scrollIntoView({behavior:'smooth'}); }
async function financeOpenNewProject(){ const name=prompt('اسم المشروع الجديد'); if(!name) return; try{ const row={name:name.trim(),status:'active',required_daily_minutes:180,friday_minutes:90,operation_type:'daily_visit',visit_type_default:'surface'}; const {data:newp,error}=await sb.from('projects').insert(row).select('*').single(); if(error) throw error; data.projects.unshift(newp); financeHydrateForms(); financeRenderAll(); msg('تم إضافة المشروع'); }catch(e){ msg(e.message||String(e),'err'); } }
function inventoryFillItemSelect(){ const el=$('inventoryMovementItem'); if(!el) return; el.innerHTML='<option value="">اختر الصنف</option>'+ (data.inventoryItems||[]).map(i=>`<option value="${i.id}">${esc(i.name)} ${i.serial_number?'('+esc(i.serial_number)+')':''} - المتوفر ${num(i.quantity)} ${esc(i.unit||'')}</option>`).join(''); }

function inventoryFillRequestSelect(){ const el=$('inventoryRequestItem'); if(!el) return; el.innerHTML='<option value="">اختر الصنف</option>'+ (data.inventoryItems||[]).map(i=>`<option value="${i.id}">${esc(i.name)} ${i.serial_number?'('+esc(i.serial_number)+')':''} - المتوفر ${num(i.quantity)} ${esc(i.unit||'')}</option>`).join(''); }
function financeFillSupervisorSelect(){ const el=$('inventoryRequestSupervisor'); if(!el) return; const rows=(data.supervisors||[]).length?data.supervisors:(data.users||[]); el.innerHTML='<option value="">اختر المشرف</option>'+ rows.map(u=>`<option value="${u.id}">${esc(u.full_name||u.username||u.name)}</option>`).join(''); }

const INVENTORY_APPROVAL_ROLES={
  warehouse:'مدير مخازن',
  ops:'مدير تشغيلي',
  finance:'مدير مالي',
  general:'مدير عام'
};
function inventoryDefaultApprovalPath(){ return ['warehouse','ops','finance','general']; }
function inventoryGetApprovalPath(){
  const saved=localStorage.getItem('tasneef_inventory_approval_path');
  if(saved){ try{ const arr=JSON.parse(saved); if(Array.isArray(arr)&&arr.length) return arr.filter(r=>INVENTORY_APPROVAL_ROLES[r]); }catch(e){} }
  return inventoryDefaultApprovalPath();
}
function inventoryReadApprovalPathFromForm(){
  const items=[
    {role:'warehouse', checked:$('approvalWarehouse')?.checked, order:num($('approvalWarehouseOrder')?.value)||1},
    {role:'ops', checked:$('approvalOps')?.checked, order:num($('approvalOpsOrder')?.value)||2},
    {role:'finance', checked:$('approvalFinance')?.checked, order:num($('approvalFinanceOrder')?.value)||3},
    {role:'general', checked:$('approvalGeneral')?.checked, order:num($('approvalGeneralOrder')?.value)||4}
  ].filter(x=>x.checked).sort((a,b)=>a.order-b.order).map(x=>x.role);
  return items.length?items:inventoryDefaultApprovalPath();
}
function inventoryLoadApprovalSettings(reset=false){
  if(reset) localStorage.removeItem('tasneef_inventory_approval_path');
  const path=inventoryGetApprovalPath();
  Object.keys(INVENTORY_APPROVAL_ROLES).forEach((role,idx)=>{
    const cap=role==='ops'?'Ops':role.charAt(0).toUpperCase()+role.slice(1);
    const chk=$('approval'+cap), ord=$('approval'+cap+'Order');
    if(chk) chk.checked=path.includes(role);
    if(ord) ord.value=path.includes(role)?path.indexOf(role)+1:idx+1;
  });
  inventoryRenderApprovalPreview();
}
function inventoryRenderApprovalPreview(){
  const el=$('inventoryApprovalPreview'); if(!el) return;
  const path=inventoryReadApprovalPathFromForm();
  el.innerHTML='<b>المسار الحالي:</b> '+path.map((r,i)=>`${i+1}. ${INVENTORY_APPROVAL_ROLES[r]}`).join(' ← ');
}
function inventorySaveApprovalSettings(){
  const path=inventoryReadApprovalPathFromForm();
  localStorage.setItem('tasneef_inventory_approval_path', JSON.stringify(path));
  inventoryRenderApprovalPreview();
  msg('تم حفظ مسار اعتماد صرف المخزون');
}
function inventoryRequestStatusText(st){
  if(st && st.startsWith('pending_')) return 'بانتظار '+(INVENTORY_APPROVAL_ROLES[st.replace('pending_','')]||st);
  return ({approved:'معتمد ومصروف',rejected:'مرفوض',returned:'معاد للتعديل'})[st] || st || '-';
}
function inventoryRequestStatusClass(st){ return st==='approved'?'green':(st==='rejected'?'red':'amber'); }
function inventoryRequestNextRole(st){ return st&&st.startsWith('pending_') ? (INVENTORY_APPROVAL_ROLES[st.replace('pending_','')]||'-') : (st==='approved'?'مكتمل':(st==='rejected'?'مغلق':'-')); }
function inventoryRequestPath(r){
  let p=null;
  if(Array.isArray(r.approval_path) && r.approval_path.length) p=r.approval_path;
  if(!p && typeof r.approval_path==='string' && r.approval_path){ try{ const x=JSON.parse(r.approval_path); if(Array.isArray(x)&&x.length) p=x; }catch(e){} }
  p=(p&&p.length?p:inventoryDefaultApprovalPath()).filter(role=>INVENTORY_APPROVAL_ROLES[role]);
  return p.length?p:inventoryDefaultApprovalPath();
}
function inventoryRequestStepHtml(r){
  const path=inventoryRequestPath(r);
  const cur=(r.current_step||String(r.status||'').replace('pending_',''));
  return `<div class="approval-track">${path.map((role,i)=>{
    const done=!!r[role+'_approved_by'] || r.status==='approved';
    const active=cur===role && String(r.status||'').startsWith('pending_');
    return `<span class="badge ${done?'green':active?'amber':''}">${i+1}. ${INVENTORY_APPROVAL_ROLES[role]||role}${done?' ✓':''}</span>`;
  }).join(' ')}</div>`;
}
async function inventorySaveRequest(btn){
  try{
    if(btn) btn.disabled=true;
    const itemId=$('inventoryRequestItem')?.value;
    const pid=$('inventoryRequestProject')?.value;
    const supervisorId=$('inventoryRequestSupervisor')?.value;
    const qty=num($('inventoryRequestQty')?.value);
    if(!itemId) throw new Error('اختر الصنف');
    if(!pid) throw new Error('اختر المشروع');
    if(!supervisorId) throw new Error('اختر المشرف');
    if(qty<=0) throw new Error('الكمية مطلوبة');
    const path=inventoryGetApprovalPath();
    const first=path[0]||'warehouse';
    const item=(data.inventoryItems||[]).find(i=>String(i.id)===String(itemId));
    const row={item_id:Number(itemId), item_name:item?.name||'', quantity:qty, project_id:Number(pid), project_name:projectName(pid), supervisor_id:Number(supervisorId), supervisor_name:supervisorName(supervisorId), request_date:$('inventoryRequestDate')?.value||today(), reason:$('inventoryRequestReason')?.value||'', notes:$('inventoryRequestNotes')?.value||'', status:'pending_'+first, current_step:first, approval_path:path, approval_log:[]};
    const {error}=await sb.from('inventory_requests').insert(row);
    if(error) throw error;
    msg('تم إرسال طلب الصرف للاعتماد');
    inventoryClearRequestForm(); await financeLoadAll();
  }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
}
function inventoryClearRequestForm(){ ['inventoryRequestItem','inventoryRequestQty','inventoryRequestSupervisor','inventoryRequestReason','inventoryRequestNotes'].forEach(id=>$(id)&&($(id).value='')); if($('inventoryRequestProject')) $('inventoryRequestProject').value=''; if($('inventoryRequestDate')) $('inventoryRequestDate').value=today(); }
async function inventoryApproveRequest(id,step,btn){
  try{
    if(btn) btn.disabled=true;
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id));
    if(!r) throw new Error('الطلب غير موجود');
    if(r.status!==('pending_'+step)) throw new Error('هذا الطلب ليس في مرحلة '+(INVENTORY_APPROVAL_ROLES[step]||step));
    if(!inventoryCurrentUserCanApprove(step)) throw new Error('ليس لديك صلاحية اعتماد هذه المرحلة');
    if(step==='warehouse'){
      const item=(data.inventoryItems||[]).find(i=>String(i.id)===String(r.item_id));
      if(!item) throw new Error('الصنف غير موجود في المخزون');
      if(num(item.quantity)<num(r.quantity)) throw new Error('الكمية غير متوفرة في المخزون ولا يمكن اعتماد مدير المخازن');
    }
    const u=session()||{};
    const approver=u.full_name||u.username||INVENTORY_APPROVAL_ROLES[step]||'مدير';
    const now=new Date().toISOString();
    const path=inventoryRequestPath(r);
    const idx=path.indexOf(step);
    const next=path[idx+1];
    const log=Array.isArray(r.approval_log)?r.approval_log:[];
    log.push({step,role:INVENTORY_APPROVAL_ROLES[step]||step,by:approver,at:now});
    let row={current_step:next||'done', approval_log:log};
    row[step+'_approved_by']=approver;
    row[step+'_approved_at']=now;
    if(next){
      row.status='pending_'+next;
    }else{
      const item=(data.inventoryItems||[]).find(i=>String(i.id)===String(r.item_id));
      if(!item) throw new Error('الصنف غير موجود في المخزون');
      if(num(item.quantity)<num(r.quantity)) throw new Error('الكمية المتوفرة لا تكفي للصرف');
      const movement={item_id:Number(r.item_id),item_name:r.item_name,movement_type:'out',quantity:num(r.quantity),movement_date:today(),project_id:r.project_id,project_name:r.project_name,receiver:r.supervisor_name,reason:'صرف بناءً على طلب معتمد رقم '+r.id,notes:r.reason||''};
      const mv=await sb.from('inventory_movements').insert(movement).select('*').single();
      if(mv.error) throw mv.error;
      const newQty=num(item.quantity)-num(r.quantity);
      const upd=await sb.from('inventory_items').update({quantity:newQty}).eq('id',r.item_id);
      if(upd.error) throw upd.error;
      row.status='approved'; row.current_step='done'; row.movement_id=mv.data?.id||null;
    }
    const res=await sb.from('inventory_requests').update(row).eq('id',id);
    if(res.error) throw res.error;
    msg(next?'تم اعتماد الطلب ونقله إلى '+(INVENTORY_APPROVAL_ROLES[next]||next):'تم اعتماد الطلب نهائيًا وصرف المخزون');
    await financeLoadAll();
  }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
}
async function inventoryRejectRequest(id,btn){
  const reason=prompt('سبب الرفض؟') || '';
  try{ if(btn) btn.disabled=true; const u=session()||{}; const {error}=await sb.from('inventory_requests').update({status:'rejected',current_step:'closed',rejected_by:u.full_name||u.username||'مدير',rejected_at:new Date().toISOString(),rejection_reason:reason}).eq('id',id); if(error) throw error; msg('تم رفض الطلب'); await financeLoadAll(); }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
}
function inventoryCurrentUserCanApprove(step){
  const userStep=approvalRoleForUser();
  return !!step && step===userStep;
}
function inventoryRenderRequests(){
  const b=$('inventoryRequestsBody'); if(!b) return;
  inventoryLoadApprovalSettings();
  let rows=financeFilterRows(data.inventoryRequests||[],'request_date');
  const st=$('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
  b.innerHTML=rows.map(r=>{
    const actions=[];
    if(String(r.status||'').startsWith('pending_')){
      const step=String(r.status).replace('pending_','');
      if(inventoryCurrentUserCanApprove(step)){
        actions.push(`<button onclick="inventoryApproveRequest('${r.id}','${step}',this)">اعتماد ${esc(INVENTORY_APPROVAL_ROLES[step]||step)}</button>`);
        actions.push(`<button class="danger" onclick="inventoryRejectRequest('${r.id}',this)">رفض</button>`);
      }else{
        actions.push(`<span class="badge neutral">بانتظار ${esc(INVENTORY_APPROVAL_ROLES[step]||'المرحلة التالية')}</span>`);
      }
    }
    actions.unshift(`<button class="light" onclick="inventoryPrintRequest('${r.id}')">طباعة</button>`);
    return `<tr><td>${esc(r.request_date||'')}</td><td><b>${esc(r.supervisor_name||supervisorName(r.supervisor_id))}</b></td><td>${esc(r.project_name||financeProjectName(r.project_id))}</td><td>${esc(r.item_name||'')}</td><td>${num(r.quantity)}</td><td>${esc(r.reason||'-')}</td><td><span class="badge ${inventoryRequestStatusClass(r.status)}">${inventoryRequestStatusText(r.status)}</span><br><small>المرحلة: ${inventoryRequestNextRole(r.status)}</small>${inventoryRequestStepHtml(r)}</td><td class="row-actions">${actions.join(' ')||'-'}</td></tr>`;
  }).join('')||`<tr><td colspan="8">${data.financeError||'لا توجد طلبات صرف'}</td></tr>`;
}


function inventoryGenerateSerial(){
  const d=new Date();
  const pad=n=>String(n).padStart(2,'0');
  const stamp=`${d.getFullYear()}${pad(d.getMonth()+1)}${pad(d.getDate())}-${pad(d.getHours())}${pad(d.getMinutes())}${pad(d.getSeconds())}`;
  return `INV-${stamp}`;
}
function inventoryRenderItemImagePreview(){
  const box=$('inventoryItemImagePreview'); if(!box) return;
  const src=inventoryItemImageData || '';
  box.innerHTML = src ? `<div class="preview-img"><img src="${src}" alt="صورة المنتج"><button onclick="inventoryItemImageData=''; inventoryRenderItemImagePreview()">×</button></div>` : '<small class="muted">لا توجد صورة مضافة</small>';
}
async function inventoryHandleItemImage(input){
  const f=(input.files||[])[0]; if(!f) return;
  try{ inventoryItemImageData=await compressImageToDataUrl(f, 1000, .82); inventoryRenderItemImagePreview(); msg('تمت إضافة صورة المنتج'); }
  catch(e){ msg('تعذر تجهيز صورة المنتج','err'); }
}
async function inventorySaveItem(btn){
  try{
    if(btn) btn.disabled=true;
    const name=($('inventoryItemName')?.value||'').trim();
    if(!name) throw new Error('اسم الصنف مطلوب');
    const row={
      name,
      serial_number: ($('inventoryItemSerial')?.value||'').trim() || inventoryGenerateSerial(),
      image_url: inventoryItemImageData || null,
      category:$('inventoryItemCategory')?.value||'أخرى',
      unit:$('inventoryItemUnit')?.value||'حبة',
      quantity:num($('inventoryItemQty')?.value),
      min_quantity:num($('inventoryItemMin')?.value),
      unit_cost:num($('inventoryItemCost')?.value),
      supplier:$('inventoryItemSupplier')?.value||'',
      notes:$('inventoryItemNotes')?.value||''
    };
    const id=$('inventoryItemId')?.value;
    const res=id?await sb.from('inventory_items').update(row).eq('id',id):await sb.from('inventory_items').insert(row);
    if(res.error) throw res.error;
    msg('تم حفظ الصنف');
    inventoryClearItemForm(); await financeLoadAll();
  }catch(e){ msg(e.message||String(e),'err'); }
  finally{ if(btn) btn.disabled=false; }
}
function inventoryClearItemForm(){ ['inventoryItemId','inventoryItemName','inventoryItemSerial','inventoryItemUnit','inventoryItemQty','inventoryItemMin','inventoryItemCost','inventoryItemSupplier','inventoryItemNotes'].forEach(id=>$(id)&&($(id).value='')); inventoryItemImageData=''; inventoryRenderItemImagePreview(); if($('inventoryItemCategory')) $('inventoryItemCategory').value='كهرباء'; if($('inventoryItemMin')) $('inventoryItemMin').value=0; }
function inventoryRenderItems(){
  const b=$('inventoryItemsBody'); if(!b) return;
  const q=($('financeSearch')?.value||'').trim(); let rows=[...(data.inventoryItems||[])];
  if(q) rows=rows.filter(i=>[i.name,i.serial_number,i.category,i.supplier,i.notes].join(' ').includes(q));
  b.innerHTML=rows.map(i=>{
    const imgSrc=String(i.image_url||'');
    const img=imgSrc?`<div class="inventory-img-box"><img src="${esc(imgSrc)}" class="inventory-thumb inventory-product-img" style="width:58px!important;height:58px!important;min-width:58px!important;min-height:58px!important;max-width:58px!important;max-height:58px!important;object-fit:contain!important;display:block!important;border:0!important;border-radius:9px!important;background:#fff!important;padding:0!important;margin:0!important;cursor:pointer!important;" alt="صورة ${esc(i.name||'المنتج')}" onclick="inventoryOpenProductImage('${String(i.id).replace(/'/g,"\'")}')"></div>`:'<span class="inventory-image-empty">لا توجد</span>';
    return `<tr><td>${img}</td><td>${esc(i.serial_number||'-')}</td><td><b>${esc(i.name)}</b></td><td>${esc(i.category||'')}</td><td>${num(i.quantity)<=num(i.min_quantity)?`<span class="badge red">${num(i.quantity)}</span>`:num(i.quantity)}</td><td>${esc(i.unit||'')}</td><td>${num(i.min_quantity)}</td><td>${money(i.unit_cost)}</td><td>${esc(i.supplier||'-')}</td><td class="row-actions"><button onclick="inventoryEditItem('${i.id}')">تعديل</button><button class="danger" onclick="financeDelete('inventory_items','${i.id}')">حذف</button></td></tr>`;
  }).join('')||`<tr><td colspan="10">${data.financeError||'لا توجد أصناف مخزون'}</td></tr>`;
  inventoryFillItemSelect(); inventoryFillRequestSelect(); financeFillSupervisorSelect(); inventoryLoadApprovalSettings();
}
function inventoryOpenProductImage(id){
  const item=(data.inventoryItems||[]).find(x=>String(x.id)===String(id));
  if(!item || !item.image_url) return;
  openImageLightbox([{data:item.image_url,url:item.image_url}],0);
}
function inventoryEditItem(id){
  const i=(data.inventoryItems||[]).find(x=>String(x.id)===String(id)); if(!i) return;
  $('inventoryItemId').value=i.id; $('inventoryItemName').value=i.name||''; $('inventoryItemSerial').value=i.serial_number||'';
  $('inventoryItemCategory').value=i.category||'أخرى'; $('inventoryItemUnit').value=i.unit||''; $('inventoryItemQty').value=i.quantity||0; $('inventoryItemMin').value=i.min_quantity||0; $('inventoryItemCost').value=i.unit_cost||0; $('inventoryItemSupplier').value=i.supplier||''; $('inventoryItemNotes').value=i.notes||'';
  inventoryItemImageData=i.image_url||''; inventoryRenderItemImagePreview();
  financeShowTab('inventory',document.querySelectorAll('.finance-tab')[2]); $('inventoryItemId')?.scrollIntoView({behavior:'smooth'});
}
async function inventorySaveMovement(btn){ try{ if(btn) btn.disabled=true; const itemId=$('inventoryMovementItem')?.value; if(!itemId) throw new Error('اختر الصنف'); const item=(data.inventoryItems||[]).find(i=>String(i.id)===String(itemId)); const type=$('inventoryMovementType')?.value||'out', qty=num($('inventoryMovementQty')?.value); if(qty<=0) throw new Error('الكمية مطلوبة'); const pid=$('inventoryMovementProject')?.value||null; const row={item_id:Number(itemId),item_name:item?.name||'',movement_type:type,quantity:qty,movement_date:$('inventoryMovementDate')?.value||today(),project_id:pid?Number(pid):null,project_name:pid?projectName(pid):'',receiver:$('inventoryMovementReceiver')?.value||'',reason:$('inventoryMovementReason')?.value||'',notes:$('inventoryMovementNotes')?.value||''}; const id=$('inventoryMovementId')?.value; if(id) throw new Error('تعديل حركة المخزون غير متاح؛ احذف الحركة وأضف حركة جديدة للحفاظ على دقة الرصيد'); const res=await sb.from('inventory_movements').insert(row); if(res.error) throw res.error; let newQty=num(item?.quantity); if(type==='in') newQty+=qty; else if(type==='out') newQty-=qty; else if(type==='adjust') newQty=qty; const upd=await sb.from('inventory_items').update({quantity:newQty}).eq('id',itemId); if(upd.error) throw upd.error; msg('تم حفظ حركة المخزون وتحديث الرصيد'); inventoryClearMovementForm(); await financeLoadAll(); }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; } }
function inventoryClearMovementForm(){ ['inventoryMovementId','inventoryMovementItem','inventoryMovementQty','inventoryMovementReceiver','inventoryMovementReason','inventoryMovementNotes'].forEach(id=>$(id)&&($(id).value='')); if($('inventoryMovementType')) $('inventoryMovementType').value='out'; if($('inventoryMovementDate')) $('inventoryMovementDate').value=today(); if($('inventoryMovementProject')) $('inventoryMovementProject').value=''; }
function inventoryRenderMovements(){ const b=$('inventoryMovementsBody'); if(!b) return; let rows=financeFilterRows(data.inventoryMovements||[],'movement_date'); b.innerHTML=rows.map(m=>{ const type=m.movement_type==='in'?'إدخال':(m.movement_type==='out'?'صرف':'تعديل رصيد'); const cls=m.movement_type==='out'?'amber':(m.movement_type==='in'?'green':''); return `<tr><td>${esc(m.movement_date||'')}</td><td><b>${esc(m.item_name||'')}</b></td><td><span class="badge ${cls}">${type}</span></td><td>${num(m.quantity)}</td><td>${esc(m.project_name||financeProjectName(m.project_id)||'-')}</td><td>${esc(m.receiver||'-')}</td><td><button class="danger" onclick="financeDelete('inventory_movements','${m.id}',true)">حذف</button></td></tr>`; }).join('')||`<tr><td colspan="7">${data.financeError||'لا توجد حركة مخزون'}</td></tr>`; }
function financeRenderReports(){
  const ep=$('expenseByProjectBody'); if(ep){
    const map={}; (financeFilterRows(data.financeExpenses||[],'expense_date')).forEach(e=>{
      const k=e.project_name||financeProjectName(e.project_id)||'بدون مشروع';
      map[k]=map[k]||{total:0,count:0}; map[k].total+=num(e.total); map[k].count++;
    });
    const rows=Object.entries(map).sort((a,b)=>b[1].total-a[1].total);
    ep.innerHTML=rows.map(([k,v])=>`<tr><td>${esc(k)}</td><td>${money(v.total)}</td><td>${v.count}</td></tr>`).join('')||'<tr><td colspan="3">لا توجد بيانات</td></tr>';
  }
  const approvedRequestMovementIds = new Set((data.inventoryRequests||[]).filter(r=>r.status==='approved' && r.movement_id).map(r=>String(r.movement_id)));
  const stockOutMovements=(data.inventoryMovements||[]).filter(m=>m.movement_type==='out' && !approvedRequestMovementIds.has(String(m.id)) && !String(m.reason||'').includes('طلب معتمد'));
  const sp=$('stockOutByProjectBody'); if(sp){
    const map={};
    (data.inventoryRequests||[]).filter(r=>r.status==='approved').forEach(r=>{ const k=r.project_name||financeProjectName(r.project_id)||'بدون مشروع'; map[k]=map[k]||{qty:0,count:0}; map[k].qty+=num(r.quantity); map[k].count++; });
    stockOutMovements.forEach(m=>{ const k=m.project_name||financeProjectName(m.project_id)||'بدون مشروع'; map[k]=map[k]||{qty:0,count:0}; map[k].qty+=num(m.quantity); map[k].count++; });
    const rows=Object.entries(map).sort((a,b)=>b[1].qty-a[1].qty);
    sp.innerHTML=rows.map(([k,v])=>`<tr><td>${esc(k)}</td><td>${v.count}</td><td>${v.qty}</td></tr>`).join('')||'<tr><td colspan="3">لا توجد بيانات</td></tr>';
  }
  const sr=$('stockOutBySupervisorBody'); if(sr){
    const map={};
    (data.inventoryRequests||[]).filter(r=>r.status==='approved').forEach(r=>{ const k=(r.supervisor_name||supervisorName(r.supervisor_id)||'بدون مشرف')+'||'+(r.project_name||financeProjectName(r.project_id)||'بدون مشروع'); map[k]=map[k]||{qty:0,count:0,supervisor:r.supervisor_name||supervisorName(r.supervisor_id),project:r.project_name||financeProjectName(r.project_id)}; map[k].qty+=num(r.quantity); map[k].count++; });
    stockOutMovements.forEach(m=>{ const k=(m.receiver||'بدون مستلم')+'||'+(m.project_name||financeProjectName(m.project_id)||'بدون مشروع'); map[k]=map[k]||{qty:0,count:0,supervisor:m.receiver||'بدون مستلم',project:m.project_name||financeProjectName(m.project_id)}; map[k].qty+=num(m.quantity); map[k].count++; });
    const rows=Object.values(map).sort((a,b)=>b.qty-a.qty);
    sr.innerHTML=rows.map(v=>`<tr><td>${esc(v.supervisor)}</td><td>${esc(v.project)}</td><td>${v.count}</td><td>${v.qty}</td></tr>`).join('')||'<tr><td colspan="4">لا توجد بيانات</td></tr>';
  }
  const ud=$('inventoryUsageDetailBody'); if(ud){
    const rows=[];
    (data.inventoryRequests||[]).filter(r=>r.status==='approved').forEach(r=>{
      const date=String(r.request_date||r.created_at||today()).slice(0,10);
      rows.push({date, project:r.project_name||financeProjectName(r.project_id), person:r.supervisor_name||supervisorName(r.supervisor_id), item:r.item_name, qty:num(r.quantity), reason:r.reason||r.notes||'-', type:'طلب صرف معتمد', ref:'REQ-'+r.id});
    });
    stockOutMovements.forEach(m=>{
      const date=String(m.movement_date||m.created_at||today()).slice(0,10);
      rows.push({date, project:m.project_name||financeProjectName(m.project_id)||'بدون مشروع', person:m.receiver||'بدون مستلم', item:m.item_name, qty:num(m.quantity), reason:m.reason||m.notes||'-', type:'صرف مباشر من المخزون', ref:'MOV-'+m.id});
    });
    rows.sort((a,b)=>String(b.date).localeCompare(String(a.date)) || String(a.project).localeCompare(String(b.project)));
    ud.innerHTML=rows.map(v=>`<tr><td>${esc(v.date)}</td><td>${esc(v.project)}</td><td>${esc(v.person)}</td><td>${esc(v.item)}</td><td>${v.qty}</td><td>${esc(v.reason)}</td><td>${esc(v.type)}</td><td>${esc(daysAgoText(v.date))}</td><td>${esc(v.ref)}</td></tr>`).join('')||'<tr><td colspan="9">لا توجد بيانات استهلاك</td></tr>';
  }
}

function daysAgoText(dateStr){
  if(!dateStr) return '-';
  const d=new Date(String(dateStr).slice(0,10)+'T00:00:00');
  if(isNaN(d.getTime())) return '-';
  const todayD=new Date(today()+'T00:00:00');
  const diff=Math.max(0, Math.round((todayD-d)/86400000));
  return diff===0?'اليوم':(diff===1?'منذ يوم':'منذ '+diff+' يوم');
}
function financePrintWindow(title, bodyHtml){
  const css=`@page{size:A4;margin:10mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;direction:rtl;color:#103d32;margin:0}.page{border:2px solid #0a4033;border-radius:18px;padding:18px;min-height:100vh}.head{display:flex;justify-content:space-between;gap:16px;border-bottom:4px solid #0a4033;padding-bottom:12px;margin-bottom:14px}.brand{font-weight:900;color:#0a4033;font-size:22px}.muted{color:#60706a;font-size:12px}h1{margin:0;color:#0a4033;font-size:24px}table{width:100%;border-collapse:collapse;margin-top:12px;font-size:12px}th{background:#0a4033;color:#fff;padding:9px}td{border:1px solid #dce6e2;padding:8px;text-align:center}tr:nth-child(even) td{background:#f7fbfa}.box{border:1px solid #dce6e2;border-radius:14px;padding:12px;margin:10px 0}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.sig{height:70px;border-bottom:1px dashed #9badA6;margin-top:20px}.footer{text-align:center;margin-top:20px;color:#60706a;font-size:11px}@media print{.no-print{display:none!important}.page{border-radius:0}}`;
  const w=window.open('','_blank');
  if(!w){ msg('المتصفح منع نافذة الطباعة','err'); return; }
  w.document.write(`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${esc(title)}</title><style>${css}</style></head><body><div class="page"><div class="head"><div><div class="brand">تصنيف لإدارة المرافق</div><div class="muted">نظام المصروفات والمخزون</div></div><div><h1>${esc(title)}</h1><div class="muted">تاريخ الطباعة: ${new Date().toLocaleString('ar-SA')}</div></div></div>${bodyHtml}<div class="footer">تم إنشاء هذا المستند من نظام تصنيف</div></div><script>window.onload=function(){setTimeout(function(){window.print()},300)}<\/script></body></html>`);
  w.document.close();
}
function inventoryPrintRequest(id){
  const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r){msg('الطلب غير موجود','err');return;}
  const log=Array.isArray(r.approval_log)?r.approval_log:[];
  const logRows=log.map(l=>`<tr><td>${esc(l.role||l.step)}</td><td>${esc(l.by||'-')}</td><td>${esc(l.at?new Date(l.at).toLocaleString('ar-SA'):'-')}</td></tr>`).join('')||'<tr><td colspan="3">لا يوجد سجل اعتماد</td></tr>';
  const body=`<div class="grid"><div class="box"><b>رقم الطلب</b><br>REQ-${esc(r.id)}</div><div class="box"><b>الحالة</b><br>${esc(inventoryRequestStatusText(r.status))}</div><div class="box"><b>التاريخ</b><br>${esc(r.request_date||'')}</div></div><table><tr><th>المشرف</th><td>${esc(r.supervisor_name||supervisorName(r.supervisor_id))}</td><th>المشروع</th><td>${esc(r.project_name||financeProjectName(r.project_id))}</td></tr><tr><th>الصنف</th><td>${esc(r.item_name||'')}</td><th>الكمية</th><td>${num(r.quantity)}</td></tr><tr><th>سبب الصرف</th><td colspan="3">${esc(r.reason||'-')}</td></tr><tr><th>ملاحظات</th><td colspan="3">${esc(r.notes||'-')}</td></tr></table><h3>سجل الاعتماد</h3><table><thead><tr><th>المرحلة</th><th>المعتمد</th><th>الوقت</th></tr></thead><tbody>${logRows}</tbody></table><div class="grid"><div><b>المستلم</b><div class="sig"></div></div><div><b>مدير المخازن</b><div class="sig"></div></div><div><b>الاعتماد النهائي</b><div class="sig"></div></div></div>`;
  financePrintWindow('أمر صرف مخزون', body);
}
function inventoryPrintMovement(id){
  const m=(data.inventoryMovements||[]).find(x=>String(x.id)===String(id)); if(!m){msg('الحركة غير موجودة','err');return;}
  const type=m.movement_type==='in'?'إدخال':(m.movement_type==='out'?'صرف':'تعديل رصيد');
  const body=`<div class="grid"><div class="box"><b>رقم الحركة</b><br>MOV-${esc(m.id)}</div><div class="box"><b>نوع الحركة</b><br>${esc(type)}</div><div class="box"><b>التاريخ</b><br>${esc(m.movement_date||'')}</div></div><table><tr><th>الصنف</th><td>${esc(m.item_name||'')}</td><th>الكمية</th><td>${num(m.quantity)}</td></tr><tr><th>المشروع</th><td>${esc(m.project_name||financeProjectName(m.project_id)||'-')}</td><th>المستلم</th><td>${esc(m.receiver||'-')}</td></tr><tr><th>السبب</th><td colspan="3">${esc(m.reason||'-')}</td></tr><tr><th>ملاحظات</th><td colspan="3">${esc(m.notes||'-')}</td></tr></table><div class="grid"><div><b>المستلم</b><div class="sig"></div></div><div><b>مسؤول المخزون</b><div class="sig"></div></div><div><b>اعتماد الإدارة</b><div class="sig"></div></div></div>`;
  financePrintWindow('حركة مخزون', body);
}
function financePrintReport(kind){
  const titles={expensesByProject:'تقرير مصروفات حسب المشروع',stockOutByProject:'تقرير صرف المخزون حسب المشروع',stockOutBySupervisor:'تقرير استهلاك المشرفين حسب المشروع',inventoryUsageDetail:'تقرير تفصيلي للاستهلاك'};
  const bodyIds={expensesByProject:'expenseByProjectBody',stockOutByProject:'stockOutByProjectBody',stockOutBySupervisor:'stockOutBySupervisorBody',inventoryUsageDetail:'inventoryUsageDetailBody'};
  const headers={expensesByProject:['المشروع','إجمالي المصروفات','عدد العمليات'],stockOutByProject:['المشروع','عدد العمليات','إجمالي الكميات المصروفة'],stockOutBySupervisor:['المشرف','المشروع','عدد الطلبات','إجمالي الكمية'],inventoryUsageDetail:['التاريخ','المشروع','المشرف / المستلم','الصنف','الكمية','سبب الاستخدام','نوع العملية','منذ كم يوم','المرجع']};
  const tbody=$(bodyIds[kind]); if(!tbody){msg('التقرير غير موجود','err');return;}
  const rows=[...tbody.querySelectorAll('tr')].map(tr=>[...tr.children].map(td=>td.innerText.trim()));
  const table=`<table><thead><tr>${(headers[kind]||[]).map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.map(c=>`<td>${esc(c)}</td>`).join('')}</tr>`).join('')}</tbody></table>`;
  financePrintWindow(titles[kind]||'تقرير', table);
}

async function financeDelete(table,id){
  if(!confirm('تأكيد الحذف؟')) return;
  try{
    if(table==='inventory_movements'){
      const m=(data.inventoryMovements||[]).find(x=>String(x.id)===String(id));
      const item=(data.inventoryItems||[]).find(x=>String(x.id)===String(m?.item_id));
      if(m && item){
        let q=num(item.quantity), mq=num(m.quantity);
        if(m.movement_type==='in') q-=mq; else if(m.movement_type==='out') q+=mq; else if(m.movement_type==='adjust') q=q; // لا يمكن معرفة الرصيد السابق لحركة تعديل الرصيد
        await sb.from('inventory_items').update({quantity:q}).eq('id',item.id);
      }
    }
    const {error}=await sb.from(table).delete().eq('id',id);
    if(error) throw error;
    msg('تم الحذف'); await financeLoadAll();
  }catch(e){ msg(e.message||String(e),'err'); }
}
function financeResetFilters(){ ['financeSearch','financeProjectFilter','financeExpenseTypeFilter'].forEach(id=>$(id)&&($(id).value='')); if($('financeMonthFilter')) $('financeMonthFilter').value=today().slice(0,7); financeRenderAll(); }
(function(){ const _showPage=window.showPage||showPage; window.showPage=function(id,btn){ _showPage(id,btn); if(id==='financeDashboard'){ financeHydrateForms(); if(!financeLoaded) financeLoadAll(); else financeRenderAll(); } }; })();


/* ===== V112: force latest permissions list and approval roles ===== */
(function(){
  function refreshPermsV112(){
    try{ if(typeof hydrateForms==='function') hydrateForms(); }catch(e){}
    try{ if(typeof renderUsers==='function') renderUsers(); }catch(e){}
  }
  window.addEventListener('load', function(){ setTimeout(refreshPermsV112, 300); setTimeout(refreshPermsV112, 1200); });
})();


/* ===== V113: strict sidebar permissions for all admin-area roles ===== */
(function(){
  'use strict';

  const PERMISSIONS_V113 = [
    ['can_dashboard','لوحة التحكم'],
    ['can_time_logs','التسجيلات اليومية'],
    ['can_manage_users','إدارة المستخدمين'],
    ['can_projects','المشاريع'],
    ['can_contracts','العقود والخدمات'],
    ['can_manage_workers','العمال'],
    ['can_attendance','الحضور والغياب'],
    ['can_monthly','الأوقات الشهرية'],
    ['can_tickets','التكتات'],
    ['can_client_reports','تقارير العملاء'],
    ['can_client_ratings','تقييمات العملاء'],
    ['can_alerts','التنبيهات'],
    ['can_assistant','مساعد تصنيف'],
    ['can_export','التصدير']
  ];

  const NAV_PERM_MAP_V113 = {
    dashboard:'can_dashboard',
    daily:'can_time_logs',
    users:'can_manage_users',
    projects:'can_projects',
    contracts:'can_contracts',
    workers:'can_manage_workers',
    attendance:'can_attendance',
    monthly:'can_monthly',
    tickets:'can_tickets',
    clientReports:'can_client_reports',
    clientRatings:'can_client_ratings',
    alerts:'can_alerts',
    assistant:'can_assistant',
    export:'can_export'
  };

  function parsePerms(extra){
    if(typeof extra === 'string'){
      try{ return JSON.parse(extra || '{}') || {}; }catch(e){ return {}; }
    }
    return extra || {};
  }

  function allPerms(v){ return Object.fromEntries(PERMISSIONS_V113.map(([k])=>[k, !!v])); }

  function roleDefaultsV113(role){
    if(role === 'admin' || role === 'general_manager') return allPerms(true);
    if(role === 'warehouse_manager') return {
      can_dashboard:true,
      can_expenses_inventory:true,
      can_inventory_requests:true,
      can_manage_inventory:true
    };
    if(role === 'operations_manager') return {
      can_dashboard:true,
      can_time_logs:true,
      can_projects:true,
      can_contracts:true,
      can_manage_workers:true,
      can_attendance:true,
      can_monthly:true,
      can_tickets:true,
      can_client_reports:true,
      can_client_ratings:true,
      can_expenses_inventory:true,
      can_inventory_requests:true,
      can_alerts:true,
      can_assistant:true,
      can_export:true
    };
    if(role === 'financial_manager') return {
      can_dashboard:true,
      can_client_reports:true,
      can_client_ratings:true,
      can_expenses_inventory:true,
      can_inventory_requests:true,
      can_export:true
    };
    if(role === 'technician') return {
      can_dashboard:true,
      can_tickets:true,
      can_inventory_requests:true
    };
    // مشرف
    return {
      can_dashboard:true,
      can_time_logs:true,
      can_projects:false,
      can_manage_workers:false,
      can_attendance:true,
      can_tickets:true,
      can_client_reports:false,
      can_client_ratings:false,
      can_expenses_inventory:false,
      can_inventory_requests:true
    };
  }

  function normalizePermsV113(user){
    user = user || (typeof session === 'function' ? session() : null) || {};
    const base = roleDefaultsV113(user.role);
    const extra = parsePerms(user.permissions);
    // دعم مفاتيح قديمة
    if(extra.can_reports === true){
      if(extra.can_client_reports === undefined) extra.can_client_reports = true;
      if(extra.can_client_ratings === undefined) extra.can_client_ratings = true;
    }
    if(extra.can_journey === true && extra.can_dashboard === undefined) extra.can_dashboard = true;
    if(extra.can_edit_time_logs === true && extra.can_time_logs === undefined) extra.can_time_logs = true;
    return Object.assign({}, base, extra || {});
  }
  window.getUserPermissionsV72 = normalizePermsV113;
  window.getUserPermissionsV113 = normalizePermsV113;

  function setPermInputsV113(perms){
    perms = perms || {};
    PERMISSIONS_V113.forEach(([key])=>{
      const el = document.getElementById('perm_'+key);
      if(el) el.checked = !!perms[key];
    });
  }
  function readPermInputsV113(){
    const out = {};
    PERMISSIONS_V113.forEach(([key])=>{
      const el = document.getElementById('perm_'+key);
      if(el) out[key] = !!el.checked;
    });
    return out;
  }
  function permissionsTextV113(perms){
    perms = perms || {};
    const labels = PERMISSIONS_V113.filter(([k])=>perms[k]).map(x=>x[1]);
    return labels.length ? labels.join('، ') : '-';
  }

  function injectPermissionsBoxV113(){
    if(window.__tasneefUnifiedPermissionsOnly) return;
    const active = document.getElementById('userActive');
    if(!active) return;
    let box = document.getElementById('userPermissionsBoxV72');
    const html = '<label>الصلاحيات</label><div class="perm-grid-v72">' + PERMISSIONS_V113.map(([key,label]) =>
      `<label class="perm-item-v72"><input type="checkbox" id="perm_${key}" data-perm="${key}"> <span>${label}</span></label>`
    ).join('') + '</div><div class="footer-note">تحدد هذه الصلاحيات ما يظهر للمستخدم داخل التطبيق. أي صلاحية غير محددة لن تظهر في القائمة الجانبية للمستخدم.</div>';
    if(!box){
      box = document.createElement('div');
      box.id = 'userPermissionsBoxV72';
      box.className = 'perm-box-v72';
      active.parentElement.insertBefore(box, active.nextSibling);
    }
    if(!box.querySelector('#perm_can_projects') || !box.querySelector('#perm_can_client_reports') || !box.querySelector('#perm_can_alerts')){
      box.innerHTML = html;
    }
    const role = document.getElementById('userRole');
    if(role && !role.dataset.permHookedV113){
      role.dataset.permHookedV113='1';
      role.addEventListener('change', ()=>setPermInputsV113(roleDefaultsV113(role.value)));
    }
    // فقط عند الإضافة الجديدة، لا نكسر التعديل الحالي
    if(!document.getElementById('userId')?.value) setPermInputsV113(roleDefaultsV113(role?.value || 'supervisor'));
  }

  const oldHydrateV113 = window.hydrateForms;
  window.hydrateForms = function(){
    if(typeof oldHydrateV113 === 'function') oldHydrateV113.apply(this, arguments);
    injectPermissionsBoxV113();
    applyCurrentPermissionsV113();
  };

  window.saveUser = async function(){
    const id=$('userId')?.value;
    const row={
      full_name:$('userFullName')?.value.trim(),
      username:$('userUsername')?.value.trim(),
      password:$('userPassword')?.value.trim()||'123456',
      role:$('userRole')?.value,
      is_active:$('userActive')?.value==='true',
      permissions: readPermInputsV113()
    };
    if(!row.full_name||!row.username) return msg('الاسم واسم المستخدم مطلوبان','err');
    let res = id ? await sb.from('app_users').update(row).eq('id',id) : await sb.from('app_users').insert(row);
    if(res.error && String(res.error.message||'').includes('permissions')){
      const safeRow = Object.assign({}, row); delete safeRow.permissions;
      res = id ? await sb.from('app_users').update(safeRow).eq('id',id) : await sb.from('app_users').insert(safeRow);
      if(!res.error) msg('تم حفظ المستخدم، لكن عمود الصلاحيات غير موجود في قاعدة البيانات','err');
    }
    if(res.error) return msg(res.error.message,'err');
    msg('تم حفظ المستخدم والصلاحيات');
    if(typeof clearUserForm === 'function') clearUserForm();
    await refreshAll();
    injectPermissionsBoxV113();
    applyCurrentPermissionsV113();
  };

  window.renderUsers = function(){
    const b=$('usersBody'); if(!b) return;
    const table=b.closest('table');
    if(table && table.tHead){
      table.tHead.innerHTML='<tr><th>الاسم</th><th>المستخدم</th><th>الدور</th><th>الحالة</th><th>الصلاحيات</th><th>إجراء</th></tr>';
    }
    b.innerHTML=(data.users||[]).map(u=>{
      const perms = normalizePermsV113(u);
      return `<tr><td>${esc(u.full_name)}</td><td>${esc(u.username)}</td><td><span class="badge">${esc(userRoleLabel(u.role))}</span></td><td><span class="badge ${u.is_active?'green':'red'}">${u.is_active?'نشط':'موقوف'}</span></td><td style="white-space:normal;min-width:220px">${esc(permissionsTextV113(perms))}</td><td class="row-actions"><button onclick="editUser(${u.id})">تعديل</button><button class="danger" onclick="deleteRow('app_users',${u.id})">حذف</button></td></tr>`;
    }).join('') || '<tr><td colspan="6">لا يوجد مستخدمون</td></tr>';
  };

  window.editUser = function(id){
    const u=(data.users||[]).find(x=>String(x.id)===String(id)); if(!u)return;
    injectPermissionsBoxV113();
    $('userId').value=u.id;
    $('userFullName').value=u.full_name||'';
    $('userUsername').value=u.username||'';
    $('userPassword').value=u.password||'';
    $('userRole').value=u.role;
    $('userActive').value=String(u.is_active!==false);
    $('userFormTitle').textContent='تعديل مستخدم';
    setPermInputsV113(normalizePermsV113(u));
  };

  const oldClearV113 = window.clearUserForm;
  window.clearUserForm = function(){
    if(typeof oldClearV113 === 'function') oldClearV113.apply(this, arguments);
    injectPermissionsBoxV113();
    const role = document.getElementById('userRole')?.value || 'supervisor';
    setPermInputsV113(roleDefaultsV113(role));
  };

  function applyCurrentPermissionsV113(){
    const u = (typeof session === 'function') ? session() : null;
    if(!u) return;
    const perms = normalizePermsV113(u);
    const navButtons = [...document.querySelectorAll('.side .nav:not(.danger)')];
    navButtons.forEach(btn=>{
      const on = String(btn.getAttribute('onclick')||'');
      const match = on.match(/showPage\('([^']+)'/);
      if(!match) return;
      const page = match[1];
      const permKey = NAV_PERM_MAP_V113[page];
      if(!permKey) return;
      if(perms[permKey]) btn.classList.remove('hidden');
      else btn.classList.add('hidden');
    });

    // إذا كان المستخدم واقفًا في صفحة لا يملك صلاحيتها، حوّله لأول صفحة مسموحة.
    const visibleActive = document.querySelector('.side .nav.active:not(.hidden):not(.danger)');
    if(!visibleActive){
      const firstAllowed = document.querySelector('.side .nav:not(.hidden):not(.danger)');
      if(firstAllowed) firstAllowed.click();
    }
  }
  window.applyCurrentPermissionsV113 = applyCurrentPermissionsV113;

  const oldShowPageV113 = window.showPage || showPage;
  window.showPage = function(id, btn){
    const u = (typeof session === 'function') ? session() : null;
    const permKey = NAV_PERM_MAP_V113[id];
    if(u && permKey && !normalizePermsV113(u)[permKey]){
      msg('ليست لديك صلاحية الدخول لهذه الصفحة','err');
      applyCurrentPermissionsV113();
      return;
    }
    oldShowPageV113(id, btn);
    setTimeout(applyCurrentPermissionsV113, 0);
  };

  window.addEventListener('load', function(){
    setTimeout(()=>{ injectPermissionsBoxV113(); applyCurrentPermissionsV113(); }, 250);
    setTimeout(()=>{ injectPermissionsBoxV113(); applyCurrentPermissionsV113(); }, 1000);
  });
})();

/* ===== V114: hard sidebar permissions fix + live session refresh ===== */
(function(){
  'use strict';

  const PERMISSIONS_V114 = [
    ['can_dashboard','لوحة التحكم'],
    ['can_time_logs','التسجيلات اليومية'],
    ['can_manage_users','إدارة المستخدمين'],
    ['can_projects','المشاريع'],
    ['can_contracts','العقود والخدمات'],
    ['can_manage_workers','العمال'],
    ['can_attendance','الحضور والغياب'],
    ['can_monthly','الأوقات الشهرية'],
    ['can_tickets','التكتات'],
    ['can_client_reports','تقارير العملاء'],
    ['can_client_ratings','تقييمات العملاء'],
    ['can_alerts','التنبيهات'],
    ['can_assistant','مساعد تصنيف'],
    ['can_export','التصدير']
  ];

  const NAV_PERM_MAP_V114 = {
    dashboard:'can_dashboard',
    daily:'can_time_logs',
    users:'can_manage_users',
    projects:'can_projects',
    contracts:'can_contracts',
    workers:'can_manage_workers',
    attendance:'can_attendance',
    monthly:'can_monthly',
    tickets:'can_tickets',
    clientReports:'can_client_reports',
    clientRatings:'can_client_ratings',
    alerts:'can_alerts',
    assistant:'can_assistant',
    export:'can_export'
  };

  function parsePermsV114(v){
    if(!v) return {};
    if(typeof v === 'string'){
      try { return JSON.parse(v || '{}') || {}; } catch(e){ return {}; }
    }
    return v || {};
  }

  function allV114(v){ return Object.fromEntries(PERMISSIONS_V114.map(([k])=>[k, !!v])); }

  function roleDefaultsV114(role){
    if(role === 'admin' || role === 'general_manager') return allV114(true);
    if(role === 'warehouse_manager') return { can_dashboard:true, can_expenses_inventory:true, can_inventory_requests:true, can_manage_inventory:true };
    if(role === 'operations_manager') return {
      can_dashboard:true, can_time_logs:true, can_projects:true, can_contracts:true,
      can_manage_workers:true, can_attendance:true, can_monthly:true, can_tickets:true,
      can_client_reports:true, can_client_ratings:true, can_expenses_inventory:true,
      can_inventory_requests:true, can_manage_inventory:false, can_alerts:true,
      can_assistant:true, can_export:true
    };
    if(role === 'financial_manager') return { can_dashboard:true, can_client_reports:true, can_client_ratings:true, can_expenses_inventory:true, can_inventory_requests:true, can_export:true };
    if(role === 'technician') return { can_dashboard:true, can_tickets:true, can_inventory_requests:true };
    return { can_dashboard:true, can_time_logs:true, can_attendance:true, can_tickets:true, can_inventory_requests:true };
  }

  function hasExplicitPermsV114(user){
    const p = parsePermsV114(user && user.permissions);
    return Object.keys(p).some(k => k.indexOf('can_') === 0);
  }

  function normalizePermsV114(user){
    user = user || (typeof session === 'function' ? session() : null) || {};
    if(user.role === 'admin' || user.role === 'general_manager') return allV114(true);

    const explicit = parsePermsV114(user.permissions);
    // مهم: إذا حفظنا صلاحيات للمستخدم، نعتمد على المختار فقط، ولا نرجع نفتح صلاحيات الدور تلقائيًا.
    if(hasExplicitPermsV114(user)){
      const out = allV114(false);
      Object.keys(out).forEach(k => { if(explicit[k] === true) out[k] = true; });
      // دعم مفاتيح قديمة لو موجودة
      if(explicit.can_reports === true){ out.can_client_reports = true; out.can_client_ratings = true; }
      if(explicit.can_journey === true) out.can_dashboard = true;
      if(explicit.can_edit_time_logs === true) out.can_time_logs = true;
      return out;
    }
    return Object.assign(allV114(false), roleDefaultsV114(user.role));
  }

  window.getUserPermissionsV72 = normalizePermsV114;
  window.getUserPermissionsV113 = normalizePermsV114;
  window.getUserPermissionsV114 = normalizePermsV114;

  function syncCurrentSessionV114(){
    try{
      const u = session && session();
      if(!u || !Array.isArray(data && data.users)) return u;
      const fresh = data.users.find(x => String(x.id) === String(u.id) || (x.username && x.username === u.username));
      if(fresh){ setSession(fresh); return fresh; }
      return u;
    }catch(e){ return null; }
  }

  function setPermInputsV114(perms){
    perms = perms || {};
    PERMISSIONS_V114.forEach(([key])=>{
      const el = document.getElementById('perm_'+key);
      if(el) el.checked = !!perms[key];
    });
  }

  function readPermInputsV114(){
    const out = {};
    PERMISSIONS_V114.forEach(([key])=>{
      const el = document.getElementById('perm_'+key);
      if(el) out[key] = !!el.checked;
    });
    return out;
  }

  function permissionsTextV114(perms){
    perms = perms || {};
    const labels = PERMISSIONS_V114.filter(([k])=>perms[k]).map(x=>x[1]);
    return labels.length ? labels.join('، ') : '-';
  }

  function injectPermissionsBoxV114(){
    if(window.__tasneefUnifiedPermissionsOnly) return;
    const active = document.getElementById('userActive');
    if(!active) return;
    let box = document.getElementById('userPermissionsBoxV72');
    const html = '<label>الصلاحيات</label><div class="perm-grid-v72">' + PERMISSIONS_V114.map(([key,label]) =>
      `<label class="perm-item-v72"><input type="checkbox" id="perm_${key}" data-perm="${key}"> <span>${label}</span></label>`
    ).join('') + '</div><div class="footer-note">تحدد هذه الصلاحيات ما يظهر للمستخدم داخل التطبيق. إذا أزلت العلامة تختفي الصفحة من القائمة الجانبية فورًا بعد دخول المستخدم.</div>';
    if(!box){
      box = document.createElement('div');
      box.id = 'userPermissionsBoxV72';
      box.className = 'perm-box-v72';
      active.parentElement.insertBefore(box, active.nextSibling);
    }
    // استبدال كامل دائمًا لمنع بقاء القائمة القديمة.
    if(box.dataset.v !== '114'){
      box.innerHTML = html;
      box.dataset.v = '114';
    }
    const role = document.getElementById('userRole');
    if(role && !role.dataset.permHookedV114){
      role.dataset.permHookedV114 = '1';
      role.addEventListener('change', ()=>setPermInputsV114(roleDefaultsV114(role.value)));
    }
    if(!document.getElementById('userId')?.value){
      setPermInputsV114(roleDefaultsV114(role?.value || 'supervisor'));
    }
  }

  window.saveUser = async function(){
    const id = $('userId')?.value;
    const row = {
      full_name: $('userFullName')?.value.trim(),
      username: $('userUsername')?.value.trim(),
      password: $('userPassword')?.value.trim() || '123456',
      role: $('userRole')?.value,
      is_active: $('userActive')?.value === 'true',
      permissions: readPermInputsV114()
    };
    if(!row.full_name || !row.username) return msg('الاسم واسم المستخدم مطلوبان','err');
    let res = id ? await sb.from('app_users').update(row).eq('id', id) : await sb.from('app_users').insert(row);
    if(res.error && String(res.error.message||'').includes('permissions')){
      const safeRow = Object.assign({}, row); delete safeRow.permissions;
      res = id ? await sb.from('app_users').update(safeRow).eq('id', id) : await sb.from('app_users').insert(safeRow);
      if(!res.error) msg('تم حفظ المستخدم، لكن عمود الصلاحيات غير موجود. شغّل ملف SQL للصلاحيات','err');
    }
    if(res.error) return msg(res.error.message,'err');
    msg('تم حفظ المستخدم والصلاحيات');
    if(typeof clearUserForm === 'function') clearUserForm();
    await refreshAll();
    syncCurrentSessionV114();
    injectPermissionsBoxV114();
    applyCurrentPermissionsV114(true);
  };

  window.renderUsers = function(){
    const b = $('usersBody'); if(!b) return;
    const table = b.closest('table');
    if(table && table.tHead){
      table.tHead.innerHTML = '<tr><th>الاسم</th><th>المستخدم</th><th>الدور</th><th>الحالة</th><th>الصلاحيات</th><th>إجراء</th></tr>';
    }
    b.innerHTML = (data.users||[]).map(u=>{
      const perms = normalizePermsV114(u);
      return `<tr><td>${esc(u.full_name)}</td><td>${esc(u.username)}</td><td><span class="badge">${esc(userRoleLabel(u.role))}</span></td><td><span class="badge ${u.is_active?'green':'red'}">${u.is_active?'نشط':'موقوف'}</span></td><td style="white-space:normal;min-width:220px">${esc(permissionsTextV114(perms))}</td><td class="row-actions"><button onclick="editUser(${u.id})">تعديل</button><button class="danger" onclick="deleteRow('app_users',${u.id})">حذف</button></td></tr>`;
    }).join('') || '<tr><td colspan="6">لا يوجد مستخدمون</td></tr>';
  };

  window.editUser = function(id){
    const u = (data.users||[]).find(x=>String(x.id)===String(id)); if(!u) return;
    injectPermissionsBoxV114();
    $('userId').value = u.id;
    $('userFullName').value = u.full_name || '';
    $('userUsername').value = u.username || '';
    $('userPassword').value = u.password || '';
    $('userRole').value = u.role;
    $('userActive').value = String(u.is_active !== false);
    $('userFormTitle').textContent = 'تعديل مستخدم';
    setPermInputsV114(normalizePermsV114(u));
  };

  const oldClearUserV114 = window.clearUserForm;
  window.clearUserForm = function(){
    if(typeof oldClearUserV114 === 'function') oldClearUserV114.apply(this, arguments);
    injectPermissionsBoxV114();
    setPermInputsV114(roleDefaultsV114(document.getElementById('userRole')?.value || 'supervisor'));
  };

  function getPageFromNavButton(btn){
    const on = String(btn.getAttribute('onclick') || '');
    const m = on.match(/showPage\('([^']+)'/);
    return m ? m[1] : '';
  }

  function hideButton(btn){
    btn.classList.add('hidden');
    btn.style.display = 'none';
    btn.setAttribute('aria-hidden','true');
    btn.tabIndex = -1;
  }
  function showButton(btn){
    btn.classList.remove('hidden');
    btn.style.display = '';
    btn.removeAttribute('aria-hidden');
    btn.tabIndex = 0;
  }

  function applyCurrentPermissionsV114(forceFirst){
    const u = syncCurrentSessionV114() || (session && session());
    if(!u) return;
    const perms = normalizePermsV114(u);
    document.querySelectorAll('.side .nav:not(.danger)').forEach(btn=>{
      const page = getPageFromNavButton(btn);
      const key = NAV_PERM_MAP_V114[page];
      if(!key) return;
      if(perms[key]) showButton(btn); else hideButton(btn);
    });

    const active = document.querySelector('.side .nav.active:not(.danger)');
    const activePage = active ? getPageFromNavButton(active) : '';
    const activeKey = NAV_PERM_MAP_V114[activePage];
    if(forceFirst || !active || (activeKey && !perms[activeKey])){
      const first = [...document.querySelectorAll('.side .nav:not(.danger)')].find(b => b.style.display !== 'none' && !b.classList.contains('hidden'));
      if(first && first !== active) first.click();
    }
  }
  window.applyCurrentPermissionsV113 = applyCurrentPermissionsV114;
  window.applyCurrentPermissionsV114 = applyCurrentPermissionsV114;

  const oldShowPageV114 = window.showPage;
  window.showPage = function(id, btn){
    const u = syncCurrentSessionV114() || (session && session());
    const key = NAV_PERM_MAP_V114[id];
    if(u && key && !normalizePermsV114(u)[key]){
      msg('ليست لديك صلاحية الدخول لهذه الصفحة','err');
      applyCurrentPermissionsV114(true);
      return;
    }
    if(typeof oldShowPageV114 === 'function') oldShowPageV114(id, btn);
    setTimeout(()=>applyCurrentPermissionsV114(false), 0);
  };

  const oldHydrateV114 = window.hydrateForms;
  window.hydrateForms = function(){
    if(typeof oldHydrateV114 === 'function') oldHydrateV114.apply(this, arguments);
    injectPermissionsBoxV114();
    setTimeout(()=>applyCurrentPermissionsV114(false), 0);
  };

  const oldRefreshAllV114 = window.refreshAll;
  window.refreshAll = async function(){
    if(typeof oldRefreshAllV114 === 'function') await oldRefreshAllV114.apply(this, arguments);
    syncCurrentSessionV114();
    injectPermissionsBoxV114();
    applyCurrentPermissionsV114(false);
  };

  window.addEventListener('load', function(){
    setTimeout(()=>{ syncCurrentSessionV114(); injectPermissionsBoxV114(); applyCurrentPermissionsV114(true); }, 200);
    setTimeout(()=>{ syncCurrentSessionV114(); injectPermissionsBoxV114(); applyCurrentPermissionsV114(true); }, 900);
    setTimeout(()=>{ syncCurrentSessionV114(); injectPermissionsBoxV114(); applyCurrentPermissionsV114(true); }, 1800);
  });
})();

/* =========================
   V211 Advanced Inventory Reports Overrides
   ========================= */
let inventoryRequestLines = [];
if(typeof inventoryItemImageData === 'undefined') var inventoryItemImageData = '';
const INVENTORY_VAT_RATE = 0.15;
function v118ProductCode(i){ return i?.serial_number || i?.product_code || ('INV-'+(i?.id||'')); }
function v118Barcode(i){ return i?.supplier_barcode || i?.barcode || i?.supplier_code || ''; }
function v118ItemType(i){ return i?.item_type || i?.type || ''; }
function v118ItemById(id){ return (data.inventoryItems||[]).find(i=>String(i.id)===String(id)); }
function v118LineItems(r){
  let raw = r?.request_items || r?.items || r?.request_lines || r?.products || null;
  if(typeof raw === 'string' && raw.trim()){ try{ raw=JSON.parse(raw); }catch(e){ raw=null; } }
  if(Array.isArray(raw) && raw.length){
    return raw.map(x=>({
      item_id:x.item_id||x.id,
      item_name:x.item_name||x.name||'',
      product_code:x.product_code||x.serial_number||'',
      barcode:x.barcode||'',
      quantity:num(x.quantity),
      available:num(x.available),
      unit_cost:num(x.unit_cost),
      unit:x.unit||''
    })).filter(x=>x.item_id && x.quantity>0);
  }
  if(r?.item_id) return [{item_id:r.item_id,item_name:r.item_name||'',product_code:r.product_code||'',barcode:r.barcode||'',quantity:num(r.quantity),available:0,unit_cost:0,unit:''}];
  return [];
}
function v118Vat(n){ return +(num(n)*INVENTORY_VAT_RATE).toFixed(2); }
function v118Gross(n){ return +(num(n)*(1+INVENTORY_VAT_RATE)).toFixed(2); }
function v118HtmlTableFromRows(headers, rows){ return `<table><thead><tr>${headers.map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${rows}</tbody></table>`; }

function financeCalcExpenseTotal(){
  const sub=num($('financeExpenseSubtotal')?.value);
  const vat=v118Vat(sub);
  if($('financeExpenseVat')) $('financeExpenseVat').value=vat.toFixed(2);
  if($('financeExpenseTotal')) $('financeExpenseTotal').value=(sub+vat).toFixed(2);
}
function inventoryCalcItemVatPreview(){
  const cost=num($('inventoryItemCost')?.value);
  if($('inventoryItemVatPreview')) $('inventoryItemVatPreview').value=v118Vat(cost).toFixed(2);
  if($('inventoryItemGrossPreview')) $('inventoryItemGrossPreview').value=v118Gross(cost).toFixed(2);
}

function financeHydrateForms(){
  if(!$('financeDashboard')) return;
  financeMonthOptions();
  const pros=(data.projects||[]).map(p=>({id:p.id,name:p.name}));
  ['financeProjectFilter','financeExpenseProject','inventoryMovementProject','inventoryRequestProject'].forEach(id=>fillSelect(id, pros, 'name', id==='inventoryMovementProject'?'بدون مشروع':'كل المشاريع'));
  inventoryFillItemSelect(); inventoryFillRequestSelect(); financeFillSupervisorSelect(); inventoryLoadApprovalSettings(); v118FillReportFilters();
  if($('financeExpenseDate') && !$('financeExpenseDate').value) $('financeExpenseDate').value=today();
  if($('inventoryMovementDate') && !$('inventoryMovementDate').value) $('inventoryMovementDate').value=today();
  if($('inventoryRequestDate') && !$('inventoryRequestDate').value) $('inventoryRequestDate').value=today();
  const reqItem=$('inventoryRequestItem'); if(reqItem && !reqItem.dataset.v118){ reqItem.addEventListener('change', v118UpdateRequestAvailable); reqItem.dataset.v118='1'; }
  financeCalcExpenseTotal(); inventoryCalcItemVatPreview();
}
function v118FillReportFilters(){
  const prod=$('inventoryReportProduct');
  if(prod){ const old=prod.value; prod.innerHTML='<option value="">اختر المنتج</option>'+(data.inventoryItems||[]).map(i=>`<option value="${i.id}">${esc(v118ProductCode(i))} - ${esc(i.name||'')}</option>`).join(''); prod.value=old; }
  const sup=$('inventoryReportSupplier');
  if(sup){ const old=sup.value; const vals=[...new Set((data.inventoryItems||[]).map(i=>i.supplier).filter(Boolean))].sort(); sup.innerHTML='<option value="">كل الموردين</option>'+vals.map(v=>`<option>${esc(v)}</option>`).join(''); sup.value=old; }
  const per=$('inventoryReportPerson');
  if(per){ const old=per.value; const vals=[...new Set([...(data.inventoryMovements||[]).map(m=>m.receiver), ...(data.inventoryRequests||[]).map(r=>r.supervisor_name)].filter(Boolean))].sort(); per.innerHTML='<option value="">كل المشرفين/المستلمين</option>'+vals.map(v=>`<option>${esc(v)}</option>`).join(''); per.value=old; }
}
function financeFilterRows(rows,dateField='expense_date'){
  const q=($('financeSearch')?.value||'').trim(), pid=$('financeProjectFilter')?.value, mon=$('financeMonthFilter')?.value;
  let out=[...rows];
  if(pid) out=out.filter(r=>String(r.project_id)===String(pid));
  if(mon) out=out.filter(r=>String(r[dateField]||'').slice(0,7)===mon);
  if(q) out=out.filter(r=>[
    r.project_name,financeProjectName(r.project_id),r.supplier,r.category,r.notes,r.name,r.item_name,r.receiver,r.reason,
    r.serial_number,r.product_code,r.barcode,r.supplier_barcode,r.item_type,r.type,r.supervisor_name
  ].join(' ').includes(q));
  return out;
}
function inventoryFillItemSelect(){
  const el=$('inventoryMovementItem'); if(!el) return;
  el.innerHTML='<option value="">اختر الصنف</option>'+ (data.inventoryItems||[]).map(i=>`<option value="${i.id}">${esc(v118ProductCode(i))} - ${esc(i.name)} ${v118Barcode(i)?' / '+esc(v118Barcode(i)):''} - المتوفر ${num(i.quantity)} ${esc(i.unit||'')}</option>`).join('');
}
function inventoryFillRequestSelect(){
  const el=$('inventoryRequestItem'); if(!el) return;
  el.innerHTML='<option value="">اختر الصنف</option>'+ (data.inventoryItems||[]).map(i=>`<option value="${i.id}">${esc(v118ProductCode(i))} - ${esc(i.name)} ${v118Barcode(i)?' / '+esc(v118Barcode(i)):''} - المتوفر ${num(i.quantity)} ${esc(i.unit||'')}</option>`).join('');
  v118UpdateRequestAvailable();
}
function v118UpdateRequestAvailable(){
  const item=v118ItemById($('inventoryRequestItem')?.value);
  if($('inventoryRequestAvailable')) $('inventoryRequestAvailable').value=item?`${num(item.quantity)} ${item.unit||''}`:'';
}
function financeFillSupervisorSelect(){
  const el=$('inventoryRequestSupervisor'); if(!el) return;
  const rows=[...(data.supervisors||[]), ...(data.technicians||[])];
  const uniq=[]; const seen=new Set();
  rows.forEach(u=>{ if(!seen.has(String(u.id))){ seen.add(String(u.id)); uniq.push(u); } });
  el.innerHTML='<option value="">اختر المشرف / الفني</option>'+ uniq.map(u=>`<option value="${u.id}">${esc(u.full_name||u.username||u.name)} - ${esc(userRoleLabel(u.role))}</option>`).join('');
}

async function inventorySaveItem(btn){
  try{
    if(btn) btn.disabled=true;
    const name=($('inventoryItemName')?.value||'').trim();
    if(!name) throw new Error('اسم المنتج مطلوب');
    if(!inventoryItemImageData) throw new Error('صورة المنتج إلزامية ولا يمكن حفظ المنتج بدون صورة');
    const row={
      name,
      serial_number: ($('inventoryItemSerial')?.value||'').trim() || inventoryGenerateSerial(),
      supplier_barcode: ($('inventoryItemBarcode')?.value||'').trim(),
      barcode: ($('inventoryItemBarcode')?.value||'').trim(),
      image_url: inventoryItemImageData,
      category:$('inventoryItemCategory')?.value||'أخرى',
      item_type:$('inventoryItemType')?.value||'مادة',
      type:$('inventoryItemType')?.value||'مادة',
      unit:$('inventoryItemUnit')?.value||'حبة',
      quantity:num($('inventoryItemQty')?.value),
      min_quantity:num($('inventoryItemMin')?.value),
      unit_cost:num($('inventoryItemCost')?.value),
      supplier:$('inventoryItemSupplier')?.value||'',
      notes:$('inventoryItemNotes')?.value||''
    };
    const id=$('inventoryItemId')?.value;
    const res=id?await sb.from('inventory_items').update(row).eq('id',id):await sb.from('inventory_items').insert(row);
    if(res.error) throw res.error;
    msg('تم حفظ المنتج'); inventoryClearItemForm(); await financeLoadAll();
  }catch(e){ msg(e.message||String(e),'err'); }
  finally{ if(btn) btn.disabled=false; }
}
function inventoryClearItemForm(){
  ['inventoryItemId','inventoryItemName','inventoryItemSerial','inventoryItemBarcode','inventoryItemUnit','inventoryItemQty','inventoryItemMin','inventoryItemCost','inventoryItemSupplier','inventoryItemNotes','inventoryItemVatPreview','inventoryItemGrossPreview'].forEach(id=>$(id)&&($(id).value=''));
  inventoryItemImageData=''; inventoryRenderItemImagePreview();
  if($('inventoryItemCategory')) $('inventoryItemCategory').value='كهرباء';
  if($('inventoryItemType')) $('inventoryItemType').value='مادة';
  if($('inventoryItemMin')) $('inventoryItemMin').value=0;
}
function inventoryEditItem(id){
  const i=v118ItemById(id); if(!i) return;
  $('inventoryItemId').value=i.id; $('inventoryItemName').value=i.name||''; $('inventoryItemSerial').value=v118ProductCode(i)||'';
  if($('inventoryItemBarcode')) $('inventoryItemBarcode').value=v118Barcode(i)||'';
  $('inventoryItemCategory').value=i.category||'أخرى'; if($('inventoryItemType')) $('inventoryItemType').value=v118ItemType(i)||'مادة';
  $('inventoryItemUnit').value=i.unit||''; $('inventoryItemQty').value=i.quantity||0; $('inventoryItemMin').value=i.min_quantity||0; $('inventoryItemCost').value=i.unit_cost||0; $('inventoryItemSupplier').value=i.supplier||''; $('inventoryItemNotes').value=i.notes||'';
  inventoryItemImageData=i.image_url||''; inventoryRenderItemImagePreview(); inventoryCalcItemVatPreview();
  financeShowTab('inventory',document.querySelectorAll('.finance-tab')[2]); $('inventoryItemId')?.scrollIntoView({behavior:'smooth'});
}
function inventoryRenderItems(){
  const b=$('inventoryItemsBody'); if(!b) return;
  const q=($('financeSearch')?.value||'').trim(); let rows=[...(data.inventoryItems||[])];
  const supplier=$('inventoryReportSupplier')?.value||'';
  if(q) rows=rows.filter(i=>[i.name,v118ProductCode(i),v118Barcode(i),i.category,i.supplier,i.notes,v118ItemType(i)].join(' ').includes(q));
  if(supplier) rows=rows.filter(i=>String(i.supplier||'')===supplier);
  b.innerHTML=rows.map(i=>{
    const imgSrc=String(i.image_url||'');
    const img=imgSrc?`<div class="inventory-img-box"><img src="${esc(imgSrc)}" class="inventory-thumb inventory-product-img" style="width:58px!important;height:58px!important;min-width:58px!important;min-height:58px!important;max-width:58px!important;max-height:58px!important;object-fit:contain!important;display:block!important;border:0!important;border-radius:9px!important;background:#fff!important;padding:0!important;margin:0!important;cursor:pointer!important;" alt="صورة ${esc(i.name||'المنتج')}" onclick="inventoryOpenProductImage('${String(i.id).replace(/'/g,"\\'")}')"></div>`:'<span class="inventory-image-empty">لا توجد</span>';
    return `<tr><td>${img}</td><td>${esc(v118ProductCode(i)||'-')}</td><td>${esc(v118Barcode(i)||'-')}</td><td><b>${esc(i.name)}</b></td><td>${esc(i.category||'')}</td><td>${esc(v118ItemType(i)||'-')}</td><td>${num(i.quantity)<=num(i.min_quantity)?`<span class="badge red">${num(i.quantity)}</span>`:num(i.quantity)}</td><td>${esc(i.unit||'')}</td><td>${num(i.min_quantity)}</td><td>${money(i.unit_cost)}</td><td>${esc(i.supplier||'-')}</td><td class="row-actions"><button onclick="inventoryEditItem('${i.id}')">تعديل</button><button class="light" onclick="v118ShowProductDetail('${i.id}')">تفاصيل</button><button class="danger" onclick="financeDelete('inventory_items','${i.id}')">حذف</button></td></tr>`;
  }).join('')||`<tr><td colspan="12">${data.financeError||'لا توجد أصناف مخزون'}</td></tr>`;
  inventoryFillItemSelect(); inventoryFillRequestSelect(); financeFillSupervisorSelect(); inventoryLoadApprovalSettings(); v118FillReportFilters();
}
function v118ShowProductDetail(id){ financeShowTab('reports',document.querySelectorAll('.finance-tab')[5]); setTimeout(()=>{ if($('inventoryReportProduct')) $('inventoryReportProduct').value=id; financeRenderReports(); $('inventoryProductDetailBox')?.scrollIntoView({behavior:'smooth'}); },80); }

function inventoryAddRequestLine(){
  const item=v118ItemById($('inventoryRequestItem')?.value); const qty=num($('inventoryRequestQty')?.value);
  if(!item){ msg('اختر الصنف أولًا','err'); return; }
  if(qty<=0){ msg('أدخل الكمية المطلوبة','err'); return; }
  const existing=inventoryRequestLines.find(x=>String(x.item_id)===String(item.id));
  if(existing) existing.quantity += qty; else inventoryRequestLines.push({item_id:item.id,item_name:item.name,product_code:v118ProductCode(item),barcode:v118Barcode(item),quantity:qty,available:num(item.quantity),unit:item.unit||'',unit_cost:num(item.unit_cost)});
  if($('inventoryRequestItem')) $('inventoryRequestItem').value=''; if($('inventoryRequestQty')) $('inventoryRequestQty').value=''; v118UpdateRequestAvailable(); inventoryRenderRequestLines();
}
function inventoryRemoveRequestLine(itemId){ inventoryRequestLines=inventoryRequestLines.filter(x=>String(x.item_id)!==String(itemId)); inventoryRenderRequestLines(); }
function inventoryRenderRequestLines(){
  const b=$('inventoryRequestLinesBody'); if(!b) return;
  b.innerHTML=inventoryRequestLines.map(l=>`<tr><td>${esc(l.product_code||'-')}</td><td><b>${esc(l.item_name)}</b></td><td>${num(l.available)} ${esc(l.unit||'')}</td><td>${num(l.quantity)}</td><td><button class="danger" onclick="inventoryRemoveRequestLine('${l.item_id}')">حذف</button></td></tr>`).join('')||'<tr><td colspan="5">لم تتم إضافة أصناف بعد</td></tr>';
}
async function inventorySaveRequest(btn){
  try{
    if(btn) btn.disabled=true;
    const pid=$('inventoryRequestProject')?.value; const supervisorId=$('inventoryRequestSupervisor')?.value;
    if(!pid) throw new Error('اختر المشروع'); if(!supervisorId) throw new Error('اختر المشرف / الفني');
    if(!inventoryRequestLines.length) throw new Error('أضف صنفًا واحدًا على الأقل إلى أمر الصرف');
    const path=inventoryGetApprovalPath(); const first=path[0]||'warehouse';
    const totalQty=inventoryRequestLines.reduce((a,x)=>a+num(x.quantity),0);
    const row={
      item_id:Number(inventoryRequestLines[0].item_id), item_name:inventoryRequestLines.map(x=>x.item_name).join('، '), quantity:totalQty,
      request_items:inventoryRequestLines, items:inventoryRequestLines,
      project_id:Number(pid), project_name:projectName(pid), supervisor_id:Number(supervisorId), supervisor_name:supervisorName(supervisorId),
      request_date:$('inventoryRequestDate')?.value||today(), reason:$('inventoryRequestReason')?.value||'', notes:$('inventoryRequestNotes')?.value||'',
      status:'pending_'+first, current_step:first, approval_path:path, approval_log:[]
    };
    const {error}=await sb.from('inventory_requests').insert(row); if(error) throw error;
    msg('تم إرسال أمر الصرف متعدد الأصناف للاعتماد'); inventoryClearRequestForm(); await financeLoadAll();
  }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
}
function inventoryClearRequestForm(){
  ['inventoryRequestItem','inventoryRequestQty','inventoryRequestSupervisor','inventoryRequestReason','inventoryRequestNotes','inventoryRequestAvailable'].forEach(id=>$(id)&&($(id).value=''));
  if($('inventoryRequestProject')) $('inventoryRequestProject').value=''; if($('inventoryRequestDate')) $('inventoryRequestDate').value=today(); inventoryRequestLines=[]; inventoryRenderRequestLines();
}
async function inventoryApproveRequest(id,step,btn){
  try{
    if(btn) btn.disabled=true;
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) throw new Error('الطلب غير موجود');
    if(r.status!==('pending_'+step)) throw new Error('هذا الطلب ليس في مرحلة '+(INVENTORY_APPROVAL_ROLES[step]||step));
    if(!inventoryCurrentUserCanApprove(step)) throw new Error('ليس لديك صلاحية اعتماد هذه المرحلة');
    const lines=v118LineItems(r); if(!lines.length) throw new Error('لا توجد أصناف في الطلب');
    if(step==='warehouse'){
      const missing=[]; lines.forEach(l=>{ const item=v118ItemById(l.item_id); if(!item || num(item.quantity)<num(l.quantity)) missing.push(`${l.item_name} المتوفر ${item?num(item.quantity):0} والمطلوب ${num(l.quantity)}`); });
      if(missing.length) throw new Error('الكمية غير متوفرة: '+missing.join(' / '));
    }
    const u=session()||{}; const approver=u.full_name||u.username||INVENTORY_APPROVAL_ROLES[step]||'مدير'; const now=new Date().toISOString();
    const path=inventoryRequestPath(r); const idx=path.indexOf(step); const next=path[idx+1]; const log=Array.isArray(r.approval_log)?r.approval_log:[];
    log.push({step,role:INVENTORY_APPROVAL_ROLES[step]||step,by:approver,at:now});
    let row={current_step:next||'done', approval_log:log}; row[step+'_approved_by']=approver; row[step+'_approved_at']=now;
    if(next){ row.status='pending_'+next; }
    else{
      const movementIds=[];
      for(const l of lines){
        const item=v118ItemById(l.item_id); if(!item) throw new Error('الصنف غير موجود: '+l.item_name);
        if(num(item.quantity)<num(l.quantity)) throw new Error('الكمية المتوفرة لا تكفي لصرف '+l.item_name);
        const movement={item_id:Number(l.item_id),item_name:l.item_name,movement_type:'out',quantity:num(l.quantity),movement_date:today(),project_id:r.project_id,project_name:r.project_name,receiver:r.supervisor_name,reason:'صرف بناءً على أمر معتمد رقم '+r.id,notes:r.reason||'',request_id:Number(r.id),product_code:l.product_code||v118ProductCode(item),barcode:l.barcode||v118Barcode(item),unit_cost:num(item.unit_cost)};
        const mv=await sb.from('inventory_movements').insert(movement).select('*').single(); if(mv.error) throw mv.error;
        movementIds.push(mv.data?.id);
        const upd=await sb.from('inventory_items').update({quantity:num(item.quantity)-num(l.quantity)}).eq('id',l.item_id); if(upd.error) throw upd.error;
      }
      row.status='approved'; row.current_step='done'; row.movement_id=movementIds[0]||null; row.movement_ids=movementIds;
    }
    const res=await sb.from('inventory_requests').update(row).eq('id',id); if(res.error) throw res.error;
    msg(next?'تم اعتماد الطلب ونقله إلى '+(INVENTORY_APPROVAL_ROLES[next]||next):'تم اعتماد أمر الصرف نهائيًا وخصم الأصناف من المخزون'); await financeLoadAll();
  }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
}
function inventoryRenderRequests(){
  const b=$('inventoryRequestsBody'); if(!b) return; inventoryLoadApprovalSettings();
  let rows=financeFilterRows(data.inventoryRequests||[],'request_date'); const st=$('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
  b.innerHTML=rows.map(r=>{
    const lines=v118LineItems(r); const itemsHtml=lines.map(l=>`${esc(l.product_code||'')} ${esc(l.item_name)} × ${num(l.quantity)}`).join('<br>') || esc(r.item_name||'');
    const totalQty=lines.reduce((a,l)=>a+num(l.quantity),0) || num(r.quantity);
    const actions=[];
    if(String(r.status||'').startsWith('pending_')){ const step=String(r.status).replace('pending_',''); if(inventoryCurrentUserCanApprove(step)){ actions.push(`<button onclick="inventoryApproveRequest('${r.id}','${step}',this)">اعتماد ${esc(INVENTORY_APPROVAL_ROLES[step]||step)}</button>`); actions.push(`<button class="danger" onclick="inventoryRejectRequest('${r.id}',this)">رفض</button>`); } else actions.push(`<span class="badge neutral">بانتظار ${esc(INVENTORY_APPROVAL_ROLES[step]||'المرحلة التالية')}</span>`); }
    actions.unshift(`<button class="light" onclick="inventoryPrintRequest('${r.id}')">طباعة</button>`);
    return `<tr><td>${esc(r.request_date||'')}</td><td><b>${esc(r.supervisor_name||supervisorName(r.supervisor_id))}</b></td><td>${esc(r.project_name||financeProjectName(r.project_id))}</td><td>${itemsHtml}</td><td>${totalQty}</td><td>${esc(r.reason||'-')}</td><td><span class="badge ${inventoryRequestStatusClass(r.status)}">${inventoryRequestStatusText(r.status)}</span><br><small>المرحلة: ${inventoryRequestNextRole(r.status)}</small>${inventoryRequestStepHtml(r)}</td><td class="row-actions">${actions.join(' ')||'-'}</td></tr>`;
  }).join('')||`<tr><td colspan="8">${data.financeError||'لا توجد طلبات صرف'}</td></tr>`;
}

async function inventorySaveMovement(btn){
  try{
    if(btn) btn.disabled=true; const itemId=$('inventoryMovementItem')?.value; if(!itemId) throw new Error('اختر الصنف');
    const item=v118ItemById(itemId); const qty=num($('inventoryMovementQty')?.value); if(qty<=0) throw new Error('الكمية مطلوبة');
    const type=$('inventoryMovementType')?.value||'out';
    const pid=$('inventoryMovementProject')?.value;
    if(['out','consume'].includes(type) && num(item.quantity)<qty) throw new Error('الكمية المتوفرة لا تكفي');
    const row={item_id:Number(itemId),item_name:item?.name||'',movement_type:type,quantity:qty,movement_date:$('inventoryMovementDate')?.value||today(),project_id:pid?Number(pid):null,project_name:pid?projectName(pid):'',receiver:$('inventoryMovementReceiver')?.value||'',reason:$('inventoryMovementReason')?.value||'',notes:$('inventoryMovementNotes')?.value||'',product_code:v118ProductCode(item),barcode:v118Barcode(item),unit_cost:num(item.unit_cost)};
    const id=$('inventoryMovementId')?.value; const res=id?await sb.from('inventory_movements').update(row).eq('id',id):await sb.from('inventory_movements').insert(row).select('*').single(); if(res.error) throw res.error;
    if(!id){ let newQty=num(item.quantity); if(type==='in'||type==='return') newQty+=qty; else if(type==='out'||type==='consume') newQty-=qty; else if(type==='adjust') newQty=qty; const upd=await sb.from('inventory_items').update({quantity:newQty}).eq('id',itemId); if(upd.error) throw upd.error; }
    msg('تم حفظ حركة المخزون'); inventoryClearMovementForm(); await financeLoadAll();
  }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
}
function inventoryRenderMovements(){
  const b=$('inventoryMovementsBody'); if(!b) return; let rows=financeFilterRows(data.inventoryMovements||[],'movement_date');
  b.innerHTML=rows.map(m=>{ const type={in:'إدخال',out:'صرف',return:'إرجاع',consume:'استهلاك',adjust:'تعديل'}[m.movement_type]||m.movement_type; const cls=m.movement_type==='out'||m.movement_type==='consume'?'amber':(m.movement_type==='in'||m.movement_type==='return'?'green':''); return `<tr><td>${esc(m.movement_date||'')}</td><td><b>${esc(m.item_name||'')}</b><br><small>${esc(m.product_code||'')}</small></td><td><span class="badge ${cls}">${type}</span></td><td>${num(m.quantity)}</td><td>${esc(m.project_name||financeProjectName(m.project_id)||'-')}</td><td>${esc(m.receiver||'-')}</td><td><button class="light" onclick="inventoryPrintMovement('${m.id}')">طباعة</button> <button class="danger" onclick="financeDelete('inventory_movements','${m.id}',true)">حذف</button></td></tr>`; }).join('')||`<tr><td colspan="7">${data.financeError||'لا توجد حركة مخزون'}</td></tr>`;
}
function v118ApprovedRequestMovementIds(){ return new Set((data.inventoryRequests||[]).filter(r=>r.status==='approved' && r.movement_id).map(r=>String(r.movement_id))); }
function v118StockOutMovements(){ const ids=v118ApprovedRequestMovementIds(); return (data.inventoryMovements||[]).filter(m=>['out','consume'].includes(m.movement_type) && !ids.has(String(m.id)) && !String(m.reason||'').includes('طلب معتمد') && !String(m.reason||'').includes('أمر معتمد')); }
function v118AllUsageRows(){
  const rows=[];
  (data.inventoryRequests||[]).filter(r=>r.status==='approved').forEach(r=>{ const date=String(r.request_date||r.created_at||today()).slice(0,10); v118LineItems(r).forEach(l=>rows.push({date,project:r.project_name||financeProjectName(r.project_id),person:r.supervisor_name||supervisorName(r.supervisor_id),item:l.item_name,item_id:l.item_id,product_code:l.product_code,qty:num(l.quantity),reason:r.reason||r.notes||'-',type:'أمر صرف معتمد',ref:'REQ-'+r.id,unit_cost:num(l.unit_cost)})); });
  v118StockOutMovements().forEach(m=>{ const date=String(m.movement_date||m.created_at||today()).slice(0,10); rows.push({date,project:m.project_name||financeProjectName(m.project_id)||'بدون مشروع',person:m.receiver||'بدون مستلم',item:m.item_name,item_id:m.item_id,product_code:m.product_code,qty:num(m.quantity),reason:m.reason||m.notes||'-',type:m.movement_type==='consume'?'استهلاك مباشر':'صرف مباشر من المخزون',ref:'MOV-'+m.id,unit_cost:num(m.unit_cost||v118ItemById(m.item_id)?.unit_cost)}); });
  return rows;
}
function financeRenderReports(){
  const ep=$('expenseByProjectBody'); if(ep){ const map={}; (financeFilterRows(data.financeExpenses||[],'expense_date')).forEach(e=>{ const k=e.project_name||financeProjectName(e.project_id)||'بدون مشروع'; map[k]=map[k]||{total:0,count:0}; map[k].total+=num(e.total); map[k].count++; }); const rows=Object.entries(map).sort((a,b)=>b[1].total-a[1].total); ep.innerHTML=rows.map(([k,v])=>`<tr><td>${esc(k)}</td><td>${money(v.total)}</td><td>${v.count}</td></tr>`).join('')||'<tr><td colspan="3">لا توجد بيانات</td></tr>'; }
  const usage=v118AllUsageRows();
  const sp=$('stockOutByProjectBody'); if(sp){ const map={}; usage.forEach(v=>{ const k=v.project||'بدون مشروع'; map[k]=map[k]||{qty:0,count:0}; map[k].qty+=num(v.qty); map[k].count++; }); const rows=Object.entries(map).sort((a,b)=>b[1].qty-a[1].qty); sp.innerHTML=rows.map(([k,v])=>`<tr><td>${esc(k)}</td><td>${v.count}</td><td>${v.qty}</td></tr>`).join('')||'<tr><td colspan="3">لا توجد بيانات</td></tr>'; }
  const sr=$('stockOutBySupervisorBody'); if(sr){ const map={}; usage.forEach(v=>{ const k=(v.person||'بدون مستلم')+'||'+(v.project||'بدون مشروع'); map[k]=map[k]||{qty:0,count:0,supervisor:v.person,project:v.project}; map[k].qty+=num(v.qty); map[k].count++; }); const rows=Object.values(map).sort((a,b)=>b.qty-a.qty); sr.innerHTML=rows.map(v=>`<tr><td>${esc(v.supervisor)}</td><td>${esc(v.project)}</td><td>${v.count}</td><td>${v.qty}</td></tr>`).join('')||'<tr><td colspan="4">لا توجد بيانات</td></tr>'; }
  const ud=$('inventoryUsageDetailBody'); if(ud){ let rows=usage; const pid=$('inventoryReportProduct')?.value; const person=$('inventoryReportPerson')?.value; if(pid) rows=rows.filter(v=>String(v.item_id)===String(pid)); if(person) rows=rows.filter(v=>String(v.person)===String(person)); rows.sort((a,b)=>String(b.date).localeCompare(String(a.date)) || String(a.project).localeCompare(String(b.project))); ud.innerHTML=rows.map(v=>`<tr><td>${esc(v.date)}</td><td>${esc(v.project)}</td><td>${esc(v.person)}</td><td>${esc(v.product_code||'')} - ${esc(v.item)}</td><td>${v.qty}</td><td>${esc(v.reason)}</td><td>${esc(v.type)}</td><td>${esc(daysAgoText(v.date))}</td><td>${esc(v.ref)}</td></tr>`).join('')||'<tr><td colspan="9">لا توجد بيانات استهلاك</td></tr>'; }
  const stock=$('stockReportBody'); if(stock){ const rows=(data.inventoryItems||[]).map(i=>{ const value=num(i.quantity)*num(i.unit_cost); return `<tr><td>${esc(v118ProductCode(i))}</td><td>${esc(i.name)}</td><td>${num(i.quantity)}</td><td>${num(i.min_quantity)}</td><td>${money(value)}</td></tr>`; }); stock.innerHTML=rows.join('')||'<tr><td colspan="5">لا توجد بيانات</td></tr>'; const sub=(data.inventoryItems||[]).reduce((a,i)=>a+num(i.quantity)*num(i.unit_cost),0); if($('stockReportTotals')) $('stockReportTotals').innerHTML=`إجمالي قبل الضريبة: <b>${money(sub)}</b> | ضريبة 15%: <b>${money(v118Vat(sub))}</b> | الإجمالي شامل الضريبة: <b>${money(v118Gross(sub))}</b>`; }
  v118RenderProductDetailReport(); v118FillReportFilters();
}
function v118RenderProductDetailReport(){
  const box=$('inventoryProductDetailBox'); if(!box) return; const id=$('inventoryReportProduct')?.value; if(!id){ box.innerHTML='<p class="muted">اختر منتجًا لعرض تفاصيله وحركته.</p>'; return; }
  const item=v118ItemById(id); if(!item){ box.innerHTML='<p class="muted">المنتج غير موجود.</p>'; return; }
  const adds=(data.inventoryMovements||[]).filter(m=>String(m.item_id)===String(id)&&m.movement_type==='in');
  const rows=usageRowsForProduct(id);
  const addedQty=adds.reduce((a,m)=>a+num(m.quantity),0); const consumedQty=rows.reduce((a,r)=>a+num(r.qty),0); const consumedVal=rows.reduce((a,r)=>a+num(r.qty)*num(item.unit_cost),0);
  const img=item.image_url?`<img src="${esc(item.image_url)}" style="width:120px;height:120px;object-fit:contain;border:1px solid #dce6e2;border-radius:14px;background:#fff">`:'';
  const addRows=adds.map(m=>`<tr><td>${esc(m.id)}</td><td>${esc(m.movement_date||'')}</td><td>${num(m.quantity)}</td><td>${money(m.unit_cost||item.unit_cost)}</td></tr>`).join('')||'<tr><td colspan="4">لا توجد إضافات مسجلة</td></tr>';
  const useRows=rows.map(r=>`<tr><td>${esc(r.date)}</td><td>${esc(r.person)}</td><td>${esc(r.project)}</td><td>${num(r.qty)}</td><td>0</td><td>${num(r.qty)}</td><td>${num(item.quantity)}</td><td>${money(num(r.qty)*num(item.unit_cost))}</td></tr>`).join('')||'<tr><td colspan="8">لا يوجد استهلاك</td></tr>';
  box.innerHTML=`<div class="card soft-card"><div style="display:flex;gap:16px;align-items:center;justify-content:space-between"><div><h3>${esc(item.name)}</h3><p>كود المنتج: <b>${esc(v118ProductCode(item))}</b> | باركود المورد: <b>${esc(v118Barcode(item)||'-')}</b> | المخزون الحالي: <b>${num(item.quantity)}</b></p><p>المورد: ${esc(item.supplier||'-')} | التصنيف: ${esc(item.category||'-')} | النوع: ${esc(v118ItemType(item)||'-')}</p></div>${img}</div></div><h3>إضافة المنتج</h3><div class="table-wrap">${v118HtmlTableFromRows(['ID','Date','Quantity Added','Unit Price'], addRows)}</div><p><b>Total Quantity Added:</b> ${addedQty} | <b>Total amount before 15% VAT:</b> ${money(addedQty*num(item.unit_cost))} | <b>Total VAT 15%:</b> ${money(v118Vat(addedQty*num(item.unit_cost)))} | <b>Total Value with 15% VAT:</b> ${money(v118Gross(addedQty*num(item.unit_cost)))}</p><h3>Consumed</h3><div class="table-wrap">${v118HtmlTableFromRows(['Employee','Project','Date','out','Return','Consumed','Current Quantity','Consumption Value'], useRows)}</div><p><b>Total amount before 15% VAT:</b> ${money(consumedVal)} | <b>Total VAT 15%:</b> ${money(v118Vat(consumedVal))} | <b>Total Consumption Value with 15% VAT:</b> ${money(v118Gross(consumedVal))}</p>`;
}
function usageRowsForProduct(id){ return v118AllUsageRows().filter(r=>String(r.item_id)===String(id)); }
function inventoryPrintRequest(id){
  const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r){msg('الطلب غير موجود','err');return;}
  const log=Array.isArray(r.approval_log)?r.approval_log:[]; const lines=v118LineItems(r);
  const lineRows=lines.map(l=>`<tr><td>${esc(l.product_code||'')}</td><td>${esc(l.item_name)}</td><td>${num(l.available)}</td><td>${num(l.quantity)}</td><td>${money(num(l.quantity)*num(l.unit_cost))}</td></tr>`).join('')||'<tr><td colspan="5">لا توجد أصناف</td></tr>';
  const logRows=log.map(l=>`<tr><td>${esc(l.role||l.step)}</td><td>${esc(l.by||'-')}</td><td>${esc(l.at?new Date(l.at).toLocaleString('ar-SA'):'-')}</td></tr>`).join('')||'<tr><td colspan="3">لا يوجد سجل اعتماد</td></tr>';
  const total=lines.reduce((a,l)=>a+num(l.quantity)*num(l.unit_cost),0);
  const body=`<div class="grid"><div class="box"><b>رقم الطلب</b><br>OUT-${esc(r.id)}</div><div class="box"><b>الحالة</b><br>${esc(inventoryRequestStatusText(r.status))}</div><div class="box"><b>التاريخ</b><br>${esc(r.request_date||'')}</div></div><table><tr><th>المشرف / الفني</th><td>${esc(r.supervisor_name||supervisorName(r.supervisor_id))}</td><th>المشروع</th><td>${esc(r.project_name||financeProjectName(r.project_id))}</td></tr><tr><th>سبب الصرف</th><td colspan="3">${esc(r.reason||'-')}</td></tr><tr><th>ملاحظات</th><td colspan="3">${esc(r.notes||'-')}</td></tr></table><h3>الأصناف المطلوبة</h3><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>المتوفر</th><th>الكمية</th><th>القيمة</th></tr></thead><tbody>${lineRows}</tbody></table><p><b>الإجمالي قبل الضريبة:</b> ${money(total)} | <b>ضريبة 15%:</b> ${money(v118Vat(total))} | <b>الإجمالي شامل الضريبة:</b> ${money(v118Gross(total))}</p><h3>سجل الاعتماد</h3><table><thead><tr><th>المرحلة</th><th>المعتمد</th><th>الوقت</th></tr></thead><tbody>${logRows}</tbody></table><div class="grid"><div><b>المستلم</b><div class="sig"></div></div><div><b>مدير المخازن</b><div class="sig"></div></div><div><b>الاعتماد النهائي</b><div class="sig"></div></div></div>`;
  financePrintWindow('أمر صرف مخزون متعدد الأصناف', body);
}
function financePrintReport(kind){
  if(kind==='productDetail'){ const html=$('inventoryProductDetailBox')?.innerHTML||'<p>لا توجد بيانات</p>'; financePrintWindow('تقرير تفاصيل منتج', html); return; }
  if(kind==='stockReport'){ const body=`<div class="box">${$('stockReportTotals')?.innerHTML||''}</div><table>${$('stockReportBody')?.closest('table')?.innerHTML||''}</table>`; financePrintWindow('تقرير المخزون العام', body); return; }
  const titles={expensesByProject:'تقرير مصروفات حسب المشروع',stockOutByProject:'تقرير صرف المخزون حسب المشروع',stockOutBySupervisor:'تقرير استهلاك المشرفين حسب المشروع',inventoryUsageDetail:'تقرير تفصيلي للاستهلاك'};
  const bodyIds={expensesByProject:'expenseByProjectBody',stockOutByProject:'stockOutByProjectBody',stockOutBySupervisor:'stockOutBySupervisorBody',inventoryUsageDetail:'inventoryUsageDetailBody'};
  const table=$(bodyIds[kind])?.closest('table'); financePrintWindow(titles[kind]||'تقرير', table?`<table>${table.innerHTML}</table>`:'<p>لا توجد بيانات</p>');
}

/* =========================
   V119 Inventory UI + VAT + Reports Fix
   ========================= */
(function(){
  function v119Text(v){ return String(v ?? '').toLowerCase(); }
  function v119MoneyValue(i){ return num(i?.quantity) * num(i?.unit_cost); }
  function v119SupplierValues(){ return [...new Set((data.inventoryItems||[]).map(i=>i.supplier).filter(Boolean))].sort(); }
  window.inventoryCalcItemVatPreview = function(){
    const cost = num($('inventoryItemCost')?.value);
    const vat = v118Vat(cost);
    const gross = v118Gross(cost);
    if($('inventoryItemVatPreview')) $('inventoryItemVatPreview').value = vat.toFixed(2);
    if($('inventoryItemGrossPreview')) $('inventoryItemGrossPreview').value = gross.toFixed(2);
  };
  function v119BindVatEvents(){
    const cost=$('inventoryItemCost');
    if(cost && !cost.dataset.v119vat){
      ['input','change','keyup','blur'].forEach(ev=>cost.addEventListener(ev, window.inventoryCalcItemVatPreview));
      cost.dataset.v119vat='1';
    }
    const subtotal=$('financeExpenseSubtotal');
    if(subtotal && !subtotal.dataset.v119vat){
      ['input','change','keyup','blur'].forEach(ev=>subtotal.addEventListener(ev, financeCalcExpenseTotal));
      subtotal.dataset.v119vat='1';
    }
    window.inventoryCalcItemVatPreview();
    financeCalcExpenseTotal && financeCalcExpenseTotal();
  }
  window.v118FillReportFilters = function(){
    const supplierFilterValue = $('inventoryReportSupplier')?.value || '';
    const supplierList = v119SupplierValues();
    ['inventoryReportSupplier','inventorySupplierFilter'].forEach(id=>{
      const el=$(id); if(!el) return;
      const old=el.value;
      el.innerHTML='<option value="">كل الموردين</option>'+supplierList.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('');
      el.value = supplierList.includes(old) ? old : '';
    });
    const prod=$('inventoryReportProduct');
    if(prod){
      const old=prod.value;
      let items=[...(data.inventoryItems||[])];
      const sup=$('inventoryReportSupplier')?.value || supplierFilterValue;
      if(sup) items=items.filter(i=>String(i.supplier||'')===String(sup));
      prod.innerHTML='<option value="">اختر المنتج</option>'+items.map(i=>`<option value="${i.id}">${esc(v118ProductCode(i))} - ${esc(i.name||'')}</option>`).join('');
      prod.value = items.some(i=>String(i.id)===String(old)) ? old : '';
    }
    const per=$('inventoryReportPerson');
    if(per){
      const old=per.value;
      const vals=[...new Set([...(data.inventoryMovements||[]).map(m=>m.receiver), ...(data.inventoryRequests||[]).map(r=>r.supervisor_name)].filter(Boolean))].sort();
      per.innerHTML='<option value="">كل المشرفين/المستلمين</option>'+vals.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('');
      per.value=vals.includes(old)?old:'';
    }
  };
  window.financeHydrateForms = function(){
    if(!$('financeDashboard')) return;
    financeMonthOptions && financeMonthOptions();
    const pros=(data.projects||[]).map(p=>({id:p.id,name:p.name}));
    ['financeProjectFilter','financeExpenseProject','inventoryMovementProject','inventoryRequestProject'].forEach(id=>fillSelect(id, pros, 'name', id==='inventoryMovementProject'?'بدون مشروع':'كل المشاريع'));
    inventoryFillItemSelect(); inventoryFillRequestSelect(); financeFillSupervisorSelect(); inventoryLoadApprovalSettings(); v118FillReportFilters();
    if($('financeExpenseDate') && !$('financeExpenseDate').value) $('financeExpenseDate').value=today();
    if($('inventoryMovementDate') && !$('inventoryMovementDate').value) $('inventoryMovementDate').value=today();
    if($('inventoryRequestDate') && !$('inventoryRequestDate').value) $('inventoryRequestDate').value=today();
    const reqItem=$('inventoryRequestItem'); if(reqItem && !reqItem.dataset.v119){ reqItem.addEventListener('change', v118UpdateRequestAvailable); reqItem.dataset.v119='1'; }
    v119BindVatEvents();
  };
  window.inventoryRenderItems = function(){
    const b=$('inventoryItemsBody'); if(!b) return;
    let rows=[...(data.inventoryItems||[])];
    const q=($('inventorySearch')?.value || $('financeSearch')?.value || '').trim().toLowerCase();
    const supplier=$('inventorySupplierFilter')?.value || '';
    const category=$('inventoryCategoryFilter')?.value || '';
    if(supplier) rows=rows.filter(i=>String(i.supplier||'')===String(supplier));
    if(category) rows=rows.filter(i=>String(i.category||'')===String(category));
    if(q) rows=rows.filter(i=>[
      i.name, v118ProductCode(i), v118Barcode(i), i.category, v118ItemType(i), i.supplier, i.unit, i.notes
    ].map(v119Text).join(' ').includes(q));
    rows.sort((a,b)=>String(a.name||'').localeCompare(String(b.name||''),'ar'));
    b.innerHTML=rows.map(i=>{
      const imgSrc=String(i.image_url||'');
      const img=imgSrc?`<img src="${esc(imgSrc)}" class="inventory-thumb-v119" alt="صورة ${esc(i.name||'المنتج')}" onclick="inventoryOpenProductImage('${String(i.id).replace(/'/g,"\\'")}')">`:'<span class="inventory-image-empty">لا توجد</span>';
      const qty = num(i.quantity)<=num(i.min_quantity) ? `<span class="badge red">${num(i.quantity)}</span>` : `<b>${num(i.quantity)}</b>`;
      const meta = `<span class="product-meta">باركود: ${esc(v118Barcode(i)||'-')} | تصنيف: ${esc(i.category||'-')} | نوع: ${esc(v118ItemType(i)||'-')} | حد الطلب: ${num(i.min_quantity)}</span>`;
      return `<tr><td>${img}</td><td><b>${esc(v118ProductCode(i)||'-')}</b></td><td><b>${esc(i.name||'-')}</b>${meta}</td><td>${qty}</td><td>${esc(i.unit||'-')}</td><td>${money(i.unit_cost)}<span class="product-meta">شامل: ${money(v118Gross(num(i.unit_cost)))}</span></td><td>${esc(i.supplier||'-')}</td><td class="row-actions"><button onclick="inventoryEditItem('${i.id}')">تعديل</button><button class="light" onclick="v118ShowProductDetail('${i.id}')">تفاصيل</button><button class="danger" onclick="financeDelete('inventory_items','${i.id}')">حذف</button></td></tr>`;
    }).join('')||`<tr><td colspan="8">${data.financeError||'لا توجد أصناف مخزون'}</td></tr>`;
    v118FillReportFilters(); inventoryFillItemSelect(); inventoryFillRequestSelect(); financeFillSupervisorSelect(); inventoryLoadApprovalSettings(); v119BindVatEvents();
  };
  window.inventoryEditItem = function(id){
    const i=(data.inventoryItems||[]).find(x=>String(x.id)===String(id)); if(!i) return;
    $('inventoryItemId').value=i.id; $('inventoryItemName').value=i.name||''; $('inventoryItemSerial').value=v118ProductCode(i)||'';
    if($('inventoryItemBarcode')) $('inventoryItemBarcode').value=v118Barcode(i)||'';
    $('inventoryItemCategory').value=i.category||'أخرى'; if($('inventoryItemType')) $('inventoryItemType').value=v118ItemType(i)||'مادة';
    $('inventoryItemUnit').value=i.unit||''; $('inventoryItemQty').value=i.quantity||0; $('inventoryItemMin').value=i.min_quantity||0; $('inventoryItemCost').value=i.unit_cost||0; $('inventoryItemSupplier').value=i.supplier||''; $('inventoryItemNotes').value=i.notes||'';
    inventoryItemImageData=i.image_url||''; inventoryRenderItemImagePreview(); window.inventoryCalcItemVatPreview();
    financeShowTab('inventory',document.querySelectorAll('.finance-tab')[2]); $('inventoryItemId')?.scrollIntoView({behavior:'smooth'});
  };
  const oldInventorySaveItemV119 = window.inventorySaveItem;
  window.inventorySaveItem = async function(btn){
    if(!$('inventoryItemName')?.value?.trim()) return msg('اسم المنتج مطلوب','err');
    if(!inventoryItemImageData) return msg('صورة المنتج إلزامية ولا يمكن حفظ المنتج بدون صورة','err');
    window.inventoryCalcItemVatPreview();
    return oldInventorySaveItemV119.call(this, btn);
  };
  function v119FilteredUsage(){
    let rows=v118AllUsageRows();
    const prod=$('inventoryReportProduct')?.value||'';
    const person=$('inventoryReportPerson')?.value||'';
    const sup=$('inventoryReportSupplier')?.value||'';
    if(prod) rows=rows.filter(v=>String(v.item_id)===String(prod));
    if(person) rows=rows.filter(v=>String(v.person)===String(person));
    if(sup){
      const ids=new Set((data.inventoryItems||[]).filter(i=>String(i.supplier||'')===String(sup)).map(i=>String(i.id)));
      rows=rows.filter(v=>ids.has(String(v.item_id)));
    }
    return rows;
  }
  window.financeRenderReports = function(){
    v118FillReportFilters();
    const ep=$('expenseByProjectBody'); if(ep){ const map={}; (financeFilterRows(data.financeExpenses||[],'expense_date')).forEach(e=>{ const k=e.project_name||financeProjectName(e.project_id)||'بدون مشروع'; map[k]=map[k]||{total:0,count:0}; map[k].total+=num(e.total); map[k].count++; }); const rows=Object.entries(map).sort((a,b)=>b[1].total-a[1].total); ep.innerHTML=rows.map(([k,v])=>`<tr><td>${esc(k)}</td><td>${money(v.total)}</td><td>${v.count}</td></tr>`).join('')||'<tr><td colspan="3">لا توجد بيانات</td></tr>'; }
    const usage=v119FilteredUsage();
    const sp=$('stockOutByProjectBody'); if(sp){ const map={}; usage.forEach(v=>{ const k=v.project||'بدون مشروع'; map[k]=map[k]||{qty:0,count:0}; map[k].qty+=num(v.qty); map[k].count++; }); const rows=Object.entries(map).sort((a,b)=>b[1].qty-a[1].qty); sp.innerHTML=rows.map(([k,v])=>`<tr><td>${esc(k)}</td><td>${v.count}</td><td>${v.qty}</td></tr>`).join('')||'<tr><td colspan="3">لا توجد بيانات</td></tr>'; }
    const sr=$('stockOutBySupervisorBody'); if(sr){ const map={}; usage.forEach(v=>{ const k=(v.person||'بدون مستلم')+'||'+(v.project||'بدون مشروع'); map[k]=map[k]||{qty:0,count:0,supervisor:v.person,project:v.project}; map[k].qty+=num(v.qty); map[k].count++; }); const rows=Object.values(map).sort((a,b)=>b.qty-a.qty); sr.innerHTML=rows.map(v=>`<tr><td>${esc(v.supervisor)}</td><td>${esc(v.project)}</td><td>${v.count}</td><td>${v.qty}</td></tr>`).join('')||'<tr><td colspan="4">لا توجد بيانات</td></tr>'; }
    const ud=$('inventoryUsageDetailBody'); if(ud){ let rows=usage; rows.sort((a,b)=>String(b.date).localeCompare(String(a.date)) || String(a.project).localeCompare(String(b.project))); ud.innerHTML=rows.map(v=>`<tr><td>${esc(v.date)}</td><td>${esc(v.project)}</td><td>${esc(v.person)}</td><td>${esc(v.product_code||'')} - ${esc(v.item)}</td><td>${v.qty}</td><td>${esc(v.reason)}</td><td>${esc(v.type)}</td><td>${esc(daysAgoText(v.date))}</td><td>${esc(v.ref)}</td></tr>`).join('')||'<tr><td colspan="9">لا توجد بيانات استهلاك</td></tr>'; }
    const stock=$('stockReportBody'); if(stock){ let items=[...(data.inventoryItems||[])]; const sup=$('inventoryReportSupplier')?.value||''; if(sup) items=items.filter(i=>String(i.supplier||'')===String(sup)); const rows=items.map(i=>{ const value=v119MoneyValue(i); return `<tr><td>${esc(v118ProductCode(i))}</td><td>${esc(i.name)}</td><td>${num(i.quantity)}</td><td>${num(i.min_quantity)}</td><td>${money(value)}</td></tr>`; }); stock.innerHTML=rows.join('')||'<tr><td colspan="5">لا توجد بيانات</td></tr>'; const sub=items.reduce((a,i)=>a+v119MoneyValue(i),0); if($('stockReportTotals')) $('stockReportTotals').innerHTML=`إجمالي قبل الضريبة: <b>${money(sub)}</b> | ضريبة 15%: <b>${money(v118Vat(sub))}</b> | الإجمالي شامل الضريبة: <b>${money(v118Gross(sub))}</b>`; }
    v118RenderProductDetailReport();
  };
  window.v118RenderProductDetailReport = function(){
    const box=$('inventoryProductDetailBox'); if(!box) return; const id=$('inventoryReportProduct')?.value; const sup=$('inventoryReportSupplier')?.value||'';
    if(!id && sup){
      const rows=(data.inventoryItems||[]).filter(i=>String(i.supplier||'')===String(sup));
      const sub=rows.reduce((a,i)=>a+v119MoneyValue(i),0);
      box.innerHTML=`<h3>منتجات المورد: ${esc(sup)}</h3><div class="table-wrap"><table><thead><tr><th>كود المنتج</th><th>اسم المنتج</th><th>المخزون</th><th>سعر الوحدة</th><th>القيمة</th></tr></thead><tbody>${rows.map(i=>`<tr><td>${esc(v118ProductCode(i))}</td><td>${esc(i.name)}</td><td>${num(i.quantity)}</td><td>${money(i.unit_cost)}</td><td>${money(v119MoneyValue(i))}</td></tr>`).join('')||'<tr><td colspan="5">لا توجد منتجات لهذا المورد</td></tr>'}</tbody></table></div><p><b>الإجمالي قبل الضريبة:</b> ${money(sub)} | <b>ضريبة 15%:</b> ${money(v118Vat(sub))} | <b>الإجمالي شامل الضريبة:</b> ${money(v118Gross(sub))}</p>`;
      return;
    }
    if(!id){ box.innerHTML='<p class="muted">اختر منتجًا لعرض تفاصيله وحركته، أو اختر موردًا لعرض منتجاته.</p>'; return; }
    const item=v118ItemById(id); if(!item){ box.innerHTML='<p class="muted">المنتج غير موجود.</p>'; return; }
    const adds=(data.inventoryMovements||[]).filter(m=>String(m.item_id)===String(id)&&m.movement_type==='in');
    const rows=usageRowsForProduct(id).filter(r=>!$('inventoryReportPerson')?.value || String(r.person)===String($('inventoryReportPerson').value));
    const addedQty=adds.reduce((a,m)=>a+num(m.quantity),0); const consumedVal=rows.reduce((a,r)=>a+num(r.qty)*num(item.unit_cost),0);
    const img=item.image_url?`<img src="${esc(item.image_url)}" style="width:110px;height:110px;object-fit:contain;border:1px solid #dce6e2;border-radius:14px;background:#fff">`:'';
    const addRows=adds.map(m=>`<tr><td>${esc(m.id)}</td><td>${esc(m.movement_date||'')}</td><td>${num(m.quantity)}</td><td>${money(m.unit_cost||item.unit_cost)}</td></tr>`).join('')||'<tr><td colspan="4">لا توجد إضافات مسجلة</td></tr>';
    const useRows=rows.map(r=>`<tr><td>${esc(r.person)}</td><td>${esc(r.project)}</td><td>${esc(r.date)}</td><td>${num(r.qty)}</td><td>0</td><td>${num(r.qty)}</td><td>${num(item.quantity)}</td><td>${money(num(r.qty)*num(item.unit_cost))}</td></tr>`).join('')||'<tr><td colspan="8">لا يوجد استهلاك</td></tr>';
    box.innerHTML=`<div class="card soft-card"><div style="display:flex;gap:16px;align-items:center;justify-content:space-between"><div><h3>${esc(item.name)}</h3><p>كود المنتج: <b>${esc(v118ProductCode(item))}</b> | باركود المورد: <b>${esc(v118Barcode(item)||'-')}</b> | المخزون الحالي: <b>${num(item.quantity)}</b></p><p>المورد: ${esc(item.supplier||'-')} | التصنيف: ${esc(item.category||'-')} | النوع: ${esc(v118ItemType(item)||'-')}</p></div>${img}</div></div><h3>إضافة المنتج</h3><div class="table-wrap">${v118HtmlTableFromRows(['ID','Date','Quantity Added','Unit Price'], addRows)}</div><p><b>Total Quantity Added:</b> ${addedQty} | <b>Total amount before 15% VAT:</b> ${money(addedQty*num(item.unit_cost))} | <b>Total VAT 15%:</b> ${money(v118Vat(addedQty*num(item.unit_cost)))} | <b>Total Value with 15% VAT:</b> ${money(v118Gross(addedQty*num(item.unit_cost)))}</p><h3>Consumed</h3><div class="table-wrap">${v118HtmlTableFromRows(['Employee','Project','Date','out','Return','Consumed','Current Quantity','Consumption Value'], useRows)}</div><p><b>Total amount before 15% VAT:</b> ${money(consumedVal)} | <b>Total VAT 15%:</b> ${money(v118Vat(consumedVal))} | <b>Total Consumption Value with 15% VAT:</b> ${money(v118Gross(consumedVal))}</p>`;
  };
  window.addEventListener('load', ()=>setTimeout(()=>{ v119BindVatEvents(); v118FillReportFilters(); inventoryRenderItems(); financeRenderReports(); }, 600));
})();


/* =========================
   V120 Detailed Inventory Reports + Print Logo
   ========================= */
(function(){
  const LOGO_URL = 'tasneef_logo_print.png';
  function unitPrice(itemOrCost){
    if(typeof itemOrCost === 'object') return num(itemOrCost?.unit_cost);
    return num(itemOrCost);
  }
  function grossUnit(item){ return v118Gross(unitPrice(item)); }
  function lineValue(qty, cost){ return num(qty) * num(cost); }
  function itemFor(row){ return v118ItemById(row.item_id) || {}; }
  function productDisplayName(i){ return i?.name || '-'; }
  function moneyCell(v){ return money(num(v)); }
  function totalsBlockV120(totalBefore, label='Total Value with 15% VAT'){
    const sub=num(totalBefore), vat=v118Vat(sub), gross=v118Gross(sub);
    return `<div class="totals-v120"><p><b>Total amount before 15% VAT:</b> ${money(sub)}</p><p><b>Total VAT 15%:</b> ${money(vat)}</p><p><b>${esc(label)}:</b> ${money(gross)}</p></div>`;
  }
  function setTableHead(tableBodyId, headers){
    const body=$(tableBodyId); const tbl=body?.closest('table'); const tr=tbl?.querySelector('thead tr');
    if(tr) tr.innerHTML=headers.map(h=>`<th>${esc(h)}</th>`).join('');
  }
  function setReportHeadsV120(){
    setTableHead('stockReportBody',['كود المنتج','اسم المنتج','المورد','سعر الحبة قبل الضريبة','سعر الحبة شامل الضريبة','الكمية المتبقية','حد الطلب','قيمة المتبقي قبل الضريبة','ضريبة 15%','الإجمالي شامل الضريبة']);
    setTableHead('stockOutByProjectBody',['المشروع','كود المنتج','اسم المنتج','سعر الحبة','المستهلك','المتبقي','قيمة الاستهلاك','ضريبة 15%','الإجمالي شامل الضريبة']);
    setTableHead('stockOutBySupervisorBody',['المشرف / المستلم','المشروع','كود المنتج','اسم المنتج','سعر الحبة','المستهلك','المتبقي','قيمة الاستهلاك','ضريبة 15%','الإجمالي شامل الضريبة']);
    setTableHead('inventoryUsageDetailBody',['التاريخ','المشروع','المشرف / المستلم','كود المنتج','اسم المنتج','المورد','سعر الحبة','الكمية','المتبقي','القيمة','ضريبة 15%','الإجمالي','سبب الاستخدام','نوع العملية','منذ كم يوم','المرجع']);
  }
  function filteredUsageV120(){
    let rows=(typeof v118AllUsageRows==='function'?v118AllUsageRows():[]).map(r=>{
      const it=itemFor(r);
      const cost=num(r.unit_cost || it.unit_cost);
      const qty=num(r.qty);
      const val=lineValue(qty,cost);
      return {...r, itemObj:it, cost, val, vat:v118Vat(val), gross:v118Gross(val), current:num(it.quantity), supplier:it.supplier||'', product_code:r.product_code||v118ProductCode(it)};
    });
    const prod=$('inventoryReportProduct')?.value||'';
    const person=$('inventoryReportPerson')?.value||'';
    const sup=$('inventoryReportSupplier')?.value||'';
    if(prod) rows=rows.filter(v=>String(v.item_id)===String(prod));
    if(person) rows=rows.filter(v=>String(v.person)===String(person));
    if(sup) rows=rows.filter(v=>String(v.supplier||'')===String(sup));
    return rows;
  }
  function renderDetailedRowsByProject(rows){
    return rows.sort((a,b)=>String(a.project).localeCompare(String(b.project),'ar') || String(a.item).localeCompare(String(b.item),'ar'))
      .map(v=>`<tr><td>${esc(v.project||'-')}</td><td>${esc(v.product_code||'-')}</td><td><b>${esc(v.item||'-')}</b></td><td>${moneyCell(v.cost)}</td><td>${num(v.qty)}</td><td>${num(v.current)}</td><td>${moneyCell(v.val)}</td><td>${moneyCell(v.vat)}</td><td>${moneyCell(v.gross)}</td></tr>`).join('') || '<tr><td colspan="9">لا توجد بيانات</td></tr>';
  }
  function renderDetailedRowsBySupervisor(rows){
    return rows.sort((a,b)=>String(a.person).localeCompare(String(b.person),'ar') || String(a.project).localeCompare(String(b.project),'ar'))
      .map(v=>`<tr><td>${esc(v.person||'-')}</td><td>${esc(v.project||'-')}</td><td>${esc(v.product_code||'-')}</td><td><b>${esc(v.item||'-')}</b></td><td>${moneyCell(v.cost)}</td><td>${num(v.qty)}</td><td>${num(v.current)}</td><td>${moneyCell(v.val)}</td><td>${moneyCell(v.vat)}</td><td>${moneyCell(v.gross)}</td></tr>`).join('') || '<tr><td colspan="10">لا توجد بيانات</td></tr>';
  }
  function renderUsageDetailRows(rows){
    return rows.sort((a,b)=>String(b.date).localeCompare(String(a.date)) || String(a.project).localeCompare(String(b.project),'ar'))
      .map(v=>`<tr><td>${esc(v.date||'')}</td><td>${esc(v.project||'-')}</td><td>${esc(v.person||'-')}</td><td>${esc(v.product_code||'-')}</td><td><b>${esc(v.item||'-')}</b></td><td>${esc(v.supplier||'-')}</td><td>${moneyCell(v.cost)}</td><td>${num(v.qty)}</td><td>${num(v.current)}</td><td>${moneyCell(v.val)}</td><td>${moneyCell(v.vat)}</td><td>${moneyCell(v.gross)}</td><td>${esc(v.reason||'-')}</td><td>${esc(v.type||'-')}</td><td>${esc(daysAgoText(v.date))}</td><td>${esc(v.ref||'-')}</td></tr>`).join('') || '<tr><td colspan="16">لا توجد بيانات استهلاك</td></tr>';
  }
  function renderStockRowsV120(items){
    return items.map(i=>{
      const cost=unitPrice(i), qty=num(i.quantity), val=qty*cost;
      return `<tr><td>${esc(v118ProductCode(i))}</td><td><b>${esc(i.name||'-')}</b><br><small>${esc(v118Barcode(i)||'')}</small></td><td>${esc(i.supplier||'-')}</td><td>${moneyCell(cost)}</td><td>${moneyCell(v118Gross(cost))}</td><td>${qty}</td><td>${num(i.min_quantity)}</td><td>${moneyCell(val)}</td><td>${moneyCell(v118Vat(val))}</td><td>${moneyCell(v118Gross(val))}</td></tr>`;
    }).join('') || '<tr><td colspan="10">لا توجد بيانات</td></tr>';
  }
  window.financeRenderReports = function(){
    if(typeof v118FillReportFilters==='function') v118FillReportFilters();
    setReportHeadsV120();
    const ep=$('expenseByProjectBody');
    if(ep){
      const map={}; (financeFilterRows(data.financeExpenses||[],'expense_date')).forEach(e=>{ const k=e.project_name||financeProjectName(e.project_id)||'بدون مشروع'; map[k]=map[k]||{total:0,count:0}; map[k].total+=num(e.total); map[k].count++; });
      const rows=Object.entries(map).sort((a,b)=>b[1].total-a[1].total); ep.innerHTML=rows.map(([k,v])=>`<tr><td>${esc(k)}</td><td>${money(v.total)}</td><td>${v.count}</td></tr>`).join('')||'<tr><td colspan="3">لا توجد بيانات</td></tr>';
    }
    const usage=filteredUsageV120();
    const sp=$('stockOutByProjectBody'); if(sp) sp.innerHTML=renderDetailedRowsByProject(usage);
    const sr=$('stockOutBySupervisorBody'); if(sr) sr.innerHTML=renderDetailedRowsBySupervisor(usage);
    const ud=$('inventoryUsageDetailBody'); if(ud) ud.innerHTML=renderUsageDetailRows(usage);
    const stock=$('stockReportBody');
    if(stock){
      let items=[...(data.inventoryItems||[])]; const sup=$('inventoryReportSupplier')?.value||''; if(sup) items=items.filter(i=>String(i.supplier||'')===String(sup));
      stock.innerHTML=renderStockRowsV120(items);
      const sub=items.reduce((a,i)=>a+num(i.quantity)*num(i.unit_cost),0);
      if($('stockReportTotals')) $('stockReportTotals').innerHTML=totalsBlockV120(sub,'Total Value with 15% VAT');
    }
    if(typeof v118RenderProductDetailReport==='function') v118RenderProductDetailReport();
  };
  window.v118RenderProductDetailReport = function(){
    const box=$('inventoryProductDetailBox'); if(!box) return; const id=$('inventoryReportProduct')?.value; const sup=$('inventoryReportSupplier')?.value||'';
    if(!id && sup){
      const rows=(data.inventoryItems||[]).filter(i=>String(i.supplier||'')===String(sup));
      const sub=rows.reduce((a,i)=>a+num(i.quantity)*num(i.unit_cost),0);
      box.innerHTML=`<h3>منتجات المورد: ${esc(sup)}</h3><div class="table-wrap"><table><thead><tr><th>كود المنتج</th><th>اسم المنتج</th><th>باركود المورد</th><th>سعر الحبة</th><th>شامل الضريبة</th><th>المتبقي</th><th>قيمة المتبقي</th></tr></thead><tbody>${rows.map(i=>`<tr><td>${esc(v118ProductCode(i))}</td><td><b>${esc(i.name)}</b></td><td>${esc(v118Barcode(i)||'-')}</td><td>${moneyCell(i.unit_cost)}</td><td>${moneyCell(v118Gross(i.unit_cost))}</td><td>${num(i.quantity)}</td><td>${moneyCell(num(i.quantity)*num(i.unit_cost))}</td></tr>`).join('')||'<tr><td colspan="7">لا توجد منتجات لهذا المورد</td></tr>'}</tbody></table></div>${totalsBlockV120(sub,'Total Value with 15% VAT')}`;
      return;
    }
    if(!id){ box.innerHTML='<p class="muted">اختر منتجًا لعرض تفاصيله وحركته، أو اختر موردًا لعرض منتجاته.</p>'; return; }
    const item=v118ItemById(id); if(!item){ box.innerHTML='<p class="muted">المنتج غير موجود.</p>'; return; }
    const adds=(data.inventoryMovements||[]).filter(m=>String(m.item_id)===String(id)&&m.movement_type==='in');
    const rows=filteredUsageV120().filter(r=>String(r.item_id)===String(id));
    const addedQty=adds.reduce((a,m)=>a+num(m.quantity),0);
    const addedVal=adds.reduce((a,m)=>a+num(m.quantity)*num(m.unit_cost||item.unit_cost),0);
    const consumedQty=rows.reduce((a,r)=>a+num(r.qty),0);
    const consumedVal=rows.reduce((a,r)=>a+num(r.val),0);
    const img=item.image_url?`<img src="${esc(item.image_url)}" style="width:115px;height:115px;object-fit:contain;border:1px solid #dce6e2;border-radius:14px;background:#fff">`:'';
    const addRows=adds.map(m=>`<tr><td>${esc(m.id)}</td><td>${esc(m.movement_date||'')}</td><td>${num(m.quantity)}</td><td>${moneyCell(m.unit_cost||item.unit_cost)}</td><td>${moneyCell(v118Gross(num(m.unit_cost||item.unit_cost)))}</td><td>${moneyCell(num(m.quantity)*num(m.unit_cost||item.unit_cost))}</td></tr>`).join('')||'<tr><td colspan="6">لا توجد إضافات مسجلة</td></tr>';
    const useRows=rows.map(r=>`<tr><td>${esc(r.date)}</td><td>${esc(r.person)}</td><td>${esc(r.project)}</td><td>${num(r.qty)}</td><td>0</td><td>${num(r.qty)}</td><td>${num(item.quantity)}</td><td>${moneyCell(r.cost)}</td><td>${moneyCell(r.val)}</td><td>${moneyCell(r.vat)}</td><td>${moneyCell(r.gross)}</td></tr>`).join('')||'<tr><td colspan="11">لا يوجد استهلاك</td></tr>';
    box.innerHTML=`<div class="card soft-card"><div style="display:flex;gap:16px;align-items:center;justify-content:space-between"><div><h3>${esc(item.name)}</h3><p>كود المنتج: <b>${esc(v118ProductCode(item))}</b> | باركود المورد: <b>${esc(v118Barcode(item)||'-')}</b></p><p>المورد: ${esc(item.supplier||'-')} | التصنيف: ${esc(item.category||'-')} | النوع: ${esc(v118ItemType(item)||'-')}</p></div>${img}</div></div><h3>إضافة المنتج</h3><div class="table-wrap"><table><thead><tr><th>ID</th><th>Date</th><th>Quantity Added</th><th>Unit Price</th><th>Unit Price with VAT</th><th>Value</th></tr></thead><tbody>${addRows}</tbody></table></div>${totalsBlockV120(addedVal,'Total Value with 15% VAT')}<h3>Consumed</h3><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>Employee</th><th>Project</th><th>out</th><th>Return</th><th>Consumed</th><th>Current Quantity</th><th>Unit Price</th><th>Consumption Value</th><th>VAT 15%</th><th>Total with VAT</th></tr></thead><tbody>${useRows}</tbody></table></div>${totalsBlockV120(consumedVal,'Total Consumption Value with 15% VAT')}`;
  };
  window.financePrintWindow = function(title, bodyHtml){
    const css=`@page{size:A4;margin:10mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;direction:rtl;color:#111;margin:0;background:#fff}.page{padding:10px;min-height:100vh}.logo-wrap{text-align:center;margin:0 0 22px}.logo-wrap img{width:250px;max-width:70%;height:auto;object-fit:contain}.meta-head{display:flex;justify-content:space-between;gap:12px;margin-bottom:18px;align-items:flex-start}.meta-head h1{margin:0;font-size:22px;color:#111}.muted{color:#555;font-size:12px}.box{border:1px solid #bbb;border-radius:8px;padding:9px;margin:8px 0}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}h2,h3{color:#111;margin:18px 0 8px}table{width:100%;border-collapse:collapse;margin-top:12px;font-size:11px;direction:rtl}th{background:#d0d0d0;color:#111;border:1px solid #333;padding:7px;font-weight:800}td{border:1px solid #333;padding:6px;text-align:center;vertical-align:middle}tr:nth-child(even) td{background:#fafafa}.totals-v120{margin-top:18px;font-size:15px;line-height:1.9;direction:ltr;text-align:left}.totals-v120 p{margin:6px 0}.sig{height:60px;border-bottom:1px dashed #777;margin-top:18px}.footer{text-align:center;margin-top:20px;color:#666;font-size:11px}.no-print{display:none}@media print{body{print-color-adjust:exact;-webkit-print-color-adjust:exact}.page{padding:0}.box,.grid,table{break-inside:auto}tr{break-inside:avoid}}`;
    const w=window.open('','_blank');
    if(!w){ msg('المتصفح منع نافذة الطباعة','err'); return; }
    w.document.write(`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${esc(title)}</title><style>${css}</style></head><body><div class="page"><div class="logo-wrap"><img src="${LOGO_URL}" alt="Tasneef Logo"></div><div class="meta-head"><div><h1>${esc(title)}</h1><div class="muted">نظام المصروفات والمخزون</div></div><div class="muted">تاريخ الطباعة: ${new Date().toLocaleString('ar-SA')}</div></div>${bodyHtml}<div class="footer">تم إنشاء هذا المستند من نظام تصنيف</div></div><script>window.onload=function(){setTimeout(function(){window.print()},500)}<\/script></body></html>`);
    w.document.close();
  };
  window.financePrintReport = function(kind){
    const titles={expensesByProject:'تقرير مصروفات حسب المشروع',stockOutByProject:'تقرير صرف المخزون حسب المشروع',stockOutBySupervisor:'تقرير استهلاك المشرفين حسب المشروع',inventoryUsageDetail:'تقرير تفصيلي للاستهلاك',productDetail:'تقرير تفاصيل منتج',stockReport:'تقرير المخزون العام'};
    if(kind==='productDetail'){ window.financePrintWindow(titles[kind], $('inventoryProductDetailBox')?.innerHTML||'<p>لا توجد بيانات</p>'); return; }
    if(kind==='stockReport'){
      const table=$('stockReportBody')?.closest('table'); window.financePrintWindow(titles[kind], `<table>${table?.innerHTML||''}</table>${$('stockReportTotals')?.innerHTML||''}`); return;
    }
    const bodyIds={expensesByProject:'expenseByProjectBody',stockOutByProject:'stockOutByProjectBody',stockOutBySupervisor:'stockOutBySupervisorBody',inventoryUsageDetail:'inventoryUsageDetailBody'};
    const table=$(bodyIds[kind])?.closest('table');
    let totalHtml='';
    if(kind==='stockOutByProject'||kind==='stockOutBySupervisor'||kind==='inventoryUsageDetail'){
      const total=filteredUsageV120().reduce((a,r)=>a+num(r.val),0); totalHtml=totalsBlockV120(total,'Total Consumption Value with 15% VAT');
    }
    window.financePrintWindow(titles[kind]||'تقرير', table?`<table>${table.innerHTML}</table>${totalHtml}`:'<p>لا توجد بيانات</p>');
  };
  window.inventoryPrintRequest = function(id){
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r){msg('الطلب غير موجود','err');return;}
    const log=Array.isArray(r.approval_log)?r.approval_log:[]; const lines=v118LineItems(r).map(l=>{ const it=v118ItemById(l.item_id)||{}; const cost=num(l.unit_cost||it.unit_cost); const qty=num(l.quantity); return {...l,it,cost,qty,val:qty*cost}; });
    const lineRows=lines.map(l=>`<tr><td>${esc(l.product_code||v118ProductCode(l.it)||'')}</td><td>${esc(l.item_name||l.it.name||'')}</td><td>${esc(l.it.supplier||'-')}</td><td>${num(l.available||l.it.quantity)}</td><td>${num(l.qty)}</td><td>${moneyCell(l.cost)}</td><td>${moneyCell(v118Gross(l.cost))}</td><td>${moneyCell(l.val)}</td></tr>`).join('')||'<tr><td colspan="8">لا توجد أصناف</td></tr>';
    const logRows=log.map(l=>`<tr><td>${esc(l.role||l.step)}</td><td>${esc(l.by||'-')}</td><td>${esc(l.at?new Date(l.at).toLocaleString('ar-SA'):'-')}</td></tr>`).join('')||'<tr><td colspan="3">لا يوجد سجل اعتماد</td></tr>';
    const total=lines.reduce((a,l)=>a+num(l.val),0);
    const body=`<div class="grid"><div class="box"><b>رقم الطلب</b><br>OUT-${esc(r.id)}</div><div class="box"><b>الحالة</b><br>${esc(inventoryRequestStatusText(r.status))}</div><div class="box"><b>التاريخ</b><br>${esc(r.request_date||'')}</div></div><table><tr><th>المشرف / الفني</th><td>${esc(r.supervisor_name||supervisorName(r.supervisor_id))}</td><th>المشروع</th><td>${esc(r.project_name||financeProjectName(r.project_id))}</td></tr><tr><th>سبب الصرف</th><td colspan="3">${esc(r.reason||'-')}</td></tr><tr><th>ملاحظات</th><td colspan="3">${esc(r.notes||'-')}</td></tr></table><h3>الأصناف المطلوبة</h3><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>المورد</th><th>المتوفر</th><th>الكمية</th><th>سعر الحبة</th><th>سعر الحبة شامل الضريبة</th><th>القيمة</th></tr></thead><tbody>${lineRows}</tbody></table>${totalsBlockV120(total,'Total Value with 15% VAT')}<h3>سجل الاعتماد</h3><table><thead><tr><th>المرحلة</th><th>المعتمد</th><th>الوقت</th></tr></thead><tbody>${logRows}</tbody></table><div class="grid"><div><b>المستلم</b><div class="sig"></div></div><div><b>مدير المخازن</b><div class="sig"></div></div><div><b>الاعتماد النهائي</b><div class="sig"></div></div></div>`;
    window.financePrintWindow('أمر صرف مخزون متعدد الأصناف', body);
  };
  window.addEventListener('load',()=>setTimeout(()=>{setReportHeadsV120(); if($('financeDashboard')) financeRenderReports();},900));
})();

/* V121: VAT modes, detailed reports, return workflow, English numbers, stamp printing */
(function(){
  const STAMP_URL='tasneef_stamp.jpeg';
  // English digits everywhere money is rendered
  money = function(v){ return `${num(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س`; };
  function moneyEn(v){ return money(v); }
  function qtyEn(v){ return num(v).toLocaleString('en-US',{maximumFractionDigits:2}); }
  function vatModeOf(i){ return (i && (i.vat_mode||i.tax_mode)) || 'exclusive'; }
  function vatRateOf(i){ return vatModeOf(i)==='exempt' ? 0 : 0.15; }
  function vatOfAmount(v,i){ return num(v)*vatRateOf(i); }
  function grossOfAmount(v,i){ return num(v)+vatOfAmount(v,i); }
  v118Vat = function(v){ return num(v)*0.15; };
  v118Gross = function(v){ return num(v)*1.15; };
  function unitNetFromInput(input, mode){ const x=num(input); return mode==='inclusive' ? x/1.15 : x; }
  function unitGross(item){ return grossOfAmount(num(item?.unit_cost), item); }
  function movementReturnsForRequest(reqId,itemId){
    return (data.inventoryMovements||[]).filter(m=>m.movement_type==='return' && String(m.request_id||'')===String(reqId||'') && String(m.item_id||'')===String(itemId||''));
  }
  function reportDatePass(date){
    const mode=$('inventoryReportDateMode')?.value||'all';
    const d=String(date||'').slice(0,10);
    if(mode==='day'){ const day=$('inventoryReportDay')?.value; return !day || d===day; }
    if(mode==='month'){ const mon=$('inventoryReportMonth')?.value; return !mon || d.slice(0,7)===mon; }
    return true;
  }
  window.inventoryClearReportDateFilter=function(){
    if($('inventoryReportDateMode')) $('inventoryReportDateMode').value='all';
    if($('inventoryReportDay')) $('inventoryReportDay').value='';
    if($('inventoryReportMonth')) $('inventoryReportMonth').value='';
    financeRenderReports();
  };
  window.inventoryCalcItemVatPreview=function(){
    const input=num($('inventoryItemCost')?.value);
    const mode=$('inventoryItemVatMode')?.value||'exclusive';
    const net=unitNetFromInput(input,mode);
    const vat=mode==='exempt'?0:net*0.15;
    const gross=mode==='inclusive'?input:(net+vat);
    if($('inventoryItemVatPreview')) $('inventoryItemVatPreview').value=vat.toFixed(2);
    if($('inventoryItemGrossPreview')) $('inventoryItemGrossPreview').value=gross.toFixed(2);
  };
  const oldFinanceHydrateForms = window.financeHydrateForms || financeHydrateForms;
  window.financeHydrateForms=function(){ oldFinanceHydrateForms(); inventoryCalcItemVatPreview(); if($('inventoryReportDay')&&!$('inventoryReportDay').value) $('inventoryReportDay').value=today(); if($('inventoryReportMonth')&&!$('inventoryReportMonth').value) $('inventoryReportMonth').value=today().slice(0,7); };
  window.inventorySaveItem=async function(btn){
    try{
      if(btn) btn.disabled=true;
      const name=($('inventoryItemName')?.value||'').trim();
      if(!name) throw new Error('اسم المنتج مطلوب');
      if(!inventoryItemImageData) throw new Error('صورة المنتج إلزامية ولا يمكن حفظ المنتج بدون صورة');
      const mode=$('inventoryItemVatMode')?.value||'exclusive';
      const netCost=unitNetFromInput($('inventoryItemCost')?.value, mode);
      const row={
        name,
        serial_number: ($('inventoryItemSerial')?.value||'').trim() || inventoryGenerateSerial(),
        supplier_barcode: ($('inventoryItemBarcode')?.value||'').trim(),
        barcode: ($('inventoryItemBarcode')?.value||'').trim(),
        image_url: inventoryItemImageData,
        category:$('inventoryItemCategory')?.value||'أخرى',
        item_type:$('inventoryItemType')?.value||'مادة',
        type:$('inventoryItemType')?.value||'مادة',
        unit:$('inventoryItemUnit')?.value||'حبة',
        quantity:num($('inventoryItemQty')?.value),
        min_quantity:num($('inventoryItemMin')?.value),
        unit_cost:netCost,
        vat_mode:mode,
        supplier:$('inventoryItemSupplier')?.value||'',
        notes:$('inventoryItemNotes')?.value||''
      };
      const id=$('inventoryItemId')?.value;
      const res=id?await sb.from('inventory_items').update(row).eq('id',id):await sb.from('inventory_items').insert(row).select('*').single();
      if(res.error) throw res.error;
      msg('تم حفظ الصنف'); inventoryClearItemForm(); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };
  window.inventoryEditItem=function(id){
    const i=(data.inventoryItems||[]).find(x=>String(x.id)===String(id)); if(!i) return;
    $('inventoryItemId').value=i.id; $('inventoryItemName').value=i.name||''; $('inventoryItemSerial').value=v118ProductCode(i)||'';
    if($('inventoryItemBarcode')) $('inventoryItemBarcode').value=v118Barcode(i)||'';
    if($('inventoryItemSupplier')) $('inventoryItemSupplier').value=i.supplier||'';
    if($('inventoryItemCategory')) $('inventoryItemCategory').value=i.category||'أخرى';
    if($('inventoryItemType')) $('inventoryItemType').value=v118ItemType(i)||'مادة';
    if($('inventoryItemUnit')) $('inventoryItemUnit').value=i.unit||'حبة';
    if($('inventoryItemQty')) $('inventoryItemQty').value=num(i.quantity);
    if($('inventoryItemMin')) $('inventoryItemMin').value=num(i.min_quantity);
    const mode=vatModeOf(i); if($('inventoryItemVatMode')) $('inventoryItemVatMode').value=mode;
    if($('inventoryItemCost')) $('inventoryItemCost').value=(mode==='inclusive'?unitGross(i):num(i.unit_cost)).toFixed(2);
    if($('inventoryItemNotes')) $('inventoryItemNotes').value=i.notes||'';
    inventoryItemImageData=i.image_url||'';
    if($('inventoryItemImagePreview')) $('inventoryItemImagePreview').innerHTML=i.image_url?`<img src="${esc(i.image_url)}" style="max-width:110px;max-height:110px;object-fit:contain;border-radius:12px;border:1px solid #dce6e2;background:#fff">`:'';
    inventoryCalcItemVatPreview(); $('inventoryItemName')?.scrollIntoView({behavior:'smooth',block:'center'});
  };
  function v121UsageRows(){
    const rows=[];
    (data.inventoryRequests||[]).filter(r=>r.status==='approved').forEach(r=>{
      const date=String(r.request_date||r.created_at||today()).slice(0,10);
      v118LineItems(r).forEach(l=>{
        const it=v118ItemById(l.item_id)||{};
        const out=num(l.quantity);
        const returned=movementReturnsForRequest(r.id,l.item_id).reduce((a,m)=>a+num(m.quantity),0);
        const consumed=Math.max(0,out-returned);
        const cost=num(l.unit_cost||it.unit_cost);
        const val=consumed*cost;
        rows.push({date,project:r.project_name||financeProjectName(r.project_id),person:r.supervisor_name||supervisorName(r.supervisor_id),item:l.item_name||it.name,item_id:l.item_id,product_code:l.product_code||v118ProductCode(it),barcode:l.barcode||v118Barcode(it),supplier:it.supplier||'',out,returned,qty:consumed,consumed,val,vat:vatOfAmount(val,it),gross:grossOfAmount(val,it),reason:r.reason||r.notes||'-',type:'أمر صرف معتمد',ref:'REQ-'+r.id,request_id:r.id,unit_cost:cost,itemObj:it,current:num(it.quantity)});
      });
    });
    (data.inventoryMovements||[]).filter(m=>['out','consume'].includes(m.movement_type)&&!m.request_id).forEach(m=>{
      const it=v118ItemById(m.item_id)||{}; const date=String(m.movement_date||m.created_at||today()).slice(0,10); const q=num(m.quantity); const cost=num(m.unit_cost||it.unit_cost); const val=q*cost;
      rows.push({date,project:m.project_name||financeProjectName(m.project_id)||'بدون مشروع',person:m.receiver||'بدون مستلم',item:m.item_name||it.name,item_id:m.item_id,product_code:m.product_code||v118ProductCode(it),barcode:m.barcode||v118Barcode(it),supplier:it.supplier||'',out:q,returned:0,qty:q,consumed:q,val,vat:vatOfAmount(val,it),gross:grossOfAmount(val,it),reason:m.reason||m.notes||'-',type:m.movement_type==='consume'?'استهلاك مباشر':'صرف مباشر من المخزون',ref:'MOV-'+m.id,unit_cost:cost,itemObj:it,current:num(it.quantity)});
    });
    return rows.filter(r=>reportDatePass(r.date));
  }
  window.v118AllUsageRows=v121UsageRows;
  function filteredUsageV121(){
    let rows=v121UsageRows();
    const prod=$('inventoryReportProduct')?.value||'';
    const person=$('inventoryReportPerson')?.value||'';
    const sup=$('inventoryReportSupplier')?.value||'';
    if(prod) rows=rows.filter(v=>String(v.item_id)===String(prod));
    if(person) rows=rows.filter(v=>String(v.person)===String(person));
    if(sup) rows=rows.filter(v=>String(v.supplier||'')===String(sup));
    return rows;
  }
  function setHead(id,heads){ const tbody=$(id); const tr=tbody?.closest('table')?.querySelector('thead tr'); if(tr) tr.innerHTML=heads.map(h=>`<th>${esc(h)}</th>`).join(''); }
  function setV121Heads(){
    setHead('stockOutByProjectBody',['المشروع','كود المنتج','اسم المنتج','سعر الحبة','الصادر','المرتجع','المستهلك','المتبقي','قيمة الاستهلاك','ضريبة 15%','الإجمالي']);
    setHead('stockOutBySupervisorBody',['المشرف / المستلم','المشروع','كود المنتج','اسم المنتج','سعر الحبة','الصادر','المرتجع','المستهلك','المتبقي','قيمة الاستهلاك','الإجمالي']);
    setHead('inventoryUsageDetailBody',['التاريخ','المشروع','المشرف / المستلم','كود المنتج','اسم المنتج','المورد','سعر الحبة','الصادر','المرتجع','المستهلك','المتبقي','القيمة','ضريبة 15%','الإجمالي','سبب الاستخدام','نوع العملية','منذ كم يوم','المرجع']);
    setHead('stockReportBody',['كود المنتج','اسم المنتج','المورد','سعر الحبة','سعر الحبة شامل الضريبة','إجمالي المخزون','حد إعادة الطلب','قيمة المخزون','ضريبة 15%','الإجمالي']);
  }
  function totals(sub,label='Total Value with 15% VAT', item=null){
    const vat= item ? vatOfAmount(sub,item) : num(sub)*0.15; const gross=num(sub)+vat;
    return `<div class="totals-v120"><p><b>Total amount before 15% VAT:</b> ${moneyEn(sub)}</p><p><b>Total VAT 15%:</b> ${moneyEn(vat)}</p><p><b>${esc(label)}:</b> ${moneyEn(gross)}</p></div><div class="stamp-near-total"><img src="${STAMP_URL}" alt="ختم تصنيف"></div>`;
  }
  function stockRows(items){ return items.map(i=>{ const cost=num(i.unit_cost), qty=num(i.quantity), val=qty*cost; return `<tr><td>${esc(v118ProductCode(i))}</td><td><b>${esc(i.name||'-')}</b><br><small>${esc(v118Barcode(i)||'')}</small></td><td>${esc(i.supplier||'-')}</td><td>${moneyEn(cost)}</td><td>${moneyEn(unitGross(i))}</td><td>${qtyEn(qty)}</td><td>${qtyEn(i.min_quantity)}</td><td>${moneyEn(val)}</td><td>${moneyEn(vatOfAmount(val,i))}</td><td>${moneyEn(grossOfAmount(val,i))}</td></tr>`; }).join('')||'<tr><td colspan="10">لا توجد بيانات</td></tr>'; }
  window.financeRenderReports=function(){
    if(typeof v118FillReportFilters==='function') v118FillReportFilters(); setV121Heads();
    const ep=$('expenseByProjectBody'); if(ep){ const map={}; (financeFilterRows(data.financeExpenses||[],'expense_date')).filter(e=>reportDatePass(e.expense_date)).forEach(e=>{ const k=e.project_name||financeProjectName(e.project_id)||'بدون مشروع'; map[k]=map[k]||{total:0,count:0}; map[k].total+=num(e.total); map[k].count++; }); ep.innerHTML=Object.entries(map).sort((a,b)=>b[1].total-a[1].total).map(([k,v])=>`<tr><td>${esc(k)}</td><td>${moneyEn(v.total)}</td><td>${qtyEn(v.count)}</td></tr>`).join('')||'<tr><td colspan="3">لا توجد بيانات</td></tr>'; }
    const usage=filteredUsageV121();
    const sp=$('stockOutByProjectBody'); if(sp) sp.innerHTML=usage.sort((a,b)=>String(a.project).localeCompare(String(b.project),'ar')).map(v=>`<tr><td>${esc(v.project||'-')}</td><td>${esc(v.product_code||'-')}</td><td><b>${esc(v.item||'-')}</b></td><td>${moneyEn(v.unit_cost)}</td><td>${qtyEn(v.out)}</td><td>${qtyEn(v.returned)}</td><td>${qtyEn(v.consumed)}</td><td>${qtyEn(v.current)}</td><td>${moneyEn(v.val)}</td><td>${moneyEn(v.vat)}</td><td>${moneyEn(v.gross)}</td></tr>`).join('')||'<tr><td colspan="11">لا توجد بيانات</td></tr>';
    const sr=$('stockOutBySupervisorBody'); if(sr) sr.innerHTML=usage.sort((a,b)=>String(a.person).localeCompare(String(b.person),'ar')).map(v=>`<tr><td>${esc(v.person||'-')}</td><td>${esc(v.project||'-')}</td><td>${esc(v.product_code||'-')}</td><td><b>${esc(v.item||'-')}</b></td><td>${moneyEn(v.unit_cost)}</td><td>${qtyEn(v.out)}</td><td>${qtyEn(v.returned)}</td><td>${qtyEn(v.consumed)}</td><td>${qtyEn(v.current)}</td><td>${moneyEn(v.val)}</td><td>${moneyEn(v.gross)}</td></tr>`).join('')||'<tr><td colspan="11">لا توجد بيانات</td></tr>';
    const ud=$('inventoryUsageDetailBody'); if(ud) ud.innerHTML=usage.sort((a,b)=>String(b.date).localeCompare(String(a.date))).map(v=>`<tr><td>${esc(v.date||'')}</td><td>${esc(v.project||'-')}</td><td>${esc(v.person||'-')}</td><td>${esc(v.product_code||'-')}</td><td><b>${esc(v.item||'-')}</b></td><td>${esc(v.supplier||'-')}</td><td>${moneyEn(v.unit_cost)}</td><td>${qtyEn(v.out)}</td><td>${qtyEn(v.returned)}</td><td>${qtyEn(v.consumed)}</td><td>${qtyEn(v.current)}</td><td>${moneyEn(v.val)}</td><td>${moneyEn(v.vat)}</td><td>${moneyEn(v.gross)}</td><td>${esc(v.reason||'-')}</td><td>${esc(v.type||'-')}</td><td>${esc(daysAgoText(v.date))}</td><td>${esc(v.ref||'-')}</td></tr>`).join('')||'<tr><td colspan="18">لا توجد بيانات استهلاك</td></tr>';
    const stock=$('stockReportBody'); if(stock){ let items=[...(data.inventoryItems||[])]; const sup=$('inventoryReportSupplier')?.value||''; if(sup) items=items.filter(i=>String(i.supplier||'')===String(sup)); stock.innerHTML=stockRows(items); const sub=items.reduce((a,i)=>a+num(i.quantity)*num(i.unit_cost),0); if($('stockReportTotals')) $('stockReportTotals').innerHTML=totals(sub); }
    v118RenderProductDetailReport();
  };
  window.v118RenderProductDetailReport=function(){
    const box=$('inventoryProductDetailBox'); if(!box) return; const id=$('inventoryReportProduct')?.value; const sup=$('inventoryReportSupplier')?.value||'';
    if(!id && sup){ const rows=(data.inventoryItems||[]).filter(i=>String(i.supplier||'')===String(sup)); const sub=rows.reduce((a,i)=>a+num(i.quantity)*num(i.unit_cost),0); box.innerHTML=`<h3>منتجات المورد: ${esc(sup)}</h3><div class="table-wrap"><table><thead><tr><th>كود المنتج</th><th>اسم المنتج</th><th>باركود المورد</th><th>سعر الحبة</th><th>شامل الضريبة</th><th>المتبقي</th><th>قيمة المتبقي</th></tr></thead><tbody>${rows.map(i=>`<tr><td>${esc(v118ProductCode(i))}</td><td><b>${esc(i.name)}</b></td><td>${esc(v118Barcode(i)||'-')}</td><td>${moneyEn(i.unit_cost)}</td><td>${moneyEn(unitGross(i))}</td><td>${qtyEn(i.quantity)}</td><td>${moneyEn(num(i.quantity)*num(i.unit_cost))}</td></tr>`).join('')||'<tr><td colspan="7">لا توجد منتجات لهذا المورد</td></tr>'}</tbody></table></div>${totals(sub)}`; return; }
    if(!id){ box.innerHTML='<p class="muted">اختر منتجًا لعرض تفاصيله وحركته، أو اختر موردًا لعرض منتجاته.</p>'; return; }
    const item=v118ItemById(id); if(!item){ box.innerHTML='<p class="muted">المنتج غير موجود.</p>'; return; }
    const rows=filteredUsageV121().filter(r=>String(r.item_id)===String(id)); const consumedQty=rows.reduce((a,r)=>a+num(r.consumed),0); const consumedVal=rows.reduce((a,r)=>a+num(r.val),0);
    const img=item.image_url?`<img src="${esc(item.image_url)}" style="width:115px;height:115px;object-fit:contain;border:1px solid #dce6e2;border-radius:14px;background:#fff">`:'';
    const useRows=rows.map(r=>`<tr><td>${esc(r.date)}</td><td>${esc(r.person)}</td><td>${esc(r.project)}</td><td>${qtyEn(r.out)}</td><td>${qtyEn(r.returned)}</td><td>${qtyEn(r.consumed)}</td><td>${qtyEn(item.quantity)}</td><td>${moneyEn(r.unit_cost)}</td><td>${moneyEn(r.val)}</td><td>${moneyEn(r.vat)}</td><td>${moneyEn(r.gross)}</td></tr>`).join('')||'<tr><td colspan="11">لا يوجد استهلاك</td></tr>';
    box.innerHTML=`<div class="card soft-card"><div style="display:flex;gap:16px;align-items:center;justify-content:space-between"><div><h3>${esc(item.name)}</h3><p>كود المنتج: <b>${esc(v118ProductCode(item))}</b> | باركود المورد: <b>${esc(v118Barcode(item)||'-')}</b></p><p>المورد: ${esc(item.supplier||'-')} | التصنيف: ${esc(item.category||'-')} | النوع: ${esc(v118ItemType(item)||'-')} | الضريبة: ${vatModeOf(item)==='exempt'?'خالي الضريبة':vatModeOf(item)==='inclusive'?'السعر شامل الضريبة':'غير شامل الضريبة'}</p></div>${img}</div></div><h3>الاستهلاك والمرتجعات</h3><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>Employee</th><th>Project</th><th>out</th><th>Return</th><th>Consumed</th><th>Current Quantity</th><th>Unit Price</th><th>Consumption Value</th><th>VAT 15%</th><th>Total with VAT</th></tr></thead><tbody>${useRows}</tbody></table></div>${totals(consumedVal,'Total Consumption Value with 15% VAT',item)}`;
  };
  window.inventoryReturnRequest=async function(id,btn){
    try{
      if(btn) btn.disabled=true; const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) throw new Error('الطلب غير موجود');
      const lines=v118LineItems(r); let any=false;
      for(const l of lines){ const it=v118ItemById(l.item_id); if(!it) continue; const already=movementReturnsForRequest(r.id,l.item_id).reduce((a,m)=>a+num(m.quantity),0); const max=Math.max(0,num(l.quantity)-already); if(max<=0) continue; const ans=prompt(`مرتجع ${l.item_name}\nالمصروف: ${l.quantity}\nالمرتجع سابقاً: ${already}\nالمتاح للإرجاع: ${max}\nاكتب كمية المرتجع أو اتركها فارغة لتجاوز الصنف`, ''); if(ans===null || String(ans).trim()==='') continue; const q=num(ans); if(q<=0) continue; if(q>max) throw new Error('كمية المرتجع أكبر من المصروف للصنف: '+l.item_name);
        const row={item_id:Number(l.item_id),item_name:l.item_name,movement_type:'return',quantity:q,movement_date:today(),project_id:r.project_id,project_name:r.project_name,receiver:r.supervisor_name||supervisorName(r.supervisor_id),reason:'مرتجع من أمر صرف REQ-'+r.id,notes:r.reason||'',request_id:Number(r.id),product_code:l.product_code||v118ProductCode(it),barcode:l.barcode||v118Barcode(it),unit_cost:num(it.unit_cost)};
        const ins=await sb.from('inventory_movements').insert(row); if(ins.error) throw ins.error; const upd=await sb.from('inventory_items').update({quantity:num(it.quantity)+q}).eq('id',l.item_id); if(upd.error) throw upd.error; any=true;
      }
      if(!any) msg('لم يتم تسجيل أي مرتجع'); else { msg('تم تسجيل المرتجع ورفعه للمخزون'); await financeLoadAll(); }
    }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };
  window.inventoryRenderRequests=function(){
    const b=$('inventoryRequestsBody'); if(!b) return; inventoryLoadApprovalSettings();
    let rows=financeFilterRows(data.inventoryRequests||[],'request_date'); const st=$('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
    b.innerHTML=rows.map(r=>{ const lines=v118LineItems(r); const itemsHtml=lines.map(l=>`${esc(l.product_code||'')} ${esc(l.item_name)} × ${qtyEn(l.quantity)}`).join('<br>')||esc(r.item_name||''); const totalQty=lines.reduce((a,l)=>a+num(l.quantity),0)||num(r.quantity); const actions=[];
      if(String(r.status||'').startsWith('pending_')){ const step=String(r.status).replace('pending_',''); if(inventoryCurrentUserCanApprove(step)){ actions.push(`<button onclick="inventoryApproveRequest('${r.id}','${step}',this)">اعتماد ${esc(INVENTORY_APPROVAL_ROLES[step]||step)}</button>`); actions.push(`<button class="danger" onclick="inventoryRejectRequest('${r.id}',this)">رفض</button>`); } else actions.push(`<span class="badge neutral">بانتظار ${esc(INVENTORY_APPROVAL_ROLES[step]||'المرحلة التالية')}</span>`); }
      actions.unshift(`<button class="light" onclick="inventoryPrintRequest('${r.id}')">طباعة</button>`); if(r.status==='approved') actions.push(`<button class="light" onclick="inventoryReturnRequest('${r.id}',this)">مرتجع</button>`);
      return `<tr><td>${esc(r.request_date||'')}</td><td><b>${esc(r.supervisor_name||supervisorName(r.supervisor_id))}</b></td><td>${esc(r.project_name||financeProjectName(r.project_id))}</td><td>${itemsHtml}</td><td>${qtyEn(totalQty)}</td><td>${esc(r.reason||'-')}</td><td><span class="badge ${inventoryRequestStatusClass(r.status)}">${inventoryRequestStatusText(r.status)}</span><br><small>المرحلة: ${inventoryRequestNextRole(r.status)}</small>${inventoryRequestStepHtml(r)}</td><td class="row-actions">${actions.join(' ')||'-'}</td></tr>`; }).join('')||`<tr><td colspan="8">${data.financeError||'لا توجد طلبات صرف'}</td></tr>`;
  };
  window.financePrintWindow=function(title, bodyHtml){
    const css=`@page{size:A4;margin:10mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;direction:rtl;color:#111;margin:0;background:#fff}.page{padding:10px;min-height:100vh}.logo-wrap{text-align:center;margin:0 0 18px}.logo-wrap img{width:250px;max-width:70%;height:auto;object-fit:contain}.meta-head{display:flex;justify-content:space-between;gap:12px;margin-bottom:16px;align-items:flex-start}.meta-head h1{margin:0;font-size:22px;color:#111}.muted{color:#555;font-size:12px}.box{border:1px solid #bbb;border-radius:8px;padding:9px;margin:8px 0}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}h2,h3{color:#111;margin:18px 0 8px}table{width:100%;border-collapse:collapse;margin-top:12px;font-size:10.5px;direction:rtl}th{background:#d0d0d0;color:#111;border:1px solid #333;padding:7px;font-weight:800}td{border:1px solid #333;padding:6px;text-align:center;vertical-align:middle}tr:nth-child(even) td{background:#fafafa}.totals-v120{margin-top:18px;font-size:15px;line-height:1.9;direction:ltr;text-align:left}.totals-v120 p{margin:6px 0}.stamp-near-total{text-align:left;margin:6px 0 12px}.stamp-near-total img,.stamp-print{width:115px;height:115px;object-fit:contain;opacity:.95}.sig{height:60px;border-bottom:1px dashed #777;margin-top:18px}.footer{text-align:center;margin-top:20px;color:#666;font-size:11px}.no-print{display:none}@media print{body{print-color-adjust:exact;-webkit-print-color-adjust:exact}.page{padding:0}tr{break-inside:avoid}}`;
    const w=window.open('','_blank'); if(!w){ msg('المتصفح منع نافذة الطباعة','err'); return; }
    w.document.write(`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${esc(title)}</title><style>${css}</style></head><body><div class="page"><div class="logo-wrap"><img src="tasneef_logo_print.png" alt="Tasneef Logo"></div><div class="meta-head"><div><h1>${esc(title)}</h1><div class="muted">نظام المصروفات والمخزون</div></div><div class="muted">تاريخ الطباعة: ${new Date().toLocaleString('en-US')}</div></div>${bodyHtml}<div class="footer">تم إنشاء هذا المستند من نظام تصنيف</div></div><script>window.onload=function(){setTimeout(function(){window.print()},500)}<\/script></body></html>`);
    w.document.close();
  };
  window.financePrintReport=function(kind){
    const titles={expensesByProject:'تقرير مصروفات حسب المشروع',stockOutByProject:'تقرير صرف المخزون حسب المشروع',stockOutBySupervisor:'تقرير استهلاك المشرفين حسب المشروع',inventoryUsageDetail:'تقرير تفصيلي للاستهلاك',productDetail:'تقرير تفاصيل منتج',stockReport:'تقرير المخزون العام'};
    if(kind==='productDetail'){ financePrintWindow(titles[kind], $('inventoryProductDetailBox')?.innerHTML||'<p>لا توجد بيانات</p>'); return; }
    if(kind==='stockReport'){ const table=$('stockReportBody')?.closest('table'); financePrintWindow(titles[kind], `<table>${table?.innerHTML||''}</table>${$('stockReportTotals')?.innerHTML||''}`); return; }
    const bodyIds={expensesByProject:'expenseByProjectBody',stockOutByProject:'stockOutByProjectBody',stockOutBySupervisor:'stockOutBySupervisorBody',inventoryUsageDetail:'inventoryUsageDetailBody'}; const table=$(bodyIds[kind])?.closest('table'); const total=filteredUsageV121().reduce((a,r)=>a+num(r.val),0); const totalHtml=(kind!=='expensesByProject')?totals(total,'Total Consumption Value with 15% VAT'):''; financePrintWindow(titles[kind]||'تقرير', table?`<table>${table.innerHTML}</table>${totalHtml}`:'<p>لا توجد بيانات</p>');
  };
  window.inventoryPrintRequest=function(id){
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r){msg('الطلب غير موجود','err');return;}
    const log=Array.isArray(r.approval_log)?r.approval_log:[]; const lines=v118LineItems(r).map(l=>{ const it=v118ItemById(l.item_id)||{}; const cost=num(l.unit_cost||it.unit_cost); const qty=num(l.quantity); const returned=movementReturnsForRequest(r.id,l.item_id).reduce((a,m)=>a+num(m.quantity),0); const consumed=Math.max(0,qty-returned); return {...l,it,cost,qty,returned,consumed,val:consumed*cost}; });
    const lineRows=lines.map(l=>`<tr><td>${esc(l.product_code||v118ProductCode(l.it)||'')}</td><td>${esc(l.item_name||l.it.name||'')}</td><td>${esc(l.it.supplier||'-')}</td><td>${qtyEn(l.qty)}</td><td>${qtyEn(l.returned)}</td><td>${qtyEn(l.consumed)}</td><td>${moneyEn(l.cost)}</td><td>${moneyEn(grossOfAmount(l.cost,l.it))}</td><td>${moneyEn(l.val)}</td></tr>`).join('')||'<tr><td colspan="9">لا توجد أصناف</td></tr>';
    const retRows=lines.filter(l=>l.returned>0).map(l=>`<tr><td>${esc(l.product_code||'')}</td><td>${esc(l.item_name||'')}</td><td>${qtyEn(l.returned)}</td><td>${moneyEn(l.cost)}</td><td>${moneyEn(l.returned*l.cost)}</td></tr>`).join('')||'<tr><td colspan="5">لا توجد مرتجعات مسجلة على هذا الأمر</td></tr>';
    const logRows=log.map(l=>`<tr><td>${esc(l.role||l.step)}</td><td>${esc(l.by||'-')}</td><td>${esc(l.at?new Date(l.at).toLocaleString('en-US'):'-')}</td></tr>`).join('')||'<tr><td colspan="3">لا يوجد سجل اعتماد</td></tr>';
    const total=lines.reduce((a,l)=>a+num(l.val),0); const retTotal=lines.reduce((a,l)=>a+num(l.returned)*num(l.cost),0);
    const body=`<div class="grid"><div class="box"><b>رقم الطلب</b><br>OUT-${esc(r.id)}</div><div class="box"><b>الحالة</b><br>${esc(inventoryRequestStatusText(r.status))}</div><div class="box"><b>التاريخ</b><br>${esc(r.request_date||'')}</div></div><table><tr><th>المشرف / الفني</th><td>${esc(r.supervisor_name||supervisorName(r.supervisor_id))}</td><th>المشروع</th><td>${esc(r.project_name||financeProjectName(r.project_id))}</td></tr><tr><th>سبب الصرف</th><td colspan="3">${esc(r.reason||'-')}</td></tr><tr><th>ملاحظات</th><td colspan="3">${esc(r.notes||'-')}</td></tr></table><h3>الأصناف المصروفة</h3><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>المورد</th><th>الصادر</th><th>مرتجع</th><th>المستهلك</th><th>سعر الحبة</th><th>سعر الحبة شامل الضريبة</th><th>قيمة الاستهلاك</th></tr></thead><tbody>${lineRows}</tbody></table>${totals(total,'Total Consumption Value with 15% VAT')}<h3>مرتجع</h3><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>كمية المرتجع</th><th>سعر الحبة</th><th>قيمة المرتجع</th></tr></thead><tbody>${retRows}</tbody></table>${totals(retTotal,'Total Return Value with 15% VAT')}<h3>سجل الاعتماد</h3><table><thead><tr><th>المرحلة</th><th>المعتمد</th><th>الوقت</th></tr></thead><tbody>${logRows}</tbody></table><div class="grid"><div><b>المستلم</b><div class="sig"></div></div><div><b>مدير المخازن</b><div class="sig"></div></div><div><b>الاعتماد النهائي</b><div class="sig"></div></div></div>`;
    financePrintWindow('أمر صرف مخزون متعدد الأصناف', body);
  };
  window.addEventListener('load',()=>setTimeout(()=>{ if($('financeDashboard')){ financeRenderReports(); inventoryCalcItemVatPreview(); } },1200));
})();

/* =========================
   V125: Auto approval, returns in same order, cost centers
   ========================= */
(function(){
  'use strict';
  if(!window.data) window.data = data || {};
  data.costCenters = data.costCenters || [];

  function ccEsc(v){ return typeof esc==='function'?esc(v):String(v??''); }
  function ccNum(v){ return typeof num==='function'?num(v):Number(String(v??0).replace(/,/g,''))||0; }
  function ccMoney(v){ return typeof moneyEn==='function'?moneyEn(v):(ccNum(v).toFixed(2)+' ر.س'); }
  function ccQty(v){ return typeof qtyEn==='function'?qtyEn(v):String(ccNum(v)); }
  function costCenterPseudoRows(){
    const rows = (data.costCenters||[]).filter(c=>String(c.status||'active')!=='inactive');
    if(rows.length) return rows;
    return (data.projects||[]).map(p=>({id:'project-'+p.id, code:'PRJ-'+p.id, name:p.name, type:'مشروع', status:'active', project_id:p.id, pseudo:true}));
  }
  window.costCenterName = function(id, fallbackProjectId){
    if(!id && fallbackProjectId) return projectName(fallbackProjectId);
    const c = costCenterPseudoRows().find(x=>String(x.id)===String(id));
    return c ? c.name : (fallbackProjectId?projectName(fallbackProjectId):'');
  };
  function costCenterValueFromProject(pid){
    const rows=costCenterPseudoRows();
    const byProj=rows.find(c=>String(c.project_id||'')===String(pid));
    if(byProj) return String(byProj.id);
    const pname=projectName(pid);
    const byName=rows.find(c=>String(c.name||'')===String(pname));
    return byName?String(byName.id):'';
  }
  function selectedCostCenterPayload(selectId, projectId){
    let cid = $(selectId)?.value || '';
    if(!cid && projectId) cid = costCenterValueFromProject(projectId);
    const c = costCenterPseudoRows().find(x=>String(x.id)===String(cid));
    return { cost_center_id: c && !c.pseudo ? Number(c.id) : null, cost_center_name: c?.name || (projectId?projectName(projectId):'') };
  }
  window.costCenterFillSelects = function(){
    const rows=costCenterPseudoRows();
    const html='<option value="">اختر مركز التكلفة</option>'+rows.map(c=>`<option value="${ccEsc(c.id)}">${ccEsc(c.code||'')} - ${ccEsc(c.name||'')}</option>`).join('');
    ['financeExpenseCostCenter','inventoryMovementCostCenter','inventoryRequestCostCenter','supInventoryRequestCostCenter'].forEach(id=>{ const el=$(id); if(el){ const old=el.value; el.innerHTML=html; if(old) el.value=old; }});
  };
  window.costCenterRender = function(){
    const b=$('costCentersBody'); if(!b) return;
    const rows=data.costCenters||[];
    b.innerHTML=rows.map(c=>`<tr><td>${ccEsc(c.code||'-')}</td><td><b>${ccEsc(c.name||'')}</b></td><td>${ccEsc(c.type||'-')}</td><td><span class="badge ${String(c.status||'active')==='active'?'green':'neutral'}">${String(c.status||'active')==='active'?'نشط':'موقوف'}</span></td><td class="row-actions"><button onclick="costCenterEdit('${c.id}')">تعديل</button><button class="danger" onclick="costCenterDelete('${c.id}')">حذف</button></td></tr>`).join('')||'<tr><td colspan="5">لا توجد مراكز تكلفة بعد. يمكنك إنشاؤها من المشاريع.</td></tr>';
  };
  window.costCenterClearForm = function(){ ['costCenterId','costCenterCode','costCenterName','costCenterNotes'].forEach(id=>$(id)&&($(id).value='')); if($('costCenterType')) $('costCenterType').value='مشروع'; if($('costCenterStatus')) $('costCenterStatus').value='active'; };
  window.costCenterEdit = function(id){ const c=(data.costCenters||[]).find(x=>String(x.id)===String(id)); if(!c) return; $('costCenterId').value=c.id; $('costCenterCode').value=c.code||''; $('costCenterName').value=c.name||''; $('costCenterType').value=c.type||'مشروع'; $('costCenterStatus').value=c.status||'active'; $('costCenterNotes').value=c.notes||''; financeShowTab('costCenters', document.querySelectorAll('.finance-tab')[6]); $('costCenterId')?.scrollIntoView({behavior:'smooth'}); };
  window.costCenterSave = async function(btn){
    try{ if(btn) btn.disabled=true; const name=($('costCenterName')?.value||'').trim(); if(!name) throw new Error('اسم مركز التكلفة مطلوب');
      const row={code:($('costCenterCode')?.value||'').trim()||('CC-'+Date.now()), name, type:$('costCenterType')?.value||'عام', status:$('costCenterStatus')?.value||'active', notes:$('costCenterNotes')?.value||''};
      const id=$('costCenterId')?.value; const res=id?await sb.from('cost_centers').update(row).eq('id',id):await sb.from('cost_centers').insert(row); if(res.error) throw res.error; msg('تم حفظ مركز التكلفة'); costCenterClearForm(); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };
  window.costCenterDelete = async function(id){ if(!confirm('تأكيد حذف مركز التكلفة؟')) return; try{ const {error}=await sb.from('cost_centers').delete().eq('id',id); if(error) throw error; msg('تم حذف مركز التكلفة'); await financeLoadAll(); }catch(e){ msg(e.message||String(e),'err'); } };
  window.costCenterCreateFromProjects = async function(){
    try{
      const existing=new Set((data.costCenters||[]).map(c=>String(c.name||'')));
      const rows=(data.projects||[]).filter(p=>!existing.has(String(p.name||''))).map(p=>({code:'PRJ-'+p.id,name:p.name,type:'مشروع',status:'active',project_id:p.id}));
      if(!rows.length){ msg('كل المشاريع لديها مراكز تكلفة أو لا توجد مشاريع جديدة'); return; }
      const {error}=await sb.from('cost_centers').insert(rows); if(error) throw error; msg('تم إنشاء مراكز تكلفة من المشاريع'); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); }
  };

  const oldFinanceLoadAllV125 = window.financeLoadAll || (typeof financeLoadAll==='function'?financeLoadAll:null);
  if(oldFinanceLoadAllV125){
    window.financeLoadAll = async function(showMessage=false){
      await oldFinanceLoadAllV125.call(this, showMessage);
      try{ const cc=await sb.from('cost_centers').select('*').order('name',{ascending:true}); data.costCenters=cc.data||[]; if(cc.error) console.warn('cost_centers:', cc.error.message); }
      catch(e){ console.warn('cost centers unavailable', e); data.costCenters=[]; }
      costCenterFillSelects(); costCenterRender(); financeRenderAll();
    };
  }
  const oldFinanceHydrateFormsV125 = window.financeHydrateForms || (typeof financeHydrateForms==='function'?financeHydrateForms:null);
  if(oldFinanceHydrateFormsV125){
    window.financeHydrateForms = function(){ oldFinanceHydrateFormsV125.apply(this, arguments); costCenterFillSelects(); costCenterRender();
      const hooks=[['inventoryRequestProject','inventoryRequestCostCenter'],['inventoryMovementProject','inventoryMovementCostCenter'],['financeExpenseProject','financeExpenseCostCenter'],['supInventoryRequestProject','supInventoryRequestCostCenter']];
      hooks.forEach(([p,s])=>{ const pe=$(p); if(pe && !pe.dataset.ccHook){ pe.dataset.ccHook='1'; pe.addEventListener('change',()=>{ const val=costCenterValueFromProject(pe.value); if($(s) && val) $(s).value=val; }); } });
    };
  }

  // Override expense save to carry cost center
  const oldFinanceSaveExpenseV125 = window.financeSaveExpense || (typeof financeSaveExpense==='function'?financeSaveExpense:null);
  if(oldFinanceSaveExpenseV125){
    window.financeSaveExpense = async function(btn){
      try{ if(btn) btn.disabled=true; const pid=$('financeExpenseProject')?.value; if(!pid) throw new Error('اختر المشروع'); const cc=selectedCostCenterPayload('financeExpenseCostCenter', pid); if(!cc.cost_center_name) throw new Error('اختر مركز التكلفة');
        const sub=ccNum($('financeExpenseSubtotal')?.value), vat=ccNum($('financeExpenseVat')?.value); const row={project_id:Number(pid),project_name:projectName(pid),cost_center_id:cc.cost_center_id,cost_center_name:cc.cost_center_name,category:$('financeExpenseCategory')?.value||'مصروف عام',expense_date:$('financeExpenseDate')?.value||today(),subtotal:sub,vat,total:sub+vat,supplier:$('financeExpenseSupplier')?.value||'',payment_method:$('financeExpenseMethod')?.value||'تحويل',notes:$('financeExpenseNotes')?.value||''};
        const id=$('financeExpenseId')?.value; const res=id?await sb.from('finance_expenses').update(row).eq('id',id):await sb.from('finance_expenses').insert(row); if(res.error) throw res.error; msg('تم حفظ المصروف'); financeClearExpenseForm(); await financeLoadAll();
      }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
    };
  }

  function lineReturnQty(requestId, itemId){ return (data.inventoryMovements||[]).filter(m=>String(m.request_id)===String(requestId) && String(m.item_id)===String(itemId) && m.movement_type==='return').reduce((a,m)=>a+ccNum(m.quantity),0); }
  function requestLineStats(r){
    const lines=v118LineItems(r); let out=0, ret=0, cons=0, val=0, vat=0, gross=0;
    lines.forEach(l=>{ const it=v118ItemById(l.item_id)||{}; const o=ccNum(l.quantity), rr=lineReturnQty(r.id,l.item_id), c=Math.max(0,o-rr), cost=ccNum(l.unit_cost||it.unit_cost); out+=o; ret+=rr; cons+=c; const v=c*cost; val+=v; vat+=(typeof vatOfAmount==='function'?vatOfAmount(v,it):v*0.15); gross+=(typeof grossOfAmount==='function'?grossOfAmount(v,it):v*1.15); });
    return {out,ret,cons,val,vat,gross};
  }

  window.inventorySaveRequest = async function(btn){
    try{ if(btn) btn.disabled=true; const pid=$('inventoryRequestProject')?.value; const supervisorId=$('inventoryRequestSupervisor')?.value; if(!pid) throw new Error('اختر المشروع'); if(!supervisorId) throw new Error('اختر المشرف / الفني'); if(!inventoryRequestLines.length) throw new Error('أضف صنفًا واحدًا على الأقل إلى أمر الصرف'); const cc=selectedCostCenterPayload('inventoryRequestCostCenter', pid); if(!cc.cost_center_name) throw new Error('اختر مركز التكلفة');
      const path=inventoryGetApprovalPath(); const first=path[0]||'warehouse'; const totalQty=inventoryRequestLines.reduce((a,x)=>a+ccNum(x.quantity),0);
      const row={item_id:Number(inventoryRequestLines[0].item_id), item_name:inventoryRequestLines.map(x=>x.item_name).join('، '), quantity:totalQty, request_items:inventoryRequestLines, items:inventoryRequestLines, project_id:Number(pid), project_name:projectName(pid), cost_center_id:cc.cost_center_id, cost_center_name:cc.cost_center_name, supervisor_id:Number(supervisorId), supervisor_name:supervisorName(supervisorId), request_date:$('inventoryRequestDate')?.value||today(), reason:$('inventoryRequestReason')?.value||'', notes:$('inventoryRequestNotes')?.value||'', status:'pending_'+first, current_step:first, approval_path:path, approval_log:[]};
      const {error}=await sb.from('inventory_requests').insert(row); if(error) throw error; msg('تم إرسال أمر الصرف للاعتماد'); inventoryClearRequestForm(); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };

  window.inventorySaveMovement = async function(btn){
    try{ if(btn) btn.disabled=true; const itemId=$('inventoryMovementItem')?.value; if(!itemId) throw new Error('اختر الصنف'); const item=v118ItemById(itemId); const qty=ccNum($('inventoryMovementQty')?.value); if(qty<=0) throw new Error('الكمية مطلوبة'); const type=$('inventoryMovementType')?.value||'out'; const pid=$('inventoryMovementProject')?.value; const cc=selectedCostCenterPayload('inventoryMovementCostCenter', pid); if(!cc.cost_center_name) throw new Error('اختر مركز التكلفة'); if(['out','consume'].includes(type) && ccNum(item.quantity)<qty) throw new Error('الكمية المتوفرة لا تكفي');
      const row={item_id:Number(itemId),item_name:item?.name||'',movement_type:type,quantity:qty,movement_date:$('inventoryMovementDate')?.value||today(),project_id:pid?Number(pid):null,project_name:pid?projectName(pid):'',cost_center_id:cc.cost_center_id,cost_center_name:cc.cost_center_name,receiver:$('inventoryMovementReceiver')?.value||'',reason:$('inventoryMovementReason')?.value||'',notes:$('inventoryMovementNotes')?.value||'',product_code:v118ProductCode(item),barcode:v118Barcode(item),unit_cost:ccNum(item.unit_cost)};
      const res=await sb.from('inventory_movements').insert(row).select('*').single(); if(res.error) throw res.error; let newQty=ccNum(item.quantity); if(type==='in'||type==='return') newQty+=qty; else if(type==='out'||type==='consume') newQty-=qty; else if(type==='adjust') newQty=qty; const upd=await sb.from('inventory_items').update({quantity:newQty}).eq('id',itemId); if(upd.error) throw upd.error; msg('تم حفظ حركة المخزون'); inventoryClearMovementForm(); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };

  async function finalizeRequestDirect(r, autoLabel){
    const lines=v118LineItems(r); if(!lines.length) throw new Error('لا توجد أصناف في الطلب');
    const missing=[]; lines.forEach(l=>{ const it=v118ItemById(l.item_id); if(!it || ccNum(it.quantity)<ccNum(l.quantity)) missing.push(`${l.item_name}: المتوفر ${it?ccNum(it.quantity):0} والمطلوب ${ccNum(l.quantity)}`); });
    if(missing.length) throw new Error('لا يمكن الاعتماد التلقائي لوجود أصناف غير متوفرة: '+missing.join(' / '));
    const movementIds=[];
    for(const l of lines){ const it=v118ItemById(l.item_id); const movement={item_id:Number(l.item_id),item_name:l.item_name,movement_type:'out',quantity:ccNum(l.quantity),movement_date:today(),project_id:r.project_id,project_name:r.project_name,cost_center_id:r.cost_center_id||null,cost_center_name:r.cost_center_name||r.project_name||'',receiver:r.supervisor_name,reason:(autoLabel||'صرف بناءً على أمر معتمد رقم ')+r.id,notes:r.reason||'',request_id:Number(r.id),product_code:l.product_code||v118ProductCode(it),barcode:l.barcode||v118Barcode(it),unit_cost:ccNum(it.unit_cost)}; const mv=await sb.from('inventory_movements').insert(movement).select('*').single(); if(mv.error) throw mv.error; movementIds.push(mv.data?.id); const upd=await sb.from('inventory_items').update({quantity:ccNum(it.quantity)-ccNum(l.quantity)}).eq('id',l.item_id); if(upd.error) throw upd.error; it.quantity=ccNum(it.quantity)-ccNum(l.quantity); }
    const u=session()||{}; const now=new Date().toISOString(); const log=Array.isArray(r.approval_log)?r.approval_log:[]; log.push({step:'auto',role:'اعتماد تلقائي',by:u.full_name||u.username||'مستخدم',at:now});
    const res=await sb.from('inventory_requests').update({status:'approved',current_step:'done',approval_log:log,auto_approved_by:u.full_name||u.username||'',auto_approved_at:now,movement_id:movementIds[0]||null,movement_ids:movementIds}).eq('id',r.id); if(res.error) throw res.error;
  }
  window.inventoryAutoApproveRequest = async function(id, btn){
    if(!confirm('سيتم اعتماد الطلب وصرفه مباشرة إذا كانت الكميات متوفرة. هل تريد المتابعة؟')) return;
    try{ if(btn) btn.disabled=true; const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) throw new Error('الطلب غير موجود'); if(r.status==='approved') throw new Error('الطلب معتمد مسبقًا'); await finalizeRequestDirect(r,'اعتماد تلقائي وصرف مباشر رقم '); msg('تم الاعتماد التلقائي وصرف المخزون'); await financeLoadAll(); }
    catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };

  // Strengthen final approval to attach cost center to movements
  const oldApproveV125=window.inventoryApproveRequest;
  window.inventoryApproveRequest = async function(id,step,btn){
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id));
    const path=r?inventoryRequestPath(r):[]; const isLast=r && path.indexOf(step)===path.length-1;
    if(isLast){
      try{ if(btn) btn.disabled=true; if(r.status!==('pending_'+step)) throw new Error('هذا الطلب ليس في مرحلة '+(INVENTORY_APPROVAL_ROLES[step]||step)); if(!inventoryCurrentUserCanApprove(step)) throw new Error('ليس لديك صلاحية اعتماد هذه المرحلة');
        const u=session()||{}; const approver=u.full_name||u.username||INVENTORY_APPROVAL_ROLES[step]||'مدير'; const now=new Date().toISOString(); const log=Array.isArray(r.approval_log)?r.approval_log:[]; log.push({step,role:INVENTORY_APPROVAL_ROLES[step]||step,by:approver,at:now}); r.approval_log=log; await finalizeRequestDirect(r,'صرف بناءً على أمر معتمد رقم '); const row={}; row[step+'_approved_by']=approver; row[step+'_approved_at']=now; try{ await sb.from('inventory_requests').update(row).eq('id',id); }catch(e){} msg('تم اعتماد أمر الصرف نهائيًا وخصم الأصناف من المخزون'); await financeLoadAll(); }
      catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
    } else return oldApproveV125.apply(this, arguments);
  };

  window.inventoryReturnRequest = async function(id, btn){
    try{ if(btn) btn.disabled=true; const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) throw new Error('الطلب غير موجود'); const lines=v118LineItems(r); const returns=[]; let any=false;
      for(const l of lines){ const it=v118ItemById(l.item_id)||{}; const already=lineReturnQty(r.id,l.item_id); const max=Math.max(0,ccNum(l.quantity)-already); if(max<=0) continue; const ans=prompt(`مرتجع للصنف: ${l.item_name}\nالمصروف: ${ccNum(l.quantity)}\nالمرتجع السابق: ${already}\nالمتاح للإرجاع: ${max}\nاكتب كمية المرتجع أو اتركها فارغة لتجاوز الصنف`, ''); if(ans===null || String(ans).trim()==='') continue; const q=ccNum(ans); if(q<=0) continue; if(q>max) throw new Error('كمية المرتجع أكبر من المصروف للصنف: '+l.item_name); const row={item_id:Number(l.item_id),item_name:l.item_name,movement_type:'return',quantity:q,movement_date:today(),project_id:r.project_id,project_name:r.project_name,cost_center_id:r.cost_center_id||null,cost_center_name:r.cost_center_name||r.project_name||'',receiver:r.supervisor_name||supervisorName(r.supervisor_id),reason:'مرتجع من أمر صرف REQ-'+r.id,notes:r.reason||'',request_id:Number(r.id),product_code:l.product_code||v118ProductCode(it),barcode:l.barcode||v118Barcode(it),unit_cost:ccNum(it.unit_cost)}; const ins=await sb.from('inventory_movements').insert(row); if(ins.error) throw ins.error; const upd=await sb.from('inventory_items').update({quantity:ccNum(it.quantity)+q}).eq('id',l.item_id); if(upd.error) throw upd.error; returns.push({item_id:l.item_id,item_name:l.item_name,quantity:q,at:new Date().toISOString()}); any=true; }
      if(any){ try{ await sb.from('inventory_requests').update({return_items:returns, has_return:true, return_updated_at:new Date().toISOString()}).eq('id',id); }catch(e){} msg('تم تسجيل المرتجع داخل نفس أمر الصرف'); await financeLoadAll(); } else msg('لم يتم تسجيل أي مرتجع');
    }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };

  window.inventoryRenderRequests=function(){
    const b=$('inventoryRequestsBody'); if(!b) return; inventoryLoadApprovalSettings(); let rows=financeFilterRows(data.inventoryRequests||[],'request_date'); const st=$('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
    b.innerHTML=rows.map(r=>{ const lines=v118LineItems(r); const itemsHtml=lines.map(l=>{ const returned=lineReturnQty(r.id,l.item_id), consumed=Math.max(0,ccNum(l.quantity)-returned); return `${ccEsc(l.product_code||'')} ${ccEsc(l.item_name)}<br><small>صادر ${ccQty(l.quantity)} | مرتجع ${ccQty(returned)} | مستهلك ${ccQty(consumed)}</small>`; }).join('<hr style="border:none;border-top:1px solid #e6eeee;margin:4px 0">')||ccEsc(r.item_name||''); const stt=requestLineStats(r); const actions=[];
      if(String(r.status||'').startsWith('pending_')){ const step=String(r.status).replace('pending_',''); if(inventoryCurrentUserCanApprove(step)){ actions.push(`<button onclick="inventoryApproveRequest('${r.id}','${step}',this)">اعتماد ${ccEsc(INVENTORY_APPROVAL_ROLES[step]||step)}</button>`); actions.push(`<button class="light" onclick="inventoryAutoApproveRequest('${r.id}',this)">اعتماد تلقائي وصرف</button>`); actions.push(`<button class="danger" onclick="inventoryRejectRequest('${r.id}',this)">رفض</button>`); } else actions.push(`<span class="badge neutral">بانتظار ${ccEsc(INVENTORY_APPROVAL_ROLES[step]||'المرحلة التالية')}</span>`); }
      actions.unshift(`<button class="light" onclick="inventoryPrintRequest('${r.id}')">طباعة</button>`); if(r.status==='approved') actions.push(`<button class="light" onclick="inventoryReturnRequest('${r.id}',this)">مرتجع</button>`);
      return `<tr><td>${ccEsc(r.request_date||'')}</td><td><b>${ccEsc(r.supervisor_name||supervisorName(r.supervisor_id))}</b></td><td>${ccEsc(r.project_name||financeProjectName(r.project_id))}</td><td>${ccEsc(r.cost_center_name||costCenterName(r.cost_center_id,r.project_id)||'-')}</td><td>${itemsHtml}</td><td>صادر ${ccQty(stt.out)}<br>مرتجع ${ccQty(stt.ret)}<br><b>مستهلك ${ccQty(stt.cons)}</b></td><td>${ccEsc(r.reason||'-')}</td><td><span class="badge ${inventoryRequestStatusClass(r.status)}">${inventoryRequestStatusText(r.status)}</span><br><small>المرحلة: ${inventoryRequestNextRole(r.status)}</small>${inventoryRequestStepHtml(r)}</td><td class="row-actions">${actions.join(' ')||'-'}</td></tr>`; }).join('')||`<tr><td colspan="9">${data.financeError||'لا توجد طلبات صرف'}</td></tr>`;
  };

  // Cost center report built from filtered usage rows
  const oldFinanceRenderReportsV125 = window.financeRenderReports || (typeof financeRenderReports==='function'?financeRenderReports:null);
  window.financeRenderReports = function(){ if(oldFinanceRenderReportsV125) oldFinanceRenderReportsV125.apply(this, arguments); const b=$('costCenterReportBody'); if(!b) return; const rows=(typeof filteredUsageV121==='function'?filteredUsageV121():[]); const map={}; rows.forEach(r=>{ const key=r.cost_center_name || costCenterName(r.cost_center_id) || r.project || 'غير محدد'; map[key]=map[key]||{count:0,out:0,ret:0,cons:0,val:0,vat:0,gross:0}; map[key].count++; map[key].out+=ccNum(r.out); map[key].ret+=ccNum(r.returned); map[key].cons+=ccNum(r.consumed||r.qty); map[key].val+=ccNum(r.val); map[key].vat+=ccNum(r.vat); map[key].gross+=ccNum(r.gross); }); const vals=Object.entries(map).sort((a,b)=>b[1].gross-a[1].gross); b.innerHTML=vals.map(([k,v])=>`<tr><td>${ccEsc(k)}</td><td>${v.count}</td><td>${ccQty(v.out)}</td><td>${ccQty(v.ret)}</td><td>${ccQty(v.cons)}</td><td>${ccMoney(v.val)}</td><td>${ccMoney(v.vat)}</td><td>${ccMoney(v.gross)}</td></tr>`).join('')||'<tr><td colspan="8">لا توجد بيانات مراكز تكلفة</td></tr>'; };

  const oldFinancePrintReportV125 = window.financePrintReport;
  window.financePrintReport=function(kind){ if(kind==='costCenters'){ const table=$('costCenterReportBody')?.closest('table'); financePrintWindow('تقرير مراكز التكلفة', table?`<table>${table.innerHTML}</table>`:'<p>لا توجد بيانات</p>'); return; } return oldFinancePrintReportV125.apply(this, arguments); };

  // Supervisor cost center support
  const oldSupervisorInventoryLoadV125 = window.supervisorInventoryLoad || (typeof supervisorInventoryLoad==='function'?supervisorInventoryLoad:null);
  if(oldSupervisorInventoryLoadV125){ window.supervisorInventoryLoad=async function(){ await oldSupervisorInventoryLoadV125.apply(this, arguments); try{ const cc=await sb.from('cost_centers').select('*').order('name',{ascending:true}); data.costCenters=cc.data||[]; }catch(e){} costCenterFillSelects(); }; }
  const oldSupervisorSaveInventoryRequestV125 = window.supervisorSaveInventoryRequest || (typeof supervisorSaveInventoryRequest==='function'?supervisorSaveInventoryRequest:null);
  if(oldSupervisorSaveInventoryRequestV125){ window.supervisorSaveInventoryRequest=async function(btn){
    try{ if(btn) btn.disabled=true; const u=session()||{}; const pid=$('supInventoryRequestProject')?.value; if(!pid) throw new Error('اختر المشروع'); if(!supInventoryRequestLines.length) throw new Error('أضف صنف واحد على الأقل للطلب'); const cc=selectedCostCenterPayload('supInventoryRequestCostCenter', pid); if(!cc.cost_center_name) throw new Error('اختر مركز التكلفة'); const path=inventoryGetApprovalPath(); const first=path[0]||'warehouse'; const firstLine=supInventoryRequestLines[0]||{}; const row={item_id:Number(firstLine.item_id),item_name:firstLine.item_name||'',quantity:supInventoryRequestLines.reduce((a,l)=>a+ccNum(l.quantity),0),request_items:supInventoryRequestLines,items:supInventoryRequestLines,project_id:Number(pid),project_name:projectName(pid),cost_center_id:cc.cost_center_id,cost_center_name:cc.cost_center_name,supervisor_id:u.id,supervisor_name:u.full_name||u.username||'',request_date:$('supInventoryRequestDate')?.value||today(),reason:$('supInventoryRequestReason')?.value||'',notes:$('supInventoryRequestNotes')?.value||'',status:'pending_'+first,current_step:first,approval_path:path,approval_log:[]}; const {error}=await sb.from('inventory_requests').insert(row); if(error) throw error; msg('تم إرسال طلب الصرف للإدارة'); supInventoryRequestLines=[]; renderSupInventoryRequestLines(); ['supInventoryRequestItem','supInventoryRequestQty','supInventoryRequestReason','supInventoryRequestNotes'].forEach(id=>$(id)&&($(id).value='')); if($('supInventoryRequestProject')) $('supInventoryRequestProject').value=''; if($('supInventoryRequestCostCenter')) $('supInventoryRequestCostCenter').value=''; await supervisorInventoryLoad(); }
    catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  }; }

  window.addEventListener('load',()=>setTimeout(()=>{ costCenterFillSelects(); costCenterRender(); if($('financeDashboard')) financeRenderReports(); },1500));
})();

/* =========================
   V126: Smart report slides and fixed action buttons
   ========================= */
(function(){
  'use strict';
  window.financeShowSmartReport = function(kind, btn){
    document.querySelectorAll('.smart-report-card').forEach(x=>x.classList.remove('active'));
    if(btn) btn.classList.add('active');
    document.querySelectorAll('.smart-report-panel').forEach(p=>p.classList.remove('active'));
    const panel=document.querySelector(`[data-report-panel="${kind}"]`);
    if(panel){
      panel.classList.add('active');
      panel.scrollIntoView({behavior:'smooth',block:'start'});
    }
  };
  window.addEventListener('load',()=>setTimeout(()=>{
    if(document.querySelector('.smart-report-panel') && !document.querySelector('.smart-report-panel.active')){
      window.financeShowSmartReport('expensesByProject', document.querySelector('.smart-report-card[data-report="expensesByProject"]'));
    }
  },900));
})();


/* =========================================================
   V127: Edit inventory movements/requests + smart return screen
   ========================================================= */
(function(){
  const fNum = v => (typeof num === 'function') ? num(v) : (Number(String(v ?? 0).replace(/,/g,'').trim()) || 0);
  const fQty = v => (typeof safeQtyEn === 'function') ? safeQtyEn(v) : String(fNum(v));
  const fEsc = s => (typeof esc === 'function') ? esc(s) : String(s ?? '').replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  function getItem(id){ return typeof v118ItemById === 'function' ? v118ItemById(id) : (data.inventoryItems||[]).find(i=>String(i.id)===String(id)); }
  function getLines(r){ return typeof v118LineItems === 'function' ? v118LineItems(r) : (Array.isArray(r.request_items)?r.request_items:(Array.isArray(r.items)?r.items:[])); }
  function productCode(it){ return typeof v118ProductCode === 'function' ? v118ProductCode(it) : (it?.product_code || it?.serial_number || ''); }
  function barcode(it){ return typeof v118Barcode === 'function' ? v118Barcode(it) : (it?.barcode || it?.supplier_barcode || ''); }
  function ccPayload(selectId, projectId){ return typeof selectedCostCenterPayload === 'function' ? selectedCostCenterPayload(selectId, projectId) : {cost_center_id:null,cost_center_name:projectName(projectId)||''}; }
  function retQty(requestId,itemId){ return (data.inventoryMovements||[]).filter(m=>String(m.request_id)===String(requestId)&&String(m.item_id)===String(itemId)&&m.movement_type==='return').reduce((a,m)=>a+fNum(m.quantity),0); }
  function reqStats(r){ let out=0, ret=0, cons=0; getLines(r).forEach(l=>{ const o=fNum(l.quantity), rr=retQty(r.id,l.item_id); out+=o; ret+=rr; cons+=Math.max(0,o-rr); }); return {out,ret,cons}; }
  function stepRole(step){ return (typeof INVENTORY_APPROVAL_ROLES !== 'undefined' && INVENTORY_APPROVAL_ROLES[step]) ? INVENTORY_APPROVAL_ROLES[step] : step; }
  function effect(type, qty){ if(type==='in'||type==='return') return fNum(qty); if(type==='out'||type==='consume') return -fNum(qty); return 0; }
  function ensureRequestHidden(){
    let h=$('inventoryRequestId');
    if(!h){ const form=$('inventoryRequestDate')?.closest('.card'); if(form){ h=document.createElement('input'); h.type='hidden'; h.id='inventoryRequestId'; form.prepend(h); } }
    return h;
  }
  function setFirstActionButtonText(card, text){ const btn=card?.querySelector('.actions button:not(.light):not(.danger)'); if(btn) btn.textContent=text; }
  function addSmartReturnStyles(){
    if(document.getElementById('v127SmartReturnStyle')) return;
    const st=document.createElement('style'); st.id='v127SmartReturnStyle';
    st.textContent=`
      .smart-return-backdrop{position:fixed;inset:0;background:rgba(2,34,24,.55);backdrop-filter:blur(5px);z-index:9998;display:flex;align-items:center;justify-content:center;padding:18px}
      .smart-return-modal{width:min(920px,96vw);max-height:90vh;overflow:auto;background:#fff;border:1px solid #cfe4dc;border-radius:24px;box-shadow:0 24px 70px rgba(0,0,0,.25);direction:rtl}
      .smart-return-head{background:linear-gradient(135deg,#064f3a,#0b7a55);color:#fff;padding:20px 24px;border-radius:24px 24px 0 0;display:flex;justify-content:space-between;gap:12px;align-items:center}
      .smart-return-head h2{margin:0;color:#fff;font-size:24px}.smart-return-head p{margin:4px 0 0;font-size:13px;opacity:.9}
      .smart-return-body{padding:18px 22px}.smart-return-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:14px}
      .smart-return-summary div{background:#f2faf7;border:1px solid #d7eee6;border-radius:14px;padding:10px 12px}.smart-return-summary small{display:block;color:#55736a}.smart-return-summary b{color:#063f31}
      .smart-return-table input{width:100%;min-width:90px}.smart-return-table td,.smart-return-table th{text-align:center}.smart-return-table .name{text-align:right;font-weight:700}
      .smart-return-actions{display:flex;gap:10px;justify-content:flex-start;padding:0 22px 22px}.smart-return-actions button{min-width:130px}
      @media(max-width:700px){.smart-return-summary{grid-template-columns:1fr}.smart-return-modal{border-radius:18px}.smart-return-head{border-radius:18px 18px 0 0;align-items:flex-start;flex-direction:column}.smart-return-actions{flex-direction:column}.smart-return-actions button{width:100%}}
    `;
    document.head.appendChild(st);
  }

  // Edit movement: fill form
  window.inventoryEditMovement = function(id){
    const m=(data.inventoryMovements||[]).find(x=>String(x.id)===String(id)); if(!m) return msg('لم يتم العثور على الحركة','err');
    if($('inventoryMovementId')) $('inventoryMovementId').value=m.id;
    if($('inventoryMovementItem')) $('inventoryMovementItem').value=m.item_id||'';
    if($('inventoryMovementType')) $('inventoryMovementType').value=m.movement_type||'out';
    if($('inventoryMovementDate')) $('inventoryMovementDate').value=m.movement_date||String(m.created_at||today()).slice(0,10);
    if($('inventoryMovementQty')) $('inventoryMovementQty').value=fNum(m.quantity);
    if($('inventoryMovementProject')) $('inventoryMovementProject').value=m.project_id||'';
    if($('inventoryMovementCostCenter')) $('inventoryMovementCostCenter').value=m.cost_center_id||'';
    if($('inventoryMovementReceiver')) $('inventoryMovementReceiver').value=m.receiver||'';
    if($('inventoryMovementReason')) $('inventoryMovementReason').value=m.reason||'';
    if($('inventoryMovementNotes')) $('inventoryMovementNotes').value=m.notes||'';
    const card=$('inventoryMovementId')?.closest('.card'); if(card){ const title=card.querySelector('h2'); if(title) title.textContent='تعديل حركة مخزون'; setFirstActionButtonText(card,'تحديث الحركة'); card.scrollIntoView({behavior:'smooth',block:'start'}); }
  };

  const oldClearMovementV127 = window.inventoryClearMovementForm || (typeof inventoryClearMovementForm==='function'?inventoryClearMovementForm:null);
  window.inventoryClearMovementForm = function(){ if(oldClearMovementV127) oldClearMovementV127.apply(this, arguments); if($('inventoryMovementId')) $('inventoryMovementId').value=''; const card=$('inventoryMovementId')?.closest('.card'); if(card){ const title=card.querySelector('h2'); if(title) title.textContent='تسجيل حركة مخزون'; setFirstActionButtonText(card,'حفظ الحركة'); } };

  window.inventorySaveMovement = async function(btn){
    try{
      if(btn) btn.disabled=true;
      const itemId=$('inventoryMovementItem')?.value; if(!itemId) throw new Error('اختر الصنف');
      const item=getItem(itemId); if(!item) throw new Error('الصنف غير موجود');
      const qty=fNum($('inventoryMovementQty')?.value); if(qty<=0) throw new Error('الكمية مطلوبة');
      const type=$('inventoryMovementType')?.value||'out';
      if(type==='adjust') throw new Error('تعديل/حفظ حركة تعديل الرصيد غير متاح من هنا؛ استخدم إدخال أو صرف أو مرتجع للحفاظ على دقة الرصيد');
      const pid=$('inventoryMovementProject')?.value; const cc=ccPayload('inventoryMovementCostCenter', pid); if(!cc.cost_center_name) throw new Error('اختر مركز التكلفة');
      const row={item_id:Number(itemId),item_name:item?.name||'',movement_type:type,quantity:qty,movement_date:$('inventoryMovementDate')?.value||today(),project_id:pid?Number(pid):null,project_name:pid?projectName(pid):'',cost_center_id:cc.cost_center_id,cost_center_name:cc.cost_center_name,receiver:$('inventoryMovementReceiver')?.value||'',reason:$('inventoryMovementReason')?.value||'',notes:$('inventoryMovementNotes')?.value||'',product_code:productCode(item),barcode:barcode(item),unit_cost:fNum(item.unit_cost)};
      const id=$('inventoryMovementId')?.value;
      if(id){
        const old=(data.inventoryMovements||[]).find(x=>String(x.id)===String(id)); if(!old) throw new Error('الحركة القديمة غير موجودة');
        if(old.movement_type==='adjust') throw new Error('لا يمكن تعديل حركة تعديل الرصيد؛ احذفها وأضف حركة جديدة');
        const oldItem=getItem(old.item_id); if(!oldItem) throw new Error('الصنف القديم غير موجود');
        if(String(old.item_id)===String(itemId)){
          const base=fNum(item.quantity) - effect(old.movement_type, old.quantity);
          const next=base + effect(type, qty);
          if(next<0) throw new Error('الكمية المتوفرة لا تكفي بعد التعديل');
          const up1=await sb.from('inventory_movements').update(row).eq('id',id); if(up1.error) throw up1.error;
          const up2=await sb.from('inventory_items').update({quantity:next}).eq('id',itemId); if(up2.error) throw up2.error;
        } else {
          const oldNext=fNum(oldItem.quantity) - effect(old.movement_type, old.quantity);
          const newNext=fNum(item.quantity) + effect(type, qty);
          if(newNext<0) throw new Error('الكمية المتوفرة لا تكفي للصنف الجديد');
          const up1=await sb.from('inventory_movements').update(row).eq('id',id); if(up1.error) throw up1.error;
          const upOld=await sb.from('inventory_items').update({quantity:oldNext}).eq('id',old.item_id); if(upOld.error) throw upOld.error;
          const upNew=await sb.from('inventory_items').update({quantity:newNext}).eq('id',itemId); if(upNew.error) throw upNew.error;
        }
        msg('تم تحديث حركة المخزون');
      } else {
        const next=fNum(item.quantity)+effect(type,qty); if(next<0) throw new Error('الكمية المتوفرة لا تكفي');
        const res=await sb.from('inventory_movements').insert(row).select('*').single(); if(res.error) throw res.error;
        const upd=await sb.from('inventory_items').update({quantity:next}).eq('id',itemId); if(upd.error) throw upd.error;
        msg('تم حفظ حركة المخزون');
      }
      inventoryClearMovementForm(); await financeLoadAll();
    } catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };

  window.inventoryRenderMovements = function(){
    const b=$('inventoryMovementsBody'); if(!b) return; let rows=(typeof financeFilterRows==='function')?financeFilterRows(data.inventoryMovements||[],'movement_date'):(data.inventoryMovements||[]);
    b.innerHTML=rows.map(m=>{ const type=m.movement_type==='in'?'إدخال':(m.movement_type==='out'?'صرف':(m.movement_type==='return'?'مرتجع':(m.movement_type==='consume'?'استهلاك':'تعديل رصيد'))); const cls=m.movement_type==='out'?'amber':((m.movement_type==='in'||m.movement_type==='return')?'green':''); return `<tr><td>${fEsc(m.movement_date||'')}</td><td><b>${fEsc(m.item_name||'')}</b><br><small>${fEsc(m.product_code||'')}</small></td><td><span class="badge ${cls}">${type}</span></td><td>${fQty(m.quantity)}</td><td>${fEsc(m.project_name||projectName(m.project_id)||'-')}</td><td>${fEsc(m.receiver||'-')}</td><td class="row-actions"><button class="light" onclick="inventoryEditMovement('${m.id}')">تعديل</button><button class="danger" onclick="financeDelete('inventory_movements','${m.id}',true)">حذف</button></td></tr>`; }).join('')||`<tr><td colspan="7">${data.financeError||'لا توجد حركة مخزون'}</td></tr>`;
  };

  // Edit inventory request
  window.inventoryEditRequest = function(id){
    ensureRequestHidden(); const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) return msg('لم يتم العثور على الطلب','err');
    if(r.status==='approved') return msg('لا يمكن تعديل أمر صرف معتمد؛ استخدم المرتجع أو أنشئ أمر جديد','err');
    $('inventoryRequestId').value=r.id;
    if($('inventoryRequestDate')) $('inventoryRequestDate').value=r.request_date||today();
    if($('inventoryRequestProject')) $('inventoryRequestProject').value=r.project_id||'';
    if($('inventoryRequestCostCenter')) $('inventoryRequestCostCenter').value=r.cost_center_id||'';
    if($('inventoryRequestSupervisor')) $('inventoryRequestSupervisor').value=r.supervisor_id||'';
    if($('inventoryRequestReason')) $('inventoryRequestReason').value=r.reason||'';
    if($('inventoryRequestNotes')) $('inventoryRequestNotes').value=r.notes||'';
    inventoryRequestLines=getLines(r).map(l=>{ const it=getItem(l.item_id)||{}; return {item_id:l.item_id,item_name:l.item_name||it.name||'',product_code:l.product_code||productCode(it),barcode:l.barcode||barcode(it),quantity:fNum(l.quantity),available:fNum(it.quantity),unit:l.unit||it.unit||'',unit_cost:fNum(l.unit_cost||it.unit_cost)}; });
    if(typeof inventoryRenderRequestLines==='function') inventoryRenderRequestLines();
    const card=$('inventoryRequestId')?.closest('.card'); if(card){ const title=card.querySelector('h2'); if(title) title.textContent='تعديل طلب صرف من المخزون'; setFirstActionButtonText(card,'تحديث طلب الصرف'); card.scrollIntoView({behavior:'smooth',block:'start'}); }
  };

  const oldClearReqV127 = window.inventoryClearRequestForm || (typeof inventoryClearRequestForm==='function'?inventoryClearRequestForm:null);
  window.inventoryClearRequestForm = function(){ ensureRequestHidden(); if(oldClearReqV127) oldClearReqV127.apply(this, arguments); if($('inventoryRequestId')) $('inventoryRequestId').value=''; const card=$('inventoryRequestId')?.closest('.card'); if(card){ const title=card.querySelector('h2'); if(title) title.textContent='طلب صرف من المخزون'; setFirstActionButtonText(card,'إرسال طلب الصرف'); } };

  window.inventorySaveRequest = async function(btn){
    try{ if(btn) btn.disabled=true; ensureRequestHidden(); const pid=$('inventoryRequestProject')?.value; const supervisorId=$('inventoryRequestSupervisor')?.value; if(!pid) throw new Error('اختر المشروع'); if(!supervisorId) throw new Error('اختر المشرف / الفني'); if(!inventoryRequestLines.length) throw new Error('أضف صنفًا واحدًا على الأقل إلى أمر الصرف'); const cc=ccPayload('inventoryRequestCostCenter', pid); if(!cc.cost_center_name) throw new Error('اختر مركز التكلفة');
      const path=inventoryGetApprovalPath(); const first=path[0]||'warehouse'; const totalQty=inventoryRequestLines.reduce((a,x)=>a+fNum(x.quantity),0);
      const row={item_id:Number(inventoryRequestLines[0].item_id), item_name:inventoryRequestLines.map(x=>x.item_name).join('، '), quantity:totalQty, request_items:inventoryRequestLines, items:inventoryRequestLines, project_id:Number(pid), project_name:projectName(pid), cost_center_id:cc.cost_center_id, cost_center_name:cc.cost_center_name, supervisor_id:Number(supervisorId), supervisor_name:supervisorName(supervisorId), request_date:$('inventoryRequestDate')?.value||today(), reason:$('inventoryRequestReason')?.value||'', notes:$('inventoryRequestNotes')?.value||'', status:'pending_'+first, current_step:first, approval_path:path, approval_log:[]};
      const id=$('inventoryRequestId')?.value;
      const res=id?await sb.from('inventory_requests').update(row).eq('id',id):await sb.from('inventory_requests').insert(row); if(res.error) throw res.error;
      msg(id?'تم تحديث أمر الصرف وإعادته لأول مرحلة اعتماد':'تم إرسال أمر الصرف للاعتماد'); inventoryClearRequestForm(); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };

  window.inventoryOpenReturnModal = function(id){
    addSmartReturnStyles(); const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) return msg('الطلب غير موجود','err'); const lines=getLines(r); if(!lines.length) return msg('لا توجد أصناف في أمر الصرف','err');
    const rows=lines.map((l,idx)=>{ const already=retQty(r.id,l.item_id); const max=Math.max(0,fNum(l.quantity)-already); const it=getItem(l.item_id)||{}; return `<tr data-item="${fEsc(l.item_id)}" data-name="${fEsc(l.item_name)}"><td class="name"><b>${fEsc(l.product_code||productCode(it)||'-')}</b><br>${fEsc(l.item_name||it.name||'')}</td><td>${fQty(l.quantity)}</td><td>${fQty(already)}</td><td>${fQty(max)}</td><td><input class="smart-return-qty" type="number" min="0" max="${max}" step="0.01" value="0" ${max<=0?'disabled':''}></td></tr>`; }).join('');
    const old=document.getElementById('smartReturnBackdrop'); if(old) old.remove();
    const div=document.createElement('div'); div.id='smartReturnBackdrop'; div.className='smart-return-backdrop'; div.innerHTML=`<div class="smart-return-modal"><div class="smart-return-head"><div><h2>تسجيل مرتجع ذكي</h2><p>أمر صرف REQ-${fEsc(r.id)} — يتم تحديث المرتجع داخل نفس أمر الصرف بدون إنشاء طلب جديد</p></div><button class="light" onclick="document.getElementById('smartReturnBackdrop')?.remove()">إغلاق</button></div><div class="smart-return-body"><div class="smart-return-summary"><div><small>المشرف</small><b>${fEsc(r.supervisor_name||supervisorName(r.supervisor_id))}</b></div><div><small>المشروع</small><b>${fEsc(r.project_name||projectName(r.project_id))}</b></div><div><small>مركز التكلفة</small><b>${fEsc(r.cost_center_name||'-')}</b></div></div><div class="table-wrap"><table class="smart-return-table"><thead><tr><th>الصنف</th><th>المصروف</th><th>المرتجع السابق</th><th>المتاح للإرجاع</th><th>كمية المرتجع الجديدة</th></tr></thead><tbody>${rows}</tbody></table></div></div><div class="smart-return-actions"><button onclick="inventorySaveSmartReturn('${fEsc(r.id)}',this)">حفظ المرتجع</button><button class="light" onclick="document.getElementById('smartReturnBackdrop')?.remove()">إلغاء</button></div></div>`;
    document.body.appendChild(div);
  };

  window.inventoryReturnRequest = function(id, btn){ inventoryOpenReturnModal(id); };

  window.inventorySaveSmartReturn = async function(id, btn){
    try{ if(btn) btn.disabled=true; const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) throw new Error('الطلب غير موجود'); const trs=[...document.querySelectorAll('#smartReturnBackdrop tbody tr')]; const returns=[]; let any=false;
      for(const tr of trs){ const itemId=tr.getAttribute('data-item'); const name=tr.getAttribute('data-name')||''; const inp=tr.querySelector('.smart-return-qty'); const q=fNum(inp?.value); if(q<=0) continue; const l=getLines(r).find(x=>String(x.item_id)===String(itemId)); const already=retQty(r.id,itemId); const max=Math.max(0,fNum(l?.quantity)-already); if(q>max) throw new Error('كمية المرتجع أكبر من المتاح للصنف: '+name); const it=getItem(itemId)||{}; const row={item_id:Number(itemId),item_name:name,movement_type:'return',quantity:q,movement_date:today(),project_id:r.project_id,project_name:r.project_name,cost_center_id:r.cost_center_id||null,cost_center_name:r.cost_center_name||r.project_name||'',receiver:r.supervisor_name||supervisorName(r.supervisor_id),reason:'مرتجع من أمر صرف REQ-'+r.id,notes:r.reason||'',request_id:Number(r.id),product_code:l?.product_code||productCode(it),barcode:l?.barcode||barcode(it),unit_cost:fNum(it.unit_cost)}; const ins=await sb.from('inventory_movements').insert(row); if(ins.error) throw ins.error; const upd=await sb.from('inventory_items').update({quantity:fNum(it.quantity)+q}).eq('id',itemId); if(upd.error) throw upd.error; returns.push({item_id:itemId,item_name:name,quantity:q,at:new Date().toISOString()}); any=true; }
      if(!any) throw new Error('لم يتم إدخال أي كمية مرتجع'); const prev=Array.isArray(r.return_items)?r.return_items:[]; await sb.from('inventory_requests').update({return_items:[...prev,...returns],has_return:true,return_updated_at:new Date().toISOString()}).eq('id',id); document.getElementById('smartReturnBackdrop')?.remove(); msg('تم تسجيل المرتجع داخل نفس أمر الصرف'); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };

  window.inventoryRenderRequests=function(){
    const b=$('inventoryRequestsBody'); if(!b) return; if(typeof inventoryLoadApprovalSettings==='function') inventoryLoadApprovalSettings(); let rows=(typeof financeFilterRows==='function')?financeFilterRows(data.inventoryRequests||[],'request_date'):(data.inventoryRequests||[]); const st=$('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
    b.innerHTML=rows.map(r=>{ const lines=getLines(r); const itemsHtml=lines.map(l=>{ const returned=retQty(r.id,l.item_id), consumed=Math.max(0,fNum(l.quantity)-returned); return `${fEsc(l.product_code||'')} ${fEsc(l.item_name)}<br><small>صادر ${fQty(l.quantity)} | مرتجع ${fQty(returned)} | مستهلك ${fQty(consumed)}</small>`; }).join('<hr style="border:none;border-top:1px solid #e6eeee;margin:4px 0">')||fEsc(r.item_name||''); const stt=reqStats(r); const actions=[];
      actions.push(`<button class="light" onclick="inventoryPrintRequest('${r.id}')">طباعة</button>`);
      if(String(r.status||'').startsWith('pending_') || r.status==='rejected'){ actions.push(`<button class="light" onclick="inventoryEditRequest('${r.id}')">تعديل</button>`); }
      if(String(r.status||'').startsWith('pending_')){ const step=String(r.status).replace('pending_',''); if(typeof inventoryCurrentUserCanApprove==='function' && inventoryCurrentUserCanApprove(step)){ actions.push(`<button onclick="inventoryApproveRequest('${r.id}','${step}',this)">اعتماد ${fEsc(stepRole(step))}</button>`); actions.push(`<button class="light" onclick="inventoryAutoApproveRequest('${r.id}',this)">اعتماد تلقائي وصرف</button>`); actions.push(`<button class="danger" onclick="inventoryRejectRequest('${r.id}',this)">رفض</button>`); } else actions.push(`<span class="badge neutral">بانتظار ${fEsc(stepRole(step)||'المرحلة التالية')}</span>`); }
      if(r.status==='approved') actions.push(`<button class="light" onclick="inventoryReturnRequest('${r.id}',this)">مرتجع</button>`);
      return `<tr><td>${fEsc(r.request_date||'')}</td><td><b>${fEsc(r.supervisor_name||supervisorName(r.supervisor_id))}</b></td><td>${fEsc(r.project_name||projectName(r.project_id))}</td><td>${fEsc(r.cost_center_name||'-')}</td><td>${itemsHtml}</td><td>صادر ${fQty(stt.out)}<br>مرتجع ${fQty(stt.ret)}<br><b>مستهلك ${fQty(stt.cons)}</b></td><td>${fEsc(r.reason||'-')}</td><td><span class="badge ${inventoryRequestStatusClass(r.status)}">${inventoryRequestStatusText(r.status)}</span><br><small>المرحلة: ${inventoryRequestNextRole(r.status)}</small>${inventoryRequestStepHtml(r)}</td><td class="row-actions">${actions.join(' ')||'-'}</td></tr>`; }).join('')||`<tr><td colspan="9">${data.financeError||'لا توجد طلبات صرف'}</td></tr>`;
  };

  document.addEventListener('DOMContentLoaded',()=>{ ensureRequestHidden(); addSmartReturnStyles(); });
})();

/* V128: Smart invoice preview + clean action buttons for inventory requests/movements */
(function(){
  'use strict';
  function e(v){ return (typeof esc==='function') ? esc(v) : String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])); }
  function n(v){ return (typeof num==='function') ? num(v) : (Number(String(v??0).replace(/,/g,''))||0); }
  function q(v){ return n(v).toLocaleString('en-US',{maximumFractionDigits:2}); }
  function m(v){ return `${n(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س`; }
  function item(id){ return (data.inventoryItems||[]).find(x=>String(x.id)===String(id)) || {}; }
  function lines(r){ return (typeof v118LineItems==='function') ? v118LineItems(r) : (Array.isArray(r.request_items)?r.request_items:(Array.isArray(r.items)?r.items:[])); }
  function code(i){ return (typeof v118ProductCode==='function') ? v118ProductCode(i) : (i.serial_number||i.product_code||('INV-'+(i.id||''))); }
  function reqStatusText(s){ return (typeof inventoryRequestStatusText==='function') ? inventoryRequestStatusText(s) : (s||'-'); }
  function reqStatusClass(s){ return (typeof inventoryRequestStatusClass==='function') ? inventoryRequestStatusClass(s) : ''; }
  function nextRole(s){ return (typeof inventoryRequestNextRole==='function') ? inventoryRequestNextRole(s) : '-'; }
  function stepHtml(r){ return (typeof inventoryRequestStepHtml==='function') ? inventoryRequestStepHtml(r) : ''; }
  function canApprove(step){ return (typeof inventoryCurrentUserCanApprove==='function') ? inventoryCurrentUserCanApprove(step) : false; }
  function stepRole(step){ const map={warehouse:'مدير المخازن',ops:'مدير تشغيلي',finance:'مدير مالي',general:'مدير عام'}; return map[step]||step||''; }
  function retQty(reqId,itemId){
    return (data.inventoryMovements||[]).filter(x=>String(x.request_id||'')===String(reqId||'') && String(x.item_id||'')===String(itemId||'') && x.movement_type==='return').reduce((a,x)=>a+n(x.quantity),0);
  }
  function effectType(t){ return t==='in'?'إدخال':(t==='out'?'صرف':(t==='return'?'مرتجع':(t==='consume'?'استهلاك':'تعديل رصيد'))); }
  function invoiceTotals(rows){
    const sub=rows.reduce((a,r)=>a+n(r.value),0); const vat=sub*0.15; return {sub,vat,total:sub+vat};
  }
  function totalsHtml(total, label='Total Value with 15% VAT'){
    const sub=n(total), vat=sub*0.15, gross=sub+vat;
    return `<div class="totals-v128"><p><b>Total amount before 15% VAT:</b> ${m(sub)}</p><p><b>Total VAT 15%:</b> ${m(vat)}</p><p><b>${e(label)}:</b> ${m(gross)}</p></div>`;
  }
  function requestInvoiceBody(id){
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) return '<p>الطلب غير موجود</p>';
    const log=Array.isArray(r.approval_log)?r.approval_log:[];
    const arr=lines(r).map(l=>{ const it=item(l.item_id); const unit=n(l.unit_cost||it.unit_cost); const out=n(l.quantity); const returned=retQty(r.id,l.item_id); const consumed=Math.max(0,out-returned); return {...l,it,unit,out,returned,consumed,value:consumed*unit}; });
    const lineRows=arr.map(l=>`<tr><td>${e(l.product_code||code(l.it)||'')}</td><td><b>${e(l.item_name||l.it.name||'')}</b></td><td>${e(l.it.supplier||'-')}</td><td>${q(l.out)}</td><td>${q(l.returned)}</td><td><b>${q(l.consumed)}</b></td><td>${m(l.unit)}</td><td>${m(l.unit*1.15)}</td><td>${m(l.value)}</td></tr>`).join('') || '<tr><td colspan="9">لا توجد أصناف</td></tr>';
    const retRows=arr.filter(l=>l.returned>0).map(l=>`<tr><td>${e(l.product_code||code(l.it)||'')}</td><td>${e(l.item_name||l.it.name||'')}</td><td>${q(l.returned)}</td><td>${m(l.unit)}</td><td>${m(l.returned*l.unit)}</td></tr>`).join('') || '<tr><td colspan="5">لا توجد مرتجعات مسجلة على هذا الأمر</td></tr>';
    const logRows=log.map(l=>`<tr><td>${e(stepRole(l.step||l.role)||l.role||'-')}</td><td>${e(l.by||'-')}</td><td>${e(l.at?new Date(l.at).toLocaleString('en-US'):'-')}</td></tr>`).join('') || '<tr><td colspan="3">لا يوجد سجل اعتماد</td></tr>';
    const total=arr.reduce((a,l)=>a+n(l.value),0), retTotal=arr.reduce((a,l)=>a+n(l.returned)*n(l.unit),0);
    return `<div class="invoice-head-v128"><div><h2>أمر صرف مخزون</h2><p>OUT-${e(r.id)} — ${e(reqStatusText(r.status))}</p></div><div><b>التاريخ</b><br>${e(r.request_date||'')}</div></div>
    <div class="grid"><div class="box"><b>المشرف / الفني</b><br>${e(r.supervisor_name||supervisorName(r.supervisor_id))}</div><div class="box"><b>المشروع</b><br>${e(r.project_name||projectName(r.project_id))}</div><div class="box"><b>مركز التكلفة</b><br>${e(r.cost_center_name||'-')}</div></div>
    <table><tr><th>سبب الصرف</th><td>${e(r.reason||'-')}</td></tr><tr><th>ملاحظات</th><td>${e(r.notes||'-')}</td></tr></table>
    <h3>الأصناف المصروفة</h3><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>المورد</th><th>الصادر</th><th>مرتجع</th><th>المستهلك</th><th>سعر الحبة</th><th>شامل الضريبة</th><th>قيمة الاستهلاك</th></tr></thead><tbody>${lineRows}</tbody></table>${totalsHtml(total,'Total Consumption Value with 15% VAT')}
    <h3>مرتجع</h3><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>كمية المرتجع</th><th>سعر الحبة</th><th>قيمة المرتجع</th></tr></thead><tbody>${retRows}</tbody></table>${totalsHtml(retTotal,'Total Return Value with 15% VAT')}
    <h3>سجل الاعتماد</h3><table><thead><tr><th>المرحلة</th><th>المعتمد</th><th>الوقت</th></tr></thead><tbody>${logRows}</tbody></table>`;
  }
  function movementInvoiceBody(id){
    const x=(data.inventoryMovements||[]).find(mv=>String(mv.id)===String(id)); if(!x) return '<p>الحركة غير موجودة</p>';
    const it=item(x.item_id); const unit=n(x.unit_cost||it.unit_cost); const qty=n(x.quantity); const value=unit*qty;
    return `<div class="invoice-head-v128"><div><h2>حركة مخزون</h2><p>MOV-${e(x.id)} — ${e(effectType(x.movement_type))}</p></div><div><b>التاريخ</b><br>${e(x.movement_date||'')}</div></div>
    <div class="grid"><div class="box"><b>الصنف</b><br>${e(x.item_name||it.name||'-')}</div><div class="box"><b>المشروع</b><br>${e(x.project_name||projectName(x.project_id)||'-')}</div><div class="box"><b>مركز التكلفة</b><br>${e(x.cost_center_name||'-')}</div></div>
    <table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>نوع الحركة</th><th>المستلم</th><th>الكمية</th><th>سعر الحبة</th><th>شامل الضريبة</th><th>القيمة</th></tr></thead><tbody><tr><td>${e(x.product_code||code(it)||'')}</td><td>${e(x.item_name||it.name||'-')}</td><td>${e(effectType(x.movement_type))}</td><td>${e(x.receiver||'-')}</td><td>${q(qty)}</td><td>${m(unit)}</td><td>${m(unit*1.15)}</td><td>${m(value)}</td></tr></tbody></table>${totalsHtml(value,'Total Value with 15% VAT')}
    <table><tr><th>سبب الحركة</th><td>${e(x.reason||'-')}</td></tr><tr><th>ملاحظات</th><td>${e(x.notes||'-')}</td></tr></table>`;
  }
  function ensureInvoiceModalStyle(){
    if(document.getElementById('invoiceV128Style')) return;
    const st=document.createElement('style'); st.id='invoiceV128Style'; st.textContent=`
      .smart-invoice-backdrop{position:fixed;inset:0;background:rgba(0,35,28,.55);z-index:9999;display:grid;place-items:center;padding:18px;backdrop-filter:blur(4px)}
      .smart-invoice-modal{width:min(1120px,96vw);max-height:92vh;overflow:auto;background:#fff;border-radius:24px;border:1px solid #cfe2dc;box-shadow:0 28px 70px rgba(0,0,0,.28);direction:rtl;color:#10231d}
      .smart-invoice-top{position:sticky;top:0;background:linear-gradient(135deg,#0A4033,#0f5a47);color:#fff;padding:14px 18px;display:flex;align-items:center;justify-content:space-between;gap:10px;z-index:2}.smart-invoice-top h2{margin:0;color:#fff}.smart-invoice-top button{background:#fff;color:#0A4033}.smart-invoice-body{padding:22px}.smart-invoice-body table{width:100%;border-collapse:collapse;margin:12px 0;font-size:12px}.smart-invoice-body th{background:#d0d0d0;color:#111;border:1px solid #333;padding:8px}.smart-invoice-body td{border:1px solid #333;padding:7px;text-align:center}.invoice-head-v128{display:flex;align-items:start;justify-content:space-between;gap:12px;margin-bottom:12px}.invoice-head-v128 h2{margin:0;color:#0A4033}.totals-v128{direction:ltr;text-align:left;margin:16px 0;font-size:15px;line-height:1.9}.smart-invoice-actions{display:flex;gap:8px;flex-wrap:wrap}.row-actions.inventory-actions-v128{min-width:280px!important;width:auto!important;justify-content:flex-start!important;flex-wrap:wrap!important;gap:6px!important}.row-actions.inventory-actions-v128 button{min-width:86px!important;border-radius:10px!important;padding:8px 10px!important}.row-actions.inventory-actions-v128 .btn-view{background:#0f766e!important}.row-actions.inventory-actions-v128 .btn-print{background:#0A4033!important}.row-actions.inventory-actions-v128 .btn-edit{background:#eef6f3!important;color:#0A4033!important;border:1px solid #cfe2dc!important}.row-actions.inventory-actions-v128 .btn-return{background:#fff5da!important;color:#8a6700!important;border:1px solid #ead28d!important}.row-actions.inventory-actions-v128 .btn-delete{background:#b83232!important;color:#fff!important}@media print{.smart-invoice-backdrop{position:static;background:#fff;display:block;padding:0}.smart-invoice-top{display:none}.smart-invoice-modal{box-shadow:none;border:0;max-height:none;width:auto}.smart-invoice-body{padding:0}}`;
    document.head.appendChild(st);
  }
  window.inventoryOpenRequestInvoice=function(id){ ensureInvoiceModalStyle(); const div=document.createElement('div'); div.className='smart-invoice-backdrop'; div.id='smartInvoiceV128'; div.innerHTML=`<div class="smart-invoice-modal"><div class="smart-invoice-top"><h2>عرض الفاتورة</h2><div class="smart-invoice-actions"><button onclick="inventoryPrintRequest('${e(id)}')">طباعة</button><button onclick="document.getElementById('smartInvoiceV128')?.remove()">إغلاق</button></div></div><div class="smart-invoice-body">${requestInvoiceBody(id)}</div></div>`; document.body.appendChild(div); };
  window.inventoryOpenMovementInvoice=function(id){ ensureInvoiceModalStyle(); const div=document.createElement('div'); div.className='smart-invoice-backdrop'; div.id='smartInvoiceV128'; div.innerHTML=`<div class="smart-invoice-modal"><div class="smart-invoice-top"><h2>عرض حركة المخزون</h2><div class="smart-invoice-actions"><button onclick="inventoryPrintMovement('${e(id)}')">طباعة</button><button onclick="document.getElementById('smartInvoiceV128')?.remove()">إغلاق</button></div></div><div class="smart-invoice-body">${movementInvoiceBody(id)}</div></div>`; document.body.appendChild(div); };
  window.inventoryPrintRequest=function(id){ if(typeof financePrintWindow==='function') financePrintWindow('أمر صرف مخزون', requestInvoiceBody(id)); };
  window.inventoryPrintMovement=function(id){ if(typeof financePrintWindow==='function') financePrintWindow('حركة مخزون', movementInvoiceBody(id)); };

  window.inventoryRenderRequests=function(){
    const b=document.getElementById('inventoryRequestsBody'); if(!b) return; if(typeof inventoryLoadApprovalSettings==='function') inventoryLoadApprovalSettings(); let rows=(typeof financeFilterRows==='function')?financeFilterRows(data.inventoryRequests||[],'request_date'):(data.inventoryRequests||[]); const st=document.getElementById('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
    b.innerHTML=rows.map(r=>{ const arr=lines(r); const itemsHtml=arr.map(l=>{ const returned=retQty(r.id,l.item_id), consumed=Math.max(0,n(l.quantity)-returned); return `${e(l.product_code||'')} ${e(l.item_name)}<br><small>صادر ${q(l.quantity)} | مرتجع ${q(returned)} | مستهلك ${q(consumed)}</small>`; }).join('<hr style="border:none;border-top:1px solid #e6eeee;margin:4px 0">')||e(r.item_name||''); const totalOut=arr.reduce((a,l)=>a+n(l.quantity),0)||n(r.quantity); const totalRet=arr.reduce((a,l)=>a+retQty(r.id,l.item_id),0); const actions=[];
      actions.push(`<button class="btn-view" onclick="inventoryOpenRequestInvoice('${e(r.id)}')">عرض الفاتورة</button>`);
      actions.push(`<button class="btn-print" onclick="inventoryPrintRequest('${e(r.id)}')">طباعة</button>`);
      if(String(r.status||'').startsWith('pending_') || r.status==='rejected') actions.push(`<button class="btn-edit" onclick="inventoryEditRequest('${e(r.id)}')">تعديل</button>`);
      if(String(r.status||'').startsWith('pending_')){ const step=String(r.status).replace('pending_',''); if(canApprove(step)){ actions.push(`<button onclick="inventoryApproveRequest('${e(r.id)}','${e(step)}',this)">اعتماد</button>`); actions.push(`<button class="btn-edit" onclick="inventoryAutoApproveRequest('${e(r.id)}',this)">اعتماد تلقائي</button>`); actions.push(`<button class="btn-delete" onclick="inventoryRejectRequest('${e(r.id)}',this)">رفض</button>`); } else actions.push(`<span class="badge neutral">بانتظار ${e(stepRole(step)||'المرحلة التالية')}</span>`); }
      if(r.status==='approved') actions.push(`<button class="btn-return" onclick="inventoryReturnRequest('${e(r.id)}',this)">مرتجع</button>`);
      return `<tr><td>${e(r.request_date||'')}</td><td><b>${e(r.supervisor_name||supervisorName(r.supervisor_id))}</b></td><td>${e(r.project_name||projectName(r.project_id))}</td><td>${e(r.cost_center_name||'-')}</td><td>${itemsHtml}</td><td>صادر ${q(totalOut)}<br>مرتجع ${q(totalRet)}<br><b>مستهلك ${q(Math.max(0,totalOut-totalRet))}</b></td><td>${e(r.reason||'-')}</td><td><span class="badge ${reqStatusClass(r.status)}">${e(reqStatusText(r.status))}</span><br><small>المرحلة: ${e(nextRole(r.status))}</small>${stepHtml(r)}</td><td class="row-actions inventory-actions-v128">${actions.join(' ')||'-'}</td></tr>`; }).join('')||`<tr><td colspan="9">${e(data.financeError||'لا توجد طلبات صرف')}</td></tr>`;
  };

  window.inventoryRenderMovements=function(){
    const b=document.getElementById('inventoryMovementsBody'); if(!b) return; let rows=(typeof financeFilterRows==='function')?financeFilterRows(data.inventoryMovements||[],'movement_date'):(data.inventoryMovements||[]);
    b.innerHTML=rows.map(x=>{ const type=effectType(x.movement_type); const cls=x.movement_type==='out'?'amber':((x.movement_type==='in'||x.movement_type==='return')?'green':''); return `<tr><td>${e(x.movement_date||'')}</td><td><b>${e(x.item_name||'')}</b><br><small>${e(x.product_code||'')}</small></td><td><span class="badge ${cls}">${e(type)}</span></td><td>${q(x.quantity)}</td><td>${e(x.project_name||projectName(x.project_id)||'-')}</td><td>${e(x.receiver||'-')}</td><td class="row-actions inventory-actions-v128"><button class="btn-view" onclick="inventoryOpenMovementInvoice('${e(x.id)}')">عرض</button><button class="btn-print" onclick="inventoryPrintMovement('${e(x.id)}')">طباعة</button><button class="btn-edit" onclick="inventoryEditMovement('${e(x.id)}')">تعديل</button><button class="btn-delete" onclick="financeDelete('inventory_movements','${e(x.id)}',true)">حذف</button></td></tr>`; }).join('')||`<tr><td colspan="7">${e(data.financeError||'لا توجد حركة مخزون')}</td></tr>`;
  };
  window.addEventListener('load',()=>setTimeout(()=>{ ensureInvoiceModalStyle(); try{ if(document.getElementById('inventoryRequestsBody')) window.inventoryRenderRequests(); if(document.getElementById('inventoryMovementsBody')) window.inventoryRenderMovements(); }catch(e){} },1300));
})();

// ================= V130 Smart UI Framework Fix =================
(function(){
  const byId = id => document.getElementById(id);
  const e = s => String(s ?? '').replace(/[&<>\"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const n = v => (typeof num==='function'?num(v):Number(v||0));
  const q = v => Number(n(v)).toLocaleString('en-US',{maximumFractionDigits:2});
  const m = v => (typeof money==='function'?money(v):`${Number(n(v)).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س`);
  const proj = id => (typeof financeProjectName==='function'?financeProjectName(id):((data.projects||[]).find(p=>String(p.id)===String(id))?.name||'-'));
  const sup = id => (typeof supervisorName==='function'?supervisorName(id):((data.users||[]).find(u=>String(u.id)===String(id))?.full_name||'-'));
  const item = id => (data.inventoryItems||[]).find(x=>String(x.id)===String(id)) || {};
  const img = it => it.image_url || it.image || it.photo_url || it.product_image || '';
  const code = it => it.product_code || it.serial_number || it.code || '';
  const barcode = it => it.barcode || it.supplier_barcode || '';
  const lineItems = r => {
    if(typeof v118LineItems==='function') return v118LineItems(r);
    if(Array.isArray(r.request_items)) return r.request_items;
    if(Array.isArray(r.items)) return r.items;
    return r.item_id ? [{item_id:r.item_id,item_name:r.item_name,quantity:r.quantity,product_code:r.product_code,unit_cost:r.unit_cost}] : [];
  };
  const returnedFor = (reqId,itemId) => (data.inventoryMovements||[]).filter(x=>String(x.request_id)===String(reqId)&&String(x.item_id)===String(itemId)&&x.movement_type==='return').reduce((a,x)=>a+n(x.quantity),0);
  const requestStatusText = s => (typeof inventoryRequestStatusText==='function'?inventoryRequestStatusText(s):(s||'-'));
  const requestStatusClass = s => String(s||'').includes('rejected')?'danger':(String(s||'').includes('pending')?'warn':(s==='approved'?'green':'neutral'));
  const canApproveStep = step => (typeof inventoryCurrentUserCanApprove==='function'?inventoryCurrentUserCanApprove(step):false);
  const stepRole = step => ({warehouse:'مدير مخازن',ops:'مدير تشغيلي',finance:'مدير مالي',general:'مدير عام'}[step]||step||'-');
  const setSmartTable = body => { try{ body.closest('table')?.classList.add('smart-list-table'); }catch(_){} };
  const cardShell = html => `<tr><td class="smart-row-cell" colspan="20"><div class="smart-grid-v129">${html}</div></td></tr>`;
  const statusSpan = (text, cls='neutral') => `<span class="smart-status-v129 ${cls}">${e(text)}</span>`;
  function smartOpen(title, bodyHtml, actions=''){
    document.getElementById('smartModalV129')?.remove();
    const div=document.createElement('div');
    div.className='smart-modal-v129'; div.id='smartModalV129';
    div.innerHTML=`<div class="smart-modal-box-v129"><div class="smart-modal-top-v129"><h2>${e(title)}</h2><div style="display:flex;gap:8px;flex-wrap:wrap">${actions}<button class="light" onclick="document.getElementById('smartModalV129')?.remove()">إغلاق</button></div></div><div class="smart-modal-body-v129">${bodyHtml}</div></div>`;
    document.body.appendChild(div);
  }
  window.smartOpenV129 = smartOpen;

  function initFinanceHero(){ return; }

  window.financeOpenExpenseSmart = function(id){
    const x=(data.financeExpenses||[]).find(r=>String(r.id)===String(id)); if(!x) return msg('المصروف غير موجود','err');
    smartOpen('تفاصيل المصروف', `<div class="smart-meta-v129"><div class="m"><small>التاريخ</small><b>${e(x.expense_date||'-')}</b></div><div class="m"><small>المشروع</small><b>${e(x.project_name||proj(x.project_id))}</b></div><div class="m"><small>النوع</small><b>${e(x.category||'-')}</b></div><div class="m"><small>المورد</small><b>${e(x.supplier||'-')}</b></div><div class="m"><small>قبل الضريبة</small><b>${m(x.amount)}</b></div><div class="m"><small>الضريبة</small><b>${m(x.vat)}</b></div><div class="m"><small>الإجمالي</small><b>${m(x.total)}</b></div><div class="m"><small>طريقة الدفع</small><b>${e(x.payment_method||'-')}</b></div></div><h3>ملاحظات</h3><p>${e(x.notes||'لا توجد ملاحظات')}</p>`);
  };

  window.financeRenderExpenses = function(){
    const b=byId('financeExpensesBody'); if(!b) return; setSmartTable(b);
    let rows=(typeof financeFilterRows==='function'?financeFilterRows(data.financeExpenses||[],'expense_date'):(data.financeExpenses||[])); const cat=byId('financeExpenseTypeFilter')?.value; if(cat) rows=rows.filter(x=>x.category===cat);
    if(!rows.length){ b.innerHTML=`<tr><td colspan="20">${e(data.financeError||'لا توجد مصروفات')}</td></tr>`; return; }
    b.innerHTML=cardShell(rows.map(x=>`<article class="smart-card-v129"><div class="smart-card-head-v129"><div><h3 class="smart-title-v129">${e(x.category||'مصروف')}</h3><div class="smart-sub-v129">${e(x.expense_date||'-')} — ${e(x.project_name||proj(x.project_id))}</div></div>${statusSpan(m(x.total),'neutral')}</div><div class="smart-meta-v129"><div class="m"><small>المورد</small><b>${e(x.supplier||'-')}</b></div><div class="m"><small>الدفع</small><b>${e(x.payment_method||'-')}</b></div><div class="m"><small>قبل الضريبة</small><b>${m(x.amount)}</b></div><div class="m"><small>الضريبة</small><b>${m(x.vat)}</b></div></div><div class="smart-card-actions-v129"><button onclick="financeOpenExpenseSmart('${e(x.id)}')">عرض</button><button class="light" onclick="financeEditExpense('${e(x.id)}')">تعديل</button><button class="danger" onclick="financeDelete('finance_expenses','${e(x.id)}')">حذف</button></div></article>`).join(''));
  };

  window.inventoryOpenItemSmart = function(id){
    const it=item(id); if(!it.id) return msg('الصنف غير موجود','err');
    const movs=(data.inventoryMovements||[]).filter(x=>String(x.item_id)===String(id));
    const out=movs.filter(x=>x.movement_type==='out').reduce((a,x)=>a+n(x.quantity),0), ret=movs.filter(x=>x.movement_type==='return').reduce((a,x)=>a+n(x.quantity),0), cons=movs.filter(x=>x.movement_type==='consume').reduce((a,x)=>a+n(x.quantity),0);
    const rows=movs.slice(0,30).map(x=>`<tr><td>${e(x.movement_date||'-')}</td><td>${e(x.movement_type||'-')}</td><td>${q(x.quantity)}</td><td>${e(x.project_name||proj(x.project_id))}</td><td>${e(x.receiver||'-')}</td><td>${e(x.reason||'-')}</td></tr>`).join('') || '<tr><td colspan="6">لا توجد حركات مسجلة</td></tr>';
    smartOpen('تفاصيل المنتج', `<div style="display:flex;gap:18px;align-items:flex-start;flex-wrap:wrap"><img class="smart-img-v129" style="width:150px;height:150px" src="${e(img(it))}" onerror="this.style.display='none'"><div style="flex:1;min-width:260px"><h2>${e(it.name||'-')}</h2><div class="smart-meta-v129"><div class="m"><small>كود المنتج</small><b>${e(code(it)||'-')}</b></div><div class="m"><small>باركود المورد</small><b>${e(barcode(it)||'-')}</b></div><div class="m"><small>المورد</small><b>${e(it.supplier||'-')}</b></div><div class="m"><small>التصنيف</small><b>${e(it.category||'-')}</b></div><div class="m"><small>الكمية الحالية</small><b>${q(it.quantity)} ${e(it.unit||'')}</b></div><div class="m"><small>حد إعادة الطلب</small><b>${q(it.min_quantity||it.reorder_level||0)}</b></div><div class="m"><small>سعر الحبة</small><b>${m(it.unit_cost)}</b></div><div class="m"><small>سعر شامل الضريبة</small><b>${m(n(it.unit_cost)*1.15)}</b></div></div></div></div><h3>ملخص الحركة</h3><div class="smart-meta-v129"><div class="m"><small>الصادر</small><b>${q(out)}</b></div><div class="m"><small>المرتجع</small><b>${q(ret)}</b></div><div class="m"><small>الاستهلاك المسجل</small><b>${q(cons)}</b></div><div class="m"><small>عدد الحركات</small><b>${movs.length}</b></div></div><h3>آخر الحركات</h3><table><thead><tr><th>التاريخ</th><th>الحركة</th><th>الكمية</th><th>المشروع</th><th>المستلم</th><th>السبب</th></tr></thead><tbody>${rows}</tbody></table>`, `<button onclick="financePrintReport && financePrintReport('productDetail')">طباعة</button>`);
  };

  window.inventoryRenderItems = function(){
    const b=byId('inventoryItemsBody'); if(!b) return; setSmartTable(b);
    let rows=data.inventoryItems||[]; const qtext=(byId('inventorySearch')?.value||'').toLowerCase(); const supFilter=byId('inventorySupplierFilter')?.value||''; const catFilter=byId('inventoryCategoryFilter')?.value||'';
    if(qtext) rows=rows.filter(it=>[it.name,code(it),barcode(it),it.supplier].some(v=>String(v||'').toLowerCase().includes(qtext)));
    if(supFilter) rows=rows.filter(it=>String(it.supplier||'')===String(supFilter)); if(catFilter) rows=rows.filter(it=>String(it.category||'')===String(catFilter));
    if(!rows.length){ b.innerHTML=`<tr><td colspan="20">${e(data.financeError||'لا توجد أصناف مخزون')}</td></tr>`; return; }
    b.innerHTML=cardShell(rows.map(it=>{ const low=n(it.quantity)<=n(it.min_quantity||it.reorder_level); return `<article class="smart-card-v129"><div class="smart-card-head-v129"><div style="display:flex;gap:12px;align-items:center"><img class="smart-img-v129" src="${e(img(it))}" onerror="this.outerHTML='<div class=&quot;smart-img-v129&quot; style=&quot;display:grid;place-items:center;color:#789&quot;>لا صورة</div>'"><div><h3 class="smart-title-v129">${e(it.name||'-')}</h3><div class="smart-sub-v129">${e(code(it)||'-')} — ${e(barcode(it)||'بدون باركود')}</div></div></div>${statusSpan(low?'منخفض':'متوفر',low?'danger':'green')}</div><div class="smart-meta-v129"><div class="m"><small>الكمية</small><b>${q(it.quantity)} ${e(it.unit||'')}</b></div><div class="m"><small>حد الطلب</small><b>${q(it.min_quantity||it.reorder_level||0)}</b></div><div class="m"><small>سعر الحبة</small><b>${m(it.unit_cost)}</b></div><div class="m"><small>المورد</small><b>${e(it.supplier||'-')}</b></div><div class="m"><small>التصنيف</small><b>${e(it.category||'-')}</b></div><div class="m"><small>النوع</small><b>${e(it.item_type||it.type||'-')}</b></div></div><div class="smart-card-actions-v129"><button onclick="inventoryOpenItemSmart('${e(it.id)}')">عرض المنتج</button><button class="light" onclick="inventoryEditItem('${e(it.id)}')">تعديل</button><button class="danger" onclick="financeDelete('inventory_items','${e(it.id)}',true)">حذف</button></div></article>`; }).join(''));
  };

  window.inventoryOpenMovementSmart = function(id){
    const x=(data.inventoryMovements||[]).find(m=>String(m.id)===String(id)); if(!x) return msg('الحركة غير موجودة','err');
    const it=item(x.item_id); const type=x.movement_type==='in'?'إدخال':(x.movement_type==='out'?'صرف':(x.movement_type==='return'?'مرتجع':(x.movement_type==='consume'?'استهلاك':'تعديل')));
    smartOpen('تفاصيل حركة المخزون', `<div class="smart-meta-v129"><div class="m"><small>التاريخ</small><b>${e(x.movement_date||'-')}</b></div><div class="m"><small>نوع الحركة</small><b>${e(type)}</b></div><div class="m"><small>الصنف</small><b>${e(x.item_name||it.name||'-')}</b></div><div class="m"><small>الكمية</small><b>${q(x.quantity)}</b></div><div class="m"><small>المشروع</small><b>${e(x.project_name||proj(x.project_id))}</b></div><div class="m"><small>المستلم</small><b>${e(x.receiver||'-')}</b></div><div class="m"><small>مركز التكلفة</small><b>${e(x.cost_center_name||'-')}</b></div><div class="m"><small>المرجع</small><b>${e(x.request_id?('REQ-'+x.request_id):'-')}</b></div></div><h3>السبب</h3><p>${e(x.reason||'-')}</p><h3>ملاحظات</h3><p>${e(x.notes||'-')}</p>`, `<button onclick="inventoryPrintMovement && inventoryPrintMovement('${e(id)}')">طباعة</button>`);
  };

  window.inventoryRenderMovements = function(){
    const b=byId('inventoryMovementsBody'); if(!b) return; setSmartTable(b);
    let rows=(typeof financeFilterRows==='function'?financeFilterRows(data.inventoryMovements||[],'movement_date'):(data.inventoryMovements||[]));
    if(!rows.length){ b.innerHTML=`<tr><td colspan="20">${e(data.financeError||'لا توجد حركة مخزون')}</td></tr>`; return; }
    b.innerHTML=cardShell(rows.map(x=>{ const type=x.movement_type==='in'?'إدخال':(x.movement_type==='out'?'صرف':(x.movement_type==='return'?'مرتجع':(x.movement_type==='consume'?'استهلاك':'تعديل رصيد'))); const cls=x.movement_type==='out'?'warn':((x.movement_type==='in'||x.movement_type==='return')?'green':'neutral'); return `<article class="smart-card-v129"><div class="smart-card-head-v129"><div><h3 class="smart-title-v129">${e(x.item_name||'-')}</h3><div class="smart-sub-v129">${e(x.movement_date||'-')} — ${e(x.product_code||'')}</div></div>${statusSpan(type,cls)}</div><div class="smart-meta-v129"><div class="m"><small>الكمية</small><b>${q(x.quantity)}</b></div><div class="m"><small>المشروع</small><b>${e(x.project_name||proj(x.project_id))}</b></div><div class="m"><small>المستلم</small><b>${e(x.receiver||'-')}</b></div><div class="m"><small>مركز التكلفة</small><b>${e(x.cost_center_name||'-')}</b></div></div><div class="smart-card-actions-v129"><button onclick="inventoryOpenMovementSmart('${e(x.id)}')">عرض</button><button onclick="inventoryPrintMovement && inventoryPrintMovement('${e(x.id)}')">طباعة</button><button class="light" onclick="inventoryEditMovement('${e(x.id)}')">تعديل</button><button class="danger" onclick="financeDelete('inventory_movements','${e(x.id)}',true)">حذف</button></div></article>`; }).join(''));
  };

  window.inventoryOpenRequestSmart = function(id){
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) return msg('الطلب غير موجود','err');
    const lines=lineItems(r); const rows=lines.map(l=>{ const ret=returnedFor(r.id,l.item_id); const consumed=Math.max(0,n(l.quantity)-ret); const it=item(l.item_id); return `<tr><td>${e(l.product_code||code(it)||'-')}</td><td>${e(l.item_name||it.name||'-')}</td><td>${q(l.quantity)}</td><td>${q(ret)}</td><td>${q(consumed)}</td><td>${m(l.unit_cost||it.unit_cost)}</td><td>${m(consumed*n(l.unit_cost||it.unit_cost))}</td></tr>`; }).join('') || '<tr><td colspan="7">لا توجد أصناف</td></tr>';
    smartOpen('تفاصيل أمر الصرف', `<div class="smart-meta-v129"><div class="m"><small>رقم الطلب</small><b>REQ-${e(r.id)}</b></div><div class="m"><small>التاريخ</small><b>${e(r.request_date||'-')}</b></div><div class="m"><small>المشرف</small><b>${e(r.supervisor_name||sup(r.supervisor_id))}</b></div><div class="m"><small>المشروع</small><b>${e(r.project_name||proj(r.project_id))}</b></div><div class="m"><small>مركز التكلفة</small><b>${e(r.cost_center_name||'-')}</b></div><div class="m"><small>الحالة</small><b>${e(requestStatusText(r.status))}</b></div></div><h3>الأصناف</h3><table><thead><tr><th>الكود</th><th>الصنف</th><th>الصادر</th><th>المرتجع</th><th>المستهلك</th><th>سعر الحبة</th><th>قيمة الاستهلاك</th></tr></thead><tbody>${rows}</tbody></table><h3>سبب الصرف</h3><p>${e(r.reason||'-')}</p><h3>ملاحظات</h3><p>${e(r.notes||'-')}</p>`, `<button onclick="inventoryPrintRequest && inventoryPrintRequest('${e(id)}')">طباعة</button>`);
  };

  window.inventoryRenderRequests = function(){
    const b=byId('inventoryRequestsBody'); if(!b) return; setSmartTable(b); if(typeof inventoryLoadApprovalSettings==='function') inventoryLoadApprovalSettings();
    let rows=(typeof financeFilterRows==='function'?financeFilterRows(data.inventoryRequests||[],'request_date'):(data.inventoryRequests||[])); const st=byId('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
    if(!rows.length){ b.innerHTML=`<tr><td colspan="20">${e(data.financeError||'لا توجد طلبات صرف')}</td></tr>`; return; }
    b.innerHTML=cardShell(rows.map(r=>{ const arr=lineItems(r); const totalOut=arr.reduce((a,l)=>a+n(l.quantity),0)||n(r.quantity); const totalRet=arr.reduce((a,l)=>a+returnedFor(r.id,l.item_id),0); const status=String(r.status||''); const step=status.replace('pending_',''); const lines=arr.map(l=>{ const ret=returnedFor(r.id,l.item_id); return `<div class="smart-line-v129"><b>${e(l.item_name||'-')}</b><br><small>${e(l.product_code||'')} — صادر ${q(l.quantity)} | مرتجع ${q(ret)} | مستهلك ${q(Math.max(0,n(l.quantity)-ret))}</small></div>`; }).join(''); let acts=[`<button onclick="inventoryOpenRequestSmart('${e(r.id)}')">عرض ذكي</button>`,`<button onclick="inventoryOpenRequestInvoice && inventoryOpenRequestInvoice('${e(r.id)}')">عرض الفاتورة</button>`,`<button onclick="inventoryPrintRequest && inventoryPrintRequest('${e(r.id)}')">طباعة</button>`]; if(status.startsWith('pending_')||status==='rejected') acts.push(`<button class="light" onclick="inventoryEditRequest('${e(r.id)}')">تعديل</button>`); if(status.startsWith('pending_')){ if(canApproveStep(step)){ acts.push(`<button onclick="inventoryApproveRequest('${e(r.id)}','${e(step)}',this)">اعتماد</button>`); acts.push(`<button class="light" onclick="inventoryAutoApproveRequest('${e(r.id)}',this)">اعتماد تلقائي</button>`); acts.push(`<button class="danger" onclick="inventoryRejectRequest('${e(r.id)}',this)">رفض</button>`); } else acts.push(`<span class="smart-status-v129 neutral">بانتظار ${e(stepRole(step))}</span>`); } if(status==='approved') acts.push(`<button class="light" onclick="inventoryReturnRequest('${e(r.id)}',this)">مرتجع</button>`);
      return `<article class="smart-card-v129"><div class="smart-card-head-v129"><div><h3 class="smart-title-v129">أمر صرف REQ-${e(r.id)}</h3><div class="smart-sub-v129">${e(r.request_date||'-')} — ${e(r.supervisor_name||sup(r.supervisor_id))}</div></div>${statusSpan(requestStatusText(r.status),requestStatusClass(r.status))}</div><div class="smart-meta-v129"><div class="m"><small>المشروع</small><b>${e(r.project_name||proj(r.project_id))}</b></div><div class="m"><small>مركز التكلفة</small><b>${e(r.cost_center_name||'-')}</b></div><div class="m"><small>الصادر</small><b>${q(totalOut)}</b></div><div class="m"><small>المرتجع</small><b>${q(totalRet)}</b></div><div class="m"><small>المستهلك</small><b>${q(Math.max(0,totalOut-totalRet))}</b></div><div class="m"><small>عدد الأصناف</small><b>${arr.length}</b></div></div><div class="smart-lines-v129">${lines}</div><div class="smart-card-actions-v129">${acts.join('')}</div></article>`; }).join(''));
  };

  const oldFinanceShowTabV129 = window.financeShowTab;
  window.financeShowTab = function(tab, btn){ if(oldFinanceShowTabV129) oldFinanceShowTabV129.apply(this, arguments); setTimeout(()=>{ initFinanceHero(); try{ financeRenderExpenses(); inventoryRenderItems(); inventoryRenderMovements(); inventoryRenderRequests(); }catch(_){} },60); };
  window.addEventListener('load',()=>setTimeout(()=>{ initFinanceHero(); try{ if(byId('financeDashboard')){ financeRenderExpenses(); inventoryRenderItems(); inventoryRenderMovements(); inventoryRenderRequests(); } }catch(e){ console.warn('V129 smart UI render warning',e); } },1500));
})();

// ================= V131 Cost Center Edit & Approved Request Update Fix =================
(function(){
  'use strict';
  const $id = id => document.getElementById(id);
  const n = v => (typeof num === 'function') ? num(v) : (Number(String(v ?? 0).replace(/,/g,'')) || 0);
  const e = v => (typeof esc === 'function') ? esc(v) : String(v ?? '').replace(/[&<>\"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const q = v => Number(n(v)).toLocaleString('en-US',{maximumFractionDigits:2});
  const projectLabel = id => (typeof projectName === 'function' ? projectName(id) : ((window.data?.projects||[]).find(p=>String(p.id)===String(id))?.name||''));
  const supervisorLabel = id => (typeof supervisorName === 'function' ? supervisorName(id) : ((window.data?.users||[]).find(u=>String(u.id)===String(id))?.full_name||''));
  const lineItems = r => (typeof v118LineItems === 'function') ? v118LineItems(r) : (Array.isArray(r?.request_items)?r.request_items:(Array.isArray(r?.items)?r.items:[]));
  const itemById = id => (typeof v118ItemById === 'function') ? v118ItemById(id) : ((window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id)) || {});
  const itemCode = it => (typeof v118ProductCode === 'function') ? v118ProductCode(it) : (it?.product_code || it?.serial_number || '');
  const itemBarcode = it => (typeof v118Barcode === 'function') ? v118Barcode(it) : (it?.barcode || it?.supplier_barcode || '');
  const returnQty = (requestId,itemId) => (window.data?.inventoryMovements||[]).filter(m=>String(m.request_id||'')===String(requestId||'') && String(m.item_id||'')===String(itemId||'') && m.movement_type==='return').reduce((a,m)=>a+n(m.quantity),0);
  const movementUnitCost = (m,it) => n(m.unit_cost || it?.unit_cost);
  const vatOf = (amount,it) => (typeof vatOfAmount === 'function') ? vatOfAmount(amount,it) : amount*0.15;
  const grossOf = (amount,it) => (typeof grossOfAmount === 'function') ? grossOfAmount(amount,it) : amount + vatOf(amount,it);

  function ccRows(){
    const saved = (window.data?.costCenters||[]).filter(c=>String(c.status||'active')!=='inactive');
    if(saved.length) return saved;
    return (window.data?.projects||[]).map(p=>({id:'project-'+p.id, code:'PRJ-'+p.id, name:p.name, type:'مشروع', status:'active', project_id:p.id, pseudo:true}));
  }
  function ccSelectValueFor(request){
    if(request?.cost_center_id) return String(request.cost_center_id);
    const rows = ccRows();
    const byProject = rows.find(c=>String(c.project_id||'')===String(request?.project_id||''));
    if(byProject) return String(byProject.id);
    const byName = rows.find(c=>String(c.name||'')===String(request?.cost_center_name||''));
    return byName ? String(byName.id) : '';
  }
  function ccPayload(selectId, projectId){
    let raw = $id(selectId)?.value || '';
    const rows = ccRows();
    let c = rows.find(x=>String(x.id)===String(raw));
    if(!c && projectId){
      c = rows.find(x=>String(x.project_id||'')===String(projectId)) || rows.find(x=>String(x.name||'')===String(projectLabel(projectId)));
    }
    return {cost_center_id: c && !c.pseudo ? Number(c.id) : null, cost_center_name: c?.name || (projectId ? projectLabel(projectId) : '')};
  }
  function fillCostCenterSelect(selectId, selected){
    const el=$id(selectId); if(!el) return;
    const html='<option value="">اختر مركز التكلفة</option>'+ccRows().map(c=>`<option value="${e(c.id)}">${e(c.code||'')} - ${e(c.name||'')}</option>`).join('');
    el.innerHTML=html;
    if(selected!==undefined && selected!==null) el.value=String(selected);
  }
  function updateSelectByProject(projectSelectId, ccSelectId){
    const pid=$id(projectSelectId)?.value || '';
    const rows=ccRows();
    const c=rows.find(x=>String(x.project_id||'')===String(pid)) || rows.find(x=>String(x.name||'')===String(projectLabel(pid)));
    if($id(ccSelectId) && c) $id(ccSelectId).value=String(c.id);
  }
  const oldFill = window.costCenterFillSelects;
  window.costCenterFillSelects = function(){
    if(oldFill) oldFill.apply(this, arguments);
    ['inventoryRequestCostCenter','inventoryMovementCostCenter','financeExpenseCostCenter','supInventoryRequestCostCenter'].forEach(id=>{ const old=$id(id)?.value; fillCostCenterSelect(id, old); });
  };
  function bindCostCenterHooks(){
    [['inventoryRequestProject','inventoryRequestCostCenter'],['inventoryMovementProject','inventoryMovementCostCenter'],['financeExpenseProject','financeExpenseCostCenter'],['supInventoryRequestProject','supInventoryRequestCostCenter']].forEach(([p,c])=>{
      const pe=$id(p); if(pe && !pe.dataset.v131cc){ pe.dataset.v131cc='1'; pe.addEventListener('change',()=>updateSelectByProject(p,c)); }
    });
  }

  const oldEditRequest = window.inventoryEditRequest;
  window.inventoryEditRequest = function(id){
    const r=(window.data?.inventoryRequests||[]).find(x=>String(x.id)===String(id));
    if(!r){ if(oldEditRequest) return oldEditRequest.apply(this, arguments); return; }
    if(typeof ensureRequestHidden === 'function') ensureRequestHidden();
    fillCostCenterSelect('inventoryRequestCostCenter', ccSelectValueFor(r));
    if($id('inventoryRequestId')) $id('inventoryRequestId').value=r.id;
    if($id('inventoryRequestDate')) $id('inventoryRequestDate').value=String(r.request_date||new Date().toISOString().slice(0,10)).slice(0,10);
    if($id('inventoryRequestProject')) $id('inventoryRequestProject').value=r.project_id||'';
    if($id('inventoryRequestCostCenter')) $id('inventoryRequestCostCenter').value=ccSelectValueFor(r);
    if($id('inventoryRequestSupervisor')) $id('inventoryRequestSupervisor').value=r.supervisor_id||'';
    if($id('inventoryRequestReason')) $id('inventoryRequestReason').value=r.reason||'';
    if($id('inventoryRequestNotes')) $id('inventoryRequestNotes').value=r.notes||'';
    window.inventoryRequestLines = lineItems(r).map(l=>{ const it=itemById(l.item_id)||{}; return {item_id:l.item_id,item_name:l.item_name||it.name||'',product_code:l.product_code||itemCode(it),barcode:l.barcode||itemBarcode(it),quantity:n(l.quantity),available:n(it.quantity),unit:l.unit||it.unit||'',unit_cost:n(l.unit_cost||it.unit_cost)}; });
    if(typeof inventoryRenderRequestLines==='function') inventoryRenderRequestLines();
    const card=$id('inventoryRequestId')?.closest('.card');
    if(card){ const title=card.querySelector('h2'); if(title) title.textContent = r.status==='approved' ? 'تعديل بيانات أمر صرف معتمد' : 'تعديل طلب صرف من المخزون'; const btn=[...card.querySelectorAll('button')].find(b=>/إرسال|تحديث|حفظ/.test(b.textContent)); if(btn) btn.textContent='حفظ التعديل'; card.scrollIntoView({behavior:'smooth',block:'start'}); }
    if(r.status==='approved') msg('يمكن تعديل بيانات الأمر المعتمد ومركز التكلفة. الكميات والأصناف لا تتغير بعد الخصم؛ استخدم المرتجع لتعديل الاستهلاك.');
  };

  async function updateRequestMovements(id, payload){
    const mvPayload={};
    ['project_id','project_name','cost_center_id','cost_center_name','receiver','notes'].forEach(k=>{ if(Object.prototype.hasOwnProperty.call(payload,k)) mvPayload[k]=payload[k]; });
    if(payload.reason!==undefined) mvPayload.notes = payload.reason;
    if(Object.keys(mvPayload).length){ try{ await sb.from('inventory_movements').update(mvPayload).eq('request_id', id); }catch(e){ console.warn('movement update skipped:', e.message||e); } }
  }
  window.inventorySaveRequest = async function(btn){
    try{
      if(btn) btn.disabled=true;
      if(typeof ensureRequestHidden === 'function') ensureRequestHidden();
      const pid=$id('inventoryRequestProject')?.value; const supervisorId=$id('inventoryRequestSupervisor')?.value; const requestId=$id('inventoryRequestId')?.value;
      if(!pid) throw new Error('اختر المشروع');
      if(!supervisorId) throw new Error('اختر المشرف / الفني');
      const existing=(window.data?.inventoryRequests||[]).find(x=>String(x.id)===String(requestId));
      const cc=ccPayload('inventoryRequestCostCenter', pid);
      if(!cc.cost_center_name) throw new Error('اختر مركز التكلفة');
      const base={project_id:Number(pid),project_name:projectLabel(pid),cost_center_id:cc.cost_center_id,cost_center_name:cc.cost_center_name,supervisor_id:Number(supervisorId),supervisor_name:supervisorLabel(supervisorId),request_date:$id('inventoryRequestDate')?.value||new Date().toISOString().slice(0,10),reason:$id('inventoryRequestReason')?.value||'',notes:$id('inventoryRequestNotes')?.value||''};
      if(existing && existing.status==='approved'){
        const res=await sb.from('inventory_requests').update(base).eq('id', requestId); if(res.error) throw res.error;
        await updateRequestMovements(requestId, {...base, receiver:base.supervisor_name});
        msg('تم تحديث بيانات أمر الصرف المعتمد ومركز التكلفة');
      }else{
        const lines=Array.isArray(window.inventoryRequestLines)?window.inventoryRequestLines:[];
        if(!lines.length) throw new Error('أضف صنفًا واحدًا على الأقل إلى أمر الصرف');
        const path=(typeof inventoryGetApprovalPath==='function')?inventoryGetApprovalPath():['warehouse']; const first=path[0]||'warehouse'; const totalQty=lines.reduce((a,x)=>a+n(x.quantity),0);
        const row={...base,item_id:Number(lines[0].item_id), item_name:lines.map(x=>x.item_name).join('، '), quantity:totalQty, request_items:lines, items:lines, status:'pending_'+first, current_step:first, approval_path:path, approval_log:[]};
        const res=requestId?await sb.from('inventory_requests').update(row).eq('id',requestId):await sb.from('inventory_requests').insert(row); if(res.error) throw res.error;
        msg(requestId?'تم تحديث أمر الصرف وإعادته لأول مرحلة اعتماد':'تم إرسال أمر الصرف للاعتماد');
      }
      if(typeof inventoryClearRequestForm==='function') inventoryClearRequestForm();
      await financeLoadAll();
    }catch(err){ msg(err.message||String(err),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };

  function usageRowsWithCostCenter(){
    const rows=[];
    (window.data?.inventoryRequests||[]).filter(r=>r.status==='approved').forEach(r=>{
      const date=String(r.request_date||r.created_at||new Date().toISOString().slice(0,10)).slice(0,10);
      lineItems(r).forEach(l=>{
        const it=itemById(l.item_id)||{}; const out=n(l.quantity); const returned=returnQty(r.id,l.item_id); const consumed=Math.max(0,out-returned); const cost=n(l.unit_cost||it.unit_cost); const val=consumed*cost;
        rows.push({date,project:r.project_name||projectLabel(r.project_id),person:r.supervisor_name||supervisorLabel(r.supervisor_id),cost_center_id:r.cost_center_id||null,cost_center_name:r.cost_center_name||r.project_name||projectLabel(r.project_id)||'غير محدد',item:l.item_name||it.name,item_id:l.item_id,product_code:l.product_code||itemCode(it),barcode:l.barcode||itemBarcode(it),supplier:it.supplier||'',out,returned,qty:consumed,consumed,val,vat:vatOf(val,it),gross:grossOf(val,it),reason:r.reason||r.notes||'-',type:'أمر صرف معتمد',ref:'REQ-'+r.id,request_id:r.id,unit_cost:cost,itemObj:it,current:n(it.quantity)});
      });
    });
    (window.data?.inventoryMovements||[]).filter(m=>['out','consume'].includes(m.movement_type)&&!m.request_id).forEach(m=>{
      const it=itemById(m.item_id)||{}; const date=String(m.movement_date||m.created_at||new Date().toISOString().slice(0,10)).slice(0,10); const qty=n(m.quantity); const cost=movementUnitCost(m,it); const val=qty*cost;
      rows.push({date,project:m.project_name||projectLabel(m.project_id)||'بدون مشروع',person:m.receiver||'بدون مستلم',cost_center_id:m.cost_center_id||null,cost_center_name:m.cost_center_name||m.project_name||projectLabel(m.project_id)||'غير محدد',item:m.item_name||it.name,item_id:m.item_id,product_code:m.product_code||itemCode(it),barcode:m.barcode||itemBarcode(it),supplier:it.supplier||'',out:qty,returned:0,qty,consumed:qty,val,vat:vatOf(val,it),gross:grossOf(val,it),reason:m.reason||m.notes||'-',type:m.movement_type==='consume'?'استهلاك مباشر':'صرف مباشر من المخزون',ref:'MOV-'+m.id,unit_cost:cost,itemObj:it,current:n(it.quantity)});
    });
    return rows;
  }
  const oldRenderReports = window.financeRenderReports;
  window.financeRenderReports = function(){
    if(oldRenderReports) oldRenderReports.apply(this, arguments);
    const body=$id('costCenterReportBody'); if(!body) return;
    let rows=usageRowsWithCostCenter();
    const prod=$id('inventoryReportProduct')?.value||''; const person=$id('inventoryReportPerson')?.value||''; const supplier=$id('inventoryReportSupplier')?.value||'';
    if(prod) rows=rows.filter(r=>String(r.item_id)===String(prod));
    if(person) rows=rows.filter(r=>String(r.person)===String(person));
    if(supplier) rows=rows.filter(r=>String(r.supplier||'')===String(supplier));
    const map={};
    rows.forEach(r=>{ const key=r.cost_center_name||'غير محدد'; map[key]=map[key]||{count:0,out:0,ret:0,cons:0,val:0,vat:0,gross:0}; map[key].count++; map[key].out+=n(r.out); map[key].ret+=n(r.returned); map[key].cons+=n(r.consumed); map[key].val+=n(r.val); map[key].vat+=n(r.vat); map[key].gross+=n(r.gross); });
    const vals=Object.entries(map).sort((a,b)=>b[1].gross-a[1].gross);
    body.innerHTML=vals.map(([k,v])=>`<tr><td>${e(k)}</td><td>${v.count}</td><td>${q(v.out)}</td><td>${q(v.ret)}</td><td>${q(v.cons)}</td><td>${v.val.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</td><td>${v.vat.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</td><td>${v.gross.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</td></tr>`).join('') || '<tr><td colspan="8">لا توجد بيانات مراكز تكلفة</td></tr>';
  };

  // Show edit button for approved requests too, because metadata/cost-center editing is now allowed after approval.
  const oldSmartRenderRequests = window.inventoryRenderRequests;
  window.inventoryRenderRequests = function(){
    if(oldSmartRenderRequests) oldSmartRenderRequests.apply(this, arguments);
    try{
      // The smart-card renderer in V129 hides edit for approved cards. Add it back next to return if missing.
      document.querySelectorAll('.smart-card-v129').forEach(card=>{
        const title=card.querySelector('.smart-title-v129')?.textContent||'';
        const m=title.match(/REQ-(\d+)/); if(!m) return;
        const r=(window.data?.inventoryRequests||[]).find(x=>String(x.id)===m[1]);
        if(!r || r.status!=='approved') return;
        const actions=card.querySelector('.smart-card-actions-v129');
        if(actions && !actions.querySelector('[data-v131-edit-approved]')) actions.insertAdjacentHTML('beforeend', `<button class="light" data-v131-edit-approved="1" onclick="inventoryEditRequest('${e(r.id)}')">تعديل البيانات</button>`);
      });
    }catch(_){ }
  };

  window.addEventListener('load',()=>setTimeout(()=>{ try{ window.costCenterFillSelects?.(); bindCostCenterHooks(); window.financeRenderReports?.(); window.inventoryRenderRequests?.(); }catch(err){ console.warn('V131 warning',err); } },1800));
})();

/* ===== V132: Cost code FM/CN + performance and stable project/supervisor labels ===== */
(function(){
  'use strict';
  const $v132 = id => document.getElementById(id);
  const n132 = v => { const x = Number(String(v ?? 0).replace(/,/g,'').trim()); return Number.isFinite(x) ? x : 0; };
  const esc132 = v => String(v ?? '').replace(/[&<>\"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
  const qty132 = v => Number(n132(v)).toLocaleString('en-US',{maximumFractionDigits:2});
  const money132 = v => Number(n132(v)).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}) + ' ر.س';
  const today132 = () => new Date().toISOString().slice(0,10);

  function uniqueById(rows){
    const out=[]; const seen=new Set();
    (rows||[]).forEach(r=>{ const id=String(r && r.id || ''); if(!id || seen.has(id)) return; seen.add(id); out.push(r); });
    return out;
  }
  function projectRows132(){ return uniqueById((window.data && window.data.projects) || []).sort((a,b)=>String(a.name||'').localeCompare(String(b.name||''),'ar')); }
  function userRows132(){ return uniqueById([...(window.data?.users||[]), ...(window.data?.supervisors||[])].filter(Boolean)); }
  function projectName132(id){
    if(id===undefined || id===null || id==='') return '';
    return projectRows132().find(p=>String(p.id)===String(id))?.name || '';
  }
  function supervisorName132(id){
    if(id===undefined || id===null || id==='') return '';
    const u=userRows132().find(x=>String(x.id)===String(id));
    return u?.full_name || u?.name || u?.username || '';
  }

  // تقوية دوال الأسماء لتمنع اختلاط المشاريع والمشرفين عند التحديث أو وجود بيانات مكررة.
  window.projectName = function(id){ return projectName132(id) || '-'; };
  window.supervisorName = function(id){ return supervisorName132(id) || '-'; };

  function fillSelect132(id, rows, placeholder, selected){
    const el=$v132(id); if(!el) return;
    const current = selected !== undefined ? selected : el.value;
    el.innerHTML = `<option value="">${esc132(placeholder||'اختر')}</option>` + (rows||[]).map(r=>`<option value="${esc132(r.id)}">${esc132(r.name||r.full_name||r.username||'')}</option>`).join('');
    if(current!==undefined && current!==null) el.value=String(current);
  }

  function hydrateProjectSupervisorSelects132(){
    const projects=projectRows132().map(p=>({id:p.id,name:p.name}));
    ['financeProjectFilter','financeExpenseProject','inventoryMovementProject','inventoryRequestProject','supInventoryRequestProject'].forEach(id=>{
      const ph = id==='financeProjectFilter' ? 'كل المشاريع' : (id==='inventoryMovementProject' ? 'بدون مشروع' : 'اختر المشروع');
      fillSelect132(id, projects, ph);
    });
    const users=userRows132().filter(u=>{
      const role=String(u.role||'').toLowerCase();
      const ar=String(u.role||u.type||'');
      return !role || ['supervisor','technician','operations_manager','warehouse_manager'].includes(role) || /مشرف|فني|مدير/.test(ar);
    }).map(u=>({id:u.id,name:(u.full_name||u.name||u.username||'') + (u.role ? ' - '+roleArabic132(u.role) : '')}));
    ['inventoryRequestSupervisor','supInventoryRequestSupervisor','inventoryReportPerson'].forEach(id=>{
      const ph = id==='inventoryReportPerson' ? 'كل المشرفين/المستلمين' : 'اختر المشرف / الفني';
      fillSelect132(id, users, ph);
    });
  }
  function roleArabic132(role){
    return ({general_manager:'مدير عام',financial_manager:'مدير مالي',operations_manager:'مدير تشغيلي',warehouse_manager:'مدير مخازن',technician:'فني',supervisor:'مشرف',admin:'مدير عام'})[role] || role;
  }

  function lineItems132(r){
    if(typeof window.v118LineItems === 'function') return window.v118LineItems(r)||[];
    return Array.isArray(r?.request_items) ? r.request_items : (Array.isArray(r?.items) ? r.items : []);
  }
  function item132(id){ return ((window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id)) || {}); }
  function itemCode132(it){ return (typeof window.v118ProductCode==='function') ? window.v118ProductCode(it) : (it?.product_code || it?.serial_number || ''); }
  function barcode132(it){ return (typeof window.v118Barcode==='function') ? window.v118Barcode(it) : (it?.barcode || it?.supplier_barcode || ''); }
  function returnQty132(requestId,itemId){ return (window.data?.inventoryMovements||[]).filter(m=>String(m.request_id||'')===String(requestId||'') && String(m.item_id||'')===String(itemId||'') && String(m.movement_type)==='return').reduce((a,m)=>a+n132(m.quantity),0); }
  function vat132(amount,it){ return (typeof window.vatOfAmount==='function') ? window.vatOfAmount(amount,it) : amount*0.15; }
  function gross132(amount,it){ return (typeof window.grossOfAmount==='function') ? window.grossOfAmount(amount,it) : amount + vat132(amount,it); }

  function costCenters132(){
    const saved=(window.data?.costCenters||[]).filter(c=>String(c.status||'active')!=='inactive');
    const savedNames=new Set(saved.map(c=>String(c.name||'')));
    const pseudo=projectRows132().filter(p=>!savedNames.has(String(p.name||''))).map(p=>({id:'project-'+p.id, code:'FM', name:p.name, type:'مشروع', project_id:p.id, pseudo:true}));
    return [...saved, ...pseudo];
  }
  function costCenterByName132(name){ return costCenters132().find(c=>String(c.name||'')===String(name||'')); }
  function costCode132(name, sampleRow){
    const c = (sampleRow?.cost_center_id ? costCenters132().find(x=>String(x.id)===String(sampleRow.cost_center_id)) : null) || costCenterByName132(name);
    const type=String(c?.type||'');
    const projectMatch = !!(c?.project_id) || type.includes('مشروع') || projectRows132().some(p=>String(p.name||'')===String(name||''));
    return projectMatch ? 'FM' : 'CN';
  }

  function usageRows132(){
    const rows=[];
    (window.data?.inventoryRequests||[]).filter(r=>String(r.status)==='approved').forEach(r=>{
      const date=String(r.request_date||r.created_at||today132()).slice(0,10);
      const pName=r.project_name || projectName132(r.project_id) || 'بدون مشروع';
      const sName=r.supervisor_name || supervisorName132(r.supervisor_id) || 'بدون مستلم';
      lineItems132(r).forEach(l=>{
        const it=item132(l.item_id); const out=n132(l.quantity); const returned=returnQty132(r.id,l.item_id); const consumed=Math.max(0,out-returned); const cost=n132(l.unit_cost || it.unit_cost); const val=consumed*cost;
        rows.push({date, project:pName, person:sName, cost_center_id:r.cost_center_id||null, cost_center_name:r.cost_center_name||pName||'غير محدد', item:l.item_name||it.name||'', item_id:l.item_id, product_code:l.product_code||itemCode132(it), barcode:l.barcode||barcode132(it), supplier:it.supplier||'', out, returned, consumed, val, vat:vat132(val,it), gross:gross132(val,it), ref:'REQ-'+r.id});
      });
    });
    (window.data?.inventoryMovements||[]).filter(m=>['out','consume'].includes(String(m.movement_type)) && !m.request_id).forEach(m=>{
      const it=item132(m.item_id); const date=String(m.movement_date||m.created_at||today132()).slice(0,10); const qty=n132(m.quantity); const cost=n132(m.unit_cost||it.unit_cost); const val=qty*cost;
      const pName=m.project_name || projectName132(m.project_id) || 'بدون مشروع';
      rows.push({date, project:pName, person:m.receiver||'بدون مستلم', cost_center_id:m.cost_center_id||null, cost_center_name:m.cost_center_name||pName||'غير محدد', item:m.item_name||it.name||'', item_id:m.item_id, product_code:m.product_code||itemCode132(it), barcode:m.barcode||barcode132(it), supplier:it.supplier||'', out:qty, returned:0, consumed:qty, val, vat:vat132(val,it), gross:gross132(val,it), ref:'MOV-'+m.id});
    });
    return rows;
  }

  function filteredRows132(){
    let rows=usageRows132();
    const prod=$v132('inventoryReportProduct')?.value||'';
    const person=$v132('inventoryReportPerson')?.value||'';
    const supplier=$v132('inventoryReportSupplier')?.value||'';
    if(prod) rows=rows.filter(r=>String(r.item_id)===String(prod));
    if(person){ const personName=supervisorName132(person); rows=rows.filter(r=>String(r.person)===String(personName) || String(r.person)===String(person)); }
    if(supplier) rows=rows.filter(r=>String(r.supplier||'')===String(supplier));
    return rows;
  }

  function renderCostCenterReport132(){
    const body=$v132('costCenterReportBody'); if(!body) return;
    const map={};
    filteredRows132().forEach(r=>{
      const key=r.cost_center_name||'غير محدد';
      if(!map[key]) map[key]={sample:r, code:costCode132(key,r), count:0,out:0,ret:0,cons:0,val:0,vat:0,gross:0};
      const v=map[key]; v.count++; v.out+=n132(r.out); v.ret+=n132(r.returned); v.cons+=n132(r.consumed); v.val+=n132(r.val); v.vat+=n132(r.vat); v.gross+=n132(r.gross);
    });
    const vals=Object.entries(map).sort((a,b)=>b[1].gross-a[1].gross);
    body.innerHTML = vals.map(([k,v])=>`<tr><td>${esc132(k)}</td><td><span class="badge ${v.code==='FM'?'green':'neutral'}">${v.code}</span></td><td>${v.count}</td><td>${qty132(v.out)}</td><td>${qty132(v.ret)}</td><td>${qty132(v.cons)}</td><td>${money132(v.val)}</td><td>${money132(v.vat)}</td><td>${money132(v.gross)}</td></tr>`).join('') || '<tr><td colspan="9">لا توجد بيانات مراكز تكلفة</td></tr>';
  }

  // تقرير الطباعة لمراكز التكلفة مع رمز التكلفة
  const oldFinancePrintReport132 = window.financePrintReport;
  window.financePrintReport = function(kind){
    if(kind==='costCenters'){
      renderCostCenterReport132();
      const table=$v132('costCenterReportBody')?.closest('table');
      if(typeof window.financePrintWindow === 'function') return window.financePrintWindow('تقرير مراكز التكلفة', table?`<table>${table.innerHTML}</table>`:'<p>لا توجد بيانات</p>');
    }
    return oldFinancePrintReport132 ? oldFinancePrintReport132.apply(this, arguments) : undefined;
  };

  // تخفيف البطء: لا نعيد رسم كل صفحات المصروفات والمخزون في كل حركة، بل الصفحة الظاهرة فقط.
  const oldRenderAll132 = window.financeRenderAll;
  window.financeRenderAll = function(){
    if(!$v132('financeDashboard')) return;
    try{ if(typeof window.financeRenderKpis==='function') window.financeRenderKpis(); }catch(_){ }
    try{ if(typeof window.financeRenderRecent==='function') window.financeRenderRecent(); }catch(_){ }
    const active=document.querySelector('.finance-tab-page:not(.hidden)')?.id || '';
    const map={
      financeTabExpenses: 'financeRenderExpenses',
      financeTabInventory: 'inventoryRenderItems',
      financeTabMovements: 'inventoryRenderMovements',
      financeTabRequests: 'inventoryRenderRequests',
      financeTabCostCenters: 'costCenterRender',
      financeTabReports: 'financeRenderReports'
    };
    const fn=map[active];
    if(fn && typeof window[fn]==='function') window[fn]();
    else if(oldRenderAll132) oldRenderAll132.apply(this, arguments);
  };

  const oldFinanceRenderReports132 = window.financeRenderReports;
  window.financeRenderReports = function(){
    if(oldFinanceRenderReports132) oldFinanceRenderReports132.apply(this, arguments);
    renderCostCenterReport132();
  };

  // ضبط القوائم لمنع اختلاط المشاريع والمشرفين بعد أي تحميل أو تحديث.
  const oldHydrate132 = window.financeHydrateForms;
  window.financeHydrateForms = function(){
    if(oldHydrate132) oldHydrate132.apply(this, arguments);
    hydrateProjectSupervisorSelects132();
    if(typeof window.costCenterFillSelects==='function') window.costCenterFillSelects();
  };

  // تحديث مركز التكلفة بعد تغيير المشروع بدون تأخير أو بقاء قيمة قديمة.
  function bindProjectToCostCenter132(){
    [['inventoryRequestProject','inventoryRequestCostCenter'],['inventoryMovementProject','inventoryMovementCostCenter'],['financeExpenseProject','financeExpenseCostCenter'],['supInventoryRequestProject','supInventoryRequestCostCenter']].forEach(([p,c])=>{
      const pe=$v132(p); if(!pe || pe.dataset.v132bind) return; pe.dataset.v132bind='1';
      pe.addEventListener('change',()=>{
        const pid=pe.value; const cc=costCenters132().find(x=>String(x.project_id||'')===String(pid)) || costCenters132().find(x=>String(x.name||'')===String(projectName132(pid)));
        if(cc && $v132(c)) $v132(c).value=String(cc.id);
      });
    });
  }

  window.addEventListener('load',()=>setTimeout(()=>{
    try{ hydrateProjectSupervisorSelects132(); bindProjectToCostCenter132(); renderCostCenterReport132(); }catch(e){ console.warn('V132 warning', e); }
  },1200));
})();


/* ===================== V133 Client Daily Reports + Smart Filters ===================== */
(function(){
  const $v133 = id => document.getElementById(id);
  const MAX_STAGE_PHOTOS_V133 = 9;
  const DAILY_TYPES_V133 = ['النظافة اليومية','إزالة النفايات','تنظيف الممرات','تنظيف المصاعد','تنظيف البيسمنت','تنظيف الأسطح','مكافحة الحشرات','غسيل خزانات','صيانة كهرباء','صيانة سباكة','شريحة مخصصة','أخرى'];
  let supClientServicesState = [];
  window.supClientServicesState = supClientServicesState;
  function v133Esc(v){ return (typeof esc==='function') ? esc(v) : String(v??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])); }
  function v133Msg(t,kind){ try{ msg(t,kind); }catch(_){ alert(t); } }
  function v133ProjectName(pid){ try{return projectName(pid)}catch(_){ const p=(data.projects||[]).find(x=>String(x.id)===String(pid)); return p?.name||''; } }
  function v133DefaultSummary(){ return 'تم تنفيذ الأعمال اليومية للمشروع حسب الخطة التشغيلية، مع توثيق الخدمات المنفذة بالصور، ورفع التقرير للإدارة لاعتماده قبل ظهوره للعميل.'; }
  function v133ServiceTemplate(t){ return {service_type:t||'النظافة اليومية', title:t||'النظافة اليومية', service_description:'', scope_work:'', notes:'', before_images:[], during_images:[], after_images:[]}; }
  function v133CountImages(s){ return (s.before_images||[]).length+(s.during_images||[]).length+(s.after_images||[]).length; }

  // Admin reports: enforce 9 images per stage as requested
  const oldHandlePremiumImages133 = window.handlePremiumImages;
  window.handlePremiumImages = async function(i, field, input){
    if(!premiumServicesState[i]) return;
    const current = premiumServicesState[i][field] || [];
    const files=[...(input.files||[])].slice(0, Math.max(0, MAX_STAGE_PHOTOS_V133-current.length));
    if(!files.length){ v133Msg('الحد الأقصى 9 صور لكل قسم في الخدمة','err'); input.value=''; return; }
    v133Msg('جاري تجهيز الصور...');
    for(const f of files){
      try{ const data=await compressImageToDataUrl(f, 1400, 0.82); current.push({name:f.name,type:'image/jpeg',data}); }
      catch(e){ console.warn(e); }
    }
    premiumServicesState[i][field]=current.slice(0,MAX_STAGE_PHOTOS_V133);
    if((input.files||[]).length>files.length) v133Msg('تمت إضافة الصور مع الاكتفاء بأول 9 صور لكل قسم');
    else v133Msg('تمت إضافة الصور');
    renderPremiumServicesEditor(); input.value='';
  };

  // Supervisor daily client reports
  window.supClientReportInit = function(){
    try{
      fillSelect('supClientReportProject', data.projects||[], 'name', 'اختر المشروع');
      if($v133('supClientReportDate') && !$v133('supClientReportDate').value) $v133('supClientReportDate').value=today();
      if(!supClientServicesState.length){ supClientServicesState=[v133ServiceTemplate('النظافة اليومية')]; window.supClientServicesState = supClientServicesState; }
      supClientRenderServices(); supClientRenderMyReports();
    }catch(e){ console.warn(e); }
  };
  window.supClientReportProjectChanged = function(){
    const p=v133ProjectName($v133('supClientReportProject')?.value);
    // V156: عنوان تقرير المشرف يتغير تلقائيًا دائمًا حسب المشروع المختار
    if($v133('supClientReportTitle')) $v133('supClientReportTitle').value = p && p !== '-' ? ('تقرير يومي - ' + p) : 'تقرير يومي للعميل';
    if($v133('supClientReportSummary')) $v133('supClientReportSummary').value = v133DefaultSummary();
  };
  window.supClientReportReset = function(){
    supClientServicesState=[v133ServiceTemplate('النظافة اليومية')];
    window.supClientServicesState = supClientServicesState;
    ['supClientReportTitle','supClientReportSummary'].forEach(id=>{ if($v133(id)) $v133(id).value=''; });
    if($v133('supClientReportDate')) $v133('supClientReportDate').value=today();
    supClientRenderServices();
  };
  let supClientActiveSlideIndex = null;
  window.supClientAddService = function(){
    supClientServicesState.push(v133ServiceTemplate($v133('supClientReportServiceType')?.value||'النظافة اليومية'));
    window.supClientServicesState = supClientServicesState;
    supClientRenderServices();
  };
  window.supClientRemoveService = function(i){
    if(!confirm('حذف هذه الشريحة؟')) return;
    supClientServicesState.splice(i,1);
    window.supClientServicesState = supClientServicesState;
    if(!supClientServicesState.length){ supClientServicesState=[v133ServiceTemplate('النظافة اليومية')]; window.supClientServicesState = supClientServicesState; }
    supClientCloseSlideModal();
    supClientRenderServices();
  };
  window.supClientSetService = function(i,f,v){ if(supClientServicesState[i]) supClientServicesState[i][f]=v; supClientRenderServices(); if(supClientActiveSlideIndex===i) supClientRenderSlideModal(); };
  window.supClientRemoveImg = function(i,f,j){ supClientServicesState[i]?.[f]?.splice(j,1); supClientRenderServices(); if(supClientActiveSlideIndex===i) supClientRenderSlideModal(); };
  window.supClientHandleImages = async function(i, field, input){
    const s=supClientServicesState[i]; if(!s) return;
    const arr=s[field]||[]; const allowed=MAX_STAGE_PHOTOS_V133-arr.length;
    const files=[...(input.files||[])].slice(0, Math.max(0,allowed));
    if(!files.length){ v133Msg('الحد الأقصى 9 صور لكل قسم','err'); input.value=''; return; }
    v133Msg('جاري تجهيز الصور...');
    for(const f of files){ try{ const data=await compressImageToDataUrl(f, 1200, .80); arr.push({name:f.name,type:'image/jpeg',data}); }catch(e){console.warn(e)} }
    s[field]=arr.slice(0,MAX_STAGE_PHOTOS_V133);
    if((input.files||[]).length>files.length) v133Msg('تمت إضافة الصور مع الاكتفاء بـ 9 صور لكل قسم'); else v133Msg('تمت إضافة الصور');
    input.value='';
    supClientRenderServices();
    if(supClientActiveSlideIndex===i) supClientRenderSlideModal();
  };
  function supClientStageCount(s, field){ return (s?.[field]||[]).length; }
  function supClientSlideStatus(s){ const total=v133CountImages(s); return total>0 ? 'جاهزة للمراجعة' : 'بانتظار الصور'; }
  function supClientStageLabel(field){ return field==='before_images'?'قبل التنفيذ':(field==='during_images'?'أثناء التنفيذ':'بعد التنفيذ'); }
  function supClientStageModalBox(i, field){
    const imgs=supClientServicesState[i]?.[field]||[];
    const title=supClientStageLabel(field);
    return `<div class="smart-stage-panel"><div class="smart-stage-head"><div><b>${title}</b><small>${imgs.length}/9 صور</small></div><label class="smart-upload-btn">+ إضافة صور<input type="file" accept="image/*" multiple onchange="supClientHandleImages(${i},'${field}',this)"></label></div><div class="smart-img-grid">${imgs.map((im,j)=>`<div class="smart-img-tile"><img src="${im.data||''}" alt="${title}"><button type="button" onclick="supClientRemoveImg(${i},'${field}',${j})">حذف</button></div>`).join('') || '<div class="empty-stage-note">لا توجد صور في هذا القسم</div>'}</div></div>`;
  }
  window.supClientOpenSlideModal = function(i){ supClientActiveSlideIndex=i; supClientRenderSlideModal(); const m=$v133('supClientSlideModal'); if(m) m.classList.remove('hidden'); };
  window.supClientCloseSlideModal = function(){ supClientActiveSlideIndex=null; const m=$v133('supClientSlideModal'); if(m) m.classList.add('hidden'); };
  window.supClientRenderSlideModal = function(){
    const i=supClientActiveSlideIndex; const s=supClientServicesState[i]; const body=$v133('supClientSlideModalBody'); if(!s || !body) return;
    const ttl=$v133('supClientSlideModalTitle'); if(ttl) ttl.textContent=s.title||s.service_type||'شريحة الخدمة';
    const sub=$v133('supClientSlideModalSub'); if(sub) sub.textContent='ارفع الصور فقط. الأقسام الفارغة لن تظهر في تقرير العميل.';
    body.innerHTML=`<div class="smart-slide-edit-head"><div><label>نوع الشريحة</label><select onchange="supClientSetService(${i},'service_type',this.value); supClientSetService(${i},'title',this.value)">${DAILY_TYPES_V133.map(t=>`<option ${t===s.service_type?'selected':''}>${v133Esc(t)}</option>`).join('')}</select></div><div><label>اسم الشريحة</label><input value="${v133Esc(s.title||'')}" oninput="supClientSetService(${i},'title',this.value)" placeholder="مثال: تنظيف الممرات"></div></div><div class="smart-stage-grid-modal">${supClientStageModalBox(i,'before_images')}${supClientStageModalBox(i,'during_images')}${supClientStageModalBox(i,'after_images')}</div>`;
  };
  function supClientGatewayIcon(type){
    const t=String(type||'');
    if(t.includes('بيسمنت')) return '🅿️';
    if(t.includes('حشرات')) return '🛡️';
    if(t.includes('أسطح')) return '🏢';
    if(t.includes('خزانات')) return '💧';
    if(t.includes('مكيف')) return '❄️';
    if(t.includes('كهرب')) return '💡';
    if(t.includes('سباك')) return '🔧';
    if(t.includes('نفايات')) return '🗑️';
    if(t.includes('مصاعد')) return '🛗';
    if(t.includes('ممرات')) return '🧹';
    return '📷';
  }
  function supClientRenderServices(){
    const box=$v133('supClientServicesEditor'); if(!box) return;
    box.innerHTML=supClientServicesState.map((s,i)=>{
      const total=v133CountImages(s);
      const status=total>0?'مكتمل':'بانتظار الصور';
      const statusClass=total>0?'complete':'pending';
      return `<button type="button" class="smart-service-gateway-v136 ${total>0?'has-images':''}" onclick="supClientOpenSlideModal(${i})" aria-label="فتح شريحة ${v133Esc(s.title||s.service_type)}">
        <span class="gateway-icon-v136">${supClientGatewayIcon(s.service_type||s.title)}</span>
        <span class="gateway-title-v136">${v133Esc(s.title||s.service_type)}</span>
        <span class="gateway-badges-v136"><span>${total} صورة</span><span class="${statusClass}">${status}</span></span>
        <span class="gateway-actions-v136"><span>فتح الشريحة</span><span class="gateway-delete-v136" onclick="event.stopPropagation(); supClientRemoveService(${i});">حذف</span></span>
      </button>`;
    }).join('');
  }

  window.supClientSaveDailyReport = async function(btn){
    try{
      if(btn) btn.disabled=true;
      const pid=$v133('supClientReportProject')?.value; if(!pid) throw new Error('اختر المشروع');
      const valid=supClientServicesState.filter(s=>v133CountImages(s)>0);
      if(!valid.length) throw new Error('أضف صورًا لخدمة واحدة على الأقل');
      const u=session()||{}; const p=v133ProjectName(pid); const reportDate=$v133('supClientReportDate')?.value||today();
      const reportRow={report_no:genReportNo(), project_id:Number(pid), project_name:p, chairman_name:'', chairman_phone:'', title:('تقرير يومي - '+p), report_type:'تقرير يومي من المشرف', report_date:reportDate, executive_summary:$v133('supClientReportSummary')?.value||v133DefaultSummary(), status:'draft', public_token:null};
      const {data:rep,error}=await sb.from('client_reports').insert(reportRow).select('id').single(); if(error) throw error;
      const rows=valid.map((s,i)=>({report_id:rep.id,sort_order:i+1,service_type:s.service_type,title:s.title||s.service_type,service_description:'',scope_work:'',notes:`المشرف: ${u.full_name||u.username||''}`,before_images:s.before_images||[],during_images:s.during_images||[],after_images:s.after_images||[]}));
      const {error:se}=await sb.from('client_report_services').insert(rows); if(se) throw se;
      v133Msg('تم إرسال التقرير اليومي للإدارة كمسودة');
      supClientReportReset(); await loadPremiumReportsOnly(false); supClientRenderMyReports();
    }catch(e){ v133Msg(e.message||String(e),'err'); } finally{ if(btn) btn.disabled=false; }
  };
  function supClientRenderMyReports(){
    const box=$v133('supClientReportsList'); if(!box) return;
    const u=session()||{}; const name=(u.full_name||u.username||'');
    const reports=(data.clientReports||[]).filter(r=>String(r.report_type||'').includes('المشرف') || (getReportServices(r.id)||[]).some(s=>String(s.notes||'').includes(name))).slice(0,20);
    box.innerHTML=reports.map(r=>`<div class="quick-item"><div><b>${v133Esc(r.title)}</b><br><small>${v133Esc(r.project_name)} - ${v133Esc(r.report_date)} - ${reportStatusText(r.status)}</small></div><span class="badge ${reportStatusClass(r.status)}">${v133Esc(reportStatusText(r.status))}</span></div>`).join('') || '<div class="quick-item">لا توجد تقارير يومية بعد</div>';
  }

  // Override initSupervisor to prepare daily report UI after normal loading
  const oldInitSup133 = window.initSupervisor;
  window.initSupervisor = async function(){
    if(oldInitSup133) await oldInitSup133.apply(this, arguments);
    try{ supClientReportInit(); }catch(e){ console.warn('V133 supervisor daily report warning',e); }
  };

  // Project/service smart slides in client reports admin
  function renderClientReportProjectSlides133(){
    const box=$v133('clientReportProjectSlides'); if(!box) return;
    const reports=data.clientReports||[]; const services=data.clientReportServices||[];
    const by=new Map();
    reports.forEach(r=>{ const k=String(r.project_id||r.project_name); if(!by.has(k)) by.set(k,{project_id:r.project_id,project:r.project_name,total:0,published:0,draft:0,services:{}}); const g=by.get(k); g.total++; if(r.status==='published') g.published++; if(r.status==='draft') g.draft++; (services.filter(s=>String(s.report_id)===String(r.id))).forEach(s=>{ const nm=s.service_type||s.title||'خدمة'; g.services[nm]=(g.services[nm]||0)+1; }); });
    const cards=[...by.values()].sort((a,b)=>b.published-a.published).map(g=>`<button type="button" class="smart-report-card" onclick="document.getElementById('premiumReportFilterProject').value='${g.project_id||''}'; renderPremiumReports(); this.scrollIntoView({behavior:'smooth',block:'nearest'})"><span>${v133Esc(g.project)}</span><b>${g.published} منشور / ${g.total} تقرير</b><small>${Object.entries(g.services).slice(0,4).map(([n,c])=>`${v133Esc(n)}: ${c}`).join(' | ')||'لا توجد خدمات'}</small></button>`).join('');
    box.innerHTML=cards||'<div class="footer-note">لا توجد تقارير بعد</div>';
    const dl=$v133('premiumServiceSmartList'); if(dl){ const names=new Set(); reports.forEach(r=>{names.add(r.project_name);names.add(r.title);}); services.forEach(s=>{names.add(s.service_type);names.add(s.title);}); dl.innerHTML=[...names].filter(Boolean).map(x=>`<option value="${v133Esc(x)}"></option>`).join(''); }
  }
  const oldRenderPremiumReports133 = window.renderPremiumReports;
  window.renderPremiumReports = function(){
    if(oldRenderPremiumReports133) oldRenderPremiumReports133.apply(this, arguments);
    const smart=($v133('premiumServiceSmartSearch')?.value||'').trim();
    if(smart && $v133('premiumReportSearch')){ /* keep original search untouched; hide rows post-render */
      const rows=[...document.querySelectorAll('#premiumReportsBody tr')]; rows.forEach(tr=>{ tr.style.display=tr.textContent.includes(smart)?'':'none'; });
    }
    renderClientReportProjectSlides133();
  };

  // Finance/inventory smart filters: cost code + quick search + existing date
  function normalize133(v){ return String(v??'').toLowerCase().trim(); }
  function applyFinanceReportRowFilters133(){
    const q=normalize133($v133('financeReportQuickSearch')?.value||''); const code=$v133('financeReportCostCodeFilter')?.value||'';
    document.querySelectorAll('.smart-report-panel table tbody tr').forEach(tr=>{
      if(tr.querySelector('td[colspan]')) return;
      const txt=normalize133(tr.textContent); let show=true;
      if(q && !txt.includes(q)) show=false;
      if(code){
        const ccText=txt.toUpperCase();
        if(!ccText.includes(code)) show=false;
      }
      tr.style.display=show?'':'none';
    });
  }
  const oldFinanceRenderReports133 = window.financeRenderReports;
  window.financeRenderReports = function(){
    if(oldFinanceRenderReports133) oldFinanceRenderReports133.apply(this, arguments);
    setTimeout(applyFinanceReportRowFilters133,0);
  };

  // Add a cost code/date/search filter row to main expenses & inventory overview if missing
  function injectFinanceMainFilters133(){
    const panel=$v133('financeDashboard'); if(!panel || $v133('financeCostCodeMainFilter')) return;
    const target=$v133('financeSearch')?.closest('.filters'); if(!target) return;
    const cc=document.createElement('select'); cc.id='financeCostCodeMainFilter'; cc.innerHTML='<option value="">كل رموز التكلفة</option><option value="FM">FM</option><option value="CN">CN</option>'; cc.onchange=()=>{ if(typeof financeRenderAll==='function') financeRenderAll(); };
    const date=document.createElement('input'); date.id='financeExactDateFilter'; date.type='date'; date.onchange=()=>{ if(typeof financeRenderAll==='function') financeRenderAll(); };
    target.insertBefore(cc, target.lastElementChild); target.insertBefore(date, target.lastElementChild);
  }
  const oldFinanceRenderAll133 = window.financeRenderAll;
  window.financeRenderAll = function(){
    if(oldFinanceRenderAll133) oldFinanceRenderAll133.apply(this, arguments);
    injectFinanceMainFilters133();
  };

  // Post-filter smart cards/tables in finance tabs by quick search/date where possible
  function filterFinanceVisibleRows133(){
    const q=normalize133($v133('financeSearch')?.value||''); const exact=$v133('financeExactDateFilter')?.value||'';
    ['financeExpensesBody','inventoryMovementsBody','inventoryRequestsBody'].forEach(id=>{
      document.querySelectorAll(`#${id} tr`).forEach(tr=>{
        const txt=normalize133(tr.textContent); let show=true; if(q && !txt.includes(q)) show=false; if(exact && !txt.includes(exact)) show=false; tr.style.display=show?'':'none';
      });
    });
  }
  const oldInvRenderReq133=window.inventoryRenderRequests;
  window.inventoryRenderRequests=function(){ if(oldInvRenderReq133) oldInvRenderReq133.apply(this,arguments); setTimeout(filterFinanceVisibleRows133,0); };
  const oldInvRenderMov133=window.inventoryRenderMovements;
  window.inventoryRenderMovements=function(){ if(oldInvRenderMov133) oldInvRenderMov133.apply(this,arguments); setTimeout(filterFinanceVisibleRows133,0); };
  const oldFinanceRenderExp133=window.financeRenderExpenses;
  window.financeRenderExpenses=function(){ if(oldFinanceRenderExp133) oldFinanceRenderExp133.apply(this,arguments); setTimeout(filterFinanceVisibleRows133,0); };

  // Styles for smart report/project cards if missing
  const st=document.createElement('style'); st.textContent=`
    .stage-box .stage-upload input{display:none}.img-previews{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}.preview-img{position:relative;width:72px;height:72px;border:1px solid #dce6e2;border-radius:12px;overflow:hidden;background:#f7fbfa}.preview-img img{width:100%;height:100%;object-fit:cover}.preview-img button{position:absolute;top:2px;left:2px;border-radius:8px;padding:1px 6px;background:#b83232}.nested-card{background:#fbfdfc;border:1px solid #dce6e2;border-radius:18px;padding:12px;margin:10px 0}.smart-report-cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:10px}.smart-report-card{text-align:right;border:1px solid #dce6e2;background:#fff;color:#0A4033;border-radius:18px;padding:14px;display:grid;gap:6px;min-height:95px}.smart-report-card b{font-size:18px}.smart-report-card small{color:#60706a;line-height:1.6}`;
  document.head.appendChild(st);

  window.addEventListener('load',()=>setTimeout(()=>{ try{ injectFinanceMainFilters133(); renderClientReportProjectSlides133(); }catch(e){ console.warn('V133 load warning',e); } },900));
})();


/* ===================== V134 Smart Daily Client Slides ===================== */
(function(){
  function v134Ready(fn){ if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',fn); else fn(); }
  v134Ready(()=>{
    try{
      const note=document.createElement('style');
      note.textContent=`
      .daily-photo-slide textarea{display:none!important}
      .smart-admin-note{background:#ecfdf5;border:1px solid #b7e4cf;border-radius:14px;padding:10px 12px;margin:10px 0;color:#064e3b;font-weight:700}
      .premium-slide-helper{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}
      `;
      document.head.appendChild(note);
    }catch(e){}
  });
})();

/* ===== V138: Reliable inventory report filters + clean printing + full-month monthly records ===== */
(function(){
  'use strict';
  const $v138 = id => document.getElementById(id);
  const n138 = v => { const x = Number(String(v ?? 0).replace(/,/g,'').trim()); return Number.isFinite(x) ? x : 0; };
  const esc138 = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money138 = v => Number(n138(v)).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}) + ' ر.س';
  const qty138 = v => Number(n138(v)).toLocaleString('en-US',{maximumFractionDigits:2});
  const today138 = () => new Date().toISOString().slice(0,10);
  const dateOnly138 = v => String(v || '').slice(0,10);
  const monthStart138 = m => (m || today138().slice(0,7)) + '-01';
  const monthEnd138 = m => {
    m = m || today138().slice(0,7);
    const [y,mo] = m.split('-').map(Number);
    const d = new Date(y, mo, 0).getDate();
    return `${m}-${String(d).padStart(2,'0')}`;
  };
  function inRange138(d,start,end){ d=dateOnly138(d); return !!d && (!start || d>=start) && (!end || d<=end); }
  function projectRows138(){
    const rows = (window.data?.projects || []);
    const seen = new Set();
    return rows.filter(p=>{ const id=String(p.id||''); if(!id || seen.has(id)) return false; seen.add(id); return true; });
  }
  function userRows138(){
    const seen = new Set();
    return [...(window.data?.users||[]), ...(window.data?.supervisors||[])].filter(u=>{ const id=String(u.id||''); if(!id || seen.has(id)) return false; seen.add(id); return true; });
  }
  function projectName138(id){ return projectRows138().find(p=>String(p.id)===String(id))?.name || (typeof window.projectName==='function' ? window.projectName(id) : '') || ''; }
  function personName138(id){ const u=userRows138().find(x=>String(x.id)===String(id)); return u?.full_name || u?.name || u?.username || (typeof window.supervisorName==='function'?window.supervisorName(id):'') || ''; }
  function item138(id){ return (window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id)) || {}; }
  function code138(it){ return (typeof window.v118ProductCode==='function' ? window.v118ProductCode(it) : '') || it.product_code || it.serial_number || it.code || ''; }
  function barcode138(it){ return (typeof window.v118Barcode==='function' ? window.v118Barcode(it) : '') || it.barcode || it.supplier_barcode || ''; }
  function lineItems138(r){
    if(typeof window.v118LineItems === 'function') return window.v118LineItems(r) || [];
    return Array.isArray(r?.request_items) ? r.request_items : (Array.isArray(r?.items) ? r.items : []);
  }
  function returnQty138(requestId,itemId){
    return (window.data?.inventoryMovements||[])
      .filter(m=>String(m.request_id||'')===String(requestId||'') && String(m.item_id||'')===String(itemId||'') && String(m.movement_type)==='return')
      .reduce((a,m)=>a+n138(m.quantity),0);
  }
  function itemUnitCost138(it){ return n138(it.unit_cost ?? it.unit_price ?? it.price ?? 0); }
  function vatAmount138(amount,it){
    const mode = String(it?.vat_mode || it?.tax_mode || 'exclusive');
    amount = n138(amount);
    if(mode==='zero' || mode==='none' || mode==='خالي الضريبة') return 0;
    if(mode==='inclusive' || mode==='شامل الضريبة') return amount - (amount / 1.15);
    return amount * 0.15;
  }
  function grossAmount138(amount,it){
    const mode = String(it?.vat_mode || it?.tax_mode || 'exclusive');
    amount = n138(amount);
    if(mode==='zero' || mode==='none' || mode==='خالي الضريبة') return amount;
    if(mode==='inclusive' || mode==='شامل الضريبة') return amount;
    return amount * 1.15;
  }
  function unitWithVat138(it){
    const cost = itemUnitCost138(it);
    return grossAmount138(cost,it);
  }
  function daysAgo138(d){
    if(!d) return '-';
    const a = new Date(dateOnly138(d)+'T00:00:00');
    const b = new Date(today138()+'T00:00:00');
    if(isNaN(a)) return '-';
    const diff = Math.max(0, Math.round((b-a)/86400000));
    return diff===0 ? 'اليوم' : (diff===1 ? 'منذ يوم' : `منذ ${diff} يوم`);
  }
  function costCode138(name,row){
    const projectMatch = !!row?.project_id || projectRows138().some(p=>String(p.name||'')===String(name||''));
    const type = String(row?.cost_center_type || row?.cost_type || '');
    if(type.includes('مشروع')) return 'FM';
    return projectMatch ? 'FM' : 'CN';
  }
  function reportDatePass138(date){
    date=dateOnly138(date);
    const mode=$v138('inventoryReportDateMode')?.value || 'all';
    if(mode==='day'){
      const d=$v138('inventoryReportDay')?.value || today138();
      return date===d;
    }
    if(mode==='month'){
      const m=$v138('inventoryReportMonth')?.value || today138().slice(0,7);
      return inRange138(date, monthStart138(m), monthEnd138(m));
    }
    return true;
  }
  function quickPass138(row){
    const q=($v138('financeReportQuickSearch')?.value || '').trim().toLowerCase();
    if(!q) return true;
    return [row.project,row.person,row.item,row.product_code,row.barcode,row.supplier,row.reason,row.ref,row.cost_center_name]
      .join(' ').toLowerCase().includes(q);
  }
  function codePass138(row){
    const code=$v138('financeReportCostCodeFilter')?.value || '';
    if(!code) return true;
    return costCode138(row.cost_center_name,row)===code;
  }
  function productPass138(row){ const prod=$v138('inventoryReportProduct')?.value || ''; return !prod || String(row.item_id)===String(prod); }
  function supplierPass138(row){ const sup=$v138('inventoryReportSupplier')?.value || ''; return !sup || String(row.supplier||'')===String(sup); }
  function personPass138(row){
    const person=$v138('inventoryReportPerson')?.value || '';
    if(!person) return true;
    const pName=personName138(person);
    return String(row.person)===String(pName) || String(row.person_id||'')===String(person) || String(row.person)===String(person);
  }
  function usageRows138(){
    const rows=[];
    (window.data?.inventoryRequests||[]).filter(r=>String(r.status)==='approved').forEach(r=>{
      const date = dateOnly138(r.request_date || r.created_at || today138());
      const project = r.project_name || projectName138(r.project_id) || 'بدون مشروع';
      const person = r.supervisor_name || personName138(r.supervisor_id) || 'بدون مستلم';
      lineItems138(r).forEach(l=>{
        const it=item138(l.item_id); const out=n138(l.quantity); const returned=returnQty138(r.id,l.item_id); const consumed=Math.max(0,out-returned);
        const unit=n138(l.unit_cost || it.unit_cost || it.unit_price || 0); const val=consumed*unit;
        rows.push({date, project, project_id:r.project_id, person, person_id:r.supervisor_id, item:l.item_name||it.name||'', item_id:l.item_id, product_code:l.product_code||code138(it), barcode:l.barcode||barcode138(it), supplier:it.supplier||'', out, returned, consumed, qty:consumed, current:n138(it.quantity), unit_cost:unit, unit_gross:grossAmount138(unit,it), val, vat:vatAmount138(val,it), gross:grossAmount138(val,it), reason:r.reason||r.notes||'-', type:'أمر صرف معتمد', ref:'REQ-'+r.id, cost_center_id:r.cost_center_id||'', cost_center_name:r.cost_center_name||project||'غير محدد', itemObj:it});
      });
    });
    (window.data?.inventoryMovements||[]).filter(m=>['out','consume'].includes(String(m.movement_type)) && !m.request_id).forEach(m=>{
      const it=item138(m.item_id); const date=dateOnly138(m.movement_date||m.created_at||today138()); const qty=n138(m.quantity); const unit=n138(m.unit_cost || it.unit_cost || it.unit_price || 0); const val=qty*unit;
      const project=m.project_name || projectName138(m.project_id) || 'بدون مشروع';
      rows.push({date, project, project_id:m.project_id, person:m.receiver||'بدون مستلم', person_id:m.receiver_id||'', item:m.item_name||it.name||'', item_id:m.item_id, product_code:m.product_code||code138(it), barcode:m.barcode||barcode138(it), supplier:it.supplier||'', out:qty, returned:0, consumed:qty, qty, current:n138(it.quantity), unit_cost:unit, unit_gross:grossAmount138(unit,it), val, vat:vatAmount138(val,it), gross:grossAmount138(val,it), reason:m.reason||m.notes||'-', type:m.movement_type==='consume'?'استهلاك مباشر':'صرف مباشر', ref:'MOV-'+m.id, cost_center_id:m.cost_center_id||'', cost_center_name:m.cost_center_name||project||'غير محدد', itemObj:it});
    });
    return rows;
  }
  function filteredUsageRows138(){
    return usageRows138().filter(r=>reportDatePass138(r.date) && quickPass138(r) && codePass138(r) && productPass138(r) && supplierPass138(r) && personPass138(r));
  }
  function filteredExpenseRows138(){
    const q=($v138('financeReportQuickSearch')?.value || $v138('financeSearch')?.value || '').trim().toLowerCase();
    const code=$v138('financeReportCostCodeFilter')?.value || '';
    let rows=[...(window.data?.financeExpenses||[])];
    rows=rows.filter(e=>reportDatePass138(e.expense_date || e.created_at));
    if(code){ rows=rows.filter(e=> costCode138(e.project_name || projectName138(e.project_id), e)===code); }
    if(q) rows=rows.filter(e=>[e.project_name,projectName138(e.project_id),e.supplier,e.category,e.notes,e.total,e.payment_method].join(' ').toLowerCase().includes(q));
    const pid=$v138('financeProjectFilter')?.value||''; if(pid) rows=rows.filter(e=>String(e.project_id)===String(pid));
    return rows;
  }
  function renderReportSelects138(){
    const supVal=$v138('inventoryReportSupplier')?.value || '';
    const prodVal=$v138('inventoryReportProduct')?.value || '';
    const perVal=$v138('inventoryReportPerson')?.value || '';
    const suppliers=[...new Set((window.data?.inventoryItems||[]).map(i=>i.supplier).filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b),'ar'));
    const sup=$v138('inventoryReportSupplier'); if(sup) { sup.innerHTML='<option value="">كل الموردين</option>'+suppliers.map(s=>`<option value="${esc138(s)}">${esc138(s)}</option>`).join(''); sup.value=supVal; }
    const supplierFilter=supVal;
    const items=(window.data?.inventoryItems||[]).filter(i=>!supplierFilter || String(i.supplier||'')===String(supplierFilter));
    const prod=$v138('inventoryReportProduct'); if(prod){ prod.innerHTML='<option value="">كل المنتجات</option>'+items.map(i=>`<option value="${esc138(i.id)}">${esc138(code138(i))} - ${esc138(i.name||'')}</option>`).join(''); if([...prod.options].some(o=>o.value===String(prodVal))) prod.value=prodVal; }
    const people=[...new Map(userRows138().map(u=>[String(u.id),u])).values()];
    const per=$v138('inventoryReportPerson'); if(per){ per.innerHTML='<option value="">كل المشرفين/المستلمين</option>'+people.map(u=>`<option value="${esc138(u.id)}">${esc138(u.full_name||u.name||u.username||'')}</option>`).join(''); per.value=perVal; }
  }
  function renderReports138(){
    renderReportSelects138();
    const usage=filteredUsageRows138();
    // مصروفات حسب المشروع
    const expBody=$v138('expenseByProjectBody');
    if(expBody){
      const map={}; filteredExpenseRows138().forEach(e=>{ const p=e.project_name||projectName138(e.project_id)||'بدون مشروع'; map[p]=map[p]||{count:0,total:0}; map[p].count++; map[p].total+=n138(e.total); });
      const vals=Object.entries(map).sort((a,b)=>b[1].total-a[1].total);
      expBody.innerHTML=vals.map(([p,v])=>`<tr><td>${esc138(p)}</td><td>${money138(v.total)}</td><td>${v.count}</td></tr>`).join('') || '<tr><td colspan="3">لا توجد بيانات</td></tr>';
    }
    // صرف المخزون حسب المشروع
    const byProject=$v138('stockOutByProjectBody');
    if(byProject){
      const map={}; usage.forEach(r=>{ const p=r.project||'بدون مشروع'; map[p]=map[p]||{count:0,out:0,ret:0,cons:0,val:0,gross:0}; const v=map[p]; v.count++; v.out+=r.out; v.ret+=r.returned; v.cons+=r.consumed; v.val+=r.val; v.gross+=r.gross; });
      const vals=Object.entries(map).sort((a,b)=>b[1].gross-a[1].gross);
      byProject.innerHTML=vals.map(([p,v])=>`<tr><td>${esc138(p)}</td><td>${v.count}</td><td>${qty138(v.out)}</td><td>${qty138(v.ret)}</td><td>${qty138(v.cons)}</td><td>${money138(v.val)}</td><td>${money138(v.gross)}</td></tr>`).join('') || '<tr><td colspan="7">لا توجد بيانات</td></tr>';
      const table=byProject.closest('table'); if(table?.tHead) table.tHead.innerHTML='<tr><th>المشروع</th><th>عدد العمليات</th><th>الصادر</th><th>المرتجع</th><th>المستهلك</th><th>قيمة الاستهلاك</th><th>الإجمالي شامل الضريبة</th></tr>';
    }
    // استهلاك المشرفين
    const bySup=$v138('stockOutBySupervisorBody');
    if(bySup){
      const map={}; usage.forEach(r=>{ const k=(r.person||'بدون مستلم')+'|||'+(r.project||'بدون مشروع'); map[k]=map[k]||{person:r.person||'بدون مستلم',project:r.project||'بدون مشروع',count:0,out:0,ret:0,cons:0,gross:0}; const v=map[k]; v.count++; v.out+=r.out; v.ret+=r.returned; v.cons+=r.consumed; v.gross+=r.gross; });
      const vals=Object.values(map).sort((a,b)=>String(a.person).localeCompare(String(b.person),'ar') || String(a.project).localeCompare(String(b.project),'ar'));
      bySup.innerHTML=vals.map(v=>`<tr><td>${esc138(v.person)}</td><td>${esc138(v.project)}</td><td>${v.count}</td><td>${qty138(v.out)}</td><td>${qty138(v.ret)}</td><td>${qty138(v.cons)}</td><td>${money138(v.gross)}</td></tr>`).join('') || '<tr><td colspan="7">لا توجد بيانات</td></tr>';
      const table=bySup.closest('table'); if(table?.tHead) table.tHead.innerHTML='<tr><th>المشرف / المستلم</th><th>المشروع</th><th>عدد العمليات</th><th>الصادر</th><th>المرتجع</th><th>المستهلك</th><th>الإجمالي شامل الضريبة</th></tr>';
    }
    // المخزون العام
    const stock=$v138('stockReportBody');
    if(stock){
      const q=($v138('financeReportQuickSearch')?.value||'').trim().toLowerCase();
      const supplier=$v138('inventoryReportSupplier')?.value||'';
      let items=[...(window.data?.inventoryItems||[])];
      if(supplier) items=items.filter(i=>String(i.supplier||'')===String(supplier));
      if(q) items=items.filter(i=>[i.name,code138(i),barcode138(i),i.supplier,i.category,i.type].join(' ').toLowerCase().includes(q));
      stock.innerHTML=items.map(i=>{ const sub=n138(i.quantity)*itemUnitCost138(i); return `<tr><td>${esc138(code138(i))}</td><td>${esc138(i.name||'')}</td><td>${esc138(i.supplier||'-')}</td><td>${qty138(i.quantity)}</td><td>${qty138(i.min_quantity)}</td><td>${money138(itemUnitCost138(i))}</td><td>${money138(unitWithVat138(i))}</td><td>${money138(sub)}</td><td>${money138(grossAmount138(sub,i))}</td></tr>`; }).join('') || '<tr><td colspan="9">لا توجد بيانات</td></tr>';
      const table=stock.closest('table'); if(table?.tHead) table.tHead.innerHTML='<tr><th>كود المنتج</th><th>اسم المنتج</th><th>المورد</th><th>المخزون</th><th>حد الطلب</th><th>سعر الحبة</th><th>سعر الحبة شامل الضريبة</th><th>قيمة المخزون</th><th>الإجمالي شامل الضريبة</th></tr>';
      const sub=items.reduce((a,i)=>a+n138(i.quantity)*itemUnitCost138(i),0); const vat=items.reduce((a,i)=>a+vatAmount138(n138(i.quantity)*itemUnitCost138(i),i),0); const gross=items.reduce((a,i)=>a+grossAmount138(n138(i.quantity)*itemUnitCost138(i),i),0);
      if($v138('stockReportTotals')) $v138('stockReportTotals').innerHTML=`Total amount before 15% VAT: <b>${money138(sub)}</b> | Total VAT 15%: <b>${money138(vat)}</b> | Total Value with 15% VAT: <b>${money138(gross)}</b>`;
    }
    // مراكز التكلفة
    const cc=$v138('costCenterReportBody');
    if(cc){
      const map={}; usage.forEach(r=>{ const name=r.cost_center_name||r.project||'غير محدد'; const code=costCode138(name,r); map[name]=map[name]||{code,count:0,out:0,ret:0,cons:0,val:0,vat:0,gross:0}; const v=map[name]; v.count++; v.out+=r.out; v.ret+=r.returned; v.cons+=r.consumed; v.val+=r.val; v.vat+=r.vat; v.gross+=r.gross; });
      const vals=Object.entries(map).sort((a,b)=>b[1].gross-a[1].gross);
      cc.innerHTML=vals.map(([name,v])=>`<tr><td>${esc138(name)}</td><td><span class="badge ${v.code==='FM'?'green':'neutral'}">${v.code}</span></td><td>${v.count}</td><td>${qty138(v.out)}</td><td>${qty138(v.ret)}</td><td>${qty138(v.cons)}</td><td>${money138(v.val)}</td><td>${money138(v.vat)}</td><td>${money138(v.gross)}</td></tr>`).join('') || '<tr><td colspan="9">لا توجد بيانات</td></tr>';
    }
    // تفصيلي الاستهلاك
    const detail=$v138('inventoryUsageDetailBody');
    if(detail){
      const rows=[...usage].sort((a,b)=>String(b.date).localeCompare(String(a.date)) || String(a.project).localeCompare(String(b.project),'ar'));
      detail.innerHTML=rows.map(r=>`<tr><td>${esc138(r.date)}</td><td>${esc138(r.project)}</td><td>${esc138(r.person)}</td><td>${esc138(r.product_code)} - ${esc138(r.item)}</td><td>${qty138(r.out)}</td><td>${qty138(r.returned)}</td><td>${qty138(r.consumed)}</td><td>${money138(r.unit_cost)}</td><td>${money138(r.gross)}</td><td>${esc138(r.reason)}</td><td>${esc138(daysAgo138(r.date))}</td><td>${esc138(r.ref)}</td></tr>`).join('') || '<tr><td colspan="12">لا توجد بيانات</td></tr>';
      const table=detail.closest('table'); if(table?.tHead) table.tHead.innerHTML='<tr><th>التاريخ</th><th>المشروع</th><th>المشرف / المستلم</th><th>الصنف</th><th>الصادر</th><th>المرتجع</th><th>المستهلك</th><th>سعر الحبة</th><th>الإجمالي شامل الضريبة</th><th>سبب الاستخدام</th><th>منذ كم يوم</th><th>المرجع</th></tr>';
    }
    renderProductBox138(usage);
  }
  function renderProductBox138(usage){
    const box=$v138('inventoryProductDetailBox'); if(!box) return;
    const id=$v138('inventoryReportProduct')?.value || '';
    if(!id){ box.innerHTML='<p class="muted">اختر منتجًا لعرض تفاصيله، أو استخدم الفلاتر لعرض بقية التقارير.</p>'; return; }
    const it=item138(id); const rows=usage.filter(r=>String(r.item_id)===String(id));
    const out=rows.reduce((a,r)=>a+r.out,0), ret=rows.reduce((a,r)=>a+r.returned,0), cons=rows.reduce((a,r)=>a+r.consumed,0), val=rows.reduce((a,r)=>a+r.val,0), vat=rows.reduce((a,r)=>a+r.vat,0), gross=rows.reduce((a,r)=>a+r.gross,0);
    const img=it.image_url||it.photo_url||it.image||'';
    const rowsHtml=rows.map(r=>`<tr><td>${esc138(r.date)}</td><td>${esc138(r.project)}</td><td>${esc138(r.person)}</td><td>${qty138(r.out)}</td><td>${qty138(r.returned)}</td><td>${qty138(r.consumed)}</td><td>${money138(r.unit_cost)}</td><td>${money138(r.gross)}</td><td>${esc138(r.ref)}</td></tr>`).join('') || '<tr><td colspan="9">لا توجد حركة لهذا المنتج ضمن الفلاتر الحالية</td></tr>';
    box.innerHTML=`<div class="product-detail-head"><div>${img?`<img src="${esc138(img)}" class="product-detail-img">`:''}</div><div><h3>${esc138(it.name||'')}</h3><p>كود المنتج: <b>${esc138(code138(it))}</b> | الباركود: <b>${esc138(barcode138(it)||'-')}</b> | المورد: <b>${esc138(it.supplier||'-')}</b></p><p>سعر الحبة: <b>${money138(itemUnitCost138(it))}</b> | سعر الحبة شامل الضريبة: <b>${money138(unitWithVat138(it))}</b> | المتبقي: <b>${qty138(it.quantity)} ${esc138(it.unit||'')}</b></p></div></div><div class="kpis small"><div class="kpi"><small>الصادر</small><b>${qty138(out)}</b></div><div class="kpi"><small>المرتجع</small><b>${qty138(ret)}</b></div><div class="kpi"><small>المستهلك</small><b>${qty138(cons)}</b></div><div class="kpi"><small>قيمة الاستهلاك</small><b>${money138(val)}</b></div><div class="kpi"><small>الضريبة</small><b>${money138(vat)}</b></div><div class="kpi"><small>الإجمالي شامل الضريبة</small><b>${money138(gross)}</b></div></div><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>المشروع</th><th>المشرف / المستلم</th><th>الصادر</th><th>المرتجع</th><th>المستهلك</th><th>سعر الحبة</th><th>الإجمالي</th><th>المرجع</th></tr></thead><tbody>${rowsHtml}</tbody></table></div>`;
  }
  const oldRenderReports138 = window.financeRenderReports;
  window.financeRenderReports = function(){
    try{ renderReports138(); }catch(err){ console.warn('V138 reports warning', err); if(oldRenderReports138) oldRenderReports138.apply(this, arguments); }
  };
  function activeReportKind138(){ return document.querySelector('.smart-report-panel:not(.hidden)')?.getAttribute('data-report-panel') || document.querySelector('.smart-report-card.active')?.getAttribute('data-report') || 'expensesByProject'; }
  function printableHtml138(kind){
    renderReports138();
    const map={
      expensesByProject:['تقرير مصروفات المشاريع','expenseByProjectBody'],
      stockOutByProject:['تقرير صرف المخزون حسب المشروع','stockOutByProjectBody'],
      stockOutBySupervisor:['تقرير استهلاك المشرفين','stockOutBySupervisorBody'],
      stockReport:['تقرير المخزون العام','stockReportBody'],
      costCenters:['تقرير مراكز التكلفة','costCenterReportBody'],
      inventoryUsageDetail:['تقرير تفصيلي للاستهلاك','inventoryUsageDetailBody']
    };
    if(kind==='productDetail'){
      return ['تقرير تفاصيل منتج', $v138('inventoryProductDetailBox')?.innerHTML || '<p>لا توجد بيانات</p>'];
    }
    const [title,id]=map[kind]||map.expensesByProject;
    const table=$v138(id)?.closest('table');
    return [title, table ? `<table>${table.innerHTML}</table>` : '<p>لا توجد بيانات</p>'];
  }
  const oldPrintReport138 = window.financePrintReport;
  window.financePrintReport = function(kind){
    kind = kind || activeReportKind138();
    const [title,html]=printableHtml138(kind);
    if(typeof window.financePrintWindow === 'function') return window.financePrintWindow(title, html);
    return oldPrintReport138 ? oldPrintReport138.apply(this, arguments) : undefined;
  };
  // Ensure filter changes always repaint all visible reports and print only the selected report.
  window.inventoryClearReportDateFilter = function(){
    if($v138('inventoryReportDateMode')) $v138('inventoryReportDateMode').value='all';
    if($v138('inventoryReportDay')) $v138('inventoryReportDay').value='';
    if($v138('inventoryReportMonth')) $v138('inventoryReportMonth').value='';
    renderReports138();
  };
  // Full-month monthly time calculation: count every record dated from the first to the last day of the selected month.
  window.monthlyRowsV60 = function(){
    const month = $v138('monthlyMonth')?.value || today138().slice(0,7);
    const sid = $v138('monthlySupervisor')?.value || '';
    const start = monthStart138(month), end = monthEnd138(month);
    let logs = (window.data?.logs||[]).filter(l=>{
      const d=dateOnly138(l.log_date || l.check_in || l.created_at);
      return inRange138(d,start,end);
    });
    if(sid) logs=logs.filter(l=>String(l.supervisor_id)===String(sid));
    const map=new Map();
    logs.forEach(l=>{
      const d=dateOnly138(l.log_date || l.check_in || l.created_at);
      const k=String(l.supervisor_id||'')+'_'+String(l.project_id||'');
      if(!map.has(k)) map.set(k,{s:l.supervisor_id,p:l.project_id,a:0,r:0,t:0,c:0,days:new Set(),reqByDay:{}});
      const x=map.get(k); x.c++; x.days.add(d);
      const actual = typeof window.logActualMinutes==='function' ? window.logActualMinutes(l) : (n138(l.duration_minutes) || (typeof window.minutesBetween==='function'?window.minutesBetween(l.check_in,l.check_out):0));
      const required = typeof window.logRequiredMinutes==='function' ? window.logRequiredMinutes(l) : n138(l.required_minutes);
      x.a += n138(actual);
      // Count required time once per project/day even if there are repeated records, including records without checkout.
      if(d && x.reqByDay[d] === undefined) x.reqByDay[d] = n138(required);
      x.t += n138(l.travel_minutes);
    });
    map.forEach(x=>{ x.uniqueDays=x.days.size; x.r=Object.values(x.reqByDay).reduce((a,v)=>a+n138(v),0); });
    const vals=[...map.values()];
    const supTotals={}; vals.forEach(r=>{ const s=String(r.s||''); supTotals[s]=(supTotals[s]||0)+n138(r.a); });
    return vals.map(r=>{
      const supTotal=supTotals[String(r.s||'')]||0;
      const workPercent=supTotal ? r.a/supTotal*100 : 0;
      const commitmentPercent=r.r ? r.a/r.r*100 : 0;
      const diff=r.a-r.r;
      const st=typeof window.monthlyStatusFromDiffV60==='function' ? window.monthlyStatusFromDiffV60(diff,r.r) : {text:diff>=0?'ممتاز':'ناقص وقت', cls:diff>=0?'green':'red'};
      return {...r, supTotal, workers:(typeof window.uniqueWorkersForProjectTextV60==='function'?window.uniqueWorkersForProjectTextV60(r.p):'-'), dailyReqText:(typeof window.dailyRequiredTextV81==='function'?window.dailyRequiredTextV81(r.p):''), workPercent, commitmentPercent, ccls:(typeof window.monthlyCommitmentClassV60==='function'?window.monthlyCommitmentClassV60(commitmentPercent,r.r):'green'), diff, st:st.text, cls:st.cls};
    }).sort((a,b)=>String(personName138(a.s)).localeCompare(String(personName138(b.s)),'ar') || String(projectName138(a.p)).localeCompare(String(projectName138(b.p)),'ar'));
  };
  const oldRenderMonthly138=window.renderMonthly;
  window.renderMonthly=function(){
    try{
      const body=$v138('monthlyBody'); if(!body) return;
      const table=body.closest('table');
      if(table?.tHead) table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>عدد السجلات</th><th>أيام الزيارة</th><th>الوقت اليومي المطلوب</th><th>إجمالي الوقت المطلوب</th><th>الساعات الفعلية</th><th>وقت الانتقال</th><th>نسبة الالتزام</th><th>حالة الأداء</th></tr>';
      const vals=window.monthlyRowsV60();
      body.innerHTML=vals.map(r=>`<tr><td>${esc138(personName138(r.s))}</td><td>${esc138(projectName138(r.p))}</td><td>${esc138(r.workers||'-')}</td><td>${r.c||0}</td><td>${r.uniqueDays||0}</td><td>${esc138(r.dailyReqText||'')}</td><td>${typeof window.minsToText==='function'?window.minsToText(r.r):r.r}</td><td>${typeof window.minsToText==='function'?window.minsToText(r.a):r.a}</td><td>${qty138(r.t)} دقيقة</td><td><span class="badge ${r.ccls}">${typeof window.percentText==='function'?window.percentText(r.commitmentPercent):qty138(r.commitmentPercent)+'%'}</span></td><td><span class="badge ${r.cls}">${esc138(r.st)}</span></td></tr>`).join('') || '<tr><td colspan="11">لا توجد سجلات من أول الشهر إلى نهايته</td></tr>';
      const total=vals.reduce((a,r)=>a+n138(r.a),0), required=vals.reduce((a,r)=>a+n138(r.r),0), travel=vals.reduce((a,r)=>a+n138(r.t),0), records=vals.reduce((a,r)=>a+n138(r.c),0), days=vals.reduce((a,r)=>a+n138(r.uniqueDays),0), commitment=required?total/required*100:0;
      const diff=total-required; const st=typeof window.monthlyStatusFromDiffV60==='function'?window.monthlyStatusFromDiffV60(diff,required):{text:diff>=0?'ممتاز':'ناقص وقت',cls:diff>=0?'green':'red'};
      if($v138('monthlySummary')) $v138('monthlySummary').innerHTML=`<div class="kpi"><small>عدد السجلات</small><b>${records}</b></div><div class="kpi"><small>أيام الزيارة</small><b>${days}</b></div><div class="kpi"><small>إجمالي الوقت المطلوب</small><b>${typeof window.minsToText==='function'?window.minsToText(required):required}</b></div><div class="kpi"><small>الساعات الفعلية</small><b>${typeof window.minsToText==='function'?window.minsToText(total):total}</b></div><div class="kpi"><small>فرق الوقت</small><b>${typeof window.monthlyDiffTextV57==='function'?window.monthlyDiffTextV57(diff):diff}</b></div><div class="kpi"><small>وقت الانتقال</small><b>${qty138(travel)} دقيقة</b></div><div class="kpi"><small>نسبة الالتزام</small><b>${typeof window.percentText==='function'?window.percentText(commitment):qty138(commitment)+'%'}</b></div><div class="kpi"><small>حالة الأداء</small><b><span class="badge ${st.cls}">${esc138(st.text)}</span></b></div>`;
    }catch(err){ console.warn('V138 monthly warning',err); if(oldRenderMonthly138) oldRenderMonthly138.apply(this,arguments); }
  };
  window.addEventListener('load',()=>setTimeout(()=>{ try{ renderReports138(); if($v138('monthlyBody')) window.renderMonthly(); }catch(e){ console.warn('V138 init',e); } },1800));
})();


/* ===== V139: Monthly back to original style + all calendar days, and correct FM/CN cost code ===== */
(function(){
  'use strict';
  const $v139 = id => document.getElementById(id);
  const n139 = v => { const x=Number(String(v??0).replace(/,/g,'').trim()); return Number.isFinite(x)?x:0; };
  const esc139 = v => String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
  const qty139 = v => Number(n139(v)).toLocaleString('en-US',{maximumFractionDigits:2});
  const money139 = v => Number(n139(v)).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})+' ر.س';
  function dateStr139(d){ return d.toISOString().slice(0,10); }
  function daysOfMonth139(month){
    const [y,m]=String(month||'').split('-').map(Number);
    if(!y||!m) return [];
    const last=new Date(y,m,0).getDate();
    const out=[];
    for(let day=1; day<=last; day++) out.push(`${y}-${String(m).padStart(2,'0')}-${String(day).padStart(2,'0')}`);
    return out;
  }
  function dateOnly139(v){
    const s=String(v||'');
    if(/^\d{4}-\d{2}-\d{2}/.test(s)) return s.slice(0,10);
    const d=new Date(s); return isNaN(d)?'':dateStr139(d);
  }
  function projectName139(id){
    if(typeof window.projectName==='function') return window.projectName(id);
    return (window.data?.projects||[]).find(p=>String(p.id)===String(id))?.name || '-';
  }
  function personName139(id){
    if(typeof window.supervisorName==='function') return window.supervisorName(id);
    const u=(window.data?.users||[]).find(x=>String(x.id)===String(id));
    return u?.full_name||u?.name||u?.username||'-';
  }
  function minsText139(v){ return typeof window.minsToText==='function' ? window.minsToText(v) : String(v||0); }
  function percentText139(v){ return typeof window.percentText==='function' ? window.percentText(v) : (Number(v||0).toFixed(1)+'%'); }
  function reqForProjectDate139(projectId, d){
    if(typeof window.requiredMinutesForLog==='function') return n139(window.requiredMinutesForLog(projectId,d));
    const p=(window.data?.projects||[]).find(x=>String(x.id)===String(projectId));
    if(!p) return 0;
    const day=new Date(d+'T00:00:00').getDay();
    if(day===5 && n139(p.friday_minutes)>0) return n139(p.friday_minutes);
    return n139(p.required_daily_minutes || p.required_minutes || 0);
  }
  function dailyReqText139(projectId){
    if(typeof window.dailyRequiredTextV81==='function') return window.dailyRequiredTextV81(projectId);
    const p=(window.data?.projects||[]).find(x=>String(x.id)===String(projectId));
    return p ? minsText139(n139(p.required_daily_minutes||p.required_minutes||0)) : 'غير محدد';
  }
  function status139(pct, req){
    if(typeof window.performanceStatus==='function') return window.performanceStatus(pct,req);
    if(!req) return {text:'غير محدد',cls:'neutral'};
    if(pct>=90 && pct<=110) return {text:'ممتاز',cls:'green'};
    if(pct>110) return {text:'زيادة',cls:'amber'};
    return {text:'ضعيف',cls:'red'};
  }

  // يرجع الحساب للشكل الأول: تجميع حسب المشرف والمشروع، لكن المطلوب يحسب كل أيام الشهر كاملة.
  window.monthlyRowsV139 = function(){
    const month=$v139('monthlyMonth')?.value || new Date().toISOString().slice(0,7);
    const sid=$v139('monthlySupervisor')?.value || '';
    const allDays=daysOfMonth139(month);
    let logs=(window.data?.logs||[]).filter(l=>{
      const d=dateOnly139(l.log_date || l.check_in || l.created_at);
      return d && d.slice(0,7)===month;
    });
    if(sid) logs=logs.filter(l=>String(l.supervisor_id)===String(sid));
    const map=new Map();
    logs.forEach(l=>{
      const key=String(l.supervisor_id||'')+'_'+String(l.project_id||'');
      if(!map.has(key)) map.set(key,{s:l.supervisor_id,p:l.project_id,c:0,a:0,t:0,logDays:new Set()});
      const x=map.get(key); x.c++; x.logDays.add(dateOnly139(l.log_date||l.check_in||l.created_at));
      const actual=typeof window.logActualMinutes==='function' ? window.logActualMinutes(l) : n139(l.duration_minutes);
      x.a += n139(actual);
      x.t += n139(l.travel_minutes);
    });
    const vals=[...map.values()].map(x=>{
      x.monthDays=allDays.length;
      x.r=allDays.reduce((sum,d)=>sum+reqForProjectDate139(x.p,d),0);
      const pct=x.r ? x.a/x.r*100 : 0;
      const perf=status139(pct,x.r);
      return {...x,percent:pct,perf, dailyReqText:dailyReqText139(x.p)};
    });
    vals.sort((a,b)=>String(personName139(a.s)).localeCompare(String(personName139(b.s)),'ar') || String(projectName139(a.p)).localeCompare(String(projectName139(b.p)),'ar'));
    return vals;
  };
  window.renderMonthly = function(){
    const body=$v139('monthlyBody'); if(!body) return;
    const vals=window.monthlyRowsV139();
    const table=body.closest('table');
    if(table?.tHead) table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>عدد السجلات</th><th>أيام الشهر</th><th>الوقت اليومي المطلوب</th><th>الساعات المطلوبة</th><th>الساعات الفعلية</th><th>وقت الانتقال</th><th>نسبة العمل</th><th>حالة الأداء</th></tr>';
    body.innerHTML=vals.map(r=>`<tr><td>${esc139(personName139(r.s))}</td><td>${esc139(projectName139(r.p))}</td><td>${r.c}</td><td>${r.monthDays}</td><td>${esc139(r.dailyReqText||'')}</td><td>${minsText139(r.r)}</td><td>${minsText139(r.a)}</td><td>${qty139(r.t)} دقيقة</td><td><span class="badge ${r.perf.cls}">${percentText139(r.percent)}</span></td><td><span class="badge ${r.perf.cls}">${esc139(r.perf.text)}</span></td></tr>`).join('') || '<tr><td colspan="10">لا توجد بيانات</td></tr>';
    const total=vals.reduce((a,r)=>a+n139(r.a),0), required=vals.reduce((a,r)=>a+n139(r.r),0), travel=vals.reduce((a,r)=>a+n139(r.t),0), records=vals.reduce((a,r)=>a+n139(r.c),0), monthDays=vals[0]?.monthDays || daysOfMonth139($v139('monthlyMonth')?.value || new Date().toISOString().slice(0,7)).length;
    const pct=required?total/required*100:0; const perf=status139(pct,required);
    if($v139('monthlySummary')) $v139('monthlySummary').innerHTML=`<div class="kpi"><small>عدد التسجيلات</small><b>${records}</b></div><div class="kpi"><small>أيام الشهر</small><b>${monthDays}</b></div><div class="kpi"><small>الساعات المطلوبة</small><b>${minsText139(required)}</b></div><div class="kpi"><small>الساعات الفعلية</small><b>${minsText139(total)}</b></div><div class="kpi"><small>وقت الانتقال</small><b>${qty139(travel)} دقيقة</b></div><div class="kpi"><small>نسبة العمل</small><b>${percentText139(pct)}</b></div><div class="kpi"><small>حالة الأداء</small><b><span class="badge ${perf.cls}">${esc139(perf.text)}</span></b></div>`;
  };

  function costCenters139(){ return (window.data?.costCenters||[]).filter(c=>String(c.status||'active')!=='inactive'); }
  function costCenter139(row,name){
    if(row?.cost_center_id){ const byId=costCenters139().find(c=>String(c.id)===String(row.cost_center_id)); if(byId) return byId; }
    const n=String(name||row?.cost_center_name||'');
    return costCenters139().find(c=>String(c.name||'')===n) || null;
  }
  function costCode139(name,row){
    const c=costCenter139(row,name);
    if(c){
      const type=String(c.type||c.cost_type||'').trim();
      // أي مركز تكلفة إداري/قسم/عام/تشغيلي يصنف CN حتى لو كان اسمه يشبه مشروعًا.
      if(/إداري|اداري|قسم|عام|شركة|تشغيلي|كلين|CN/i.test(type+' '+(c.code||''))) return 'CN';
      if(c.project_id || /مشروع|FM/i.test(type+' '+(c.code||''))) return 'FM';
      return 'CN';
    }
    // إذا لا يوجد مركز تكلفة محفوظ، نعتمد على وجود project_id فقط.
    if(row?.project_id) return 'FM';
    return 'CN';
  }
  function item139(id){ return (window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id))||{}; }
  function lineItems139(r){ if(typeof window.v118LineItems==='function') return window.v118LineItems(r)||[]; return Array.isArray(r?.request_items)?r.request_items:(Array.isArray(r?.items)?r.items:[]); }
  function retQty139(requestId,itemId){ return (window.data?.inventoryMovements||[]).filter(m=>String(m.request_id||'')===String(requestId||'') && String(m.item_id||'')===String(itemId||'') && String(m.movement_type)==='return').reduce((a,m)=>a+n139(m.quantity),0); }
  function vat139(amount,it){ if(typeof window.vatAmount138==='function') return window.vatAmount138(amount,it); if(typeof window.vatOfAmount==='function') return window.vatOfAmount(amount,it); return amount*0.15; }
  function gross139(amount,it){ if(typeof window.grossAmount138==='function') return window.grossAmount138(amount,it); if(typeof window.grossOfAmount==='function') return window.grossOfAmount(amount,it); return amount+vat139(amount,it); }
  function usageRows139(){
    const rows=[];
    (window.data?.inventoryRequests||[]).filter(r=>String(r.status)==='approved').forEach(r=>{
      const date=dateOnly139(r.request_date||r.created_at||new Date()); const project=projectName139(r.project_id)||r.project_name||'بدون مشروع'; const person=personName139(r.supervisor_id)||r.supervisor_name||'بدون مستلم';
      lineItems139(r).forEach(l=>{ const it=item139(l.item_id); const out=n139(l.quantity); const returned=retQty139(r.id,l.item_id); const consumed=Math.max(0,out-returned); const unit=n139(l.unit_cost||it.unit_cost||it.unit_price); const val=consumed*unit; rows.push({date,project,project_id:r.project_id,person,person_id:r.supervisor_id,item:l.item_name||it.name||'',item_id:l.item_id,supplier:it.supplier||'',out,returned,consumed,val,vat:vat139(val,it),gross:gross139(val,it),cost_center_id:r.cost_center_id||'',cost_center_name:r.cost_center_name||project,ref:'REQ-'+r.id}); });
    });
    (window.data?.inventoryMovements||[]).filter(m=>['out','consume'].includes(String(m.movement_type)) && !m.request_id).forEach(m=>{ const it=item139(m.item_id); const qty=n139(m.quantity); const unit=n139(m.unit_cost||it.unit_cost||it.unit_price); const val=qty*unit; const project=projectName139(m.project_id)||m.project_name||'بدون مشروع'; rows.push({date:dateOnly139(m.movement_date||m.created_at||new Date()),project,project_id:m.project_id,person:m.receiver||'بدون مستلم',person_id:m.receiver_id||'',item:m.item_name||it.name||'',item_id:m.item_id,supplier:it.supplier||'',out:qty,returned:0,consumed:qty,val,vat:vat139(val,it),gross:gross139(val,it),cost_center_id:m.cost_center_id||'',cost_center_name:m.cost_center_name||project,ref:'MOV-'+m.id}); });
    return rows;
  }
  function reportPass139(r){
    const code=$v139('financeReportCostCodeFilter')?.value||''; if(code && costCode139(r.cost_center_name,r)!==code) return false;
    const q=($v139('financeReportQuickSearch')?.value||'').trim().toLowerCase(); if(q && ![r.project,r.person,r.item,r.supplier,r.cost_center_name,r.ref].join(' ').toLowerCase().includes(q)) return false;
    return true;
  }
  const oldFinanceRenderReports139=window.financeRenderReports;
  window.financeRenderReports=function(){
    if(oldFinanceRenderReports139) oldFinanceRenderReports139.apply(this,arguments);
    try{
      const cc=$v139('costCenterReportBody'); if(!cc) return;
      const map={}; usageRows139().filter(reportPass139).forEach(r=>{ const name=r.cost_center_name||r.project||'غير محدد'; const code=costCode139(name,r); map[name]=map[name]||{code,count:0,out:0,ret:0,cons:0,val:0,vat:0,gross:0}; const v=map[name]; v.count++; v.out+=n139(r.out); v.ret+=n139(r.returned); v.cons+=n139(r.consumed); v.val+=n139(r.val); v.vat+=n139(r.vat); v.gross+=n139(r.gross); });
      const vals=Object.entries(map).sort((a,b)=>b[1].gross-a[1].gross);
      cc.innerHTML=vals.map(([name,v])=>`<tr><td>${esc139(name)}</td><td><span class="badge ${v.code==='FM'?'green':'neutral'}">${v.code}</span></td><td>${v.count}</td><td>${qty139(v.out)}</td><td>${qty139(v.ret)}</td><td>${qty139(v.cons)}</td><td>${money139(v.val)}</td><td>${money139(v.vat)}</td><td>${money139(v.gross)}</td></tr>`).join('') || '<tr><td colspan="9">لا توجد بيانات</td></tr>';
    }catch(e){ console.warn('V139 cost report warning',e); }
  };
  window.addEventListener('load',()=>setTimeout(()=>{ try{ window.renderMonthly(); window.financeRenderReports?.(); }catch(e){ console.warn('V139 init',e); } },1800));
})();

/* ===== V140: Step fix - print visible finance report only + simplified monthly columns ===== */
(function(){
  'use strict';
  const $v140 = id => document.getElementById(id);
  const esc140 = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const num140 = v => { const n = Number(String(v ?? 0).replace(/,/g,'')); return Number.isFinite(n) ? n : 0; };
  const dateOnly140 = v => String(v || '').slice(0,10);
  const today140 = () => new Date().toISOString().slice(0,10);
  function monthDays140(month){
    month = month || today140().slice(0,7);
    const [y,m] = month.split('-').map(Number);
    return new Date(y, m, 0).getDate();
  }
  function monthStart140(month){ return (month || today140().slice(0,7)) + '-01'; }
  function monthEnd140(month){ return (month || today140().slice(0,7)) + '-' + String(monthDays140(month)).padStart(2,'0'); }
  function inMonth140(d,month){ d=dateOnly140(d); return d >= monthStart140(month) && d <= monthEnd140(month); }
  function minsTxt140(m){ return typeof window.minsToText === 'function' ? window.minsToText(num140(m)) : String(num140(m)); }
  function pct140(v){ return typeof window.percentText === 'function' ? window.percentText(num140(v)) : (num140(v).toFixed(1)+'%'); }
  function supName140(id){ return (typeof window.supervisorName==='function' ? window.supervisorName(id) : '') || ((window.data?.users||[]).find(u=>String(u.id)===String(id))?.full_name) || ''; }
  function projName140(id){ return (typeof window.projectName==='function' ? window.projectName(id) : '') || ((window.data?.projects||[]).find(p=>String(p.id)===String(id))?.name) || ''; }
  function workers140(pid){ return (typeof window.uniqueWorkersForProjectTextV60==='function' ? window.uniqueWorkersForProjectTextV60(pid) : '-') || '-'; }
  function actualMins140(l){ return typeof window.logActualMinutes==='function' ? num140(window.logActualMinutes(l)) : (num140(l.duration_minutes) || (typeof window.minutesBetween==='function' ? num140(window.minutesBetween(l.check_in,l.check_out)) : 0)); }
  function statusFromPercent140(p){
    p = num140(p);
    if(p >= 90 && p <= 110) return {text:'ممتاز', cls:'ok'};
    if((p >= 70 && p < 90) || (p > 110 && p <= 140)) return {text:'جيد', cls:'warn'};
    return {text:'ضعيف', cls:'bad'};
  }
  window.monthlyRowsV140 = function(){
    const month = $v140('monthlyMonth')?.value || today140().slice(0,7);
    const sid = $v140('monthlySupervisor')?.value || '';
    let logs = (window.data?.logs || []).filter(l => inMonth140(l.log_date || l.check_in || l.created_at, month));
    if(sid) logs = logs.filter(l => String(l.supervisor_id) === String(sid));
    const map = new Map();
    logs.forEach(l => {
      const k = String(l.supervisor_id || '') + '_' + String(l.project_id || '');
      if(!map.has(k)) map.set(k, {s:l.supervisor_id, p:l.project_id, a:0});
      map.get(k).a += actualMins140(l);
    });
    const rows = [...map.values()];
    const supTotals = {};
    rows.forEach(r => { const s=String(r.s||''); supTotals[s] = (supTotals[s] || 0) + num140(r.a); });
    const days = monthDays140(month);
    return rows.map(r => {
      const supTotal = supTotals[String(r.s||'')] || 0;
      const workPercent = supTotal ? (r.a / supTotal * 100) : 0;
      const st = statusFromPercent140(workPercent);
      return {...r, days, workers:workers140(r.p), workPercent, st:st.text, cls:st.cls};
    }).sort((a,b)=>String(supName140(a.s)).localeCompare(String(supName140(b.s)),'ar') || String(projName140(a.p)).localeCompare(String(projName140(b.p)),'ar'));
  };
  window.renderMonthly = function(){
    const body = $v140('monthlyBody'); if(!body) return;
    const table = body.closest('table');
    if(table?.tHead) table.tHead.innerHTML = '<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>أيام الشهر</th><th>الساعات الفعلية</th><th>نسبة العمل</th><th>حالة الأداء</th></tr>';
    const rows = window.monthlyRowsV140();
    body.innerHTML = rows.map(r => `<tr><td>${esc140(supName140(r.s))}</td><td>${esc140(projName140(r.p))}</td><td>${esc140(r.workers||'-')}</td><td>${r.days}</td><td>${esc140(minsTxt140(r.a))}</td><td><span class="badge green">${esc140(pct140(r.workPercent))}</span></td><td><span class="badge ${r.cls}">${esc140(r.st)}</span></td></tr>`).join('') || '<tr><td colspan="7">لا توجد بيانات لهذا الشهر</td></tr>';
    const total = rows.reduce((a,r)=>a+num140(r.a),0);
    if($v140('monthlySummary')) $v140('monthlySummary').innerHTML = `<div class="kpi"><small>أيام الشهر</small><b>${monthDays140($v140('monthlyMonth')?.value || today140().slice(0,7))}</b></div><div class="kpi"><small>الساعات الفعلية</small><b>${esc140(minsTxt140(total))}</b></div><div class="kpi"><small>عدد المشاريع</small><b>${rows.length}</b></div>`;
  };
  window.exportMonthlyCSV = function(){
    const rows=[...document.querySelectorAll('#monthlyBody tr')].map(tr=>[...tr.children].map(td=>td.textContent.trim()));
    const csv=['المشرف,المشروع,أسماء العمال,أيام الشهر,الساعات الفعلية,نسبة العمل,حالة الأداء',...rows.map(r=>r.map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))].join('\n');
    if(typeof window.download==='function') window.download('monthly.csv',csv);
  };
  window.printMonthlyReportV57 = function(){
    const rows = window.monthlyRowsV140();
    if(!rows.length){ if(typeof window.msg==='function') msg('لا توجد بيانات في الأوقات الشهرية للطباعة','err'); return; }
    const month = $v140('monthlyMonth')?.value || today140().slice(0,7);
    const sup = $v140('monthlySupervisor')?.value ? supName140($v140('monthlySupervisor').value) : 'الكل';
    const trs = rows.map((r,i)=>`<tr><td>${i+1}</td><td>${esc140(supName140(r.s))}</td><td>${esc140(projName140(r.p))}</td><td>${esc140(r.workers||'-')}</td><td>${r.days}</td><td>${esc140(minsTxt140(r.a))}</td><td>${esc140(pct140(r.workPercent))}</td><td>${esc140(r.st)}</td></tr>`).join('');
    const html = `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>@page{size:A4 landscape;margin:10mm}body{font-family:Tahoma,Arial,sans-serif;color:#123d32}h1{color:#063d31}table{width:100%;border-collapse:collapse}th{background:#063d31;color:white}td,th{border:1px solid #cfded8;padding:8px;text-align:center}.meta{display:flex;gap:12px;margin:12px 0}.box{border:1px solid #cfded8;border-radius:10px;padding:10px}</style></head><body><h1>تقرير الأوقات الشهرية</h1><div class="meta"><div class="box"><b>الشهر:</b> ${esc140(month)}</div><div class="box"><b>المشرف:</b> ${esc140(sup)}</div></div><table><thead><tr><th>#</th><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>أيام الشهر</th><th>الساعات الفعلية</th><th>نسبة العمل</th><th>حالة الأداء</th></tr></thead><tbody>${trs}</tbody></table><script>window.onload=function(){setTimeout(function(){window.print()},300)}</script></body></html>`;
    const w=window.open('','_blank'); if(w){w.document.write(html);w.document.close();}
  };
  function titleForKind140(kind){
    return ({expensesByProject:'مصروفات حسب المشروع',stockOutByProject:'صرف المخزون حسب المشروع',stockOutBySupervisor:'استهلاك المشرفين حسب المشروع',productDetail:'تقرير تفاصيل منتج',stockReport:'تقرير المخزون العام',costCenters:'تقرير مراكز التكلفة',inventoryUsageDetail:'تقرير توضيحي للاستهلاك'})[kind] || 'تقرير';
  }
  function panelForKind140(kind){ return document.querySelector(`.smart-report-panel[data-report-panel="${kind}"]`) || document.querySelector('.smart-report-panel.active'); }
  function cleanCloneForPrint140(panel){
    const clone = panel.cloneNode(true);
    clone.querySelectorAll('button,.report-filters-panel,input,select,textarea,.no-print').forEach(x=>x.remove());
    clone.querySelectorAll('[onclick]').forEach(x=>x.removeAttribute('onclick'));
    clone.classList.remove('hidden'); clone.style.display='block';
    return clone.innerHTML;
  }
  window.financePrintReport = function(kind){
    try{
      if(typeof window.financeRenderReports==='function') window.financeRenderReports();
      kind = kind || document.querySelector('.smart-report-panel.active')?.getAttribute('data-report-panel') || document.querySelector('.smart-report-card.active')?.getAttribute('data-report') || 'expensesByProject';
      // Product report uses current visible product detail; if no product selected, print the message visible in the panel only.
      const panel = panelForKind140(kind);
      const title = titleForKind140(kind);
      const content = panel ? cleanCloneForPrint140(panel) : '<p>لا توجد بيانات</p>';
      const html = `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${esc140(title)}</title><style>@page{size:A4 landscape;margin:10mm}body{font-family:Tahoma,Arial,sans-serif;color:#111}h1{color:#063d31;text-align:right}table{width:100%;border-collapse:collapse;margin-top:10px}th{background:#d0d0d0;color:#111}td,th{border:1px solid #333;padding:7px;text-align:center}.card,.wide-card{border:0!important;box-shadow:none!important}.table-head{display:block!important}.table-head h2{color:#063d31}.product-detail-img{max-width:130px;max-height:130px;object-fit:contain}.kpis{display:flex;gap:8px;flex-wrap:wrap}.kpi{border:1px solid #ddd;border-radius:10px;padding:8px}</style></head><body><h1>${esc140(title)}</h1>${content}<script>window.onload=function(){setTimeout(function(){window.print()},300)}</script></body></html>`;
      const w=window.open('','_blank'); if(w){ w.document.write(html); w.document.close(); }
    }catch(e){ console.error(e); if(typeof window.msg==='function') msg('تعذر طباعة التقرير الحالي: '+e.message,'err'); }
  };
  window.addEventListener('load',()=>setTimeout(()=>{ try{ if($v140('monthlyBody')) window.renderMonthly(); }catch(e){} },2200));
})();


/* ===== V141: Monthly totals visible + Excel-like print blocks ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const num = v => { const n=Number(String(v ?? 0).replace(/,/g,'')); return Number.isFinite(n)?n:0; };
  const dateOnly = v => String(v || '').slice(0,10);
  const today = () => { const d=new Date(); return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0'); };
  function inMonth(d,month){ d=dateOnly(d); return d && d.slice(0,7)===(month||today().slice(0,7)); }
  function minsText(m){ return (typeof window.minsToText==='function') ? window.minsToText(num(m)) : String(num(m)); }
  function pctText(v){ return (typeof window.percentText==='function') ? window.percentText(num(v)) : (num(v).toFixed(1)+'%'); }
  function supName(id){ return (typeof window.supervisorName==='function'?window.supervisorName(id):'') || ((window.data?.users||[]).find(u=>String(u.id)===String(id))?.full_name) || '-'; }
  function projName(id){ return (typeof window.projectName==='function'?window.projectName(id):'') || ((window.data?.projects||[]).find(p=>String(p.id)===String(id))?.name) || '-'; }
  function workerNames(pid){ return (typeof window.uniqueWorkersForProjectTextV60==='function'?window.uniqueWorkersForProjectTextV60(pid):'-') || '-'; }
  function actualMins(l){ return (typeof window.logActualMinutes==='function') ? num(window.logActualMinutes(l)) : (num(l.duration_minutes) || (typeof window.minutesBetween==='function' ? num(window.minutesBetween(l.check_in,l.check_out)) : 0)); }
  function status(p){ p=num(p); if(p>=90&&p<=110) return {text:'ممتاز',cls:'ok'}; if((p>=70&&p<90)||(p>110&&p<=140)) return {text:'جيد',cls:'warn'}; return {text:'ضعيف',cls:'bad'}; }
  function monthLabel(month){
    const parts=(month||today().slice(0,7)).split('-');
    const y=parts[0], m=Number(parts[1]||1);
    const en=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    return (en[m-1]||month)+'-'+String(y).slice(-2);
  }
  window.monthlyRowsV141 = function(){
    const month = $('monthlyMonth')?.value || today().slice(0,7);
    const sid = $('monthlySupervisor')?.value || '';
    let logs = (window.data?.logs || []).filter(l => inMonth(l.log_date || l.check_in || l.created_at, month));
    if(sid) logs = logs.filter(l => String(l.supervisor_id) === String(sid));
    const map = new Map();
    logs.forEach(l=>{
      const k=String(l.supervisor_id||'')+'_'+String(l.project_id||'');
      if(!map.has(k)) map.set(k,{s:l.supervisor_id,p:l.project_id,a:0});
      map.get(k).a += actualMins(l);
    });
    const rows=[...map.values()];
    const totals={}; rows.forEach(r=>{ const s=String(r.s||''); totals[s]=(totals[s]||0)+num(r.a); });
    return rows.map(r=>{ const total=totals[String(r.s||'')]||0; const percent=total? r.a/total*100 : 0; const st=status(percent); return {...r,total,percent,workers:workerNames(r.p),st:st.text,cls:st.cls}; })
      .sort((a,b)=>String(supName(a.s)).localeCompare(String(supName(b.s)),'ar') || String(projName(a.p)).localeCompare(String(projName(b.p)),'ar'));
  };
  window.renderMonthly = function(){
    const body=$('monthlyBody'); if(!body) return;
    const table=body.closest('table');
    if(table?.tHead) table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الساعات الفعلية</th><th>نسبة العمل</th><th>حالة الأداء</th></tr>';
    const rows=window.monthlyRowsV141();
    if(!rows.length){ body.innerHTML='<tr><td colspan="6">لا توجد بيانات لهذا الشهر</td></tr>'; return; }
    let html='', last=null, groupTotal=0;
    const flush=()=>{ if(last!==null){ html += `<tr class="monthly-total-row" style="background:#f4fbf8;font-weight:900"><td colspan="3">مجموع ${esc(supName(last))}</td><td>${esc(minsText(groupTotal))}</td><td><span class="badge green">100%</span></td><td>—</td></tr>`; }};
    rows.forEach(r=>{
      if(last!==null && String(last)!==String(r.s)){ flush(); groupTotal=0; }
      last=r.s; groupTotal += num(r.a);
      html += `<tr><td>${esc(supName(r.s))}</td><td>${esc(projName(r.p))}</td><td>${esc(r.workers||'-')}</td><td>${esc(minsText(r.a))}</td><td><span class="badge green">${esc(pctText(r.percent))}</span></td><td><span class="badge ${r.cls}">${esc(r.st)}</span></td></tr>`;
    });
    flush();
    body.innerHTML=html;
    const grand=rows.reduce((a,r)=>a+num(r.a),0);
    const supCount=new Set(rows.map(r=>String(r.s||''))).size;
    if($('monthlySummary')) $('monthlySummary').innerHTML=`<div class="kpi"><small>إجمالي الساعات الفعلية</small><b>${esc(minsText(grand))}</b></div><div class="kpi"><small>عدد المشرفين</small><b>${supCount}</b></div><div class="kpi"><small>عدد المشاريع</small><b>${rows.length}</b></div><div class="kpi"><small>مجموع كل مشرف</small><b>100%</b></div>`;
  };
  window.exportMonthlyCSV = function(){
    const rows=[...document.querySelectorAll('#monthlyBody tr')].map(tr=>[...tr.children].map(td=>td.textContent.trim()));
    const csv=['المشرف,المشروع,أسماء العمال,الساعات الفعلية,نسبة العمل,حالة الأداء',...rows.map(r=>r.map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))].join('\n');
    if(typeof window.download==='function') window.download('monthly.csv',csv);
  };
  window.printMonthlyReportV57 = function(){
    const rows = window.monthlyRowsV141();
    if(!rows.length){ if(typeof window.msg==='function') msg('لا توجد بيانات في الأوقات الشهرية للطباعة','err'); return; }
    const month = $('monthlyMonth')?.value || today().slice(0,7);
    const sup = $('monthlySupervisor')?.value ? supName($('monthlySupervisor').value) : 'الكل';
    const groups = new Map();
    rows.forEach(r=>{ const sid=String(r.s||''); if(!groups.has(sid)) groups.set(sid,[]); groups.get(sid).push(r); });
    const now = new Date();
    const dateTxt = now.toLocaleDateString('en-GB');
    const timeTxt = now.toLocaleTimeString('en-US',{hour:'2-digit',minute:'2-digit'});
    function fmtMin(v){ return String(Math.round(num(v))).replace(/\B(?=(\d{3})+(?!\d))/g, ','); }
    function pctOne(v){ if(!isFinite(Number(v))) return '-'; return (Math.round(Number(v||0)*10)/10).toFixed(1).replace(/\.0$/,'') + '%'; }
    function workerList(arr){
      const parts=[...new Set(arr.flatMap(r=>String(r.workers||'').split(/[،,\n]+/).map(x=>x.trim()).filter(Boolean)))];
      if(!parts.length) return '<div class="empty-line"></div><div class="empty-line"></div><div class="empty-line"></div>';
      return parts.map(n=>`<div class="worker-row"><span>•</span><b>${esc(n)}</b></div>`).join('');
    }
    const cards=[...groups.entries()].map(([sid,arr])=>{
      const total=arr.reduce((a,r)=>a+num(r.a),0);
      const body=arr.map(r=>`<tr><td class="project">${esc(projName(r.p))}</td><td>${fmtMin(r.a)}</td><td>${total ? pctOne(num(r.a)/total*100) : '-'}</td></tr>`).join('');
      return `<section class="super-card">
        <div class="super-title">${esc(supName(sid))}</div>
        <table class="mini-table">
          <thead><tr><th>المشروع</th><th>الوقت (دقيقة)</th><th>النسبة</th></tr></thead>
          <tbody>${body}</tbody>
          <tfoot><tr><td>الإجمالي</td><td>${fmtMin(total)}</td><td>100%</td></tr></tfoot>
        </table>
        <div class="workers-box"><h4>أسماء العمال</h4>${workerList(arr)}</div>
      </section>`;
    }).join('');
    const totalActual = rows.reduce((a,r)=>a+num(r.a),0);
    const totalProjects = rows.length;
    const totalSupervisors = groups.size;
    const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>
      @page{size:A4 landscape;margin:7mm}
      *{box-sizing:border-box}
      body{margin:0;background:#fff;font-family:Tahoma,Arial,sans-serif;color:#061a2e;-webkit-print-color-adjust:exact;print-color-adjust:exact}
      .page{min-height:190mm;border:1.6px solid #aeb8c4;border-radius:12px;padding:10px 12px 9px;background:linear-gradient(180deg,#fff 0%,#fbfdff 100%)}
      .header{display:grid;grid-template-columns:250px 1fr 250px;gap:10px;align-items:center;border-bottom:1.5px solid #d5dce5;padding-bottom:8px;margin-bottom:8px}
      .meta{display:grid;gap:6px;font-size:12px;color:#0b2440}.meta-row{display:grid;grid-template-columns:28px 78px 1fr;align-items:center;gap:5px}.ico{font-size:18px;color:#062f5f;text-align:center}.meta b{color:#061a2e}.meta span{font-weight:700;color:#111}
      .center{text-align:center}.center h1{margin:0;color:#062f5f;font-size:29px;font-weight:900;letter-spacing:-.5px}.center h2{margin:4px 0 7px;color:#223c5b;font-size:18px;font-weight:700}.month-pill{display:inline-block;min-width:210px;background:linear-gradient(90deg,#062f5f,#0a477e);color:white;border-radius:18px;padding:5px 36px;font-size:18px;font-weight:900;box-shadow:0 2px 8px rgba(6,47,95,.18)}
      .brand{text-align:right;display:grid;grid-template-columns:1fr 78px;gap:10px;align-items:center}.brand h3{margin:0;color:#111;font-size:21px}.brand p{margin:6px 0 0;color:#1d2d44;font-size:12px}.brand small{display:block;margin-top:10px;color:#062f5f;font-size:13px}.brand img{width:76px;max-height:76px;object-fit:contain}
      .summary-strip{display:flex;gap:8px;justify-content:center;margin:7px 0 8px}.chip{background:#f3f7fb;border:1px solid #d4dce5;border-radius:999px;padding:5px 13px;font-size:11px;color:#17324d}.chip b{color:#062f5f}
      .cards{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;align-items:start}.super-card{border:1.4px solid #bcc7d3;border-radius:8px;overflow:hidden;background:white;min-height:124mm;display:flex;flex-direction:column}.super-title{background:linear-gradient(90deg,#062f5f,#073966);color:white;text-align:center;font-size:18px;font-weight:900;padding:7px 5px}.mini-table{width:100%;border-collapse:collapse;table-layout:fixed;font-size:11px}.mini-table th{background:#f2f5f8;color:#061a2e;border:1px solid #d8dee6;padding:6px 3px}.mini-table td{border:1px solid #e1e6ed;padding:5px 3px;text-align:center;height:24px}.mini-table .project{text-align:right;font-weight:700}.mini-table tfoot td{font-weight:900;color:#062f5f;background:#f9fbfe;border-top:1.5px solid #062f5f}.workers-box{border-top:1.2px solid #062f5f;margin-top:auto;padding:8px 12px 10px;min-height:43mm;text-align:center}.workers-box h4{margin:0 0 7px;color:#062f5f;font-size:13px}.worker-row{display:flex;justify-content:center;gap:8px;align-items:center;border-bottom:1px dashed #d4dce5;min-height:19px;color:#111}.worker-row span{font-size:15px}.worker-row b{font-size:11px;font-weight:500}.empty-line{height:19px;border-bottom:1px dashed #d4dce5}
      .footer{display:grid;grid-template-columns:250px 1fr 1.35fr;gap:8px;margin-top:9px}.footer-card{border:1.2px solid #d1d9e3;border-radius:8px;background:#fff;padding:10px;min-height:27mm}.qr-card{display:grid;grid-template-columns:74px 1fr;gap:10px;align-items:center}.qr{width:72px;height:72px;border:5px solid #111;background:repeating-linear-gradient(45deg,#111 0 4px,#fff 4px 8px);position:relative}.qr:after{content:'QR';position:absolute;inset:22px 14px;background:#fff;text-align:center;font-weight:900;line-height:20px}.qr-text{font-size:12px;line-height:1.7;color:#112}.notes h4,.sign h4{margin:0 0 6px;color:#062f5f;text-align:center}.notes ul{margin:0;padding-right:18px;font-size:11px;line-height:1.8}.sign{display:grid;grid-template-columns:repeat(3,1fr);gap:0;padding:0}.sig-box{padding:9px;text-align:center;border-left:1px solid #d1d9e3}.sig-box:first-child{border-left:0}.sig-box h4{font-size:12px}.person{font-size:26px;margin:8px 0;color:#111}.sig-line{border-bottom:1px dotted #444;height:18px;margin:0 18px}
      @media print{.page{border-radius:0}.super-card,.footer-card{break-inside:avoid}}
    </style></head><body><main class="page">
      <header class="header">
        <div class="meta"><div class="meta-row"><span class="ico">📅</span><b>تاريخ التقرير:</b><span>${dateTxt}</span></div><div class="meta-row"><span class="ico">🕒</span><b>وقت التقرير:</b><span>${timeTxt}</span></div><div class="meta-row"><span class="ico">👤</span><b>المستخدم:</b><span>وائل شاكر</span></div></div>
        <div class="center"><h1>تقرير الأوقات الشهرية</h1><h2>مشاريع الصيانة اليومية</h2><div class="month-pill">${esc(monthLabel(month))}</div></div>
        <div class="brand"><div><h3>شركة تصنيف</h3><p>إدارة المرافق والتشغيل والصيانة</p><small>إدارة التقارير</small></div><img src="tasneef_logo_print.png" alt="Tasneef"></div>
      </header>
      <div class="summary-strip"><span class="chip">المشرف: <b>${esc(sup)}</b></span><span class="chip">عدد المشرفين: <b>${totalSupervisors}</b></span><span class="chip">عدد المشاريع: <b>${totalProjects}</b></span><span class="chip">إجمالي الدقائق: <b>${fmtMin(totalActual)}</b></span></div>
      <section class="cards">${cards}</section>
      <footer class="footer"><div class="footer-card qr-card"><div class="qr"></div><div class="qr-text">للتأكد من صحة التقرير<br>امسح كود QR</div></div><div class="footer-card notes"><h4>ملاحظات</h4><ul><li>جميع الأوقات محسوبة بالدقيقة.</li><li>النسبة = وقت المشروع ÷ إجمالي وقت المشرف × 100.</li><li>التقرير يشمل المشاريع النشطة خلال الشهر.</li></ul></div><div class="footer-card sign"><div class="sig-box"><h4>إعداد التقرير</h4><div class="person">♙</div><div class="sig-line"></div></div><div class="sig-box"><h4>مراجعة</h4><div class="person">♙</div><div class="sig-line"></div></div><div class="sig-box"><h4>اعتماد</h4><div class="person">♙</div><div class="sig-line"></div></div></div></footer>
      <script>window.onload=function(){setTimeout(function(){window.print()},450)}</script>
    </main></body></html>`;
    const w=window.open('','_blank'); if(w){w.document.write(html);w.document.close();}
  };
  window.addEventListener('load',()=>setTimeout(()=>{ try{ if($('monthlyBody')) window.renderMonthly(); }catch(e){console.warn('V141 monthly',e)} },2400));
})();

/* ===== V143: Restore classic finance/inventory print design + detailed totals at bottom ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const num = v => { const n = Number(String(v ?? 0).replace(/[^0-9.\-]/g,'')); return Number.isFinite(n) ? n : 0; };
  const money = v => Number(num(v)).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}) + ' SAR';
  const qty = v => Number(num(v)).toLocaleString('en-US',{maximumFractionDigits:2});
  const titleMap = {
    expensesByProject:'تقرير مصروفات المشاريع',
    stockOutByProject:'تقرير صرف المخزون حسب المشروع',
    stockOutBySupervisor:'تقرير استهلاك المشرفين',
    productDetail:'تقرير تفاصيل منتج',
    stockReport:'تقرير المخزون العام',
    costCenters:'تقرير مراكز التكلفة',
    inventoryUsageDetail:'تقرير تفصيلي للاستهلاك'
  };
  const bodyMap = {
    expensesByProject:'expenseByProjectBody',
    stockOutByProject:'stockOutByProjectBody',
    stockOutBySupervisor:'stockOutBySupervisorBody',
    stockReport:'stockReportBody',
    costCenters:'costCenterReportBody',
    inventoryUsageDetail:'inventoryUsageDetailBody'
  };
  function activeKind(){
    return document.querySelector('.smart-report-panel.active')?.getAttribute('data-report-panel') ||
           document.querySelector('.smart-report-panel:not(.hidden)')?.getAttribute('data-report-panel') ||
           document.querySelector('.smart-report-card.active')?.getAttribute('data-report') ||
           'expensesByProject';
  }
  function tableForKind(kind){
    const bodyId = bodyMap[kind];
    if(bodyId && $(bodyId)) return $(bodyId).closest('table');
    const panel = document.querySelector(`.smart-report-panel[data-report-panel="${kind}"]`) || document.querySelector('.smart-report-panel.active');
    return panel?.querySelector('table') || null;
  }
  function visibleFiltersText(){
    const parts=[];
    const dateMode = $('inventoryReportDateMode')?.value || '';
    if(dateMode==='day') parts.push('اليوم: '+($('inventoryReportDay')?.value||''));
    if(dateMode==='month') parts.push('الشهر: '+($('inventoryReportMonth')?.value||''));
    const code=$('financeReportCostCodeFilter')?.value; if(code) parts.push('رمز التكلفة: '+code);
    const sup=$('inventoryReportSupplier')?.value; if(sup) parts.push('المورد: '+sup);
    const prod=$('inventoryReportProduct')?.selectedOptions?.[0]?.textContent; if($('inventoryReportProduct')?.value && prod) parts.push('المنتج: '+prod.trim());
    const person=$('inventoryReportPerson')?.selectedOptions?.[0]?.textContent; if($('inventoryReportPerson')?.value && person) parts.push('المستلم: '+person.trim());
    const q=$('financeReportQuickSearch')?.value || $('financeSearch')?.value; if(q) parts.push('بحث: '+q);
    return parts.length ? parts.join(' | ') : 'كل البيانات حسب التقرير الظاهر في النظام';
  }
  function cloneTableHtml(table){
    if(!table) return '<p class="empty">لا توجد بيانات للطباعة</p>';
    const clone=table.cloneNode(true);
    clone.querySelectorAll('button,input,select,textarea,.no-print').forEach(x=>x.remove());
    clone.querySelectorAll('[onclick]').forEach(x=>x.removeAttribute('onclick'));
    clone.removeAttribute('class');
    clone.querySelectorAll('*').forEach(x=>{ x.removeAttribute('style'); });
    return clone.outerHTML;
  }
  function getHeaders(table){
    return [...(table?.querySelectorAll('thead th')||[])].map(th=>th.textContent.trim());
  }
  function rows(table){ return [...(table?.querySelectorAll('tbody tr')||[])].filter(tr=>!tr.textContent.includes('لا توجد بيانات')); }
  function idx(headers, patterns){
    return headers.findIndex(h => patterns.some(p => h.includes(p)));
  }
  function sumCol(table, patterns){
    const headers=getHeaders(table); const i=idx(headers, patterns); if(i<0) return 0;
    return rows(table).reduce((a,tr)=>a+num(tr.children[i]?.textContent||0),0);
  }
  function countRows(table){ return rows(table).length; }
  function summaryFor(kind, table){
    const headers=getHeaders(table);
    let cards=[];
    const count=countRows(table);
    cards.push(['عدد السطور', qty(count)]);
    if(kind==='expensesByProject'){
      const total=sumCol(table,['إجمالي المصروفات','المصروفات','الإجمالي']);
      cards.push(['إجمالي المصروفات', money(total)]);
    } else if(kind==='stockOutByProject' || kind==='stockOutBySupervisor'){
      const ops=sumCol(table,['عدد العمليات','عدد الطلبات']);
      const q=sumCol(table,['إجمالي الكميات','إجمالي الكمية','الكمية']);
      cards.push(['عدد العمليات / الطلبات', qty(ops)]);
      cards.push(['إجمالي الكمية', qty(q)]);
    } else {
      const out=sumCol(table,['الصادر']);
      const ret=sumCol(table,['المرتجع']);
      const cons=sumCol(table,['المستهلك','الكمية']);
      const current=sumCol(table,['المتبقي','إجمالي المخزون','الكمية المتبقية','Total Stock']);
      const before=sumCol(table,['قيمة الاستهلاك','قيمة المخزون','القيمة','Value','قبل الضريبة']);
      const vat=sumCol(table,['ضريبة','VAT']);
      const gross=sumCol(table,['الإجمالي شامل','الإجمالي','Total Value','Consumption Value']);
      if(out) cards.push(['إجمالي الصادر', qty(out)]);
      if(ret) cards.push(['إجمالي المرتجع', qty(ret)]);
      if(cons) cards.push(['إجمالي المستهلك', qty(cons)]);
      if(current) cards.push(['إجمالي المتبقي / المخزون', qty(current)]);
      if(before) cards.push(['Total amount before 15% VAT', money(before)]);
      if(vat) cards.push(['Total VAT 15%', money(vat)]);
      if(gross) cards.push(['Total Value with 15% VAT', money(gross)]);
    }
    return `<div class="totals-title">إجماليات نهاية التقرير</div><div class="totals-grid">${cards.map(([a,b])=>`<div class="total-card"><span>${esc(a)}</span><b>${esc(b)}</b></div>`).join('')}</div>`;
  }
  function productDetailFallback(kind){
    const panel=document.querySelector(`.smart-report-panel[data-report-panel="${kind}"]`) || $('inventoryProductDetailBox') || document.querySelector('.smart-report-panel.active');
    if(!panel) return '';
    const clone=panel.cloneNode(true);
    clone.querySelectorAll('button,input,select,textarea,.no-print').forEach(x=>x.remove());
    clone.querySelectorAll('[onclick]').forEach(x=>x.removeAttribute('onclick'));
    return clone.innerHTML;
  }
  window.financePrintReport = function(kind){
    try{
      if(typeof window.financeRenderReports==='function') window.financeRenderReports();
      kind = kind || activeKind();
      const title = titleMap[kind] || 'تقرير';
      const table = tableForKind(kind);
      let mainHtml = table ? cloneTableHtml(table) : productDetailFallback(kind);
      const summary = table ? summaryFor(kind, table) : '';
      const html = `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${esc(title)}</title><style>
        @page{size:A4 landscape;margin:10mm}
        *{box-sizing:border-box} body{margin:0;font-family:Tahoma,Arial,sans-serif;color:#1c2522;background:#fff;font-size:11px;-webkit-print-color-adjust:exact;print-color-adjust:exact}
        .page{min-height:100vh;padding:14px;border:1px solid #9fb4ac}
        .header{display:flex;align-items:center;justify-content:space-between;border-bottom:3px solid #0a5a49;padding-bottom:10px;margin-bottom:10px}
        .brand{display:flex;align-items:center;gap:12px}.brand img{width:58px;height:58px;object-fit:contain}.brand h2{margin:0;color:#0a5a49;font-size:20px}.brand p{margin:3px 0 0;color:#6a766f}.report-title{text-align:left}.report-title h1{margin:0;color:#0a5a49;font-size:24px}.report-title p{margin:5px 0 0;color:#6a766f}
        .meta{display:grid;grid-template-columns:1.2fr 2.5fr 1.2fr;gap:8px;margin-bottom:10px}.meta div{border:1px solid #d6e2dd;background:#f8fbfa;border-radius:8px;padding:8px}.meta b{color:#0a5a49}
        table{width:100%;border-collapse:collapse;margin-top:8px;direction:rtl} th{background:#0a5a49;color:#fff;font-weight:800} td,th{border:1px solid #8ea79e;padding:6px;text-align:center;vertical-align:middle} tbody tr:nth-child(even) td{background:#f5f8f7} td:nth-child(2),td:nth-child(3),td:nth-child(5){text-align:right}
        .totals-title{margin:14px 0 8px;background:#0a5a49;color:white;border-radius:999px;padding:7px 18px;display:inline-block;font-weight:900}
        .totals-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:4px}.total-card{border:1px solid #cfded8;border-radius:10px;padding:9px;background:#fff}.total-card span{display:block;color:#60736b;font-size:10px}.total-card b{display:block;color:#0a5a49;font-size:15px;margin-top:4px;direction:ltr;text-align:right}
        .signs{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:14px}.sign{height:58px;border:1px dashed #9fb4ac;border-radius:10px;padding:8px}.footer{text-align:center;margin-top:10px;color:#6a766f;border-top:1px solid #d9e6e1;padding-top:7px}
        .empty{padding:30px;text-align:center;border:1px dashed #aaa}
      </style></head><body><div class="page"><div class="header"><div class="brand"><img src="tasneef_logo_print.png" onerror="this.style.display='none'"><div><h2>شركة تصنيف</h2><p>إدارة المرافق والتشغيل والصيانة</p></div></div><div class="report-title"><h1>${esc(title)}</h1><p>تقرير المصروفات والمخزون</p></div></div><div class="meta"><div><b>تاريخ الطباعة:</b><br>${esc(new Date().toLocaleString('en-US'))}</div><div><b>الفلاتر المستخدمة:</b><br>${esc(visibleFiltersText())}</div><div><b>المستخدم:</b><br>${esc(window.currentUser?.full_name || window.currentUser?.username || 'النظام')}</div></div>${mainHtml}${summary}<div class="signs"><div class="sign">إعداد:</div><div class="sign">مراجعة:</div><div class="sign">اعتماد:</div></div><div class="footer">هذا التقرير يطبع نفس البيانات الظاهرة في النظام حسب الفلاتر الحالية</div></div><script>window.onload=function(){setTimeout(function(){window.print()},350)}</script></body></html>`;
      const w=window.open('','_blank'); if(w){ w.document.write(html); w.document.close(); }
    }catch(e){ console.error(e); if(typeof window.msg==='function') msg('تعذر طباعة التقرير الحالي: '+e.message,'err'); }
  };
})();


/* ===== V144: Detailed classic print for expenses and inventory reports ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const num = v => { const n = Number(String(v ?? 0).replace(/[^0-9.\-]/g,'')); return Number.isFinite(n) ? n : 0; };
  const fmtQty = v => Number(num(v)).toLocaleString('en-US',{maximumFractionDigits:2});
  const fmtMoney = v => Number(num(v)).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}) + ' SAR';
  const today = () => { const d=new Date(); return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0'); };
  const dateOnly = v => String(v || '').slice(0,10);
  const monthEnd = m => { const [y,mo]=String(m||today().slice(0,7)).split('-').map(Number); return `${y}-${String(mo).padStart(2,'0')}-${String(new Date(y,mo,0).getDate()).padStart(2,'0')}`; };
  const inRange = (d,a,b) => d>=a && d<=b;
  function itemById(id){ return (window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id)) || {}; }
  function itemCode(it){ return (typeof window.v118ProductCode==='function'?window.v118ProductCode(it):'') || it.product_code || it.code || it.serial_number || it.id || ''; }
  function itemBarcode(it){ return (typeof window.v118Barcode==='function'?window.v118Barcode(it):'') || it.barcode || it.supplier_barcode || ''; }
  function projectName(id){ return (typeof window.financeProjectName==='function'?window.financeProjectName(id):'') || (typeof window.projectName==='function'?window.projectName(id):'') || (window.data?.projects||[]).find(p=>String(p.id)===String(id))?.name || ''; }
  function userName(id){ return (typeof window.supervisorName==='function'?window.supervisorName(id):'') || (window.data?.users||[]).find(u=>String(u.id)===String(id))?.full_name || ''; }
  function lineItems(r){
    let arr = r.request_items || r.request_lines || r.items || [];
    if(typeof arr === 'string'){ try{ arr=JSON.parse(arr); }catch(e){ arr=[]; } }
    if(!Array.isArray(arr)) arr=[];
    if(!arr.length && r.item_id) arr=[{item_id:r.item_id,item_name:r.item_name,product_code:r.product_code,barcode:r.barcode,quantity:r.quantity,unit_cost:r.unit_cost}];
    return arr;
  }
  function returnQty(reqId,itemId){
    return (window.data?.inventoryMovements||[]).filter(m=>String(m.request_id)===String(reqId) && String(m.item_id)===String(itemId) && String(m.movement_type)==='return').reduce((a,m)=>a+num(m.quantity),0);
  }
  function vatMode(it){ return String(it.vat_mode || it.tax_mode || it.vat_status || 'exclusive'); }
  function unitBefore(it, raw){
    raw = num(raw ?? it.unit_cost ?? it.unit_price);
    return vatMode(it)==='inclusive' ? raw/1.15 : raw;
  }
  function unitVat(it, raw){ return vatMode(it)==='exempt' ? 0 : unitBefore(it, raw)*0.15; }
  function unitGross(it, raw){ return unitBefore(it, raw) + unitVat(it, raw); }
  function amountVat(amount, it){ return vatMode(it)==='exempt' ? 0 : num(amount)*0.15; }
  function amountGross(amount, it){ return num(amount)+amountVat(amount,it); }
  function datePass(d){
    d=dateOnly(d||today()); const mode=$('inventoryReportDateMode')?.value || 'all';
    if(mode==='day') return d === ($('inventoryReportDay')?.value || today());
    if(mode==='month'){ const m=$('inventoryReportMonth')?.value || today().slice(0,7); return inRange(d, m+'-01', monthEnd(m)); }
    return true;
  }
  function activeCostCenters(){ return (window.data?.costCenters||[]).filter(c=>String(c.status||'active')!=='inactive'); }
  function findCostCenter(row){
    if(row?.cost_center_id){
      const byId = activeCostCenters().find(c=>String(c.id)===String(row.cost_center_id));
      if(byId) return byId;
    }
    const names = [row?.cost_center_name, row?.cost_center, row?.project].filter(Boolean).map(x=>String(x).trim());
    for(const n of names){
      const byName = activeCostCenters().find(c=>String(c.name||'').trim()===n);
      if(byName) return byName;
    }
    return null;
  }
  function costCode(row){
    // القاعدة المعتمدة: مركز التكلفة الإداري/القسم/كلين/عام = CN، والمشروع فقط = FM.
    const c = findCostCenter(row);
    if(c){
      const text = [c.type, c.cost_type, c.code, c.name].filter(Boolean).join(' ');
      if(/إداري|اداري|قسم|عام|شركة|تشغيلي|كلين|clean|admin|CN/i.test(text)) return 'CN';
      if(c.project_id || /مشروع|project|FM/i.test(text)) return 'FM';
      return 'CN';
    }
    const explicitType = [row?.cost_center_type,row?.cost_type].filter(Boolean).join(' ');
    if(/إداري|اداري|قسم|عام|شركة|تشغيلي|كلين|clean|admin|CN/i.test(explicitType)) return 'CN';
    // إذا لا يوجد مركز تكلفة محفوظ، لا نعتبره FM إلا بوجود project_id صريح.
    return row?.project_id ? 'FM' : 'CN';
  }
  function quickPass(row){
    const q=($('financeReportQuickSearch')?.value || $('financeSearch')?.value || '').trim().toLowerCase();
    if(!q) return true;
    return Object.values(row).join(' ').toLowerCase().includes(q);
  }
  function usageRows(){
    const rows=[];
    (window.data?.inventoryRequests||[]).filter(r=>String(r.status)==='approved').forEach(r=>{
      const date=dateOnly(r.request_date||r.created_at||today());
      const project=r.project_name || projectName(r.project_id) || 'بدون مشروع';
      const person=r.supervisor_name || userName(r.supervisor_id) || 'بدون مستلم';
      lineItems(r).forEach(l=>{
        const it=itemById(l.item_id); const out=num(l.quantity); const ret=returnQty(r.id,l.item_id); const consumed=Math.max(0,out-ret);
        const unit=unitBefore(it,l.unit_cost); const val=consumed*unit;
        rows.push({date,project,project_id:r.project_id,person,person_id:r.supervisor_id,item:l.item_name||it.name||'',item_id:l.item_id,product_code:l.product_code||itemCode(it),barcode:l.barcode||itemBarcode(it),supplier:it.supplier||'',category:it.category||'',item_type:it.item_type||it.type||'',unit_name:it.unit||l.unit||'',out,returned:ret,consumed,current:num(it.quantity),unit_before:unit,unit_vat:unitVat(it,l.unit_cost),unit_gross:unitGross(it,l.unit_cost),value_before:val,vat:amountVat(val,it),gross:amountGross(val,it),reason:r.reason||r.notes||'-',movement_type:'أمر صرف معتمد',ref:'REQ-'+r.id,cost_center_id:r.cost_center_id||null,cost_center_name:r.cost_center_name||project,cost_center_type:r.cost_center_type||'',cost_code:costCode({project,project_id:r.project_id,cost_center_id:r.cost_center_id,cost_center_name:r.cost_center_name,cost_center_type:r.cost_center_type})});
      });
    });
    (window.data?.inventoryMovements||[]).filter(m=>['out','consume'].includes(String(m.movement_type)) && !m.request_id).forEach(m=>{
      const it=itemById(m.item_id); const q=num(m.quantity); const unit=unitBefore(it,m.unit_cost); const val=q*unit; const project=m.project_name||projectName(m.project_id)||'بدون مشروع';
      rows.push({date:dateOnly(m.movement_date||m.created_at||today()),project,project_id:m.project_id,person:m.receiver||'بدون مستلم',item:m.item_name||it.name||'',item_id:m.item_id,product_code:m.product_code||itemCode(it),barcode:m.barcode||itemBarcode(it),supplier:it.supplier||'',category:it.category||'',item_type:it.item_type||it.type||'',unit_name:it.unit||'',out:q,returned:0,consumed:q,current:num(it.quantity),unit_before:unit,unit_vat:unitVat(it,m.unit_cost),unit_gross:unitGross(it,m.unit_cost),value_before:val,vat:amountVat(val,it),gross:amountGross(val,it),reason:m.reason||m.notes||'-',movement_type:m.movement_type==='consume'?'استهلاك مباشر':'صرف مباشر',ref:'MOV-'+m.id,cost_center_id:m.cost_center_id||null,cost_center_name:m.cost_center_name||project,cost_center_type:m.cost_center_type||'',cost_code:costCode({project,project_id:m.project_id,cost_center_id:m.cost_center_id,cost_center_name:m.cost_center_name,cost_center_type:m.cost_center_type})});
    });
    return rows;
  }
  function filteredUsage(){
    const prod=$('inventoryReportProduct')?.value||''; const supplier=$('inventoryReportSupplier')?.value||''; const person=$('inventoryReportPerson')?.value||''; const code=$('financeReportCostCodeFilter')?.value||'';
    return usageRows().filter(r=>datePass(r.date) && (!prod||String(r.item_id)===String(prod)) && (!supplier||String(r.supplier)===String(supplier)) && (!person||String(r.person_id)===String(person)||String(r.person)===String(person)) && (!code||r.cost_code===code) && quickPass(r));
  }
  function filteredItems(){
    const supplier=$('inventoryReportSupplier')?.value||''; const prod=$('inventoryReportProduct')?.value||''; const q=($('financeReportQuickSearch')?.value || '').trim().toLowerCase();
    return [...(window.data?.inventoryItems||[])].filter(i=>(!supplier||String(i.supplier||'')===String(supplier)) && (!prod||String(i.id)===String(prod)) && (!q||[i.name,itemCode(i),itemBarcode(i),i.supplier,i.category,i.type,i.item_type].join(' ').toLowerCase().includes(q)) );
  }
  function filteredExpenses(){
    const q=($('financeReportQuickSearch')?.value || $('financeSearch')?.value || '').trim().toLowerCase(); const code=$('financeReportCostCodeFilter')?.value||''; const pid=$('financeProjectFilter')?.value||'';
    return [...(window.data?.financeExpenses||[])].filter(e=>datePass(e.expense_date||e.created_at) && (!pid||String(e.project_id)===String(pid)) && (!code||costCode({project:e.project_name||projectName(e.project_id),project_id:e.project_id,cost_center_id:e.cost_center_id,cost_center_name:e.cost_center_name,cost_center_type:e.cost_center_type})===code) && (!q||[e.project_name,projectName(e.project_id),e.supplier,e.category,e.expense_type,e.notes,e.payment_method].join(' ').toLowerCase().includes(q)) );
  }
  function table(headers, rows){
    return `<table><thead><tr>${headers.map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.join(''):`<tr><td colspan="${headers.length}">لا توجد بيانات</td></tr>`}</tbody></table>`;
  }
  function detailedUsageTable(rows){
    const heads=['التاريخ','رمز التكلفة','مركز التكلفة','المشروع','المشرف / المستلم','كود المنتج','باركود المورد','نوع المنتج','الصنف','المورد','الوحدة','سعر الحبة قبل الضريبة','ضريبة الحبة 15%','سعر الحبة شامل','الصادر','المرتجع','المستهلك','المتبقي','قيمة قبل الضريبة','VAT 15%','الإجمالي شامل VAT','نوع الحركة','المرجع','سبب الاستخدام'];
    const trs=rows.map(r=>`<tr><td>${esc(r.date)}</td><td>${esc(r.cost_code)}</td><td>${esc(r.cost_center_name||'-')}</td><td>${esc(r.project)}</td><td>${esc(r.person)}</td><td>${esc(r.product_code)}</td><td>${esc(r.barcode||'-')}</td><td>${esc(r.item_type||'-')}</td><td>${esc(r.item)}</td><td>${esc(r.supplier||'-')}</td><td>${esc(r.unit_name||'-')}</td><td>${fmtMoney(r.unit_before)}</td><td>${fmtMoney(r.unit_vat)}</td><td>${fmtMoney(r.unit_gross)}</td><td>${fmtQty(r.out)}</td><td>${fmtQty(r.returned)}</td><td>${fmtQty(r.consumed)}</td><td>${fmtQty(r.current)}</td><td>${fmtMoney(r.value_before)}</td><td>${fmtMoney(r.vat)}</td><td>${fmtMoney(r.gross)}</td><td>${esc(r.movement_type)}</td><td>${esc(r.ref)}</td><td>${esc(r.reason)}</td></tr>`);
    return table(heads,trs);
  }
  function stockTable(items){
    const heads=['كود المنتج','باركود المورد','اسم المنتج','التصنيف','نوع المنتج','الوحدة','المورد','سعر الحبة قبل الضريبة','ضريبة الحبة 15%','سعر الحبة شامل','الكمية المتبقية','حد إعادة الطلب','قيمة المخزون قبل الضريبة','VAT 15%','Total Value with 15% VAT'];
    const trs=items.map(i=>{ const unit=unitBefore(i,i.unit_cost); const vat=unitVat(i,i.unit_cost); const gross=unitGross(i,i.unit_cost); const qty=num(i.quantity); const val=qty*unit; return `<tr><td>${esc(itemCode(i))}</td><td>${esc(itemBarcode(i)||'-')}</td><td>${esc(i.name||'-')}</td><td>${esc(i.category||'-')}</td><td>${esc(i.item_type||i.type||'-')}</td><td>${esc(i.unit||'-')}</td><td>${esc(i.supplier||'-')}</td><td>${fmtMoney(unit)}</td><td>${fmtMoney(vat)}</td><td>${fmtMoney(gross)}</td><td>${fmtQty(qty)}</td><td>${fmtQty(i.min_quantity)}</td><td>${fmtMoney(val)}</td><td>${fmtMoney(amountVat(val,i))}</td><td>${fmtMoney(amountGross(val,i))}</td></tr>`; });
    return table(heads,trs);
  }
  function expensesTable(rows){
    const heads=['التاريخ','رمز التكلفة','المشروع / مركز التكلفة','نوع المصروف','المورد','طريقة الدفع','المبلغ قبل الضريبة','VAT 15%','الإجمالي شامل VAT','ملاحظات'];
    const trs=rows.map(e=>{ const before=num(e.amount || e.subtotal || e.before_vat || (num(e.total)/(1.15))); const vat=('vat' in e)?num(e.vat):before*0.15; const total=num(e.total || before+vat); const pname=e.project_name||projectName(e.project_id)||e.cost_center_name||'بدون مشروع'; const code=costCode({project:pname,project_id:e.project_id,cost_center_id:e.cost_center_id,cost_center_name:e.cost_center_name,cost_center_type:e.cost_center_type}); return `<tr><td>${esc(dateOnly(e.expense_date||e.created_at))}</td><td>${esc(code)}</td><td>${esc(pname)}</td><td>${esc(e.category||e.expense_type||'-')}</td><td>${esc(e.supplier||'-')}</td><td>${esc(e.payment_method||'-')}</td><td>${fmtMoney(before)}</td><td>${fmtMoney(vat)}</td><td>${fmtMoney(total)}</td><td>${esc(e.notes||'-')}</td></tr>`; });
    return table(heads,trs);
  }
  function totalsBlock(rows, mode){
    let count=rows.length, qty=0, out=0, ret=0, cons=0, before=0, vat=0, gross=0;
    if(mode==='expenses') rows.forEach(e=>{ const b=num(e.amount || e.subtotal || e.before_vat || (num(e.total)/(1.15))); const v=('vat' in e)?num(e.vat):b*0.15; before+=b; vat+=v; gross+=num(e.total||b+v); });
    else if(mode==='stock') rows.forEach(i=>{ const unit=unitBefore(i,i.unit_cost); const val=num(i.quantity)*unit; qty+=num(i.quantity); before+=val; vat+=amountVat(val,i); gross+=amountGross(val,i); });
    else rows.forEach(r=>{ out+=num(r.out); ret+=num(r.returned); cons+=num(r.consumed); qty+=num(r.current); before+=num(r.value_before); vat+=num(r.vat); gross+=num(r.gross); });
    const cards=[['عدد السطور',fmtQty(count)]];
    if(mode==='stock') cards.push(['إجمالي الكمية المتبقية',fmtQty(qty)]);
    if(mode==='usage') cards.push(['إجمالي الصادر',fmtQty(out)],['إجمالي المرتجع',fmtQty(ret)],['إجمالي المستهلك',fmtQty(cons)],['إجمالي المتبقي الحالي',fmtQty(qty)]);
    cards.push(['Total amount before 15% VAT',fmtMoney(before)],['Total VAT 15%',fmtMoney(vat)],['Total Value with 15% VAT',fmtMoney(gross)]);
    return `<section class="totals-bottom"><h3>إجماليات نهاية التقرير</h3><div class="totals-grid">${cards.map(([a,b])=>`<div class="total-card"><span>${esc(a)}</span><b>${esc(b)}</b></div>`).join('')}</div></section>`;
  }
  function title(kind){ return ({expensesByProject:'تقرير المصروفات التفصيلي',stockOutByProject:'تقرير صرف المخزون حسب المشروع - تفصيلي',stockOutBySupervisor:'تقرير استهلاك المشرفين - تفصيلي',productDetail:'تقرير تفاصيل منتج',stockReport:'تقرير المخزون العام التفصيلي',costCenters:'تقرير مراكز التكلفة التفصيلي',inventoryUsageDetail:'تقرير توضيحي تفصيلي للاستهلاك'})[kind]||'تقرير'; }
  function filtersText(){
    const parts=[]; const mode=$('inventoryReportDateMode')?.value||'all';
    if(mode==='day') parts.push('اليوم: '+($('inventoryReportDay')?.value||today())); if(mode==='month') parts.push('الشهر: '+($('inventoryReportMonth')?.value||today().slice(0,7)));
    const code=$('financeReportCostCodeFilter')?.value; if(code) parts.push('رمز التكلفة: '+code);
    const sup=$('inventoryReportSupplier')?.value; if(sup) parts.push('المورد: '+sup);
    const p=$('inventoryReportProduct')?.selectedOptions?.[0]?.textContent; if($('inventoryReportProduct')?.value && p) parts.push('المنتج: '+p.trim());
    const per=$('inventoryReportPerson')?.selectedOptions?.[0]?.textContent; if($('inventoryReportPerson')?.value && per) parts.push('المستلم: '+per.trim());
    const q=$('financeReportQuickSearch')?.value || $('financeSearch')?.value; if(q) parts.push('بحث: '+q);
    return parts.join(' | ') || 'كل البيانات';
  }
  window.financePrintReport = function(kind){
    try{
      if(typeof window.financeRenderReports==='function') window.financeRenderReports();
      kind = kind || document.querySelector('.smart-report-panel.active')?.getAttribute('data-report-panel') || 'expensesByProject';
      let rows, htmlTable, mode;
      if(kind==='expensesByProject'){ rows=filteredExpenses(); htmlTable=expensesTable(rows); mode='expenses'; }
      else if(kind==='stockReport'){ rows=filteredItems(); htmlTable=stockTable(rows); mode='stock'; }
      else { rows=filteredUsage(); if(kind==='productDetail'){ const prod=$('inventoryReportProduct')?.value||''; if(prod) rows=rows.filter(r=>String(r.item_id)===String(prod)); } htmlTable=detailedUsageTable(rows); mode='usage'; }
      const reportTitle=title(kind); const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${esc(reportTitle)}</title><style>
        @page{size:A3 landscape;margin:7mm}*{box-sizing:border-box}body{margin:0;font-family:Tahoma,Arial,sans-serif;color:#17231f;background:white;font-size:8.2px;-webkit-print-color-adjust:exact;print-color-adjust:exact}.page{padding:9px;border:1px solid #aabdb5;min-height:100vh;overflow:visible}.header{display:flex;align-items:center;justify-content:space-between;border-bottom:3px solid #0a5a49;padding-bottom:7px;margin-bottom:7px;break-inside:avoid}.brand{display:flex;align-items:center;gap:8px}.brand img{width:48px;height:48px;object-fit:contain}.brand h2{margin:0;font-size:17px;color:#0a5a49}.brand p{margin:3px 0 0;color:#65756f}.report-title{text-align:left}.report-title h1{margin:0;color:#0a5a49;font-size:20px}.report-title p{margin:4px 0 0;color:#66736e}.meta{display:grid;grid-template-columns:1fr 2.5fr 1fr;gap:6px;margin-bottom:7px;break-inside:avoid}.meta div{border:1px solid #d7e4df;background:#f8fbfa;border-radius:8px;padding:6px}.meta b{color:#0a5a49}table{width:100%;border-collapse:collapse;direction:rtl;margin-top:6px;table-layout:fixed;page-break-inside:auto}thead{display:table-header-group}tfoot{display:table-footer-group}tr{page-break-inside:avoid;page-break-after:auto}th{background:#0a5a49;color:#fff;font-weight:800}td,th{border:1px solid #8da69d;padding:3px;text-align:center;vertical-align:middle;white-space:normal;overflow-wrap:anywhere;word-break:break-word;line-height:1.35}td:nth-child(3),td:nth-child(9),td:nth-child(10),td:nth-child(24){text-align:right}tbody tr:nth-child(even) td{background:#f6f9f8}.totals-bottom{margin-top:10px;break-inside:avoid;page-break-inside:avoid}.totals-bottom h3{display:inline-block;margin:0 0 6px;background:#0a5a49;color:white;padding:6px 16px;border-radius:999px;font-size:12px}.totals-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px}.total-card{border:1px solid #cfddd8;border-radius:9px;padding:6px;background:#fff}.total-card span{display:block;color:#62736d;font-size:9px}.total-card b{display:block;color:#0a5a49;font-size:12px;margin-top:3px;direction:ltr;text-align:right}.signs{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:10px;break-inside:avoid}.sign{height:45px;border:1px dashed #9fb4ac;border-radius:10px;padding:7px}.footer{text-align:center;margin-top:8px;color:#6a766f;border-top:1px solid #d9e6e1;padding-top:6px}.print-hint{font-size:9px;color:#6a766f;margin-top:4px}@media print{body{zoom:.92}}
      </style></head><body><div class="page"><div class="header"><div class="brand"><img src="tasneef_logo_print.png" onerror="this.style.display='none'"><div><h2>شركة تصنيف</h2><p>إدارة المرافق والتشغيل والصيانة</p></div></div><div class="report-title"><h1>${esc(reportTitle)}</h1><p>تقرير تفصيلي للمصروفات والمخزون</p></div></div><div class="meta"><div><b>تاريخ الطباعة:</b><br>${esc(new Date().toLocaleString('en-US'))}</div><div><b>الفلاتر المستخدمة:</b><br>${esc(filtersText())}</div><div><b>المستخدم:</b><br>${esc(window.currentUser?.full_name || window.currentUser?.username || 'النظام')}</div></div>${htmlTable}${totalsBlock(rows,mode)}<div class="signs"><div class="sign">إعداد:</div><div class="sign">مراجعة:</div><div class="sign">اعتماد:</div></div><div class="footer">يظهر هذا التقرير جميع التفاصيل والأسعار قبل الضريبة وبعدها وشامل الضريبة حسب الفلاتر الحالية<br><span class="print-hint">ملاحظة: التقارير الكبيرة مهيأة للطباعة على A3 أفقي حتى تظهر الأعمدة كاملة.</span></div></div><script>window.onload=function(){setTimeout(function(){window.print()},350)}</script></body></html>`;
      const w=window.open('','_blank'); if(w){ w.document.write(html); w.document.close(); }
    }catch(e){ console.error(e); if(typeof window.msg==='function') msg('تعذر طباعة التقرير التفصيلي: '+(e.message||e),'err'); }
  };
})();

/* ===== V145: Fix FM/CN cost code and large detailed print sizing ===== */

/* ===== V146: A4 auto landscape print + reliable FM/CN cost code ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const n = v => { const x = Number(String(v ?? 0).replace(/[^0-9.\-]/g,'')); return Number.isFinite(x) ? x : 0; };
  const qty = v => Number(n(v)).toLocaleString('en-US',{maximumFractionDigits:2});
  const money = v => Number(n(v)).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}) + ' SAR';
  const today = () => { const d=new Date(); return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0'); };
  const dateOnly = v => String(v || '').slice(0,10);
  const monthEnd = m => { const [y,mo]=String(m||today().slice(0,7)).split('-').map(Number); return `${y}-${String(mo).padStart(2,'0')}-${String(new Date(y,mo,0).getDate()).padStart(2,'0')}`; };
  const inRange = (d,a,b) => d >= a && d <= b;
  const centers = () => (window.data?.costCenters || window.data?.cost_centers || []).filter(c => String(c.status || 'active') !== 'inactive');
  const projects = () => (window.data?.projects || []);
  function itemById(id){ return (window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id)) || {}; }
  function projectName(id){ return (typeof window.financeProjectName==='function' ? window.financeProjectName(id) : '') || (typeof window.projectName==='function' ? window.projectName(id) : '') || projects().find(p=>String(p.id)===String(id))?.name || ''; }
  function userName(id){ return (typeof window.supervisorName==='function' ? window.supervisorName(id) : '') || (window.data?.users||[]).find(u=>String(u.id)===String(id))?.full_name || ''; }
  function itemCode(it){ return it.product_code || it.code || it.serial_number || it.sku || it.id || ''; }
  function itemBarcode(it){ return it.supplier_barcode || it.barcode || it.vendor_barcode || ''; }
  function normalizeTaxStatus(it){ return String(it.vat_status || it.tax_status || it.vat_type || it.tax_type || '').trim().toLowerCase(); }
  function isExempt(it){ return /خالي|معفى|exempt|zero|0%|no vat/i.test(normalizeTaxStatus(it)); }
  function isIncluded(it){ return /شامل|included|include|gross/i.test(normalizeTaxStatus(it)) || it.vat_included === true || it.tax_included === true; }
  function unitBefore(it, raw){ raw = n(raw); if(isExempt(it)) return raw; if(isIncluded(it)) return raw / 1.15; return raw; }
  function unitVat(it, raw){ return isExempt(it) ? 0 : unitBefore(it, raw) * 0.15; }
  function unitGross(it, raw){ return unitBefore(it, raw) + unitVat(it, raw); }
  function amountVat(it, before){ return isExempt(it) ? 0 : n(before) * 0.15; }
  function amountGross(it, before){ return n(before) + amountVat(it, before); }
  function findCostCenter(row){
    if(row?.cost_center_id){ const c = centers().find(x => String(x.id) === String(row.cost_center_id)); if(c) return c; }
    const names = [row?.cost_center_name, row?.cost_center, row?.costCenterName].filter(Boolean).map(x=>String(x).trim());
    for(const name of names){ const c = centers().find(x => String(x.name || '').trim() === name); if(c) return c; }
    return null;
  }
  function projectExistsByName(name){ const s = String(name||'').trim(); return !!s && projects().some(p => String(p.name||'').trim() === s); }
  function costCode(row){
    const c = findCostCenter(row);
    if(c){
      const type = String(c.type || c.cost_type || c.category || '').trim();
      const code = String(c.code || c.cost_code || '').trim().toUpperCase();
      if(code === 'CN' || /اداري|إداري|قسم|عام|شركة|كلين|clean|admin|administrative|department|general|cn/i.test(type)) return 'CN';
      if(code === 'FM' || c.project_id || /مشروع|project|fm/i.test(type)) return 'FM';
      // إذا مركز التكلفة غير مربوط بمشروع فهو إداري/عام افتراضًا.
      return 'CN';
    }
    if(row?.cost_center_type && /اداري|إداري|قسم|عام|شركة|كلين|clean|admin|department|cn/i.test(String(row.cost_center_type))) return 'CN';
    if(row?.project_id) return 'FM';
    if(row?.project && projectExistsByName(row.project)) return 'FM';
    return 'CN';
  }
  window.inventoryCostCodeV146 = costCode;
  function lineItems(req){
    let arr = req?.request_items || req?.items || req?.request_lines || [];
    if(typeof arr === 'string'){ try{ arr = JSON.parse(arr); }catch(e){ arr = []; } }
    if(!Array.isArray(arr)) arr = [];
    if(!arr.length && req?.item_id) arr = [{item_id:req.item_id,item_name:req.item_name,quantity:req.quantity,unit_cost:req.unit_cost}];
    return arr;
  }
  function returnedQty(requestId, itemId){
    return (window.data?.inventoryMovements||[])
      .filter(m => String(m.request_id||'') === String(requestId||'') && String(m.item_id||'') === String(itemId||'') && /return|مرتجع/.test(String(m.movement_type||'')))
      .reduce((a,m)=>a+n(m.quantity),0);
  }
  function datePass(d){
    d = dateOnly(d || today());
    const mode = $('inventoryReportDateMode')?.value || 'all';
    if(mode === 'day') return d === ($('inventoryReportDay')?.value || today());
    if(mode === 'month'){ const m = $('inventoryReportMonth')?.value || today().slice(0,7); return inRange(d, m+'-01', monthEnd(m)); }
    return true;
  }
  function quickTextPass(row){
    const q = ($('financeReportQuickSearch')?.value || $('financeSearch')?.value || '').trim().toLowerCase();
    if(!q) return true;
    return Object.values(row).join(' ').toLowerCase().includes(q);
  }
  function usageRows(){
    const rows = [];
    (window.data?.inventoryRequests||[]).filter(r => String(r.status||'') === 'approved').forEach(r => {
      const d = dateOnly(r.request_date || r.created_at || today());
      const project = r.project_name || projectName(r.project_id) || 'بدون مشروع';
      const person = r.supervisor_name || userName(r.supervisor_id) || 'بدون مستلم';
      lineItems(r).forEach(l => {
        const it = itemById(l.item_id);
        const out = n(l.quantity);
        const ret = returnedQty(r.id, l.item_id);
        const cons = Math.max(0, out - ret);
        const rawUnit = n(l.unit_cost || l.unit_price || it.unit_cost || it.unit_price || 0);
        const beforeUnit = unitBefore(it, rawUnit);
        const before = cons * beforeUnit;
        rows.push({date:d, project, project_id:r.project_id, person, person_id:r.supervisor_id, item:l.item_name||it.name||'', item_id:l.item_id, product_code:itemCode(it), barcode:itemBarcode(it), supplier:it.supplier||'', unit_name:it.unit||'', item_type:it.item_type||it.type||'', category:it.category||'', unit_before:beforeUnit, unit_vat:unitVat(it, rawUnit), unit_gross:unitGross(it, rawUnit), out, returned:ret, consumed:cons, current:n(it.quantity), value_before:before, vat:amountVat(it,before), gross:amountGross(it,before), cost_center_id:r.cost_center_id||'', cost_center_name:r.cost_center_name||project, cost_center_type:r.cost_center_type||'', movement_type:'طلب صرف معتمد', ref:'REQ-'+r.id, reason:r.reason||r.notes||''});
      });
    });
    (window.data?.inventoryMovements||[]).filter(m => ['out','consume'].includes(String(m.movement_type)) && !m.request_id).forEach(m => {
      const it = itemById(m.item_id);
      const q = n(m.quantity), rawUnit = n(m.unit_cost || m.unit_price || it.unit_cost || it.unit_price || 0), beforeUnit = unitBefore(it, rawUnit), before = q * beforeUnit;
      const project = m.project_name || projectName(m.project_id) || 'بدون مشروع';
      rows.push({date:dateOnly(m.movement_date||m.created_at||today()), project, project_id:m.project_id, person:m.receiver||'بدون مستلم', person_id:m.receiver_id||'', item:m.item_name||it.name||'', item_id:m.item_id, product_code:itemCode(it), barcode:itemBarcode(it), supplier:it.supplier||'', unit_name:it.unit||'', item_type:it.item_type||it.type||'', category:it.category||'', unit_before:beforeUnit, unit_vat:unitVat(it,rawUnit), unit_gross:unitGross(it,rawUnit), out:q, returned:0, consumed:q, current:n(it.quantity), value_before:before, vat:amountVat(it,before), gross:amountGross(it,before), cost_center_id:m.cost_center_id||'', cost_center_name:m.cost_center_name||project, cost_center_type:m.cost_center_type||'', movement_type:'صرف مباشر', ref:'MOV-'+m.id, reason:m.reason||m.notes||''});
    });
    return rows;
  }
  function filterUsage(rows){
    const supplier = $('inventoryReportSupplier')?.value || '';
    const prod = $('inventoryReportProduct')?.value || '';
    const person = $('inventoryReportPerson')?.value || '';
    const cc = $('financeReportCostCodeFilter')?.value || '';
    return rows.filter(r => datePass(r.date) && (!supplier || r.supplier === supplier) && (!prod || String(r.item_id) === String(prod)) && (!person || String(r.person_id) === String(person) || String(r.person) === String(person)) && (!cc || costCode(r) === cc) && quickTextPass(r));
  }
  function filteredItems(){
    const supplier = $('inventoryReportSupplier')?.value || '';
    const q = ($('financeReportQuickSearch')?.value || $('financeSearch')?.value || '').trim().toLowerCase();
    return (window.data?.inventoryItems||[]).filter(i => (!supplier || i.supplier === supplier) && (!q || [i.name,itemCode(i),itemBarcode(i),i.supplier,i.category,i.item_type,i.type].join(' ').toLowerCase().includes(q)));
  }
  function filteredExpenses(){
    const cc = $('financeReportCostCodeFilter')?.value || '';
    return (window.data?.financeExpenses||[]).filter(e => datePass(e.expense_date||e.created_at) && (!cc || costCode(e) === cc) && quickTextPass(e));
  }
  function table(heads, trs){ return `<table><thead><tr>${heads.map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${trs.join('') || `<tr><td colspan="${heads.length}">لا توجد بيانات</td></tr>`}</tbody></table>`; }
  function usageTable(rows){
    const heads=['التاريخ','رمز التكلفة','المشروع / مركز التكلفة','المشرف / المستلم','كود المنتج','باركود المورد','اسم المنتج','المورد','الوحدة','سعر الحبة قبل الضريبة','ضريبة الحبة 15%','سعر الحبة شامل','الصادر','المرتجع','المستهلك','المتبقي','القيمة قبل الضريبة','VAT 15%','الإجمالي شامل VAT','نوع الحركة','المرجع','سبب الاستخدام'];
    const trs = rows.map(r => `<tr><td>${esc(r.date)}</td><td>${esc(costCode(r))}</td><td>${esc(r.cost_center_name||r.project||'-')}</td><td>${esc(r.person||'-')}</td><td>${esc(r.product_code||'-')}</td><td>${esc(r.barcode||'-')}</td><td>${esc(r.item||'-')}</td><td>${esc(r.supplier||'-')}</td><td>${esc(r.unit_name||'-')}</td><td>${money(r.unit_before)}</td><td>${money(r.unit_vat)}</td><td>${money(r.unit_gross)}</td><td>${qty(r.out)}</td><td>${qty(r.returned)}</td><td>${qty(r.consumed)}</td><td>${qty(r.current)}</td><td>${money(r.value_before)}</td><td>${money(r.vat)}</td><td>${money(r.gross)}</td><td>${esc(r.movement_type)}</td><td>${esc(r.ref)}</td><td>${esc(r.reason)}</td></tr>`);
    return table(heads, trs);
  }
  function stockTable(items){
    const heads=['كود المنتج','باركود المورد','اسم المنتج','التصنيف','نوع المنتج','الوحدة','المورد','سعر الحبة قبل الضريبة','ضريبة الحبة 15%','سعر الحبة شامل','الكمية المتبقية','حد إعادة الطلب','قيمة المخزون قبل الضريبة','VAT 15%','Total Value with 15% VAT'];
    const trs = items.map(i => { const raw=n(i.unit_cost||i.unit_price), unit=unitBefore(i,raw), qtyVal=n(i.quantity), val=qtyVal*unit; return `<tr><td>${esc(itemCode(i))}</td><td>${esc(itemBarcode(i)||'-')}</td><td>${esc(i.name||'-')}</td><td>${esc(i.category||'-')}</td><td>${esc(i.item_type||i.type||'-')}</td><td>${esc(i.unit||'-')}</td><td>${esc(i.supplier||'-')}</td><td>${money(unit)}</td><td>${money(unitVat(i,raw))}</td><td>${money(unitGross(i,raw))}</td><td>${qty(qtyVal)}</td><td>${qty(i.min_quantity)}</td><td>${money(val)}</td><td>${money(amountVat(i,val))}</td><td>${money(amountGross(i,val))}</td></tr>`; });
    return table(heads, trs);
  }
  function expenseTable(rows){
    const heads=['التاريخ','رمز التكلفة','المشروع / مركز التكلفة','نوع المصروف','المورد','طريقة الدفع','المبلغ قبل الضريبة','VAT 15%','الإجمالي شامل VAT','ملاحظات'];
    const trs = rows.map(e => { const before=n(e.amount||e.subtotal||e.before_vat||(n(e.total)/1.15)), vat=('vat' in e)?n(e.vat):before*.15, total=n(e.total||before+vat); const row={...e,project:e.project_name||projectName(e.project_id)||e.cost_center_name||''}; return `<tr><td>${esc(dateOnly(e.expense_date||e.created_at))}</td><td>${esc(costCode(row))}</td><td>${esc(row.project||'بدون مركز تكلفة')}</td><td>${esc(e.category||e.expense_type||'-')}</td><td>${esc(e.supplier||'-')}</td><td>${esc(e.payment_method||'-')}</td><td>${money(before)}</td><td>${money(vat)}</td><td>${money(total)}</td><td>${esc(e.notes||'-')}</td></tr>`; });
    return table(heads, trs);
  }
  function totalsBlock(rows, mode){
    let out=0, ret=0, cons=0, current=0, before=0, vat=0, gross=0;
    if(mode==='stock') rows.forEach(i=>{ const unit=unitBefore(i,n(i.unit_cost||i.unit_price)), val=n(i.quantity)*unit; current+=n(i.quantity); before+=val; vat+=amountVat(i,val); gross+=amountGross(i,val); });
    else if(mode==='expenses') rows.forEach(e=>{ const b=n(e.amount||e.subtotal||e.before_vat||(n(e.total)/1.15)), v=('vat' in e)?n(e.vat):b*.15, g=n(e.total||b+v); before+=b; vat+=v; gross+=g; });
    else rows.forEach(r=>{ out+=n(r.out); ret+=n(r.returned); cons+=n(r.consumed); current+=n(r.current); before+=n(r.value_before); vat+=n(r.vat); gross+=n(r.gross); });
    const cards=[['عدد السطور',qty(rows.length)]];
    if(mode==='usage') cards.push(['إجمالي الصادر',qty(out)],['إجمالي المرتجع',qty(ret)],['إجمالي المستهلك',qty(cons)],['إجمالي المتبقي الحالي',qty(current)]);
    if(mode==='stock') cards.push(['إجمالي الكمية المتبقية',qty(current)]);
    cards.push(['Total amount before 15% VAT',money(before)],['Total VAT 15%',money(vat)],['Total Value with 15% VAT',money(gross)]);
    return `<section class="totals-bottom"><h3>إجماليات نهاية التقرير</h3><div class="totals-grid">${cards.map(([a,b])=>`<div class="total-card"><span>${esc(a)}</span><b>${esc(b)}</b></div>`).join('')}</div></section>`;
  }
  function title(kind){ return ({expensesByProject:'تقرير المصروفات التفصيلي',stockOutByProject:'تقرير صرف المخزون حسب المشروع - تفصيلي',stockOutBySupervisor:'تقرير استهلاك المشرفين - تفصيلي',productDetail:'تقرير تفاصيل منتج',stockReport:'تقرير المخزون العام التفصيلي',costCenters:'تقرير مراكز التكلفة التفصيلي',inventoryUsageDetail:'تقرير توضيحي تفصيلي للاستهلاك'})[kind] || 'تقرير'; }
  function filtersText(){
    const parts=[]; const mode=$('inventoryReportDateMode')?.value||'all';
    if(mode==='day') parts.push('اليوم: '+($('inventoryReportDay')?.value||today())); if(mode==='month') parts.push('الشهر: '+($('inventoryReportMonth')?.value||today().slice(0,7)));
    const cc=$('financeReportCostCodeFilter')?.value; if(cc) parts.push('رمز التكلفة: '+cc);
    const sup=$('inventoryReportSupplier')?.value; if(sup) parts.push('المورد: '+sup);
    const prod=$('inventoryReportProduct')?.selectedOptions?.[0]?.textContent; if($('inventoryReportProduct')?.value && prod) parts.push('المنتج: '+prod.trim());
    const person=$('inventoryReportPerson')?.selectedOptions?.[0]?.textContent; if($('inventoryReportPerson')?.value && person) parts.push('المستلم: '+person.trim());
    const q=$('financeReportQuickSearch')?.value || $('financeSearch')?.value; if(q) parts.push('بحث: '+q);
    return parts.join(' | ') || 'كل البيانات';
  }
  function activeKind(){ return document.querySelector('.smart-report-panel.active')?.getAttribute('data-report-panel') || document.querySelector('.smart-report-card.active')?.getAttribute('data-report') || 'expensesByProject'; }
  function openPrint(titleText, htmlTable, rows, mode){
    const tmp=document.createElement('div'); tmp.innerHTML=htmlTable; const colCount=tmp.querySelectorAll('thead th').length;
    const isWide = colCount > 10;
    const pageSize = isWide ? 'A4 landscape' : 'A4 portrait';
    const fontSize = colCount > 18 ? '6.6px' : (colCount > 14 ? '7.4px' : (isWide ? '8.5px' : '10.5px'));
    const pad = colCount > 18 ? '2px' : (isWide ? '3px' : '6px');
    const html = `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${esc(titleText)}</title><style>
      @page{size:${pageSize};margin:7mm}*{box-sizing:border-box}body{margin:0;font-family:Tahoma,Arial,sans-serif;color:#17231f;background:white;font-size:${fontSize};-webkit-print-color-adjust:exact;print-color-adjust:exact}.page{padding:8px;border:1px solid #aabdb5;min-height:100vh;overflow:visible}.header{display:flex;align-items:center;justify-content:space-between;border-bottom:3px solid #0a5a49;padding-bottom:7px;margin-bottom:7px;break-inside:avoid}.brand{display:flex;align-items:center;gap:8px}.brand img{width:46px;height:46px;object-fit:contain}.brand h2{margin:0;font-size:16px;color:#0a5a49}.brand p{margin:3px 0 0;color:#65756f}.report-title{text-align:left}.report-title h1{margin:0;color:#0a5a49;font-size:18px}.report-title p{margin:4px 0 0;color:#66736e}.meta{display:grid;grid-template-columns:1fr 2.5fr 1fr;gap:6px;margin-bottom:7px;break-inside:avoid}.meta div{border:1px solid #d7e4df;background:#f8fbfa;border-radius:8px;padding:6px}.meta b{color:#0a5a49}table{width:100%;border-collapse:collapse;direction:rtl;margin-top:6px;table-layout:fixed;page-break-inside:auto}thead{display:table-header-group}tfoot{display:table-footer-group}tr{page-break-inside:avoid;page-break-after:auto}th{background:#0a5a49;color:#fff;font-weight:800}td,th{border:1px solid #8da69d;padding:${pad};text-align:center;vertical-align:middle;white-space:normal;overflow-wrap:anywhere;word-break:break-word;line-height:1.28}tbody tr:nth-child(even) td{background:#f6f9f8}.totals-bottom{margin-top:10px;break-inside:avoid;page-break-inside:avoid}.totals-bottom h3{display:inline-block;margin:0 0 6px;background:#0a5a49;color:white;padding:6px 16px;border-radius:999px;font-size:11px}.totals-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px}.total-card{border:1px solid #cfddd8;border-radius:9px;padding:6px;background:#fff}.total-card span{display:block;color:#62736d;font-size:9px}.total-card b{display:block;color:#0a5a49;font-size:11px;margin-top:3px;direction:ltr;text-align:right}.signs{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:10px;break-inside:avoid}.sign{height:42px;border:1px dashed #9fb4ac;border-radius:10px;padding:7px}.footer{text-align:center;margin-top:8px;color:#6a766f;border-top:1px solid #d9e6e1;padding-top:6px}.print-hint{font-size:9px;color:#6a766f;margin-top:4px}@media print{body{zoom:1}.page{break-after:auto}}
    </style></head><body><div class="page"><div class="header"><div class="brand"><img src="tasneef_logo_print.png" onerror="this.style.display='none'"><div><h2>شركة تصنيف</h2><p>إدارة المرافق والتشغيل والصيانة</p></div></div><div class="report-title"><h1>${esc(titleText)}</h1><p>تقرير تفصيلي للمصروفات والمخزون</p></div></div><div class="meta"><div><b>تاريخ الطباعة:</b><br>${esc(new Date().toLocaleString('en-US'))}</div><div><b>الفلاتر المستخدمة:</b><br>${esc(filtersText())}</div><div><b>المستخدم:</b><br>${esc(window.currentUser?.full_name || window.currentUser?.username || 'النظام')}</div></div>${htmlTable}${totalsBlock(rows,mode)}<div class="signs"><div class="sign">إعداد:</div><div class="sign">مراجعة:</div><div class="sign">اعتماد:</div></div><div class="footer">الجداول العريضة تُطبع تلقائيًا على A4 بالعرض حتى تظهر كاملة، والتقارير العادية تطبع بالطول.</div></div><script>window.onload=function(){setTimeout(function(){window.print()},350)}</script></body></html>`;
    const w = window.open('','_blank'); if(w){ w.document.write(html); w.document.close(); }
  }
  window.financePrintReport = function(kind){
    try{
      if(typeof window.financeRenderReports === 'function') window.financeRenderReports();
      kind = kind || activeKind();
      let rows, htmlTable, mode;
      if(kind === 'expensesByProject'){ rows = filteredExpenses(); htmlTable = expenseTable(rows); mode='expenses'; }
      else if(kind === 'stockReport'){ rows = filteredItems(); htmlTable = stockTable(rows); mode='stock'; }
      else { rows = filterUsage(usageRows()); if(kind === 'productDetail'){ const prod=$('inventoryReportProduct')?.value||''; if(prod) rows = rows.filter(r => String(r.item_id) === String(prod)); } htmlTable = usageTable(rows); mode='usage'; }
      openPrint(title(kind), htmlTable, rows, mode);
    }catch(e){ console.error(e); if(typeof window.msg==='function') msg('تعذر طباعة التقرير: '+(e.message||e),'err'); }
  };
  // Fix visible Cost Centers report as well, so FM/CN in screen and print match.
  const oldRender = window.financeRenderReports;
  window.financeRenderReports = function(){
    if(oldRender) oldRender.apply(this, arguments);
    try{
      const body = $('costCenterReportBody'); if(!body) return;
      const map = {};
      filterUsage(usageRows()).forEach(r => {
        const name = r.cost_center_name || r.project || 'غير محدد';
        const code = costCode(r);
        map[name] = map[name] || {code,count:0,out:0,ret:0,cons:0,before:0,vat:0,gross:0};
        const x = map[name]; x.count++; x.out+=n(r.out); x.ret+=n(r.returned); x.cons+=n(r.consumed); x.before+=n(r.value_before); x.vat+=n(r.vat); x.gross+=n(r.gross);
      });
      const vals = Object.entries(map).sort((a,b)=>b[1].gross-a[1].gross);
      body.innerHTML = vals.map(([name,v]) => `<tr><td>${esc(name)}</td><td><span class="badge ${v.code==='FM'?'green':'neutral'}">${v.code}</span></td><td>${qty(v.count)}</td><td>${qty(v.out)}</td><td>${qty(v.ret)}</td><td>${qty(v.cons)}</td><td>${money(v.before)}</td><td>${money(v.vat)}</td><td>${money(v.gross)}</td></tr>`).join('') || '<tr><td colspan="9">لا توجد بيانات</td></tr>';
    }catch(e){ console.warn('V146 cost centers render warning', e); }
  };
})();

/* ===== V147: Smart Tickets UI - visual layer only, same workflow ===== */
(function(){
  const $id = (id)=>document.getElementById(id);
  const safe = (v)=> (typeof esc === 'function' ? esc(v) : String(v ?? '').replace(/[&<>\"']/g, m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m])));
  const fdate = (v)=> (typeof fmt === 'function' ? fmt(v) : (v ? new Date(v).toLocaleString('ar-SA') : '-'));
  const pName = (id)=> (typeof projectName === 'function' ? projectName(id) : '-');
  const sName = (id)=> (typeof supervisorName === 'function' ? supervisorName(id) : '-');
  const no = (t)=> t.ticket_number || ('T-' + String(t.id||0).padStart(4,'0'));
  const minsBetweenSafe = (a,b)=>{ try{ return typeof minutesBetween==='function' ? minutesBetween(a,b) : Math.max(0, Math.round((new Date(b||Date.now())-new Date(a||Date.now()))/60000)); }catch(e){ return 0; } };
  const duration = (t)=>{ try{ const m = (t.status==='closed' && t.open_duration_minutes != null) ? Number(t.open_duration_minutes||0) : minsBetweenSafe(t.created_at, new Date().toISOString()); return (typeof durationLabel==='function') ? durationLabel(m) : (Math.floor(m/60)+'س '+(m%60)+'د'); }catch(e){return '-';} };
  const stLabel = (st)=> st==='closed'?'مغلق':(st==='processing'?'تحت المعالجة':'مفتوح');
  const prLabel = (p)=> p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي'));
  const stClass = (st)=> st==='closed'?'done':(st==='processing'?'doing':'open');
  const prClass = (p)=> p==='urgent'?'urgent':(p==='high'?'high':(p==='low'?'low':'normal'));
  function filterAdmin(rows){
    const st=$id('ticketFilterStatus')?.value||'';
    const q=($id('ticketSearch')?.value||'').trim().toLowerCase();
    let list=[...(rows||[])];
    if(st) list=list.filter(t=>String(t.status||'open')===String(st));
    if(q) list=list.filter(t=>[no(t),t.title,t.description,pName(t.project_id),sName(t.supervisor_id),stLabel(t.status),prLabel(t.priority),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));
    return list;
  }
  function filterSupervisor(rows){
    const st=$id('supTicketFilterStatus')?.value||'';
    const pid=$id('supTicketFilterProject')?.value||'';
    const q=($id('supTicketSearch')?.value||'').trim().toLowerCase();
    let list=[...(rows||[])];
    if(pid) list=list.filter(t=>String(t.project_id)===String(pid));
    if(st) list=list.filter(t=>String(t.status||'open')===String(st));
    if(q) list=list.filter(t=>[no(t),t.title,t.description,pName(t.project_id),stLabel(t.status),prLabel(t.priority),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));
    return list;
  }
  function summaryHtml(list){
    const total=list.length, open=list.filter(t=>(t.status||'open')==='open').length, proc=list.filter(t=>t.status==='processing').length, closed=list.filter(t=>t.status==='closed').length, urgent=list.filter(t=>['urgent','high'].includes(t.priority)).length;
    return `
      <div class="smart-ticket-kpi"><b>${total}</b><span>إجمالي التكتات</span></div>
      <div class="smart-ticket-kpi red"><b>${open}</b><span>مفتوحة</span></div>
      <div class="smart-ticket-kpi amber"><b>${proc}</b><span>تحت المعالجة</span></div>
      <div class="smart-ticket-kpi green"><b>${closed}</b><span>مغلقة</span></div>
      <div class="smart-ticket-kpi dark"><b>${urgent}</b><span>عاجلة / مهمة</span></div>`;
  }
  function cardHtml(t, mode){
    const canDelete = mode==='admin';
    const waFn = (typeof window.sendTicketWhatsAppV43 === 'function') ? 'sendTicketWhatsAppV43' : ((typeof window.sendTicketWhatsApp === 'function') ? 'sendTicketWhatsApp' : 'sendTicketWhatsAppV43');
    const actions = `
      <button type="button" onclick="viewTicketSmartV147(${Number(t.id)||0})">عرض</button>
      <button type="button" class="light" onclick="editTicket(${Number(t.id)||0})">تعديل</button>
      ${t.status==='closed' ? `<button type="button" class="light" onclick="setTicketStatus(${Number(t.id)||0},'open')">إعادة فتح</button>` : `${t.status!=='processing'?`<button type="button" class="light" onclick="claimTicket(${Number(t.id)||0})">استلام</button>`:''}<button type="button" onclick="closeTicket(${Number(t.id)||0})">إغلاق</button>`}
      ${typeof window[waFn] === 'function' ? `<button type="button" class="ticket-wa-v147" onclick="${waFn}(${Number(t.id)||0})">واتساب</button>` : ''}
      ${canDelete?`<button type="button" class="danger" onclick="deleteRow('tickets',${Number(t.id)||0})">حذف</button>`:''}`;
    return `<article class="smart-ticket-card ${stClass(t.status)} ${prClass(t.priority)}">
      <div class="smart-ticket-top">
        <div><strong>${safe(no(t))}</strong><small>${safe(fdate(t.created_at))}</small></div>
        <span class="smart-ticket-status ${stClass(t.status)}">${safe(stLabel(t.status))}</span>
      </div>
      <h3>${safe(t.title||'-')}</h3>
      <div class="smart-ticket-meta">
        <span>المشروع: <b>${safe(pName(t.project_id))}</b></span>
        <span>المشرف: <b>${safe(sName(t.supervisor_id))}</b></span>
        <span>الأولوية: <b>${safe(prLabel(t.priority))}</b></span>
        <span>مدة الفتح: <b>${safe(duration(t))}</b></span>
      </div>
      <p>${safe(t.description||'لا يوجد وصف')}</p>
      <div class="smart-ticket-mini">
        <span>استلم: ${safe(t.claimed_by_name||'-')}</span>
        <span>أغلق: ${safe(t.closed_by_name||'-')}</span>
      </div>
      ${t.closure_note?`<div class="smart-ticket-note">الحل: ${safe(t.closure_note)}</div>`:''}
      <div class="smart-ticket-actions">${actions}</div>
    </article>`;
  }
  function renderInto(containerId, summaryId, list, mode){
    const body=$id(containerId); if(!body) return;
    const sum=$id(summaryId); if(sum) sum.innerHTML=summaryHtml(list);
    body.classList.add('smart-ticket-grid');
    body.innerHTML = list.map(t=>cardHtml(t,mode)).join('') || '<div class="empty-smart-ticket">لا توجد تكتات مطابقة للبحث الحالي</div>';
  }
  window.renderTickets = function(){
    const all=[...(data.tickets||[])];
    if($id('ticketsBody')) renderInto('ticketsBody','ticketsSmartSummary', filterAdmin(all), 'admin');
    if($id('supTicketsBody')) renderInto('supTicketsBody','supTicketsSmartSummary', filterSupervisor(all), 'supervisor');
  };
  window.viewTicketSmartV147 = function(id){
    const t=(data.tickets||[]).find(x=>String(x.id)===String(id)); if(!t) return msg && msg('التكت غير موجود','err');
    let modal=$id('ticketSmartModalV147');
    if(!modal){ modal=document.createElement('div'); modal.id='ticketSmartModalV147'; modal.className='smart-modal-v147 hidden'; document.body.appendChild(modal); }
    modal.innerHTML=`<div class="smart-modal-backdrop" onclick="closeTicketSmartV147()"></div>
      <div class="smart-modal-card">
        <button class="smart-modal-close" onclick="closeTicketSmartV147()">×</button>
        <div class="smart-modal-head"><div><small>${safe(no(t))}</small><h2>${safe(t.title||'-')}</h2></div><span class="smart-ticket-status ${stClass(t.status)}">${safe(stLabel(t.status))}</span></div>
        <div class="smart-modal-grid">
          <div><label>المشروع</label><b>${safe(pName(t.project_id))}</b></div>
          <div><label>المشرف</label><b>${safe(sName(t.supervisor_id))}</b></div>
          <div><label>الأولوية</label><b>${safe(prLabel(t.priority))}</b></div>
          <div><label>مدة الفتح</label><b>${safe(duration(t))}</b></div>
          <div><label>تاريخ الإنشاء</label><b>${safe(fdate(t.created_at))}</b></div>
          <div><label>تاريخ الإغلاق</label><b>${safe(t.closed_at?fdate(t.closed_at):'-')}</b></div>
        </div>
        <div class="smart-modal-section"><h3>وصف البلاغ</h3><p>${safe(t.description||'لا يوجد وصف')}</p></div>
        <div class="smart-modal-section"><h3>سجل المعالجة</h3>
          <div class="ticket-timeline-v147">
            <div><b>تم إنشاء التكت</b><span>${safe(fdate(t.created_at))}</span></div>
            ${t.claimed_at?`<div><b>تم استلام التكت</b><span>${safe(t.claimed_by_name||'-')} - ${safe(fdate(t.claimed_at))}</span></div>`:''}
            ${t.closed_at?`<div><b>تم إغلاق التكت</b><span>${safe(t.closed_by_name||'-')} - ${safe(fdate(t.closed_at))}</span></div>`:''}
          </div>
        </div>
        ${t.closure_note?`<div class="smart-modal-section"><h3>طريقة الإغلاق</h3><p>${safe(t.closure_note)}</p></div>`:''}
        <div class="smart-modal-actions">
          <button onclick="editTicket(${Number(t.id)||0});closeTicketSmartV147();">تعديل</button>
          ${t.status==='closed'?`<button class="light" onclick="setTicketStatus(${Number(t.id)||0},'open');closeTicketSmartV147();">إعادة فتح</button>`:`${t.status!=='processing'?`<button class="light" onclick="claimTicket(${Number(t.id)||0});closeTicketSmartV147();">استلام</button>`:''}<button onclick="closeTicket(${Number(t.id)||0});closeTicketSmartV147();">إغلاق</button>`}
        </div>
      </div>`;
    modal.classList.remove('hidden');
  };
  window.closeTicketSmartV147 = function(){ const m=$id('ticketSmartModalV147'); if(m) m.classList.add('hidden'); };
  const css=document.createElement('style'); css.textContent=`
    .smart-ticket-summary{display:grid;grid-template-columns:repeat(5,minmax(120px,1fr));gap:10px;margin:10px 0 14px}.smart-ticket-kpi{background:#fff;border:1px solid #e5ece7;border-radius:16px;padding:12px;text-align:center;box-shadow:0 8px 22px rgba(0,0,0,.05)}.smart-ticket-kpi b{display:block;font-size:22px;color:#174d35}.smart-ticket-kpi span{font-size:12px;color:#61716a}.smart-ticket-kpi.red b{color:#b83232}.smart-ticket-kpi.amber b{color:#b7791f}.smart-ticket-kpi.green b{color:#138a4b}.smart-ticket-kpi.dark b{color:#1f2937}
    .smart-ticket-grid{display:grid!important;grid-template-columns:repeat(auto-fit,minmax(290px,1fr));gap:14px;align-items:stretch}.smart-ticket-card{background:#fff;border:1px solid #e5ece7;border-radius:20px;padding:16px;box-shadow:0 10px 26px rgba(0,0,0,.06);position:relative;overflow:hidden}.smart-ticket-card:before{content:"";position:absolute;inset-inline-start:0;top:0;bottom:0;width:5px;background:#174d35}.smart-ticket-card.open:before{background:#b83232}.smart-ticket-card.doing:before{background:#d69e2e}.smart-ticket-card.done:before{background:#138a4b}.smart-ticket-top{display:flex;justify-content:space-between;gap:10px;align-items:flex-start}.smart-ticket-top strong{font-size:15px;color:#153f2c}.smart-ticket-top small{display:block;color:#7b8a83;margin-top:3px}.smart-ticket-status{border-radius:999px;padding:5px 10px;font-size:12px;font-weight:700;white-space:nowrap}.smart-ticket-status.open{background:#fde8e8;color:#9b1c1c}.smart-ticket-status.doing{background:#fff7db;color:#8a5a00}.smart-ticket-status.done{background:#e5f7ec;color:#116c3b}.smart-ticket-card h3{margin:12px 0 8px;font-size:18px;color:#10291d}.smart-ticket-meta{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin:8px 0}.smart-ticket-meta span,.smart-ticket-mini span{background:#f7faf8;border:1px solid #edf3ef;border-radius:12px;padding:7px;font-size:12px;color:#60726a}.smart-ticket-card p{color:#43534c;line-height:1.65;margin:10px 0;min-height:42px}.smart-ticket-mini{display:grid;grid-template-columns:1fr 1fr;gap:7px}.smart-ticket-note{margin-top:8px;background:#f4fbf6;border:1px dashed #bfe1c9;border-radius:12px;padding:8px;font-size:12px;color:#24563b}.smart-ticket-actions{display:flex;flex-wrap:wrap;gap:6px;margin-top:12px}.smart-ticket-actions button,.smart-modal-actions button{padding:8px 10px;border-radius:11px}.ticket-wa-v147{background:#128C7E!important;color:#fff!important}.empty-smart-ticket{grid-column:1/-1;text-align:center;background:#fff;border-radius:16px;padding:25px;color:#6b7d74;border:1px dashed #d3ded8}
    .smart-modal-v147.hidden{display:none}.smart-modal-v147{position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:20px}.smart-modal-backdrop{position:absolute;inset:0;background:rgba(0,0,0,.45);backdrop-filter:blur(2px)}.smart-modal-card{position:relative;background:#fff;border-radius:24px;padding:22px;max-width:760px;width:min(760px,96vw);max-height:90vh;overflow:auto;box-shadow:0 20px 60px rgba(0,0,0,.25)}.smart-modal-close{position:absolute;top:12px;left:12px;border-radius:50%;width:34px;height:34px;padding:0;background:#f2f5f3;color:#333}.smart-modal-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;border-bottom:1px solid #edf2ef;padding-bottom:12px;margin-bottom:14px}.smart-modal-head small{color:#6b7d74}.smart-modal-head h2{margin:4px 0 0;color:#173c2b}.smart-modal-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.smart-modal-grid div{background:#f8fbf9;border:1px solid #edf3ef;border-radius:14px;padding:10px}.smart-modal-grid label{display:block;font-size:11px;color:#718078;margin-bottom:5px}.smart-modal-section{margin-top:14px;background:#fff;border:1px solid #edf3ef;border-radius:16px;padding:12px}.smart-modal-section h3{margin:0 0 8px;color:#173c2b}.ticket-timeline-v147{display:grid;gap:8px}.ticket-timeline-v147 div{border-right:3px solid #174d35;background:#f8fbf9;border-radius:12px;padding:9px}.ticket-timeline-v147 span{display:block;color:#6b7d74;font-size:12px;margin-top:3px}.smart-modal-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.page#tickets .grid.two{grid-template-columns:minmax(280px,360px) 1fr!important}
    @media(max-width:900px){.smart-ticket-summary{grid-template-columns:repeat(2,1fr)}.smart-modal-grid{grid-template-columns:1fr}.page#tickets .grid.two{grid-template-columns:1fr!important}.smart-ticket-meta{grid-template-columns:1fr}}
  `; document.head.appendChild(css);
})();

/* ===== V148: Inventory catalog + stock entry batches + request insert hardening ===== */
(function(){
  'use strict';
  const VAT_RATE_V148 = 0.15;
  let batchLinesV148 = window.batchLinesV148 || [];
  window.batchLinesV148 = batchLinesV148;
  let pendingLineImageV148 = '';
  let batchDataLoadedV148 = false;

  const $v = id => document.getElementById(id);
  const n = v => { const x = parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const s = v => String(v ?? '');
  const money = v => n(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  const qty = v => n(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const html = v => (typeof esc === 'function') ? esc(v) : s(v).replace(/[&<>"']/g, m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const todayV = () => (typeof today === 'function' ? today() : new Date().toISOString().slice(0,10));
  const productCodeOf = it => s(it?.product_code || it?.serial_number || it?.barcode || it?.supplier_barcode || '');
  const barcodeOf = it => s(it?.barcode || it?.supplier_barcode || it?.product_code || it?.serial_number || '');
  const vatCalc = (price, mode) => {
    price = n(price); mode = mode || 'exclusive';
    if(mode === 'inclusive'){ const net = price / (1 + VAT_RATE_V148); return {net, vat: price-net, gross: price}; }
    if(mode === 'exempt'){ return {net: price, vat: 0, gross: price}; }
    return {net: price, vat: price * VAT_RATE_V148, gross: price * (1 + VAT_RATE_V148)};
  };

  function relabelInventoryFormV148(){
    try{
      const bar = $v('inventoryItemBarcode');
      if(bar){ const lab = bar.closest('div')?.querySelector('label'); if(lab) lab.textContent='كود المنتج'; bar.placeholder='كود المنتج / باركود المنتج'; }
      const qtyInp = $v('inventoryItemQty');
      if(qtyInp){ const lab = qtyInp.closest('div')?.querySelector('label'); if(lab) lab.textContent='الكمية'; }
      const h = [...document.querySelectorAll('#financeTabInventory h2')].find(x=>x.textContent.trim()==='قائمة المخزون');
      if(h) h.textContent='قائمة الأصناف';
      const itemFormTitle = [...document.querySelectorAll('#financeTabInventory h2')].find(x=>x.textContent.includes('إضافة / تعديل صنف'));
      if(itemFormTitle) itemFormTitle.textContent='إضافة / تعديل صنف في الكتالوج';
    }catch(e){}
  }

  function ensureBatchUiV148(){
    const tab = $v('financeTabInventory'); if(!tab || $v('stockBatchCardV148')) return;
    relabelInventoryFormV148();
    const wrap = tab.querySelector('.report-premium-wrap') || tab;
    const card = document.createElement('div');
    card.className = 'card report-form-card';
    card.id = 'stockBatchCardV148';
    card.innerHTML = `
      <h2>إدخال مخزون / فاتورة متعددة الأصناف</h2>
      <div class="split"><div><label>تاريخ الفاتورة</label><input type="date" id="batchDateV148"></div><div><label>المورد</label><input id="batchSupplierV148" placeholder="اسم المورد"></div></div>
      <div class="split"><div><label>رقم الفاتورة / المرجع</label><input id="batchInvoiceV148" placeholder="مثال: INV-2026-001"></div><div><label>حالة الضريبة</label><select id="batchVatModeV148" onchange="stockBatchRenderLinesV148()"><option value="exclusive">غير شامل الضريبة</option><option value="inclusive">شامل الضريبة</option><option value="exempt">خالي الضريبة</option></select></div></div>
      <div class="nested-card">
        <h3>إضافة صنف داخل نفس الفاتورة</h3>
        <div class="split"><div><label>كود المنتج</label><input id="batchProductCodeV148" placeholder="كود المنتج"></div><div><label>اسم الصنف</label><input id="batchProductNameV148" placeholder="اسم الصنف"></div></div>
        <div class="split"><div><label>التصنيف</label><input id="batchCategoryV148" placeholder="نظافة / كهرباء / سباكة"></div><div><label>نوع المنتج</label><select id="batchItemTypeV148"><option>مادة</option><option>أداة</option><option>معدات</option><option>قطعة غيار</option><option>أخرى</option></select></div></div>
        <div class="split"><div><label>الوحدة</label><input id="batchUnitV148" placeholder="حبة / لتر / متر"></div><div><label>الكمية</label><input type="number" id="batchQtyV148" step="0.01"></div></div>
        <div class="split"><div><label>سعر الوحدة حسب حالة الضريبة</label><input type="number" id="batchUnitPriceV148" step="0.01"></div><div><label>صورة الصنف الجديد</label><label class="stage-upload">+ اختيار صورة<input type="file" accept="image/*" onchange="stockBatchHandleImageV148(this)"></label></div></div>
        <div id="batchLineImagePreviewV148" class="img-previews"><small class="muted">الصورة إلزامية إذا كان الصنف جديدًا وغير موجود في قائمة الأصناف.</small></div>
        <div class="actions"><button type="button" class="light" onclick="stockBatchAddLineV148()">+ إضافة الصنف للفاتورة</button><button type="button" class="light" onclick="stockBatchClearLineV148()">تفريغ السطر</button></div>
      </div>
      <div class="table-wrap"><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>الكمية</th><th>سعر قبل الضريبة</th><th>VAT 15%</th><th>سعر شامل</th><th>الإجمالي شامل</th><th>حذف</th></tr></thead><tbody id="batchLinesBodyV148"><tr><td colspan="8">لم تتم إضافة أصناف بعد</td></tr></tbody></table></div>
      <div id="batchTotalsV148" class="footer-note"></div>
      <div class="actions"><button type="button" onclick="stockBatchSaveV148(this)">حفظ فاتورة الإدخال</button><button type="button" class="light" onclick="stockBatchPrintDraftV148()">طباعة مسودة</button><button type="button" class="light" onclick="stockBatchClearV148()">تفريغ الفاتورة</button></div>
    `;
    wrap.insertBefore(card, wrap.firstElementChild);
    const list = document.createElement('div');
    list.className = 'card';
    list.id = 'stockBatchesListCardV148';
    list.innerHTML = `<div class="table-head"><h2>قائمة المخزون</h2><button class="light" onclick="stockBatchLoadV148(true)">تحديث</button></div><div class="table-wrap"><table><thead><tr><th>الرقم</th><th>التاريخ</th><th>المورد</th><th>عدد الأصناف</th><th>قبل الضريبة</th><th>VAT</th><th>الإجمالي شامل</th><th>إجراء</th></tr></thead><tbody id="stockBatchesBodyV148"><tr><td colspan="8">لا توجد فواتير إدخال</td></tr></tbody></table></div>`;
    const itemListCard = [...wrap.querySelectorAll('.card')].find(c=>c.querySelector('#inventoryItemsBody'));
    if(itemListCard) wrap.insertBefore(list, itemListCard); else wrap.appendChild(list);
    if($v('batchDateV148')) $v('batchDateV148').value = todayV();
  }

  async function compressFileV148(f){
    if(typeof compressImageToDataUrl === 'function') return await compressImageToDataUrl(f, 900, .82);
    return await new Promise((res,rej)=>{ const r=new FileReader(); r.onload=()=>res(r.result); r.onerror=rej; r.readAsDataURL(f); });
  }
  window.stockBatchHandleImageV148 = async function(input){
    const f=(input.files||[])[0]; if(!f) return;
    pendingLineImageV148 = await compressFileV148(f);
    const box=$v('batchLineImagePreviewV148'); if(box) box.innerHTML=`<div class="preview-img"><img src="${pendingLineImageV148}"><button type="button" onclick="stockBatchRemoveLineImageV148()">×</button></div>`;
  };
  window.stockBatchRemoveLineImageV148 = function(){ pendingLineImageV148=''; const box=$v('batchLineImagePreviewV148'); if(box) box.innerHTML='<small class="muted">الصورة إلزامية إذا كان الصنف جديدًا وغير موجود في قائمة الأصناف.</small>'; };
  window.stockBatchClearLineV148 = function(){ ['batchProductCodeV148','batchProductNameV148','batchCategoryV148','batchUnitV148','batchQtyV148','batchUnitPriceV148'].forEach(id=>{ if($v(id)) $v(id).value=''; }); if($v('batchItemTypeV148')) $v('batchItemTypeV148').value='مادة'; stockBatchRemoveLineImageV148(); };
  window.stockBatchAddLineV148 = function(){
    const code=s($v('batchProductCodeV148')?.value).trim(); const name=s($v('batchProductNameV148')?.value).trim(); const q=n($v('batchQtyV148')?.value); const price=n($v('batchUnitPriceV148')?.value);
    if(!code) return msg('كود المنتج مطلوب','err'); if(!name) return msg('اسم الصنف مطلوب','err'); if(q<=0) return msg('الكمية مطلوبة','err'); if(price<0) return msg('سعر الوحدة غير صحيح','err');
    const existingLine = batchLinesV148.find(l=>String(l.product_code).trim()===code);
    if(existingLine){ existingLine.quantity += q; existingLine.unit_price = price; existingLine.unit = s($v('batchUnitV148')?.value)||existingLine.unit||'حبة'; existingLine.image_url = existingLine.image_url || pendingLineImageV148; }
    else batchLinesV148.push({ product_code:code, item_name:name, category:s($v('batchCategoryV148')?.value)||'أخرى', item_type:s($v('batchItemTypeV148')?.value)||'مادة', unit:s($v('batchUnitV148')?.value)||'حبة', quantity:q, unit_price:price, image_url:pendingLineImageV148 });
    stockBatchClearLineV148(); stockBatchRenderLinesV148();
  };
  window.stockBatchRemoveLineV148 = function(code){ batchLinesV148=batchLinesV148.filter(l=>String(l.product_code)!==String(code)); window.batchLinesV148=batchLinesV148; stockBatchRenderLinesV148(); };
  window.stockBatchRenderLinesV148 = function(){
    const b=$v('batchLinesBodyV148'); if(!b) return;
    const mode=$v('batchVatModeV148')?.value||'exclusive';
    let netTotal=0, vatTotal=0, grossTotal=0;
    b.innerHTML = batchLinesV148.map(l=>{ const c=vatCalc(l.unit_price, mode); const net=c.net*n(l.quantity), vat=c.vat*n(l.quantity), gross=c.gross*n(l.quantity); netTotal+=net; vatTotal+=vat; grossTotal+=gross; return `<tr><td>${html(l.product_code)}</td><td><b>${html(l.item_name)}</b><br><small>${html(l.category)} / ${html(l.item_type)}</small></td><td>${qty(l.quantity)} ${html(l.unit)}</td><td>${money(c.net)} ر.س</td><td>${money(c.vat)} ر.س</td><td>${money(c.gross)} ر.س</td><td>${money(gross)} ر.س</td><td><button class="danger" type="button" onclick="stockBatchRemoveLineV148('${html(l.product_code)}')">حذف</button></td></tr>`; }).join('') || '<tr><td colspan="8">لم تتم إضافة أصناف بعد</td></tr>';
    const t=$v('batchTotalsV148'); if(t) t.innerHTML = `<b>Total amount before 15% VAT:</b> ${money(netTotal)} SAR &nbsp; | &nbsp; <b>Total VAT 15%:</b> ${money(vatTotal)} SAR &nbsp; | &nbsp; <b>Total Value with 15% VAT:</b> ${money(grossTotal)} SAR`;
  };

  function findItemByCode(code){ code=s(code).trim(); return (window.data?.inventoryItems||[]).find(i=>[i.product_code,i.serial_number,i.barcode,i.supplier_barcode].map(x=>s(x).trim()).includes(code)); }

  async function ensureBatchTablesExistV148(){
    if(!window.sb) return false;
    const test = await sb.from('inventory_batches').select('id').limit(1);
    return !test.error;
  }

  window.stockBatchSaveV148 = async function(btn){
    try{
      if(btn) btn.disabled=true;
      if(!batchLinesV148.length) throw new Error('أضف صنفًا واحدًا على الأقل داخل الفاتورة');
      const supplier=s($v('batchSupplierV148')?.value).trim(); if(!supplier) throw new Error('اسم المورد مطلوب');
      const invoiceNo=s($v('batchInvoiceV148')?.value).trim() || ('ADD-'+Date.now());
      const date=s($v('batchDateV148')?.value)||todayV(); const mode=$v('batchVatModeV148')?.value||'exclusive';
      let netTotal=0, vatTotal=0, grossTotal=0; const savedLines=[]; const movements=[];
      for(const l of batchLinesV148){
        let item=findItemByCode(l.product_code);
        if(!item && !l.image_url) throw new Error('الصنف الجديد '+l.item_name+' يحتاج صورة قبل الحفظ');
        const calc=vatCalc(l.unit_price, mode); const lineNet=calc.net*n(l.quantity), lineVat=calc.vat*n(l.quantity), lineGross=calc.gross*n(l.quantity);
        netTotal+=lineNet; vatTotal+=lineVat; grossTotal+=lineGross;
        if(item){
          const newQty=n(item.quantity)+n(l.quantity);
          const upd={quantity:newQty, unit_cost:calc.net, supplier, category:l.category||item.category, unit:l.unit||item.unit, product_code:l.product_code, serial_number:l.product_code, barcode:l.product_code, supplier_barcode:l.product_code, item_type:l.item_type||item.item_type, type:l.item_type||item.type};
          const res=await sb.from('inventory_items').update(upd).eq('id', item.id).select('*').single(); if(res.error) throw res.error; item=res.data;
        }else{
          const ins={name:l.item_name, serial_number:l.product_code, product_code:l.product_code, barcode:l.product_code, supplier_barcode:l.product_code, image_url:l.image_url, category:l.category||'أخرى', item_type:l.item_type||'مادة', type:l.item_type||'مادة', unit:l.unit||'حبة', quantity:n(l.quantity), min_quantity:0, unit_cost:calc.net, supplier, notes:'تم إنشاؤه من فاتورة إدخال '+invoiceNo};
          const res=await sb.from('inventory_items').insert(ins).select('*').single(); if(res.error) throw res.error; item=res.data;
        }
        const mv={item_id:item.id,item_name:item.name,movement_type:'in',quantity:n(l.quantity),movement_date:date,project_id:null,project_name:'',receiver:supplier,reason:'إدخال مخزون - فاتورة '+invoiceNo,notes:'batch:'+invoiceNo,product_code:l.product_code,barcode:l.product_code,unit_cost:calc.net};
        const mres=await sb.from('inventory_movements').insert(mv).select('*').single(); if(mres.error) throw mres.error;
        movements.push(mres.data);
        savedLines.push({...l,item_id:item.id,unit_cost:calc.net,unit_vat:calc.vat,unit_gross:calc.gross,line_net:lineNet,line_vat:lineVat,line_gross:lineGross,movement_id:mres.data.id});
      }
      const hasBatchTables = await ensureBatchTablesExistV148();
      if(hasBatchTables){
        const bres=await sb.from('inventory_batches').insert({batch_date:date,supplier,invoice_no:invoiceNo,vat_mode:mode,total_before_vat:netTotal,total_vat:vatTotal,total_with_vat:grossTotal,notes:'فاتورة إدخال مخزون'}).select('*').single();
        if(bres.error) throw bres.error;
        const bid=bres.data.id;
        const rows=savedLines.map(l=>({batch_id:bid,item_id:l.item_id,product_code:l.product_code,item_name:l.item_name,category:l.category,item_type:l.item_type,unit:l.unit,quantity:l.quantity,unit_price_before_vat:l.unit_cost,unit_vat:l.unit_vat,unit_price_with_vat:l.unit_gross,total_before_vat:l.line_net,total_vat:l.line_vat,total_with_vat:l.line_gross,movement_id:l.movement_id}));
        const li=await sb.from('inventory_batch_items').insert(rows); if(li.error) throw li.error;
      }
      msg('تم حفظ فاتورة إدخال المخزون');
      stockBatchPrintV148({invoice_no:invoiceNo,batch_date:date,supplier,vat_mode:mode,lines:savedLines,total_before_vat:netTotal,total_vat:vatTotal,total_with_vat:grossTotal});
      stockBatchClearV148(); await financeLoadAll(); await stockBatchLoadV148(true);
    }catch(e){ msg(e.message||String(e),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };
  window.stockBatchClearV148 = function(){ batchLinesV148=[]; window.batchLinesV148=batchLinesV148; ['batchSupplierV148','batchInvoiceV148'].forEach(id=>{ if($v(id)) $v(id).value=''; }); if($v('batchDateV148')) $v('batchDateV148').value=todayV(); stockBatchClearLineV148(); stockBatchRenderLinesV148(); };
  window.stockBatchPrintDraftV148 = function(){ const mode=$v('batchVatModeV148')?.value||'exclusive'; let net=0,vat=0,gross=0; const lines=batchLinesV148.map(l=>{ const c=vatCalc(l.unit_price,mode); const line_net=c.net*n(l.quantity), line_vat=c.vat*n(l.quantity), line_gross=c.gross*n(l.quantity); net+=line_net; vat+=line_vat; gross+=line_gross; return {...l,unit_cost:c.net,unit_vat:c.vat,unit_gross:c.gross,line_net,line_vat,line_gross}; }); stockBatchPrintV148({invoice_no:s($v('batchInvoiceV148')?.value)||'مسودة',batch_date:s($v('batchDateV148')?.value)||todayV(),supplier:s($v('batchSupplierV148')?.value)||'-',lines,total_before_vat:net,total_vat:vat,total_with_vat:gross}); };

  window.stockBatchPrintV148 = function(batch){
    const rows=(batch.lines||[]).map(l=>`<tr><td>${html(l.product_code)}</td><td>${html(l.item_name)}</td><td>${html(l.category||'')}</td><td>${html(l.item_type||'')}</td><td>${html(l.unit||'')}</td><td>${qty(l.quantity)}</td><td>${money(l.unit_cost)} SAR</td><td>${money(l.unit_vat)} SAR</td><td>${money(l.unit_gross)} SAR</td><td>${money(l.line_gross)} SAR</td></tr>`).join('');
    const body=`<div class="grid"><div class="box"><b>ID</b><br>${html(batch.invoice_no)}</div><div class="box"><b>Date</b><br>${html(batch.batch_date)}</div><div class="box"><b>Warehouse / Supplier</b><br>${html(batch.supplier)}</div></div><h2>Products</h2><table><thead><tr><th>Product Code</th><th>Product Name</th><th>Category</th><th>Type</th><th>Unit</th><th>Quantity Added</th><th>Unit Price</th><th>VAT 15%</th><th>Unit Price with VAT</th><th>Value</th></tr></thead><tbody>${rows||'<tr><td colspan="10">لا توجد أصناف</td></tr>'}</tbody></table><div style="margin-top:22px;font-size:16px;line-height:2"><b>Total amount before 15% VAT:</b> ${money(batch.total_before_vat)} SAR<br><b>Total VAT 15%:</b> ${money(batch.total_vat)} SAR<br><b>Total Value with 15% VAT:</b> ${money(batch.total_with_vat)} SAR</div>`;
    if(typeof financePrintWindow === 'function') financePrintWindow('Added Product', body); else { const w=window.open('','_blank'); w.document.write(body); w.print(); }
  };

  async function fetchBatchesFromTablesV148(){
    const ok=await ensureBatchTablesExistV148(); if(!ok) return null;
    const [batches,lines]=await Promise.all([sb.from('inventory_batches').select('*').order('batch_date',{ascending:false}), sb.from('inventory_batch_items').select('*')]);
    if(batches.error || lines.error) return null;
    return (batches.data||[]).map(b=>({...b, lines:(lines.data||[]).filter(l=>String(l.batch_id)===String(b.id))}));
  }
  function buildBatchesFromMovementsV148(){
    const map={};
    (window.data?.inventoryMovements||[]).filter(m=>m.movement_type==='in').forEach(m=>{
      const ref=(s(m.notes).match(/batch:([^\s]+)/)||[])[1] || (s(m.reason).match(/فاتورة\s+(.+)$/)||[])[1] || ('MOV-'+m.id);
      if(!map[ref]) map[ref]={invoice_no:ref,batch_date:m.movement_date,supplier:m.receiver||'-',lines:[],total_before_vat:0,total_vat:0,total_with_vat:0};
      const it=(window.data?.inventoryItems||[]).find(x=>String(x.id)===String(m.item_id))||{};
      const c=vatCalc(n(m.unit_cost||it.unit_cost), 'exclusive'); const qn=n(m.quantity); const ln=c.net*qn, lv=c.vat*qn, lg=c.gross*qn;
      map[ref].lines.push({product_code:m.product_code||productCodeOf(it),item_name:m.item_name,category:it.category,item_type:it.item_type||it.type,unit:it.unit,quantity:qn,unit_cost:c.net,unit_vat:c.vat,unit_gross:c.gross,line_net:ln,line_vat:lv,line_gross:lg});
      map[ref].total_before_vat+=ln; map[ref].total_vat+=lv; map[ref].total_with_vat+=lg;
    });
    return Object.values(map).sort((a,b)=>s(b.batch_date).localeCompare(s(a.batch_date)));
  }
  window.stockBatchLoadV148 = async function(force){
    try{
      if(!force && batchDataLoadedV148) return stockBatchRenderBatchesV148(window.stockBatchesV148||[]);
      let rows = await fetchBatchesFromTablesV148();
      if(!rows) rows = buildBatchesFromMovementsV148();
      window.stockBatchesV148 = rows || [];
      batchDataLoadedV148 = true;
      stockBatchRenderBatchesV148(window.stockBatchesV148);
    }catch(e){ console.warn(e); }
  };
  window.stockBatchRenderBatchesV148 = function(rows){
    const b=$v('stockBatchesBodyV148'); if(!b) return;
    b.innerHTML=(rows||[]).map((r,idx)=>`<tr><td>${html(r.invoice_no||r.id||idx+1)}</td><td>${html(r.batch_date||'')}</td><td>${html(r.supplier||'-')}</td><td>${(r.lines||[]).length}</td><td>${money(r.total_before_vat)} ر.س</td><td>${money(r.total_vat)} ر.س</td><td>${money(r.total_with_vat)} ر.س</td><td class="row-actions"><button class="light" onclick="stockBatchPrintSavedV148('${idx}')">طباعة</button></td></tr>`).join('') || '<tr><td colspan="8">لا توجد فواتير إدخال</td></tr>';
  };
  window.stockBatchPrintSavedV148 = function(idx){ const b=(window.stockBatchesV148||[])[Number(idx)]; if(b) stockBatchPrintV148(b); };

  // Improve item save: duplicate product code updates existing item instead of creating duplicate.
  const oldSaveItem = window.inventorySaveItem;
  window.inventorySaveItem = async function(btn){
    try{
      if(btn) btn.disabled=true;
      const name=s($v('inventoryItemName')?.value).trim(); if(!name) throw new Error('اسم الصنف مطلوب');
      const code=s($v('inventoryItemSerial')?.value || $v('inventoryItemBarcode')?.value).trim() || (typeof inventoryGenerateSerial==='function'?inventoryGenerateSerial():'INV-'+Date.now());
      const id=s($v('inventoryItemId')?.value).trim();
      const duplicate=(window.data?.inventoryItems||[]).find(i=>String(i.id)!==id && [i.product_code,i.serial_number,i.barcode,i.supplier_barcode].map(x=>s(x).trim()).includes(code));
      if(duplicate && !id){
        // Update existing item instead of duplicating it
        const row={
          name, serial_number:code, product_code:code, barcode:code, supplier_barcode:code,
          image_url: inventoryItemImageData || duplicate.image_url || null,
          category:$v('inventoryItemCategory')?.value||duplicate.category||'أخرى', item_type:$v('inventoryItemType')?.value||duplicate.item_type||'مادة', type:$v('inventoryItemType')?.value||duplicate.type||'مادة', unit:$v('inventoryItemUnit')?.value||duplicate.unit||'حبة',
          quantity:n(duplicate.quantity)+n($v('inventoryItemQty')?.value), min_quantity:n($v('inventoryItemMin')?.value), unit_cost:n($v('inventoryItemCost')?.value), supplier:$v('inventoryItemSupplier')?.value||duplicate.supplier||'', notes:$v('inventoryItemNotes')?.value||duplicate.notes||''
        };
        const res=await sb.from('inventory_items').update(row).eq('id', duplicate.id); if(res.error) throw res.error;
        msg('الصنف موجود مسبقًا؛ تم تحديث كميته وبياناته بدل تكراره'); inventoryClearItemForm(); await financeLoadAll(); return;
      }
      if(!id && !inventoryItemImageData) throw new Error('صورة المنتج إلزامية عند إضافة صنف جديد');
      const row={name, serial_number:code, product_code:code, barcode:code, supplier_barcode:code, image_url:inventoryItemImageData||null, category:$v('inventoryItemCategory')?.value||'أخرى', item_type:$v('inventoryItemType')?.value||'مادة', type:$v('inventoryItemType')?.value||'مادة', unit:$v('inventoryItemUnit')?.value||'حبة', quantity:n($v('inventoryItemQty')?.value), min_quantity:n($v('inventoryItemMin')?.value), unit_cost:n($v('inventoryItemCost')?.value), supplier:$v('inventoryItemSupplier')?.value||'', notes:$v('inventoryItemNotes')?.value||''};
      const res=id?await sb.from('inventory_items').update(row).eq('id',id):await sb.from('inventory_items').insert(row); if(res.error) throw res.error;
      msg('تم حفظ الصنف'); inventoryClearItemForm(); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };

  // Harden request save when schema cache is stale; tell user exactly what to run.
  async function safeInsertInventoryRequestV148(row){
    const res = await sb.from('inventory_requests').insert(row);
    if(!res.error) return res;
    if(String(res.error.message||'').includes('request_items') || String(res.error.message||'').includes('schema cache')){
      throw new Error('قاعدة البيانات تحتاج تحديث طلبات الصرف. شغّل ملف schema_update_v148_inventory_batches_requests.sql في Supabase ثم جرّب مرة أخرى.');
    }
    throw res.error;
  }
  window.safeInsertInventoryRequestV148 = safeInsertInventoryRequestV148;

  // Patch known request save functions to use safe insert if possible.
  const oldAdminReq = window.inventorySaveRequest;
  if(oldAdminReq){
    window.inventorySaveRequest = async function(btn){
      try{
        if(btn) btn.disabled=true;
        const pid=$v('inventoryRequestProject')?.value, supervisorId=$v('inventoryRequestSupervisor')?.value;
        if(!pid) throw new Error('اختر المشروع'); if(!supervisorId) throw new Error('اختر المشرف / الفني');
        const lines=Array.isArray(window.inventoryRequestLines)?window.inventoryRequestLines:(typeof inventoryRequestLines!=='undefined'?inventoryRequestLines:[]);
        if(!lines.length && $v('inventoryRequestItem')?.value){ const item=(window.data?.inventoryItems||[]).find(i=>String(i.id)===String($v('inventoryRequestItem').value)); const q=n($v('inventoryRequestQty')?.value); if(item && q>0) lines.push({item_id:item.id,item_name:item.name,product_code:productCodeOf(item),barcode:barcodeOf(item),quantity:q,available:n(item.quantity),unit:item.unit||'',unit_cost:n(item.unit_cost)}); }
        if(!lines.length) throw new Error('أضف صنفًا واحدًا على الأقل إلى أمر الصرف');
        const path=inventoryGetApprovalPath(); const first=path[0]||'warehouse';
        const cc = (typeof selectedCostCenterPayload==='function') ? selectedCostCenterPayload('inventoryRequestCostCenter', pid) : {cost_center_id:null,cost_center_name:projectName(pid)};
        const row={item_id:Number(lines[0].item_id),item_name:lines.map(x=>x.item_name).join('، '),quantity:lines.reduce((a,x)=>a+n(x.quantity),0),request_items:lines,items:lines,project_id:Number(pid),project_name:projectName(pid),cost_center_id:cc.cost_center_id,cost_center_name:cc.cost_center_name,supervisor_id:Number(supervisorId),supervisor_name:supervisorName(supervisorId),request_date:$v('inventoryRequestDate')?.value||todayV(),reason:$v('inventoryRequestReason')?.value||'',notes:$v('inventoryRequestNotes')?.value||'',status:'pending_'+first,current_step:first,approval_path:path,approval_log:[]};
        await safeInsertInventoryRequestV148(row); msg('تم إرسال طلب الصرف للاعتماد'); if(typeof inventoryClearRequestForm==='function') inventoryClearRequestForm(); await financeLoadAll();
      }catch(e){ msg(e.message||String(e),'err'); }
      finally{ if(btn) btn.disabled=false; }
    };
  }
  const oldSupReq = window.supervisorSaveInventoryRequest;
  if(oldSupReq){
    window.supervisorSaveInventoryRequest = async function(btn){
      try{
        if(btn) btn.disabled=true; const u=session()||{}; const pid=$v('supInventoryRequestProject')?.value; if(!pid) throw new Error('اختر المشروع');
        const lines=Array.isArray(window.supInventoryRequestLines)?window.supInventoryRequestLines:(typeof supInventoryRequestLines!=='undefined'?supInventoryRequestLines:[]);
        if(!lines.length) throw new Error('أضف صنف واحد على الأقل للطلب');
        const path=inventoryGetApprovalPath(); const first=path[0]||'warehouse'; const cc = (typeof selectedCostCenterPayload==='function') ? selectedCostCenterPayload('supInventoryRequestCostCenter', pid) : {cost_center_id:null,cost_center_name:projectName(pid)};
        const row={item_id:Number(lines[0].item_id),item_name:lines.map(x=>x.item_name).join('، '),quantity:lines.reduce((a,l)=>a+n(l.quantity),0),request_items:lines,items:lines,project_id:Number(pid),project_name:projectName(pid),cost_center_id:cc.cost_center_id,cost_center_name:cc.cost_center_name,supervisor_id:u.id,supervisor_name:u.full_name||u.username||'',request_date:$v('supInventoryRequestDate')?.value||todayV(),reason:$v('supInventoryRequestReason')?.value||'',notes:$v('supInventoryRequestNotes')?.value||'',status:'pending_'+first,current_step:first,approval_path:path,approval_log:[]};
        await safeInsertInventoryRequestV148(row); msg('تم إرسال طلب الصرف للإدارة'); window.supInventoryRequestLines=[]; if(typeof renderSupInventoryRequestLines==='function') renderSupInventoryRequestLines(); ['supInventoryRequestItem','supInventoryRequestQty','supInventoryRequestReason','supInventoryRequestNotes'].forEach(id=>$v(id)&&($v(id).value='')); await supervisorInventoryLoad();
      }catch(e){ msg(e.message||String(e),'err'); }
      finally{ if(btn) btn.disabled=false; }
    };
  }

  // Improve item rendering title and code/barcode filters after render.
  const oldInvRender = window.inventoryRenderItems;
  window.inventoryRenderItems = function(){
    if(typeof oldInvRender === 'function') oldInvRender.apply(this, arguments);
    relabelInventoryFormV148();
  };

  const oldFinanceLoad = window.financeLoadAll;
  window.financeLoadAll = async function(){
    const out = oldFinanceLoad ? await oldFinanceLoad.apply(this, arguments) : undefined;
    ensureBatchUiV148(); relabelInventoryFormV148(); stockBatchLoadV148(true);
    return out;
  };
  const oldFinanceRenderAll = window.financeRenderAll;
  window.financeRenderAll = function(){ if(oldFinanceRenderAll) oldFinanceRenderAll.apply(this, arguments); ensureBatchUiV148(); relabelInventoryFormV148(); stockBatchLoadV148(false); };

  window.addEventListener('load', ()=>{ setTimeout(()=>{ ensureBatchUiV148(); relabelInventoryFormV148(); stockBatchRenderLinesV148(); stockBatchLoadV148(true); }, 700); });
})();

/* ===== V149: Smart stock invoice cards + independent catalog view ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const str = v => String(v ?? '');
  const num2 = v => { const x = parseFloat(str(v).replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const esc2 = v => (typeof esc === 'function') ? esc(v) : str(v).replace(/[&<>"']/g, m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const money2 = v => num2(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  const qty2 = v => num2(v).toLocaleString('en-US',{maximumFractionDigits:2});

  function injectSmartInventoryCssV149(){
    if($('v149SmartInventoryCss')) return;
    const st=document.createElement('style'); st.id='v149SmartInventoryCss';
    st.textContent=`
      #stockBatchCardV148,#stockBatchesListCardV148{display:none!important;}
      #stockBatchCardV148.v149-open{display:block!important;position:fixed;inset:30px;z-index:9999;overflow:auto;max-width:980px;margin:auto;background:#fff;border:1px solid #cfe3dd;border-radius:24px;box-shadow:0 24px 80px rgba(0,45,31,.28);padding:22px;}
      .v149-modal-backdrop{position:fixed;inset:0;background:rgba(0,40,28,.45);z-index:9998;display:none;backdrop-filter:blur(3px)}
      .v149-modal-backdrop.show{display:block;}
      .v149-smart-head{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;margin-bottom:14px;}
      .v149-smart-head h2{margin:0;color:#003f2e;font-size:25px;font-weight:900;}
      .v149-smart-actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap;}
      .v149-filter-bar{display:grid;grid-template-columns:repeat(4,minmax(150px,1fr));gap:10px;margin:12px 0 18px;}
      .v149-filter-bar input,.v149-filter-bar select{height:42px;border:1px solid #cfe3dd;border-radius:14px;padding:0 12px;background:#fff;}
      .v149-invoice-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(270px,1fr));gap:14px;}
      .v149-invoice-card{border:1px solid #cfe3dd;border-radius:22px;background:linear-gradient(180deg,#fff,#f8fcfa);padding:16px;box-shadow:0 10px 24px rgba(0,55,38,.06);transition:.18s ease;}
      .v149-invoice-card:hover{transform:translateY(-2px);box-shadow:0 14px 32px rgba(0,55,38,.10);}
      .v149-invoice-top{display:flex;justify-content:space-between;gap:10px;align-items:flex-start;border-bottom:1px dashed #cfe3dd;padding-bottom:10px;margin-bottom:10px;}
      .v149-invoice-no{font-size:19px;font-weight:900;color:#004532;}
      .v149-invoice-date{font-size:12px;color:#5c736b;background:#edf8f4;border-radius:999px;padding:5px 9px;white-space:nowrap;}
      .v149-invoice-meta{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:10px 0;}
      .v149-mini{background:#eef8f4;border:1px solid #d6eae4;border-radius:14px;padding:9px;text-align:center;}
      .v149-mini small{display:block;color:#60766f;font-size:11px;margin-bottom:3px;}.v149-mini b{color:#003f2e;font-size:15px;}
      .v149-invoice-card .row-actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-start;margin-top:12px;}
      .v149-pill{display:inline-flex;align-items:center;gap:5px;background:#e9f7f2;color:#004532;border-radius:999px;padding:5px 10px;font-size:12px;font-weight:700;}
      .v149-empty{border:1px dashed #cfe3dd;background:#fbfffd;border-radius:18px;padding:28px;text-align:center;color:#647a73;}
      .v149-modal-card{position:fixed;inset:36px;max-width:1050px;margin:auto;z-index:10000;background:#fff;border:1px solid #cfe3dd;border-radius:24px;box-shadow:0 24px 80px rgba(0,45,31,.30);padding:22px;overflow:auto;display:none;}
      .v149-modal-card.show{display:block;}.v149-close{float:left;background:#eef8f4;color:#003f2e;border:1px solid #cfe3dd;border-radius:12px;padding:8px 14px;cursor:pointer;font-weight:800;}
      .v149-catalog-title{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap;}
      #financeTabInventory .report-form-card:not(#stockBatchCardV148){display:none!important;}
      @media(max-width:800px){.v149-filter-bar{grid-template-columns:1fr}.v149-modal-card,#stockBatchCardV148.v149-open{inset:10px;border-radius:18px}.v149-invoice-grid{grid-template-columns:1fr}}
    `;
    document.head.appendChild(st);
  }

  function ensureSmartInventoryUiV149(){
    injectSmartInventoryCssV149();
    const tab=$('financeTabInventory'); if(!tab || $('smartStockInvoicesV149')) return;
    let wrap=tab.querySelector('.report-premium-wrap') || tab;
    const backdrop=document.createElement('div'); backdrop.id='v149Backdrop'; backdrop.className='v149-modal-backdrop'; backdrop.onclick=()=>closeAllV149(); document.body.appendChild(backdrop);
    const hub=document.createElement('div'); hub.id='smartStockInvoicesV149'; hub.className='card';
    hub.innerHTML=`
      <div class="v149-smart-head">
        <div><h2>فواتير إدخال المخزون</h2><p class="muted">كل فاتورة تظهر كشريحة واحدة، وداخلها عدة أصناف. قائمة الأصناف مستقلة ولا تتكرر حسب كود المنتج.</p></div>
        <div class="v149-smart-actions"><button type="button" onclick="openStockBatchModalV149()">+ إضافة فاتورة مخزون</button><button type="button" class="light" onclick="stockBatchLoadV148(true)">تحديث</button></div>
      </div>
      <div class="v149-filter-bar">
        <input id="batchSearchV149" oninput="renderStockBatchCardsV149()" placeholder="بحث برقم الفاتورة / المورد">
        <input type="date" id="batchDateFilterV149" onchange="renderStockBatchCardsV149()" title="فلتر باليوم">
        <input type="month" id="batchMonthFilterV149" onchange="renderStockBatchCardsV149()" title="فلتر بالشهر">
        <button type="button" class="light" onclick="clearBatchFiltersV149()">مسح الفلاتر</button>
      </div>
      <div id="stockBatchCardsGridV149" class="v149-invoice-grid"><div class="v149-empty">لا توجد فواتير إدخال</div></div>
    `;
    wrap.insertBefore(hub, wrap.firstElementChild);
    const itemCard=[...wrap.querySelectorAll('.card')].find(c=>c.querySelector('#inventoryItemsBody'));
    if(itemCard){
      const h=itemCard.querySelector('h2'); if(h) h.textContent='قائمة الأصناف';
      itemCard.classList.add('v149-catalog-card');
      const th=itemCard.querySelector('.table-head');
      if(th && !th.querySelector('.v149-catalog-note')) th.insertAdjacentHTML('beforeend','<small class="v149-catalog-note muted">الكتالوج يعرض كل صنف مرة واحدة حسب كود المنتج.</small>');
    }
    const bcard=$('stockBatchCardV148'); if(bcard && !bcard.querySelector('.v149-close')) bcard.insertAdjacentHTML('afterbegin','<button type="button" class="v149-close" onclick="closeStockBatchModalV149()">إغلاق</button>');
  }

  function closeAllV149(){ closeStockBatchModalV149(); closeInvoiceViewV149(); }
  window.openStockBatchModalV149=function(){ ensureSmartInventoryUiV149(); const c=$('stockBatchCardV148'); if(c){ c.classList.add('v149-open'); const bd=$('v149Backdrop'); if(bd) bd.classList.add('show'); } };
  window.closeStockBatchModalV149=function(){ const c=$('stockBatchCardV148'); if(c) c.classList.remove('v149-open'); const bd=$('v149Backdrop'); if(bd && !$('invoiceViewModalV149')?.classList.contains('show')) bd.classList.remove('show'); };

  function filteredBatchesV149(){
    const rows=window.stockBatchesV148 || [];
    const q=str($('batchSearchV149')?.value).trim().toLowerCase();
    const d=str($('batchDateFilterV149')?.value);
    const m=str($('batchMonthFilterV149')?.value);
    return rows.filter(r=>{
      const date=str(r.batch_date||'');
      if(d && date!==d) return false;
      if(m && !date.startsWith(m)) return false;
      if(q){ const hay=[r.invoice_no,r.id,r.supplier,r.batch_date].join(' ').toLowerCase(); if(!hay.includes(q)) return false; }
      return true;
    });
  }
  window.clearBatchFiltersV149=function(){ ['batchSearchV149','batchDateFilterV149','batchMonthFilterV149'].forEach(id=>{ if($(id)) $(id).value=''; }); renderStockBatchCardsV149(); };
  window.renderStockBatchCardsV149=function(){
    ensureSmartInventoryUiV149();
    const grid=$('stockBatchCardsGridV149'); if(!grid) return;
    const rows=filteredBatchesV149();
    grid.innerHTML=rows.map((r)=>{
      const idx=(window.stockBatchesV148||[]).indexOf(r);
      const count=(r.lines||[]).length;
      return `<div class="v149-invoice-card">
        <div class="v149-invoice-top"><div><div class="v149-invoice-no">${esc2(r.invoice_no||r.id||'-')}</div><span class="v149-pill">${count} صنف</span></div><div class="v149-invoice-date">${esc2(r.batch_date||'-')}</div></div>
        <div class="muted">المورد: <b>${esc2(r.supplier||'-')}</b></div>
        <div class="v149-invoice-meta"><div class="v149-mini"><small>قبل الضريبة</small><b>${money2(r.total_before_vat)} ر.س</b></div><div class="v149-mini"><small>VAT 15%</small><b>${money2(r.total_vat)} ر.س</b></div><div class="v149-mini"><small>الإجمالي شامل</small><b>${money2(r.total_with_vat)} ر.س</b></div><div class="v149-mini"><small>المرجع</small><b>${esc2(r.invoice_no||r.id||'-')}</b></div></div>
        <div class="row-actions"><button type="button" onclick="viewStockBatchV149(${idx})">عرض الفاتورة</button><button type="button" class="light" onclick="stockBatchPrintSavedV148('${idx}')">طباعة</button></div>
      </div>`;
    }).join('') || '<div class="v149-empty">لا توجد فواتير حسب الفلتر الحالي</div>';
  };

  window.viewStockBatchV149=function(idx){
    const r=(window.stockBatchesV148||[])[Number(idx)]; if(!r) return;
    let modal=$('invoiceViewModalV149');
    if(!modal){ modal=document.createElement('div'); modal.id='invoiceViewModalV149'; modal.className='v149-modal-card'; document.body.appendChild(modal); }
    const rows=(r.lines||[]).map(l=>`<tr><td>${esc2(l.product_code)}</td><td>${esc2(l.item_name)}</td><td>${esc2(l.category||'')}</td><td>${esc2(l.item_type||'')}</td><td>${esc2(l.unit||'')}</td><td>${qty2(l.quantity)}</td><td>${money2(l.unit_cost)} ر.س</td><td>${money2(l.unit_vat)} ر.س</td><td>${money2(l.unit_gross)} ر.س</td><td>${money2(l.line_gross)} ر.س</td></tr>`).join('');
    const idxReal=(window.stockBatchesV148||[]).indexOf(r);
    modal.innerHTML=`<button type="button" class="v149-close" onclick="closeInvoiceViewV149()">إغلاق</button><h2>فاتورة إدخال مخزون</h2><div class="v149-invoice-meta"><div class="v149-mini"><small>رقم الفاتورة</small><b>${esc2(r.invoice_no||r.id||'-')}</b></div><div class="v149-mini"><small>التاريخ</small><b>${esc2(r.batch_date||'-')}</b></div><div class="v149-mini"><small>المورد</small><b>${esc2(r.supplier||'-')}</b></div><div class="v149-mini"><small>عدد الأصناف</small><b>${(r.lines||[]).length}</b></div></div><div class="table-wrap"><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>التصنيف</th><th>النوع</th><th>الوحدة</th><th>الكمية</th><th>سعر قبل الضريبة</th><th>VAT 15%</th><th>سعر شامل</th><th>الإجمالي شامل</th></tr></thead><tbody>${rows||'<tr><td colspan="10">لا توجد أصناف</td></tr>'}</tbody></table></div><div class="footer-note"><b>Total amount before 15% VAT:</b> ${money2(r.total_before_vat)} SAR<br><b>Total VAT 15%:</b> ${money2(r.total_vat)} SAR<br><b>Total Value with 15% VAT:</b> ${money2(r.total_with_vat)} SAR</div><div class="actions"><button type="button" onclick="stockBatchPrintSavedV148('${idxReal}')">طباعة</button></div>`;
    modal.classList.add('show'); const bd=$('v149Backdrop'); if(bd) bd.classList.add('show');
  };
  window.closeInvoiceViewV149=function(){ const m=$('invoiceViewModalV149'); if(m) m.classList.remove('show'); const bd=$('v149Backdrop'); if(bd && !$('stockBatchCardV148')?.classList.contains('v149-open')) bd.classList.remove('show'); };

  // Wrap existing renderer so old table can update the smart cards too.
  const oldRender = window.stockBatchRenderBatchesV148;
  window.stockBatchRenderBatchesV148=function(rows){ if(oldRender) oldRender(rows); renderStockBatchCardsV149(); };

  const oldLoad = window.stockBatchLoadV148;
  if(oldLoad){ window.stockBatchLoadV148 = async function(force){ const out=await oldLoad.apply(this, arguments); renderStockBatchCardsV149(); return out; }; }

  const oldEnsure=window.financeRenderAll;
  if(oldEnsure){ window.financeRenderAll=function(){ oldEnsure.apply(this, arguments); setTimeout(()=>{ ensureSmartInventoryUiV149(); renderStockBatchCardsV149(); },50); }; }
  window.addEventListener('load',()=>setTimeout(()=>{ ensureSmartInventoryUiV149(); renderStockBatchCardsV149(); },900));
})();

/* ===== V150: Move catalog into its own finance tab and keep inventory tab for stock invoices only ===== */
(function(){
  function $(id){ return document.getElementById(id); }
  function ensureCatalogTabV150(){
    const tabsBox = document.querySelector('#financeDashboard .finance-tabs') || document.querySelector('.finance-tabs');
    const invTab = $('financeTabInventory');
    if(!tabsBox || !invTab) return;

    // Add a dedicated tab button next to المخزون
    if(!document.querySelector('.finance-tab[data-v150-catalog="1"]')){
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'light finance-tab';
      btn.dataset.v150Catalog = '1';
      btn.textContent = 'الأصناف';
      btn.onclick = function(){ financeShowTab('catalog', this); };
      const inventoryBtn = [...tabsBox.querySelectorAll('.finance-tab')].find(b => (b.textContent||'').trim()==='المخزون');
      if(inventoryBtn && inventoryBtn.nextSibling) tabsBox.insertBefore(btn, inventoryBtn.nextSibling);
      else tabsBox.appendChild(btn);
    }

    // Create catalog page if not exists
    let catPage = $('financeTabCatalog');
    if(!catPage){
      catPage = document.createElement('div');
      catPage.id = 'financeTabCatalog';
      catPage.className = 'finance-tab-page hidden';
      invTab.parentNode.insertBefore(catPage, invTab.nextSibling);
    }

    // Move the catalog card from the inventory page into the dedicated catalog page
    const itemCard = document.querySelector('#financeTabInventory .card:has(#inventoryItemsBody)') || document.querySelector('.card:has(#inventoryItemsBody)');
    if(itemCard && itemCard.parentElement !== catPage){
      catPage.appendChild(itemCard);
      itemCard.classList.add('v150-catalog-full');
      const h = itemCard.querySelector('h2');
      if(h) h.textContent = 'قائمة الأصناف';
    }

    // Make the inventory tab focused on stock entry invoices only
    const hub = $('smartStockInvoicesV149');
    if(hub){
      hub.classList.add('v150-invoices-only');
      const title = hub.querySelector('h2');
      if(title) title.textContent = 'فواتير إدخال المخزون';
    }
  }

  function injectCssV150(){
    if($('v150CatalogTabCss')) return;
    const st = document.createElement('style');
    st.id = 'v150CatalogTabCss';
    st.textContent = `
      #financeTabInventory .report-premium-wrap{display:block!important;}
      #financeTabInventory #smartStockInvoicesV149{width:100%;margin:0;}
      #financeTabCatalog .v150-catalog-full{width:100%;margin:0;}
      #financeTabCatalog .inventory-filter-bar{margin-bottom:18px;}
      #financeTabCatalog .inventory-cards-grid,
      #financeTabCatalog .smart-card-grid{width:100%;}
      @media(max-width:900px){#financeTabCatalog .inventory-filter-bar{grid-template-columns:1fr!important;}}
    `;
    document.head.appendChild(st);
  }

  const oldFinanceRenderAll = window.financeRenderAll;
  if(oldFinanceRenderAll){
    window.financeRenderAll = function(){
      const out = oldFinanceRenderAll.apply(this, arguments);
      setTimeout(()=>{ injectCssV150(); ensureCatalogTabV150(); }, 80);
      return out;
    };
  }

  const oldFinanceShowTab = window.financeShowTab;
  if(oldFinanceShowTab){
    window.financeShowTab = function(tab, btn){
      injectCssV150(); ensureCatalogTabV150();
      const out = oldFinanceShowTab.apply(this, arguments);
      setTimeout(()=>{ ensureCatalogTabV150(); if(tab==='catalog' && typeof inventoryRenderItems==='function') inventoryRenderItems(); }, 60);
      return out;
    };
  }

  window.addEventListener('load',()=>setTimeout(()=>{ injectCssV150(); ensureCatalogTabV150(); },1100));
})();

/* ===== V151: Catalog code lookup, company code, item stats, warehouse simplified view ===== */
(function(){
  const $ = id => document.getElementById(id);
  const safe = v => (v ?? '').toString();
  const e = v => safe(v).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const n = v => Number(String(v ?? 0).replace(/,/g,'')) || 0;
  const q = v => Number.isFinite(n(v)) ? (Math.round(n(v)*100)/100).toLocaleString('en-US') : '0';
  const money = v => (Math.round(n(v)*100)/100).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  function role(){ try{return (typeof session==='function' ? (session()||{}).role : (JSON.parse(localStorage.getItem('tasneef_session')||'{}').role))||'';}catch(_){return '';} }
  function isWarehouse(){ return role()==='warehouse_manager'; }
  function itemCode(it){ return safe(it?.product_code || it?.barcode || it?.supplier_barcode || it?.serial_number || '').trim(); }
  function companyCode(it){ return safe(it?.company_code || it?.internal_code || it?.serial_number || itemCode(it)).trim(); }
  function findItemByAnyCodeV151(code){
    code = safe(code).trim(); if(!code) return null;
    return (window.data?.inventoryItems||[]).find(it => [it.id,it.product_code,it.barcode,it.supplier_barcode,it.serial_number,it.company_code,it.internal_code]
      .map(x=>safe(x).trim()).includes(code));
  }
  function itemStatsV151(it){
    const id=safe(it?.id); const code=itemCode(it);
    let inQty=0, outQty=0, retQty=0, consumeQty=0;
    (window.data?.inventoryMovements||[]).forEach(m=>{
      const match = safe(m.item_id)===id || safe(m.product_code)===code || safe(m.barcode)===code || safe(m.item_name)===safe(it?.name);
      if(!match) return;
      const qty=n(m.quantity); const type=safe(m.movement_type);
      if(type==='in') inQty+=qty;
      else if(type==='out') outQty+=qty;
      else if(type==='return') retQty+=qty;
      else if(type==='consume') consumeQty+=qty;
    });
    (window.data?.inventoryRequests||[]).filter(r=>safe(r.status)==='approved').forEach(r=>{
      const lines = Array.isArray(r.request_items)?r.request_items:(Array.isArray(r.items)?r.items:[]);
      if(lines.length){
        lines.forEach(l=>{ if(safe(l.item_id)===id || safe(l.product_code)===code || safe(l.item_name)===safe(it?.name)) outQty+=n(l.quantity); });
      } else if(safe(r.item_id)===id || safe(r.item_name)===safe(it?.name)) outQty+=n(r.quantity);
    });
    // If explicit consume movements do not exist, consumption = issued - returned.
    const consumed = consumeQty || Math.max(0,outQty-retQty);
    return {inQty,outQty,retQty,consumed,remaining:n(it?.quantity)};
  }
  window.inventoryItemStatsV151 = itemStatsV151;

  function patchLabelsAndFieldsV151(){
    // Admin item form labels.
    const barcode = $('inventoryItemBarcode');
    if(barcode){
      const label = barcode.closest('div')?.querySelector('label') || [...document.querySelectorAll('label')].find(l=>l.getAttribute('for')==='inventoryItemBarcode');
      if(label) label.textContent='كود المنتج';
      barcode.placeholder='كود المنتج من المورد أو الموجود على المنتج';
    }
    const qty = $('inventoryItemQty');
    if(qty){ const label = qty.closest('div')?.querySelector('label'); if(label) label.textContent='الكمية'; }
    if($('inventoryItemSerial') && !$('inventoryCompanyCodeV151')){
      const host = $('inventoryItemSerial').closest('div');
      if(host){
        const box=document.createElement('div');
        box.innerHTML='<label>كود الشركة</label><input id="inventoryCompanyCodeV151" placeholder="كود داخلي خاص بالشركة">';
        host.insertAdjacentElement('afterend', box);
      }
    }
    // Batch invoice line: add company code and smart lookup.
    const batchCode = $('batchProductCodeV148');
    if(batchCode){
      const label = batchCode.closest('div')?.querySelector('label'); if(label) label.textContent='كود المنتج';
      batchCode.placeholder='اكتب كود المنتج ليتعرف على الصنف تلقائيًا';
      if(!$('batchCompanyCodeV151')){
        const host=batchCode.closest('div');
        if(host){
          const box=document.createElement('div');
          box.innerHTML='<label>كود الشركة</label><input id="batchCompanyCodeV151" placeholder="كود داخلي اختياري">';
          host.insertAdjacentElement('afterend', box);
        }
      }
      if(!batchCode.dataset.v151Lookup){
        batchCode.dataset.v151Lookup='1';
        batchCode.addEventListener('input',()=>setTimeout(()=>lookupBatchProductV151(),60));
        batchCode.addEventListener('change',lookupBatchProductV151);
      }
    }
  }

  function lookupBatchProductV151(){
    const code = $('batchProductCodeV148')?.value;
    const it = findItemByAnyCodeV151(code);
    const hintId='batchLookupHintV151';
    let hint=$(hintId);
    if(!hint && $('batchProductCodeV148')){ hint=document.createElement('div'); hint.id=hintId; hint.className='v151-lookup-hint'; $('batchProductCodeV148').closest('div')?.appendChild(hint); }
    if(!it){ if(hint) hint.textContent=''; return; }
    if($('batchProductNameV148')) $('batchProductNameV148').value=it.name||'';
    if($('batchCategoryV148')) $('batchCategoryV148').value=it.category||'';
    if($('batchItemTypeV148')) $('batchItemTypeV148').value=it.item_type||it.type||'مادة';
    if($('batchUnitV148')) $('batchUnitV148').value=it.unit||'حبة';
    if($('batchUnitPriceV148')) $('batchUnitPriceV148').value=n(it.unit_cost)||'';
    if($('batchCompanyCodeV151')) $('batchCompanyCodeV151').value=companyCode(it)||'';
    if(hint) hint.innerHTML=`تم التعرف على الصنف: <b>${e(it.name)}</b> — المتبقي ${q(it.quantity)} ${e(it.unit||'')}`;
  }
  window.lookupBatchProductV151 = lookupBatchProductV151;

  // Preserve company code on direct catalog save.
  const oldSaveItem = window.inventorySaveItem;
  if(oldSaveItem && !oldSaveItem.v151Wrapped){
    const wrapped = async function(btn){
      const company = safe($('inventoryCompanyCodeV151')?.value).trim();
      // Let the existing save run first, then patch the saved/duplicated item by product code.
      await oldSaveItem.apply(this, arguments);
      if(company && window.sb){
        const code = safe($('inventoryItemSerial')?.value || $('inventoryItemBarcode')?.value).trim();
        const found = findItemByAnyCodeV151(code) || (window.data?.inventoryItems||[]).find(x=>safe(x.name)===safe($('inventoryItemName')?.value));
        if(found){ try{ await sb.from('inventory_items').update({company_code:company, internal_code:company}).eq('id', found.id); }catch(_){} }
      }
    };
    wrapped.v151Wrapped=true; window.inventorySaveItem=wrapped;
  }

  // Extend batch add/save lines with company_code.
  const oldAddLine = window.stockBatchAddLineV148;
  if(oldAddLine && !oldAddLine.v151Wrapped){
    const wrapped=function(){
      const before = (typeof batchLinesV148!=='undefined' && Array.isArray(batchLinesV148)) ? batchLinesV148.length : null;
      const code = safe($('batchProductCodeV148')?.value).trim();
      const company = safe($('batchCompanyCodeV151')?.value).trim();
      oldAddLine.apply(this, arguments);
      try{
        if(typeof batchLinesV148!=='undefined' && Array.isArray(batchLinesV148)){
          const line = [...batchLinesV148].reverse().find(l=>safe(l.product_code)===code) || batchLinesV148[batchLinesV148.length-1];
          if(line) line.company_code = company || line.company_code || code;
        }
      }catch(_){}
    };
    wrapped.v151Wrapped=true; window.stockBatchAddLineV148=wrapped;
  }

  function renderWarehouseItemsV151(rows){
    return `<tr><td colspan="20"><div class="v151-catalog-grid">${rows.map(it=>{
      const st=itemStatsV151(it);
      const low=n(it.quantity)<=n(it.min_quantity||it.reorder_level);
      const img=it.image_url?`<img src="${e(it.image_url)}" onerror="this.style.display='none'">`:'<span>لا صورة</span>';
      return `<article class="v151-item-card">
        <div class="v151-item-top"><div class="v151-img">${img}</div><div><h3>${e(it.name||'-')}</h3><small>${e(itemCode(it)||'-')} · كود الشركة: ${e(companyCode(it)||'-')}</small></div><b class="${low?'danger-badge':'ok-badge'}">${low?'منخفض':'متوفر'}</b></div>
        <div class="v151-stats"><div><small>الكمية</small><b>${q(st.remaining)} ${e(it.unit||'')}</b></div><div><small>دخل</small><b>${q(st.inQty)}</b></div><div><small>خرج</small><b>${q(st.outQty)}</b></div><div><small>مرتجع</small><b>${q(st.retQty)}</b></div><div><small>مستهلك</small><b>${q(st.consumed)}</b></div><div><small>حد الطلب</small><b>${q(it.min_quantity||0)}</b></div></div>
        <div class="row-actions"><button onclick="inventoryOpenItemSmart&&inventoryOpenItemSmart('${e(it.id)}')">عرض</button></div>
      </article>`;
    }).join('') || '<div class="muted">لا توجد أصناف</div>'}</div></td></tr>`;
  }

  const oldRenderItems = window.inventoryRenderItems;
  if(oldRenderItems && !oldRenderItems.v151Wrapped){
    const wrapped=function(){
      patchLabelsAndFieldsV151();
      if(isWarehouse()){
        const b=$('inventoryItemsBody');
        if(!b) return oldRenderItems.apply(this, arguments);
        const search=safe($('inventorySearch')?.value || $('financeSearch')?.value).trim().toLowerCase();
        const supplier=safe($('inventorySupplierFilter')?.value || $('inventoryReportSupplier')?.value);
        const cat=safe($('inventoryCategoryFilter')?.value);
        let rows=[...(window.data?.inventoryItems||[])];
        if(search) rows=rows.filter(it=>[it.name,itemCode(it),companyCode(it),it.supplier,it.category,it.unit].join(' ').toLowerCase().includes(search));
        if(supplier) rows=rows.filter(it=>safe(it.supplier)===supplier);
        if(cat) rows=rows.filter(it=>safe(it.category)===cat);
        b.innerHTML=renderWarehouseItemsV151(rows);
        if(typeof inventoryFillItemSelect==='function') inventoryFillItemSelect();
        if(typeof inventoryFillRequestSelect==='function') inventoryFillRequestSelect();
        return;
      }
      const out=oldRenderItems.apply(this, arguments);
      // Add remaining/consumed fields to normal catalog cards if possible.
      try{
        document.querySelectorAll('#inventoryItemsBody .smart-card-v129, #inventoryItemsBody .v151-item-card').forEach(card=>{});
      }catch(_){}
      return out;
    };
    wrapped.v151Wrapped=true; window.inventoryRenderItems=wrapped;
  }

  function applyWarehouseViewV151(){
    patchLabelsAndFieldsV151();
    if(!isWarehouse()) return;
    const tabs=[...document.querySelectorAll('#financeDashboard .finance-tab')];
    tabs.forEach(btn=>{
      const t=(btn.textContent||'').trim();
      const allow = t==='طلبات الصرف' || t==='الأصناف';
      btn.style.display=allow?'':'none';
    });
    const pages=['financeTabOverview','financeTabExpenses','financeTabInventory','financeTabMovements','financeTabReports','financeTabCostCenters'];
    pages.forEach(id=>{ const el=$(id); if(el) el.classList.add('hidden'); });
    // Show catalog by default if no visible page.
    const catalog=$('financeTabCatalog'); if(catalog) catalog.classList.remove('hidden');
    // Hide all price labels inside catalog for warehouse manager.
    document.body.classList.add('warehouse-manager-view-v151');
  }

  function injectCssV151(){
    if($('v151Css')) return;
    const st=document.createElement('style'); st.id='v151Css'; st.textContent=`
      .v151-lookup-hint{font-size:12px;color:#006b4a;margin-top:6px;background:#eefaf5;border:1px solid #cfe9df;border-radius:10px;padding:6px 8px}
      .v151-catalog-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:14px;padding:8px;direction:rtl}
      .v151-item-card{border:1px solid #cfe3dd;border-radius:18px;padding:14px;background:#fff;box-shadow:0 10px 22px rgba(0,45,31,.06)}
      .v151-item-top{display:flex;align-items:center;gap:12px;justify-content:space-between}.v151-item-top h3{margin:0;color:#003f31}.v151-img{width:64px;height:64px;border:1px solid #dbeee8;border-radius:14px;display:grid;place-items:center;background:#f7fbf9;overflow:hidden;flex:0 0 64px}.v151-img img{max-width:100%;max-height:100%;object-fit:contain}.v151-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.v151-stats div{background:#f1faf6;border:1px solid #d9eee6;border-radius:12px;padding:8px;text-align:center}.v151-stats small{display:block;color:#5b756d}.v151-stats b{color:#004c3a}.ok-badge,.danger-badge{border-radius:999px;padding:5px 8px;font-size:12px}.ok-badge{background:#e5f8ee;color:#067647}.danger-badge{background:#ffe6e6;color:#b42318}
      .warehouse-manager-view-v151 #financeTabCatalog .smart-card-v129 .m:nth-child(3),
      .warehouse-manager-view-v151 #financeTabCatalog .smart-card-v129 .m:has(small){ }
      .warehouse-manager-view-v151 #financeTabCatalog .smart-card-v129 .m small:where(:not(.keep)){ }
      @media(max-width:700px){.v151-stats{grid-template-columns:repeat(2,1fr)}}
    `; document.head.appendChild(st);
  }

  const oldShowTab = window.financeShowTab;
  if(oldShowTab && !oldShowTab.v151Wrapped){
    const wrapped=function(tab,btn){ const out=oldShowTab.apply(this, arguments); setTimeout(()=>{injectCssV151(); patchLabelsAndFieldsV151(); applyWarehouseViewV151(); if(tab==='catalog'&&window.inventoryRenderItems) inventoryRenderItems();},80); return out; };
    wrapped.v151Wrapped=true; window.financeShowTab=wrapped;
  }
  const oldRenderAll = window.financeRenderAll;
  if(oldRenderAll && !oldRenderAll.v151Wrapped){
    const wrapped=function(){ const out=oldRenderAll.apply(this, arguments); setTimeout(()=>{injectCssV151(); patchLabelsAndFieldsV151(); applyWarehouseViewV151();},120); return out; };
    wrapped.v151Wrapped=true; window.financeRenderAll=wrapped;
  }
  window.addEventListener('load',()=>setTimeout(()=>{injectCssV151(); patchLabelsAndFieldsV151(); applyWarehouseViewV151(); if(window.inventoryRenderItems) inventoryRenderItems();},1200));
})();

/* ===== V152: Cost Reduction Center + Smart inventory invoice edit/delete ===== */
(function(){
  'use strict';
  const $ = (id)=>document.getElementById(id);
  const esc = (v)=>String(v ?? '').replace(/[&<>"']/g, m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
  const num = (v)=>{ const x=parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x)?x:0; };
  const money = (v)=>num(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  const qty = (v)=>num(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const todayMonth = ()=>new Date().toISOString().slice(0,7);
  const itemCode = (it)=>String(it?.product_code || it?.serial_number || it?.barcode || it?.supplier_barcode || it?.id || '');
  const itemById = (id)=>(window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id));
  const itemByCode = (code)=>(window.data?.inventoryItems||[]).find(i=>[i.product_code,i.serial_number,i.barcode,i.supplier_barcode].map(x=>String(x||'').trim()).includes(String(code||'').trim()));
  const currentMonthRows = (rows, field)=>{ const mon=$('costReductionMonthV152')?.value || ''; return mon ? rows.filter(r=>String(r[field]||'').slice(0,7)===mon) : rows; };
  const supplierFilter = ()=>String($('costReductionSupplierV152')?.value||'');
  const projectFilter = ()=>String($('costReductionProjectV152')?.value||'');
  const supervisorFilter = ()=>String($('costReductionSupervisorV152')?.value||'');
  const searchFilter = ()=>String($('costReductionSearchV152')?.value||'').trim().toLowerCase();

  function calcLineValue(item, qtyValue){ return num(item?.unit_cost) * num(qtyValue); }
  function requestLines(r){
    let arr = r?.request_items || r?.items || r?.request_lines || [];
    if(typeof arr === 'string'){ try{ arr = JSON.parse(arr); }catch(_){ arr=[]; } }
    if(!Array.isArray(arr)) arr=[];
    if(!arr.length && r?.item_id) arr=[{item_id:r.item_id,item_name:r.item_name,quantity:r.quantity,product_code:r.product_code,unit_cost:r.unit_cost}];
    return arr;
  }
  function lineReturnQty(r, line){
    const ret = r?.return_items || r?.returned_items || r?.returns || [];
    let arr = ret;
    if(typeof arr === 'string'){ try{ arr=JSON.parse(arr); }catch(_){ arr=[]; } }
    if(!Array.isArray(arr)) arr=[];
    const code = String(line.product_code||line.barcode||'');
    const id = String(line.item_id||'');
    const hit = arr.find(x=>String(x.item_id||'')===id || String(x.product_code||x.code||'')===code);
    return num(hit?.quantity || hit?.return_qty || hit?.qty || line.return_qty || line.returned_qty || 0);
  }
  function consumptionRows(){
    const rows=[];
    const reqs=currentMonthRows((window.data?.inventoryRequests||[]).filter(r=>String(r.status)==='approved'),'request_date');
    reqs.forEach(r=>{
      const pName=r.project_name || (typeof projectName==='function'?projectName(r.project_id):'') || '';
      const supName=r.supervisor_name || (typeof supervisorName==='function'?supervisorName(r.supervisor_id):'') || '';
      requestLines(r).forEach(l=>{
        const it=itemById(l.item_id)||itemByCode(l.product_code)||{};
        const out=num(l.quantity);
        const ret=lineReturnQty(r,l);
        const cons=Math.max(0,out-ret);
        rows.push({date:r.request_date,project_id:r.project_id,project_name:pName,supervisor_id:r.supervisor_id,supervisor_name:supName,item_id:l.item_id||it.id,product_code:l.product_code||itemCode(it),item_name:l.item_name||it.name||'',supplier:it.supplier||l.supplier||'',category:it.category||l.category||'',out,ret,cons,value:calcLineValue(it, cons),unit_cost:num(it.unit_cost||l.unit_cost)});
      });
    });
    // Add direct inventory consumption/out movements not tied to approved request.
    currentMonthRows((window.data?.inventoryMovements||[]),'movement_date').forEach(m=>{
      if(m.request_id) return;
      if(!['out','consume'].includes(String(m.movement_type))) return;
      const it=itemById(m.item_id)||{};
      const cons=num(m.quantity);
      rows.push({date:m.movement_date,project_id:m.project_id,project_name:m.project_name||(typeof projectName==='function'?projectName(m.project_id):''),supervisor_id:'',supervisor_name:m.receiver||'',item_id:m.item_id,product_code:m.product_code||itemCode(it),item_name:m.item_name||it.name||'',supplier:it.supplier||'',category:it.category||'',out:cons,ret:0,cons,value:calcLineValue(it,cons),unit_cost:num(m.unit_cost||it.unit_cost)});
    });
    return applyCostFilters(rows);
  }
  function applyCostFilters(rows){
    const sf=supplierFilter(), pf=projectFilter(), sup=supervisorFilter(), q=searchFilter();
    let out=[...rows];
    if(sf) out=out.filter(r=>String(r.supplier)===sf);
    if(pf) out=out.filter(r=>String(r.project_id)===pf || String(r.project_name)===pf);
    if(sup) out=out.filter(r=>String(r.supervisor_id)===sup || String(r.supervisor_name)===sup);
    if(q) out=out.filter(r=>[r.product_code,r.item_name,r.supplier,r.project_name,r.supervisor_name,r.category].join(' ').toLowerCase().includes(q));
    return out;
  }
  function groupSum(rows, keyFn){
    const map={};
    rows.forEach(r=>{ const k=keyFn(r)||'-'; if(!map[k]) map[k]={key:k,qty:0,value:0,count:0,items:{},projects:{},supervisors:{}}; map[k].qty+=num(r.cons); map[k].value+=num(r.value); map[k].count++; map[k].items[r.item_name]=(map[k].items[r.item_name]||0)+num(r.cons); map[k].projects[r.project_name]=(map[k].projects[r.project_name]||0)+num(r.cons); map[k].supervisors[r.supervisor_name]=(map[k].supervisors[r.supervisor_name]||0)+num(r.cons); });
    return Object.values(map).sort((a,b)=>b.value-a.value);
  }
  function topName(obj){ const a=Object.entries(obj||{}).sort((x,y)=>y[1]-x[1])[0]; return a ? `${a[0]} (${qty(a[1])})` : '-'; }

  function injectCostReductionUi(){
    if(!$('financeDashboard')) return;
    const tabs = document.querySelector('#financeDashboard .finance-tabs');
    if(tabs && !$('financeTabBtnCostReductionV152')){
      const btn=document.createElement('button'); btn.id='financeTabBtnCostReductionV152'; btn.className='light finance-tab'; btn.textContent='تقليل التكلفة'; btn.onclick=function(){ financeShowTab('costReduction',this); setTimeout(renderCostReductionV152,80); };
      tabs.appendChild(btn);
    }
    if(!$('financeTabCostReduction')){
      const sec=document.createElement('div'); sec.id='financeTabCostReduction'; sec.className='finance-tab-page hidden';
      sec.innerHTML=`
        <div class="v152-cr-head">
          <div><h2>Cost Reduction Center</h2><p>تحليل ذكي لاستهلاك المخزون والمصروفات لتحديد الهدر وفرص التوفير.</p></div>
          <button class="light" onclick="renderCostReductionV152()">تحديث التحليل</button>
        </div>
        <div class="v152-filter-panel">
          <input id="costReductionSearchV152" placeholder="بحث: صنف / مشروع / مشرف / مورد" oninput="renderCostReductionV152()">
          <select id="costReductionMonthV152" onchange="renderCostReductionV152()"></select>
          <select id="costReductionProjectV152" onchange="renderCostReductionV152()"><option value="">كل المشاريع</option></select>
          <select id="costReductionSupervisorV152" onchange="renderCostReductionV152()"><option value="">كل المشرفين</option></select>
          <select id="costReductionSupplierV152" onchange="renderCostReductionV152()"><option value="">كل الموردين</option></select>
        </div>
        <div class="v152-kpis" id="costReductionKpisV152"></div>
        <div class="v152-cr-grid">
          <section class="v152-panel"><h3>أعلى الأصناف استهلاكًا</h3><div id="costTopItemsV152"></div></section>
          <section class="v152-panel"><h3>أعلى المشاريع تكلفة</h3><div id="costTopProjectsV152"></div></section>
          <section class="v152-panel"><h3>أعلى المشرفين صرفًا</h3><div id="costTopSupervisorsV152"></div></section>
          <section class="v152-panel"><h3>مقارنة الموردين والأسعار</h3><div id="costSupplierCompareV152"></div></section>
          <section class="v152-panel v152-wide"><h3>توصيات تقليل التكلفة</h3><div id="costRecommendationsV152"></div></section>
        </div>`;
      const after=$('financeTabCostCenters') || $('financeTabReports') || $('financeTabInventory');
      (after?.parentNode||$('financeDashboard')).appendChild(sec);
    }
    fillCostFiltersV152(); injectCssV152();
  }
  function fillCostFiltersV152(){
    const mon=$('costReductionMonthV152'); if(mon && !mon.dataset.ready){ const now=todayMonth(); mon.innerHTML='<option value="">كل الفترات</option>'+Array.from({length:14},(_,i)=>{ const d=new Date(); d.setMonth(d.getMonth()-i); const v=d.toISOString().slice(0,7); return `<option value="${v}" ${v===now?'selected':''}>${v}</option>`; }).join(''); mon.dataset.ready='1'; }
    const proj=$('costReductionProjectV152'); if(proj){ const cur=proj.value; proj.innerHTML='<option value="">كل المشاريع</option>'+(window.data?.projects||[]).map(p=>`<option value="${esc(p.id)}">${esc(p.name)}</option>`).join(''); proj.value=cur; }
    const sup=$('costReductionSupervisorV152'); if(sup){ const cur=sup.value; const rows=(window.data?.supervisors||[]).length?window.data.supervisors:(window.data?.users||[]); sup.innerHTML='<option value="">كل المشرفين</option>'+rows.map(s=>`<option value="${esc(s.id)}">${esc(s.full_name||s.username||s.name)}</option>`).join(''); sup.value=cur; }
    const suppliers=[...new Set((window.data?.inventoryItems||[]).map(i=>i.supplier).filter(Boolean))].sort(); const sel=$('costReductionSupplierV152'); if(sel){ const cur=sel.value; sel.innerHTML='<option value="">كل الموردين</option>'+suppliers.map(s=>`<option value="${esc(s)}">${esc(s)}</option>`).join(''); sel.value=cur; }
  }
  function tableHtml(headers, rows){ return `<div class="table-wrap"><table><thead><tr>${headers.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${rows || `<tr><td colspan="${headers.length}">لا توجد بيانات</td></tr>`}</tbody></table></div>`; }
  window.renderCostReductionV152=function(){
    injectCostReductionUi(); fillCostFiltersV152();
    const rows=consumptionRows();
    const totalValue=rows.reduce((a,r)=>a+num(r.value),0), totalQty=rows.reduce((a,r)=>a+num(r.cons),0);
    const itemGroups=groupSum(rows,r=>r.item_name).slice(0,8);
    const projectGroups=groupSum(rows,r=>r.project_name).slice(0,8);
    const supervisorGroups=groupSum(rows,r=>r.supervisor_name).slice(0,8);
    const kpis=$('costReductionKpisV152'); if(kpis) kpis.innerHTML=`
      <div><small>قيمة الاستهلاك</small><b>${money(totalValue)} ر.س</b></div>
      <div><small>إجمالي الكمية</small><b>${qty(totalQty)}</b></div>
      <div><small>عدد العمليات</small><b>${rows.length}</b></div>
      <div><small>الأصناف المتأثرة</small><b>${new Set(rows.map(r=>r.item_name)).size}</b></div>`;
    const ti=$('costTopItemsV152'); if(ti) ti.innerHTML=tableHtml(['الصنف','الكمية','القيمة','أكثر مشروع','أكثر مشرف'], itemGroups.map(g=>`<tr><td><b>${esc(g.key)}</b></td><td>${qty(g.qty)}</td><td>${money(g.value)} ر.س</td><td>${esc(topName(g.projects))}</td><td>${esc(topName(g.supervisors))}</td></tr>`).join(''));
    const tp=$('costTopProjectsV152'); if(tp) tp.innerHTML=tableHtml(['المشروع','الكمية','القيمة','أكثر صنف'], projectGroups.map(g=>`<tr><td><b>${esc(g.key)}</b></td><td>${qty(g.qty)}</td><td>${money(g.value)} ر.س</td><td>${esc(topName(g.items))}</td></tr>`).join(''));
    const ts=$('costTopSupervisorsV152'); if(ts) ts.innerHTML=tableHtml(['المشرف','الطلبات/الحركات','الكمية','القيمة','أكثر صنف'], supervisorGroups.map(g=>`<tr><td><b>${esc(g.key)}</b></td><td>${g.count}</td><td>${qty(g.qty)}</td><td>${money(g.value)} ر.س</td><td>${esc(topName(g.items))}</td></tr>`).join(''));
    renderSupplierCompareV152(rows); renderRecommendationsV152(rows,itemGroups,projectGroups,supervisorGroups);
  };
  function renderSupplierCompareV152(rows){
    const items=window.data?.inventoryItems||[]; const byCode={};
    items.forEach(it=>{ const code=itemCode(it)||it.name; if(!byCode[code]) byCode[code]=[]; byCode[code].push(it); });
    const lines=[];
    Object.values(byCode).forEach(list=>{
      const uniq=[...new Map(list.filter(x=>x.supplier).map(x=>[x.supplier+'|'+num(x.unit_cost),x])).values()];
      if(uniq.length<2) return;
      uniq.sort((a,b)=>num(a.unit_cost)-num(b.unit_cost)); const low=uniq[0], high=uniq[uniq.length-1]; const diff=num(high.unit_cost)-num(low.unit_cost);
      if(diff>0) lines.push(`<tr><td>${esc(low.name||high.name)}</td><td>${esc(low.supplier)}</td><td>${money(low.unit_cost)} ر.س</td><td>${esc(high.supplier)}</td><td>${money(high.unit_cost)} ر.س</td><td><b>${money(diff)} ر.س</b></td></tr>`);
    });
    const el=$('costSupplierCompareV152'); if(el) el.innerHTML=tableHtml(['الصنف','أقل مورد','أقل سعر','أعلى مورد','أعلى سعر','فرق الحبة'], lines.slice(0,10).join(''));
  }
  function renderRecommendationsV152(rows,itemGroups,projectGroups,supervisorGroups){
    const rec=[];
    if(itemGroups[0]) rec.push(`راجع استهلاك <b>${esc(itemGroups[0].key)}</b> لأنه أعلى صنف من حيث القيمة خلال الفترة المحددة.`);
    if(projectGroups[0]) rec.push(`مشروع <b>${esc(projectGroups[0].key)}</b> هو الأعلى تكلفة؛ يفضل مراجعة سبب الصرف وحدود الاستهلاك الشهرية.`);
    if(supervisorGroups[0]) rec.push(`المشرف/المستلم <b>${esc(supervisorGroups[0].key)}</b> لديه أعلى قيمة صرف؛ المؤشر للمراجعة وليس اتهامًا.`);
    const cnRows=rows.filter(r=>String(r.project_name||'').match(/كلين|ادار|إدار|عام|شركة|سكن|سيارات/i));
    if(cnRows.length) rec.push(`يوجد استهلاك على مراكز عامة/إدارية؛ الأفضل تحميلها على مشروع محدد عند الإمكان لتقليل مصروفات CN.`);
    if(!rec.length) rec.push('لا توجد مؤشرات هدر واضحة حسب الفلاتر الحالية.');
    const el=$('costRecommendationsV152'); if(el) el.innerHTML=`<div class="v152-recs">${rec.map((r,i)=>`<div><b>توصية ${i+1}</b><p>${r}</p></div>`).join('')}</div>`;
  }

  async function reverseBatchLines(batch){
    for(const l of (batch.lines||[])){
      const it=itemById(l.item_id)||itemByCode(l.product_code);
      if(it && window.sb){ await sb.from('inventory_items').update({quantity:Math.max(0,num(it.quantity)-num(l.quantity))}).eq('id', it.id); }
      if(l.movement_id && window.sb){ try{ await sb.from('inventory_movements').delete().eq('id', l.movement_id); }catch(_){} }
    }
  }
  window.stockBatchDeleteV152 = async function(idx){
    const batch=(window.stockBatchesV148||[])[Number(idx)]; if(!batch) return msg('لم يتم العثور على الفاتورة','err');
    if(!confirm('تأكيد حذف فاتورة المخزون؟ سيتم عكس كمياتها من الأصناف.')) return;
    try{
      await reverseBatchLines(batch);
      if(batch.id && window.sb){ await sb.from('inventory_batch_items').delete().eq('batch_id', batch.id); await sb.from('inventory_batches').delete().eq('id', batch.id); }
      msg('تم حذف فاتورة المخزون وعكس كمياتها'); await financeLoadAll(); await stockBatchLoadV148(true); renderCostReductionV152();
    }catch(e){ msg(e.message||String(e),'err'); }
  };

  window.stockBatchEditV152 = function(idx){
    const batch=(window.stockBatchesV148||[])[Number(idx)]; if(!batch) return msg('لم يتم العثور على الفاتورة','err');
    const rows=(batch.lines||[]).map((l,i)=>`<tr><td>${esc(l.product_code||'')}</td><td>${esc(l.item_name||'')}</td><td><input type="number" step="0.01" id="editQty_${i}" value="${num(l.quantity)}"></td><td><input type="number" step="0.01" id="editCost_${i}" value="${num(l.unit_price_before_vat||l.unit_cost)}"></td></tr>`).join('');
    const html=`<div class="v152-edit-modal" id="batchEditModalV152"><div class="v152-edit-card"><button class="v152-close" onclick="document.getElementById('batchEditModalV152').remove()">إغلاق</button><h2>تعديل فاتورة مخزون</h2><div class="split"><div><label>رقم الفاتورة</label><input id="editInvoiceV152" value="${esc(batch.invoice_no||'')}"></div><div><label>التاريخ</label><input type="date" id="editDateV152" value="${esc(batch.batch_date||'')}"></div></div><label>المورد</label><input id="editSupplierV152" value="${esc(batch.supplier||'')}"><div class="table-wrap"><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>الكمية</th><th>سعر قبل الضريبة</th></tr></thead><tbody>${rows}</tbody></table></div><div class="actions"><button onclick="stockBatchSaveEditV152(${Number(idx)})">حفظ التعديل</button><button class="light" onclick="document.getElementById('batchEditModalV152').remove()">إلغاء</button></div></div></div>`;
    document.body.insertAdjacentHTML('beforeend', html);
  };
  window.stockBatchSaveEditV152 = async function(idx){
    const batch=(window.stockBatchesV148||[])[Number(idx)]; if(!batch) return;
    try{
      let net=0, vat=0, gross=0; const vatRate=0.15; const updated=[];
      for(let i=0;i<(batch.lines||[]).length;i++){
        const l=batch.lines[i]; const newQty=num($('editQty_'+i)?.value); const newCost=num($('editCost_'+i)?.value); const oldQty=num(l.quantity); const diff=newQty-oldQty; const unitVat=newCost*vatRate; const unitGross=newCost+unitVat; const lineNet=newQty*newCost; const lineVat=newQty*unitVat; const lineGross=newQty*unitGross;
        net+=lineNet; vat+=lineVat; gross+=lineGross;
        const it=itemById(l.item_id)||itemByCode(l.product_code); if(it && diff && window.sb){ await sb.from('inventory_items').update({quantity:Math.max(0,num(it.quantity)+diff), unit_cost:newCost}).eq('id', it.id); }
        if(l.id && window.sb){ await sb.from('inventory_batch_items').update({quantity:newQty,unit_price_before_vat:newCost,unit_vat:unitVat,unit_price_with_vat:unitGross,total_before_vat:lineNet,total_vat:lineVat,total_with_vat:lineGross}).eq('id', l.id); }
        if(l.movement_id && window.sb){ await sb.from('inventory_movements').update({quantity:newQty,unit_cost:newCost}).eq('id', l.movement_id); }
        updated.push({...l,quantity:newQty,unit_price_before_vat:newCost,unit_cost:newCost,unit_vat:unitVat,unit_gross:unitGross,total_before_vat:lineNet,total_vat:lineVat,total_with_vat:lineGross});
      }
      const patch={invoice_no:$('editInvoiceV152')?.value||batch.invoice_no,batch_date:$('editDateV152')?.value||batch.batch_date,supplier:$('editSupplierV152')?.value||batch.supplier,total_before_vat:net,total_vat:vat,total_with_vat:gross};
      if(batch.id && window.sb) await sb.from('inventory_batches').update(patch).eq('id', batch.id);
      document.getElementById('batchEditModalV152')?.remove(); msg('تم تعديل فاتورة المخزون'); await financeLoadAll(); await stockBatchLoadV148(true); renderCostReductionV152();
    }catch(e){ msg(e.message||String(e),'err'); }
  };

  function enhanceBatchCards(){
    const old=window.renderStockBatchCardsV149;
    if(old && !old.v152Wrapped){
      window.renderStockBatchCardsV149=function(){
        old.apply(this, arguments);
        const grid=$('stockBatchCardsGridV149'); if(!grid) return;
        [...grid.querySelectorAll('.v149-invoice-card')].forEach((card,idx)=>{
          const actions=card.querySelector('.row-actions'); if(actions && !actions.querySelector('.v152-edit')){
            actions.insertAdjacentHTML('beforeend', `<button type="button" class="light v152-edit" onclick="stockBatchEditV152('${idx}')">تعديل</button><button type="button" class="danger v152-delete" onclick="stockBatchDeleteV152('${idx}')">حذف</button>`);
          }
        });
      };
      window.renderStockBatchCardsV149.v152Wrapped=true;
    }
  }

  function injectCssV152(){
    if($('v152Css')) return; const st=document.createElement('style'); st.id='v152Css'; st.textContent=`
      .v152-cr-head{display:flex;justify-content:space-between;gap:16px;align-items:center;background:linear-gradient(135deg,#004b3a,#0f6b4f);color:#fff;border-radius:22px;padding:18px;margin-bottom:14px}.v152-cr-head h2{margin:0;color:#fff}.v152-cr-head p{margin:4px 0 0;color:#dff5ec}.v152-filter-panel{display:grid;grid-template-columns:2fr repeat(4,1fr);gap:10px;background:#fff;border:1px solid #d6e6df;border-radius:18px;padding:12px;margin-bottom:14px}.v152-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:14px}.v152-kpis div{background:#fff;border:1px solid #d6e6df;border-radius:18px;padding:14px;text-align:center;box-shadow:0 10px 24px rgba(0,45,31,.05)}.v152-kpis small{display:block;color:#60756d}.v152-kpis b{display:block;color:#004b3a;font-size:22px;margin-top:4px}.v152-cr-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.v152-panel{background:#fff;border:1px solid #d6e6df;border-radius:22px;padding:16px;box-shadow:0 10px 24px rgba(0,45,31,.04)}.v152-panel h3{margin:0 0 12px;color:#004b3a}.v152-wide{grid-column:1/-1}.v152-recs{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:10px}.v152-recs div{border:1px solid #d6e6df;border-radius:16px;padding:12px;background:#f8fcfa}.v152-recs p{margin:6px 0 0;line-height:1.7}.v152-edit-modal{position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:99999;display:grid;place-items:center;padding:20px}.v152-edit-card{background:#fff;border-radius:24px;padding:22px;max-width:900px;width:min(900px,96vw);max-height:90vh;overflow:auto;position:relative}.v152-close{position:absolute;top:10px;left:10px;background:#eef4f1!important;color:#173c2b!important}.v152-delete{background:#c73535!important;color:#fff!important}@media(max-width:900px){.v152-filter-panel,.v152-kpis,.v152-cr-grid{grid-template-columns:1fr}.v152-cr-head{display:block}}`;
    document.head.appendChild(st);
  }

  const oldShow=window.financeShowTab;
  if(oldShow && !oldShow.v152Wrapped){
    const wrapped=function(tab,btn){ const out=oldShow.apply(this, arguments); setTimeout(()=>{ injectCostReductionUi(); enhanceBatchCards(); if(tab==='costReduction') renderCostReductionV152(); },120); return out; };
    wrapped.v152Wrapped=true; window.financeShowTab=wrapped;
  }
  const oldRender=window.financeRenderAll;
  if(oldRender && !oldRender.v152Wrapped){
    const wrapped=function(){ const out=oldRender.apply(this, arguments); setTimeout(()=>{ injectCostReductionUi(); enhanceBatchCards(); if(!$('financeTabCostReduction')?.classList.contains('hidden')) renderCostReductionV152(); },160); return out; };
    wrapped.v152Wrapped=true; window.financeRenderAll=wrapped;
  }
  window.addEventListener('load',()=>setTimeout(()=>{ injectCostReductionUi(); enhanceBatchCards(); if(window.renderStockBatchCardsV149) renderStockBatchCardsV149(); },1200));
})();

// ================= V153 Product Consumption Summary Fix =================
// Fix product detail summary: consumed must be calculated as issued/out minus returns,
// not only movements explicitly marked as consume. This keeps all product cards and
// product detail modals logically aligned with request returns.
(function(){
  const byId = id => document.getElementById(id);
  const esc2 = s => String(s ?? '').replace(/[&<>\"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const n2 = v => (typeof num === 'function' ? num(v) : Number(String(v ?? 0).replace(/,/g,''))) || 0;
  const q2 = v => n2(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const money2 = v => (typeof money === 'function' ? money(v) : `${n2(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س`);
  const project2 = id => (typeof financeProjectName === 'function' ? financeProjectName(id) : ((window.data?.projects||[]).find(p=>String(p.id)===String(id))?.name || '-'));
  const itemCode2 = it => (typeof v118ProductCode === 'function' ? v118ProductCode(it) : '') || it?.product_code || it?.serial_number || it?.code || '';
  const itemBarcode2 = it => (typeof v118Barcode === 'function' ? v118Barcode(it) : '') || it?.barcode || it?.supplier_barcode || '';
  const itemImg2 = it => it?.image_url || it?.image || it?.photo_url || it?.product_image || '';

  function metricsForItem(itemId){
    const it = (window.data?.inventoryItems || []).find(x => String(x.id) === String(itemId)) || {id:itemId};
    const movements = (window.data?.inventoryMovements || []).filter(m => String(m.item_id) === String(itemId) || String(m.item_name||'') === String(it.name||''));
    let movOut=0, issuedFromRequests=0, returned=0, directConsumed=0;
    movements.forEach(m=>{
      const t=String(m.movement_type||'').toLowerCase(); const q=n2(m.quantity);
      if(t==='out' || t==='صرف' || t==='issue') movOut += q;
      else if(t==='return' || t==='مرتجع' || t==='رجوع') returned += q;
      else if(t==='consume' || t==='consumption' || t==='استهلاك') directConsumed += q;
    });
    (window.data?.inventoryRequests || []).forEach(r=>{
      if(!/approved|issued|صرف|معتمد|done|completed/i.test(String(r.status||''))) return;
      let lines = r.request_items || r.items || r.request_lines || r.lines || [];
      if(typeof lines === 'string'){ try{ lines = JSON.parse(lines); }catch(_){ lines = []; } }
      if(Array.isArray(lines) && lines.length){
        lines.forEach(l=>{ if(String(l.item_id)===String(itemId) || String(l.item_name||'')===String(it.name||'')) issuedFromRequests += Math.abs(n2(l.quantity)); });
      } else if(String(r.item_id)===String(itemId) || String(r.item_name||'')===String(it.name||'')) issuedFromRequests += Math.abs(n2(r.quantity));
    });
    const issued = issuedFromRequests > 0 ? issuedFromRequests : movOut;
    const netIssued = Math.max(0, issued - returned);
    const totalConsumed = netIssued + directConsumed;
    return {movements, issued, returned, netIssued, directConsumed, consumed:netIssued, totalConsumed};
  }
  window.inventoryProductMetricsV153 = metricsForItem;

  window.inventoryOpenItemSmart = function(id){
    const it = (window.data?.inventoryItems || []).find(x => String(x.id) === String(id));
    if(!it) return (typeof msg === 'function' ? msg('الصنف غير موجود','err') : alert('الصنف غير موجود'));
    const m = metricsForItem(id);
    const rows = m.movements.slice(0,40).map(x=>{
      const label = x.movement_type === 'out' ? 'صرف' : (x.movement_type === 'return' ? 'مرتجع' : (x.movement_type === 'consume' ? 'استهلاك مباشر' : (x.movement_type === 'in' ? 'إدخال' : 'تعديل')));
      return `<tr><td>${esc2(x.movement_date || String(x.created_at||'').slice(0,10) || '-')}</td><td>${esc2(label)}</td><td>${q2(x.quantity)}</td><td>${esc2(x.project_name || project2(x.project_id))}</td><td>${esc2(x.receiver || '-')}</td><td>${esc2(x.reason || x.notes || '-')}</td></tr>`;
    }).join('') || '<tr><td colspan="6">لا توجد حركات مسجلة</td></tr>';
    const img = itemImg2(it);
    const html = `<div style="display:flex;gap:18px;align-items:flex-start;flex-wrap:wrap">
      ${img ? `<img class="smart-img-v129" style="width:150px;height:150px;object-fit:contain" src="${esc2(img)}">` : ''}
      <div style="flex:1;min-width:260px"><h2>${esc2(it.name || '-')}</h2>
        <div class="smart-meta-v129">
          <div class="m"><small>كود المنتج</small><b>${esc2(itemCode2(it) || '-')}</b></div>
          <div class="m"><small>كود / باركود المورد</small><b>${esc2(itemBarcode2(it) || '-')}</b></div>
          <div class="m"><small>المورد</small><b>${esc2(it.supplier || '-')}</b></div>
          <div class="m"><small>التصنيف</small><b>${esc2(it.category || '-')}</b></div>
          <div class="m"><small>المتبقي</small><b>${q2(it.quantity)} ${esc2(it.unit || '')}</b></div>
          <div class="m"><small>حد إعادة الطلب</small><b>${q2(it.min_quantity || it.reorder_level || 0)}</b></div>
          <div class="m"><small>سعر الحبة</small><b>${money2(it.unit_cost)}</b></div>
          <div class="m"><small>سعر شامل الضريبة</small><b>${money2(n2(it.unit_cost) * 1.15)}</b></div>
        </div>
      </div>
    </div>
    <h3>ملخص الحركة</h3>
    <div class="smart-meta-v129">
      <div class="m"><small>الصادر لأوامر الصرف</small><b>${q2(m.issued)}</b></div>
      <div class="m"><small>المرتجع</small><b>${q2(m.returned)}</b></div>
      <div class="m"><small>صافي المستهلك</small><b>${q2(m.consumed)}</b></div>
      <div class="m"><small>استهلاك مباشر</small><b>${q2(m.directConsumed)}</b></div>
      <div class="m"><small>إجمالي المستهلك</small><b>${q2(m.totalConsumed)}</b></div>
      <div class="m"><small>المتبقي</small><b>${q2(it.quantity)}</b></div>
      <div class="m"><small>عدد الحركات</small><b>${m.movements.length}</b></div>
    </div>
    <h3>آخر الحركات</h3>
    <table><thead><tr><th>التاريخ</th><th>الحركة</th><th>الكمية</th><th>المشروع</th><th>المستلم</th><th>السبب</th></tr></thead><tbody>${rows}</tbody></table>`;
    if(typeof window.smartOpenV129 === 'function'){
      window.smartOpenV129('تفاصيل المنتج', html, `<button onclick="financePrintReport && financePrintReport('productDetail')">طباعة</button>`);
    } else {
      const box = byId('inventoryProductDetailBox'); if(box) box.innerHTML = html;
    }
  };

  // Patch report product detail box after any report render, so the visible report also uses the correct consumed formula.
  const oldFinanceRenderReportsV153 = window.financeRenderReports;
  window.financeRenderReports = function(){
    if(typeof oldFinanceRenderReportsV153 === 'function') oldFinanceRenderReportsV153.apply(this, arguments);
    try{
      const select = byId('inventoryReportProduct');
      const box = byId('inventoryProductDetailBox');
      if(!select || !box || !select.value) return;
      const it = (window.data?.inventoryItems || []).find(x => String(x.id) === String(select.value));
      if(!it) return;
      const met = metricsForItem(it.id);
      // Replace only summary labels/values when old content exists; keep detailed rows from existing render.
      const summary = `<div class="kpis small">
        <div class="kpi"><small>الصادر لأوامر الصرف</small><b>${q2(met.issued)}</b></div>
        <div class="kpi"><small>المرتجع</small><b>${q2(met.returned)}</b></div>
        <div class="kpi"><small>صافي المستهلك</small><b>${q2(met.consumed)}</b></div>
        <div class="kpi"><small>استهلاك مباشر</small><b>${q2(met.directConsumed)}</b></div>
        <div class="kpi"><small>إجمالي المستهلك</small><b>${q2(met.totalConsumed)}</b></div>
        <div class="kpi"><small>المتبقي</small><b>${q2(it.quantity)}</b></div>
      </div>`;
      const existing = box.innerHTML || '';
      if(existing.includes('class="kpis small"')){
        box.innerHTML = existing.replace(/<div class="kpis small">[\s\S]*?<\/div>\s*<div class="table-wrap">/, summary + '<div class="table-wrap">');
      }
    }catch(e){}
  };
})();


/* ===================== V157 Client Reports Service/Edit + Smart Sidebar Logo Fix ===================== */
(function(){
  'use strict';
  const $v157 = id => document.getElementById(id);
  const esc157 = v => (typeof esc==='function'?esc(v):String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])));
  function arr157(v){
    if(Array.isArray(v)) return v;
    if(!v) return [];
    if(typeof v==='string'){
      try{ const x=JSON.parse(v); return Array.isArray(x)?x:[]; }catch(_){ return []; }
    }
    return [];
  }
  function reportServices157(reportId){
    return (window.data?.clientReportServices||[])
      .filter(s=>String(s.report_id)===String(reportId))
      .sort((a,b)=>(Number(a.sort_order)||0)-(Number(b.sort_order)||0));
  }
  function serviceName157(s){
    return String(s?.service_type || s?.title || s?.service_name || 'خدمة').trim();
  }
  function serviceNames157(reportId, fallback){
    const names = reportServices157(reportId).map(serviceName157).filter(Boolean);
    if(names.length) return [...new Set(names)];
    return fallback ? [fallback] : [];
  }
  function reportRating157(reportId){
    if(typeof avgRatingLabel==='function') return avgRatingLabel(reportId);
    const rs=(window.data?.clientServiceRatings||[]).filter(r=>String(r.report_id)===String(reportId));
    return rs.length ? (rs[0].rating||'تم التقييم') : 'لم يتم التقييم';
  }
  function badgeClass157(v){
    if(typeof ratingClass==='function') return ratingClass(v);
    return v==='يحتاج تحسين'?'red':(v&&v!=='لم يتم التقييم'?'green':'amber');
  }
  function statusText157(s){ return typeof reportStatusText==='function'?reportStatusText(s):(s==='published'?'معتمد ومنشور':s==='draft'?'مسودة':'غير منشور'); }
  function statusClass157(s){ return typeof reportStatusClass==='function'?reportStatusClass(s):(s==='published'?'green':s==='draft'?'amber':'red'); }
  function reportUrl157(token){ return typeof clientReportUrl==='function'?clientReportUrl(token):('client-report.html?token='+encodeURIComponent(token||'')); }

  // Fix service display in client reports table and keep filters working.
  window.serviceNamesForReport = function(reportId){ return serviceNames157(reportId, ''); };
  window.reportMatchesService = function(reportId, service){
    if(!service) return true;
    return serviceNames157(reportId,'').some(n=>String(n).includes(service));
  };

  window.renderPremiumReports = function(){
    const body=$v157('premiumReportsBody'); if(!body) return;
    const reports=window.data?.clientReports||[];
    const month=$v157('premiumReportFilterMonth')?.value || (typeof today==='function'?today().slice(0,7):'');
    const mReports=reports.filter(r=>String(r.report_date||'').startsWith(month));
    if($v157('reportsTotalKpi')) $v157('reportsTotalKpi').textContent=reports.length;
    if($v157('reportsMonthKpi')) $v157('reportsMonthKpi').textContent=mReports.length;
    if($v157('reportsDraftKpi')) $v157('reportsDraftKpi').textContent=reports.filter(r=>r.status==='draft').length;
    if($v157('reportsPublishedKpi')) $v157('reportsPublishedKpi').textContent=reports.filter(r=>r.status==='published').length;
    if($v157('reportsFollowKpi')) $v157('reportsFollowKpi').textContent=(window.data?.clientServiceRatings||[]).filter(r=>r.rating==='يحتاج تحسين'||r.followup_requested).length;
    try{ if(typeof renderProjectsWithoutReports==='function') renderProjectsWithoutReports(); }catch(_){ }
    const q=($v157('premiumReportSearch')?.value||'').trim();
    const smart=($v157('premiumServiceSmartSearch')?.value||'').trim();
    const pid=$v157('premiumReportFilterProject')?.value||'';
    const st=$v157('premiumReportFilterStatus')?.value||'';
    const sv=$v157('premiumReportFilterService')?.value||'';
    const rt=$v157('premiumReportFilterRating')?.value||'';
    let rows=[...reports];
    if(q) rows=rows.filter(r=>[r.report_no,r.project_name,r.title,r.report_type,serviceNames157(r.id,r.report_type).join(' ')].join(' ').includes(q));
    if(smart) rows=rows.filter(r=>[r.report_no,r.project_name,r.title,r.report_type,serviceNames157(r.id,r.report_type).join(' ')].join(' ').includes(smart));
    if(pid) rows=rows.filter(r=>String(r.project_id)===String(pid));
    if(st) rows=rows.filter(r=>(r.status||'unpublished')===st);
    if(month) rows=rows.filter(r=>String(r.report_date||'').startsWith(month));
    if(sv) rows=rows.filter(r=>serviceNames157(r.id,r.report_type).some(n=>String(n).includes(sv)));
    if(rt) rows=rows.filter(r=> rt==='none' ? !(window.data?.clientServiceRatings||[]).some(x=>String(x.report_id)===String(r.id)) : (window.data?.clientServiceRatings||[]).some(x=>String(x.report_id)===String(r.id)&&x.rating===rt));
    body.innerHTML=rows.map(r=>{
      const names=serviceNames157(r.id,r.report_type);
      const rating=reportRating157(r.id);
      const url=r.public_token?reportUrl157(r.public_token):'';
      const servicesHtml=names.length?names.map(n=>`<span class="service-chip">${esc157(n)}</span>`).join(''):'<span class="service-chip">غير محدد</span>';
      return `<tr><td><b>${esc157(r.report_no||r.id)}</b><br><small>${esc157(r.title||'')}</small></td><td>${esc157(r.report_date||'')}</td><td>${esc157(r.project_name||(typeof projectName==='function'?projectName(r.project_id):''))}</td><td>${servicesHtml}</td><td><span class="badge ${statusClass157(r.status)}">${statusText157(r.status)}</span></td><td><span class="badge ${badgeClass157(rating)}">${esc157(rating)}</span></td><td>${url?`<div class="client-copy">${esc157(url)}</div><button class="light" onclick="copyText('${encodeURIComponent(url)}')">نسخ</button>`:'غير منشور'}</td><td class="row-actions"><button onclick="editPremiumReport('${r.id}')">تعديل</button>${r.status==='published'?`<button class="light" onclick="openClientReport('${r.public_token}')">عرض</button><button class="light" onclick="sendPremiumWhatsapp('${r.id}')">واتساب</button>`:''}<button class="danger" onclick="deletePremiumReport('${r.id}')">حذف</button></td></tr>`;
    }).join('') || `<tr><td colspan="8">${window.data?.clientReportsError?'شغّل ملف SQL الخاص بالتقارير في Supabase':'لا توجد تقارير'}</td></tr>`;
    try{ renderClientReportProjectSlides157(); }catch(_){ }
  };

  function makeServiceState157(s){
    return {
      service_type: serviceName157(s),
      title: s?.title || serviceName157(s),
      service_description: s?.service_description || '',
      scope_work: s?.scope_work || '',
      notes: s?.notes || '',
      before_images: arr157(s?.before_images),
      during_images: arr157(s?.during_images),
      after_images: arr157(s?.after_images),
      source: s?.source || 'admin'
    };
  }

  // Fix Edit button: always show the edit form and rebuild services editor.
  window.editPremiumReport = async function(id){
    const r=(window.data?.clientReports||[]).find(x=>String(x.id)===String(id));
    if(!r){ try{ msg('لم يتم العثور على التقرير','err'); }catch(_){ alert('لم يتم العثور على التقرير'); } return; }
    const set=(id,v)=>{ const el=$v157(id); if(el) el.value=v??''; };
    set('premiumReportId', r.id);
    set('premiumReportProject', r.project_id||'');
    set('premiumReportDate', r.report_date || (typeof today==='function'?today():''));
    set('premiumReportTitle', r.title||'');
    set('premiumReportType', r.report_type||'تقرير خدمات');
    set('premiumChairmanName', r.chairman_name||'');
    set('premiumChairmanPhone', r.chairman_phone||'');
    set('premiumSummary', r.executive_summary || (typeof defaultReportSummary==='function'?defaultReportSummary():''));
    const services=reportServices157(id).map(makeServiceState157);
    window.premiumServicesState = services.length ? services : [(typeof makeService==='function'?makeService(r.report_type||'خدمة'):{service_type:r.report_type||'خدمة',title:r.report_type||'خدمة',before_images:[],during_images:[],after_images:[]})];
    // premiumServicesState is a global let in the original script; assign via eval-safe fallback.
    try{ premiumServicesState = window.premiumServicesState; }catch(_){ }
    if(typeof renderPremiumServicesEditor==='function') renderPremiumServicesEditor();
    const title=$v157('premiumReportFormTitle'); if(title) title.textContent='تعديل تقرير';
    const card=$v157('premiumReportFormCard');
    if(card){ card.classList.remove('hidden'); card.style.display='block'; card.scrollIntoView({behavior:'smooth',block:'start'}); }
    try{ msg('تم فتح التقرير للتعديل','ok'); }catch(_){ }
  };

  function renderClientReportProjectSlides157(){
    const box=$v157('clientReportProjectSlides'); if(!box) return;
    const reports=window.data?.clientReports||[];
    const by=new Map();
    reports.forEach(r=>{
      const k=String(r.project_id||r.project_name||'');
      if(!by.has(k)) by.set(k,{project_id:r.project_id,project:r.project_name||'',total:0,published:0,draft:0,services:{}});
      const g=by.get(k); g.total++; if(r.status==='published') g.published++; if(r.status==='draft') g.draft++;
      serviceNames157(r.id,r.report_type).forEach(n=>{g.services[n]=(g.services[n]||0)+1;});
    });
    box.innerHTML=[...by.values()].sort((a,b)=>b.total-a.total).map(g=>`<button type="button" class="smart-report-card" onclick="document.getElementById('premiumReportFilterProject').value='${g.project_id||''}'; renderPremiumReports();"><span>${esc157(g.project)}</span><b>${g.published} منشور / ${g.total} تقرير</b><small>${Object.entries(g.services).slice(0,4).map(([n,c])=>`${esc157(n)}: ${c}`).join(' | ')||'لا توجد خدمات'}</small></button>`).join('')||'<div class="footer-note">لا توجد تقارير بعد</div>';
    const dl=$v157('premiumServiceSmartList'); if(dl){ const names=new Set(); reports.forEach(r=>{names.add(r.project_name);names.add(r.title);names.add(r.report_type);}); (window.data?.clientReportServices||[]).forEach(s=>{names.add(serviceName157(s)); names.add(s.title);}); dl.innerHTML=[...names].filter(Boolean).map(x=>`<option value="${esc157(x)}"></option>`).join(''); }
  }

  // Smart sidebar logo/brand: clearer logo card without disturbing menu behavior.
  function applySmartSidebarLogo157(){
    const brand=document.querySelector('.side .brand'); if(!brand || brand.dataset.v157==='1') return;
    brand.dataset.v157='1';
    brand.innerHTML=`<div class="sidebar-logo-card-v157"><img src="tasneef_logo_print.png" onerror="this.style.display='none'"><span>ت</span></div><div class="sidebar-brand-text-v157"><b>شركة تصنيف</b><small>إدارة المرافق</small></div>`;
  }
  const st=document.createElement('style');
  st.textContent=`
    .side .brand{gap:10px!important;margin-bottom:20px!important;align-items:center!important;justify-content:flex-start!important;direction:rtl}
    .sidebar-logo-card-v157{width:52px;height:52px;border-radius:18px;background:linear-gradient(135deg,#ffffff,#dff2eb);border:1px solid rgba(255,255,255,.28);box-shadow:0 10px 26px rgba(0,0,0,.16);display:grid;place-items:center;overflow:hidden;position:relative;flex:0 0 52px}
    .sidebar-logo-card-v157 img{width:42px;height:42px;object-fit:contain;position:absolute;inset:5px;margin:auto}
    .sidebar-logo-card-v157 span{display:none;width:38px;height:38px;border-radius:14px;background:#0a5a49;color:white;font-size:22px;font-weight:900;place-items:center}
    .sidebar-logo-card-v157 img[style*="display: none"] + span{display:grid!important}
    .sidebar-brand-text-v157{line-height:1.4;text-align:right;color:#fff;min-width:0}.sidebar-brand-text-v157 b{display:block;font-size:15px;font-weight:900;white-space:nowrap}.sidebar-brand-text-v157 small{display:block;color:#b8c8df;font-size:11px;white-space:nowrap}
    .service-chip{display:inline-block;margin:2px;padding:4px 8px;border-radius:999px;background:#e9f6f1;color:#0a5a49;font-weight:700;font-size:12px}
  `;
  document.head.appendChild(st);

  window.addEventListener('load',()=>setTimeout(()=>{ applySmartSidebarLogo157(); try{ window.renderPremiumReports(); }catch(_){ } },500));
  document.addEventListener('DOMContentLoaded',applySmartSidebarLogo157);
})();

/* ===== V158: WhatsApp report button robust fix ===== */
(function(){
  function waEsc(v){ try{return (typeof esc==='function') ? esc(v) : String(v||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}catch(e){return String(v||'');} }
  function normalizeWaV158(p){
    p=String(p||'').trim();
    p=p.replace(/[^0-9+]/g,'');
    if(p.startsWith('+')) p=p.slice(1);
    p=p.replace(/[^0-9]/g,'');
    if(!p) return '';
    if(p.startsWith('00')) p=p.slice(2);
    if(p.startsWith('05')) p='966'+p.slice(1);
    else if(p.startsWith('5') && p.length===9) p='966'+p;
    else if(p.startsWith('0') && p.length>9) p=p.slice(1);
    return p;
  }
  function buildReportWaTextV158(r){
    const project = r.project_name || (typeof projectName==='function'?projectName(r.project_id):'') || 'المشروع';
    const title = r.title || 'تقرير الخدمات';
    const token = r.public_token || '';
    const url = token && typeof clientReportUrl==='function' ? clientReportUrl(token) : '';
    return [
      `السيد / رئيس جمعية ${project} المحترم`,
      '',
      `تم نشر ${title}.`,
      url ? `للاطلاع على التقرير:\n${url}` : '',
      '',
      'شركة تصنيف لإدارة المرافق',
      '920015589'
    ].filter(Boolean).join('\n');
  }
  window.sendPremiumWhatsapp = function(id){
    try{
      const r=(window.data?.clientReports||data?.clientReports||[]).find(x=>String(x.id)===String(id));
      if(!r) return (typeof msg==='function'?msg('لم يتم العثور على التقرير','err'):alert('لم يتم العثور على التقرير'));
      if(!r.public_token) return (typeof msg==='function'?msg('التقرير غير منشور بعد','err'):alert('التقرير غير منشور بعد'));
      const text=buildReportWaTextV158(r);
      const phone=normalizeWaV158(r.chairman_phone || r.phone || r.mobile || '');
      const url= phone ? `https://wa.me/${phone}?text=${encodeURIComponent(text)}` : `https://wa.me/?text=${encodeURIComponent(text)}`;
      if(navigator.clipboard && navigator.clipboard.writeText){ navigator.clipboard.writeText(text).catch(()=>{}); }
      const w=window.open(url,'_blank','noopener,noreferrer');
      if(!w){
        // Fallback for popup blockers: create a direct link the user can click.
        const box=document.createElement('div');
        box.className='wa-fallback-v158';
        box.innerHTML=`<div class="wa-fallback-card-v158"><b>فتح واتساب</b><p>إذا لم يفتح واتساب تلقائيًا، اضغط الزر التالي.</p><a target="_blank" rel="noopener" href="${url}">فتح واتساب الآن</a><button type="button" onclick="this.closest('.wa-fallback-v158').remove()">إغلاق</button></div>`;
        document.body.appendChild(box);
      }
      if(typeof msg==='function') msg(phone?'تم فتح واتساب للتقرير':'تم فتح واتساب بدون رقم محدد، اختر جهة الإرسال');
    }catch(e){
      console.error('sendPremiumWhatsapp V158 failed',e);
      if(typeof msg==='function') msg('تعذر فتح واتساب: '+(e.message||e),'err');
    }
  };
  // Make report WhatsApp buttons reliably clickable even if older render markup exists.
  const oldRender = window.renderPremiumReports;
  if(typeof oldRender==='function'){
    window.renderPremiumReports=function(){
      oldRender.apply(this,arguments);
      setTimeout(()=>{
        document.querySelectorAll('button[onclick^="sendPremiumWhatsapp("]').forEach(b=>{
          b.type='button';
          b.classList.add('wa-report-btn-v158');
        });
      },30);
    };
  }
  const css=document.createElement('style');
  css.textContent=`
    .wa-report-btn-v158{background:#128C7E!important;color:#fff!important;border:0!important;border-radius:10px!important;font-weight:800!important}
    .wa-fallback-v158{position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:999999;display:flex;align-items:center;justify-content:center;direction:rtl}
    .wa-fallback-card-v158{background:#fff;border-radius:18px;padding:22px;max-width:360px;box-shadow:0 18px 50px rgba(0,0,0,.25);text-align:center;color:#063f32}
    .wa-fallback-card-v158 a{display:inline-block;background:#128C7E;color:#fff;text-decoration:none;border-radius:12px;padding:10px 18px;margin:8px;font-weight:800}
    .wa-fallback-card-v158 button{background:#edf7f2;color:#063f32;border:1px solid #cfe4db;border-radius:12px;padding:10px 18px;margin:8px;font-weight:800;cursor:pointer}
  `;
  document.head.appendChild(css);
})();

/* ===================== V159 Client Reports Performance & Stable Buttons Fix ===================== */
(function(){
  'use strict';
  if(window.__clientReportsStableV159) return;
  window.__clientReportsStableV159 = true;

  const $v159 = id => document.getElementById(id);
  const esc159 = v => {
    try{ return (typeof esc === 'function') ? esc(v) : String(v ?? '').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
    catch(_){ return String(v ?? ''); }
  };
  const today159 = () => (typeof today === 'function' ? today() : new Date().toISOString().slice(0,10));

  // لا تجعل شاشة التحميل تعطل أزرار التقارير أثناء جلب البيانات.
  const css = document.createElement('style');
  css.id = 'clientReportsStableV159Style';
  css.textContent = `
    .tasneef-loading-v154{pointer-events:none!important}
    .client-reports-loading-v159{opacity:.65;position:relative}
    .client-reports-loading-v159:after{content:'جاري تحديث التقارير...';position:absolute;inset:auto 12px 12px auto;background:#0a5a49;color:#fff;padding:8px 12px;border-radius:12px;font-weight:800;font-size:12px;box-shadow:0 10px 24px rgba(0,0,0,.15)}
    .client-action-btn-v159{min-width:64px;white-space:nowrap}
    .row-actions .client-action-btn-v159{margin:2px}
  `;
  document.head.appendChild(css);

  function parseImgs159(v){
    if(Array.isArray(v)) return v;
    if(!v) return [];
    if(typeof v === 'string'){
      try{ const x = JSON.parse(v); return Array.isArray(x) ? x : []; }catch(_){ return []; }
    }
    return [];
  }
  function buildClientReportIndexes159(){
    const services = window.data?.clientReportServices || [];
    const ratings = window.data?.clientServiceRatings || [];
    const serviceMap = new Map();
    services.forEach(s=>{
      const k = String(s.report_id || '');
      if(!serviceMap.has(k)) serviceMap.set(k, []);
      serviceMap.get(k).push(s);
    });
    serviceMap.forEach(arr=>arr.sort((a,b)=>(Number(a.sort_order)||0)-(Number(b.sort_order)||0)));
    const ratingMap = new Map();
    ratings.forEach(r=>{
      const k = String(r.report_id || '');
      if(!ratingMap.has(k)) ratingMap.set(k, []);
      ratingMap.get(k).push(r);
    });
    window.__clientReportIndexV159 = {serviceMap, ratingMap, ts: Date.now()};
    return window.__clientReportIndexV159;
  }
  function indexes159(){ return window.__clientReportIndexV159 || buildClientReportIndexes159(); }
  function services159(reportId){ return indexes159().serviceMap.get(String(reportId)) || []; }
  function serviceName159(s){ return String(s?.service_type || s?.title || s?.service_name || 'خدمة').trim(); }
  function serviceNames159(reportId, fallback){
    const names = services159(reportId).map(serviceName159).filter(Boolean);
    return names.length ? [...new Set(names)] : (fallback ? [fallback] : []);
  }
  function ratings159(reportId){ return indexes159().ratingMap.get(String(reportId)) || []; }
  function ratingLabel159(reportId){
    const rs = ratings159(reportId);
    if(!rs.length) return 'لم يتم التقييم';
    if(rs.some(r=>r.rating === 'يحتاج تحسين')) return 'يحتاج تحسين';
    return rs[0].rating || 'تم التقييم';
  }
  function ratingClass159(v){ return (typeof ratingClass === 'function') ? ratingClass(v) : (v==='يحتاج تحسين'?'red':(v&&v!=='لم يتم التقييم'?'green':'amber')); }
  function statusText159(s){ return (typeof reportStatusText === 'function') ? reportStatusText(s) : (s==='published'?'معتمد ومنشور':(s==='draft'?'مسودة':'غير منشور')); }
  function statusClass159(s){ return (typeof reportStatusClass === 'function') ? reportStatusClass(s) : (s==='published'?'green':(s==='draft'?'amber':'red')); }
  function reportUrl159(token){ return (typeof clientReportUrl === 'function') ? clientReportUrl(token) : ('client-report.html?token='+encodeURIComponent(token||'')); }

  let renderLock = false;
  window.serviceNamesForReport = function(reportId){ return serviceNames159(reportId, ''); };
  window.reportMatchesService = function(reportId, service){
    if(!service) return true;
    return serviceNames159(reportId,'').some(n=>String(n).includes(service));
  };

  window.renderPremiumReports = function(){
    if(renderLock) return;
    renderLock = true;
    try{
      buildClientReportIndexes159();
      const body = $v159('premiumReportsBody');
      if(!body) return;
      const reports = window.data?.clientReports || [];
      const ratings = window.data?.clientServiceRatings || [];
      const month = $v159('premiumReportFilterMonth')?.value || today159().slice(0,7);
      const mReports = reports.filter(r=>String(r.report_date||'').startsWith(month));
      if($v159('reportsTotalKpi')) $v159('reportsTotalKpi').textContent = reports.length;
      if($v159('reportsMonthKpi')) $v159('reportsMonthKpi').textContent = mReports.length;
      if($v159('reportsDraftKpi')) $v159('reportsDraftKpi').textContent = reports.filter(r=>r.status==='draft').length;
      if($v159('reportsPublishedKpi')) $v159('reportsPublishedKpi').textContent = reports.filter(r=>r.status==='published').length;
      if($v159('reportsFollowKpi')) $v159('reportsFollowKpi').textContent = ratings.filter(r=>r.rating==='يحتاج تحسين'||r.followup_requested).length;

      const q = ($v159('premiumReportSearch')?.value || '').trim();
      const smart = ($v159('premiumServiceSmartSearch')?.value || '').trim();
      const pid = $v159('premiumReportFilterProject')?.value || '';
      const st = $v159('premiumReportFilterStatus')?.value || '';
      const sv = $v159('premiumReportFilterService')?.value || '';
      const rt = $v159('premiumReportFilterRating')?.value || '';
      let rows = [...reports];
      const matchText = (r) => [r.report_no,r.project_name,r.title,r.report_type,serviceNames159(r.id,r.report_type).join(' ')].join(' ');
      if(q) rows = rows.filter(r=>matchText(r).includes(q));
      if(smart) rows = rows.filter(r=>matchText(r).includes(smart));
      if(pid) rows = rows.filter(r=>String(r.project_id)===String(pid));
      if(st) rows = rows.filter(r=>(r.status||'unpublished')===st);
      if(month) rows = rows.filter(r=>String(r.report_date||'').startsWith(month));
      if(sv) rows = rows.filter(r=>serviceNames159(r.id,r.report_type).some(n=>String(n).includes(sv)));
      if(rt) rows = rows.filter(r=> rt==='none' ? !ratings159(r.id).length : ratings159(r.id).some(x=>x.rating===rt));

      body.innerHTML = rows.map(r=>{
        const names = serviceNames159(r.id,r.report_type);
        const rating = ratingLabel159(r.id);
        const url = r.public_token ? reportUrl159(r.public_token) : '';
        const servicesHtml = names.length ? names.map(n=>`<span class="service-chip">${esc159(n)}</span>`).join('') : '<span class="service-chip">غير محدد</span>';
        return `<tr data-report-id="${esc159(r.id)}"><td><b>${esc159(r.report_no||r.id)}</b><br><small>${esc159(r.title||'')}</small></td><td>${esc159(r.report_date||'')}</td><td>${esc159(r.project_name||(typeof projectName==='function'?projectName(r.project_id):''))}</td><td>${servicesHtml}</td><td><span class="badge ${statusClass159(r.status)}">${statusText159(r.status)}</span></td><td><span class="badge ${ratingClass159(rating)}">${esc159(rating)}</span></td><td>${url?`<div class="client-copy">${esc159(url)}</div><button type="button" class="light client-action-btn-v159" data-action="copy" data-url="${encodeURIComponent(url)}">نسخ</button>`:'غير منشور'}</td><td class="row-actions"><button type="button" class="client-action-btn-v159" data-action="edit" data-id="${esc159(r.id)}">تعديل</button>${r.status==='published'?`<button type="button" class="light client-action-btn-v159" data-action="view" data-token="${esc159(r.public_token)}">عرض</button><button type="button" class="light wa-report-btn-v158 client-action-btn-v159" data-action="whatsapp" data-id="${esc159(r.id)}">واتساب</button>`:''}<button type="button" class="danger client-action-btn-v159" data-action="delete" data-id="${esc159(r.id)}">حذف</button></td></tr>`;
      }).join('') || `<tr><td colspan="8">${window.data?.clientReportsError?'شغّل ملف SQL الخاص بالتقارير في Supabase':'لا توجد تقارير'}</td></tr>`;
      try{ renderClientReportProjectSlides159(); }catch(_){ }
      try{ if(typeof renderProjectsWithoutReports === 'function' && window.premiumWithoutMode) renderProjectsWithoutReports(); }catch(_){ }
    }finally{
      renderLock = false;
    }
  };

  function renderClientReportProjectSlides159(){
    const box = $v159('clientReportProjectSlides');
    if(!box) return;
    const reports = window.data?.clientReports || [];
    const by = new Map();
    reports.forEach(r=>{
      const k = String(r.project_id || r.project_name || 'بدون مشروع');
      if(!by.has(k)) by.set(k,{project_id:r.project_id,project:r.project_name||'',total:0,published:0,draft:0,services:{}});
      const g = by.get(k);
      g.total++; if(r.status==='published') g.published++; if(r.status==='draft') g.draft++;
      serviceNames159(r.id,r.report_type).forEach(n=>{ g.services[n]=(g.services[n]||0)+1; });
    });
    box.innerHTML = [...by.values()].sort((a,b)=>b.total-a.total).map(g=>`<button type="button" class="smart-report-card" data-action="filter-project" data-project="${esc159(g.project_id||'')}"><span>${esc159(g.project||'مشروع')}</span><b>${g.published} منشور / ${g.total} تقرير</b><small>${Object.entries(g.services).slice(0,4).map(([n,c])=>`${esc159(n)}: ${c}`).join(' | ') || 'لا توجد خدمات'}</small></button>`).join('') || '<div class="footer-note">لا توجد تقارير بعد</div>';
    const dl=$v159('premiumServiceSmartList');
    if(dl){
      const names = new Set();
      reports.forEach(r=>{ names.add(r.project_name); names.add(r.title); names.add(r.report_type); });
      (window.data?.clientReportServices||[]).forEach(s=>{ names.add(serviceName159(s)); names.add(s.title); });
      dl.innerHTML = [...names].filter(Boolean).map(x=>`<option value="${esc159(x)}"></option>`).join('');
    }
  }

  // تحميل تقارير العملاء بأمان بدون تعطيل الأزرار أو تكرار الجلب.
  let reportsLoading = null;
  window.loadPremiumReportsOnly = async function(showMessage=false){
    if(reportsLoading) return reportsLoading;
    const body = $v159('premiumReportsBody');
    try{
      if(body) body.closest('.card,section,div')?.classList.add('client-reports-loading-v159');
      reportsLoading = (async()=>{
        const [reports, services, ratings] = await Promise.all([
          sb.from('client_reports').select('*').order('created_at',{ascending:false}),
          sb.from('client_report_services').select('*').order('sort_order',{ascending:true}),
          sb.from('client_service_ratings').select('*').order('created_at',{ascending:false})
        ]);
        window.data.clientReports = reports.data || [];
        window.data.clientReportServices = services.data || [];
        window.data.clientServiceRatings = ratings.data || [];
        window.data.clientReportsError = reports.error?.message || services.error?.message || ratings.error?.message || '';
        buildClientReportIndexes159();
        window.renderPremiumReports();
        try{ if(typeof renderClientRatings === 'function') renderClientRatings(); }catch(_){ }
        if(showMessage && typeof msg==='function') msg(window.data.clientReportsError ? 'تأكد من تشغيل ملف SQL الخاص بالتقارير في Supabase' : 'تم تحديث التقارير', window.data.clientReportsError?'err':'ok');
      })();
      await reportsLoading;
    }catch(e){
      window.data.clientReportsError = e.message || String(e);
      if(showMessage && typeof msg==='function') msg(window.data.clientReportsError,'err');
      try{ window.renderPremiumReports(); }catch(_){ }
    }finally{
      reportsLoading = null;
      if(body) body.closest('.card,section,div')?.classList.remove('client-reports-loading-v159');
    }
  };

  // تعديل ثابت وسريع بدون الاعتماد على كاش قديم.
  window.editPremiumReport = function(id){
    const r = (window.data?.clientReports||[]).find(x=>String(x.id)===String(id));
    if(!r){ if(typeof msg==='function') msg('لم يتم العثور على التقرير','err'); return; }
    const set=(id,v)=>{ const el=$v159(id); if(el) el.value = v ?? ''; };
    set('premiumReportId', r.id);
    set('premiumReportProject', r.project_id||'');
    set('premiumReportDate', r.report_date||today159());
    set('premiumReportTitle', r.title||'');
    set('premiumReportType', r.report_type||'تقرير خدمات');
    set('premiumChairmanName', r.chairman_name||'');
    set('premiumChairmanPhone', r.chairman_phone||'');
    set('premiumSummary', r.executive_summary || (typeof defaultReportSummary==='function'?defaultReportSummary():''));
    const list = services159(id).map(s=>({
      service_type: serviceName159(s),
      title: s.title || serviceName159(s),
      service_description: s.service_description || '',
      scope_work: s.scope_work || '',
      notes: s.notes || '',
      before_images: parseImgs159(s.before_images),
      during_images: parseImgs159(s.during_images),
      after_images: parseImgs159(s.after_images),
      source: s.source || 'admin'
    }));
    try{ premiumServicesState = list.length ? list : (typeof makeService==='function' ? [makeService(r.report_type||'خدمة')] : []); }catch(_){ window.premiumServicesState = list; }
    try{ if(typeof renderPremiumServicesEditor === 'function') renderPremiumServicesEditor(); }catch(e){ console.warn(e); }
    const title = $v159('premiumReportFormTitle'); if(title) title.textContent = 'تعديل تقرير';
    const card = $v159('premiumReportFormCard');
    if(card){ card.classList.remove('hidden'); card.style.display='block'; card.scrollIntoView({behavior:'smooth', block:'start'}); }
    if(typeof msg==='function') msg('تم فتح التقرير للتعديل','ok');
  };

  // منع تعطل الأزرار بسبب inline handlers أو إعادة رسم الجدول.
  if(!window.__clientReportsClickV159){
    window.__clientReportsClickV159 = true;
    document.addEventListener('click', async function(e){
      const btn = e.target.closest('[data-action]');
      if(!btn) return;
      const action = btn.dataset.action;
      if(!['edit','view','whatsapp','delete','copy','filter-project'].includes(action)) return;
      e.preventDefault(); e.stopPropagation();
      if(btn.dataset.busy === '1') return;
      btn.dataset.busy = '1';
      const old = btn.innerHTML;
      try{
        if(action === 'edit') window.editPremiumReport(btn.dataset.id);
        else if(action === 'view') window.openClientReport ? window.openClientReport(btn.dataset.token) : window.open(reportUrl159(btn.dataset.token),'_blank');
        else if(action === 'whatsapp') window.sendPremiumWhatsapp(btn.dataset.id);
        else if(action === 'delete') { if(typeof deletePremiumReport === 'function') await deletePremiumReport(btn.dataset.id); }
        else if(action === 'copy') { if(typeof copyText === 'function') copyText(btn.dataset.url); else navigator.clipboard?.writeText(decodeURIComponent(btn.dataset.url||'')); }
        else if(action === 'filter-project') { const f=$v159('premiumReportFilterProject'); if(f) f.value=btn.dataset.project||''; window.renderPremiumReports(); }
      }finally{
        btn.dataset.busy = '0';
        btn.innerHTML = old;
      }
    }, true);
  }

  // ربط الفلاتر بتأخير بسيط حتى لا يحصل تعليق أثناء الكتابة.
  let filterTimer = null;
  document.addEventListener('input', function(e){
    if(!e.target || !['premiumReportSearch','premiumServiceSmartSearch'].includes(e.target.id)) return;
    clearTimeout(filterTimer);
    filterTimer = setTimeout(()=>{ try{ window.renderPremiumReports(); }catch(_){} }, 180);
  });
  document.addEventListener('change', function(e){
    if(!e.target || !/^premiumReportFilter/.test(e.target.id)) return;
    clearTimeout(filterTimer);
    filterTimer = setTimeout(()=>{ try{ window.renderPremiumReports(); }catch(_){} }, 80);
  });

  setTimeout(()=>{ try{ buildClientReportIndexes159(); window.renderPremiumReports(); }catch(e){ console.warn('V159 initial render skipped', e); } }, 700);
})();

/* ===== V160: Inventory exact product-code lookup + simplified admin/warehouse stock views ===== */
(function(){
  'use strict';
  const $ = (id)=>document.getElementById(id);
  const safe = (v)=>String(v ?? '').trim();
  const n = (v)=>{ const x=parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x)?x:0; };
  const q = (v)=>n(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const e = (v)=>safe(v).replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const sessionRole = ()=>{ try{ return (typeof session==='function' ? (session()||{}).role : (JSON.parse(localStorage.getItem('tasneef_session')||'{}').role)) || ''; }catch(_){ return ''; } };
  const isWarehouse = ()=>sessionRole()==='warehouse_manager';
  function codeOf(it){ return safe(it?.product_code || it?.barcode || it?.supplier_barcode || it?.serial_number || ''); }
  function companyCodeOf(it){ return safe(it?.company_code || it?.internal_code || ''); }
  function exactFindByCode(code){
    code = safe(code);
    if(!code) return null;
    return (window.data?.inventoryItems||[]).find(it => {
      const keys = [it.product_code,it.barcode,it.supplier_barcode,it.serial_number,it.company_code,it.internal_code].map(x=>safe(x)).filter(Boolean);
      return keys.includes(code);
    }) || null;
  }
  window.v160FindInventoryItemByExactCode = exactFindByCode;

  function setVal(id, val){ const el=$(id); if(el) el.value = val ?? ''; }
  function ensureNewItemDefaults(){
    if($('batchItemTypeV148') && !$('batchItemTypeV148').value) $('batchItemTypeV148').value='مادة';
    if($('batchUnitV148') && !$('batchUnitV148').value) $('batchUnitV148').value='حبة';
  }
  function resetBatchFieldsForNewCode(){
    setVal('batchProductNameV148','');
    setVal('batchCategoryV148','');
    setVal('batchUnitV148','حبة');
    if($('batchItemTypeV148')) $('batchItemTypeV148').value='مادة';
    setVal('batchUnitPriceV148','');
    setVal('batchCompanyCodeV151','');
    const hint=$('batchLookupHintV151');
    if(hint) hint.textContent='';
  }
  function v160LookupBatchProduct(){
    const inp=$('batchProductCodeV148'); if(!inp) return;
    const code=safe(inp.value);
    if(!code){ resetBatchFieldsForNewCode(); return; }
    const it=exactFindByCode(code);
    const hintId='batchLookupHintV151';
    let hint=$(hintId);
    if(!hint){ hint=document.createElement('div'); hint.id=hintId; hint.className='v151-lookup-hint'; inp.closest('div')?.appendChild(hint); }
    if(!it){
      // مهم: لا نتعرف على المنتج بمجرد رقم عشوائي أو ID. التعرف فقط من كود المنتج الفعلي.
      resetBatchFieldsForNewCode();
      if(hint) hint.innerHTML='';
      ensureNewItemDefaults();
      return;
    }
    setVal('batchProductNameV148', it.name||'');
    setVal('batchCategoryV148', it.category||'');
    if($('batchItemTypeV148')) $('batchItemTypeV148').value = it.item_type || it.type || 'مادة';
    setVal('batchUnitV148', it.unit || 'حبة');
    setVal('batchUnitPriceV148', n(it.unit_cost) || '');
    setVal('batchCompanyCodeV151', companyCodeOf(it));
    if(hint) hint.innerHTML=`تم التعرف على الصنف: <b>${e(it.name)}</b> — الكمية ${q(it.quantity)} ${e(it.unit||'')}`;
  }
  window.lookupBatchProductV151 = v160LookupBatchProduct;

  function bindExactLookup(){
    const inp=$('batchProductCodeV148'); if(!inp || inp.dataset.v160ExactLookup) return;
    inp.dataset.v160ExactLookup='1';
    const run=()=>setTimeout(v160LookupBatchProduct, 160);
    inp.addEventListener('input', run);
    inp.addEventListener('change', run);
    inp.addEventListener('blur', run);
  }

  function simplifyBatchLineForm(){
    const code=$('batchProductCodeV148'); if(!code) return;
    // أسماء الحقول المطلوبة في الإدارة: الصورة، الاسم، الكود، الكمية، السعر.
    const name=$('batchProductNameV148'), qtyInp=$('batchQtyV148'), price=$('batchUnitPriceV148');
    const codeBox=code.closest('div'), nameBox=name?.closest('div'), qtyBox=qtyInp?.closest('div'), priceBox=price?.closest('div');
    if(codeBox?.querySelector('label')) codeBox.querySelector('label').textContent='كود المنتج';
    if(nameBox?.querySelector('label')) nameBox.querySelector('label').textContent='اسم المنتج';
    if(qtyBox?.querySelector('label')) qtyBox.querySelector('label').textContent='الكمية';
    if(priceBox?.querySelector('label')) priceBox.querySelector('label').textContent='السعر قبل الضريبة';
    code.placeholder='اكتب كود المنتج للتعرف عليه تلقائيًا';
    name && (name.placeholder='اسم المنتج الجديد أو الموجود');
    // نخفي التفاصيل غير الأساسية من نموذج الإدخال مع بقاء قيمها محفوظة.
    ['batchCategoryV148','batchItemTypeV148','batchUnitV148','batchCompanyCodeV151'].forEach(id=>{
      const box=$(id)?.closest('div');
      if(box) box.classList.add('v160-optional-stock-field');
    });
    ensureNewItemDefaults();
    bindExactLookup();
  }

  // منع إضافة صنف خطأ عند كتابة رقم يطابق ID فقط. المنتج الموجود يتعرف فقط من كود المنتج الفعلي.
  const oldAddLine = window.stockBatchAddLineV148;
  if(oldAddLine && !oldAddLine.v160Wrapped){
    const wrapped=function(){
      const code=safe($('batchProductCodeV148')?.value);
      const it=exactFindByCode(code);
      // إذا لا يوجد صنف بهذا الكود، فهو صنف جديد؛ لا نملأه تلقائيًا ولا نقبل الاسم الناتج من رقم عشوائي.
      if(!it){
        const hint=$('batchLookupHintV151'); if(hint) hint.textContent='';
        if(!$('batchUnitV148')?.value) setVal('batchUnitV148','حبة');
        if($('batchItemTypeV148') && !$('batchItemTypeV148').value) $('batchItemTypeV148').value='مادة';
      }
      return oldAddLine.apply(this, arguments);
    };
    wrapped.v160Wrapped=true; window.stockBatchAddLineV148=wrapped;
  }

  function statsFor(it){
    try{ if(typeof inventoryItemStatsV151==='function') return inventoryItemStatsV151(it); }catch(_){}
    return {remaining:n(it?.quantity), inQty:0, outQty:0, retQty:0, consumed:0};
  }
  function renderWarehouseSimpleCatalog(rows){
    return `<tr><td colspan="20"><div class="v160-warehouse-grid">${(rows||[]).map(it=>{
      const st=statsFor(it); const img=it.image_url?`<img src="${e(it.image_url)}" onerror="this.style.display='none'">`:'<span>لا صورة</span>';
      return `<article class="v160-warehouse-card">
        <div class="v160-wh-img">${img}</div>
        <div class="v160-wh-body"><h3>${e(it.name||'-')}</h3><p>كود المنتج: <b>${e(codeOf(it)||'-')}</b></p><p>الكمية: <b>${q(st.remaining)} ${e(it.unit||'')}</b></p></div>
        <button type="button" onclick="inventoryOpenItemSmart&&inventoryOpenItemSmart('${e(it.id)}')">عرض</button>
      </article>`;
    }).join('') || '<div class="muted">لا توجد أصناف</div>'}</div></td></tr>`;
  }

  const oldRenderItems = window.inventoryRenderItems;
  if(oldRenderItems && !oldRenderItems.v160Wrapped){
    const wrapped=function(){
      simplifyBatchLineForm();
      if(isWarehouse()){
        const b=$('inventoryItemsBody');
        if(b){
          const search=safe($('inventorySearch')?.value || $('financeSearch')?.value).toLowerCase();
          const supplier=safe($('inventorySupplierFilter')?.value || $('inventoryReportSupplier')?.value);
          const cat=safe($('inventoryCategoryFilter')?.value);
          let rows=[...(window.data?.inventoryItems||[])];
          if(search) rows=rows.filter(it=>[it.name,codeOf(it),companyCodeOf(it),it.supplier,it.category,it.unit].join(' ').toLowerCase().includes(search));
          if(supplier) rows=rows.filter(it=>safe(it.supplier)===supplier);
          if(cat) rows=rows.filter(it=>safe(it.category)===cat);
          b.innerHTML=renderWarehouseSimpleCatalog(rows);
          return;
        }
      }
      const out=oldRenderItems.apply(this, arguments);
      setTimeout(simplifyBatchLineForm,50);
      return out;
    };
    wrapped.v160Wrapped=true; window.inventoryRenderItems=wrapped;
  }

  function injectCss(){
    if($('v160Css')) return;
    const st=document.createElement('style'); st.id='v160Css'; st.textContent=`
      .v160-optional-stock-field{display:none!important}
      #stockBatchCardV148 .nested-card{border:1px solid #dceee7;background:#fbfffd;border-radius:18px;padding:14px}
      #stockBatchCardV148 .nested-card .split{grid-template-columns:1fr 1fr!important;gap:12px!important}
      #stockBatchCardV148 #batchLineImagePreviewV148{border:1px dashed #b9dbcf;background:#f4fbf8;border-radius:14px;padding:8px;min-height:44px}
      .v160-warehouse-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:14px;padding:8px;direction:rtl}
      .v160-warehouse-card{display:grid;grid-template-columns:70px 1fr auto;align-items:center;gap:12px;background:#fff;border:1px solid #d8eae3;border-radius:18px;padding:12px;box-shadow:0 8px 18px rgba(0,45,31,.05)}
      .v160-wh-img{width:64px;height:64px;border:1px solid #d6ebe3;border-radius:14px;background:#f7fbf9;display:grid;place-items:center;overflow:hidden;color:#789}
      .v160-wh-img img{max-width:100%;max-height:100%;object-fit:contain}.v160-wh-body h3{margin:0 0 5px;color:#004331;font-size:16px}.v160-wh-body p{margin:3px 0;color:#526b62;font-size:12px}.v160-wh-body b{color:#003f31}.v160-warehouse-card button{white-space:nowrap}
      @media(max-width:750px){#stockBatchCardV148 .nested-card .split{grid-template-columns:1fr!important}.v160-warehouse-card{grid-template-columns:60px 1fr}.v160-warehouse-card button{grid-column:1/-1}}
    `; document.head.appendChild(st);
  }

  const oldRenderAll=window.financeRenderAll;
  if(oldRenderAll && !oldRenderAll.v160Wrapped){
    const wrapped=function(){ const out=oldRenderAll.apply(this, arguments); setTimeout(()=>{injectCss(); simplifyBatchLineForm(); if(isWarehouse() && window.inventoryRenderItems) inventoryRenderItems();},160); return out; };
    wrapped.v160Wrapped=true; window.financeRenderAll=wrapped;
  }
  const oldShow=window.financeShowTab;
  if(oldShow && !oldShow.v160Wrapped){
    const wrapped=function(){ const out=oldShow.apply(this, arguments); setTimeout(()=>{injectCss(); simplifyBatchLineForm(); if(isWarehouse() && window.inventoryRenderItems) inventoryRenderItems();},120); return out; };
    wrapped.v160Wrapped=true; window.financeShowTab=wrapped;
  }
  window.addEventListener('load',()=>setTimeout(()=>{injectCss(); simplifyBatchLineForm(); if(isWarehouse() && window.inventoryRenderItems) inventoryRenderItems();},1600));
})();

/* ===== V161: Real inventory invoice delete + strict product-code lookup + clean invoice entry ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const txt = v => String(v ?? '').trim();
  const n = v => { const x = parseFloat(String(v ?? 0).replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const escHtml = v => txt(v).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money = v => n(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  const qty = v => n(v).toLocaleString('en-US',{maximumFractionDigits:2});

  function msgSafe(message,type){ if(typeof msg === 'function') msg(message,type); else alert(message); }

  function productCodeOf(item){
    return txt(item?.product_code || item?.barcode || item?.supplier_barcode || item?.serial_number || '');
  }

  function findByProductCodeOnly(code){
    const c = txt(code);
    if(!c) return null;
    return (window.data?.inventoryItems || []).find(it => {
      const keys = [it.product_code, it.barcode, it.supplier_barcode, it.serial_number]
        .map(txt).filter(Boolean);
      return keys.includes(c);
    }) || null;
  }
  window.v161FindByProductCodeOnly = findByProductCodeOnly;
  window.v160FindInventoryItemByExactCode = findByProductCodeOnly;

  function setVal(id,value){ const el=$(id); if(el) el.value = value ?? ''; }

  function clearAutoFields(){
    setVal('batchProductNameV148','');
    setVal('batchCategoryV148','');
    setVal('batchUnitV148','حبة');
    setVal('batchUnitPriceV148','');
    setVal('batchCompanyCodeV151','');
    if($('batchItemTypeV148')) $('batchItemTypeV148').value='مادة';
    const hint=$('batchLookupHintV151'); if(hint) hint.innerHTML='';
  }

  function lookupProductV161(){
    const inp=$('batchProductCodeV148'); if(!inp) return;
    const code=txt(inp.value);
    let hint=$('batchLookupHintV151');
    if(!hint){ hint=document.createElement('div'); hint.id='batchLookupHintV151'; hint.className='v151-lookup-hint'; inp.closest('div')?.appendChild(hint); }
    if(!code){ clearAutoFields(); return null; }
    const item=findByProductCodeOnly(code);
    if(!item){
      // لا نطابق رقم ID أو أي رقم عشوائي. التعرف فقط من كود المنتج الحقيقي.
      clearAutoFields();
      if(hint) hint.innerHTML='<small style="color:#778">كود جديد — أدخل اسم المنتج والصورة والكمية والسعر.</small>';
      return null;
    }
    setVal('batchProductNameV148', item.name || '');
    setVal('batchCategoryV148', item.category || '');
    if($('batchItemTypeV148')) $('batchItemTypeV148').value = item.item_type || item.type || 'مادة';
    setVal('batchUnitV148', item.unit || 'حبة');
    setVal('batchUnitPriceV148', n(item.unit_cost) || '');
    setVal('batchCompanyCodeV151', item.company_code || item.internal_code || '');
    if(hint) hint.innerHTML=`تم التعرف على الصنف: <b>${escHtml(item.name)}</b> — الكمية ${qty(item.quantity)} ${escHtml(item.unit||'')}`;
    return item;
  }
  window.lookupBatchProductV151 = lookupProductV161;

  function bindStrictLookup(){
    const inp=$('batchProductCodeV148'); if(!inp || inp.dataset.v161StrictLookup) return;
    inp.dataset.v161StrictLookup='1';
    const run=()=>setTimeout(lookupProductV161,90);
    inp.addEventListener('input', run);
    inp.addEventListener('change', run);
    inp.addEventListener('blur', run);
  }

  function cleanInvoiceEntryForm(){
    const code=$('batchProductCodeV148'); if(!code) return;
    const title = $('#stockBatchCardV148 h2'); if(title) title.textContent='إضافة فاتورة مخزون';
    const nested = code.closest('.nested-card');
    if(nested){
      const h3=nested.querySelector('h3'); if(h3) h3.textContent='إضافة صنف للفاتورة';
      nested.classList.add('v161-simple-invoice-entry');
    }
    const labelMap = {
      batchProductCodeV148:'كود المنتج',
      batchProductNameV148:'اسم المنتج',
      batchQtyV148:'الكمية',
      batchUnitPriceV148:'السعر قبل الضريبة'
    };
    Object.entries(labelMap).forEach(([id,label])=>{ const el=$(id); const l=el?.closest('div')?.querySelector('label'); if(l) l.textContent=label; });
    code.placeholder='اكتب كود المنتج فقط';
    if($('batchProductNameV148')) $('batchProductNameV148').placeholder='اسم المنتج';
    ['batchCategoryV148','batchItemTypeV148','batchUnitV148','batchCompanyCodeV151'].forEach(id=>{
      const box=$(id)?.closest('div'); if(box) box.classList.add('v161-hide-advanced-field');
    });
    bindStrictLookup();
  }

  const oldAddLine = window.stockBatchAddLineV148;
  if(oldAddLine && !oldAddLine.v161Wrapped){
    window.stockBatchAddLineV148 = function(){
      const code = txt($('batchProductCodeV148')?.value);
      if(!code) return msgSafe('كود المنتج مطلوب','err');
      const item = findByProductCodeOnly(code);
      if(item) lookupProductV161();
      else {
        // صنف جديد: لا نترك أي بيانات تلقائية من رقم عشوائي.
        const name = txt($('batchProductNameV148')?.value);
        if(!name) return msgSafe('هذا كود جديد. أدخل اسم المنتج أولًا.','err');
        if(!$('batchUnitV148')?.value) setVal('batchUnitV148','حبة');
        if($('batchItemTypeV148') && !$('batchItemTypeV148').value) $('batchItemTypeV148').value='مادة';
      }
      return oldAddLine.apply(this, arguments);
    };
    window.stockBatchAddLineV148.v161Wrapped = true;
  }

  async function updateItemQuantity(itemId, delta){
    if(!window.sb || !itemId) return;
    const item=(window.data?.inventoryItems||[]).find(x=>String(x.id)===String(itemId));
    const current = n(item?.quantity);
    const next = Math.max(0, current + n(delta));
    const {error}=await sb.from('inventory_items').update({quantity:next}).eq('id', itemId);
    if(error) throw error;
    if(item) item.quantity=next;
  }

  async function deleteMovementsForBatch(batch){
    const movementIds = (batch.lines||[]).map(l=>l.movement_id).filter(Boolean);
    if(!movementIds.length || !window.sb) return;
    const {error}=await sb.from('inventory_movements').delete().in('id', movementIds);
    if(error) console.warn('لم يتم حذف بعض حركات الفاتورة:', error.message || error);
  }

  window.stockBatchDeleteV161 = async function(idx){
    const batch = (window.stockBatchesV148||[])[Number(idx)];
    if(!batch) return msgSafe('لم يتم العثور على الفاتورة','err');
    const inv = batch.invoice_no || batch.id || idx;
    if(!confirm('تأكيد حذف فاتورة المخزون رقم '+inv+'؟\nسيتم عكس كميات الأصناف المرتبطة بها.')) return;
    try{
      const btns=[...document.querySelectorAll(`button[onclick*="stockBatchDeleteV161('${idx}')"],button[onclick*="stockBatchDeleteV161(${idx})"]`)];
      btns.forEach(b=>b.disabled=true);
      // عكس الكميات أولًا.
      for(const line of (batch.lines||[])){
        const itemId = line.item_id || line.inventory_item_id;
        await updateItemQuantity(itemId, -n(line.quantity));
      }
      // حذف سجلات الفاتورة من الجداول إذا موجودة.
      if(window.sb && batch.id){
        const delItems=await sb.from('inventory_batch_items').delete().eq('batch_id', batch.id);
        if(delItems.error) throw delItems.error;
        const delBatch=await sb.from('inventory_batches').delete().eq('id', batch.id);
        if(delBatch.error) throw delBatch.error;
      }
      await deleteMovementsForBatch(batch);
      msgSafe('تم حذف فاتورة المخزون وعكس الكميات');
      if(typeof financeLoadAll === 'function') await financeLoadAll();
      if(typeof stockBatchLoadV148 === 'function') await stockBatchLoadV148(true);
      if(typeof renderStockBatchCardsV149 === 'function') renderStockBatchCardsV149();
    }catch(e){
      msgSafe(e.message || String(e),'err');
    }
  };

  // إعادة رسم شرائح فواتير المخزون بأزرار صحيحة تستخدم الفهرس الحقيقي، وليس ترتيب الفلتر.
  function getFilteredBatches(){
    const rows = window.stockBatchesV148 || [];
    const q = txt($('batchSearchV149')?.value).toLowerCase();
    const d = txt($('batchDateFilterV149')?.value);
    const m = txt($('batchMonthFilterV149')?.value);
    return rows.filter(r=>{
      const date=txt(r.batch_date||'');
      if(d && date !== d) return false;
      if(m && !date.startsWith(m)) return false;
      if(q){
        const hay=[r.invoice_no,r.id,r.supplier,r.batch_date].join(' ').toLowerCase();
        if(!hay.includes(q)) return false;
      }
      return true;
    });
  }

  window.renderStockBatchCardsV149 = function(){
    if(typeof ensureSmartInventoryUiV149 === 'function') try{ ensureSmartInventoryUiV149(); }catch(_){}
    const grid=$('stockBatchCardsGridV149'); if(!grid) return;
    const rows=getFilteredBatches();
    grid.innerHTML = rows.map(r=>{
      const idx=(window.stockBatchesV148||[]).indexOf(r);
      const count=(r.lines||[]).length;
      return `<div class="v149-invoice-card">
        <div class="v149-invoice-top"><div><div class="v149-invoice-no">${escHtml(r.invoice_no||r.id||'-')}</div><span class="v149-pill">${count} صنف</span></div><div class="v149-invoice-date">${escHtml(r.batch_date||'-')}</div></div>
        <div class="muted">المورد: <b>${escHtml(r.supplier||'-')}</b></div>
        <div class="v149-invoice-meta"><div class="v149-mini"><small>قبل الضريبة</small><b>${money(r.total_before_vat)} ر.س</b></div><div class="v149-mini"><small>VAT 15%</small><b>${money(r.total_vat)} ر.س</b></div><div class="v149-mini"><small>الإجمالي شامل</small><b>${money(r.total_with_vat)} ر.س</b></div><div class="v149-mini"><small>المرجع</small><b>${escHtml(r.invoice_no||r.id||'-')}</b></div></div>
        <div class="row-actions"><button type="button" onclick="viewStockBatchV149(${idx})">عرض الفاتورة</button><button type="button" class="light" onclick="stockBatchPrintSavedV148('${idx}')">طباعة</button><button type="button" class="light" onclick="stockBatchEditV152 && stockBatchEditV152('${idx}')">تعديل</button><button type="button" class="danger" onclick="stockBatchDeleteV161('${idx}')">حذف</button></div>
      </div>`;
    }).join('') || '<div class="v149-empty">لا توجد فواتير حسب الفلتر الحالي</div>';
  };

  function injectCss(){
    if($('v161Css')) return;
    const st=document.createElement('style'); st.id='v161Css';
    st.textContent=`
      .v161-hide-advanced-field{display:none!important}
      #stockBatchCardV148.v149-open{max-width:860px!important}
      #stockBatchCardV148 .v161-simple-invoice-entry{background:#fbfffd!important;border:1px solid #d6eae4!important;border-radius:18px!important}
      #stockBatchCardV148 .v161-simple-invoice-entry .split{grid-template-columns:1fr 1fr!important;gap:12px!important}
      #stockBatchCardV148 #batchLineImagePreviewV148{border:1px dashed #bddbd2;border-radius:14px;background:#f5fbf8;min-height:42px;padding:8px}
      @media(max-width:800px){#stockBatchCardV148 .v161-simple-invoice-entry .split{grid-template-columns:1fr!important}}
    `;
    document.head.appendChild(st);
  }

  const oldLoad = window.stockBatchLoadV148;
  if(oldLoad && !oldLoad.v161Wrapped){
    window.stockBatchLoadV148 = async function(){
      const out = await oldLoad.apply(this, arguments);
      setTimeout(()=>{ if(window.renderStockBatchCardsV149) renderStockBatchCardsV149(); },80);
      return out;
    };
    window.stockBatchLoadV148.v161Wrapped = true;
  }

  const oldShowTab = window.financeShowTab;
  if(oldShowTab && !oldShowTab.v161Wrapped){
    window.financeShowTab = function(){
      const out = oldShowTab.apply(this, arguments);
      setTimeout(()=>{ injectCss(); cleanInvoiceEntryForm(); bindStrictLookup(); if(window.renderStockBatchCardsV149) renderStockBatchCardsV149(); },120);
      return out;
    };
    window.financeShowTab.v161Wrapped = true;
  }

  const oldRenderAll = window.financeRenderAll;
  if(oldRenderAll && !oldRenderAll.v161Wrapped){
    window.financeRenderAll = function(){
      const out = oldRenderAll.apply(this, arguments);
      setTimeout(()=>{ injectCss(); cleanInvoiceEntryForm(); bindStrictLookup(); if(window.renderStockBatchCardsV149) renderStockBatchCardsV149(); },160);
      return out;
    };
    window.financeRenderAll.v161Wrapped = true;
  }

  window.addEventListener('load',()=>setTimeout(()=>{
    injectCss(); cleanInvoiceEntryForm(); bindStrictLookup(); lookupProductV161();
    if(window.renderStockBatchCardsV149) renderStockBatchCardsV149();
  },1800));
})();

/* ===== V162: Warehouse visibility, invoice delete/actions, client reports stability ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const txt = v => String(v ?? '').trim();
  const n = v => { const x=parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x)?x:0; };
  const esc = v => String(v ?? '').replace(/[&<>"']/g, m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const money = v => n(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  const currentRole = () => { try{ return (typeof session==='function' ? (session()?.role||'') : '') || JSON.parse(localStorage.getItem('tasneef_session')||'{}')?.role || ''; }catch(_){ return ''; } };
  const isWarehouse = () => currentRole()==='warehouse_manager' || /مدير مخازن/.test(txt(document.body?.innerText||''));

  function batchRows(){ return window.stockBatchesV148 || []; }
  function filteredBatches(){
    const rows=batchRows();
    const q=txt($('batchSearchV149')?.value).toLowerCase();
    const d=txt($('batchDateFilterV149')?.value);
    const m=txt($('batchMonthFilterV149')?.value);
    return rows.filter(r=>{
      const date=txt(r.batch_date||'');
      if(d && date!==d) return false;
      if(m && !date.startsWith(m)) return false;
      if(q){ const hay=[r.invoice_no,r.id,r.supplier,r.batch_date].join(' ').toLowerCase(); if(!hay.includes(q)) return false; }
      return true;
    });
  }

  function invoiceCard(r,idx){
    const count=(r.lines||[]).length;
    if(isWarehouse()){
      return `<div class="v149-invoice-card v162-warehouse-hidden-invoice">
        <div class="v149-invoice-top"><div><div class="v149-invoice-no">${esc(r.invoice_no||r.id||'-')}</div><span class="v149-pill">${count} صنف</span></div><div class="v149-invoice-date">${esc(r.batch_date||'-')}</div></div>
        <div class="muted">المورد: <b>${esc(r.supplier||'-')}</b></div>
        <div class="v149-invoice-meta"><div class="v149-mini"><small>عدد الأصناف</small><b>${count}</b></div><div class="v149-mini"><small>المرجع</small><b>${esc(r.invoice_no||r.id||'-')}</b></div></div>
        <div class="row-actions"><button type="button" onclick="viewStockBatchV149(${idx})">عرض</button></div>
      </div>`;
    }
    return `<div class="v149-invoice-card">
      <div class="v149-invoice-top"><div><div class="v149-invoice-no">${esc(r.invoice_no||r.id||'-')}</div><span class="v149-pill">${count} صنف</span></div><div class="v149-invoice-date">${esc(r.batch_date||'-')}</div></div>
      <div class="muted">المورد: <b>${esc(r.supplier||'-')}</b></div>
      <div class="v149-invoice-meta"><div class="v149-mini"><small>قبل الضريبة</small><b>${money(r.total_before_vat)} ر.س</b></div><div class="v149-mini"><small>VAT 15%</small><b>${money(r.total_vat)} ر.س</b></div><div class="v149-mini"><small>الإجمالي شامل</small><b>${money(r.total_with_vat)} ر.س</b></div><div class="v149-mini"><small>المرجع</small><b>${esc(r.invoice_no||r.id||'-')}</b></div></div>
      <div class="row-actions v162-invoice-actions"><button type="button" onclick="viewStockBatchV149(${idx})">عرض الفاتورة</button><button type="button" class="light" onclick="stockBatchPrintSavedV148('${idx}')">طباعة</button><button type="button" class="light" onclick="stockBatchEditV152 && stockBatchEditV152('${idx}')">تعديل</button><button type="button" class="danger" onclick="stockBatchDeleteV162('${idx}')">حذف</button></div>
    </div>`;
  }

  window.renderStockBatchCardsV149 = function(){
    try{ if(typeof ensureSmartInventoryUiV149==='function') ensureSmartInventoryUiV149(); }catch(_){ }
    const grid=$('stockBatchCardsGridV149'); if(!grid) return;
    const rows=filteredBatches();
    grid.innerHTML=rows.map(r=>invoiceCard(r,batchRows().indexOf(r))).join('') || '<div class="v149-empty">لا توجد فواتير حسب الفلتر الحالي</div>';
    cleanupDuplicateInvoiceActions();
    applyWarehouseRestrictions();
  };
  window.renderStockBatchCardsV149.v152Wrapped = true;
  window.renderStockBatchCardsV149.v161Wrapped = true;

  function cleanupDuplicateInvoiceActions(){
    document.querySelectorAll('#stockBatchCardsGridV149 .v149-invoice-card').forEach(card=>{
      const actions=card.querySelectorAll('.row-actions');
      if(actions.length>1){ actions.forEach((a,i)=>{ if(i>0) a.remove(); }); }
      const act=card.querySelector('.row-actions'); if(!act) return;
      ['تعديل','حذف','طباعة','عرض الفاتورة'].forEach(label=>{
        const btns=[...act.querySelectorAll('button')].filter(b=>txt(b.textContent)===label);
        btns.forEach((b,i)=>{ if(i>0) b.remove(); });
      });
    });
  }

  function movementMatchesBatch(m,batch){
    const inv=txt(batch.invoice_no||batch.id||'');
    if(!inv) return false;
    if(txt(m.notes).includes('batch:'+inv)) return true;
    if(txt(m.reason).includes(inv) && m.movement_type==='in') return true;
    return false;
  }
  async function deleteBatchMovements(batch){
    if(!window.sb) return;
    let ids=(batch.lines||[]).map(l=>l.movement_id).filter(Boolean);
    if(!ids.length){
      ids=(window.data?.inventoryMovements||[]).filter(m=>movementMatchesBatch(m,batch)).map(m=>m.id).filter(Boolean);
    }
    if(ids.length){ const {error}=await sb.from('inventory_movements').delete().in('id',ids); if(error) throw error; }
  }
  async function reverseBatchQuantities(batch){
    if(!window.sb) return;
    for(const line of (batch.lines||[])){
      const code=txt(line.product_code||line.barcode||'');
      let item=(window.data?.inventoryItems||[]).find(i=>String(i.id)===String(line.item_id||line.inventory_item_id));
      if(!item && code){ item=(window.data?.inventoryItems||[]).find(i=>[i.product_code,i.serial_number,i.barcode,i.supplier_barcode].map(txt).includes(code)); }
      if(!item?.id) continue;
      const next=Math.max(0,n(item.quantity)-n(line.quantity));
      const {error}=await sb.from('inventory_items').update({quantity:next}).eq('id',item.id);
      if(error) throw error;
      item.quantity=next;
    }
  }
  window.stockBatchDeleteV162 = async function(idx){
    const batch=batchRows()[Number(idx)];
    if(!batch) return (typeof msg==='function'?msg('لم يتم العثور على الفاتورة','err'):alert('لم يتم العثور على الفاتورة'));
    const inv=batch.invoice_no||batch.id||idx;
    if(!confirm('تأكيد حذف فاتورة المخزون رقم '+inv+'؟\nسيتم عكس الكميات وحذف الحركات المرتبطة.')) return;
    try{
      document.querySelectorAll(`button[onclick*="stockBatchDeleteV162('${idx}')"],button[onclick*="stockBatchDeleteV162(${idx})"]`).forEach(b=>b.disabled=true);
      await reverseBatchQuantities(batch);
      if(window.sb && batch.id){
        // نحاول حذف بنود الفاتورة أولًا. إذا كان السجل مبنيًا من الحركات فقط، نتجاوز خطأ عدم وجود الجدول.
        try{ const r=await sb.from('inventory_batch_items').delete().eq('batch_id',batch.id); if(r.error && !/does not exist|relation/.test(r.error.message||'')) throw r.error; }catch(e){ if(!/does not exist|relation/.test(e.message||'')) throw e; }
        try{ const r=await sb.from('inventory_batches').delete().eq('id',batch.id); if(r.error && !/does not exist|relation/.test(r.error.message||'')) throw r.error; }catch(e){ if(!/does not exist|relation/.test(e.message||'')) throw e; }
      }
      await deleteBatchMovements(batch);
      if(typeof msg==='function') msg('تم حذف الفاتورة وعكس الكميات');
      window.stockBatchesV148=batchRows().filter((_,i)=>i!==Number(idx));
      if(typeof financeLoadAll==='function') await financeLoadAll();
      if(typeof stockBatchLoadV148==='function') await stockBatchLoadV148(true);
      if(typeof renderStockBatchCardsV149==='function') renderStockBatchCardsV149();
      if(typeof inventoryRenderItems==='function') inventoryRenderItems();
    }catch(e){
      console.error('delete invoice failed',e);
      if(typeof msg==='function') msg(e.message||String(e),'err'); else alert(e.message||String(e));
    }
  };
  // Compatibility: all delete buttons should use the robust deleter.
  window.stockBatchDeleteV161 = window.stockBatchDeleteV162;
  window.stockBatchDeleteV152 = window.stockBatchDeleteV162;

  function applyWarehouseRestrictions(){
    if(!isWarehouse()) return;
    document.body.classList.add('warehouse-manager-view-v162');
    const invTab=$('financeTabInventory'); if(invTab) invTab.classList.add('hidden');
    const catTab=$('financeTabCatalog'); if(catTab) catTab.classList.remove('hidden');
    const btns=[...document.querySelectorAll('#financeDashboard .finance-tab')];
    btns.forEach(btn=>{ const t=txt(btn.textContent); btn.style.display=(t==='طلبات الصرف'||t==='الأصناف')?'':'none'; });
    document.querySelectorAll('#stockBatchCardsGridV149,.v149-invoice-meta,#smartStockInvoicesV149,#stockBatchCardV148').forEach(el=>{ el.style.display='none'; });
    // Hide any price text in catalog/product modal for warehouse manager.
    document.querySelectorAll('.warehouse-manager-view-v162 .v151-item-card, .warehouse-manager-view-v162 #financeTabCatalog').forEach(root=>{
      root.querySelectorAll('*').forEach(el=>{ const t=txt(el.textContent); if(/سعر|ر\.س|VAT|ضريبة|الإجمالي|قبل الضريبة|شامل/.test(t)) el.classList.add('v162-price-hidden'); });
    });
  }

  // Make client report edit stable: fetch fresh report/services instead of relying on stale local cache.
  window.editPremiumReport = async function(id){
    try{
      const local=(window.data?.clientReports||[]).find(x=>String(x.id)===String(id));
      let r=local, services=null;
      if(window.sb){
        const [rr,ss]=await Promise.all([
          sb.from('client_reports').select('*').eq('id',id).maybeSingle(),
          sb.from('client_report_services').select('*').eq('report_id',id).order('sort_order',{ascending:true})
        ]);
        if(rr.error) throw rr.error;
        if(ss.error) throw ss.error;
        r=rr.data||local; services=ss.data||[];
      }
      if(!r) throw new Error('لم يتم العثور على التقرير');
      const set=(k,v)=>{ const el=$(k); if(el) el.value=v??''; };
      set('premiumReportId',r.id); set('premiumReportProject',r.project_id||''); set('premiumReportDate',r.report_date||((typeof today==='function')?today():new Date().toISOString().slice(0,10))); set('premiumReportTitle',r.title||''); set('premiumReportType',r.report_type||'تقرير خدمات'); set('premiumChairmanName',r.chairman_name||''); set('premiumChairmanPhone',r.chairman_phone||''); set('premiumSummary',r.executive_summary||((typeof defaultReportSummary==='function')?defaultReportSummary():''));
      const rows=services || (typeof getReportServices==='function'?getReportServices(id):[]);
      window.premiumServicesState=rows.map(s=>({service_type:s.service_type||s.title||'خدمة',title:s.title||s.service_type||'خدمة',service_description:s.service_description||'',scope_work:s.scope_work||'',notes:s.notes||'',before_images:Array.isArray(s.before_images)?s.before_images:[],during_images:Array.isArray(s.during_images)?s.during_images:[],after_images:Array.isArray(s.after_images)?s.after_images:[],source:s.source||'admin'}));
      if(!window.premiumServicesState.length && typeof makeService==='function') window.premiumServicesState=[makeService()];
      if(typeof renderPremiumServicesEditor==='function') renderPremiumServicesEditor();
      const title=$('premiumReportFormTitle'); if(title) title.textContent='تعديل تقرير';
      $('premiumReportFormCard')?.scrollIntoView({behavior:'smooth',block:'start'});
    }catch(e){ console.error(e); if(typeof msg==='function') msg(e.message||String(e),'err'); }
  };

  function injectCssV162(){
    if($('v162Css')) return;
    const st=document.createElement('style'); st.id='v162Css'; st.textContent=`
      .warehouse-manager-view-v162 #smartStockInvoicesV149,.warehouse-manager-view-v162 #stockBatchCardV148,.warehouse-manager-view-v162 #stockBatchCardsGridV149,.warehouse-manager-view-v162 .v149-invoice-meta{display:none!important}
      .warehouse-manager-view-v162 .v162-price-hidden{display:none!important}
      .v162-invoice-actions{display:flex!important;gap:8px!important;flex-wrap:wrap!important;align-items:center!important}.v162-invoice-actions button{min-width:76px!important}
    `; document.head.appendChild(st);
  }
  const oldShow=window.financeShowTab;
  if(oldShow && !oldShow.v162Wrapped){ window.financeShowTab=function(){ const out=oldShow.apply(this,arguments); setTimeout(()=>{injectCssV162(); applyWarehouseRestrictions(); cleanupDuplicateInvoiceActions(); if(typeof renderStockBatchCardsV149==='function') renderStockBatchCardsV149();},90); return out; }; window.financeShowTab.v162Wrapped=true; }
  const oldRender=window.financeRenderAll;
  if(oldRender && !oldRender.v162Wrapped){ window.financeRenderAll=function(){ const out=oldRender.apply(this,arguments); setTimeout(()=>{injectCssV162(); applyWarehouseRestrictions(); cleanupDuplicateInvoiceActions();},120); return out; }; window.financeRenderAll.v162Wrapped=true; }
  window.addEventListener('load',()=>setTimeout(()=>{injectCssV162(); applyWarehouseRestrictions(); cleanupDuplicateInvoiceActions(); if(typeof renderStockBatchCardsV149==='function') renderStockBatchCardsV149();},1400));
})();

/* ===== V163: Warehouse catalog visibility + real stock invoice delete persistence ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const s = v => String(v ?? '').trim();
  const n = v => { const x = parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const q = v => n(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const esc = v => String(v ?? '').replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const role = () => { try { return (typeof session === 'function' ? (session()||{}).role : '') || (JSON.parse(localStorage.getItem('tasneef_session')||'{}').role||''); } catch(_) { return ''; } };
  const isWarehouse = () => role() === 'warehouse_manager';
  const codeOf = it => s(it?.product_code || it?.barcode || it?.supplier_barcode || it?.serial_number || it?.company_code || '');
  const companyCodeOf = it => s(it?.company_code || it?.internal_code || it?.serial_number || codeOf(it));
  const itemName = id => ((window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id))||{}).name || '';

  function itemStats(it){
    const id=s(it?.id), code=codeOf(it), name=s(it?.name);
    let inQty=0,outQty=0,retQty=0,consumeDirect=0;
    (window.data?.inventoryMovements||[]).forEach(m=>{
      const match = s(m.item_id)===id || (code && [m.product_code,m.barcode,m.supplier_barcode].map(s).includes(code)) || (name && s(m.item_name)===name);
      if(!match) return;
      const qty=n(m.quantity), type=s(m.movement_type);
      if(type==='in') inQty+=qty;
      else if(type==='out') outQty+=qty;
      else if(type==='return') retQty+=qty;
      else if(type==='consume') consumeDirect+=qty;
    });
    (window.data?.inventoryRequests||[]).filter(r=>['approved','issued','closed'].includes(s(r.status))).forEach(r=>{
      const lines=Array.isArray(r.request_items)?r.request_items:(Array.isArray(r.items)?r.items:[]);
      if(lines.length){
        lines.forEach(l=>{ if(s(l.item_id)===id || (code && s(l.product_code)===code) || (name && s(l.item_name)===name)) outQty+=n(l.quantity); });
      }else if(s(r.item_id)===id || (name && s(r.item_name)===name)) outQty+=n(r.quantity);
    });
    const consumed = Math.max(0, (outQty - retQty)) + consumeDirect;
    return {inQty,outQty,retQty,consumed,remaining:n(it?.quantity)};
  }

  function renderWarehouseCatalogV163(){
    if(!isWarehouse()) return false;
    const body=$('inventoryItemsBody');
    if(!body) return false;
    const search=s($('inventorySearch')?.value || $('financeSearch')?.value).toLowerCase();
    const supplier=s($('inventorySupplierFilter')?.value || $('inventoryReportSupplier')?.value);
    const cat=s($('inventoryCategoryFilter')?.value);
    let rows=[...(window.data?.inventoryItems||[])];
    if(search) rows=rows.filter(it=>[it.name,codeOf(it),companyCodeOf(it),it.supplier,it.category,it.unit].join(' ').toLowerCase().includes(search));
    if(supplier) rows=rows.filter(it=>s(it.supplier)===supplier);
    if(cat) rows=rows.filter(it=>s(it.category)===cat);
    body.innerHTML = `<tr><td colspan="20"><div class="v163-warehouse-catalog-grid">${rows.map(it=>{
      const st=itemStats(it);
      const img=it.image_url ? `<img src="${esc(it.image_url)}" onerror="this.style.display='none'">` : '<span>لا صورة</span>';
      const low=n(it.quantity)<=n(it.min_quantity||it.reorder_level||0);
      return `<article class="v163-warehouse-item-card">
        <div class="v163-item-head"><div class="v163-img">${img}</div><div><h3>${esc(it.name||'-')}</h3><small>كود المنتج: ${esc(codeOf(it)||'-')}</small><br><small>كود الشركة: ${esc(companyCodeOf(it)||'-')}</small></div><span class="${low?'v163-low':'v163-ok'}">${low?'منخفض':'متوفر'}</span></div>
        <div class="v163-stats"><div><small>الكمية</small><b>${q(st.remaining)} ${esc(it.unit||'')}</b></div><div><small>دخل</small><b>${q(st.inQty)}</b></div><div><small>خرج</small><b>${q(st.outQty)}</b></div><div><small>مرتجع</small><b>${q(st.retQty)}</b></div><div><small>مستهلك</small><b>${q(st.consumed)}</b></div><div><small>حد الطلب</small><b>${q(it.min_quantity||it.reorder_level||0)}</b></div></div>
        <div class="row-actions"><button type="button" onclick="inventoryOpenItemSmart&&inventoryOpenItemSmart('${esc(it.id)}')">عرض</button></div>
      </article>`;
    }).join('') || '<div class="muted">لا توجد أصناف</div>'}</div></td></tr>`;
    return true;
  }

  function ensureWarehouseViewV163(){
    if(!isWarehouse()) return;
    document.body.classList.add('warehouse-manager-view-v163');
    // Show only requests + catalog tabs; do not remove catalog contents.
    document.querySelectorAll('#financeDashboard .finance-tab').forEach(btn=>{
      const t=s(btn.textContent);
      btn.style.display = (t==='طلبات الصرف' || t==='الأصناف') ? '' : 'none';
    });
    ['financeTabOverview','financeTabExpenses','financeTabInventory','financeTabMovements','financeTabReports','financeTabCostCenters'].forEach(id=>$(id)?.classList.add('hidden'));
    $('financeTabCatalog')?.classList.remove('hidden');
    renderWarehouseCatalogV163();
  }

  function movementBelongsToInvoice(m,batch){
    const inv=s(batch.invoice_no||batch.id||'');
    if(!inv) return false;
    return s(m.notes).includes('batch:'+inv) || s(m.reason).includes(inv) || s(m.reference).includes(inv) || s(m.batch_no).includes(inv) || s(m.invoice_no).includes(inv);
  }

  async function deleteByInvoiceNo(table, invoiceNo){
    if(!window.sb || !invoiceNo) return;
    // Try common columns; ignore missing-column errors.
    for(const col of ['invoice_no','reference','batch_no','notes']){
      try{
        let qy=sb.from(table).delete();
        if(col==='notes') qy=qy.ilike(col, `%${invoiceNo}%`); else qy=qy.eq(col, invoiceNo);
        const r=await qy;
        if(r.error && !/column|schema cache|does not exist|relation/i.test(r.error.message||'')) throw r.error;
      }catch(e){ if(!/column|schema cache|does not exist|relation/i.test(e.message||'')) throw e; }
    }
  }

  async function reverseBatchQuantitiesV163(batch){
    for(const line of (batch.lines||[])){
      const qty=n(line.quantity);
      if(!qty) continue;
      const code=s(line.product_code||line.barcode||line.supplier_barcode||'');
      let item=(window.data?.inventoryItems||[]).find(i=>String(i.id)===String(line.item_id||line.inventory_item_id));
      if(!item && code) item=(window.data?.inventoryItems||[]).find(i=>[i.product_code,i.barcode,i.supplier_barcode,i.serial_number,i.company_code].map(s).includes(code));
      if(!item?.id) continue;
      const next=Math.max(0,n(item.quantity)-qty);
      if(window.sb){
        const r=await sb.from('inventory_items').update({quantity:next}).eq('id',item.id);
        if(r.error) throw r.error;
      }
      item.quantity=next;
    }
  }

  window.stockBatchDeleteV163 = async function(idx){
    const list=window.stockBatchesV148||[];
    const batch=list[Number(idx)];
    if(!batch) return (typeof msg==='function'?msg('لم يتم العثور على الفاتورة','err'):alert('لم يتم العثور على الفاتورة'));
    const inv=s(batch.invoice_no||batch.id||idx);
    if(!confirm('تأكيد حذف فاتورة المخزون رقم '+inv+'؟\nسيتم عكس الكميات وحذف الفاتورة من قاعدة البيانات.')) return;
    try{
      document.querySelectorAll(`button[onclick*="stockBatchDeleteV"],button[onclick*="stockBatchDeleteV163"]`).forEach(b=>{ if((b.getAttribute('onclick')||'').includes(String(idx))) b.disabled=true; });
      await reverseBatchQuantitiesV163(batch);
      if(window.sb){
        // Delete batch item rows by batch id and invoice no.
        if(batch.id){
          try{ const r=await sb.from('inventory_batch_items').delete().eq('batch_id',batch.id); if(r.error && !/does not exist|relation/i.test(r.error.message||'')) throw r.error; }catch(e){ if(!/does not exist|relation/i.test(e.message||'')) throw e; }
        }
        await deleteByInvoiceNo('inventory_batch_items', inv);
        // Delete movements first and also by IDs known in lines.
        const movementIds=(batch.lines||[]).map(l=>l.movement_id).filter(Boolean);
        if(movementIds.length){ try{ const r=await sb.from('inventory_movements').delete().in('id',movementIds); if(r.error) throw r.error; }catch(e){ console.warn(e); } }
        await deleteByInvoiceNo('inventory_movements', inv);
        // Delete direct batch row by id and invoice number.
        if(batch.id){ try{ const r=await sb.from('inventory_batches').delete().eq('id',batch.id); if(r.error && !/does not exist|relation/i.test(r.error.message||'')) throw r.error; }catch(e){ if(!/does not exist|relation/i.test(e.message||'')) throw e; } }
        await deleteByInvoiceNo('inventory_batches', inv);
      }
      // Local delete immediately and persist hidden invoice fallback so it will not reappear from old movement fallback.
      const hidden=JSON.parse(localStorage.getItem('deleted_stock_invoices_v163')||'[]');
      if(!hidden.includes(inv)){ hidden.push(inv); localStorage.setItem('deleted_stock_invoices_v163', JSON.stringify(hidden)); }
      window.stockBatchesV148=list.filter((_,i)=>i!==Number(idx)).filter(b=>!hidden.includes(s(b.invoice_no||b.id||'')));
      if(typeof msg==='function') msg('تم حذف الفاتورة وعكس الكميات');
      if(typeof financeLoadAll==='function') await financeLoadAll();
      if(typeof stockBatchLoadV148==='function') await stockBatchLoadV148(true);
      window.stockBatchesV148=(window.stockBatchesV148||[]).filter(b=>!hidden.includes(s(b.invoice_no||b.id||'')));
      if(typeof renderStockBatchCardsV149==='function') renderStockBatchCardsV149();
      if(typeof inventoryRenderItems==='function') inventoryRenderItems();
    }catch(e){ console.error(e); if(typeof msg==='function') msg(e.message||String(e),'err'); else alert(e.message||String(e)); }
  };
  window.stockBatchDeleteV162=window.stockBatchDeleteV163;
  window.stockBatchDeleteV161=window.stockBatchDeleteV163;
  window.stockBatchDeleteV152=window.stockBatchDeleteV163;

  // Wrap batch loader to filter any locally deleted invoices that were built from older movement fallback.
  const oldLoad=window.stockBatchLoadV148;
  if(oldLoad && !oldLoad.v163Wrapped){
    window.stockBatchLoadV148=async function(){
      const out=await oldLoad.apply(this,arguments);
      const hidden=JSON.parse(localStorage.getItem('deleted_stock_invoices_v163')||'[]');
      if(hidden.length) window.stockBatchesV148=(window.stockBatchesV148||[]).filter(b=>!hidden.includes(s(b.invoice_no||b.id||'')));
      if(typeof renderStockBatchCardsV149==='function') renderStockBatchCardsV149();
      return out;
    };
    window.stockBatchLoadV148.v163Wrapped=true;
  }

  // Override invoice cards once more so delete buttons use V163 and warehouse manager never sees invoice prices.
  const oldRenderCards=window.renderStockBatchCardsV149;
  window.renderStockBatchCardsV149=function(){
    const grid=$('stockBatchCardsGridV149');
    if(!grid){ if(oldRenderCards) return oldRenderCards.apply(this,arguments); return; }
    const all=window.stockBatchesV148||[];
    const hidden=JSON.parse(localStorage.getItem('deleted_stock_invoices_v163')||'[]');
    const qf=s($('batchSearchV149')?.value).toLowerCase(), df=s($('batchDateFilterV149')?.value), mf=s($('batchMonthFilterV149')?.value);
    const rows=all.filter(r=>{
      if(hidden.includes(s(r.invoice_no||r.id||''))) return false;
      const d=s(r.batch_date||'');
      if(df && d!==df) return false;
      if(mf && !d.startsWith(mf)) return false;
      if(qf && ![r.invoice_no,r.id,r.supplier,r.batch_date].join(' ').toLowerCase().includes(qf)) return false;
      return true;
    });
    grid.innerHTML=rows.map(r=>{
      const idx=all.indexOf(r), count=(r.lines||[]).length;
      if(isWarehouse()) return '';
      return `<div class="v149-invoice-card"><div class="v149-invoice-top"><div><div class="v149-invoice-no">${esc(r.invoice_no||r.id||'-')}</div><span class="v149-pill">${count} صنف</span></div><div class="v149-invoice-date">${esc(r.batch_date||'-')}</div></div><div class="muted">المورد: <b>${esc(r.supplier||'-')}</b></div><div class="v149-invoice-meta"><div class="v149-mini"><small>قبل الضريبة</small><b>${n(r.total_before_vat).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</b></div><div class="v149-mini"><small>VAT 15%</small><b>${n(r.total_vat).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</b></div><div class="v149-mini"><small>الإجمالي شامل</small><b>${n(r.total_with_vat).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</b></div><div class="v149-mini"><small>المرجع</small><b>${esc(r.invoice_no||r.id||'-')}</b></div></div><div class="row-actions v163-invoice-actions"><button type="button" onclick="viewStockBatchV149(${idx})">عرض الفاتورة</button><button type="button" class="light" onclick="stockBatchPrintSavedV148('${idx}')">طباعة</button><button type="button" class="light" onclick="stockBatchEditV152&&stockBatchEditV152('${idx}')">تعديل</button><button type="button" class="danger" onclick="stockBatchDeleteV163('${idx}')">حذف</button></div></div>`;
    }).join('') || '<div class="v149-empty">لا توجد فواتير حسب الفلتر الحالي</div>';
  };

  // Wrap inventory renderer last: warehouse manager must see catalog items, not blank catalog.
  const oldItems=window.inventoryRenderItems;
  if(oldItems && !oldItems.v163Wrapped){
    window.inventoryRenderItems=function(){
      if(isWarehouse()) return renderWarehouseCatalogV163();
      return oldItems.apply(this,arguments);
    };
    window.inventoryRenderItems.v163Wrapped=true;
  }

  function injectCss(){
    if($('v163Css')) return;
    const st=document.createElement('style'); st.id='v163Css'; st.textContent=`
      .warehouse-manager-view-v162 .v162-price-hidden{display:revert!important}
      .warehouse-manager-view-v163 #financeTabCatalog{display:block!important}
      .warehouse-manager-view-v163 #smartStockInvoicesV149,.warehouse-manager-view-v163 #stockBatchCardV148,.warehouse-manager-view-v163 #stockBatchCardsGridV149{display:none!important}
      .v163-warehouse-catalog-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:14px;padding:8px;direction:rtl}
      .v163-warehouse-item-card{border:1px solid #cfe3dd;border-radius:18px;padding:14px;background:#fff;box-shadow:0 10px 22px rgba(0,45,31,.06)}
      .v163-item-head{display:flex;align-items:center;justify-content:space-between;gap:12px}.v163-item-head h3{margin:0;color:#003f31}.v163-img{width:66px;height:66px;border:1px solid #dbeee8;border-radius:14px;background:#f7fbf9;display:grid;place-items:center;overflow:hidden;flex:0 0 66px}.v163-img img{max-width:100%;max-height:100%;object-fit:contain}.v163-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.v163-stats div{background:#f1faf6;border:1px solid #d9eee6;border-radius:12px;padding:8px;text-align:center}.v163-stats small{display:block;color:#5b756d}.v163-stats b{color:#004c3a}.v163-ok,.v163-low{border-radius:999px;padding:5px 8px;font-size:12px}.v163-ok{background:#e5f8ee;color:#067647}.v163-low{background:#ffe6e6;color:#b42318}.v163-invoice-actions{display:flex!important;gap:8px!important;flex-wrap:wrap!important}.v163-invoice-actions button{min-width:78px!important}
      @media(max-width:700px){.v163-stats{grid-template-columns:repeat(2,1fr)}}
    `; document.head.appendChild(st);
  }

  const oldShow=window.financeShowTab;
  if(oldShow && !oldShow.v163Wrapped){
    window.financeShowTab=function(tab,btn){
      const out=oldShow.apply(this,arguments);
      setTimeout(()=>{ injectCss(); ensureWarehouseViewV163(); if(tab==='catalog'&&isWarehouse()) renderWarehouseCatalogV163(); },120);
      return out;
    };
    window.financeShowTab.v163Wrapped=true;
  }
  const oldRenderAll=window.financeRenderAll;
  if(oldRenderAll && !oldRenderAll.v163Wrapped){
    window.financeRenderAll=function(){
      const out=oldRenderAll.apply(this,arguments);
      setTimeout(()=>{ injectCss(); ensureWarehouseViewV163(); },180);
      return out;
    };
    window.financeRenderAll.v163Wrapped=true;
  }
  window.addEventListener('load',()=>setTimeout(()=>{ injectCss(); ensureWarehouseViewV163(); if(typeof renderStockBatchCardsV149==='function') renderStockBatchCardsV149(); },1800));
})();

/* ===== V164: final cache bump + warehouse catalog visible + persistent invoice delete ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const N = v => { const x = parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const E = v => S(v).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const Q = v => N(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const role = () => { try { return (typeof session==='function' ? (session()||{}).role : '') || JSON.parse(localStorage.getItem('tasneef_session')||'{}').role || ''; } catch(_) { return ''; } };
  const isWarehouse = () => role()==='warehouse_manager';
  const codeOf = it => S(it?.product_code || it?.barcode || it?.supplier_barcode || it?.serial_number || it?.company_code || '');
  const companyCodeOf = it => S(it?.company_code || it?.internal_code || it?.serial_number || codeOf(it));
  const hiddenKey = 'deleted_stock_invoices_v164';
  function hiddenList(){ try{return JSON.parse(localStorage.getItem(hiddenKey)||'[]').map(S);}catch(_){return [];} }
  function addHiddenInvoice(batch){
    const hidden = new Set(hiddenList());
    [batch?.id,batch?.invoice_no,batch?.reference].map(S).filter(Boolean).forEach(x=>hidden.add(x));
    localStorage.setItem(hiddenKey, JSON.stringify([...hidden]));
  }
  function isHiddenBatch(batch){
    const h = hiddenList();
    return [batch?.id,batch?.invoice_no,batch?.reference].map(S).some(x=>x && h.includes(x));
  }
  function itemStats(it){
    const id=S(it?.id), code=codeOf(it), name=S(it?.name);
    let inQty=0,outQty=0,retQty=0,consumeQty=0;
    (window.data?.inventoryMovements||[]).forEach(m=>{
      const match = S(m.item_id)===id || (code && [m.product_code,m.barcode,m.supplier_barcode,m.serial_number].map(S).includes(code)) || (name && S(m.item_name)===name);
      if(!match) return;
      const qty=N(m.quantity); const t=S(m.movement_type);
      if(t==='in') inQty+=qty;
      else if(t==='out') outQty+=qty;
      else if(t==='return') retQty+=qty;
      else if(t==='consume') consumeQty+=qty;
    });
    (window.data?.inventoryRequests||[]).filter(r=>['approved','issued','closed'].includes(S(r.status))).forEach(r=>{
      const lines = Array.isArray(r.request_items)?r.request_items:(Array.isArray(r.items)?r.items:[]);
      if(lines.length){
        lines.forEach(l=>{ if(S(l.item_id)===id || (code && S(l.product_code)===code) || (name && S(l.item_name)===name)) outQty+=N(l.quantity); });
      } else if(S(r.item_id)===id || (name && S(r.item_name)===name)) outQty+=N(r.quantity);
    });
    return {inQty,outQty,retQty,consumed:Math.max(0,outQty-retQty)+consumeQty,remaining:N(it?.quantity)};
  }
  function ensureCatalogTab(){
    const tabsBox = document.querySelector('#financeDashboard .finance-tabs') || document.querySelector('.finance-tabs');
    const invTab = $('financeTabInventory');
    if(!tabsBox || !invTab) return;
    if(!document.querySelector('.finance-tab[data-v164-catalog="1"]') && ![...tabsBox.querySelectorAll('.finance-tab')].some(b=>S(b.textContent)==='الأصناف')){
      const btn=document.createElement('button'); btn.type='button'; btn.className='light finance-tab'; btn.dataset.v164Catalog='1'; btn.textContent='الأصناف'; btn.onclick=function(){ financeShowTab('catalog',this); };
      const inventoryBtn=[...tabsBox.querySelectorAll('.finance-tab')].find(b=>S(b.textContent)==='المخزون');
      if(inventoryBtn && inventoryBtn.nextSibling) tabsBox.insertBefore(btn, inventoryBtn.nextSibling); else tabsBox.appendChild(btn);
    }
    let catPage=$('financeTabCatalog');
    if(!catPage){ catPage=document.createElement('div'); catPage.id='financeTabCatalog'; catPage.className='finance-tab-page hidden'; invTab.parentNode.insertBefore(catPage,invTab.nextSibling); }
    const itemCard=document.querySelector('#financeTabInventory .card:has(#inventoryItemsBody)') || document.querySelector('.card:has(#inventoryItemsBody)');
    if(itemCard && itemCard.parentElement!==catPage){ catPage.appendChild(itemCard); const h=itemCard.querySelector('h2'); if(h) h.textContent='قائمة الأصناف'; }
  }
  function renderWarehouseCatalog(){
    const body=$('inventoryItemsBody'); if(!body) return false;
    const search=S($('inventorySearch')?.value || $('financeSearch')?.value).toLowerCase();
    const supplier=S($('inventorySupplierFilter')?.value || $('inventoryReportSupplier')?.value);
    const cat=S($('inventoryCategoryFilter')?.value);
    let rows=[...(window.data?.inventoryItems||[])];
    if(search) rows=rows.filter(it=>[it.name,codeOf(it),companyCodeOf(it),it.supplier,it.category,it.unit].join(' ').toLowerCase().includes(search));
    if(supplier) rows=rows.filter(it=>S(it.supplier)===supplier);
    if(cat) rows=rows.filter(it=>S(it.category)===cat);
    body.innerHTML=`<tr><td colspan="20"><div class="v164-warehouse-catalog-grid">${rows.map(it=>{
      const st=itemStats(it); const img=it.image_url?`<img src="${E(it.image_url)}" onerror="this.style.display='none'">`:'<span>لا صورة</span>'; const low=N(it.quantity)<=N(it.min_quantity||it.reorder_level||0);
      return `<article class="v164-warehouse-item-card"><div class="v164-item-head"><div class="v164-img">${img}</div><div><h3>${E(it.name||'-')}</h3><small>كود المنتج: ${E(codeOf(it)||'-')}</small><br><small>كود الشركة: ${E(companyCodeOf(it)||'-')}</small></div><span class="${low?'v164-low':'v164-ok'}">${low?'منخفض':'متوفر'}</span></div><div class="v164-stats"><div><small>الكمية</small><b>${Q(st.remaining)} ${E(it.unit||'')}</b></div><div><small>دخل</small><b>${Q(st.inQty)}</b></div><div><small>خرج</small><b>${Q(st.outQty)}</b></div><div><small>مرتجع</small><b>${Q(st.retQty)}</b></div><div><small>مستهلك</small><b>${Q(st.consumed)}</b></div><div><small>حد الطلب</small><b>${Q(it.min_quantity||it.reorder_level||0)}</b></div></div><div class="row-actions"><button type="button" onclick="inventoryOpenItemSmart&&inventoryOpenItemSmart('${E(it.id)}')">عرض</button></div></article>`;
    }).join('') || '<div class="muted">لا توجد أصناف</div>'}</div></td></tr>`;
    return true;
  }
  function applyWarehouseView(){
    if(!isWarehouse()) return;
    ensureCatalogTab();
    document.body.classList.add('warehouse-manager-view-v164');
    document.querySelectorAll('#financeDashboard .finance-tab').forEach(btn=>{ const t=S(btn.textContent); btn.style.display=(t==='طلبات الصرف'||t==='الأصناف')?'':'none'; });
    ['financeTabOverview','financeTabExpenses','financeTabInventory','financeTabMovements','financeTabReports','financeTabCostCenters'].forEach(id=>$(id)?.classList.add('hidden'));
    $('financeTabCatalog')?.classList.remove('hidden');
    renderWarehouseCatalog();
  }
  async function updateItemQty(item, delta){
    if(!item?.id || !window.sb) return;
    const next=Math.max(0,N(item.quantity)+N(delta));
    const r=await sb.from('inventory_items').update({quantity:next}).eq('id',item.id);
    if(r.error) throw r.error;
    item.quantity=next;
  }
  async function reverseBatch(batch){
    for(const line of (batch?.lines||[])){
      const code=S(line.product_code||line.barcode||line.supplier_barcode||'');
      let item=(window.data?.inventoryItems||[]).find(i=>String(i.id)===String(line.item_id||line.inventory_item_id));
      if(!item && code) item=(window.data?.inventoryItems||[]).find(i=>[i.product_code,i.barcode,i.supplier_barcode,i.serial_number,i.company_code].map(S).includes(code));
      if(item) await updateItemQty(item, -N(line.quantity));
    }
  }
  async function tryDelete(table, fn){ try{ const r=await fn(sb.from(table).delete()); if(r.error && !/schema cache|column|relation|does not exist/i.test(r.error.message||'')) throw r.error; }catch(e){ if(!/schema cache|column|relation|does not exist/i.test(e.message||'')) throw e; } }
  async function deleteRowsForBatch(batch){
    if(!window.sb) return;
    const inv=S(batch.invoice_no||batch.reference||'');
    if(batch.id){ await tryDelete('inventory_batch_items', q=>q.eq('batch_id',batch.id)); }
    if(inv){
      await tryDelete('inventory_batch_items', q=>q.eq('invoice_no',inv));
      await tryDelete('inventory_batch_items', q=>q.eq('reference',inv));
      await tryDelete('inventory_movements', q=>q.eq('invoice_no',inv));
      await tryDelete('inventory_movements', q=>q.eq('reference',inv));
      await tryDelete('inventory_movements', q=>q.ilike('notes',`%${inv}%`));
    }
    const movementIds=(batch.lines||[]).map(l=>l.movement_id).filter(Boolean);
    if(movementIds.length) await tryDelete('inventory_movements', q=>q.in('id',movementIds));
    if(batch.id) await tryDelete('inventory_batches', q=>q.eq('id',batch.id));
    if(inv) await tryDelete('inventory_batches', q=>q.eq('invoice_no',inv));
  }
  window.stockBatchDeleteV164 = async function(idx){
    const all=window.stockBatchesV148||[]; const batch=all[Number(idx)];
    if(!batch) return (typeof msg==='function'?msg('لم يتم العثور على الفاتورة','err'):alert('لم يتم العثور على الفاتورة'));
    const inv=S(batch.invoice_no||batch.id||idx);
    if(!confirm('تأكيد حذف فاتورة المخزون رقم '+inv+'؟')) return;
    try{
      await reverseBatch(batch);
      await deleteRowsForBatch(batch);
      addHiddenInvoice(batch);
      window.stockBatchesV148=(window.stockBatchesV148||[]).filter((b,i)=>i!==Number(idx) && !isHiddenBatch(b));
      if(typeof msg==='function') msg('تم حذف الفاتورة وعكس الكميات');
      if(typeof financeLoadAll==='function') await financeLoadAll();
      if(typeof stockBatchLoadV148==='function') await stockBatchLoadV148(true);
      window.stockBatchesV148=(window.stockBatchesV148||[]).filter(b=>!isHiddenBatch(b));
      if(typeof renderStockBatchCardsV149==='function') renderStockBatchCardsV149();
      if(typeof inventoryRenderItems==='function') inventoryRenderItems();
    }catch(e){ console.error(e); if(typeof msg==='function') msg(e.message||String(e),'err'); else alert(e.message||String(e)); }
  };
  window.stockBatchDeleteV163=window.stockBatchDeleteV164; window.stockBatchDeleteV162=window.stockBatchDeleteV164; window.stockBatchDeleteV161=window.stockBatchDeleteV164; window.stockBatchDeleteV152=window.stockBatchDeleteV164;
  function renderInvoices(){
    const grid=$('stockBatchCardsGridV149'); if(!grid) return;
    if(isWarehouse()){ grid.innerHTML=''; return; }
    const all=(window.stockBatchesV148||[]).filter(b=>!isHiddenBatch(b));
    const qf=S($('batchSearchV149')?.value).toLowerCase(), df=S($('batchDateFilterV149')?.value), mf=S($('batchMonthFilterV149')?.value);
    const rows=all.filter(r=>{ const d=S(r.batch_date||''); if(df && d!==df) return false; if(mf && !d.startsWith(mf)) return false; if(qf && ![r.invoice_no,r.id,r.supplier,r.batch_date].join(' ').toLowerCase().includes(qf)) return false; return true; });
    grid.innerHTML=rows.map(r=>{ const idx=(window.stockBatchesV148||[]).indexOf(r), count=(r.lines||[]).length; return `<div class="v149-invoice-card"><div class="v149-invoice-top"><div><div class="v149-invoice-no">${E(r.invoice_no||r.id||'-')}</div><span class="v149-pill">${count} صنف</span></div><div class="v149-invoice-date">${E(r.batch_date||'-')}</div></div><div class="muted">المورد: <b>${E(r.supplier||'-')}</b></div><div class="v149-invoice-meta"><div class="v149-mini"><small>قبل الضريبة</small><b>${N(r.total_before_vat).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</b></div><div class="v149-mini"><small>VAT 15%</small><b>${N(r.total_vat).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</b></div><div class="v149-mini"><small>الإجمالي شامل</small><b>${N(r.total_with_vat).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س</b></div><div class="v149-mini"><small>المرجع</small><b>${E(r.invoice_no||r.id||'-')}</b></div></div><div class="row-actions v164-invoice-actions"><button type="button" onclick="viewStockBatchV149(${idx})">عرض الفاتورة</button><button type="button" class="light" onclick="stockBatchPrintSavedV148('${idx}')">طباعة</button><button type="button" class="light" onclick="stockBatchEditV152&&stockBatchEditV152('${idx}')">تعديل</button><button type="button" class="danger" onclick="stockBatchDeleteV164('${idx}')">حذف</button></div></div>`; }).join('') || '<div class="v149-empty">لا توجد فواتير حسب الفلتر الحالي</div>';
  }
  window.renderStockBatchCardsV149 = renderInvoices;
  const oldLoad=window.stockBatchLoadV148;
  if(oldLoad && !oldLoad.v164Wrapped){ window.stockBatchLoadV148=async function(){ const out=await oldLoad.apply(this,arguments); window.stockBatchesV148=(window.stockBatchesV148||[]).filter(b=>!isHiddenBatch(b)); renderInvoices(); return out; }; window.stockBatchLoadV148.v164Wrapped=true; }
  const oldItems=window.inventoryRenderItems;
  if(oldItems && !oldItems.v164Wrapped){ window.inventoryRenderItems=function(){ if(isWarehouse()) return renderWarehouseCatalog(); return oldItems.apply(this,arguments); }; window.inventoryRenderItems.v164Wrapped=true; }
  function injectCss(){ if($('v164Css')) return; const st=document.createElement('style'); st.id='v164Css'; st.textContent=`.warehouse-manager-view-v164 #smartStockInvoicesV149,.warehouse-manager-view-v164 #stockBatchCardV148,.warehouse-manager-view-v164 #stockBatchCardsGridV149{display:none!important}.warehouse-manager-view-v164 #financeTabCatalog{display:block!important}.v164-warehouse-catalog-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:14px;padding:8px;direction:rtl}.v164-warehouse-item-card{border:1px solid #cfe3dd;border-radius:18px;padding:14px;background:#fff;box-shadow:0 10px 22px rgba(0,45,31,.06)}.v164-item-head{display:flex;align-items:center;justify-content:space-between;gap:12px}.v164-item-head h3{margin:0;color:#003f31}.v164-img{width:66px;height:66px;border:1px solid #dbeee8;border-radius:14px;background:#f7fbf9;display:grid;place-items:center;overflow:hidden;flex:0 0 66px}.v164-img img{max-width:100%;max-height:100%;object-fit:contain}.v164-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.v164-stats div{background:#f1faf6;border:1px solid #d9eee6;border-radius:12px;padding:8px;text-align:center}.v164-stats small{display:block;color:#5b756d}.v164-stats b{color:#004c3a}.v164-ok,.v164-low{border-radius:999px;padding:5px 8px;font-size:12px}.v164-ok{background:#e5f8ee;color:#067647}.v164-low{background:#ffe6e6;color:#b42318}.v164-invoice-actions{display:flex!important;gap:8px!important;flex-wrap:wrap!important}.v164-invoice-actions button{min-width:78px!important}@media(max-width:700px){.v164-stats{grid-template-columns:repeat(2,1fr)}}`; document.head.appendChild(st); }
  const oldShow=window.financeShowTab;
  if(oldShow && !oldShow.v164Wrapped){ window.financeShowTab=function(tab,btn){ const out=oldShow.apply(this,arguments); setTimeout(()=>{ injectCss(); ensureCatalogTab(); if(tab==='catalog' && typeof inventoryRenderItems==='function') inventoryRenderItems(); applyWarehouseView(); renderInvoices(); },120); return out; }; window.financeShowTab.v164Wrapped=true; }
  const oldRender=window.financeRenderAll;
  if(oldRender && !oldRender.v164Wrapped){ window.financeRenderAll=function(){ const out=oldRender.apply(this,arguments); setTimeout(()=>{ injectCss(); ensureCatalogTab(); applyWarehouseView(); renderInvoices(); },180); return out; }; window.financeRenderAll.v164Wrapped=true; }
  window.addEventListener('load',()=>setTimeout(()=>{ injectCss(); ensureCatalogTab(); applyWarehouseView(); if(typeof renderStockBatchCardsV149==='function') renderStockBatchCardsV149(); },1800));
})();

/* ===== V165: Warehouse price hiding in product details + single active invoice delete button ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const N = v => { const x=parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x)?x:0; };
  const E = v => S(v).replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const Q = v => N(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const role = () => { try { return (typeof session==='function' ? (session()||{}).role : '') || JSON.parse(localStorage.getItem('tasneef_session')||'{}').role || ''; } catch(_) { return ''; } };
  const isWarehouse = () => role()==='warehouse_manager';
  const codeOf = it => S(it?.product_code || it?.barcode || it?.supplier_barcode || it?.serial_number || it?.company_code || '');
  const companyCodeOf = it => S(it?.company_code || it?.internal_code || it?.serial_number || codeOf(it));
  const imgOf = it => S(it?.image_url || it?.image || it?.photo_url || '');

  function itemStats(it){
    const id=S(it?.id), code=codeOf(it), name=S(it?.name);
    let inQty=0,outQty=0,retQty=0,consumeQty=0;
    (window.data?.inventoryMovements||[]).forEach(m=>{
      const match = S(m.item_id)===id || (code && [m.product_code,m.barcode,m.supplier_barcode,m.serial_number].map(S).includes(code)) || (name && S(m.item_name)===name);
      if(!match) return;
      const qty=N(m.quantity), t=S(m.movement_type);
      if(t==='in') inQty+=qty; else if(t==='out') outQty+=qty; else if(t==='return') retQty+=qty; else if(t==='consume') consumeQty+=qty;
    });
    return {inQty,outQty,retQty,consumed:Math.max(0,outQty-retQty)+consumeQty,remaining:N(it?.quantity)};
  }

  const oldOpenItem = window.inventoryOpenItemSmart;
  window.inventoryOpenItemSmart = function(id){
    if(!isWarehouse()) return oldOpenItem ? oldOpenItem.apply(this,arguments) : undefined;
    const it=(window.data?.inventoryItems||[]).find(x=>String(x.id)===String(id));
    if(!it) return (typeof msg==='function'?msg('الصنف غير موجود','err'):alert('الصنف غير موجود'));
    const st=itemStats(it);
    const rows=(window.data?.inventoryMovements||[]).filter(m=>{
      const c=codeOf(it), nm=S(it.name);
      return S(m.item_id)===S(it.id) || (c && [m.product_code,m.barcode,m.supplier_barcode,m.serial_number].map(S).includes(c)) || (nm && S(m.item_name)===nm);
    }).slice(0,40).map(m=>{
      const label = S(m.movement_type)==='in'?'إدخال':(S(m.movement_type)==='out'?'صرف':(S(m.movement_type)==='return'?'مرتجع':(S(m.movement_type)==='consume'?'استهلاك':'تعديل')));
      return `<tr><td>${E(m.movement_date || String(m.created_at||'').slice(0,10) || '-')}</td><td>${E(label)}</td><td>${Q(m.quantity)}</td><td>${E(m.project_name || (typeof projectName==='function'?projectName(m.project_id):'') || '-')}</td><td>${E(m.receiver||'-')}</td><td>${E(m.reason||m.notes||'-')}</td></tr>`;
    }).join('') || '<tr><td colspan="6">لا توجد حركات مسجلة</td></tr>';
    const img=imgOf(it);
    const html=`<div class="v165-warehouse-detail">
      <div class="v165-prod-head">${img?`<img src="${E(img)}" onerror="this.style.display='none'">`:''}<div><h2>${E(it.name||'-')}</h2><p>كود المنتج: <b>${E(codeOf(it)||'-')}</b></p><p>كود الشركة: <b>${E(companyCodeOf(it)||'-')}</b></p></div></div>
      <div class="v165-meta"><div><small>الكمية</small><b>${Q(st.remaining)} ${E(it.unit||'')}</b></div><div><small>دخل</small><b>${Q(st.inQty)}</b></div><div><small>خرج</small><b>${Q(st.outQty)}</b></div><div><small>مرتجع</small><b>${Q(st.retQty)}</b></div><div><small>مستهلك</small><b>${Q(st.consumed)}</b></div><div><small>حد الطلب</small><b>${Q(it.min_quantity||it.reorder_level||0)}</b></div><div><small>المورد</small><b>${E(it.supplier||'-')}</b></div><div><small>التصنيف</small><b>${E(it.category||'-')}</b></div></div>
      <h3>آخر الحركات</h3><table><thead><tr><th>التاريخ</th><th>الحركة</th><th>الكمية</th><th>المشروع</th><th>المستلم</th><th>السبب</th></tr></thead><tbody>${rows}</tbody></table>
    </div>`;
    if(typeof window.smartOpenV129==='function') window.smartOpenV129('تفاصيل المنتج', html, '');
    else alert(`${it.name}\nالكمية: ${Q(st.remaining)}`);
  };

  function cleanupInvoiceCardsV165(){
    document.querySelectorAll('#stockBatchCardsGridV149 .v149-invoice-card').forEach(card=>{
      const allButtons=[...card.querySelectorAll('button')];
      const pick = (re)=> allButtons.find(b=>re.test(S(b.textContent)));
      const view=pick(/عرض/), print=pick(/طباعة/), edit=pick(/تعديل/), del=pick(/^حذف$/);
      card.querySelectorAll('.row-actions').forEach(a=>a.remove());
      const invText = S(card.querySelector('.v149-invoice-no')?.textContent || '');
      let idx = -1;
      try{ idx=(window.stockBatchesV148||[]).findIndex(b=>S(b.invoice_no||b.id||'')===invText); }catch(_){ idx=-1; }
      if(idx<0) idx=[...document.querySelectorAll('#stockBatchCardsGridV149 .v149-invoice-card')].indexOf(card);
      const actions=document.createElement('div'); actions.className='row-actions v165-invoice-actions';
      actions.innerHTML=`<button type="button" onclick="viewStockBatchV149(${idx})">عرض الفاتورة</button><button type="button" class="light" onclick="stockBatchPrintSavedV148('${idx}')">طباعة</button><button type="button" class="light" onclick="stockBatchEditV152&&stockBatchEditV152('${idx}')">تعديل</button><button type="button" class="danger" onclick="stockBatchDeleteV164('${idx}')">حذف</button>`;
      card.appendChild(actions);
    });
  }
  const oldRender = window.renderStockBatchCardsV149;
  if(oldRender && !oldRender.v165Wrapped){
    window.renderStockBatchCardsV149=function(){ const out=oldRender.apply(this,arguments); setTimeout(cleanupInvoiceCardsV165,0); return out; };
    window.renderStockBatchCardsV149.v165Wrapped=true;
  }
  const oldShow=window.financeShowTab;
  if(oldShow && !oldShow.v165Wrapped){
    window.financeShowTab=function(){ const out=oldShow.apply(this,arguments); setTimeout(cleanupInvoiceCardsV165,160); return out; };
    window.financeShowTab.v165Wrapped=true;
  }
  function injectCss(){
    if($('v165Css')) return;
    const st=document.createElement('style'); st.id='v165Css'; st.textContent=`
      .v165-invoice-actions{display:flex!important;gap:8px!important;flex-wrap:wrap!important;margin-top:12px!important}.v165-invoice-actions button{min-width:78px!important}
      .v165-prod-head{display:flex;align-items:flex-start;gap:18px;margin-bottom:16px}.v165-prod-head img{width:130px;height:130px;object-fit:contain;border:1px solid #d6e9e2;border-radius:16px;background:#f8fcfa}.v165-prod-head h2{margin:0 0 8px;color:#073f31}.v165-prod-head p{margin:4px 0;color:#526b63}.v165-meta{display:grid;grid-template-columns:repeat(auto-fit,minmax(155px,1fr));gap:10px;margin:12px 0 20px}.v165-meta div{background:#eef8f4;border:1px solid #d5ebe3;border-radius:14px;padding:10px}.v165-meta small{display:block;color:#658078;margin-bottom:5px}.v165-meta b{color:#004c3a}.warehouse-manager-view-v164 .v149-mini:has(small),.warehouse-manager-view-v164 .v149-invoice-meta{display:none!important}
    `; document.head.appendChild(st);
  }
  window.addEventListener('load',()=>setTimeout(()=>{ injectCss(); cleanupInvoiceCardsV165(); },1500));
  setTimeout(()=>{ injectCss(); cleanupInvoiceCardsV165(); },2500);
})();

/* ===================== V166 Client Reports Edit/Save Hard Stability Fix ===================== */
(function(){
  'use strict';
  const V='166';
  const $id = (id)=>document.getElementById(id);
  const escV166 = (v)=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  function parseImagesV166(v){
    if(!v) return [];
    if(Array.isArray(v)) return v.filter(Boolean);
    if(typeof v === 'string'){
      const t=v.trim();
      if(!t) return [];
      try{
        const parsed = JSON.parse(t);
        if(Array.isArray(parsed)) return parsed.filter(Boolean);
        if(parsed && typeof parsed === 'object') return [parsed];
      }catch(_){
        // Sometimes the field stores a single URL/data-url string.
        if(/^data:image\//i.test(t) || /^https?:\/\//i.test(t)) return [{url:t}];
      }
    }
    if(typeof v === 'object') return [v];
    return [];
  }
  function normalizeServiceV166(s){
    const serviceType = s?.service_type || s?.title || s?.name || 'خدمة';
    return {
      service_type: serviceType,
      title: s?.title || serviceType,
      service_description: s?.service_description || s?.description || '',
      scope_work: s?.scope_work || s?.scope || '',
      notes: s?.notes || '',
      before_images: parseImagesV166(s?.before_images),
      during_images: parseImagesV166(s?.during_images),
      after_images: parseImagesV166(s?.after_images),
      source: s?.source || 'admin'
    };
  }
  function fillReportFormV166(r, services){
    if(!r) throw new Error('لم يتم العثور على التقرير');
    const set=(k,v)=>{ const el=$id(k); if(el) el.value = v ?? ''; };
    set('premiumReportId', r.id);
    set('premiumReportProject', r.project_id || '');
    set('premiumReportDate', r.report_date || (typeof today==='function' ? today() : new Date().toISOString().slice(0,10)));
    set('premiumReportTitle', r.title || '');
    set('premiumReportType', r.report_type || 'تقرير خدمات');
    set('premiumChairmanName', r.chairman_name || '');
    set('premiumChairmanPhone', r.chairman_phone || '');
    set('premiumSummary', r.executive_summary || (typeof defaultReportSummary==='function' ? defaultReportSummary() : ''));

    const rows = (services||[]).map(normalizeServiceV166);
    // Important: premiumServicesState is a lexical variable in the original app.js.
    // Assigning window.premiumServicesState does not update the editor. Assign the real variable.
    try{
      premiumServicesState = rows.length ? rows : (typeof makeService==='function' ? [makeService(r.report_type || 'خدمة')] : []);
    }catch(e){
      window.premiumServicesState = rows;
    }
    if(typeof renderPremiumServicesEditor === 'function') renderPremiumServicesEditor();
    const title=$id('premiumReportFormTitle'); if(title) title.textContent='تعديل تقرير';
    const card=$id('premiumReportFormCard');
    if(card){ card.classList.remove('hidden'); card.style.display='block'; card.scrollIntoView({behavior:'smooth', block:'start'}); }
  }

  // Fresh, deterministic edit: always fetch current report and services, never rely on stale cache.
  window.editPremiumReport = async function(id){
    const btn = document.querySelector(`[data-action="edit"][data-id="${CSS && CSS.escape ? CSS.escape(String(id)) : String(id)}"]`) || event?.target;
    let old='';
    try{
      if(btn){ old=btn.innerHTML; btn.disabled=true; btn.innerHTML='جاري الفتح...'; }
      if(typeof msg==='function') msg('جاري تحميل بيانات التقرير...');
      let rr, ss;
      if(!window.sb && typeof sb !== 'undefined') window.sb = sb;
      if(!window.sb && typeof sb === 'undefined') throw new Error('اتصال قاعدة البيانات غير جاهز');
      [rr, ss] = await Promise.all([
        sb.from('client_reports').select('*').eq('id', id).maybeSingle(),
        sb.from('client_report_services').select('*').eq('report_id', id).order('sort_order', {ascending:true})
      ]);
      if(rr.error) throw rr.error;
      if(ss.error) throw ss.error;
      fillReportFormV166(rr.data, ss.data || []);

      // Refresh local cache for this report so table actions stay stable.
      try{
        window.data = window.data || {};
        const reports = window.data.clientReports || [];
        const idx = reports.findIndex(x=>String(x.id)===String(id));
        if(idx >= 0) reports[idx] = rr.data; else reports.unshift(rr.data);
        window.data.clientReports = reports;
        window.data.clientReportServices = (window.data.clientReportServices||[]).filter(x=>String(x.report_id)!==String(id)).concat(ss.data||[]);
      }catch(_){ }
      if(typeof msg==='function') msg('تم فتح التقرير للتعديل','ok');
    }catch(e){
      console.error('V166 editPremiumReport failed', e);
      if(typeof msg==='function') msg(e.message || String(e),'err');
    }finally{
      if(btn){ btn.disabled=false; if(old) btn.innerHTML=old; }
    }
  };

  // Make the report list lighter: table only needs services names, not full image blobs.
  window.loadPremiumReportsOnly = async function(showMessage=false){
    try{
      const body=$id('premiumReportsBody');
      if(body) body.classList.add('loading-soft-v166');
      const [reports, services, ratings] = await Promise.all([
        sb.from('client_reports').select('*').order('created_at',{ascending:false}),
        sb.from('client_report_services').select('id,report_id,sort_order,service_type,title,source').order('sort_order',{ascending:true}),
        sb.from('client_service_ratings').select('id,report_id,service_id,rating,created_at').order('created_at',{ascending:false})
      ]);
      data.clientReports = reports.data || [];
      data.clientReportServices = services.data || [];
      data.clientServiceRatings = ratings.data || [];
      data.clientReportsError = reports.error?.message || services.error?.message || ratings.error?.message || '';
      if(typeof renderPremiumReports==='function') renderPremiumReports();
      try{ if(typeof renderClientRatings==='function') renderClientRatings(); }catch(_){ }
      if(showMessage && typeof msg==='function') msg(data.clientReportsError ? 'تأكد من تشغيل ملف SQL الخاص بالتقارير في Supabase' : 'تم تحديث التقارير', data.clientReportsError?'err':'ok');
    }catch(e){
      console.error('V166 loadPremiumReportsOnly failed', e);
      if(showMessage && typeof msg==='function') msg(e.message||String(e),'err');
    }finally{
      const body=$id('premiumReportsBody'); if(body) body.classList.remove('loading-soft-v166');
    }
  };

  // Faster post-save refresh: update the table without loading image blobs.
  const originalSaveV166 = window.savePremiumReport;
  window.savePremiumReport = async function(status='draft'){
    const btn = event?.target;
    const old = btn?.innerHTML;
    try{
      if(btn){ btn.disabled=true; btn.innerHTML = status==='published' ? 'جاري النشر...' : 'جاري الحفظ...'; }
      // The original savePremiumReport already performs the required DB insert/update.
      // We keep it, then override the heavy reload with our lighter loader below.
      const out = await originalSaveV166(status);
      setTimeout(()=>{ try{ window.loadPremiumReportsOnly(false); }catch(_){ } }, 80);
      return out;
    }catch(e){
      console.error('V166 save wrapper failed', e);
      if(typeof msg==='function') msg(e.message||String(e),'err');
    }finally{
      if(btn){ btn.disabled=false; if(old) btn.innerHTML=old; }
    }
  };

  function injectCssV166(){
    if($id('v166ClientReportCss')) return;
    const st=document.createElement('style'); st.id='v166ClientReportCss';
    st.textContent=`
      #premiumReportsBody.loading-soft-v166{opacity:.65;pointer-events:auto!important;transition:.18s}
      #premiumReportFormCard{scroll-margin-top:18px}
      .client-action-btn-v159[disabled]{opacity:.65;cursor:wait}
    `;
    document.head.appendChild(st);
  }
  window.addEventListener('load', injectCssV166);
  injectCssV166();
  console.log('V167 client reports edit database connection fix loaded');
})();

/* ===== V175 System Stability Rebuild: Monthly engine + camera + loading ===== */
(function(){
  if(window.__v175SystemStability) return;
  window.__v175SystemStability = true;
  const $id = (id)=>document.getElementById(id);
  const esc175 = (s)=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const n175 = (v)=>{ const x=Number(v||0); return Number.isFinite(x)?x:0; };
  const pct175 = (v)=> (Number(v||0)).toLocaleString('en-US',{minimumFractionDigits:1,maximumFractionDigits:1}) + '%';
  const mins175 = (m)=> (typeof minsToText==='function'?minsToText(Math.round(n175(m))):String(Math.round(n175(m))));
  const supName175 = (id)=> (typeof supervisorName==='function'?supervisorName(id):'-');
  const projName175 = (id)=> (typeof projectName==='function'?projectName(id):'-');
  const actualMins175 = (l)=> n175(typeof logActualMinutes==='function'?logActualMinutes(l):(l.duration_minutes||minutesBetween(l.check_in,l.check_out)));
  const reqMins175 = (l)=> n175(typeof logRequiredMinutes==='function'?logRequiredMinutes(l):l.required_minutes);
  const workers175 = (pid)=> (typeof uniqueWorkersForProjectTextV60==='function'?uniqueWorkersForProjectTextV60(pid):'-') || '-';
  const project175 = (pid)=> (window.data?.projects||[]).find(p=>String(p.id)===String(pid));
  function monthRange175(month){
    const y=Number(String(month).slice(0,4)), m=Number(String(month).slice(5,7));
    const last=new Date(y,m,0).getDate();
    return {start:`${month}-01`, end:`${month}-${String(last).padStart(2,'0')}`, days:last};
  }
  function isPermanentProject175(pid, maxReq){
    const p=project175(pid);
    const base=n175(p?.required_daily_minutes || p?.required_minutes || p?.daily_minutes || maxReq);
    const op=String(p?.operation_type||p?.visit_type_default||'').toLowerCase();
    const name=String(p?.name||'');
    return base>=540 || op.includes('full') || op.includes('permanent') || name.includes('دائم') || name.includes('دوام كامل');
  }
  function buildMonthlyRows175(){
    const month=$id('monthlyMonth')?.value || (typeof today==='function'?today().slice(0,7):new Date().toISOString().slice(0,7));
    const sid=$id('monthlySupervisor')?.value;
    const mr=monthRange175(month);
    let logs=(window.data?.logs||[]).filter(l=>{
      const d=l.log_date || String(l.check_in||'').slice(0,10);
      return d && d>=mr.start && d<=mr.end;
    });
    if(sid) logs=logs.filter(l=>String(l.supervisor_id)===String(sid));
    const map=new Map();
    logs.forEach(l=>{
      const key=String(l.supervisor_id||'')+'|'+String(l.project_id||'');
      if(!map.has(key)) map.set(key,{s:l.supervisor_id,p:l.project_id,a:0,r:0,t:0,c:0,days:new Set(),maxReq:0});
      const x=map.get(key); const d=l.log_date || String(l.check_in||'').slice(0,10); const req=reqMins175(l);
      x.a += actualMins175(l); x.r += req; x.t += n175(l.travel_minutes); x.c += 1; x.days.add(d); x.maxReq=Math.max(x.maxReq,req);
    });
    const all=[...map.values()].map(x=>({...x,uniqueDays:x.days.size,workers:workers175(x.p),permanent:isPermanentProject175(x.p,x.maxReq)}));
    const daily=all.filter(x=>!x.permanent);
    const permanent=all.filter(x=>x.permanent);
    const supTotals={}; daily.forEach(r=>{ const s=String(r.s||''); supTotals[s]=(supTotals[s]||0)+r.a; });
    const bySup={}; daily.forEach(r=>{ const s=String(r.s||''); (bySup[s]||(bySup[s]=[])).push(r); });
    Object.keys(bySup).forEach(s=>{
      const rows=bySup[s].sort((a,b)=>projName175(a.p).localeCompare(projName175(b.p),'ar'));
      const total=supTotals[s]||0; let sum=0;
      rows.forEach((r,i)=>{
        const raw=total?(r.a/total*100):0;
        let disp=Math.round(raw*10)/10;
        if(i===rows.length-1 && total>0) disp=Math.max(0,Math.round((100-sum)*10)/10);
        sum+=disp; r.workPercentRaw=raw; r.workPercentDisplay=disp;
      });
    });
    const rows=daily.sort((a,b)=>{ const s=supName175(a.s).localeCompare(supName175(b.s),'ar'); return s||projName175(a.p).localeCompare(projName175(b.p),'ar'); });
    permanent.sort((a,b)=>{ const s=supName175(a.s).localeCompare(supName175(b.s),'ar'); return s||projName175(a.p).localeCompare(projName175(b.p),'ar'); });
    return {month,range:mr,logs,rows,permanent};
  }
  window.monthlyRowsV175 = ()=>buildMonthlyRows175().rows;
  window.renderMonthly = function(){
    const body=$id('monthlyBody'); if(!body) return;
    const table=body.closest('table');
    if(table && table.tHead){ table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الساعات الفعلية</th><th>نسبة العمل</th><th>حالة الأداء</th></tr>'; }
    const res=buildMonthlyRows175();
    body.innerHTML=res.rows.map(r=>{
      const cls=r.workPercentDisplay>=90?'green':(r.workPercentDisplay>=70?'amber':'red');
      const text=r.workPercentDisplay>=90?'ممتاز':(r.workPercentDisplay>=70?'جيد':'يحتاج متابعة');
      return `<tr><td>${esc175(supName175(r.s))}</td><td>${esc175(projName175(r.p))}</td><td>${esc175(r.workers||'-')}</td><td>${mins175(r.a)}</td><td><span class="badge ${cls}">${pct175(r.workPercentDisplay)}</span></td><td><span class="badge ${cls}">${text}</span></td></tr>`;
    }).join('') || '<tr><td colspan="6">لا توجد بيانات في مشاريع الصيانة اليومية لهذا الشهر</td></tr>';
    const dailyTotal=res.rows.reduce((a,r)=>a+r.a,0), permTotal=res.permanent.reduce((a,r)=>a+r.a,0);
    if($id('monthlySummary')) $id('monthlySummary').innerHTML=`<div class="kpi"><small>مشاريع يومية</small><b>${res.rows.length}</b></div><div class="kpi"><small>مشاريع دائمة</small><b>${res.permanent.length}</b></div><div class="kpi"><small>وقت اليومية</small><b>${mins175(dailyTotal)}</b></div><div class="kpi"><small>وقت الدائمة</small><b>${mins175(permTotal)}</b></div><div class="kpi"><small>نطاق التقرير</small><b>${esc175(res.range.start)} إلى ${esc175(res.range.end)}</b></div>`;
  };
  function permanentSectionHtml175(permanent){
    const cards=permanent.map(r=>`<div class="perm-card"><div class="perm-title">${esc175(projName175(r.p))}</div><div class="perm-sup">المشرف: ${esc175(supName175(r.s))}</div><div class="perm-workers">${esc175(r.workers||'-')}</div><div class="perm-time">${mins175(r.a)}</div></div>`).join('');
    return `<section class="block"><h2>مشاريع العمالة الدائمة</h2><div class="perm-grid">${cards || '<div class="empty">لا توجد مشاريع دائمة 9 ساعات أو أكثر خلال هذا الشهر</div>'}</div></section>`;
  }
  window.printMonthlyReportV57 = window.printMonthlyReportV175 = function(){
    const res=buildMonthlyRows175();
    const monthLabel=res.month;
    const bySup={}; res.rows.forEach(r=>{ const s=String(r.s||''); (bySup[s]||(bySup[s]=[])).push(r); });
    const cards=Object.keys(bySup).map(s=>{
      const rows=bySup[s]; const sum=rows.reduce((a,r)=>a+r.workPercentDisplay,0);
      const trs=rows.map(r=>`<tr><td>${esc175(projName175(r.p))}</td><td>${mins175(r.a)}</td><td>${pct175(r.workPercentDisplay)}</td></tr>`).join('');
      const workers=[...new Set(rows.flatMap(r=>String(r.workers||'-').split('،').map(x=>x.trim()).filter(Boolean)))].join('، ');
      return `<div class="sup-card"><div class="sup-head"><b>${esc175(supName175(s))}</b><span>${pct175(sum)}</span></div><table><thead><tr><th>المشروع</th><th>الوقت</th><th>النسبة</th></tr></thead><tbody>${trs}</tbody></table><div class="workers"><b>أسماء العمال</b><p>${esc175(workers||'-')}</p></div></div>`;
    }).join('');
    const totalRows=res.rows.reduce((a,r)=>a+r.a,0); const permMins=res.permanent.reduce((a,r)=>a+r.a,0);
    const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;color:#153d33;margin:0;background:#fff;font-size:11px}.page{padding:14px;border:2px solid #0a5a49;min-height:100vh}.top{display:flex;justify-content:space-between;align-items:center;border-bottom:3px solid #0a5a49;padding-bottom:10px}.brand{display:flex;align-items:center;gap:10px}.logo{width:58px;height:58px;border-radius:18px;border:2px solid #c7a24d;display:grid;place-items:center;font-weight:900;color:#0a5a49;background:#f7fbfa}.brand h1{margin:0;font-size:20px}.title{text-align:left}.title h2{font-size:26px;margin:0;color:#0a5a49}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:12px 0}.box{border:1px solid #d9e6e1;border-radius:13px;padding:10px;text-align:center;background:#fff}.box small{display:block;color:#697b74}.box b{font-size:16px;color:#0a5a49}.block{margin-top:12px}.block h2{margin:0 0 8px;text-align:center}.block h2 span,.block h2{color:#fff;background:#0a5a49;border:2px solid #c7a24d;border-radius:999px;padding:7px 35px;display:block}.sup-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.sup-card,.perm-card{border:2px solid #0a5a49;border-radius:12px;overflow:hidden;background:#fff;break-inside:avoid}.sup-head{display:flex;justify-content:space-between;padding:8px;background:#f7fbfa;color:#0a5a49;border-bottom:1px solid #d9e6e1}table{width:100%;border-collapse:collapse}th{background:#0a5a49;color:white;padding:6px}td{border:1px solid #e2ece8;padding:5px;text-align:center}.workers{padding:8px;text-align:center}.workers p{margin:4px 0 0;line-height:1.7}.perm-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.perm-card{text-align:center;padding-bottom:8px}.perm-title{background:#0a5a49;color:#fff;padding:10px;font-weight:900}.perm-sup{padding:7px;color:#0a5a49;font-weight:700}.perm-workers{min-height:55px;padding:7px;line-height:1.7}.perm-time{display:inline-block;border-radius:999px;background:#edf8f3;color:#0a5a49;padding:5px 15px;font-weight:900}.empty{grid-column:1/-1;text-align:center;border:1px dashed #aabdb6;border-radius:12px;padding:20px;color:#697b74}.foot{display:grid;grid-template-columns:1fr 1fr 1fr;gap:10px;margin-top:14px}.sign{border:1px solid #d9e6e1;border-radius:12px;padding:10px;min-height:70px}.note{margin-top:8px;text-align:center;color:#697b74}@media print{body{-webkit-print-color-adjust:exact;print-color-adjust:exact}.page{border-radius:0}}</style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h1>شركة تصنيف لإدارة المرافق</h1><small>تقرير تشغيل شهري</small></div></div><div class="title"><h2>تقرير الأوقات الشهرية</h2><p>الشهر: ${esc175(monthLabel)}</p></div></div><div class="meta"><div class="box"><small>مشاريع يومية</small><b>${res.rows.length}</b></div><div class="box"><small>مشاريع دائمة</small><b>${res.permanent.length}</b></div><div class="box"><small>وقت اليومية</small><b>${mins175(totalRows)}</b></div><div class="box"><small>وقت الدائمة</small><b>${mins175(permMins)}</b></div></div><section class="block"><h2>مشاريع الصيانة اليومية</h2><div class="sup-grid">${cards || '<div class="empty">لا توجد بيانات</div>'}</div></section>${permanentSectionHtml175(res.permanent)}<div class="foot"><div class="sign"><b>إعداد</b><br><br>الاسم: ____________</div><div class="sign"><b>مراجعة</b><br><br>الاسم: ____________</div><div class="sign"><b>اعتماد</b><br><br>الاسم: ____________</div></div><div class="note">تم توليد التقرير من نظام شركة تصنيف لإدارة المرافق. مجموع نسب كل مشرف في المشاريع اليومية يساوي 100%.</div></div><script>window.onload=function(){setTimeout(function(){window.print()},450)}</script></body></html>`;
    const w=window.open('','_blank'); if(!w){ if(typeof msg==='function') msg('اسمح بالنوافذ المنبثقة للطباعة','err'); return; } w.document.write(html); w.document.close();
  };
  window.exportMonthlyCSV = function(){
    const res=buildMonthlyRows175();
    const csv=['المشرف,المشروع,أسماء العمال,الساعات الفعلية,نسبة العمل,نوع المشروع',...res.rows.map(r=>[supName175(r.s),projName175(r.p),r.workers,mins175(r.a),pct175(r.workPercentDisplay),'يومي'].map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(',')),...res.permanent.map(r=>[supName175(r.s),projName175(r.p),r.workers,mins175(r.a),'','دائم'].map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))].join('\n');
    if(typeof download==='function') download('monthly-v175.csv',csv);
  };
  // Camera stability: avoid rerender/loading overlays while the OS camera picker is active.
  let cameraBusy=false; let busyTimer=null;
  function setCameraBusy(v){ cameraBusy=!!v; clearTimeout(busyTimer); if(v){ busyTimer=setTimeout(()=>{cameraBusy=false;},20000); } }
  window.__tasneefCameraBusy = ()=>cameraBusy;
  document.addEventListener('pointerdown',e=>{ const el=e.target; if(el && el.matches && el.matches('input[type="file"],button[data-camera],.camera-btn')) setCameraBusy(true); },true);
  document.addEventListener('click',e=>{ const el=e.target; if(el && el.matches && el.matches('input[type="file"]')) setCameraBusy(true); },true);
  document.addEventListener('change',e=>{ const el=e.target; if(el && el.matches && el.matches('input[type="file"]')) setTimeout(()=>setCameraBusy(false),650); },true);
  const oldShow=window.showTasneefLoading, oldHide=window.hideTasneefLoading;
  window.showTasneefLoading=function(message){ if(cameraBusy) return; return oldShow?oldShow(message):undefined; };
  window.hideTasneefLoading=function(){ cameraBusy=false; return oldHide?oldHide():undefined; };
  // Make file inputs safer on Android: keep capture but do not force reload/rerender.
  function stabilizeFileInputs(){
    document.querySelectorAll('input[type="file"]').forEach(inp=>{
      if(inp.__v175Stable) return; inp.__v175Stable=true;
      if(!inp.getAttribute('accept')) inp.setAttribute('accept','image/*');
      inp.addEventListener('click',()=>setCameraBusy(true),{capture:true});
      inp.addEventListener('change',()=>setTimeout(()=>setCameraBusy(false),650));
    });
  }
  document.addEventListener('DOMContentLoaded',stabilizeFileInputs); setInterval(stabilizeFileInputs,2500);
  // Faster page switching: render only the opened page instead of every heavy section.
  const pageRenderers={dashboard:['renderDashboard','renderAlerts'],daily:['renderTimeLogs'],users:['renderUsers'],projects:['renderProjects'],workers:['renderWorkers'],attendance:['renderAttendance'],monthly:['renderMonthly'],tickets:['renderTickets'],contracts:['renderContractServices'],financeDashboard:['financeRenderAll'],premiumReports:['renderPremiumReports'],clientRatings:['renderClientRatings']};
  window.showPage=function(id,btn){
    document.querySelectorAll('.page').forEach(p=>p.classList.add('hidden'));
    const page=$id(id); if(page) page.classList.remove('hidden');
    document.querySelectorAll('.nav').forEach(n=>n.classList.remove('active')); if(btn) btn.classList.add('active');
    if(cameraBusy) return;
    (pageRenderers[id]||[]).forEach(fn=>{ try{ if(typeof window[fn]==='function') window[fn](); }catch(e){ console.warn('V175 page renderer',fn,e); } });
    if(id==='contracts' && typeof window.showContractsSubTab==='function') window.showContractsSubTab('services');
  };
  // Prevent stuck loading screens.
  window.addEventListener('pageshow',()=>{ try{ if(window.hideTasneefLoading) window.hideTasneefLoading(); }catch(_){ } });
  window.addEventListener('focus',()=>{ if(cameraBusy) setTimeout(()=>setCameraBusy(false),900); });
  console.log('V175 System Stability Rebuild loaded');
})();


/* ===== V177 Monthly Permanent Projects Final Layout ===== */
(function(){
  'use strict';
  if(window.__v177MonthlyPermanentFinal) return;
  window.__v177MonthlyPermanentFinal = true;
  const $id = (id)=>document.getElementById(id);
  const esc = (v)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const num = (v)=>{ const x=Number(v||0); return Number.isFinite(x)?x:0; };
  const minutesText = (m)=> `${Math.round(num(m)).toLocaleString('en-US')} دقيقة`;
  const pctText = (p)=> `${Number(num(p)).toLocaleString('en-US',{minimumFractionDigits:1,maximumFractionDigits:1})}%`;
  const dateOfLog = (l)=> String(l.log_date || l.date || l.check_in || l.created_at || '').slice(0,10);
  const monthOfLog = (l)=> dateOfLog(l).slice(0,7);
  const projectNameSafe = (id)=> (typeof window.projectName==='function' ? window.projectName(id) : '') || (window.data?.projects||[]).find(p=>String(p.id)===String(id))?.name || '-';
  const supervisorNameSafe = (id)=> (typeof window.supervisorName==='function' ? window.supervisorName(id) : '') || (window.data?.supervisors||[]).find(s=>String(s.id)===String(id))?.full_name || (window.data?.users||[]).find(u=>String(u.id)===String(id))?.full_name || '-';
  const projectRow = (id)=> (window.data?.projects||[]).find(p=>String(p.id)===String(id)) || {};
  function actualMinutes(l){
    try{ if(typeof window.logActualMinutes==='function') return num(window.logActualMinutes(l)); }catch(_){ }
    if(l.duration_minutes) return num(l.duration_minutes);
    if(l.actual_minutes) return num(l.actual_minutes);
    const inn = l.check_in || l.in_time || '';
    const out = l.check_out || l.out_time || '';
    if(inn && out){
      const toM = (t)=>{ const s=String(t).slice(-5); const [h,m]=s.split(':').map(Number); return h*60+m; };
      let a=toM(inn), b=toM(out); if(Number.isFinite(a)&&Number.isFinite(b)){ if(b<a) b+=1440; return Math.max(0,b-a); }
    }
    return 0;
  }
  function requiredMinutes(l){
    try{ if(typeof window.logRequiredMinutes==='function') return num(window.logRequiredMinutes(l)); }catch(_){ }
    return num(l.required_minutes || l.daily_required_minutes || l.required_daily_minutes || 0);
  }
  function workerNames(pid){
    try{ if(typeof window.uniqueWorkersForProjectTextV60==='function'){ const s=window.uniqueWorkersForProjectTextV60(pid); if(s) return s; } }catch(_){ }
    const ws=(window.data?.workers||[]).filter(w=>String(w.project_id||w.current_project_id||w.assigned_project_id||'')===String(pid) || String(w.project||'')===String(pid));
    return ws.map(w=>w.name||w.full_name).filter(Boolean).join('، ') || '-';
  }
  function projectDailyRequired(pid, maxReq){
    const p=projectRow(pid);
    const vals=[p.required_daily_minutes,p.daily_required_minutes,p.required_minutes,p.daily_minutes,p.monthly_daily_minutes,maxReq].map(num).filter(Boolean);
    return vals.length ? Math.max(...vals) : 0;
  }
  function isPermanentProject(pid, maxReq){
    const p=projectRow(pid);
    const text=[p.name,p.type,p.category,p.contract_type,p.operation_type,p.notes,p.service_type,p.status].join(' ');
    const req=projectDailyRequired(pid,maxReq);
    return req>=540 || /دائم|دائمة|دوام|٩ ساعات|9 ساعات|9h|full.?time|عمالة دائمة/i.test(text);
  }
  function monthRange(month){
    const [y,m]=String(month).split('-').map(Number);
    const last=new Date(y,m,0).getDate();
    return {start:`${month}-01`,end:`${month}-${String(last).padStart(2,'0')}`,days:last};
  }
  function buildMonthlyV177(){
    const month=$id('monthlyMonth')?.value || new Date().toISOString().slice(0,7);
    const sid=$id('monthlySupervisor')?.value || '';
    let logs=(window.data?.logs||[]).filter(l=>monthOfLog(l)===month);
    if(sid) logs=logs.filter(l=>String(l.supervisor_id)===String(sid));
    const groups=new Map();
    logs.forEach(l=>{
      const key=`${l.supervisor_id||''}_${l.project_id||''}`;
      if(!groups.has(key)) groups.set(key,{s:l.supervisor_id,p:l.project_id,actual:0,requiredMax:0,records:0,workers:''});
      const g=groups.get(key);
      g.actual += actualMinutes(l);
      g.requiredMax = Math.max(g.requiredMax, requiredMinutes(l));
      g.records += 1;
    });
    const daily=[], permanent=[];
    [...groups.values()].forEach(g=>{
      g.workers=workerNames(g.p);
      g.projectName=projectNameSafe(g.p);
      g.supervisorName=supervisorNameSafe(g.s);
      if(isPermanentProject(g.p,g.requiredMax)) permanent.push(g); else daily.push(g);
    });
    const totalsBySup={};
    daily.forEach(r=>{ const k=String(r.s||''); totalsBySup[k]=(totalsBySup[k]||0)+num(r.actual); });
    const bySup={};
    daily.forEach(r=>{ const k=String(r.s||''); (bySup[k]||(bySup[k]=[])).push(r); });
    Object.keys(bySup).forEach(s=>{
      bySup[s].sort((a,b)=>String(a.projectName).localeCompare(String(b.projectName),'ar'));
      let used=0;
      bySup[s].forEach((r,i)=>{
        const total=totalsBySup[s]||0;
        let p= total ? (r.actual/total)*100 : 0;
        if(i < bySup[s].length-1){ p = Math.round(p*10)/10; used += p; }
        else { p = Math.max(0, Math.round((100-used)*10)/10); }
        r.percent=p;
      });
    });
    const dailySorted=Object.keys(bySup).sort((a,b)=>String(supervisorNameSafe(a)).localeCompare(String(supervisorNameSafe(b)),'ar')).flatMap(s=>bySup[s]);
    permanent.sort((a,b)=>String(a.projectName).localeCompare(String(b.projectName),'ar'));
    return {month,range:monthRange(month),daily:dailySorted,permanent,totalsBySup};
  }
  window.monthlyRowsV177 = buildMonthlyV177;
  window.renderMonthly = function(){
    const body=$id('monthlyBody'); if(!body) return;
    const table=body.closest('table');
    if(table?.tHead) table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الوقت بالدقائق</th><th>النسبة المئوية</th><th>حالة الأداء</th></tr>';
    const res=buildMonthlyV177();
    if(!res.daily.length){ body.innerHTML='<tr><td colspan="6">لا توجد بيانات في مشاريع الزيارة اليومية لهذا الشهر</td></tr>'; }
    else{
      let html='', last=null, total=0, pctSum=0;
      const flush=()=>{ if(last!==null){ html += `<tr class="monthly-total-row" style="background:#f4fbf8;font-weight:900"><td colspan="3">مجموع ${esc(supervisorNameSafe(last))}</td><td>${minutesText(total)}</td><td><span class="badge green">${pctText(100)}</span></td><td>—</td></tr>`; }};
      res.daily.forEach(r=>{
        if(last!==null && String(last)!==String(r.s)){ flush(); total=0; pctSum=0; }
        last=r.s; total+=num(r.actual); pctSum+=num(r.percent);
        html += `<tr><td>${esc(r.supervisorName)}</td><td>${esc(r.projectName)}</td><td>${esc(r.workers||'-')}</td><td>${minutesText(r.actual)}</td><td><span class="badge green">${pctText(r.percent)}</span></td><td><span class="badge green">مكتمل</span></td></tr>`;
      });
      flush(); body.innerHTML=html;
    }
    const totalDaily=res.daily.reduce((a,r)=>a+num(r.actual),0), totalPerm=res.permanent.reduce((a,r)=>a+num(r.actual),0);
    const supCount=new Set(res.daily.map(r=>String(r.s||''))).size;
    if($id('monthlySummary')) $id('monthlySummary').innerHTML=`<div class="kpi"><small>مشاريع الزيارة اليومية</small><b>${res.daily.length}</b></div><div class="kpi"><small>مشاريع العمالة الدائمة</small><b>${res.permanent.length}</b></div><div class="kpi"><small>مجموع دقائق الزيارة</small><b>${minutesText(totalDaily)}</b></div><div class="kpi"><small>مجموع النسبة</small><b>100%</b></div><div class="kpi"><small>دقائق الدائمة</small><b>${minutesText(totalPerm)}</b></div><div class="kpi"><small>عدد المشرفين</small><b>${supCount}</b></div>`;
  };
  function permanentBlocks(permanent){
    const cards=permanent.map(r=>{
      const workers=String(r.workers||'-').split(/[،,\n]/).map(x=>x.trim()).filter(Boolean);
      const workerLines=(workers.length?workers:['-']).map(w=>`<div class="worker-line">${esc(w)}</div>`).join('');
      return `<div class="perm-col"><div class="perm-project">${esc(r.projectName)}</div><div class="perm-meta">المشرف: ${esc(r.supervisorName)}</div><div class="perm-workers">${workerLines}</div><div class="perm-mins">${minutesText(r.actual)}</div></div>`;
    }).join('');
    return `<section class="permanent-section"><div class="month-title">${esc((buildMonthlyV177().month||''))}</div><h2>مشاريع العمالة الدائمة</h2><div class="perm-wrap">${cards || '<div class="empty">لا توجد مشاريع دائمة 9 ساعات أو أكثر خلال هذا الشهر</div>'}</div></section>`;
  }
  window.printMonthlyReportV57 = window.printMonthlyReportV177 = function(){
    const res=buildMonthlyV177();
    const bySup={}; res.daily.forEach(r=>{ const k=String(r.s||''); (bySup[k]||(bySup[k]=[])).push(r); });
    const supCards=Object.keys(bySup).sort((a,b)=>String(supervisorNameSafe(a)).localeCompare(String(supervisorNameSafe(b)),'ar')).map(s=>{
      const rows=bySup[s]; const total=rows.reduce((a,r)=>a+num(r.actual),0);
      const trs=rows.map(r=>`<tr><td>${esc(r.projectName)}</td><td>${minutesText(r.actual)}</td><td>${pctText(r.percent)}</td></tr>`).join('');
      const workers=[...new Set(rows.flatMap(r=>String(r.workers||'').split(/[،,\n]/).map(x=>x.trim()).filter(Boolean)))].join('، ') || '-';
      return `<div class="sup-card"><div class="sup-head"><b>${esc(supervisorNameSafe(s))}</b><span>100%</span></div><table><thead><tr><th>المشروع</th><th>الدقائق</th><th>النسبة</th></tr></thead><tbody>${trs}<tr class="sum-row"><td>المجموع</td><td>${minutesText(total)}</td><td>100%</td></tr></tbody></table><div class="workers"><b>أسماء العمال</b><p>${esc(workers)}</p></div></div>`;
    }).join('');
    const totalDaily=res.daily.reduce((a,r)=>a+num(r.actual),0), totalPerm=res.permanent.reduce((a,r)=>a+num(r.actual),0);
    const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>
      @page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;color:#153d33;margin:0;background:#fff;font-size:11px;-webkit-print-color-adjust:exact;print-color-adjust:exact}.page{padding:14px;border:2px solid #0a5a49;min-height:100vh}.top{display:flex;justify-content:space-between;align-items:center;border-bottom:3px solid #0a5a49;padding-bottom:10px}.brand{display:flex;align-items:center;gap:10px}.logo{width:58px;height:58px;border-radius:18px;border:2px solid #c7a24d;display:grid;place-items:center;font-weight:900;color:#0a5a49;background:#f7fbfa}.brand h1{margin:0;font-size:20px}.title{text-align:left}.title h2{font-size:26px;margin:0;color:#0a5a49}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:12px 0}.box{border:1px solid #d9e6e1;border-radius:13px;padding:10px;text-align:center;background:#fff}.box small{display:block;color:#697b74}.box b{font-size:16px;color:#0a5a49}.block{margin-top:12px}.block h2,.permanent-section h2{margin:0 0 8px;text-align:center;color:#fff;background:#0a5a49;border:2px solid #c7a24d;border-radius:999px;padding:7px 35px;display:block}.sup-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.sup-card{border:2px solid #0a5a49;border-radius:12px;overflow:hidden;background:#fff;break-inside:avoid}.sup-head{display:flex;justify-content:space-between;padding:8px;background:#f7fbfa;color:#0a5a49;border-bottom:1px solid #d9e6e1}table{width:100%;border-collapse:collapse}th{background:#0a5a49;color:white;padding:6px}td{border:1px solid #e2ece8;padding:5px;text-align:center}.sum-row td{font-weight:900;background:#edf8f3}.workers{padding:8px;text-align:center}.workers p{margin:4px 0 0;line-height:1.7}.permanent-section{margin-top:14px;break-before:auto}.month-title{text-align:center;font-weight:900;color:#0a5a49;margin:8px auto 6px;border:1px solid #c7a24d;border-radius:999px;width:max-content;padding:5px 28px;background:#fffaf0}.perm-wrap{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;border:2px solid #0a5a49;border-radius:13px;padding:8px;background:#f7fbfa}.perm-col{background:#fff;border:1px solid #0a5a49;border-radius:10px;overflow:hidden;text-align:center;min-height:145px;break-inside:avoid}.perm-project{background:#0a5a49;color:#fff;font-weight:900;padding:9px}.perm-meta{padding:6px;color:#0a5a49;font-weight:700;border-bottom:1px solid #e2ece8}.perm-workers{padding:7px;min-height:58px}.worker-line{padding:3px 0;border-bottom:1px dashed #e5eee9}.worker-line:last-child{border-bottom:0}.perm-mins{display:inline-block;margin:6px auto 10px;border-radius:999px;background:#edf8f3;color:#0a5a49;padding:5px 15px;font-weight:900}.empty{grid-column:1/-1;text-align:center;border:1px dashed #aabdb6;border-radius:12px;padding:20px;color:#697b74;background:#fff}.foot{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:14px}.sign{border:1px solid #d9e6e1;border-radius:12px;padding:10px;min-height:70px}.note{margin-top:8px;text-align:center;color:#697b74}@media print{.page{border-radius:0}.sup-card,.perm-col,.sign{break-inside:avoid}}
    </style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h1>شركة تصنيف لإدارة المرافق</h1><small>تقرير تشغيل شهري</small></div></div><div class="title"><h2>تقرير الأوقات الشهرية</h2><p>الشهر: ${esc(res.month)}</p></div></div><div class="meta"><div class="box"><small>مشاريع الزيارة اليومية</small><b>${res.daily.length}</b></div><div class="box"><small>مجموع دقائق الزيارة</small><b>${minutesText(totalDaily)}</b></div><div class="box"><small>مجموع النسبة</small><b>100%</b></div><div class="box"><small>مشاريع العمالة الدائمة</small><b>${res.permanent.length}</b></div></div><section class="block"><h2>مشاريع الزيارة اليومية</h2><div class="sup-grid">${supCards || '<div class="empty">لا توجد بيانات</div>'}</div></section>${permanentBlocks(res.permanent)}<div class="foot"><div class="sign"><b>إعداد</b><br><br>الاسم: ____________</div><div class="sign"><b>مراجعة</b><br><br>الاسم: ____________</div><div class="sign"><b>اعتماد</b><br><br>الاسم: ____________</div></div><div class="note">تم توليد التقرير من نظام شركة تصنيف لإدارة المرافق. مشاريع العمالة الدائمة منفصلة ولا تدخل في نسب الزيارة اليومية.</div></div><script>window.onload=function(){setTimeout(function(){window.print()},450)}</script></body></html>`;
    const w=window.open('','_blank'); if(!w){ if(typeof msg==='function') msg('اسمح بالنوافذ المنبثقة للطباعة','err'); return; } w.document.write(html); w.document.close();
  };
  window.exportMonthlyCSV = function(){
    const res=buildMonthlyV177();
    const lines=['المشرف,المشروع,أسماء العمال,الدقائق,النسبة,نوع المشروع'];
    res.daily.forEach(r=>lines.push([r.supervisorName,r.projectName,r.workers,Math.round(r.actual),pctText(r.percent),'زيارة يومية'].map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(',')));
    res.permanent.forEach(r=>lines.push([r.supervisorName,r.projectName,r.workers,Math.round(r.actual),'','عمالة دائمة'].map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(',')));
    if(typeof download==='function') download('monthly-v177.csv', lines.join('\n'));
  };
  window.addEventListener('load',()=>setTimeout(()=>{ try{ if($id('monthlyBody')) window.renderMonthly(); }catch(e){ console.warn('V177 monthly render',e); } },900));
  console.log('V177 Monthly Permanent Projects Final Layout loaded');
})();


/* ===== V178: Inventory Suppliers + Operations Consumption Flow + stable tabs ===== */
(function(){
  'use strict';
  const $v = (id)=>document.getElementById(id);
  const E = (s)=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const N = (v)=>{ const n=Number(String(v??0).replace(/,/g,'').trim()); return Number.isFinite(n)?n:0; };
  const role = ()=>{ try{return (JSON.parse(localStorage.getItem('tasneef_session')||'{}').role)||'';}catch(_){return '';} };
  const isWarehouseOnly = ()=> role()==='warehouse_manager';
  const supplierSet = ()=>{
    const s=new Set();
    (window.data?.inventoryItems||[]).forEach(i=>{ if(i.supplier) s.add(String(i.supplier).trim()); });
    (window.data?.financeExpenses||[]).forEach(e=>{ if(e.supplier) s.add(String(e.supplier).trim()); });
    (window.stockBatchesV148||[]).forEach(b=>{ if(b.supplier) s.add(String(b.supplier).trim()); });
    return [...s].filter(Boolean).sort((a,b)=>a.localeCompare(b,'ar'));
  };
  function projectNameLocal(id){ try{return typeof projectName==='function'?projectName(id):'';}catch(_){return '';} }
  function supervisorNameLocal(id){ try{return typeof supervisorName==='function'?supervisorName(id):'';}catch(_){return '';} }
  function itemById(id){ return (window.data?.inventoryItems||[]).find(i=>String(i.id)===String(id))||{}; }
  function productCode(i){ return i?.serial_number || i?.product_code || i?.barcode || i?.supplier_barcode || ''; }
  function lineItems(r){
    let raw=r?.request_items || r?.items || r?.request_lines || [];
    if(typeof raw==='string' && raw.trim()){ try{ raw=JSON.parse(raw); }catch(_){ raw=[]; } }
    if(Array.isArray(raw) && raw.length) return raw.map(x=>({
      item_id:x.item_id||x.id,
      item_name:x.item_name||x.name||'',
      product_code:x.product_code||x.serial_number||'',
      quantity:N(x.quantity),
      unit:x.unit||'',
      unit_cost:N(x.unit_cost)
    })).filter(x=>x.item_id && x.quantity>0);
    return r?.item_id ? [{item_id:r.item_id,item_name:r.item_name||'',product_code:r.product_code||'',quantity:N(r.quantity),unit:'',unit_cost:0}] : [];
  }
  function financeTabId(tab){ return 'financeTab'+String(tab||'').charAt(0).toUpperCase()+String(tab||'').slice(1); }
  function ensureFinanceTabsVisibleV178(){
    const tabsWrap=document.querySelector('#financeDashboard .finance-tabs');
    if(!tabsWrap) return;
    document.body.classList.toggle('warehouse-manager-view-v178', isWarehouseOnly());
    const allTabs=[...tabsWrap.querySelectorAll('.finance-tab')];
    allTabs.forEach(btn=>{ btn.style.display='inline-flex'; btn.hidden=false; });
    document.querySelectorAll('.finance-tab-page').forEach(p=>{ p.hidden=false; });
    if(isWarehouseOnly()){
      allTabs.forEach(btn=>{
        const t=(btn.getAttribute('onclick')||'');
        const keep=/requests|catalog|items/.test(t) || /طلبات الصرف|الأصناف/.test(btn.textContent||'');
        btn.style.display=keep?'inline-flex':'none';
      });
      // Keep only current page visible is handled by financeShowTab. We only limit accessible tabs, not pages by CSS.
    }
  }
  function ensureSuppliersTabV178(){
    const tabs=document.querySelector('#financeDashboard .finance-tabs');
    const reportsPage=$v('financeTabReports');
    if(!tabs || $v('financeSuppliersTabV178')) return;
    const btn=document.createElement('button');
    btn.id='financeSuppliersTabV178';
    btn.type='button';
    btn.className='light finance-tab';
    btn.textContent='الموردين';
    btn.onclick=function(){ window.financeShowTab && window.financeShowTab('suppliers',this); };
    const reportsBtn=[...tabs.querySelectorAll('button')].find(b=>/reports/.test(b.getAttribute('onclick')||''));
    tabs.insertBefore(btn, reportsBtn||null);
    const page=document.createElement('div');
    page.id='financeTabSuppliers';
    page.className='finance-tab-page hidden';
    page.innerHTML=`
      <div class="card">
        <div class="table-head"><h2>الموردين</h2><button class="light" onclick="renderSuppliersV178()">تحديث</button></div>
        <div class="report-filters-panel">
          <input id="supplierSearchV178" list="supplierNamesV178" oninput="renderSuppliersV178()" placeholder="بحث باسم المورد أو رقم الفاتورة أو الصنف">
          <datalist id="supplierNamesV178"></datalist>
        </div>
        <div id="supplierCardsV178" class="v178-supplier-grid"></div>
      </div>`;
    if(reportsPage && reportsPage.parentNode) reportsPage.parentNode.insertBefore(page,reportsPage); else document.querySelector('#financeDashboard')?.appendChild(page);
  }
  function hydrateSupplierInputsV178(){
    const vals=supplierSet();
    let dl=$v('supplierNamesV178');
    if(!dl){ dl=document.createElement('datalist'); dl.id='supplierNamesV178'; document.body.appendChild(dl); }
    dl.innerHTML=vals.map(v=>`<option value="${E(v)}"></option>`).join('');
    ['batchSupplierV148','financeExpenseSupplier','inventoryItemSupplier','inventorySupplierFilter','inventoryReportSupplier','supplierSearchV178'].forEach(id=>{
      const el=$v(id); if(!el) return;
      if(el.tagName==='INPUT') el.setAttribute('list','supplierNamesV178');
      if(el.tagName==='SELECT'){
        const old=el.value;
        el.innerHTML='<option value="">كل الموردين</option>'+vals.map(v=>`<option value="${E(v)}">${E(v)}</option>`).join('');
        if(vals.includes(old)) el.value=old;
      }
    });
  }
  window.renderSuppliersV178=function(){
    hydrateSupplierInputsV178();
    const box=$v('supplierCardsV178'); if(!box) return;
    const q=String($v('supplierSearchV178')?.value||'').trim().toLowerCase();
    const map={};
    function rec(name){ name=String(name||'غير محدد').trim()||'غير محدد'; if(!map[name]) map[name]={name,items:0,batches:0,expenses:0,totalPurchases:0,totalExpenses:0,invoices:[]}; return map[name]; }
    (window.data?.inventoryItems||[]).forEach(i=>{ const r=rec(i.supplier); r.items++; });
    (window.stockBatchesV148||[]).forEach(b=>{ const r=rec(b.supplier); r.batches++; r.totalPurchases+=N(b.total_with_vat); r.invoices.push(b.invoice_no||b.id||'-'); });
    (window.data?.financeExpenses||[]).forEach(e=>{ const r=rec(e.supplier); r.expenses++; r.totalExpenses+=N(e.total); });
    let rows=Object.values(map);
    if(q) rows=rows.filter(r=>[r.name,...r.invoices].join(' ').toLowerCase().includes(q));
    rows.sort((a,b)=>(b.totalPurchases+b.totalExpenses)-(a.totalPurchases+a.totalExpenses));
    box.innerHTML=rows.map(r=>`<article class="v178-supplier-card"><h3>${E(r.name)}</h3><div class="v178-supplier-stats"><div><small>الأصناف</small><b>${r.items}</b></div><div><small>الفواتير</small><b>${r.batches}</b></div><div><small>المصروفات</small><b>${r.expenses}</b></div><div><small>إجمالي المشتريات</small><b>${(r.totalPurchases||0).toFixed(2)}</b></div></div><button class="light" onclick="filterSupplierInvoicesV178('${E(r.name)}')">عرض فواتيره</button></article>`).join('') || '<div class="empty">لا توجد بيانات موردين</div>';
  };
  window.filterSupplierInvoicesV178=function(name){
    if($v('stockSupplierFilterV149')) $v('stockSupplierFilterV149').value=name;
    if(window.financeShowTab){ const btn=[...document.querySelectorAll('.finance-tab')].find(b=>/inventory/.test(b.getAttribute('onclick')||'')); window.financeShowTab('inventory',btn); }
    if(window.renderStockBatchCardsV149) setTimeout(window.renderStockBatchCardsV149,60);
  };
  function ensureCssV178(){ if($v('v178Css')) return; const st=document.createElement('style'); st.id='v178Css'; st.textContent=`
    .v178-supplier-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:14px}.v178-supplier-card{border:1px solid #cfe3dd;border-radius:20px;background:#fff;padding:16px;box-shadow:0 10px 22px rgba(0,45,31,.06)}.v178-supplier-card h3{margin:0 0 10px;color:#063d31}.v178-supplier-stats{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-bottom:12px}.v178-supplier-stats div{background:#f2fbf7;border:1px solid #dbeee7;border-radius:12px;padding:8px;text-align:center}.v178-supplier-stats small{display:block;color:#637970}.v178-supplier-stats b{color:#064534}.warehouse-manager-view-v178 #financeSuppliersTabV178{display:none!important}.warehouse-manager-view-v178 #financeTabSuppliers{display:none!important}
  `; document.head.appendChild(st); }
  // Stable finance tab controller. It does not rebuild tab buttons, so buttons do not disappear.
  const oldFinanceShow = window.financeShowTab;
  window.financeShowTab=function(tab,btn){
    ensureFinanceTabsVisibleV178();
    ensureSuppliersTabV178();
    if(isWarehouseOnly() && !['requests','catalog','items'].includes(tab)) tab='requests';
    window.financeCurrentTab=tab;
    document.querySelectorAll('.finance-tab-page').forEach(x=>x.classList.add('hidden'));
    const page=$v(financeTabId(tab)); if(page) page.classList.remove('hidden');
    document.querySelectorAll('.finance-tab').forEach(x=>x.classList.remove('active'));
    if(btn) btn.classList.add('active'); else {
      const b=[...document.querySelectorAll('.finance-tab')].find(x=>(x.getAttribute('onclick')||'').includes(`'${tab}'`)); b?.classList.add('active');
    }
    if(!window.financeLoaded && typeof window.financeLoadAll==='function') window.financeLoadAll();
    if(typeof window.financeRenderAll==='function') window.financeRenderAll();
    ensureFinanceTabsVisibleV178();
    if(tab==='suppliers') renderSuppliersV178();
  };
  // Operations manager only approval flow. Request moves to ops and gets issued once.
  window.inventoryDefaultApprovalPath=function(){ return ['ops']; };
  window.inventoryGetApprovalPath=function(){ return ['ops']; };
  window.inventoryLoadApprovalSettings=function(){
    ['approvalWarehouse','approvalFinance','approvalGeneral'].forEach(id=>{ if($v(id)) $v(id).checked=false; });
    if($v('approvalOps')) $v('approvalOps').checked=true;
    ['approvalWarehouseOrder','approvalFinanceOrder','approvalGeneralOrder'].forEach(id=>{ if($v(id)) $v(id).value=''; });
    if($v('approvalOpsOrder')) $v('approvalOpsOrder').value=1;
    if($v('inventoryApprovalPreview')) $v('inventoryApprovalPreview').innerHTML='<b>المسار الحالي:</b> 1. مدير تشغيلي';
  };
  window.inventorySaveApprovalSettings=function(){ localStorage.setItem('tasneef_inventory_approval_path',JSON.stringify(['ops'])); window.inventoryLoadApprovalSettings(); if(typeof msg==='function') msg('تم تثبيت مسار الاعتماد: مدير التشغيل فقط'); };
  window.inventoryCurrentUserCanApprove=function(step){ const r=role(); return step==='ops' && ['admin','general_manager','operations_manager'].includes(r); };
  // Make invoice number manual / required.
  const oldSaveBatch=window.stockBatchSaveV148;
  if(oldSaveBatch && !oldSaveBatch.v178Wrapped){ window.stockBatchSaveV148=async function(btn){
    const inv=String($v('batchInvoiceV148')?.value||'').trim();
    if(!inv){ if(typeof msg==='function') msg('اكتب رقم الفاتورة يدويًا قبل الحفظ','err'); $v('batchInvoiceV148')?.focus(); return; }
    return oldSaveBatch.apply(this,arguments);
  }; window.stockBatchSaveV148.v178Wrapped=true; }
  // Consumption registration after issuing. This separates issued from actually consumed.
  window.openRequestConsumptionV178=function(id){
    const r=(window.data?.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) return msg('الطلب غير موجود','err');
    const lines=lineItems(r); const projects=(window.data?.projects||[]).map(p=>`<option value="${E(p.id)}">${E(p.name)}</option>`).join('');
    const workers=(window.data?.workers||[]).map(w=>`<option value="${E(w.id)}">${E(w.name||w.full_name||w.username||'عامل')}</option>`).join('');
    const rows=lines.map((l,i)=>`<tr><td>${E(l.product_code||'')}</td><td>${E(l.item_name)}</td><td>${N(l.quantity)}</td><td><input type="number" step="0.01" min="0" max="${N(l.quantity)}" id="consQtyV178_${i}" value="0"></td><td><select id="consProjectV178_${i}"><option value="">اختر المشروع</option>${projects}</select></td><td><select id="consWorkerV178_${i}"><option value="">اختر العامل / الفني</option>${workers}</select></td></tr>`).join('');
    const m=document.createElement('div'); m.id='consumptionModalV178'; m.className='smart-invoice-backdrop'; m.innerHTML=`<div class="smart-invoice-modal"><div class="smart-invoice-top"><h2>تسجيل الاستهلاك الفعلي</h2><button onclick="document.getElementById('consumptionModalV178')?.remove()">إغلاق</button></div><div class="smart-invoice-body"><p>أمر الصرف: REQ-${E(id)} — المستلم: ${E(r.supervisor_name||'')}</p><div class="table-wrap"><table><thead><tr><th>الكود</th><th>الصنف</th><th>المصروف</th><th>المستهلك</th><th>المشروع</th><th>المستخدم الفعلي</th></tr></thead><tbody>${rows}</tbody></table></div><div class="actions"><button onclick="saveRequestConsumptionV178('${E(id)}')">حفظ الاستهلاك</button></div></div></div>`;
    document.body.appendChild(m);
  };
  window.saveRequestConsumptionV178=async function(id){
    try{
      const r=(window.data?.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) throw new Error('الطلب غير موجود');
      const lines=lineItems(r); let saved=0;
      for(let i=0;i<lines.length;i++){
        const l=lines[i], qty=N($v('consQtyV178_'+i)?.value), pid=$v('consProjectV178_'+i)?.value, wid=$v('consWorkerV178_'+i)?.value;
        if(qty<=0) continue;
        if(qty>N(l.quantity)) throw new Error('كمية الاستهلاك أكبر من المصروف للصنف: '+l.item_name);
        if(!pid) throw new Error('اختر المشروع للصنف: '+l.item_name);
        const item=itemById(l.item_id);
        const worker=(window.data?.workers||[]).find(w=>String(w.id)===String(wid))||{};
        const movement={item_id:Number(l.item_id),item_name:l.item_name,movement_type:'consume',quantity:qty,movement_date:(typeof today==='function'?today():new Date().toISOString().slice(0,10)),project_id:Number(pid),project_name:projectNameLocal(pid),receiver:worker.name||worker.full_name||r.supervisor_name||'',reason:'تسجيل استهلاك فعلي لأمر صرف '+id,notes:'consumption:req:'+id,request_id:Number(id),product_code:l.product_code||productCode(item),unit_cost:N(l.unit_cost||item.unit_cost)};
        const res=await sb.from('inventory_movements').insert(movement); if(res.error) throw res.error; saved++;
      }
      if(!saved) throw new Error('لم يتم إدخال أي كمية استهلاك');
      document.getElementById('consumptionModalV178')?.remove();
      if(typeof msg==='function') msg('تم تسجيل الاستهلاك الفعلي');
      if(typeof financeLoadAll==='function') await financeLoadAll(true);
    }catch(e){ if(typeof msg==='function') msg(e.message||String(e),'err'); else alert(e.message||e); }
  };
  // Render requests with consumption button and stable tab behavior.
  window.inventoryRenderRequests=function(){
    const b=$v('inventoryRequestsBody'); if(!b) return;
    let rows=(window.data?.inventoryRequests||[]).slice();
    try{ if(typeof financeFilterRows==='function') rows=financeFilterRows(rows,'request_date'); }catch(_){ }
    const st=$v('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
    b.innerHTML=rows.map(r=>{
      const lines=lineItems(r); const itemsHtml=lines.map(l=>`${E(l.product_code||'')} ${E(l.item_name)} × ${N(l.quantity)}`).join('<br>') || E(r.item_name||'');
      const totalQty=lines.reduce((a,l)=>a+N(l.quantity),0)||N(r.quantity);
      const actions=[];
      if(String(r.status||'').startsWith('pending_')){
        const step=String(r.status).replace('pending_','');
        if(window.inventoryCurrentUserCanApprove && window.inventoryCurrentUserCanApprove(step)){
          actions.push(`<button onclick="inventoryApproveRequest('${E(r.id)}','${E(step)}',this)">اعتماد مدير التشغيل</button>`);
          actions.push(`<button class="danger" onclick="inventoryRejectRequest('${E(r.id)}',this)">رفض</button>`);
        } else actions.push(`<span class="badge neutral">بانتظار مدير التشغيل</span>`);
      }
      if(r.status==='approved') actions.push(`<button class="light" onclick="openRequestConsumptionV178('${E(r.id)}')">تسجيل استهلاك</button>`);
      actions.unshift(`<button class="light" onclick="inventoryPrintRequest('${E(r.id)}')">طباعة</button>`);
      return `<tr><td>${E(r.request_date||'')}</td><td><b>${E(r.supervisor_name||supervisorNameLocal(r.supervisor_id))}</b></td><td>${E(r.project_name||projectNameLocal(r.project_id))}</td><td>${itemsHtml}</td><td>${N(totalQty)}</td><td>${E(r.reason||'-')}</td><td><span class="badge ${typeof inventoryRequestStatusClass==='function'?inventoryRequestStatusClass(r.status):''}">${typeof inventoryRequestStatusText==='function'?inventoryRequestStatusText(r.status):E(r.status||'-')}</span><br><small>${String(r.status||'').startsWith('pending_')?'المرحلة: مدير التشغيل':'-'}</small></td><td class="row-actions">${actions.join(' ')||'-'}</td></tr>`;
    }).join('') || `<tr><td colspan="8">لا توجد طلبات صرف</td></tr>`;
  };
  // Refresh/hydrate wrappers.
  const oldHydrate=window.financeHydrateForms;
  if(oldHydrate && !oldHydrate.v178Wrapped){ window.financeHydrateForms=function(){ const out=oldHydrate.apply(this,arguments); ensureCssV178(); ensureSuppliersTabV178(); hydrateSupplierInputsV178(); ensureFinanceTabsVisibleV178(); const inv=$v('batchInvoiceV148'); if(inv){inv.placeholder='اكتب رقم الفاتورة يدويًا'; inv.required=true;} const sup=$v('batchSupplierV148'); if(sup) sup.setAttribute('list','supplierNamesV178'); return out; }; window.financeHydrateForms.v178Wrapped=true; }
  const oldRenderAll=window.financeRenderAll;
  if(oldRenderAll && !oldRenderAll.v178Wrapped){ window.financeRenderAll=function(){ const out=oldRenderAll.apply(this,arguments); ensureCssV178(); ensureSuppliersTabV178(); hydrateSupplierInputsV178(); ensureFinanceTabsVisibleV178(); if(!$v('financeTabSuppliers')?.classList.contains('hidden')) renderSuppliersV178(); return out; }; window.financeRenderAll.v178Wrapped=true; }
  window.addEventListener('load',()=>setTimeout(()=>{ ensureCssV178(); ensureSuppliersTabV178(); hydrateSupplierInputsV178(); ensureFinanceTabsVisibleV178(); localStorage.setItem('tasneef_inventory_approval_path',JSON.stringify(['ops'])); if(typeof inventoryLoadApprovalSettings==='function') inventoryLoadApprovalSettings(); },900));
  console.log('V178 Inventory Suppliers Consumption Flow loaded');
})();

/* ===== V179: Real admin finance tabs restore after Requests tab ===== */
(function(){
  'use strict';
  const $ = (id)=>document.getElementById(id);
  const clean = (s)=>String(s||'').replace(/\s+/g,' ').trim();
  function sessionRoleV179(){
    try{
      const raw = localStorage.getItem('tasneef_session') || sessionStorage.getItem('tasneef_session') || '{}';
      return (JSON.parse(raw).role || '').trim();
    }catch(_){ return ''; }
  }
  function isWarehouseV179(){ return sessionRoleV179() === 'warehouse_manager'; }
  function removeWrongWarehouseClasses(){
    if(isWarehouseV179()) return;
    document.body.classList.remove(
      'warehouse-manager-view-v151',
      'warehouse-manager-view-v162',
      'warehouse-manager-view-v163',
      'warehouse-manager-view-v164',
      'warehouse-manager-view-v178'
    );
  }
  function financePages(){ return [...document.querySelectorAll('#financeDashboard .finance-tab-page, .finance-tab-page')]; }
  function financeButtons(){ return [...document.querySelectorAll('#financeDashboard .finance-tabs .finance-tab, #financeDashboard .finance-tabs button, .finance-tabs .finance-tab')]; }
  function tabKeyFromButton(btn){
    const oc = btn.getAttribute('onclick') || '';
    const text = clean(btn.textContent);
    const m = oc.match(/financeShowTab\(['"]([^'"]+)['"]/);
    if(m) return m[1];
    const map = {
      'الملخص':'overview',
      'المصروفات':'expenses',
      'المخزون':'inventory',
      'الأصناف':'catalog',
      'حركة المخزون':'movements',
      'طلبات الصرف':'requests',
      'الموردين':'suppliers',
      'التقارير':'reports',
      'مراكز التكلفة':'costCenters',
      'تقليل التكلفة':'costReduction'
    };
    return map[text] || '';
  }
  function tabPageId(tab){
    if(tab === 'catalog' || tab === 'items') return 'financeTabCatalog';
    if(tab === 'suppliers') return 'financeTabSuppliers';
    return 'financeTab' + String(tab||'').charAt(0).toUpperCase() + String(tab||'').slice(1);
  }
  function showAllowedTabsV179(){
    const wh = isWarehouseV179();
    removeWrongWarehouseClasses();
    const allowedForWh = new Set(['requests','catalog','items']);
    financeButtons().forEach(btn=>{
      const key = tabKeyFromButton(btn);
      if(wh){
        const allow = allowedForWh.has(key) || /طلبات الصرف|الأصناف/.test(clean(btn.textContent));
        btn.style.setProperty('display', allow ? 'inline-flex' : 'none', 'important');
        btn.hidden = !allow;
      }else{
        btn.style.setProperty('display','inline-flex','important');
        btn.hidden = false;
      }
    });
  }
  function activateFinanceTabV179(tab, btn){
    if(isWarehouseV179() && !['requests','catalog','items'].includes(tab)) tab = 'requests';
    if(tab === 'items') tab = 'catalog';
    if(!tab) tab = 'overview';
    window.financeCurrentTab = tab;
    financePages().forEach(page=>page.classList.add('hidden'));
    const page = $(tabPageId(tab));
    if(page) page.classList.remove('hidden');
    financeButtons().forEach(x=>x.classList.remove('active'));
    if(btn) btn.classList.add('active');
    else {
      const target = financeButtons().find(b=>tabKeyFromButton(b)===tab || (tab==='catalog' && ['items','catalog'].includes(tabKeyFromButton(b))));
      target && target.classList.add('active');
    }
    showAllowedTabsV179();
  }
  function renderFinanceSectionV179(tab){
    try{
      if(!window.financeLoaded && typeof window.financeLoadAll === 'function') window.financeLoadAll();
      if(tab === 'requests' && typeof window.inventoryRenderRequests === 'function') window.inventoryRenderRequests();
      else if((tab === 'catalog' || tab === 'items') && typeof window.inventoryRenderItems === 'function') window.inventoryRenderItems();
      else if(tab === 'inventory'){
        if(typeof window.renderStockBatchCardsV149 === 'function') window.renderStockBatchCardsV149();
        else if(typeof window.inventoryRenderItems === 'function') window.inventoryRenderItems();
      }
      else if(tab === 'movements' && typeof window.inventoryRenderMovements === 'function') window.inventoryRenderMovements();
      else if(tab === 'reports' && typeof window.financeRenderReports === 'function') window.financeRenderReports();
      else if(tab === 'costCenters' && typeof window.costCenterRender === 'function') window.costCenterRender();
      else if(tab === 'costReduction' && typeof window.renderCostReductionV152 === 'function') window.renderCostReductionV152();
      else if(tab === 'suppliers' && typeof window.renderSuppliersV178 === 'function') window.renderSuppliersV178();
      else if(typeof window.financeRenderAll === 'function') window.financeRenderAll();
    }catch(e){ console.warn('V179 finance render warning', e); }
    showAllowedTabsV179();
    setTimeout(showAllowedTabsV179, 80);
    setTimeout(showAllowedTabsV179, 250);
  }
  window.financeShowTab = function(tab, btn){
    activateFinanceTabV179(tab, btn);
    renderFinanceSectionV179(tab);
  };
  window.financeRestoreTabsV179 = showAllowedTabsV179;
  const oldRenderAll = window.financeRenderAll;
  if(oldRenderAll && !oldRenderAll.v179Wrapped){
    window.financeRenderAll = function(){
      const out = oldRenderAll.apply(this, arguments);
      showAllowedTabsV179();
      setTimeout(showAllowedTabsV179, 80);
      return out;
    };
    window.financeRenderAll.v179Wrapped = true;
  }
  function installObserver(){
    const wrap = document.querySelector('#financeDashboard .finance-tabs') || document.querySelector('.finance-tabs');
    if(!wrap || wrap.v179Observed) return;
    wrap.v179Observed = true;
    new MutationObserver(()=>showAllowedTabsV179()).observe(wrap,{childList:true,subtree:true,attributes:true,attributeFilter:['style','hidden','class']});
  }
  function injectCssV179(){
    if($('v179TabsCss')) return;
    const st = document.createElement('style');
    st.id = 'v179TabsCss';
    st.textContent = `
      body:not(.warehouse-manager-view-v179) #financeDashboard .finance-tabs .finance-tab,
      body:not(.warehouse-manager-view-v179) #financeDashboard .finance-tabs button{display:inline-flex!important;visibility:visible!important;}
      body:not(.warehouse-manager-view-v179) #financeSuppliersTabV178{display:inline-flex!important;}
    `;
    document.head.appendChild(st);
  }
  window.addEventListener('load',()=>{
    injectCssV179();
    setTimeout(()=>{ showAllowedTabsV179(); installObserver(); },300);
    setTimeout(()=>{ showAllowedTabsV179(); installObserver(); },1000);
  });
  document.addEventListener('click',(ev)=>{
    const btn = ev.target.closest && ev.target.closest('#financeDashboard .finance-tabs button, .finance-tabs .finance-tab');
    if(!btn) return;
    const key = tabKeyFromButton(btn);
    if(!key) return;
    // Use our stable controller and stop old inline handlers from rebuilding/hiding tabs.
    ev.preventDefault();
    ev.stopPropagation();
    ev.stopImmediatePropagation();
    window.financeShowTab(key, btn);
  }, true);
  console.log('V179 finance tabs real admin fix loaded');
})();


/* ===== V180: Finance/Inventory final stability: admin tabs, catalog grid, warehouse-safe stats, units filter ===== */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const N = v => { const n = Number(String(v ?? 0).replace(/,/g,'').trim()); return Number.isFinite(n) ? n : 0; };
  const E = v => S(v).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const Q = v => Math.max(0, N(v)).toLocaleString('en-US', { maximumFractionDigits: 2 });
  const M = v => N(v).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });

  function currentUserV180(){
    try { if (typeof session === 'function') return session() || {}; } catch(_) {}
    try { return JSON.parse(localStorage.getItem('tasneef_user') || '{}') || {}; } catch(_) { return {}; }
  }
  function roleV180(){ return S(currentUserV180().role); }
  function isWarehouseV180(){ return roleV180() === 'warehouse_manager'; }
  function tabText(el){ return S(el?.textContent).replace(/\s+/g,' '); }
  function tabKey(btn){
    const oc = btn?.getAttribute?.('onclick') || '';
    const m = oc.match(/financeShowTab\(['"]([^'"]+)['"]/);
    if(m) return m[1];
    const t = tabText(btn);
    const map = {'الملخص':'overview','المصروفات':'expenses','المخزون':'inventory','الأصناف':'catalog','حركة المخزون':'movements','طلبات الصرف':'requests','الموردين':'suppliers','التقارير':'reports','مراكز التكلفة':'costCenters','تقليل التكلفة':'costReduction'};
    return map[t] || '';
  }
  function pageIdFor(tab){
    if(tab === 'catalog' || tab === 'items') return 'financeTabCatalog';
    if(tab === 'suppliers') return 'financeTabSuppliers';
    return 'financeTab' + S(tab).charAt(0).toUpperCase() + S(tab).slice(1);
  }
  function pages(){ return [...document.querySelectorAll('#financeDashboard .finance-tab-page')]; }
  function buttons(){ return [...document.querySelectorAll('#financeDashboard .finance-tabs button, #financeDashboard .finance-tabs .finance-tab')]; }

  function normalizeWarehouseClassesV180(){
    const cls = ['warehouse-manager-view-v151','warehouse-manager-view-v162','warehouse-manager-view-v163','warehouse-manager-view-v164','warehouse-manager-view-v178','warehouse-manager-view-v179','warehouse-manager-view-v180'];
    cls.forEach(c => document.body.classList.remove(c));
    if(isWarehouseV180()) document.body.classList.add('warehouse-manager-view-v180');
  }

  function applyFinanceTabsV180(activeTab){
    const wh = isWarehouseV180();
    normalizeWarehouseClassesV180();
    buttons().forEach(btn => {
      const key = tabKey(btn);
      const keep = !wh || key === 'requests' || key === 'catalog' || key === 'items' || /طلبات الصرف|الأصناف/.test(tabText(btn));
      btn.hidden = !keep;
      btn.style.setProperty('display', keep ? 'inline-flex' : 'none', 'important');
      btn.style.setProperty('visibility', keep ? 'visible' : 'hidden', 'important');
    });
    if(wh){
      // Only tab content accessibility for warehouse manager.
      pages().forEach(p => {
        const keep = ['financeTabRequests','financeTabCatalog'].includes(p.id);
        if(!keep) p.classList.add('hidden');
      });
      const current = activeTab || window.financeCurrentTab;
      if(!['requests','catalog','items'].includes(current)){
        const req = $('financeTabRequests');
        if(req){ pages().forEach(p=>p.classList.add('hidden')); req.classList.remove('hidden'); }
      }
    }
  }

  function activateTabV180(tab, btn){
    if(isWarehouseV180() && !['requests','catalog','items'].includes(tab)) tab = 'requests';
    if(tab === 'items') tab = 'catalog';
    if(!tab) tab = 'overview';
    window.financeCurrentTab = tab;
    pages().forEach(p => p.classList.add('hidden'));
    const page = $(pageIdFor(tab));
    if(page) page.classList.remove('hidden');
    buttons().forEach(b => b.classList.remove('active'));
    const realBtn = btn || buttons().find(b => tabKey(b) === tab || (tab === 'catalog' && ['items','catalog'].includes(tabKey(b))));
    realBtn?.classList.add('active');
    applyFinanceTabsV180(tab);
  }

  function renderTabDataV180(tab){
    try{
      if(!window.financeLoaded && typeof window.financeLoadAll === 'function') window.financeLoadAll();
      if(tab === 'requests' && typeof window.inventoryRenderRequests === 'function') window.inventoryRenderRequests();
      else if((tab === 'catalog' || tab === 'items') && typeof window.inventoryRenderItems === 'function') window.inventoryRenderItems();
      else if(tab === 'inventory'){
        if(typeof window.renderStockBatchCardsV149 === 'function') window.renderStockBatchCardsV149();
        if(typeof window.stockBatchLoadV148 === 'function') window.stockBatchLoadV148(false);
      }
      else if(tab === 'movements' && typeof window.inventoryRenderMovements === 'function') window.inventoryRenderMovements();
      else if(tab === 'reports' && typeof window.financeRenderReports === 'function') window.financeRenderReports();
      else if(tab === 'costReduction' && typeof window.renderCostReductionV152 === 'function') window.renderCostReductionV152();
      else if(tab === 'suppliers' && typeof window.renderSuppliersV178 === 'function') window.renderSuppliersV178();
    }catch(e){ console.warn('V180 render tab warning', e); }
    applyFinanceTabsV180(tab);
  }

  window.financeShowTab = function(tab, btn){
    activateTabV180(tab, btn);
    renderTabDataV180(tab);
    setTimeout(()=>applyFinanceTabsV180(tab), 80);
    setTimeout(()=>applyFinanceTabsV180(tab), 250);
  };

  function productCode(it){ return S(it.product_code || it.serial_number || it.barcode || it.supplier_barcode || it.company_code || it.id); }
  function productImg(it){ return S(it.image_url || it.product_image || it.image || ''); }
  function matchesItem(it, code){
    const c = S(code);
    if(!c) return false;
    return [it.id,it.product_code,it.serial_number,it.barcode,it.supplier_barcode,it.company_code,it.internal_code].map(S).includes(c);
  }
  function requestLinesV202(r){
    let lines = r?.request_items || r?.items || r?.request_lines || r?.lines || [];
    if(typeof lines === 'string'){ try{ lines = JSON.parse(lines); }catch(_){ lines = []; } }
    if(Array.isArray(lines) && lines.length) return lines;
    if(r?.item_id || r?.item_name) return [{item_id:r.item_id,item_name:r.item_name,quantity:r.quantity,product_code:r.product_code,barcode:r.barcode}];
    return [];
  }
  function requestIssuedV202(r){ return /approved|issued|صرف|معتمد|done|completed/i.test(S(r?.status)); }
  function itemMatchV202(it,row){
    const id=S(it?.id), name=S(it?.name), code=productCode(it);
    return S(row?.item_id)===id || S(row?.item_name)===name || (!!code && [row?.product_code,row?.barcode,row?.supplier_barcode,row?.company_code,row?.serial_number].map(S).includes(code));
  }
  function itemStatsV180(it){
    let inMov = 0, inBatch = 0, outMov = 0, outReq = 0, ret = 0, consumeDirect = 0;
    (window.data?.inventoryMovements || []).forEach(m => {
      if(!itemMatchV202(it,m)) return;
      const q = Math.abs(N(m.quantity));
      const t = S(m.movement_type).toLowerCase();
      if(['in','إدخال','add','invoice_add','purchase'].includes(t)) inMov += q;
      else if(['out','صرف','issue'].includes(t)) outMov += q;
      else if(['return','مرتجع','رجوع'].includes(t)) ret += q;
      else if(['consume','consumption','استهلاك'].includes(t)) consumeDirect += q;
    });
    (window.stockBatchesV148 || []).forEach(b => (b.lines || []).forEach(l => {
      if(itemMatchV202(it,l)) inBatch += Math.abs(N(l.quantity));
    }));
    (window.data?.inventoryRequests || []).filter(requestIssuedV202).forEach(r => {
      requestLinesV202(r).forEach(l => { if(itemMatchV202(it,l)) outReq += Math.abs(N(l.quantity)); });
    });
    // مصدر الصادر الرسمي: أمر الصرف إذا موجود، وإلا حركة out. لا نجمع الاثنين حتى لا يظهر خرج مضاعف.
    const out = outReq > 0 ? outReq : outMov;
    const netOut = Math.max(0, out - ret);
    const totalConsumed = netOut + consumeDirect;
    const entered = inMov > 0 ? inMov : inBatch;
    const remaining = Number.isFinite(N(it.quantity)) ? N(it.quantity) : Math.max(0, entered - totalConsumed);
    return { entered, out, ret, netOut, consumeDirect, consumed: netOut, totalConsumed, remaining };
  }
  window.inventoryItemStatsV151 = itemStatsV180;

  function renderCatalogV180(){
    const body = $('inventoryItemsBody');
    if(!body) return;
    const q = S($('financeSearch')?.value).toLowerCase();
    const supplier = S($('inventoryReportSupplier')?.value);
    const cat = S($('inventoryCategoryFilter')?.value || $('inventoryItemCategoryFilter')?.value);
    let rows = [...(window.data?.inventoryItems || [])];
    if(q) rows = rows.filter(it => [it.name, productCode(it), it.supplier, it.category, it.unit, it.notes].map(S).join(' ').toLowerCase().includes(q));
    if(supplier) rows = rows.filter(it => S(it.supplier) === supplier);
    if(cat) rows = rows.filter(it => S(it.category) === cat);
    const wh = isWarehouseV180();
    const cards = rows.map(it => {
      const st = itemStatsV180(it);
      const low = st.remaining <= N(it.min_quantity || it.reorder_level || 0);
      const img = productImg(it) ? `<img src="${E(productImg(it))}" alt="${E(it.name)}">` : '<span>لا توجد</span>';
      const priceBlock = wh ? '' : `<div><small>سعر الوحدة</small><b>${M(it.unit_cost)} ر.س</b></div>`;
      const actions = wh ? `<button type="button" onclick="inventoryOpenItemSmart&&inventoryOpenItemSmart('${E(it.id)}')">عرض</button>` : `<button type="button" onclick="inventoryOpenItemSmart&&inventoryOpenItemSmart('${E(it.id)}')">عرض المنتج</button><button type="button" class="light" onclick="inventoryEditItem&&inventoryEditItem('${E(it.id)}')">تعديل</button><button type="button" class="danger" onclick="financeDelete&&financeDelete('inventory_items','${E(it.id)}')">حذف</button>`;
      return `<article class="v180-catalog-card"><div class="v180-item-head"><div class="v180-img">${img}</div><div class="v180-title"><h3>${E(it.name || '-')}</h3><small>كود المنتج: ${E(productCode(it) || '-')}</small><br><small>المورد: ${E(it.supplier || '-')}</small></div><span class="${low?'v180-low':'v180-ok'}">${low?'منخفض':'متوفر'}</span></div><div class="v180-stats"><div><small>الكمية</small><b>${Q(st.remaining)} ${E(it.unit || '')}</b></div><div><small>دخل</small><b>${Q(st.entered)}</b></div><div><small>خرج</small><b>${Q(st.out)}</b></div><div><small>مرتجع</small><b>${Q(st.ret)}</b></div><div><small>صافي المستهلك</small><b>${Q(st.consumed)}</b></div><div><small>استهلاك مباشر</small><b>${Q(st.consumeDirect)}</b></div><div><small>إجمالي المستهلك</small><b>${Q(st.totalConsumed)}</b></div><div><small>حد الطلب</small><b>${Q(it.min_quantity || it.reorder_level || 0)}</b></div>${priceBlock}</div><div class="row-actions v180-actions">${actions}</div></article>`;
    }).join('') || '<div class="empty">لا توجد أصناف</div>';
    body.innerHTML = `<tr class="v180-catalog-row"><td colspan="20"><div class="v180-catalog-grid">${cards}</div></td></tr>`;
  }
  window.inventoryRenderItems = function(){ renderCatalogV180(); try{ if(typeof inventoryFillItemSelect==='function') inventoryFillItemSelect(); if(typeof inventoryFillRequestSelect==='function') inventoryFillRequestSelect(); if(typeof financeFillSupervisorSelect==='function') financeFillSupervisorSelect(); }catch(_){} };

  function enhanceUnitInputV180(){
    let dl = $('unitOptionsV180');
    if(!dl){
      dl = document.createElement('datalist');
      dl.id = 'unitOptionsV180';
      dl.innerHTML = ['حبة','لتر','كرتون','علبة','متر','كيس','رول','جالون','طقم','زوج','عبوة','درزن','كيلو','أخرى'].map(x=>`<option value="${x}"></option>`).join('');
      document.body.appendChild(dl);
    }
    const u = $('batchUnitV148');
    if(u){ u.setAttribute('list','unitOptionsV180'); u.placeholder = 'اختر أو اكتب: حبة / لتر / كرتون'; }
  }

  function injectCssV180(){
    if($('v180FixCss')) return;
    const st = document.createElement('style');
    st.id = 'v180FixCss';
    st.textContent = `
      #financeTabCatalog table thead{display:none!important;}
      #financeTabCatalog .table-wrap{overflow:visible!important;}
      .v180-catalog-row>td{border:0!important;background:transparent!important;padding:0!important;}
      .v180-catalog-grid{display:grid!important;grid-template-columns:repeat(auto-fit,minmax(310px,1fr))!important;gap:16px!important;width:100%!important;direction:rtl!important;padding:8px!important;align-items:stretch!important;}
      .v180-catalog-card{border:1px solid #cfe3dd;border-radius:20px;background:#fff;padding:14px;box-shadow:0 10px 22px rgba(0,45,31,.06);display:flex;flex-direction:column;gap:12px;min-height:235px;}
      .v180-item-head{display:flex;align-items:center;gap:12px;justify-content:space-between;}
      .v180-img{width:66px;height:66px;flex:0 0 66px;border:1px solid #dceee7;border-radius:15px;background:#f7fbf9;display:grid;place-items:center;overflow:hidden;color:#789;}
      .v180-img img{max-width:100%;max-height:100%;object-fit:contain;display:block;}
      .v180-title{flex:1;min-width:0}.v180-title h3{margin:0;color:#003f31;font-size:18px}.v180-title small{color:#668178}
      .v180-ok,.v180-low{border-radius:999px;padding:5px 9px;font-size:12px;white-space:nowrap}.v180-ok{background:#e5f8ee;color:#067647}.v180-low{background:#ffe6e6;color:#b42318}
      .v180-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.v180-stats div{background:#f1faf6;border:1px solid #d9eee6;border-radius:12px;padding:8px;text-align:center}.v180-stats small{display:block;color:#637970}.v180-stats b{color:#064534}
      .v180-actions{display:flex!important;gap:8px!important;flex-wrap:wrap!important;margin-top:auto}.v180-actions button{min-width:78px!important}
      body:not(.warehouse-manager-view-v180) #financeDashboard .finance-tabs button, body:not(.warehouse-manager-view-v180) #financeDashboard .finance-tabs .finance-tab{display:inline-flex!important;visibility:visible!important;}
      body.warehouse-manager-view-v180 #financeDashboard .finance-tabs button:not(.active), body.warehouse-manager-view-v180 #financeDashboard .finance-tabs .finance-tab:not(.active){visibility:visible;}
      @media(max-width:800px){.v180-catalog-grid{grid-template-columns:1fr!important}.v180-stats{grid-template-columns:repeat(2,1fr)}}
    `;
    document.head.appendChild(st);
  }

  function bootV180(){
    injectCssV180(); enhanceUnitInputV180(); applyFinanceTabsV180(window.financeCurrentTab);
    if(['catalog','items'].includes(S(window.financeCurrentTab))) renderCatalogV180();
  }
  const oldFinanceRenderAllV180 = window.financeRenderAll;
  if(oldFinanceRenderAllV180 && !oldFinanceRenderAllV180.v180Wrapped){
    window.financeRenderAll = function(){ const out = oldFinanceRenderAllV180.apply(this, arguments); setTimeout(bootV180, 60); return out; };
    window.financeRenderAll.v180Wrapped = true;
  }
  document.addEventListener('click', function(ev){
    const btn = ev.target.closest && ev.target.closest('#financeDashboard .finance-tabs button, #financeDashboard .finance-tabs .finance-tab');
    if(!btn) return;
    const key = tabKey(btn);
    if(!key) return;
    ev.preventDefault(); ev.stopPropagation(); ev.stopImmediatePropagation();
    window.financeShowTab(key, btn);
  }, true);
  window.addEventListener('load', ()=>setTimeout(bootV180, 900));
  window.addEventListener('storage', ()=>setTimeout(bootV180, 80));
  console.log('V180 finance inventory role/catalog/unit fix loaded');
})();


/* ================= V181 Invoice Unit & VAT Display Fix =================
   إصلاح نافذة إدخال المخزون:
   - إظهار نوع/وحدة الكمية بوضوح داخل إضافة صنف للفاتورة.
   - منع إخفاء خانة الوحدة بسبب باتشات سابقة.
   - توحيد حساب سعر قبل الضريبة / VAT / سعر شامل / الإجمالي.
   - إصلاح عرض VAT إذا كان سعر قبل الضريبة صفر.
======================================================================= */
(function(){
  const $ = (id)=>document.getElementById(id);
  const N = (v)=>{ const x=parseFloat(String(v??'').replace(/,/g,'')); return Number.isFinite(x)?x:0; };
  const S = (v)=>String(v??'');
  const money = (v)=>N(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  const qty = (v)=>N(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const E = (v)=>S(v).replace(/[&<>"']/g, m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const VAT=0.15;

  function calcVat(price, mode){
    price=N(price); mode=mode||'exclusive';
    if(price<=0) return {net:0,vat:0,gross:0};
    if(mode==='inclusive'){
      const net=price/(1+VAT);
      return {net, vat:price-net, gross:price};
    }
    if(mode==='exempt') return {net:price, vat:0, gross:price};
    return {net:price, vat:price*VAT, gross:price*(1+VAT)};
  }

  function unitOptions(){
    return ['حبة','لتر','كرتون','علبة','متر','كيس','رول','جالون','عبوة','طقم','زوج','درزن','كيلو','ملي','أخرى'];
  }

  function ensureUnitField(){
    let u=$('batchUnitV148');
    const qtyEl=$('batchQtyV148');
    if(!qtyEl) return;

    // إذا كانت خانة الوحدة موجودة، نعيد إظهارها مهما كانت باتشات قديمة أخفتها.
    if(u){
      const box=u.closest('div');
      if(box){
        box.classList.remove('v161-hide-advanced-field');
        box.style.display='';
        box.hidden=false;
      }
      u.style.display='';
      u.hidden=false;
      const lab=box?.querySelector('label');
      if(lab) lab.textContent='نوع الكمية / الوحدة';
      u.placeholder='اختر أو اكتب: حبة / لتر / كرتون';
      u.setAttribute('list','unitOptionsV181');
    } else {
      const qtyBox=qtyEl.closest('div');
      const holder=document.createElement('div');
      holder.innerHTML='<label>نوع الكمية / الوحدة</label><input id="batchUnitV148" list="unitOptionsV181" placeholder="اختر أو اكتب: حبة / لتر / كرتون" value="حبة">';
      qtyBox?.parentElement?.insertBefore(holder, qtyBox);
      u=$('batchUnitV148');
    }

    let dl=$('unitOptionsV181') || $('unitOptionsV180');
    if(!dl){
      dl=document.createElement('datalist');
      dl.id='unitOptionsV181';
      document.body.appendChild(dl);
    } else {
      dl.id='unitOptionsV181';
    }
    dl.innerHTML=unitOptions().map(x=>`<option value="${E(x)}"></option>`).join('');
    if(u && !u.value) u.value='حبة';
  }

  function patchLineAdd(){
    const oldAdd=window.stockBatchAddLineV148;
    if(!oldAdd || oldAdd.v181Wrapped) return;
    window.stockBatchAddLineV148=function(){
      ensureUnitField();
      const price=N($('batchUnitPriceV148')?.value);
      if(price<0) return (window.msg?msg('السعر غير صحيح','err'):alert('السعر غير صحيح'));
      if($('batchUnitV148') && !$('batchUnitV148').value) $('batchUnitV148').value='حبة';
      return oldAdd.apply(this, arguments);
    };
    window.stockBatchAddLineV148.v181Wrapped=true;
  }

  function patchRenderLines(){
    const oldRemove=window.stockBatchRemoveLineV148;
    window.stockBatchRenderLinesV148=function(){
      const b=$('batchLinesBodyV148'); if(!b) return;
      const mode=$('batchVatModeV148')?.value||'exclusive';
      let netTotal=0, vatTotal=0, grossTotal=0;
      const lines=(window.batchLinesV148 || batchLinesV148 || []);
      b.innerHTML = lines.map(l=>{
        const c=calcVat(l.unit_price, mode);
        const q=N(l.quantity);
        const net=c.net*q, vat=c.vat*q, gross=c.gross*q;
        netTotal+=net; vatTotal+=vat; grossTotal+=gross;
        return `<tr><td>${E(l.product_code)}</td><td><b>${E(l.item_name)}</b><br><small>${E(l.category||'')} / ${E(l.item_type||'')}</small></td><td>${qty(q)} ${E(l.unit||'حبة')}</td><td>${money(c.net)} ر.س</td><td>${money(c.vat)} ر.س</td><td>${money(c.gross)} ر.س</td><td>${money(gross)} ر.س</td><td><button class="danger" type="button" onclick="stockBatchRemoveLineV148('${E(l.product_code)}')">حذف</button></td></tr>`;
      }).join('') || '<tr><td colspan="8">لم تتم إضافة أصناف بعد</td></tr>';
      const t=$('batchTotalsV148');
      if(t) t.innerHTML = `<b>Total amount before 15% VAT:</b> ${money(netTotal)} SAR &nbsp; | &nbsp; <b>Total VAT 15%:</b> ${money(vatTotal)} SAR &nbsp; | &nbsp; <b>Total Value with 15% VAT:</b> ${money(grossTotal)} SAR`;
    };
    if(oldRemove && !oldRemove.v181Wrapped){
      window.stockBatchRemoveLineV148=function(code){
        if(window.batchLinesV148) window.batchLinesV148=window.batchLinesV148.filter(l=>String(l.product_code)!==String(code));
        if(typeof batchLinesV148!=='undefined') batchLinesV148=batchLinesV148.filter(l=>String(l.product_code)!==String(code));
        window.stockBatchRenderLinesV148();
      };
      window.stockBatchRemoveLineV148.v181Wrapped=true;
    }
  }

  function patchSaveAndPrint(){
    // نعيد تعريف دالة حساب الطباعة فقط حتى لا يظهر VAT بدون سعر.
    const oldPrint=window.stockBatchPrintV148;
    if(oldPrint && !oldPrint.v181Wrapped){
      window.stockBatchPrintV148=function(batch){
        const rows=(batch.lines||[]).map(l=>{
          const c={net:N(l.unit_cost),vat:N(l.unit_vat),gross:N(l.unit_gross)};
          const unitVat=(c.net<=0 && c.gross<=0)?0:c.vat;
          const lineGross=N(l.line_gross || c.gross*N(l.quantity));
          return `<tr><td>${E(l.product_code)}</td><td>${E(l.item_name)}</td><td>${E(l.category||'')}</td><td>${E(l.item_type||'')}</td><td>${E(l.unit||'حبة')}</td><td>${qty(l.quantity)}</td><td>${money(c.net)} SAR</td><td>${money(unitVat)} SAR</td><td>${money(c.gross)} SAR</td><td>${money(lineGross)} SAR</td></tr>`;
        }).join('');
        const body=`<div class="grid"><div class="box"><b>ID</b><br>${E(batch.invoice_no)}</div><div class="box"><b>Date</b><br>${E(batch.batch_date)}</div><div class="box"><b>Warehouse / Supplier</b><br>${E(batch.supplier)}</div></div><h2>Products</h2><table><thead><tr><th>Product Code</th><th>Product Name</th><th>Category</th><th>Type</th><th>Unit</th><th>Quantity Added</th><th>Unit Price</th><th>VAT 15%</th><th>Unit Price with VAT</th><th>Value</th></tr></thead><tbody>${rows||'<tr><td colspan="10">لا توجد أصناف</td></tr>'}</tbody></table><div style="margin-top:22px;font-size:16px;line-height:2"><b>Total amount before 15% VAT:</b> ${money(batch.total_before_vat)} SAR<br><b>Total VAT 15%:</b> ${money(batch.total_vat)} SAR<br><b>Total Value with 15% VAT:</b> ${money(batch.total_with_vat)} SAR</div>`;
        if(typeof financePrintWindow === 'function') financePrintWindow('Added Product', body); else { const w=window.open('','_blank'); w.document.write(body); w.print(); }
      };
      window.stockBatchPrintV148.v181Wrapped=true;
    }
  }

  function patchLookupUnit(){
    const oldLookup=window.lookupBatchProductV151;
    if(oldLookup && !oldLookup.v181Wrapped){
      window.lookupBatchProductV151=function(){
        const out=oldLookup.apply(this, arguments);
        setTimeout(()=>{ ensureUnitField(); const u=$('batchUnitV148'); if(u && !u.value) u.value='حبة'; }, 20);
        return out;
      };
      window.lookupBatchProductV151.v181Wrapped=true;
    }
  }

  function injectCss(){
    if($('v181InvoiceUnitFixCss')) return;
    const st=document.createElement('style'); st.id='v181InvoiceUnitFixCss';
    st.textContent=`
      #batchUnitV148{display:block!important;visibility:visible!important;opacity:1!important;}
      #batchUnitV148.closest{display:block!important;}
      .v161-hide-advanced-field:has(#batchUnitV148){display:block!important;}
      #batchUnitV148 + *{display:block;}
    `;
    document.head.appendChild(st);
  }

  function boot(){
    injectCss(); ensureUnitField(); patchLineAdd(); patchRenderLines(); patchSaveAndPrint(); patchLookupUnit();
    try{ window.stockBatchRenderLinesV148 && window.stockBatchRenderLinesV148(); }catch(e){}
  }
  window.addEventListener('load', ()=>setTimeout(boot, 700));
  document.addEventListener('click', ()=>setTimeout(boot, 60), true);
  window.addEventListener('focus', ()=>setTimeout(boot, 80));
  setTimeout(boot, 1000);
  console.log('V181 invoice unit and VAT display fix loaded');
})();

/* V182 Inventory Invoice Unit + Consumption Distribution
   - keeps invoice unit/VAT logic stable
   - allows multiple actual consumption rows per issued item
   - actual user list uses supervisors
   - total consumed cannot exceed issued minus returns and previous consumption
*/
(function(){
  const V='182';
  const $ = id => document.getElementById(id);
  const n = v => { const x=parseFloat(String(v??'').replace(/,/g,'')); return Number.isFinite(x)?x:0; };
  const s = v => String(v??'');
  const e = v => s(v).replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));
  const q = v => { const x=n(v); return Number.isInteger(x)?String(x):x.toFixed(2).replace(/\.00$/,''); };
  const todayLocal = () => (typeof today==='function'?today():new Date().toISOString().slice(0,10));
  const arr = v => Array.isArray(v)?v:[];
  const lineItems = r => {
    try { if(typeof v118LineItems==='function') return v118LineItems(r) || []; } catch(_){ }
    const raw = r?.request_items || r?.items || r?.request_lines || [];
    if(Array.isArray(raw)) return raw;
    try { const x=JSON.parse(raw); return Array.isArray(x)?x:[]; } catch(_){ return []; }
  };
  const itemById = id => (window.data?.inventoryItems||[]).find(x=>String(x.id)===String(id)) || {};
  const projectList = () => arr(window.data?.projects).filter(p=>String(p.status||'active')!=='inactive');
  const supervisorList = () => {
    const users = arr(window.data?.users).filter(u => ['supervisor','operations_manager','admin','manager'].includes(String(u.role||'').toLowerCase()) && u.is_active!==false);
    const sups = arr(window.data?.supervisors).filter(u=>u.is_active!==false);
    const map=new Map();
    [...sups,...users].forEach(u=>{ const id=s(u.id||u.user_id||u.full_name||u.username); if(id && !map.has(id)) map.set(id,u); });
    return [...map.values()];
  };
  const pName = id => { try { return typeof projectName==='function'?projectName(id):''; } catch(_){ return ''; } };
  const supName = id => { try { return typeof supervisorName==='function'?supervisorName(id):''; } catch(_){ return ''; } };
  const movementsFor = (requestId,itemId,type) => arr(window.data?.inventoryMovements).filter(m =>
    String(m.request_id||'')===String(requestId) && String(m.item_id||'')===String(itemId) && (!type || String(m.movement_type)===type)
  );
  const returnedQty = (requestId,itemId) => movementsFor(requestId,itemId,'return').reduce((a,m)=>a+n(m.quantity),0);
  const consumedQty = (requestId,itemId) => movementsFor(requestId,itemId,'consume').reduce((a,m)=>a+n(m.quantity),0);
  const availableToConsume = (requestId,line) => Math.max(0, n(line.quantity) - returnedQty(requestId,line.item_id) - consumedQty(requestId,line.item_id));

  function ensureUnitOptions(){
    let dl=$('unitOptionsV182') || $('unitOptionsV181') || $('unitOptionsV180');
    if(!dl){ dl=document.createElement('datalist'); document.body.appendChild(dl); }
    dl.id='unitOptionsV182';
    dl.innerHTML=['حبة','لتر','كرتون','علبة','متر','كيس','رول','جالون','عبوة','طقم','كيلو','درزن','باكيت','أخرى'].map(x=>`<option value="${e(x)}"></option>`).join('');
    const unit=$('batchUnitV148');
    if(unit){ unit.setAttribute('list','unitOptionsV182'); unit.placeholder='حبة / لتر / كرتون'; if(!unit.value) unit.value='حبة'; const lab=unit.closest('div')?.querySelector('label'); if(lab) lab.textContent='نوع الكمية / الوحدة'; }
  }

  function patchVatLineRender(){
    if(!window.stockBatchRenderLinesV148 || window.stockBatchRenderLinesV148.v182Wrapped) return;
    window.stockBatchRenderLinesV148 = function(){
      const body=$('batchLinesBodyV148'); if(!body) return;
      const mode=$('batchVatModeV148')?.value || 'exclusive';
      const lines = window.batchLinesV148 || (typeof batchLinesV148!=='undefined'?batchLinesV148:[]);
      let netTotal=0, vatTotal=0, grossTotal=0;
      const calc=(price)=>{
        const p=n(price);
        if(p<=0) return {net:0,vat:0,gross:0};
        if(mode==='exempt') return {net:p,vat:0,gross:p};
        if(mode==='inclusive'){ const net=p/1.15; return {net,vat:p-net,gross:p}; }
        return {net:p,vat:p*0.15,gross:p*1.15};
      };
      body.innerHTML = arr(lines).map(l=>{
        const c=calc(l.unit_price ?? l.unit_cost ?? 0); const qty=n(l.quantity);
        const net=c.net*qty, vat=c.vat*qty, gross=c.gross*qty;
        netTotal+=net; vatTotal+=vat; grossTotal+=gross;
        return `<tr><td>${e(l.product_code||'')}</td><td><b>${e(l.item_name||'')}</b><br><small>${e(l.category||'')} / ${e(l.item_type||'')}</small></td><td>${q(qty)} ${e(l.unit||'حبة')}</td><td>${c.net.toFixed(2)} ر.س</td><td>${c.vat.toFixed(2)} ر.س</td><td>${c.gross.toFixed(2)} ر.س</td><td>${gross.toFixed(2)} ر.س</td><td><button class="danger" type="button" onclick="stockBatchRemoveLineV148('${e(l.product_code||'')}')">حذف</button></td></tr>`;
      }).join('') || '<tr><td colspan="8">لم تتم إضافة أصناف بعد</td></tr>';
      const totals=$('batchTotalsV148');
      if(totals) totals.innerHTML = `<b>Total amount before 15% VAT:</b> ${netTotal.toFixed(2)} SAR &nbsp; | &nbsp; <b>Total VAT 15%:</b> ${vatTotal.toFixed(2)} SAR &nbsp; | &nbsp; <b>Total Value with 15% VAT:</b> ${grossTotal.toFixed(2)} SAR`;
    };
    window.stockBatchRenderLinesV148.v182Wrapped=true;
  }

  window.addConsumptionRowV182 = function(lineIndex, values={}){
    const req=window._v182ConsumptionRequest; if(!req) return;
    const line=req.lines[Number(lineIndex)]; if(!line) return;
    const tbody=$(`v182ConsumeRows_${lineIndex}`); if(!tbody) return;
    const projects=projectList().map(p=>`<option value="${e(p.id)}" ${String(values.project_id||'')===String(p.id)?'selected':''}>${e(p.name||p.project_name||'')}</option>`).join('');
    const supervisors=supervisorList().map(u=>`<option value="${e(u.id)}" ${String(values.user_id||'')===String(u.id)?'selected':''}>${e(u.full_name||u.name||u.username||'')}</option>`).join('');
    const row=document.createElement('tr');
    row.className='v182-cons-row'; row.dataset.itemId=s(line.item_id); row.dataset.lineIndex=s(lineIndex);
    row.innerHTML=`<td><select class="v182-cons-project"><option value="">اختر المشروع</option>${projects}</select></td><td><input class="v182-cons-qty" type="number" step="0.01" min="0" placeholder="الكمية" value="${e(values.qty||'')}"></td><td><select class="v182-cons-user"><option value="">اختر المشرف</option>${supervisors}</select></td><td><input class="v182-cons-notes" placeholder="ملاحظات" value="${e(values.notes||'')}"></td><td><button type="button" class="danger" onclick="this.closest('tr').remove()">حذف</button></td>`;
    tbody.appendChild(row);
  };

  window.openRequestConsumptionV178 = function(id){
    const r=arr(window.data?.inventoryRequests).find(x=>String(x.id)===String(id));
    if(!r) return (typeof msg==='function'?msg('لم يتم العثور على أمر الصرف','err'):alert('لم يتم العثور على أمر الصرف'));
    const lines=lineItems(r);
    if(!lines.length) return (typeof msg==='function'?msg('لا توجد أصناف داخل أمر الصرف','err'):alert('لا توجد أصناف'));
    window._v182ConsumptionRequest={r,lines};
    document.getElementById('consumptionModalV178')?.remove();
    const cards=lines.map((l,i)=>{
      const item=itemById(l.item_id); const unit=l.unit||item.unit||'حبة';
      const ret=returnedQty(r.id,l.item_id); const cons=consumedQty(r.id,l.item_id); const avail=Math.max(0,n(l.quantity)-ret-cons);
      const prev=movementsFor(r.id,l.item_id,'consume').map(m=>`<tr><td>${e(m.movement_date||'')}</td><td>${e(m.project_name||pName(m.project_id)||'-')}</td><td>${q(m.quantity)} ${e(unit)}</td><td>${e(m.receiver||'-')}</td><td>${e(m.notes||'-')}</td></tr>`).join('') || '<tr><td colspan="5">لا يوجد استهلاك مسجل سابقًا</td></tr>';
      return `<section class="v182-cons-card"><div class="v182-cons-head"><div><h3>${e(l.item_name||item.name||'-')}</h3><small>الكود: ${e(l.product_code||item.product_code||item.barcode||'-')} | الوحدة: ${e(unit)}</small></div><div class="v182-cons-stats"><span>مصروف: <b>${q(l.quantity)}</b></span><span>مرتجع: <b>${q(ret)}</b></span><span>مستهلك سابق: <b>${q(cons)}</b></span><span>متاح: <b>${q(avail)}</b></span></div></div><div class="table-wrap"><table><thead><tr><th>المشروع</th><th>كمية الاستهلاك</th><th>المستخدم الفعلي</th><th>ملاحظات</th><th>إجراء</th></tr></thead><tbody id="v182ConsumeRows_${i}" data-available="${avail}"></tbody></table></div><button type="button" class="light" onclick="addConsumptionRowV182(${i})">+ إضافة استهلاك لهذا الصنف</button><details><summary>الاستهلاك المسجل سابقًا</summary><table><thead><tr><th>التاريخ</th><th>المشروع</th><th>الكمية</th><th>المستخدم الفعلي</th><th>ملاحظات</th></tr></thead><tbody>${prev}</tbody></table></details></section>`;
    }).join('');
    const modal=document.createElement('div'); modal.id='consumptionModalV178'; modal.className='v182-backdrop';
    modal.innerHTML=`<div class="v182-modal"><div class="v182-top"><div><h2>توزيع الاستهلاك الفعلي</h2><p>أمر الصرف: REQ-${e(r.id)} — المستلم: ${e(r.supervisor_name||supName(r.supervisor_id)||'-')}</p></div><button type="button" onclick="document.getElementById('consumptionModalV178')?.remove()">إغلاق</button></div><div class="v182-body">${cards}</div><div class="v182-actions"><button type="button" onclick="saveRequestConsumptionV178('${e(id)}')">حفظ الاستهلاك</button><button type="button" class="light" onclick="document.getElementById('consumptionModalV178')?.remove()">إلغاء</button></div></div>`;
    document.body.appendChild(modal);
    lines.forEach((_,i)=>window.addConsumptionRowV182(i));
  };

  window.saveRequestConsumptionV178 = async function(id){
    const req=window._v182ConsumptionRequest;
    const r=req?.r || arr(window.data?.inventoryRequests).find(x=>String(x.id)===String(id));
    const lines=req?.lines || lineItems(r);
    if(!r || !lines?.length) return (typeof msg==='function'?msg('لم يتم العثور على الطلب','err'):alert('لم يتم العثور على الطلب'));
    const rows=[...document.querySelectorAll('#consumptionModalV178 .v182-cons-row')];
    const byItem={}; const payload=[];
    for(const row of rows){
      const idx=Number(row.dataset.lineIndex); const line=lines[idx]; if(!line) continue;
      const qty=n(row.querySelector('.v182-cons-qty')?.value); if(qty<=0) continue;
      const pid=row.querySelector('.v182-cons-project')?.value; if(!pid) throw new Error('اختر المشروع لكل سطر استهلاك');
      const uid=row.querySelector('.v182-cons-user')?.value; if(!uid) throw new Error('اختر المستخدم الفعلي من قائمة المشرفين');
      byItem[line.item_id]=(byItem[line.item_id]||0)+qty;
      const item=itemById(line.item_id); const sup=supervisorList().find(u=>String(u.id)===String(uid))||{};
      payload.push({
        item_id:Number(line.item_id), item_name:line.item_name||item.name||'', movement_type:'consume', quantity:qty,
        movement_date:todayLocal(), project_id:Number(pid), project_name:pName(pid), receiver_id:Number(uid)||null,
        receiver:sup.full_name||sup.name||sup.username||'', reason:'تسجيل استهلاك فعلي لأمر صرف REQ-'+r.id,
        notes:row.querySelector('.v182-cons-notes')?.value||'', request_id:Number(r.id),
        product_code:line.product_code||item.product_code||item.barcode||'', barcode:line.barcode||item.barcode||'',
        unit_cost:n(line.unit_cost||item.unit_cost||item.unit_price||0), unit:line.unit||item.unit||'حبة'
      });
    }
    if(!payload.length) throw new Error('أدخل كمية استهلاك واحدة على الأقل');
    for(const [itemId,total] of Object.entries(byItem)){
      const line=lines.find(l=>String(l.item_id)===String(itemId));
      const avail=availableToConsume(r.id,line);
      if(total>avail+0.0001) throw new Error('مجموع الاستهلاك أكبر من المتاح للصنف: '+(line.item_name||itemById(itemId).name||itemId));
    }
    const res=await sb.from('inventory_movements').insert(payload);
    if(res.error) throw res.error;
    try{ await sb.from('inventory_requests').update({consumption_updated_at:new Date().toISOString(), has_consumption:true}).eq('id',r.id); }catch(_){ }
    document.getElementById('consumptionModalV178')?.remove();
    if(typeof msg==='function') msg('تم حفظ توزيع الاستهلاك الفعلي');
    if(typeof financeLoadAll==='function') await financeLoadAll();
  };

  function injectCss(){
    if($('v182ConsumptionCss')) return;
    const st=document.createElement('style'); st.id='v182ConsumptionCss';
    st.textContent=`
      .v182-backdrop{position:fixed;inset:0;background:rgba(0,20,14,.45);z-index:99999;display:flex;align-items:center;justify-content:center;padding:18px;direction:rtl}
      .v182-modal{background:#fff;border-radius:24px;box-shadow:0 30px 100px rgba(0,0,0,.25);width:min(1120px,96vw);max-height:92vh;overflow:auto;border:1px solid #d6ebe3}
      .v182-top{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;padding:18px 22px;border-bottom:1px solid #e5f1ed;background:linear-gradient(135deg,#f7fffb,#eefaf5)}
      .v182-top h2{margin:0;color:#07563d}.v182-top p{margin:6px 0 0;color:#557067}.v182-body{padding:18px}.v182-actions{padding:16px 22px;border-top:1px solid #e5f1ed;display:flex;gap:10px;justify-content:flex-start}
      .v182-cons-card{border:1px solid #dceee7;border-radius:20px;margin-bottom:16px;padding:14px;background:#fbfffd}.v182-cons-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;margin-bottom:12px}.v182-cons-head h3{margin:0;color:#093f31}.v182-cons-stats{display:flex;flex-wrap:wrap;gap:8px}.v182-cons-stats span{background:#eefaf5;border:1px solid #cfe8dd;border-radius:999px;padding:6px 10px;color:#194b3b}
      #consumptionModalV178 select,#consumptionModalV178 input{width:100%;min-height:38px;border:1px solid #cfded8;border-radius:10px;padding:7px;background:#fff}
      #consumptionModalV178 details{margin-top:10px}#consumptionModalV178 summary{cursor:pointer;color:#07563d;font-weight:700}
      @media(max-width:800px){.v182-cons-head{display:block}.v182-modal{width:98vw}.v182-body{padding:10px}.v182-top{padding:14px}}
    `;
    document.head.appendChild(st);
  }

  function boot(){ ensureUnitOptions(); patchVatLineRender(); injectCss(); try{ window.stockBatchRenderLinesV148 && window.stockBatchRenderLinesV148(); }catch(_){ } }
  window.addEventListener('load',()=>setTimeout(boot,500));
  document.addEventListener('click',()=>setTimeout(boot,80),true);
  setTimeout(boot,900);
  console.log('V182 consumption distribution and invoice unit fix loaded');
})();


/* ================= V183 Inventory Invoice Row Final Fix =================
   - يثبت حقل الوحدة في فاتورة إدخال المخزون.
   - يعرض ويحفظ سعر قبل الضريبة / VAT / سعر شامل بشكل موحد.
   - يمنع ظهور VAT إذا كان السعر صفر.
   - يجعل مصدر الحساب الوحيد هو سعر الوحدة المدخل حسب حالة الضريبة.
======================================================================= */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const N = v => { const x = parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const S = v => String(v ?? '');
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const money = v => N(v).toLocaleString('en-US',{minimumFractionDigits:2, maximumFractionDigits:2});
  const qty = v => N(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const VAT = 0.15;

  function calcUnit(price, mode){
    price = N(price); mode = mode || 'exclusive';
    if(price <= 0) return { net:0, vat:0, gross:0 };
    if(mode === 'inclusive'){
      const net = price / (1 + VAT);
      return { net, vat: price - net, gross: price };
    }
    if(mode === 'exempt') return { net:price, vat:0, gross:price };
    return { net:price, vat:price * VAT, gross:price * (1 + VAT) };
  }

  function getLines(){
    if(Array.isArray(window.batchLinesV148)) return window.batchLinesV148;
    return [];
  }

  function units(){ return ['حبة','لتر','كرتون','علبة','متر','كيس','رول','جالون','عبوة','طقم','زوج','درزن','كيلو','ملي','أخرى']; }

  function ensureUnitFieldV183(){
    const qtyInput = $('batchQtyV148');
    if(!qtyInput) return;
    let unit = $('batchUnitV148');
    if(unit){
      const box = unit.closest('div');
      if(box){ box.hidden = false; box.style.display = ''; box.classList.remove('v161-hide-advanced-field'); }
      unit.hidden = false; unit.style.display = ''; unit.setAttribute('list','unitOptionsV183');
      unit.placeholder = 'حبة / لتر / كرتون / علبة';
      const lab = box?.querySelector('label'); if(lab) lab.textContent = 'نوع الكمية / الوحدة';
    } else {
      const qtyBox = qtyInput.closest('div');
      const holder = document.createElement('div');
      holder.innerHTML = '<label>نوع الكمية / الوحدة</label><input id="batchUnitV148" list="unitOptionsV183" placeholder="حبة / لتر / كرتون / علبة" value="حبة">';
      qtyBox?.parentElement?.insertBefore(holder, qtyBox);
      unit = $('batchUnitV148');
    }
    let dl = $('unitOptionsV183');
    if(!dl){ dl = document.createElement('datalist'); dl.id = 'unitOptionsV183'; document.body.appendChild(dl); }
    dl.innerHTML = units().map(u => `<option value="${E(u)}"></option>`).join('');
    if(unit && !unit.value) unit.value = 'حبة';
  }

  function enhancePriceLabelsV183(){
    const price = $('batchUnitPriceV148');
    if(price){
      const lab = price.closest('div')?.querySelector('label');
      if(lab) lab.textContent = 'سعر الوحدة';
      price.placeholder = 'اكتب السعر حسب حالة الضريبة المختارة';
      if(!price.dataset.v183){
        ['input','change','keyup','blur'].forEach(ev => price.addEventListener(ev, () => window.stockBatchRenderLinesV148 && window.stockBatchRenderLinesV148()));
        price.dataset.v183 = '1';
      }
    }
    const mode = $('batchVatModeV148');
    if(mode && !mode.dataset.v183){
      mode.addEventListener('change', () => window.stockBatchRenderLinesV148 && window.stockBatchRenderLinesV148());
      mode.dataset.v183 = '1';
    }
  }

  function patchAddLineV183(){
    const old = window.stockBatchAddLineV148;
    if(!old || old.v183Wrapped) return;
    window.stockBatchAddLineV148 = function(){
      ensureUnitFieldV183();
      const unit = $('batchUnitV148');
      if(unit && !unit.value) unit.value = 'حبة';
      const price = N($('batchUnitPriceV148')?.value);
      if(price < 0){ if(window.msg) msg('سعر الوحدة غير صحيح','err'); else alert('سعر الوحدة غير صحيح'); return; }
      const before = getLines().length;
      const out = old.apply(this, arguments);
      // ضمان حفظ الوحدة حتى لو دالة قديمة لم تلتقطها.
      const lines = getLines();
      const last = lines[lines.length - 1];
      if(lines.length >= before && last && !last.unit) last.unit = unit?.value || 'حبة';
      window.stockBatchRenderLinesV148 && window.stockBatchRenderLinesV148();
      return out;
    };
    window.stockBatchAddLineV148.v183Wrapped = true;
  }

  window.stockBatchRenderLinesV148 = function(){
    const body = $('batchLinesBodyV148');
    if(!body) return;
    const mode = $('batchVatModeV148')?.value || 'exclusive';
    const lines = getLines();
    let totalNet = 0, totalVat = 0, totalGross = 0;
    body.innerHTML = lines.map(l => {
      const q = N(l.quantity);
      const c = calcUnit(l.unit_price, mode);
      const lineNet = c.net * q;
      const lineVat = c.vat * q;
      const lineGross = c.gross * q;
      totalNet += lineNet; totalVat += lineVat; totalGross += lineGross;
      return `<tr>
        <td>${E(l.product_code)}</td>
        <td><b>${E(l.item_name)}</b><br><small>${E(l.category || '')} / ${E(l.item_type || '')}</small></td>
        <td>${qty(q)} ${E(l.unit || 'حبة')}</td>
        <td>${money(c.net)} ر.س</td>
        <td>${money(c.vat)} ر.س</td>
        <td>${money(c.gross)} ر.س</td>
        <td>${money(lineGross)} ر.س</td>
        <td><button class="danger" type="button" onclick="stockBatchRemoveLineV148('${E(l.product_code)}')">حذف</button></td>
      </tr>`;
    }).join('') || '<tr><td colspan="8">لم تتم إضافة أصناف بعد</td></tr>';
    const totals = $('batchTotalsV148');
    if(totals){
      totals.innerHTML = `<b>Total amount before 15% VAT:</b> ${money(totalNet)} SAR &nbsp; | &nbsp; <b>Total VAT 15%:</b> ${money(totalVat)} SAR &nbsp; | &nbsp; <b>Total Value with 15% VAT:</b> ${money(totalGross)} SAR`;
    }
  };

  function patchRemoveClearV183(){
    window.stockBatchRemoveLineV148 = function(code){
      window.batchLinesV148 = getLines().filter(l => String(l.product_code) !== String(code));
      window.stockBatchRenderLinesV148();
    };
    const oldClear = window.stockBatchClearV148;
    if(oldClear && !oldClear.v183Wrapped){
      window.stockBatchClearV148 = function(){
        const out = oldClear.apply(this, arguments);
        window.batchLinesV148 = [];
        ensureUnitFieldV183();
        window.stockBatchRenderLinesV148();
        return out;
      };
      window.stockBatchClearV148.v183Wrapped = true;
    }
  }

  function patchPrintV183(){
    window.stockBatchPrintV148 = function(batch){
      const rows = (batch.lines || []).map(l => {
        const unitNet = N(l.unit_cost || l.unit_price_before_vat);
        const unitVat = N(l.unit_vat || (unitNet > 0 ? unitNet * VAT : 0));
        const unitGross = N(l.unit_gross || unitNet + unitVat);
        const lineGross = N(l.line_gross || unitGross * N(l.quantity));
        return `<tr><td>${E(l.product_code)}</td><td>${E(l.item_name)}</td><td>${E(l.category || '')}</td><td>${E(l.item_type || '')}</td><td>${E(l.unit || 'حبة')}</td><td>${qty(l.quantity)}</td><td>${money(unitNet)} SAR</td><td>${money(unitVat)} SAR</td><td>${money(unitGross)} SAR</td><td>${money(lineGross)} SAR</td></tr>`;
      }).join('');
      const body = `<div class="grid"><div class="box"><b>ID</b><br>${E(batch.invoice_no)}</div><div class="box"><b>Date</b><br>${E(batch.batch_date)}</div><div class="box"><b>Warehouse / Supplier</b><br>${E(batch.supplier)}</div></div><h2>Products</h2><table><thead><tr><th>Product Code</th><th>Product Name</th><th>Category</th><th>Type</th><th>Unit</th><th>Quantity Added</th><th>Unit Price</th><th>VAT 15%</th><th>Unit Price with VAT</th><th>Value</th></tr></thead><tbody>${rows || '<tr><td colspan="10">لا توجد أصناف</td></tr>'}</tbody></table><div style="margin-top:22px;font-size:16px;line-height:2"><b>Total amount before 15% VAT:</b> ${money(batch.total_before_vat)} SAR<br><b>Total VAT 15%:</b> ${money(batch.total_vat)} SAR<br><b>Total Value with 15% VAT:</b> ${money(batch.total_with_vat)} SAR</div>`;
      if(typeof window.financePrintWindow === 'function') financePrintWindow('Added Product', body);
      else { const w = window.open('', '_blank'); w.document.write(body); w.print(); }
    };
  }

  function patchInvoiceViewV183(){
    const oldView = window.viewStockBatchV149;
    if(oldView && !oldView.v183Wrapped){
      window.viewStockBatchV149 = function(idx){
        const r = (window.stockBatchesV148 || [])[Number(idx)];
        if(!r) return oldView.apply(this, arguments);
        oldView.apply(this, arguments);
        setTimeout(() => {
          document.querySelectorAll('#invoiceViewModalV149 table tbody tr').forEach(tr => {
            // مجرد ضمان عدم ترك خلايا VAT قديمة إذا كان السعر صفر.
            const cells = [...tr.children];
            if(cells.length >= 10 && N(cells[6]?.textContent) <= 0){
              cells[7].textContent = '0.00'; cells[8].textContent = '0.00'; cells[9].textContent = '0.00';
            }
          });
        }, 50);
      };
      window.viewStockBatchV149.v183Wrapped = true;
    }
  }

  function bootV183(){
    ensureUnitFieldV183(); enhancePriceLabelsV183(); patchAddLineV183(); patchRemoveClearV183(); patchPrintV183(); patchInvoiceViewV183();
    try{ window.stockBatchRenderLinesV148 && window.stockBatchRenderLinesV148(); }catch(e){ console.warn(e); }
  }

  window.addEventListener('load', () => setTimeout(bootV183, 700));
  document.addEventListener('click', () => setTimeout(bootV183, 80), true);
  document.addEventListener('input', (e) => { if(e.target && ['batchQtyV148','batchUnitPriceV148','batchUnitV148'].includes(e.target.id)) setTimeout(() => window.stockBatchRenderLinesV148 && window.stockBatchRenderLinesV148(), 20); }, true);
  setTimeout(bootV183, 1200);
  console.log('V183 inventory invoice row final fix loaded');
})();

/* ================= V184 Invoice Unit Visible + Invoice Values Final Fix =================
   Fixes:
   - Shows the unit field clearly in the stock invoice item form.
   - Adds a separate Unit column in the draft invoice table.
   - Keeps VAT/unit prices consistent in invoice preview and print, even for old saved invoices.
============================================================================ */
(function(){
  'use strict';
  const $ = id => document.getElementById(id);
  const N = v => { const x = parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const S = v => String(v ?? '');
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const M = v => N(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  const Q = v => N(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const VAT = 0.15;

  function unitOptions(){ return ['حبة','لتر','كرتون','علبة','متر','كيس','رول','جالون','عبوة','طقم','زوج','درزن','كيلو','ملي','أخرى']; }
  function calc(price, mode){
    price = N(price); mode = mode || 'exclusive';
    if(price <= 0) return {net:0, vat:0, gross:0};
    if(mode === 'inclusive'){ const net = price / (1 + VAT); return {net, vat:price-net, gross:price}; }
    if(mode === 'exempt') return {net:price, vat:0, gross:price};
    return {net:price, vat:price*VAT, gross:price*(1+VAT)};
  }
  function lines(){ return Array.isArray(window.batchLinesV148) ? window.batchLinesV148 : []; }
  function currentMode(){ return $('batchVatModeV148')?.value || 'exclusive'; }

  function ensureUnitDatalistV184(){
    let dl = $('unitOptionsV184');
    if(!dl){ dl = document.createElement('datalist'); dl.id = 'unitOptionsV184'; document.body.appendChild(dl); }
    dl.innerHTML = unitOptions().map(u => `<option value="${E(u)}"></option>`).join('');
  }

  function ensureInvoiceUnitFieldV184(){
    ensureUnitDatalistV184();
    const qty = $('batchQtyV148');
    const price = $('batchUnitPriceV148');
    if(!qty) return;

    let unit = $('batchUnitV148');
    if(!unit){
      const holder = document.createElement('div');
      holder.id = 'batchUnitHolderV184';
      holder.innerHTML = `<label>نوع الكمية / الوحدة</label><input id="batchUnitV148" list="unitOptionsV184" placeholder="حبة / لتر / كرتون / علبة" value="حبة">`;
      const split = qty.closest('.split') || qty.parentElement?.parentElement;
      const qtyBox = qty.closest('div');
      if(split && qtyBox) split.insertBefore(holder, qtyBox);
      else qty.parentElement?.insertAdjacentElement('beforebegin', holder);
      unit = $('batchUnitV148');
    }
    const box = unit.closest('div');
    if(box){
      box.hidden = false;
      box.style.display = '';
      box.classList.remove('hidden','v161-hide-advanced-field','v180-hidden');
      const lab = box.querySelector('label');
      if(lab) lab.textContent = 'نوع الكمية / الوحدة';
    }
    unit.hidden = false;
    unit.style.display = '';
    unit.disabled = false;
    unit.setAttribute('list','unitOptionsV184');
    unit.placeholder = 'حبة / لتر / كرتون / علبة';
    if(!unit.value) unit.value = 'حبة';

    // Make sure the row visually contains unit, quantity, and price in an obvious order.
    if(price){
      const pl = price.closest('div')?.querySelector('label');
      if(pl) pl.textContent = 'سعر الوحدة';
      price.placeholder = 'اكتب السعر حسب حالة الضريبة المختارة';
    }
  }

  function ensureDraftHeaderV184(){
    const body = $('batchLinesBodyV148');
    if(!body) return;
    const table = body.closest('table');
    const tr = table?.querySelector('thead tr');
    if(!tr) return;
    const wanted = ['كود المنتج','الصنف','الكمية','الوحدة','سعر قبل الضريبة','VAT 15%','سعر شامل','الإجمالي شامل','حذف'];
    tr.innerHTML = wanted.map(h => `<th>${h}</th>`).join('');
  }

  function renderDraftRowsV184(){
    const body = $('batchLinesBodyV148');
    if(!body) return;
    ensureDraftHeaderV184();
    const mode = currentMode();
    let totalNet=0, totalVat=0, totalGross=0;
    body.innerHTML = lines().map(l => {
      const c = calc(l.unit_price, mode);
      const q = N(l.quantity);
      const lineNet = c.net*q, lineVat = c.vat*q, lineGross = c.gross*q;
      totalNet += lineNet; totalVat += lineVat; totalGross += lineGross;
      return `<tr>
        <td>${E(l.product_code)}</td>
        <td><b>${E(l.item_name)}</b><br><small>${E(l.category||'')} / ${E(l.item_type||'')}</small></td>
        <td>${Q(q)}</td>
        <td>${E(l.unit || 'حبة')}</td>
        <td>${M(c.net)} ر.س</td>
        <td>${M(c.vat)} ر.س</td>
        <td>${M(c.gross)} ر.س</td>
        <td>${M(lineGross)} ر.س</td>
        <td><button class="danger" type="button" onclick="stockBatchRemoveLineV148('${E(l.product_code)}')">حذف</button></td>
      </tr>`;
    }).join('') || '<tr><td colspan="9">لم تتم إضافة أصناف بعد</td></tr>';
    const totals = $('batchTotalsV148');
    if(totals) totals.innerHTML = `<b>Total amount before 15% VAT:</b> ${M(totalNet)} SAR &nbsp; | &nbsp; <b>Total VAT 15%:</b> ${M(totalVat)} SAR &nbsp; | &nbsp; <b>Total Value with 15% VAT:</b> ${M(totalGross)} SAR`;
  }

  function patchAddLineV184(){
    const old = window.stockBatchAddLineV148;
    if(!old || old.v184Wrapped) return;
    window.stockBatchAddLineV148 = function(){
      ensureInvoiceUnitFieldV184();
      const unit = $('batchUnitV148');
      if(unit && !unit.value) unit.value = 'حبة';
      const beforeLen = lines().length;
      const codeBefore = S($('batchProductCodeV148')?.value).trim();
      const out = old.apply(this, arguments);
      const arr = lines();
      const target = arr.find(l => S(l.product_code).trim() === codeBefore) || arr[arr.length-1];
      if(target && !target.unit) target.unit = unit?.value || 'حبة';
      window.batchLinesV148 = arr;
      ensureInvoiceUnitFieldV184();
      renderDraftRowsV184();
      return out;
    };
    window.stockBatchAddLineV148.v184Wrapped = true;
  }

  function patchRenderV184(){
    window.stockBatchRenderLinesV148 = renderDraftRowsV184;
    const oldRemove = window.stockBatchRemoveLineV148;
    window.stockBatchRemoveLineV148 = function(code){
      if(typeof oldRemove === 'function') oldRemove.apply(this, arguments);
      else window.batchLinesV148 = lines().filter(l => S(l.product_code) !== S(code));
      renderDraftRowsV184();
    };
  }

  function normalizeLineValues(l, batch){
    const q = N(l.quantity);
    let unitNet = N(l.unit_cost || l.unit_price_before_vat || l.unit_price);
    let unitVat = N(l.unit_vat);
    let unitGross = N(l.unit_gross || l.unit_price_with_vat);
    let lineNet = N(l.line_net || l.total_before_vat);
    let lineVat = N(l.line_vat || l.total_vat);
    let lineGross = N(l.line_gross || l.total_with_vat);

    // Repair old saved lines that have only invoice totals but missing line prices.
    if(q > 0 && unitNet <= 0 && lineNet > 0) unitNet = lineNet / q;
    if(q > 0 && unitNet <= 0 && (batch?.lines||[]).length === 1 && N(batch.total_before_vat) > 0) unitNet = N(batch.total_before_vat) / q;

    if(unitNet > 0 && unitVat <= 0) unitVat = unitNet * VAT;
    if(unitNet > 0 && unitGross <= 0) unitGross = unitNet + unitVat;
    if(q > 0 && lineNet <= 0) lineNet = unitNet * q;
    if(q > 0 && lineVat <= 0) lineVat = unitVat * q;
    if(q > 0 && lineGross <= 0) lineGross = unitGross * q;

    // If exempt/inclusive totals indicate zero VAT, respect it only when batch total VAT is zero.
    if(N(batch?.total_vat) === 0 && N(batch?.total_before_vat) > 0){
      unitVat = 0; lineVat = 0; unitGross = unitNet; lineGross = lineNet;
    }
    return {q, unitNet, unitVat, unitGross, lineNet, lineVat, lineGross};
  }

  function patchInvoiceViewV184(){
    window.viewStockBatchV149 = function(idx){
      const r = (window.stockBatchesV148 || [])[Number(idx)];
      if(!r) return;
      let modal = $('invoiceViewModalV149');
      if(!modal){ modal = document.createElement('div'); modal.id = 'invoiceViewModalV149'; modal.className = 'v149-modal-card'; document.body.appendChild(modal); }
      const rows = (r.lines || []).map(l => {
        const v = normalizeLineValues(l, r);
        return `<tr><td>${E(l.product_code)}</td><td>${E(l.item_name)}</td><td>${E(l.category||'')}</td><td>${E(l.item_type||'')}</td><td>${E(l.unit||'حبة')}</td><td>${Q(v.q)}</td><td>${M(v.unitNet)} ر.س</td><td>${M(v.unitVat)} ر.س</td><td>${M(v.unitGross)} ر.س</td><td>${M(v.lineGross)} ر.س</td></tr>`;
      }).join('');
      const idxReal = (window.stockBatchesV148 || []).indexOf(r);
      modal.innerHTML = `<button type="button" class="v149-close" onclick="closeInvoiceViewV149()">إغلاق</button><h2>فاتورة إدخال مخزون</h2><div class="v149-invoice-meta"><div class="v149-mini"><small>رقم الفاتورة</small><b>${E(r.invoice_no||r.id||'-')}</b></div><div class="v149-mini"><small>التاريخ</small><b>${E(r.batch_date||'-')}</b></div><div class="v149-mini"><small>المورد</small><b>${E(r.supplier||'-')}</b></div><div class="v149-mini"><small>عدد الأصناف</small><b>${(r.lines||[]).length}</b></div></div><div class="table-wrap"><table><thead><tr><th>كود المنتج</th><th>الصنف</th><th>التصنيف</th><th>النوع</th><th>الوحدة</th><th>الكمية</th><th>سعر قبل الضريبة</th><th>VAT 15%</th><th>سعر شامل</th><th>الإجمالي شامل</th></tr></thead><tbody>${rows || '<tr><td colspan="10">لا توجد أصناف</td></tr>'}</tbody></table></div><div class="footer-note"><b>Total amount before 15% VAT:</b> ${M(r.total_before_vat)} SAR<br><b>Total VAT 15%:</b> ${M(r.total_vat)} SAR<br><b>Total Value with 15% VAT:</b> ${M(r.total_with_vat)} SAR</div><div class="actions"><button type="button" onclick="stockBatchPrintSavedV148('${idxReal}')">طباعة</button></div>`;
      modal.classList.add('show'); const bd = $('v149Backdrop'); if(bd) bd.classList.add('show');
    };
  }

  function patchPrintV184(){
    window.stockBatchPrintV148 = function(batch){
      const rows = (batch.lines || []).map(l => {
        const v = normalizeLineValues(l, batch);
        return `<tr><td>${E(l.product_code)}</td><td>${E(l.item_name)}</td><td>${E(l.category||'')}</td><td>${E(l.item_type||'')}</td><td>${E(l.unit||'حبة')}</td><td>${Q(v.q)}</td><td>${M(v.unitNet)} SAR</td><td>${M(v.unitVat)} SAR</td><td>${M(v.unitGross)} SAR</td><td>${M(v.lineGross)} SAR</td></tr>`;
      }).join('');
      const body = `<div class="grid"><div class="box"><b>ID</b><br>${E(batch.invoice_no)}</div><div class="box"><b>Date</b><br>${E(batch.batch_date)}</div><div class="box"><b>Warehouse / Supplier</b><br>${E(batch.supplier)}</div></div><h2>Products</h2><table><thead><tr><th>Product Code</th><th>Product Name</th><th>Category</th><th>Type</th><th>Unit</th><th>Quantity Added</th><th>Unit Price</th><th>VAT 15%</th><th>Unit Price with VAT</th><th>Value</th></tr></thead><tbody>${rows || '<tr><td colspan="10">لا توجد أصناف</td></tr>'}</tbody></table><div style="margin-top:22px;font-size:16px;line-height:2"><b>Total amount before 15% VAT:</b> ${M(batch.total_before_vat)} SAR<br><b>Total VAT 15%:</b> ${M(batch.total_vat)} SAR<br><b>Total Value with 15% VAT:</b> ${M(batch.total_with_vat)} SAR</div>`;
      if(typeof window.financePrintWindow === 'function') financePrintWindow('Added Product', body);
      else { const w=window.open('', '_blank'); w.document.write(body); w.print(); }
    };
  }

  function bootV184(){
    ensureInvoiceUnitFieldV184();
    ensureDraftHeaderV184();
    patchAddLineV184();
    patchRenderV184();
    patchInvoiceViewV184();
    patchPrintV184();
    renderDraftRowsV184();
  }

  // Run when modal is opened or tabs are changed.
  window.addEventListener('load', () => setTimeout(bootV184, 600));
  document.addEventListener('click', () => setTimeout(bootV184, 60), true);
  document.addEventListener('input', (e) => { if(e.target && ['batchQtyV148','batchUnitPriceV148','batchUnitV148'].includes(e.target.id)) setTimeout(renderDraftRowsV184, 30); }, true);
  setInterval(() => { if($('stockBatchCardV148')?.classList.contains('v149-open')) bootV184(); }, 800);
  setTimeout(bootV184, 1200);
  console.log('V184 invoice unit visible and values fix loaded');
})();

/* ================= V185 Invoice Unit Dropdown Final Visible Fix =================
   يظهر خانة نوع الكمية/الوحدة فعليًا داخل نافذة إضافة فاتورة المخزون.
   السبب السابق: حقل الوحدة كان موجودًا لكن مخفيًا بسبب كلاس قديم v161-hide-advanced-field.
============================================================================ */
(function(){
  'use strict';
  const $ = (id)=>document.getElementById(id);
  const units = ['حبة','لتر','كرتون','علبة','متر','كيس','رول','جالون','عبوة','طقم','زوج','درزن','كيلو','ملي','أخرى'];
  function ensureStyleV185(){
    if($('v185UnitStyle')) return;
    const st=document.createElement('style'); st.id='v185UnitStyle'; st.textContent=`
      #batchUnitVisibleHolderV185{display:block!important;visibility:visible!important;opacity:1!important;position:relative!important;z-index:2!important}
      #batchUnitVisibleHolderV185 label{display:block!important;font-weight:800!important;color:#064637!important;margin-bottom:6px!important;text-align:right!important}
      #batchUnitV148{display:block!important;visibility:visible!important;opacity:1!important;width:100%!important;height:42px!important;border:1px solid #cfe2dc!important;border-radius:12px!important;background:#fff!important;color:#064637!important;padding:8px 12px!important;box-sizing:border-box!important}
      #stockBatchCardV148 .v161-hide-advanced-field:has(#batchUnitV148){display:block!important;visibility:visible!important;opacity:1!important}
      #stockBatchCardV148 .split #batchUnitVisibleHolderV185{grid-column:auto!important}
    `; document.head.appendChild(st);
  }
  function ensureDataListV185(){
    let dl=$('unitOptionsV185');
    if(!dl){ dl=document.createElement('datalist'); dl.id='unitOptionsV185'; document.body.appendChild(dl); }
    dl.innerHTML=units.map(u=>`<option value="${u}"></option>`).join('');
  }
  function forceUnitFieldV185(){
    ensureStyleV185(); ensureDataListV185();
    const qty=$('batchQtyV148');
    if(!qty) return;
    const qtyBox=qty.closest('div');
    const split=qtyBox?.parentElement;
    let holder=$('batchUnitVisibleHolderV185');
    let unit=$('batchUnitV148');

    if(!holder){
      holder=document.createElement('div');
      holder.id='batchUnitVisibleHolderV185';
      holder.innerHTML='<label>نوع الكمية / الوحدة</label>';
    }
    if(!unit){
      unit=document.createElement('input');
      unit.id='batchUnitV148';
      unit.value='حبة';
    }

    // انقل الحقل من أي صندوق قديم مخفي إلى صندوق ظاهر جديد
    const oldBox=unit.closest('div');
    if(oldBox){ oldBox.classList.remove('v161-hide-advanced-field','hidden','v180-hidden'); oldBox.hidden=false; oldBox.style.display=''; }
    holder.classList.remove('v161-hide-advanced-field','hidden','v180-hidden');
    holder.hidden=false;
    holder.style.cssText='display:block!important;visibility:visible!important;opacity:1!important;';
    unit.hidden=false; unit.disabled=false;
    unit.setAttribute('list','unitOptionsV185');
    unit.placeholder='اختر أو اكتب: حبة / لتر / كرتون';
    unit.style.cssText='display:block!important;visibility:visible!important;opacity:1!important;width:100%!important;';
    if(!unit.value) unit.value='حبة';
    if(unit.parentElement!==holder) holder.appendChild(unit);

    // ضعه داخل نفس صف الكمية قبل الكمية مباشرة
    if(split && qtyBox && holder.parentElement!==split){ split.insertBefore(holder, qtyBox); }
    else if(!holder.parentElement && qtyBox){ qtyBox.insertAdjacentElement('beforebegin', holder); }

    // تأكد من العنوان
    const lab=holder.querySelector('label'); if(lab) lab.textContent='نوع الكمية / الوحدة';
  }

  // لف زر فتح فاتورة المخزون حتى يظهر الحقل مباشرة عند الفتح
  const oldOpen=window.openStockBatchModalV149;
  if(oldOpen && !oldOpen.v185Wrapped){
    window.openStockBatchModalV149=function(){ const out=oldOpen.apply(this,arguments); setTimeout(forceUnitFieldV185,30); setTimeout(forceUnitFieldV185,250); return out; };
    window.openStockBatchModalV149.v185Wrapped=true;
  }

  // لف إضافة السطر حتى لا يضيع نوع الوحدة
  const oldAdd=window.stockBatchAddLineV148;
  if(oldAdd && !oldAdd.v185Wrapped){
    window.stockBatchAddLineV148=function(){
      forceUnitFieldV185();
      const u=$('batchUnitV148'); if(u && !u.value) u.value='حبة';
      const code=String($('batchProductCodeV148')?.value||'').trim();
      const out=oldAdd.apply(this,arguments);
      const arr=Array.isArray(window.batchLinesV148)?window.batchLinesV148:[];
      const line=arr.find(x=>String(x.product_code||'').trim()===code) || arr[arr.length-1];
      if(line) line.unit=(u?.value||line.unit||'حبة');
      window.batchLinesV148=arr;
      if(typeof window.stockBatchRenderLinesV148==='function') window.stockBatchRenderLinesV148();
      setTimeout(forceUnitFieldV185,20);
      return out;
    };
    window.stockBatchAddLineV148.v185Wrapped=true;
  }

  // بعد تفريغ السطر أرجع الوحدة الافتراضية
  const oldClear=window.stockBatchClearLineV148;
  if(oldClear && !oldClear.v185Wrapped){
    window.stockBatchClearLineV148=function(){ const out=oldClear.apply(this,arguments); setTimeout(()=>{forceUnitFieldV185(); const u=$('batchUnitV148'); if(u) u.value='حبة';},20); return out; };
    window.stockBatchClearLineV148.v185Wrapped=true;
  }

  document.addEventListener('click',()=>setTimeout(forceUnitFieldV185,60),true);
  document.addEventListener('focusin',()=>setTimeout(forceUnitFieldV185,30),true);
  window.addEventListener('load',()=>setTimeout(forceUnitFieldV185,700));
  setInterval(()=>{ if($('stockBatchCardV148')?.classList.contains('v149-open')) forceUnitFieldV185(); },500);
  setTimeout(forceUnitFieldV185,1200);
  console.log('V185 invoice unit dropdown final visible fix loaded');
})();


/* ===== V194: Custody request real flow + editable permissions + delete requests ===== */
(function(){
  'use strict';
  const VERSION='V194';
  const $id = id => document.getElementById(id);
  const n = v => Number(v || 0) || 0;
  const e = v => (typeof esc === 'function' ? esc(v) : String(v ?? '').replace(/[&<>"']/g, s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[s])));
  const msg2 = (m,t) => (typeof msg === 'function' ? msg(m,t) : alert(m));
  const current = () => (typeof session === 'function' ? (session()||{}) : {});
  const parsePerms = u => { let p=(u||{}).permissions||{}; if(typeof p==='string'){ try{p=JSON.parse(p||'{}')}catch(_){p={}} } return p||{}; };
  function hasPerm(k){ const u=current(); const p=parsePerms(u); return u.role==='admin' || u.role==='general_manager' || p[k]===true; }
  function canEditRequests(){ const u=current(); return hasPerm('can_edit_inventory_requests') || ['admin','general_manager','operations_manager','warehouse_manager'].includes(u.role); }
  function canDeleteRequests(){ const u=current(); return hasPerm('can_delete_inventory_requests') || ['admin','general_manager','operations_manager','warehouse_manager'].includes(u.role); }
  function lineItems(r){ return (typeof v118LineItems==='function') ? v118LineItems(r) : (Array.isArray(r?.request_items)?r.request_items:(Array.isArray(r?.items)?r.items:[])); }
  function roleArabic(role){ return ({admin:'إداري',general_manager:'إداري',operations_manager:'إداري',financial_manager:'إداري',warehouse_manager:'إداري',supervisor:'مشرف',technician:'فني'}[role] || role || ''); }
  function uniquePeople(){
    const out=[]; const seen=new Set();
    const add=(id,name,role)=>{ name=String(name||'').trim(); if(!name) return; const key=(String(id||'')+'|'+name).toLowerCase(); const nameKey=name.toLowerCase(); if(seen.has(key)||seen.has(nameKey)) return; seen.add(key); seen.add(nameKey); out.push({id:id||name, name, role:role||''}); };
    (data.users||[]).forEach(u=>{ const role=u.role||''; if(['supervisor','technician','admin','general_manager','operations_manager','financial_manager','warehouse_manager'].includes(role)) add(u.id,u.full_name||u.name||u.username,role); });
    (data.supervisors||[]).forEach(s=>add(s.id,s.full_name||s.name||s.username,'supervisor'));
    return out;
  }
  function fillRequesterSelect(id, selected){ const el=$id(id); if(!el) return; const old=selected ?? el.value; const rows=uniquePeople(); el.innerHTML='<option value="">اختر الطالب</option>'+rows.map(p=>`<option value="${e(p.id)}" data-name="${e(p.name)}" data-role="${e(p.role)}">${e(p.name)}${p.role?' - '+e(roleArabic(p.role)):''}</option>`).join(''); if(old) el.value=String(old); }
  window.financeFillSupervisorSelect = function(){ fillRequesterSelect('inventoryRequestSupervisor'); fillRequesterSelect('supInventoryRequestSupervisor'); };

  function hideRequestProjectCost(){
    ['inventoryRequestProject','inventoryRequestCostCenter','supInventoryRequestProject','supInventoryRequestCostCenter'].forEach(id=>{
      const el=$id(id); if(!el) return; const wrap=el.closest('.split') || el.closest('div') || el.parentElement; if(wrap){ wrap.classList.add('custody-hidden-v194'); wrap.style.display='none'; }
      el.removeAttribute('required'); el.value='';
    });
    const supLabel=$id('inventoryRequestSupervisor')?.closest('label') || document.querySelector('label[for="inventoryRequestSupervisor"]');
    const adminSel=$id('inventoryRequestSupervisor'); if(adminSel){ const lab=[...document.querySelectorAll('label')].find(l=>l.nextElementSibling===adminSel || l.contains(adminSel)); if(lab && !lab.querySelector('select')) lab.textContent='اسم الطالب (مشرف / فني / إداري)'; }
    const supForm=$id('supInventoryRequestItem')?.closest('section');
    const adminForm=$id('inventoryRequestItem')?.closest('.card');
    [adminForm,supForm].forEach(form=>{ if(!form) return; const labels=[...form.querySelectorAll('label')]; labels.forEach(l=>{ if(/المشرف الطالب|المشرف/.test(l.textContent||'')) l.textContent='اسم الطالب (مشرف / فني / إداري)'; }); });
    fillRequesterSelect('inventoryRequestSupervisor');
    fillRequesterSelect('supInventoryRequestSupervisor');
  }
  window.applyCustodyRequestUiV194 = hideRequestProjectCost;

  function getRequesterPayload(selectId){ const el=$id(selectId); const opt=el?.selectedOptions?.[0]; const id=el?.value||''; return {supervisor_id:id?Number(id)||null:null, supervisor_name: opt?.dataset?.name || opt?.textContent?.replace(/ - .*/,'')?.trim() || '', role_hint: opt?.dataset?.role || ''}; }
  function requestPath(){ return ['ops']; }

  window.inventorySaveRequest = async function(btn){
    try{
      if(btn) btn.disabled=true;
      hideRequestProjectCost();
      const requester=getRequesterPayload('inventoryRequestSupervisor');
      const requestId=$id('inventoryRequestId')?.value;
      if(!requester.supervisor_name) throw new Error('اختر اسم الطالب');
      if(!Array.isArray(inventoryRequestLines) || !inventoryRequestLines.length) throw new Error('أضف صنفًا واحدًا على الأقل إلى أمر الصرف');
      const lines=inventoryRequestLines.map(l=>({...l, quantity:n(l.quantity)}));
      const total=lines.reduce((a,l)=>a+n(l.quantity),0);
      const base={
        item_id:Number(lines[0].item_id), item_name:lines.map(x=>x.item_name).join('، '), quantity:total,
        request_items:lines, items:lines,
        project_id:null, project_name:null, cost_center_id:null, cost_center_name:null, cost_center_type:null,
        supervisor_id:requester.supervisor_id, supervisor_name:requester.supervisor_name,
        request_date:$id('inventoryRequestDate')?.value || (typeof today==='function'?today():new Date().toISOString().slice(0,10)),
        reason:$id('inventoryRequestReason')?.value||'', notes:$id('inventoryRequestNotes')?.value||''
      };
      if(requestId){
        const res=await sb.from('inventory_requests').update(base).eq('id',requestId); if(res.error) throw res.error; msg2('تم تحديث طلب الصرف');
      }else{
        const row={...base,status:'pending_ops',current_step:'ops',approval_path:requestPath(),approval_log:[]};
        const res=await sb.from('inventory_requests').insert(row); if(res.error) throw res.error; msg2('تم إرسال طلب الصرف إلى مدير التشغيل');
      }
      inventoryClearRequestForm();
      await financeLoadAll();
    }catch(err){ msg2(err.message||String(err),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };

  window.inventoryClearRequestForm = function(){
    ['inventoryRequestItem','inventoryRequestQty','inventoryRequestSupervisor','inventoryRequestReason','inventoryRequestNotes','inventoryRequestAvailable','inventoryRequestId'].forEach(id=>{ const el=$id(id); if(el) el.value=''; });
    ['inventoryRequestProject','inventoryRequestCostCenter'].forEach(id=>{ const el=$id(id); if(el) el.value=''; });
    if($id('inventoryRequestDate')) $id('inventoryRequestDate').value=(typeof today==='function'?today():new Date().toISOString().slice(0,10));
    if(typeof inventoryRequestLines!=='undefined') inventoryRequestLines=[];
    if(typeof inventoryRenderRequestLines==='function') inventoryRenderRequestLines();
    hideRequestProjectCost();
  };

  window.supervisorSaveInventoryRequest = async function(btn){
    try{
      if(btn) btn.disabled=true;
      const u=current(); const lines=(Array.isArray(window.supInventoryRequestLines)?window.supInventoryRequestLines:(typeof supInventoryRequestLines!=='undefined'?supInventoryRequestLines:[]));
      if(!lines.length) throw new Error('أضف صنفًا واحدًا على الأقل إلى أمر الصرف');
      const clean=lines.map(l=>({...l, quantity:n(l.quantity)})); const total=clean.reduce((a,l)=>a+n(l.quantity),0);
      const requesterName=u.full_name||u.name||u.username||'الطالب';
      const row={
        item_id:Number(clean[0].item_id), item_name:clean.map(x=>x.item_name).join('، '), quantity:total,
        request_items:clean, items:clean, project_id:null, project_name:null, cost_center_id:null, cost_center_name:null, cost_center_type:null,
        supervisor_id:u.id||null, supervisor_name:requesterName,
        request_date:$id('supInventoryRequestDate')?.value || (typeof today==='function'?today():new Date().toISOString().slice(0,10)),
        reason:$id('supInventoryRequestReason')?.value||'', notes:$id('supInventoryRequestNotes')?.value||'',
        status:'pending_ops', current_step:'ops', approval_path:requestPath(), approval_log:[]
      };
      const res=await sb.from('inventory_requests').insert(row); if(res.error) throw res.error;
      msg2('تم إرسال طلب الصرف إلى مدير التشغيل');
      if(typeof supInventoryRequestLines!=='undefined') supInventoryRequestLines=[]; window.supInventoryRequestLines=[];
      if(typeof renderSupInventoryRequestLines==='function') renderSupInventoryRequestLines();
      ['supInventoryRequestItem','supInventoryRequestQty','supInventoryRequestReason','supInventoryRequestNotes','supInventoryRequestProject','supInventoryRequestCostCenter'].forEach(id=>{ const el=$id(id); if(el) el.value=''; });
      if(typeof supervisorInventoryLoad==='function') await supervisorInventoryLoad();
    }catch(err){ msg2(err.message||String(err),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };

  window.inventoryDeleteRequest = async function(id, btn){
    try{
      if(!canDeleteRequests()) throw new Error('ليست لديك صلاحية حذف طلبات الصرف');
      if(!confirm('هل تريد حذف طلب الصرف؟')) return;
      if(btn) btn.disabled=true;
      const movements=(data.inventoryMovements||[]).filter(m=>String(m.request_id)===String(id));
      const hasFinal=movements.some(m=>['consume','consumption','return'].includes(String(m.movement_type||'')));
      if(hasFinal) throw new Error('لا يمكن حذف طلب عليه استهلاك أو مرتجع. احذف/راجع الحركات أولاً.');
      // عكس الصرف لو كان الطلب معتمدًا وخرجت كميات
      for(const m of movements.filter(m=>String(m.movement_type)==='out')){
        const item=(data.inventoryItems||[]).find(x=>String(x.id)===String(m.item_id));
        if(item){ await sb.from('inventory_items').update({quantity:n(item.quantity)+n(m.quantity)}).eq('id', item.id); }
      }
      await sb.from('inventory_movements').delete().eq('request_id', id);
      const res=await sb.from('inventory_requests').delete().eq('id', id); if(res.error) throw res.error;
      msg2('تم حذف طلب الصرف'); await financeLoadAll();
    }catch(err){ msg2(err.message||String(err),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };

  window.inventoryRenderRequests = function(){
    const b=$id('inventoryRequestsBody'); if(!b) return; hideRequestProjectCost();
    const table=b.closest('table');
    if(table && table.tHead){ table.tHead.innerHTML='<tr><th>التاريخ</th><th>اسم الطالب</th><th>الأصناف</th><th>الكمية</th><th>سبب الصرف</th><th>الحالة</th><th>إجراء</th></tr>'; }
    let rows=(typeof financeFilterRows==='function')?financeFilterRows(data.inventoryRequests||[],'request_date'):(data.inventoryRequests||[]);
    const st=$id('inventoryRequestStatusFilter')?.value; if(st) rows=rows.filter(r=>r.status===st);
    b.innerHTML=rows.map(r=>{
      const arr=lineItems(r); const total=arr.reduce((a,l)=>a+n(l.quantity),0)||n(r.quantity);
      const itemsHtml=arr.length?arr.map(l=>`${e(l.product_code||'')} - <b>${e(l.item_name||'')}</b> × ${n(l.quantity)} ${e(l.unit||'')}`).join('<br>'):e(r.item_name||'-');
      const actions=[];
      if(typeof inventoryCanApprove==='function' && inventoryCanApprove(r)) actions.push(`<button onclick="inventoryApproveRequest(${r.id},this)">اعتماد</button>`);
      if(canEditRequests()) actions.push(`<button class="light" onclick="inventoryEditRequest(${r.id})">تعديل</button>`);
      if(canDeleteRequests()) actions.push(`<button class="danger" onclick="inventoryDeleteRequest(${r.id},this)">حذف</button>`);
      if(r.status==='approved' && typeof inventoryOpenConsumption==='function') actions.push(`<button class="light" onclick="inventoryOpenConsumption(${r.id})">تسجيل استهلاك</button>`);
      if(typeof inventoryOpenRequestInvoice==='function') actions.push(`<button class="light" onclick="inventoryOpenRequestInvoice(${r.id})">عرض</button>`);
      return `<tr><td>${e(r.request_date||'')}</td><td><b>${e(r.supervisor_name||supervisorName?.(r.supervisor_id)||'-')}</b><br><small>${e(roleArabic(r.role_hint||''))}</small></td><td>${itemsHtml}</td><td>${total}</td><td>${e(r.reason||'-')}</td><td><span class="badge ${typeof inventoryRequestStatusClass==='function'?inventoryRequestStatusClass(r.status):''}">${e(typeof inventoryRequestStatusText==='function'?inventoryRequestStatusText(r.status):(r.status||'-'))}</span><br><small>المرحلة: ${e(typeof inventoryRequestNextRole==='function'?inventoryRequestNextRole(r.status):'')}</small></td><td class="row-actions">${actions.join(' ')||'-'}</td></tr>`;
    }).join('') || '<tr><td colspan="7">لا توجد طلبات صرف</td></tr>';
  };

  window.inventoryEditRequest = function(id){
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id)); if(!r) return;
    hideRequestProjectCost();
    if($id('inventoryRequestId')) $id('inventoryRequestId').value=r.id;
    if($id('inventoryRequestDate')) $id('inventoryRequestDate').value=String(r.request_date||'').slice(0,10) || (typeof today==='function'?today():new Date().toISOString().slice(0,10));
    fillRequesterSelect('inventoryRequestSupervisor', r.supervisor_id||'');
    if($id('inventoryRequestSupervisor') && r.supervisor_id) $id('inventoryRequestSupervisor').value=String(r.supervisor_id);
    if($id('inventoryRequestReason')) $id('inventoryRequestReason').value=r.reason||'';
    if($id('inventoryRequestNotes')) $id('inventoryRequestNotes').value=r.notes||'';
    if(typeof inventoryRequestLines!=='undefined') inventoryRequestLines=lineItems(r).map(l=>({...l}));
    if(typeof inventoryRenderRequestLines==='function') inventoryRenderRequestLines();
    const card=$id('inventoryRequestItem')?.closest('.card'); if(card){ card.scrollIntoView({behavior:'smooth',block:'start'}); const h=card.querySelector('h2'); if(h) h.textContent='تعديل طلب صرف من المخزون'; }
  };

  // تعديل عرض الفاتورة/التفاصيل: الصرف عهدة فقط ولا يظهر المشروع أو مركز التكلفة في أمر الصرف
  const oldReqInvoice = window.inventoryOpenRequestInvoice;
  window.inventoryOpenRequestInvoice = function(id){
    const r=(data.inventoryRequests||[]).find(x=>String(x.id)===String(id));
    if(!r || typeof smartOpen!=='function'){ if(typeof oldReqInvoice==='function') return oldReqInvoice(id); return; }
    const rows=lineItems(r).map(l=>`<tr><td>${e(l.product_code||'')}</td><td>${e(l.item_name||'')}</td><td>${n(l.quantity)} ${e(l.unit||'')}</td></tr>`).join('');
    smartOpen('أمر صرف عهدة', `<div class="smart-meta-v129"><div class="m"><small>رقم الطلب</small><b>REQ-${e(r.id)}</b></div><div class="m"><small>التاريخ</small><b>${e(r.request_date||'-')}</b></div><div class="m"><small>اسم الطالب</small><b>${e(r.supervisor_name||'-')}</b></div><div class="m"><small>الحالة</small><b>${e(typeof inventoryRequestStatusText==='function'?inventoryRequestStatusText(r.status):r.status)}</b></div></div><h3>الأصناف</h3><table><thead><tr><th>الكود</th><th>الصنف</th><th>الكمية</th></tr></thead><tbody>${rows}</tbody></table><h3>سبب الصرف</h3><p>${e(r.reason||'-')}</p><h3>ملاحظات</h3><p>${e(r.notes||'-')}</p>`, `<button onclick="inventoryPrintRequest && inventoryPrintRequest('${e(id)}')">طباعة</button>`);
  };

  // صلاحيات تعديل/حذف طلبات الصرف في إدارة المستخدمين
  function addRequestPermissionCheckboxes(){
    if(window.__tasneefUnifiedPermissionsOnly) return;
    const box=document.getElementById('userPermissionsBoxV72'); if(!box) return;
    const grid=box.querySelector('.perm-grid-v72') || box;
    [['can_edit_inventory_requests','تعديل طلبات الصرف'],['can_delete_inventory_requests','حذف طلبات الصرف']].forEach(([key,label])=>{
      if(!document.getElementById('perm_'+key)) grid.insertAdjacentHTML('beforeend', `<label class="perm-item-v72"><input type="checkbox" id="perm_${key}" data-perm="${key}"> <span>${label}</span></label>`);
    });
  }
  const oldSaveUser=window.saveUser;
  window.saveUser = async function(){
    addRequestPermissionCheckboxes();
    const id=$id('userId')?.value;
    const perms={}; document.querySelectorAll('[data-perm]').forEach(el=>{ perms[el.dataset.perm]=!!el.checked; });
    const row={full_name:$id('userFullName')?.value.trim(),username:$id('userUsername')?.value.trim(),password:$id('userPassword')?.value.trim()||'123456',role:$id('userRole')?.value,is_active:$id('userActive')?.value==='true',permissions:perms};
    if(!row.full_name||!row.username) return msg2('الاسم واسم المستخدم مطلوبان','err');
    let res=id?await sb.from('app_users').update(row).eq('id',id):await sb.from('app_users').insert(row);
    if(res.error && String(res.error.message||'').includes('permissions')){ const safe={...row}; delete safe.permissions; res=id?await sb.from('app_users').update(safe).eq('id',id):await sb.from('app_users').insert(safe); }
    if(res.error) return msg2(res.error.message,'err');
    msg2('تم حفظ المستخدم والصلاحيات'); if(typeof clearUserForm==='function') clearUserForm(); if(typeof refreshAll==='function') await refreshAll(); setTimeout(addRequestPermissionCheckboxes,100);
  };
  ['hydrateForms','renderUsers','editUser','clearUserForm'].forEach(fn=>{ const old=window[fn]; if(typeof old==='function'){ window[fn]=function(){ const r=old.apply(this,arguments); setTimeout(addRequestPermissionCheckboxes,100); return r; }; } });

  function boot(){ hideRequestProjectCost(); addRequestPermissionCheckboxes(); }
  window.addEventListener('DOMContentLoaded',()=>setTimeout(boot,300));
  window.addEventListener('load',()=>setTimeout(boot,900));
  const oldOpen=window.openFinanceTab; if(typeof oldOpen==='function'){ window.openFinanceTab=function(){ const r=oldOpen.apply(this,arguments); setTimeout(()=>{ hideRequestProjectCost(); if(arguments[0]==='requests' && typeof inventoryRenderRequests==='function') inventoryRenderRequests(); },50); return r; }; }
})();


/* V200 Stable Compatibility Layer - لا يغير آلية العمل، فقط يمنع تعطل الأزرار القديمة عند عدم وجود دالة */
(function(){
  if (window.__tasneefV200Compat) return;
  window.__tasneefV200Compat = true;

  window.enableSupervisorSounds = window.enableSupervisorSounds || function(){
    try {
      ['checkin','checkout','ticket'].forEach(function(name){
        var audio = new Audio('sounds/' + name + '.wav');
        audio.volume = 0.01;
        var p = audio.play();
        if (p && p.then) p.then(function(){ audio.pause(); audio.currentTime = 0; }).catch(function(){});
      });
      if (typeof msg === 'function') msg('تم تفعيل الأصوات');
    } catch(e) {}
  };

  window.tasneefSafeCall = window.tasneefSafeCall || function(fnName){
    if (typeof window[fnName] === 'function') {
      return window[fnName].apply(window, Array.prototype.slice.call(arguments, 1));
    }
    console.warn('Tasneef V200: missing function', fnName);
  };
})();

/* ================= V201 Inventory Reports Permissions Fix =================
   الهدف: إصلاح حساب المخزون، منع طباعة بيانات محذوفة قدر الإمكان،
   وإضافة صلاحيات مشاهدة/إضافة/تعديل/حذف/طباعة لكل تبويب بدون تغيير آلية العمل.
======================================================================= */
(function(){
  'use strict';
  if(window.__tasneefV201Loaded) return;
  window.__tasneefV201Loaded = true;

  const $ = id => document.getElementById(id);
  const S = v => String(v ?? '');
  const N = v => { const x = parseFloat(S(v).replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const Q = v => N(v).toLocaleString('en-US', { maximumFractionDigits: 2 });
  const M = v => N(v).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
  const arr = v => Array.isArray(v) ? v : [];
  const say = (txt,type) => { try{ if(typeof msg==='function') msg(txt,type); else alert(txt); }catch(_){ alert(txt); } };

  function currentUser(){
    try{ if(typeof session === 'function') return session() || {}; }catch(_){ }
    try{ return JSON.parse(localStorage.getItem('tasneef_user') || '{}') || {}; }catch(_){ return {}; }
  }
  function role(){ return S(currentUser().role); }
  function perms(){
    const u = currentUser();
    if(typeof u.permissions === 'string') { try{ return JSON.parse(u.permissions) || {}; }catch(_){ return {}; } }
    return u.permissions || {};
  }
  function isAdmin(){ return ['admin','مدير عام','system_admin'].includes(role()); }
  function hasPerm(key){
    if(isAdmin()) return true;
    const p = perms();
    if(Object.prototype.hasOwnProperty.call(p, key)) return !!p[key];
    // توافق مع الصلاحيات القديمة حتى لا تتعطل النسخة على المستخدمين القدامى
    const legacy = {
      tab_overview_view: 'can_reports',
      tab_reports_view: 'can_reports', tab_reports_print: 'can_reports',
      tab_monthly_view: 'can_monthly', tab_monthly_print: 'can_monthly',
      tab_users_view: 'can_manage_users', tab_users_add: 'can_manage_users', tab_users_edit: 'can_manage_users', tab_users_delete: 'can_manage_users',
      tab_workers_view: 'can_manage_workers', tab_workers_add: 'can_manage_workers', tab_workers_edit: 'can_manage_workers', tab_workers_delete: 'can_manage_workers',
      tab_inventory_view: 'can_manage_inventory', tab_inventory_add: 'can_manage_inventory', tab_inventory_edit: 'can_manage_inventory', tab_inventory_delete: 'can_manage_inventory', tab_inventory_print: 'can_manage_inventory',
      tab_catalog_view: 'can_manage_inventory', tab_catalog_add: 'can_manage_inventory', tab_catalog_edit: 'can_manage_inventory', tab_catalog_delete: 'can_manage_inventory', tab_catalog_print: 'can_manage_inventory',
      tab_movements_view: 'can_manage_inventory', tab_movements_add: 'can_manage_inventory', tab_movements_edit: 'can_manage_inventory', tab_movements_delete: 'can_manage_inventory', tab_movements_print: 'can_manage_inventory',
      tab_requests_view: 'can_inventory_requests', tab_requests_add: 'can_inventory_requests', tab_requests_edit: 'can_edit_inventory_requests', tab_requests_delete: 'can_delete_inventory_requests', tab_requests_print: 'can_inventory_requests',
      tab_expenses_view: 'can_expenses_inventory', tab_expenses_add: 'can_expenses_inventory', tab_expenses_edit: 'can_expenses_inventory', tab_expenses_delete: 'can_expenses_inventory', tab_expenses_print: 'can_expenses_inventory',
      tab_costCenters_view: 'can_expenses_inventory', tab_costCenters_add: 'can_expenses_inventory', tab_costCenters_edit: 'can_expenses_inventory', tab_costCenters_delete: 'can_expenses_inventory', tab_costCenters_print: 'can_expenses_inventory',
      tab_suppliers_view: 'can_manage_inventory', tab_suppliers_add: 'can_manage_inventory', tab_suppliers_edit: 'can_manage_inventory', tab_suppliers_delete: 'can_manage_inventory',
      tab_tickets_view: 'can_tickets', tab_tickets_add: 'can_tickets', tab_tickets_edit: 'can_tickets', tab_tickets_delete: 'can_tickets', tab_tickets_print: 'can_tickets',
      tab_attendance_view: 'can_attendance', tab_attendance_add: 'can_attendance', tab_attendance_edit: 'can_attendance', tab_attendance_delete: 'can_attendance', tab_attendance_print: 'can_attendance',
      tab_time_logs_view: 'can_time_logs', tab_time_logs_add: 'can_time_logs', tab_time_logs_edit: 'can_edit_time_logs', tab_time_logs_delete: 'can_edit_time_logs', tab_time_logs_print: 'can_time_logs'
    };
    return legacy[key] ? !!p[legacy[key]] : false;
  }
  window.tasneefCanV201 = hasPerm;

  const TAB_LABELS = {
    overview:'الملخص', expenses:'المصروفات', inventory:'المخزون', catalog:'الأصناف', movements:'حركة المخزون',
    requests:'طلبات الصرف', reports:'التقارير', costCenters:'مراكز التكلفة', suppliers:'الموردين', costReduction:'تقليل التكلفة',
    users:'إدارة المستخدمين', workers:'إدارة العمال', tickets:'التكتات', attendance:'الحضور والغياب', time_logs:'السجلات اليومية', monthly:'الأوقات الشهرية'
  };
  const ACTIONS = [['view','مشاهدة'],['add','إضافة'],['edit','تعديل'],['delete','حذف'],['print','طباعة/تصدير']];
  const CORE_TABS = ['overview','expenses','inventory','catalog','movements','requests','reports','costCenters','suppliers','users','workers','tickets','attendance','time_logs','monthly'];

  function ensurePermissionsMatrix(){
    const box = $('userPermissionsBoxV72');
    if(!box || $('v201PermMatrix')) return;
    const html = `<div id="v201PermMatrix" class="v201-perm-card">
      <h3>صلاحيات التبويبات</h3>
      <div class="v201-perm-actions"><button type="button" class="light" id="v201SelectAllPerms">تحديد الكل</button><button type="button" class="light" id="v201ClearAllPerms">إلغاء الكل</button></div>
      <div class="v201-perm-table-wrap"><table class="v201-perm-table"><thead><tr><th>التبويب</th>${ACTIONS.map(a=>`<th>${a[1]}</th>`).join('')}</tr></thead><tbody>
      ${CORE_TABS.map(t=>`<tr><td><b>${TAB_LABELS[t]}</b></td>${ACTIONS.map(a=>`<td><input type="checkbox" id="perm_tab_${t}_${a[0]}" data-perm="tab_${t}_${a[0]}"></td>`).join('')}</tr>`).join('')}
      </tbody></table></div><div class="footer-note">الصلاحية تطبق على ظهور التبويب والأزرار، وتمنع تنفيذ الإجراء عند محاولة استخدامه بدون صلاحية.</div></div>`;
    box.insertAdjacentHTML('beforeend', html);
    $('v201SelectAllPerms')?.addEventListener('click',()=>box.querySelectorAll('#v201PermMatrix input[data-perm]').forEach(x=>x.checked=true));
    $('v201ClearAllPerms')?.addEventListener('click',()=>box.querySelectorAll('#v201PermMatrix input[data-perm]').forEach(x=>x.checked=false));
  }

  function hydratePermissionChecks(){
    ensurePermissionsMatrix();
    const id = $('userId')?.value;
    let user = null;
    try{ user = arr(window.data?.appUsers || window.appUsers).find(u => S(u.id) === S(id)); }catch(_){ }
    if(!user) return;
    let p = user.permissions || {};
    if(typeof p === 'string'){ try{ p = JSON.parse(p) || {}; }catch(_){ p = {}; } }
    document.querySelectorAll('[data-perm]').forEach(el => { if(Object.prototype.hasOwnProperty.call(p, el.dataset.perm)) el.checked = !!p[el.dataset.perm]; });
  }

  ['renderUsers','editUser','clearUserForm','hydrateForms','financeHydrateForms'].forEach(fn=>{
    const old = window[fn];
    if(typeof old === 'function' && !old.__v201Wrapped){
      window[fn] = function(){ const r = old.apply(this, arguments); setTimeout(hydratePermissionChecks, 120); return r; };
      window[fn].__v201Wrapped = true;
    }
  });

  const oldSaveUser = window.saveUser;
  if(typeof oldSaveUser === 'function' && !oldSaveUser.__v201Wrapped){
    window.saveUser = async function(){
      ensurePermissionsMatrix();
      // نعتمد على دالة الحفظ الحالية لأنها تجمع كل data-perm، ونضيف عليها المصفوفة فقط.
      return oldSaveUser.apply(this, arguments);
    };
    window.saveUser.__v201Wrapped = true;
  }

  function tabKeyFromButton(btn){
    const oc = btn?.getAttribute?.('onclick') || '';
    const m = oc.match(/financeShowTab\(['"]([^'"]+)['"]/);
    if(m) return m[1] === 'items' ? 'catalog' : m[1];
    const text = S(btn?.textContent).replace(/\s+/g,' ').trim();
    const byText = {'الملخص':'overview','المصروفات':'expenses','المخزون':'inventory','الأصناف':'catalog','حركة المخزون':'movements','طلبات الصرف':'requests','التقارير':'reports','مراكز التكلفة':'costCenters','الموردين':'suppliers','تقليل التكلفة':'costReduction'};
    return byText[text] || '';
  }
  function applyTabPermissions(){
    if(isAdmin()) return;
    document.querySelectorAll('#financeDashboard .finance-tabs button, #financeDashboard .finance-tabs .finance-tab').forEach(btn=>{
      const key = tabKeyFromButton(btn);
      if(!key) return;
      const ok = hasPerm(`tab_${key}_view`);
      btn.hidden = !ok;
      btn.style.display = ok ? '' : 'none';
    });
  }
  function actionForButton(btn){
    const txt = S(btn.textContent).replace(/\s+/g,' ').trim();
    const oc = S(btn.getAttribute('onclick'));
    if(/حذف|delete/i.test(txt) || /financeDelete|Delete|delete/i.test(oc)) return 'delete';
    if(/تعديل|edit/i.test(txt) || /Edit|edit/i.test(oc)) return 'edit';
    if(/طباعة|تصدير|PDF|print/i.test(txt) || /Print|print|Report/i.test(oc)) return 'print';
    if(/إضافة|اضافة|جديد|حفظ|اعتماد|صرف|مرتجع|استهلاك|add|save|approve|return/i.test(txt+oc)) return 'add';
    return '';
  }
  function currentFinanceTab(){
    const active = document.querySelector('#financeDashboard .finance-tabs button.active, #financeDashboard .finance-tabs .finance-tab.active');
    return tabKeyFromButton(active) || S(window.financeCurrentTab || 'overview');
  }
  function applyActionPermissions(){
    if(isAdmin()) return;
    const tab = currentFinanceTab();
    document.querySelectorAll('#financeDashboard button').forEach(btn=>{
      if(btn.closest('.finance-tabs')) return;
      const act = actionForButton(btn); if(!act) return;
      const ok = hasPerm(`tab_${tab}_${act}`);
      btn.style.display = ok ? '' : 'none';
    });
  }
  const oldFinanceShowTab = window.financeShowTab;
  if(typeof oldFinanceShowTab === 'function' && !oldFinanceShowTab.__v201Wrapped){
    window.financeShowTab = function(tab, btn){
      const key = tab === 'items' ? 'catalog' : tab;
      if(!isAdmin() && !hasPerm(`tab_${key}_view`)){ say('لا تملك صلاحية مشاهدة هذا التبويب','err'); return; }
      const r = oldFinanceShowTab.apply(this, arguments);
      setTimeout(()=>{ applyTabPermissions(); applyActionPermissions(); },150);
      return r;
    };
    window.financeShowTab.__v201Wrapped = true;
  }

  function guard(action, tab, fnName){
    return function(oldFn){
      if(typeof oldFn !== 'function' || oldFn.__v201Guarded) return oldFn;
      const wrapped = function(){
        if(!hasPerm(`tab_${tab}_${action}`)){ say('لا تملك صلاحية '+({'add':'الإضافة','edit':'التعديل','delete':'الحذف','print':'الطباعة'}[action]||action),'err'); return; }
        return oldFn.apply(this, arguments);
      };
      wrapped.__v201Guarded = true;
      return wrapped;
    };
  }

  // حماية أهم الإجراءات التنفيذية، بجانب إخفاء الأزرار.
  window.inventoryEditItem = guard('edit','catalog','inventoryEditItem')(window.inventoryEditItem);
  window.inventoryEditMovement = guard('edit','movements','inventoryEditMovement')(window.inventoryEditMovement);
  window.inventoryEditRequest = guard('edit','requests','inventoryEditRequest')(window.inventoryEditRequest);
  window.inventoryPrintRequest = guard('print','requests','inventoryPrintRequest')(window.inventoryPrintRequest);
  window.inventoryPrintMovement = guard('print','movements','inventoryPrintMovement')(window.inventoryPrintMovement);

  const oldFinanceDelete = window.financeDelete;
  if(typeof oldFinanceDelete === 'function' && !oldFinanceDelete.__v201Wrapped){
    window.financeDelete = async function(table, id){
      let tab = currentFinanceTab();
      if(table === 'inventory_items') tab = 'catalog';
      if(table === 'inventory_movements') tab = 'movements';
      if(table === 'inventory_requests') tab = 'requests';
      if(!hasPerm(`tab_${tab}_delete`)){ say('لا تملك صلاحية الحذف','err'); return; }
      const r = await oldFinanceDelete.apply(this, arguments);
      clearReportCacheV201();
      await reloadFinanceFreshV201();
      return r;
    };
    window.financeDelete.__v201Wrapped = true;
  }

  // ===== المخزون: مصدر واحد للحساب يمنع تضاعف الصادر =====
  function itemCode(it){ return S(it?.product_code || it?.serial_number || it?.barcode || it?.supplier_barcode || it?.company_code || it?.id); }
  function matchItem(it, row){
    const id = S(it?.id), name = S(it?.name), code = itemCode(it);
    return S(row?.item_id) === id || S(row?.item_name) === name || (!!code && [row?.product_code,row?.barcode,row?.supplier_barcode,row?.company_code].map(S).includes(code));
  }
  function lineItems(r){
    let lines = r?.request_items || r?.items || r?.request_lines || r?.lines || [];
    if(typeof lines === 'string'){ try{ lines = JSON.parse(lines); }catch(_){ lines = []; } }
    if(Array.isArray(lines) && lines.length) return lines;
    if(r?.item_id || r?.item_name) return [{item_id:r.item_id,item_name:r.item_name,quantity:r.quantity,product_code:r.product_code,unit_cost:r.unit_cost}];
    return [];
  }
  function requestIsIssued(r){ return /approved|issued|صرف|معتمد|done|completed/i.test(S(r?.status)); }
  function itemStatsV201(it){
    const movements = arr(window.data?.inventoryMovements).filter(m => matchItem(it,m));
    let enteredMov = 0, enteredBatch = 0, issuedReq = 0, issuedMov = 0, returned = 0, direct = 0;
    movements.forEach(m=>{
      const t = S(m.movement_type).toLowerCase(); const q = Math.abs(N(m.quantity));
      if(['in','إدخال','add','invoice_add','purchase'].includes(t)) enteredMov += q;
      else if(['out','صرف','issue'].includes(t)) issuedMov += q;
      else if(['return','مرتجع','رجوع'].includes(t)) returned += q;
      else if(['consume','consumption','استهلاك'].includes(t)) direct += q;
    });
    arr(window.stockBatchesV148).forEach(b => arr(b.lines).forEach(l => { if(matchItem(it,l)) enteredBatch += Math.abs(N(l.quantity)); }));
    arr(window.data?.inventoryRequests).filter(requestIsIssued).forEach(r=>{
      lineItems(r).forEach(l=>{ if(matchItem(it,l)) issuedReq += Math.abs(N(l.quantity)); });
    });
    // إذا كانت أوامر الصرف موجودة نعتبرها المصدر الرسمي للصادر ونمنع جمعها مرة ثانية مع حركة out.
    const issued = issuedReq > 0 ? issuedReq : issuedMov;
    const netIssued = Math.max(0, issued - returned);
    const consumed = netIssued + direct;
    const entered = enteredMov > 0 ? enteredMov : enteredBatch;
    const remaining = Number.isFinite(N(it?.quantity)) ? N(it?.quantity) : Math.max(0, entered - consumed);
    return { entered, issued, returned, netIssued, direct, consumed, remaining, movements };
  }
  window.inventoryItemStatsV151 = itemStatsV201;
  window.inventoryProductMetricsV153 = function(id){
    const it = arr(window.data?.inventoryItems).find(x=>S(x.id)===S(id)) || {id};
    const st = itemStatsV201(it);
    return { movements:st.movements, issued:st.issued, returned:st.returned, directConsumed:st.direct, consumed:st.consumed };
  };

  function renderCatalogV201(){
    const body = $('inventoryItemsBody'); if(!body) return;
    const search = S($('financeSearch')?.value || $('inventorySearch')?.value).toLowerCase();
    const supplier = S($('inventoryReportSupplier')?.value || $('inventorySupplierFilter')?.value);
    const cat = S($('inventoryCategoryFilter')?.value || $('inventoryItemCategoryFilter')?.value);
    let rows = arr(window.data?.inventoryItems).slice();
    if(search) rows = rows.filter(it => [it.name,itemCode(it),it.supplier,it.category,it.unit,it.notes].map(S).join(' ').toLowerCase().includes(search));
    if(supplier) rows = rows.filter(it => S(it.supplier) === supplier);
    if(cat) rows = rows.filter(it => S(it.category) === cat);
    if(!rows.length){ body.innerHTML = '<tr><td colspan="20">لا توجد أصناف</td></tr>'; return; }
    const cards = rows.map(it=>{
      const st = itemStatsV201(it); const low = st.remaining <= N(it.min_quantity || it.reorder_level || 0);
      const img = S(it.image_url || it.product_image || it.image) ? `<img src="${E(it.image_url || it.product_image || it.image)}" alt="${E(it.name)}">` : '<span>لا توجد</span>';
      const canEdit = hasPerm('tab_catalog_edit'), canDel = hasPerm('tab_catalog_delete');
      return `<article class="v180-catalog-card"><div class="v180-item-head"><div class="v180-img">${img}</div><div class="v180-title"><h3>${E(it.name||'-')}</h3><small>كود المنتج: ${E(itemCode(it)||'-')}</small><br><small>المورد: ${E(it.supplier||'-')}</small></div><span class="${low?'v180-low':'v180-ok'}">${low?'منخفض':'متوفر'}</span></div><div class="v180-stats"><div><small>الكمية</small><b>${Q(st.remaining)} ${E(it.unit||'')}</b></div><div><small>دخل</small><b>${Q(st.entered)}</b></div><div><small>خرج</small><b>${Q(st.issued)}</b></div><div><small>مرتجع</small><b>${Q(st.returned)}</b></div><div><small>صافي الصادر</small><b>${Q(st.netIssued)}</b></div><div><small>استهلاك مباشر</small><b>${Q(st.direct)}</b></div><div><small>إجمالي مستهلك</small><b>${Q(st.consumed)}</b></div><div><small>حد الطلب</small><b>${Q(it.min_quantity||it.reorder_level||0)}</b></div></div><div class="row-actions v180-actions"><button type="button" onclick="inventoryOpenItemSmart&&inventoryOpenItemSmart('${E(it.id)}')">عرض المنتج</button>${canEdit?`<button type="button" class="light" onclick="inventoryEditItem&&inventoryEditItem('${E(it.id)}')">تعديل</button>`:''}${canDel?`<button type="button" class="danger" onclick="financeDelete&&financeDelete('inventory_items','${E(it.id)}')">حذف</button>`:''}</div></article>`;
    }).join('');
    body.innerHTML = `<tr class="v180-catalog-row"><td colspan="20"><div class="v180-catalog-grid">${cards}</div></td></tr>`;
    setTimeout(applyActionPermissions,50);
  }
  window.inventoryRenderItems = renderCatalogV201;

  window.inventoryOpenItemSmart = function(id){
    const it = arr(window.data?.inventoryItems).find(x => S(x.id) === S(id));
    if(!it) return say('الصنف غير موجود','err');
    const st = itemStatsV201(it);
    const rows = st.movements.slice(0,60).map(x=>{
      const t = S(x.movement_type).toLowerCase();
      const label = t==='out'?'صرف':(t==='return'?'مرتجع':(t==='consume'?'استهلاك مباشر':(t==='in'?'إدخال':'تعديل')));
      return `<tr><td>${E(x.movement_date || S(x.created_at).slice(0,10) || '-')}</td><td>${E(label)}</td><td>${Q(x.quantity)}</td><td>${E(x.project_name || (typeof financeProjectName==='function'?financeProjectName(x.project_id):'-'))}</td><td>${E(x.receiver || '-')}</td><td>${E(x.reason || x.notes || '-')}</td></tr>`;
    }).join('') || '<tr><td colspan="6">لا توجد حركات مسجلة</td></tr>';
    const img = S(it.image_url || it.product_image || it.image);
    const html = `<div style="display:flex;gap:18px;align-items:flex-start;flex-wrap:wrap">${img?`<img class="smart-img-v129" style="width:150px;height:150px;object-fit:contain" src="${E(img)}">`:''}<div style="flex:1;min-width:260px"><h2>${E(it.name||'-')}</h2><div class="smart-meta-v129"><div class="m"><small>كود المنتج</small><b>${E(itemCode(it)||'-')}</b></div><div class="m"><small>المورد</small><b>${E(it.supplier||'-')}</b></div><div class="m"><small>التصنيف</small><b>${E(it.category||'-')}</b></div><div class="m"><small>المتبقي</small><b>${Q(st.remaining)} ${E(it.unit||'')}</b></div><div class="m"><small>سعر الحبة</small><b>${M(it.unit_cost)}</b></div></div></div></div><h3>ملخص الحركة</h3><div class="smart-meta-v129"><div class="m"><small>الصادر لأوامر الصرف</small><b>${Q(st.issued)}</b></div><div class="m"><small>المرتجع</small><b>${Q(st.returned)}</b></div><div class="m"><small>صافي الصادر</small><b>${Q(st.netIssued)}</b></div><div class="m"><small>الاستهلاك المباشر</small><b>${Q(st.direct)}</b></div><div class="m"><small>إجمالي المستهلك</small><b>${Q(st.consumed)}</b></div><div class="m"><small>المتبقي</small><b>${Q(st.remaining)}</b></div></div><h3>آخر الحركات</h3><table><thead><tr><th>التاريخ</th><th>الحركة</th><th>الكمية</th><th>المشروع</th><th>المستلم</th><th>السبب</th></tr></thead><tbody>${rows}</tbody></table>`;
    if(typeof window.smartOpenV129 === 'function') window.smartOpenV129('تفاصيل المنتج', html, hasPerm('tab_catalog_print')?`<button onclick="financePrintReport&&financePrintReport('productDetail')">طباعة</button>`:'');
    else { const box = $('inventoryProductDetailBox'); if(box) box.innerHTML = html; }
  };

  // ===== الطباعة: تحديث البيانات وإزالة الكاش قبل الطباعة =====
  function clearReportCacheV201(){
    try{
      Object.keys(localStorage).forEach(k=>{ if(/report|print|deleted|cache/i.test(k)) localStorage.removeItem(k); });
    }catch(_){ }
  }
  async function reloadFinanceFreshV201(){
    try{
      if(typeof financeLoadAll === 'function') await financeLoadAll();
      else if(typeof refreshAll === 'function') await refreshAll();
      if(typeof financeRenderAll === 'function') financeRenderAll();
      if(typeof financeRenderReports === 'function') financeRenderReports();
      if(typeof inventoryRenderItems === 'function') inventoryRenderItems();
    }catch(e){ console.warn('V201 refresh warning', e); }
  }
  window.tasneefReloadFinanceFreshV201 = reloadFinanceFreshV201;
  const oldPrintReport = window.financePrintReport;
  if(typeof oldPrintReport === 'function' && !oldPrintReport.__v201Wrapped){
    window.financePrintReport = function(kind){
      if(!hasPerm(`tab_${currentFinanceTab()}_print`) && kind !== 'productDetail'){ say('لا تملك صلاحية الطباعة','err'); return; }
      clearReportCacheV201();
      try{ if(typeof financeRenderReports === 'function') financeRenderReports(); }catch(_){ }
      // طباعة جدول مرئي بعد الفلترة الحالية، مع استبعاد الصفوف المخفية.
      const bodyIds = {expensesByProject:'expenseByProjectBody',stockOutByProject:'stockOutByProjectBody',stockOutBySupervisor:'stockOutBySupervisorBody',inventoryUsageDetail:'inventoryUsageDetailBody',costCenters:'costCenterReportBody'};
      if(kind !== 'productDetail' && bodyIds[kind] && typeof window.financePrintWindow === 'function'){
        const table = $(bodyIds[kind])?.closest('table');
        if(table){
          const clone = table.cloneNode(true);
          clone.querySelectorAll('tr').forEach(tr=>{ const cs = getComputedStyle(tr); if(tr.hidden || cs.display === 'none' || cs.visibility === 'hidden') tr.remove(); });
          window.financePrintWindow('تقرير', `<table>${clone.innerHTML}</table>`);
          return;
        }
      }
      return oldPrintReport.apply(this, arguments);
    };
    window.financePrintReport.__v201Wrapped = true;
  }

  function injectCss(){
    if($('v201Css')) return;
    const st = document.createElement('style'); st.id = 'v201Css';
    st.textContent = `.v201-perm-card{margin-top:14px;border:1px solid #d7e8e1;border-radius:16px;padding:12px;background:#fbfffd}.v201-perm-card h3{margin:0 0 10px;color:#07563d}.v201-perm-actions{display:flex;gap:8px;margin-bottom:8px}.v201-perm-table-wrap{overflow:auto;border:1px solid #e2eee9;border-radius:12px}.v201-perm-table{width:100%;border-collapse:collapse;font-size:12px}.v201-perm-table th{background:#07563d;color:#fff;padding:8px;white-space:nowrap}.v201-perm-table td{border:1px solid #e2eee9;padding:7px;text-align:center}.v201-perm-table td:first-child{text-align:right;white-space:nowrap;background:#f7fbfa;color:#164638}.v201-perm-table input{transform:scale(1.1)}.v180-stats{grid-template-columns:repeat(auto-fit,minmax(105px,1fr))}`;
    document.head.appendChild(st);
  }
  function boot(){ injectCss(); ensurePermissionsMatrix(); hydratePermissionChecks(); applyTabPermissions(); applyActionPermissions(); }
  window.addEventListener('DOMContentLoaded',()=>setTimeout(boot,300));
  window.addEventListener('load',()=>setTimeout(boot,900));
  document.addEventListener('click',()=>setTimeout(()=>{ applyTabPermissions(); applyActionPermissions(); },120), true);
  setTimeout(boot,1200);
  console.log('V201 inventory/reports/permissions patch loaded');
})();


/* ===================== V202 Final Upload Patch: cache bust + visible print + permission UI ===================== */
(function(){
  'use strict';
  window.TASNEEF_BUILD = 'V202_FINAL_UPLOAD_2026_05_20';
  function $(id){return document.getElementById(id)}
  function safe(fn){try{return fn()}catch(e){console.warn('V202 warning',e)}}
  function clearPrintCache(){ safe(()=>Object.keys(localStorage).forEach(k=>{ if(/report|print|cache|deleted/i.test(k)) localStorage.removeItem(k); })); }
  const oldDeleteV202 = window.financeDelete;
  if(typeof oldDeleteV202 === 'function' && !oldDeleteV202.__v202){
    window.financeDelete = async function(){
      const r = await oldDeleteV202.apply(this, arguments);
      clearPrintCache();
      if(typeof financeLoadAll === 'function') await financeLoadAll();
      if(typeof financeRenderAll === 'function') financeRenderAll();
      return r;
    };
    window.financeDelete.__v202 = true;
  }
  const oldPrintV202 = window.financePrintReport;
  if(typeof oldPrintV202 === 'function' && !oldPrintV202.__v202){
    window.financePrintReport = function(kind){
      clearPrintCache();
      safe(()=>{ if(typeof financeRenderReports === 'function') financeRenderReports(); });
      const map={expensesByProject:'expenseByProjectBody',stockOutByProject:'stockOutByProjectBody',stockOutBySupervisor:'stockOutBySupervisorBody',inventoryUsageDetail:'inventoryUsageDetailBody',costCenters:'costCenterReportBody'};
      const bodyId=map[kind];
      if(bodyId && typeof financePrintWindow === 'function'){
        const table=$(bodyId)?.closest('table');
        if(table){
          const clone=table.cloneNode(true);
          clone.querySelectorAll('tr').forEach(tr=>{ const cs=getComputedStyle(tr); if(tr.hidden || cs.display==='none' || cs.visibility==='hidden') tr.remove(); });
          financePrintWindow('تقرير', '<table>'+clone.innerHTML+'</table>');
          return;
        }
      }
      return oldPrintV202.apply(this, arguments);
    };
    window.financePrintReport.__v202=true;
  }
  function repaint(){
    safe(()=>{ if(typeof inventoryRenderItems==='function') inventoryRenderItems(); });
    safe(()=>{ if(typeof financeRenderReports==='function') financeRenderReports(); });
  }
  window.addEventListener('load',()=>setTimeout(repaint,1800));
  console.log('Tasneef V202 Final Upload Patch loaded');
})();

/* ===================== V204 USER ROLE/PERMISSIONS HARD FIX =====================
   يعالج مشكلة: app_users_role_check عند الحفظ، خصوصًا عندما يكون الدور فارغًا
   أو عندما لا تكون قاعدة البيانات محدثة للأدوار الجديدة.
============================================================================= */
(function(){
  'use strict';
  window.TASNEEF_BUILD = 'V204_ROLE_PERMISSIONS_HARD_FIX_2026_05_20';
  const $ = (id)=>document.getElementById(id);
  const S = (v)=>String(v ?? '').trim();
  const toast = (t,kind)=>{ try{ (typeof msg==='function'?msg:alert)(t,kind); }catch(_){ alert(t); } };

  function usersArray(){
    try{
      if(Array.isArray(window.data?.users)) return window.data.users;
      if(Array.isArray(window.data?.appUsers)) return window.data.appUsers;
      if(Array.isArray(window.users)) return window.users;
    }catch(_){ }
    return [];
  }
  function findEditingUser(){
    const id = S($('userId')?.value);
    if(!id) return null;
    return usersArray().find(u=>S(u.id)===id) || null;
  }
  function normalizeRole(v, fallback){
    const x = S(v || fallback);
    const map = {
      'مدير عام':'admin', 'مدير النظام':'admin', 'ادمن':'admin', 'admin':'admin',
      'مشرف':'supervisor', 'supervisor':'supervisor',
      'فني':'technician', 'technician':'technician',
      'مدير مالي':'financial_manager', 'financial_manager':'financial_manager',
      'مدير تشغيلي':'operations_manager', 'operations_manager':'operations_manager',
      'مدير مخازن':'warehouse_manager', 'warehouse_manager':'warehouse_manager',
      'general_manager':'general_manager'
    };
    return map[x] || x || 'supervisor';
  }
  function safeDbRole(role){
    // عند عدم تشغيل SQL الجديد، أغلب قواعدك تقبل فقط admin/supervisor/technician.
    // نحفظ المديرين كـ admin حتى لا يفشل الحفظ، والصلاحيات التفصيلية تبقى في permissions.
    if(['admin','supervisor','technician'].includes(role)) return role;
    if(/manager|admin|مدير/i.test(role)) return 'admin';
    return 'supervisor';
  }
  function ensureRoleOptions(){
    const sel = $('userRole'); if(!sel) return;
    const opts = [
      ['admin','مدير عام'],
      ['financial_manager','مدير مالي'],
      ['operations_manager','مدير تشغيلي'],
      ['warehouse_manager','مدير مخازن'],
      ['technician','فني'],
      ['supervisor','مشرف']
    ];
    const existing = new Set(Array.from(sel.options).map(o=>o.value));
    opts.forEach(([v,l])=>{ if(!existing.has(v)){ const o=document.createElement('option'); o.value=v; o.textContent=l; sel.appendChild(o); } });
    if(!S(sel.value)){
      const u=findEditingUser();
      sel.value = normalizeRole(u?.role, 'supervisor');
      if(!S(sel.value)) sel.value = safeDbRole(normalizeRole(u?.role,'supervisor'));
    }
  }
  function collectPerms(){
    const perms = {};
    document.querySelectorAll('[data-perm]').forEach(el=>{ if(el.dataset?.perm) perms[el.dataset.perm] = !!el.checked; });
    return perms;
  }
  async function writeUser(row, id){
    if(!window.sb) throw new Error('Supabase غير متصل');
    return id ? await sb.from('app_users').update(row).eq('id', id) : await sb.from('app_users').insert(row);
  }

  window.saveUser = async function(){
    ensureRoleOptions();
    const id = S($('userId')?.value);
    const oldUser = findEditingUser();
    const roleRaw = S($('userRole')?.value) || S(oldUser?.role) || 'supervisor';
    const role = normalizeRole(roleRaw, 'supervisor');
    const row = {
      full_name: S($('userFullName')?.value),
      username: S($('userUsername')?.value),
      password: S($('userPassword')?.value) || S(oldUser?.password) || '123456',
      role,
      is_active: S($('userActive')?.value || 'true') === 'true',
      permissions: collectPerms()
    };
    if(!row.full_name || !row.username) return toast('الاسم واسم المستخدم مطلوبان','err');

    let attempts = [row];
    // لو قاعدة البيانات لا تقبل الأدوار الجديدة أو عمود permissions غير موجود، نجرب بدون تعطيل الحفظ.
    if(!['admin','supervisor','technician'].includes(row.role)) attempts.push({...row, role:safeDbRole(row.role)});
    attempts.push((()=>{ const r={...row, role:safeDbRole(row.role)}; delete r.permissions; return r; })());
    attempts.push((()=>{ const r={...row}; delete r.permissions; return r; })());

    let lastErr = null, usedFallback = false;
    for(const candidate of attempts){
      const res = await writeUser(candidate, id);
      if(!res.error){ usedFallback = candidate.role !== row.role || !('permissions' in candidate); lastErr=null; break; }
      lastErr = res.error;
      const msgTxt = String(res.error.message || '');
      if(!/role_check|permissions|column|constraint/i.test(msgTxt)) break;
    }
    if(lastErr) return toast(lastErr.message || String(lastErr),'err');
    toast(usedFallback ? 'تم الحفظ. ملاحظة: قاعدة البيانات تحتاج تحديث ملف SQL حتى تحفظ الأدوار الجديدة والصلاحيات كاملة.' : 'تم حفظ المستخدم والصلاحيات');
    try{ if(typeof clearUserForm==='function') clearUserForm(); }catch(_){ }
    try{ if(typeof refreshAll==='function') await refreshAll(); }catch(_){ }
  };

  const oldEdit = window.editUser;
  if(typeof oldEdit === 'function'){
    window.editUser = function(){
      const r = oldEdit.apply(this, arguments);
      setTimeout(ensureRoleOptions, 80);
      return r;
    };
  }
  const oldClear = window.clearUserForm;
  if(typeof oldClear === 'function'){
    window.clearUserForm = function(){
      const r = oldClear.apply(this, arguments);
      setTimeout(()=>{ ensureRoleOptions(); if($('userRole') && !S($('userRole').value)) $('userRole').value='supervisor'; }, 80);
      return r;
    };
  }
  window.addEventListener('DOMContentLoaded',()=>setTimeout(ensureRoleOptions,300));
  window.addEventListener('load',()=>setTimeout(ensureRoleOptions,900));
  console.log('Tasneef V204 role/permissions hard fix loaded');
})();


/* ===================== V205 SAFE FIX: RESTORE FINANCE TABS + REPORTS VISIBILITY =====================
   إصلاح محافظ: يمنع اختفاء تبويبات المصروفات/المخزون والتقارير بسبب الصلاحيات أو كاش العرض.
   لا يغير طريقة العمل ولا يحذف الدوال القديمة؛ فقط يفرض عرض التبويبات ويعيد التحكم الآمن بها.
=============================================================================================== */
(function(){
  'use strict';
  if(window.__tasneefFinanceProExclusive) return;
  window.TASNEEF_BUILD = 'V205_SAFE_TABS_REPORTS_FIX_2026_05_20';
  const $ = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const byText = {
    'الملخص':'overview',
    'المصروفات':'expenses',
    'المخزون':'inventory',
    'الأصناف':'catalog',
    'حركة المخزون':'movements',
    'طلبات الصرف':'requests',
    'الموردين':'suppliers',
    'التقارير':'reports',
    'مراكز التكلفة':'costCenters',
    'تقليل التكلفة':'costReduction'
  };
  const label = {
    overview:'الملخص', expenses:'المصروفات', inventory:'المخزون', catalog:'الأصناف',
    movements:'حركة المخزون', requests:'طلبات الصرف', suppliers:'الموردين', reports:'التقارير',
    costCenters:'مراكز التكلفة', costReduction:'تقليل التكلفة'
  };
  const order = ['overview','expenses','inventory','catalog','movements','requests','suppliers','reports','costCenters','costReduction'];
  function sessionUser(){
    try{ if(typeof session==='function') return session() || {}; }catch(_){ }
    try{ return JSON.parse(localStorage.getItem('tasneef_session')||'{}') || {}; }catch(_){ return {}; }
  }
  function isAdminLike(){
    const r=S(sessionUser().role);
    return !r || ['admin','general_manager','financial_manager','operations_manager','warehouse_manager'].includes(r);
  }
  function perms(){
    const u=sessionUser();
    const p=u.permissions || u.extra_permissions || {};
    if(typeof p === 'string'){ try{return JSON.parse(p)||{};}catch(_){return {}; } }
    return p || {};
  }
  function hasExplicitTabPerms(){ return Object.keys(perms()).some(k=>/^tab_/.test(k)); }
  function canViewTab(key){
    if(isAdminLike()) return true;
    const p=perms();
    if(!hasExplicitTabPerms()) return true; // مهم: إذا لم تُحفظ الصلاحيات التفصيلية لا نخفي التبويبات بالغلط
    return p['tab_'+key+'_view'] !== false && p['tab_'+key+'_view'] !== undefined;
  }
  function keyFromBtn(btn){
    const oc=S(btn?.getAttribute?.('onclick'));
    const m=oc.match(/financeShowTab\(['"]([^'"]+)['"]/);
    if(m) return m[1] === 'items' ? 'catalog' : m[1];
    return byText[S(btn?.textContent).replace(/\s+/g,' ').trim()] || '';
  }
  function pageId(key){ return 'financeTab'+key.charAt(0).toUpperCase()+key.slice(1); }
  function ensureCss(){
    if($('v205SafeTabsCss')) return;
    const st=document.createElement('style'); st.id='v205SafeTabsCss';
    st.textContent=`
      #financeDashboard .finance-tabs{display:flex!important;gap:8px!important;flex-wrap:wrap!important;visibility:visible!important;min-height:42px!important;}
      #financeDashboard .finance-tabs button,#financeDashboard .finance-tabs .finance-tab{visibility:visible!important;opacity:1!important;}
      #financeDashboard .finance-tabs button.v205-hidden-perm{display:none!important;}
      #financeDashboard .finance-tab-page.hidden{display:none!important;}
      #financeDashboard .finance-tab-page:not(.hidden){display:block!important;visibility:visible!important;}
    `;
    document.head.appendChild(st);
  }
  function ensureTabs(){
    ensureCss();
    const dash=$('financeDashboard'); if(!dash) return;
    let wrap=dash.querySelector('.finance-tabs');
    if(!wrap){
      wrap=document.createElement('div'); wrap.className='finance-tabs actions';
      const firstCard=dash.querySelector('.card, .wide-card');
      dash.insertBefore(wrap, firstCard || dash.firstChild);
    }
    order.forEach(key=>{
      const page=$(pageId(key));
      if(!page && !['suppliers','catalog','costReduction'].includes(key)) return;
      let btn=[...wrap.querySelectorAll('button,.finance-tab')].find(b=>keyFromBtn(b)===key || S(b.dataset?.tab)===key);
      if(!btn){
        btn=document.createElement('button');
        btn.type='button'; btn.className='light finance-tab'; btn.dataset.tab=key; btn.textContent=label[key]||key;
        btn.onclick=function(){ window.financeShowTab(key,this); };
        wrap.appendChild(btn);
      }
      btn.classList.add('finance-tab'); btn.hidden=false;
      if(canViewTab(key)){ btn.classList.remove('v205-hidden-perm'); btn.style.display='inline-flex'; }
      else { btn.classList.add('v205-hidden-perm'); }
    });
    // تحديث عنوان التقارير حتى نعرف أن النسخة الجديدة وصلت
    [...dash.querySelectorAll('h2')].forEach(h=>{ if(/V211 Advanced Inventory Reports/i.test(h.textContent||'')) h.textContent='V211 Advanced Inventory Reports'; });
  }
  function showTabPage(key, btn){
    ensureTabs();
    if(!canViewTab(key)){
      try{ if(typeof msg==='function') msg('لا تملك صلاحية مشاهدة هذا التبويب','err'); else alert('لا تملك صلاحية مشاهدة هذا التبويب'); }catch(_){ }
      return;
    }
    const dash=$('financeDashboard'); if(!dash) return;
    dash.querySelectorAll('.finance-tab-page').forEach(p=>{ p.classList.add('hidden'); p.style.display='none'; });
    const page=$(pageId(key));
    if(page){ page.classList.remove('hidden'); page.style.display='block'; }
    dash.querySelectorAll('.finance-tabs button,.finance-tabs .finance-tab').forEach(b=>b.classList.remove('active'));
    if(btn) btn.classList.add('active');
    else {
      const b=[...dash.querySelectorAll('.finance-tabs button,.finance-tabs .finance-tab')].find(x=>keyFromBtn(x)===key || S(x.dataset?.tab)===key);
      b?.classList.add('active');
    }
    window.financeCurrentTab=key;
    try{ if(!window.financeLoaded && typeof financeLoadAll==='function') financeLoadAll(); }catch(_){ }
    setTimeout(()=>{
      try{
        if(key==='reports' && typeof window.financeRenderReports==='function') window.financeRenderReports();
        if(key==='inventory' && typeof window.inventoryRenderItems==='function') window.inventoryRenderItems();
        if(key==='movements' && typeof window.inventoryRenderMovements==='function') window.inventoryRenderMovements();
        if(key==='requests' && typeof window.inventoryRenderRequests==='function') window.inventoryRenderRequests();
        if(key==='overview' && typeof window.financeRenderAll==='function') window.financeRenderAll();
      }catch(e){ console.warn('V205 tab render warning',e); }
      ensureTabs();
    },80);
  }
  window.financeShowTab=function(tab,btn){ showTabPage(tab==='items'?'catalog':tab,btn); };
  window.inventoryClearReportDateFilter=function(){
    ['financeReportQuickSearch','financeReportCostCodeFilter','inventoryReportDay','inventoryReportMonth'].forEach(id=>{ const el=$(id); if(el) el.value=''; });
    const mode=$('inventoryReportDateMode'); if(mode) mode.value='all';
    try{ if(typeof window.financeRenderReports==='function') window.financeRenderReports(); }catch(_){ }
  };
  function init(){
    ensureTabs();
    const active=document.querySelector('#financeDashboard .finance-tabs .active');
    const activeKey=keyFromBtn(active) || S(window.financeCurrentTab) || 'overview';
    if($('financeDashboard') && !document.querySelector('#financeDashboard .finance-tab-page:not(.hidden)')) showTabPage(activeKey || 'overview', active);
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(init, ev==='load'?900:200)));
  setInterval(()=>{ if($('financeDashboard')) ensureTabs(); }, 2500);
  console.log('Tasneef V205 safe tabs/reports fix loaded');
})();

/* ===================== V206: REMOVE TOTAL CONSUMED + TICKETS PDF EXPORT =====================
   المطلوب:
   1) حذف خانة "إجمالي المستهلك" من كرت/تفاصيل المنتج.
   2) إضافة تنزيل/تصدير PDF للتكتات من الإدارة والمشرف.
============================================================================================ */
(function(){
  'use strict';
  window.TASNEEF_BUILD = 'V206_REMOVE_TOTAL_CONSUMED_TICKETS_PDF_2026_05_20';
  const $ = id => document.getElementById(id);
  const arr = v => Array.isArray(v) ? v : [];
  const S = v => String(v ?? '').trim();
  const N = v => { const x = parseFloat(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const Q = v => N(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const M = v => N(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}) + ' ر.س';
  const fmt = v => { try{ return v ? new Date(v).toLocaleString('ar-SA') : '-'; }catch(_){ return S(v)||'-'; } };
  const pName = id => { try{ return typeof projectName==='function' ? projectName(id) : '-'; }catch(_){ return '-'; } };
  const sName = id => { try{ return typeof supervisorName==='function' ? supervisorName(id) : '-'; }catch(_){ return '-'; } };
  const say = (t,type) => { try{ if(typeof msg==='function') msg(t,type); else alert(t); }catch(_){ alert(t); } };
  const no = t => t?.ticket_number || ('T-' + String(t?.id || 0).padStart(4,'0'));
  const stLabel = st => st==='closed'?'مغلق':(st==='processing'?'تحت المعالجة':'مفتوح');
  const prLabel = p => p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي'));
  const stClass = st => st==='closed'?'done':(st==='processing'?'doing':'open');
  const prClass = p => p==='urgent'?'urgent':(p==='high'?'high':(p==='low'?'low':'normal'));
  const dur = t => {
    try{
      if(typeof durationLabel==='function' && typeof minutesBetween==='function'){
        const m = (t.status==='closed' && t.open_duration_minutes != null) ? Number(t.open_duration_minutes||0) : minutesBetween(t.created_at, new Date().toISOString());
        return durationLabel(m);
      }
      const m = Math.max(0, Math.round((new Date(t.status==='closed'?(t.closed_at||t.updated_at||Date.now()):Date.now()) - new Date(t.created_at||Date.now()))/60000));
      return Math.floor(m/60)+'س '+(m%60)+'د';
    }catch(_){ return '-'; }
  };

  function itemCode(it){ return S(it?.product_code || it?.serial_number || it?.barcode || it?.supplier_barcode || it?.company_code || it?.id); }
  function matchItem(it,row){
    const id=S(it?.id), code=itemCode(it), name=S(it?.name);
    return S(row?.item_id)===id || S(row?.inventory_item_id)===id || S(row?.item_name)===name || (!!code && [row?.product_code,row?.barcode,row?.supplier_barcode,row?.company_code].map(S).includes(code));
  }
  function lineItems(r){
    let lines = r?.request_items || r?.items || r?.request_lines || r?.lines || [];
    if(typeof lines==='string'){ try{ lines=JSON.parse(lines); }catch(_){ lines=[]; } }
    if(Array.isArray(lines) && lines.length) return lines;
    if(r?.item_id || r?.item_name) return [{item_id:r.item_id,item_name:r.item_name,quantity:r.quantity,product_code:r.product_code,unit_cost:r.unit_cost}];
    return [];
  }
  function requestIsIssued(r){ return /approved|issued|صرف|معتمد|done|completed/i.test(S(r?.status)); }
  function stats(it){
    const moves = arr(window.data?.inventoryMovements).filter(m=>matchItem(it,m));
    let enteredMov=0, enteredBatch=0, issuedReq=0, issuedMov=0, returned=0, direct=0;
    moves.forEach(m=>{
      const t=S(m.movement_type).toLowerCase(); const q=Math.abs(N(m.quantity));
      if(['in','إدخال','add','invoice_add','purchase'].includes(t)) enteredMov+=q;
      else if(['out','صرف','issue'].includes(t)) issuedMov+=q;
      else if(['return','مرتجع','رجوع'].includes(t)) returned+=q;
      else if(['consume','consumption','استهلاك','استهلاك مباشر'].includes(t)) direct+=q;
    });
    arr(window.stockBatchesV148).forEach(b=>arr(b.lines).forEach(l=>{ if(matchItem(it,l)) enteredBatch+=Math.abs(N(l.quantity)); }));
    arr(window.data?.inventoryRequests).filter(requestIsIssued).forEach(r=> lineItems(r).forEach(l=>{ if(matchItem(it,l)) issuedReq+=Math.abs(N(l.quantity)); }));
    const issued = issuedReq > 0 ? issuedReq : issuedMov;
    const netIssued = Math.max(0, issued - returned);
    const consumed = netIssued + direct;
    const entered = enteredMov > 0 ? enteredMov : enteredBatch;
    const remaining = Number.isFinite(N(it?.quantity)) ? N(it.quantity) : Math.max(0, entered-consumed);
    return {entered, issued, returned, netIssued, direct, consumed, remaining, moves};
  }

  // حذف إجمالي المستهلك من كروت الأصناف؛ نبقي صافي الصادر والاستهلاك المباشر فقط حتى لا يتكرر المعنى.
  function renderCatalogV206(){
    const body=$('inventoryItemsBody'); if(!body) return;
    const search=S($('financeSearch')?.value || $('inventorySearch')?.value).toLowerCase();
    let rows=arr(window.data?.inventoryItems).slice();
    if(search) rows=rows.filter(it=>[it.name,itemCode(it),it.supplier,it.category,it.unit,it.notes].map(S).join(' ').toLowerCase().includes(search));
    if(!rows.length){ body.innerHTML='<tr><td colspan="20">لا توجد أصناف</td></tr>'; return; }
    const canEdit = () => true, canDel = () => true;
    body.innerHTML = `<tr class="v180-catalog-row"><td colspan="20"><div class="v180-catalog-grid">${rows.map(it=>{
      const st=stats(it), low = st.remaining <= N(it.min_quantity || it.reorder_level || 0);
      const imgSrc=S(it.image_url || it.product_image || it.image);
      const img=imgSrc?`<img src="${E(imgSrc)}" alt="${E(it.name)}">`:'<span>لا توجد</span>';
      const priceBlock = `<div><small>سعر الوحدة</small><b>${M(it.unit_cost)}</b></div>`;
      return `<article class="v180-catalog-card"><div class="v180-item-head"><div class="v180-img">${img}</div><div class="v180-title"><h3>${E(it.name||'-')}</h3><small>كود المنتج: ${E(itemCode(it)||'-')}</small><br><small>المورد: ${E(it.supplier||'-')}</small></div><span class="${low?'v180-low':'v180-ok'}">${low?'منخفض':'متوفر'}</span></div><div class="v180-stats"><div><small>الكمية</small><b>${Q(st.remaining)} ${E(it.unit||'')}</b></div><div><small>دخل</small><b>${Q(st.entered)}</b></div><div><small>خرج</small><b>${Q(st.issued)}</b></div><div><small>مرتجع</small><b>${Q(st.returned)}</b></div><div><small>صافي الصادر</small><b>${Q(st.netIssued)}</b></div><div><small>استهلاك مباشر</small><b>${Q(st.direct)}</b></div><div><small>حد الطلب</small><b>${Q(it.min_quantity||it.reorder_level||0)}</b></div>${priceBlock}</div><div class="row-actions v180-actions"><button type="button" onclick="inventoryOpenItemSmart&&inventoryOpenItemSmart('${E(it.id)}')">عرض المنتج</button>${canEdit()?`<button type="button" class="light" onclick="inventoryEditItem&&inventoryEditItem('${E(it.id)}')">تعديل</button>`:''}${canDel()?`<button type="button" class="danger" onclick="financeDelete&&financeDelete('inventory_items','${E(it.id)}')">حذف</button>`:''}</div></article>`;
    }).join('')}</div></td></tr>`;
  }

  window.inventoryOpenItemSmart = function(id){
    const it=arr(window.data?.inventoryItems).find(x=>S(x.id)===S(id));
    if(!it) return say('الصنف غير موجود','err');
    const st=stats(it);
    const rows=st.moves.slice(0,60).map(x=>{
      const t=S(x.movement_type).toLowerCase();
      const label = t==='out'?'صرف':(t==='return'?'مرتجع':(t==='consume'||t==='consumption'?'استهلاك مباشر':(t==='in'?'إدخال':'تعديل')));
      return `<tr><td>${E(x.movement_date || S(x.created_at).slice(0,10) || '-')}</td><td>${E(label)}</td><td>${Q(x.quantity)}</td><td>${E(x.project_name || pName(x.project_id))}</td><td>${E(x.receiver || x.receiver_name || '-')}</td><td>${E(x.reason || x.notes || '-')}</td></tr>`;
    }).join('') || '<tr><td colspan="6">لا توجد حركات مسجلة</td></tr>';
    const img=S(it.image_url || it.product_image || it.image);
    const html=`<div style="display:flex;gap:18px;align-items:flex-start;flex-wrap:wrap">${img?`<img class="smart-img-v129" style="width:150px;height:150px;object-fit:contain" src="${E(img)}">`:''}<div style="flex:1;min-width:260px"><h2>${E(it.name||'-')}</h2><div class="smart-meta-v129"><div class="m"><small>كود المنتج</small><b>${E(itemCode(it)||'-')}</b></div><div class="m"><small>المورد</small><b>${E(it.supplier||'-')}</b></div><div class="m"><small>التصنيف</small><b>${E(it.category||'-')}</b></div><div class="m"><small>المتبقي</small><b>${Q(st.remaining)} ${E(it.unit||'')}</b></div><div class="m"><small>سعر الحبة</small><b>${M(it.unit_cost)}</b></div></div></div></div><h3>ملخص الحركة</h3><div class="smart-meta-v129"><div class="m"><small>الصادر لأوامر الصرف</small><b>${Q(st.issued)}</b></div><div class="m"><small>المرتجع</small><b>${Q(st.returned)}</b></div><div class="m"><small>صافي الصادر</small><b>${Q(st.netIssued)}</b></div><div class="m"><small>الاستهلاك المباشر</small><b>${Q(st.direct)}</b></div><div class="m"><small>المتبقي</small><b>${Q(st.remaining)}</b></div></div><h3>آخر الحركات</h3><table><thead><tr><th>التاريخ</th><th>الحركة</th><th>الكمية</th><th>المشروع</th><th>المستلم</th><th>السبب</th></tr></thead><tbody>${rows}</tbody></table>`;
    if(typeof window.smartOpenV129==='function') window.smartOpenV129('تفاصيل المنتج', html, `<button onclick="window.print()">طباعة</button>`);
    else { const box=$('inventoryProductDetailBox'); if(box) box.innerHTML=html; }
  };
  window.inventoryRenderItems = renderCatalogV206;

  function ticketHtml(t){
    return `<div class="ticket-pdf-card"><h2>تقرير تكت</h2><table><tr><th>رقم التكت</th><td>${E(no(t))}</td><th>الحالة</th><td>${E(stLabel(t.status))}</td></tr><tr><th>المشروع</th><td>${E(pName(t.project_id))}</td><th>المشرف</th><td>${E(sName(t.supervisor_id))}</td></tr><tr><th>الأولوية</th><td>${E(prLabel(t.priority))}</td><th>مدة الفتح</th><td>${E(dur(t))}</td></tr><tr><th>تاريخ الإنشاء</th><td>${E(fmt(t.created_at))}</td><th>تاريخ الإغلاق</th><td>${E(t.closed_at?fmt(t.closed_at):'-')}</td></tr><tr><th>استلم التكت</th><td>${E(t.claimed_by_name||'-')}</td><th>أغلق التكت</th><td>${E(t.closed_by_name||'-')}</td></tr><tr><th>العنوان</th><td colspan="3">${E(t.title||'-')}</td></tr><tr><th>الوصف</th><td colspan="3">${E(t.description||'-')}</td></tr><tr><th>طريقة الإغلاق</th><td colspan="3">${E(t.closure_note||'-')}</td></tr></table></div>`;
  }
  function openPdfWindow(title, body){
    const w=window.open('', '_blank');
    if(!w) return say('المتصفح منع فتح نافذة PDF. اسمح بالنوافذ المنبثقة ثم حاول مرة أخرى.','err');
    w.document.open();
    w.document.write(`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${E(title)}</title><style>@page{size:A4;margin:12mm}body{font-family:Arial,Tahoma,sans-serif;color:#143c2b;direction:rtl}h1,h2{margin:0 0 12px;color:#07563d}.top{display:flex;justify-content:space-between;align-items:center;border-bottom:3px solid #07563d;padding-bottom:10px;margin-bottom:18px}.brand{font-weight:700}.hint{font-size:12px;color:#60726a;margin-top:6px}table{width:100%;border-collapse:collapse;margin-top:10px;font-size:13px}th,td{border:1px solid #cfded7;padding:8px;text-align:right;vertical-align:top}th{background:#eef7f3;color:#07563d}.ticket-pdf-card{page-break-inside:avoid;margin-bottom:18px}.actions{position:fixed;top:10px;left:10px}@media print{.actions{display:none}.hint{display:none}}</style></head><body><div class="actions"><button onclick="window.print()">حفظ / طباعة PDF</button></div><div class="top"><div><h1>${E(title)}</h1><div class="hint">اضغط زر حفظ / طباعة PDF ثم اختر Save as PDF من نافذة الطباعة</div></div><div class="brand">شركة تصنيف لإدارة المرافق</div></div>${body}<script>setTimeout(()=>window.print(),500)<\/script></body></html>`);
    w.document.close();
  }
  function filteredTickets(){
    let list=arr(window.data?.tickets).slice();
    const admin=$('ticketsBody'), sup=$('supTicketsBody');
    if(admin){
      const st=$('ticketFilterStatus')?.value||'', q=S($('ticketSearch')?.value).toLowerCase();
      if(st) list=list.filter(t=>S(t.status||'open')===st);
      if(q) list=list.filter(t=>[no(t),t.title,t.description,pName(t.project_id),sName(t.supervisor_id),stLabel(t.status),prLabel(t.priority),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));
    }else if(sup){
      const st=$('supTicketFilterStatus')?.value||'', pid=$('supTicketFilterProject')?.value||'', q=S($('supTicketSearch')?.value).toLowerCase();
      if(pid) list=list.filter(t=>S(t.project_id)===pid);
      if(st) list=list.filter(t=>S(t.status||'open')===st);
      if(q) list=list.filter(t=>[no(t),t.title,t.description,pName(t.project_id),stLabel(t.status),prLabel(t.priority),t.claimed_by_name,t.closed_by_name,t.closure_note].join(' ').toLowerCase().includes(q));
    }
    return list;
  }
  window.ticketDownloadPdfV206 = function(id){
    const t=arr(window.data?.tickets).find(x=>S(x.id)===S(id));
    if(!t) return say('التكت غير موجود','err');
    openPdfWindow('تقرير التكت ' + no(t), ticketHtml(t));
  };
  window.ticketsDownloadPdfV206 = function(){
    const list=filteredTickets();
    if(!list.length) return say('لا توجد تكتات للتصدير','err');
    openPdfWindow('تقرير التكتات', list.map(ticketHtml).join(''));
  };
  function addPdfButtons(){
    [['ticketPdfBtnV206','ticketsBody'],['supTicketPdfBtnV206','supTicketsBody']].forEach(([btnId,bodyId])=>{
      const body=$(bodyId); if(!body || $(btnId)) return;
      const host = body.closest('.card')?.querySelector('.filters') || body.closest('.card')?.querySelector('h2') || body.parentElement;
      const btn=document.createElement('button'); btn.id=btnId; btn.type='button'; btn.className='light ticket-pdf-v206'; btn.textContent='تصدير PDF'; btn.onclick=window.ticketsDownloadPdfV206;
      if(host?.classList?.contains('filters')) host.appendChild(btn); else host?.insertAdjacentElement('afterend', btn);
    });
  }
  function summaryHtml(list){
    const total=list.length, open=list.filter(t=>(t.status||'open')==='open').length, proc=list.filter(t=>t.status==='processing').length, closed=list.filter(t=>t.status==='closed').length, urgent=list.filter(t=>['urgent','high'].includes(t.priority)).length;
    return `<div class="smart-ticket-kpi"><b>${total}</b><span>إجمالي التكتات</span></div><div class="smart-ticket-kpi red"><b>${open}</b><span>مفتوحة</span></div><div class="smart-ticket-kpi amber"><b>${proc}</b><span>تحت المعالجة</span></div><div class="smart-ticket-kpi green"><b>${closed}</b><span>مغلقة</span></div><div class="smart-ticket-kpi dark"><b>${urgent}</b><span>عاجلة / مهمة</span></div>`;
  }
  function card(t,mode){
    const canDelete=mode==='admin';
    const waFn = typeof window.sendTicketWhatsAppV43==='function' ? 'sendTicketWhatsAppV43' : (typeof window.sendTicketWhatsApp==='function' ? 'sendTicketWhatsApp' : '');
    const actions = `<button type="button" onclick="viewTicketSmartV147&&viewTicketSmartV147(${Number(t.id)||0})">عرض</button><button type="button" class="light" onclick="ticketDownloadPdfV206(${Number(t.id)||0})">PDF</button><button type="button" class="light" onclick="editTicket(${Number(t.id)||0})">تعديل</button>${t.status==='closed'?`<button type="button" class="light" onclick="setTicketStatus(${Number(t.id)||0},'open')">إعادة فتح</button>`:`${t.status!=='processing'?`<button type="button" class="light" onclick="claimTicket(${Number(t.id)||0})">استلام</button>`:''}<button type="button" onclick="closeTicket(${Number(t.id)||0})">إغلاق</button>`}${waFn?`<button type="button" class="ticket-wa-v147" onclick="${waFn}(${Number(t.id)||0})">واتساب</button>`:''}${canDelete?`<button type="button" class="danger" onclick="deleteRow('tickets',${Number(t.id)||0})">حذف</button>`:''}`;
    return `<article class="smart-ticket-card ${stClass(t.status)} ${prClass(t.priority)}"><div class="smart-ticket-top"><div><strong>${E(no(t))}</strong><small>${E(fmt(t.created_at))}</small></div><span class="smart-ticket-status ${stClass(t.status)}">${E(stLabel(t.status))}</span></div><h3>${E(t.title||'-')}</h3><div class="smart-ticket-meta"><span>المشروع: <b>${E(pName(t.project_id))}</b></span><span>المشرف: <b>${E(sName(t.supervisor_id))}</b></span><span>الأولوية: <b>${E(prLabel(t.priority))}</b></span><span>مدة الفتح: <b>${E(dur(t))}</b></span></div><p>${E(t.description||'لا يوجد وصف')}</p><div class="smart-ticket-mini"><span>استلم: ${E(t.claimed_by_name||'-')}</span><span>أغلق: ${E(t.closed_by_name||'-')}</span></div>${t.closure_note?`<div class="smart-ticket-note">الحل: ${E(t.closure_note)}</div>`:''}<div class="smart-ticket-actions">${actions}</div></article>`;
  }
  function renderTicketsV206(){
    addPdfButtons();
    if($('ticketsBody')){
      const list=filteredTickets(); const sum=$('ticketsSmartSummary'); if(sum) sum.innerHTML=summaryHtml(list);
      $('ticketsBody').classList.add('smart-ticket-grid'); $('ticketsBody').innerHTML=list.map(t=>card(t,'admin')).join('') || '<div class="empty-smart-ticket">لا توجد تكتات مطابقة للبحث الحالي</div>';
    }
    if($('supTicketsBody')){
      const list=filteredTickets(); const sum=$('supTicketsSmartSummary'); if(sum) sum.innerHTML=summaryHtml(list);
      $('supTicketsBody').classList.add('smart-ticket-grid'); $('supTicketsBody').innerHTML=list.map(t=>card(t,'supervisor')).join('') || '<div class="empty-smart-ticket">لا توجد تكتات مطابقة للبحث الحالي</div>';
    }
  }
  window.renderTickets = renderTicketsV206;
  const oldView = window.viewTicketSmartV147;
  window.viewTicketSmartV147 = function(id){
    if(typeof oldView==='function') oldView(id);
    setTimeout(()=>{
      const box=$('ticketSmartModalV147'); if(!box) return;
      const actions=box.querySelector('.smart-modal-actions');
      if(actions && !actions.querySelector('.ticket-single-pdf-v206')){
        const b=document.createElement('button'); b.type='button'; b.className='light ticket-single-pdf-v206'; b.textContent='تنزيل PDF'; b.onclick=()=>window.ticketDownloadPdfV206(id);
        actions.prepend(b);
      }
    },80);
  };
  function css(){
    if($('v206Css')) return;
    const st=document.createElement('style'); st.id='v206Css'; st.textContent='.ticket-pdf-v206{background:#eef7f3!important;color:#07563d!important;border:1px solid #cfe3da!important}.v180-stats{grid-template-columns:repeat(auto-fit,minmax(105px,1fr))!important}'; document.head.appendChild(st);
  }
  function boot(){ css(); addPdfButtons(); try{ if(typeof window.inventoryRenderItems==='function') window.inventoryRenderItems(); }catch(_){ } try{ renderTicketsV206(); }catch(_){ } }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(boot, ev==='load'?900:300)));
  setTimeout(boot,1200);
  console.log('Tasneef V206 remove total consumed + ticket PDF export loaded');
})();

/* ===================== V207: DIRECT TICKETS PDF DOWNLOAD GROUPED BY SUPERVISOR =====================
   - تنزيل PDF مباشر في مجلد التنزيلات باستخدام html2pdf.
   - التقرير حسب الفلاتر الحالية.
   - ترتيب التكتات تحت اسم كل مشرف.
   - إظهار من رفع التكت حتى لو كان إداريًا.
=============================================================================================== */
(function(){
  'use strict';
  window.TASNEEF_BUILD = 'V207_TICKETS_DIRECT_PDF_GROUPED_BY_SUPERVISOR_2026_05_20';
  const $ = id => document.getElementById(id);
  const arr = v => Array.isArray(v) ? v : [];
  const S = v => String(v ?? '').trim();
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const say = (t,type) => { try{ if(typeof msg==='function') msg(t,type); else alert(t); }catch(_){ alert(t); } };
  const ticketNo = t => t?.ticket_number || ('T-' + String(t?.id || 0).padStart(4,'0'));
  const stLabel = st => st==='closed'?'مغلق':(st==='processing'?'تحت المعالجة':'مفتوح');
  const prLabel = p => p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي'));
  const safeDate = v => { try{ return v ? new Date(v).toLocaleString('ar-SA') : '-'; }catch(_){ return S(v)||'-'; } };
  const dayOnly = v => { try{ return v ? new Date(v).toLocaleDateString('ar-SA') : '-'; }catch(_){ return S(v)||'-'; } };
  const projectLabel = id => { try{ return typeof projectName==='function' ? projectName(id) : '-'; }catch(_){ return '-'; } };
  const supervisorLabel = id => { try{ return typeof supervisorName==='function' ? supervisorName(id) : '-'; }catch(_){ return '-'; } };
  function userLabel(id){
    if(!S(id)) return '-';
    const u = arr(window.data?.users).find(x => S(x.id)===S(id) || S(x.user_id)===S(id) || S(x.username)===S(id));
    if(u) return S(u.full_name || u.name || u.username || id);
    return S(id);
  }
  function raisedBy(t){
    return S(t.created_by_name || t.created_by_full_name || t.raised_by_name || t.created_name || t.created_user_name) || userLabel(t.created_by || t.raised_by || t.user_id || t.created_user_id);
  }
  function durationText(t){
    try{
      if(typeof durationLabel==='function' && typeof minutesBetween==='function'){
        const end = t.status==='closed' ? (t.closed_at || t.updated_at || new Date().toISOString()) : new Date().toISOString();
        const m = t.open_duration_minutes != null && t.status==='closed' ? Number(t.open_duration_minutes||0) : minutesBetween(t.created_at, end);
        return durationLabel(m);
      }
    }catch(_){ }
    return '-';
  }
  function currentTicketFilters(){
    const adminMode = !!$('ticketsBody');
    if(adminMode){
      return {
        mode:'admin',
        status:$('ticketFilterStatus')?.value||'',
        search:S($('ticketSearch')?.value).toLowerCase(),
        project:'',
        supervisor:''
      };
    }
    return {
      mode:'supervisor',
      status:$('supTicketFilterStatus')?.value||'',
      search:S($('supTicketSearch')?.value).toLowerCase(),
      project:$('supTicketFilterProject')?.value||'',
      supervisor:''
    };
  }
  function ticketsForCurrentFilterV207(){
    const f=currentTicketFilters();
    let list=arr(window.data?.tickets).slice();
    if(f.project) list=list.filter(t=>S(t.project_id)===S(f.project));
    if(f.supervisor) list=list.filter(t=>S(t.supervisor_id)===S(f.supervisor));
    if(f.status) list=list.filter(t=>S(t.status || 'open')===S(f.status));
    if(f.search){
      list=list.filter(t=>[
        ticketNo(t), t.title, t.description, projectLabel(t.project_id), supervisorLabel(t.supervisor_id),
        stLabel(t.status), prLabel(t.priority), t.claimed_by_name, t.closed_by_name, t.closure_note, raisedBy(t)
      ].join(' ').toLowerCase().includes(f.search));
    }
    return list.sort((a,b)=>{
      const sa=supervisorLabel(a.supervisor_id), sb=supervisorLabel(b.supervisor_id);
      if(sa!==sb) return sa.localeCompare(sb,'ar');
      return new Date(b.created_at||0)-new Date(a.created_at||0);
    });
  }
  function filterSummaryTextV207(){
    const f=currentTicketFilters();
    const bits=[];
    bits.push('الحالة: '+(f.status?stLabel(f.status):'كل الحالات'));
    if(f.project) bits.push('المشروع: '+projectLabel(f.project));
    if(f.search) bits.push('البحث: '+S(f.search));
    return bits.join(' | ');
  }
  function groupBySupervisor(list){
    const m=new Map();
    list.forEach(t=>{
      const key=S(t.supervisor_id)||'without_supervisor';
      const name=S(supervisorLabel(t.supervisor_id)) || 'بدون مشرف';
      if(!m.has(key)) m.set(key,{name,rows:[]});
      m.get(key).rows.push(t);
    });
    return Array.from(m.values()).sort((a,b)=>a.name.localeCompare(b.name,'ar'));
  }
  function buildTicketsPdfNodeV207(list, title){
    const groups=groupBySupervisor(list);
    const root=document.createElement('div');
    root.dir='rtl';
    root.className='tasneef-ticket-pdf-v207';
    const total=list.length;
    const open=list.filter(t=>S(t.status||'open')==='open').length;
    const processing=list.filter(t=>S(t.status)==='processing').length;
    const closed=list.filter(t=>S(t.status)==='closed').length;
    root.innerHTML = `
      <style>
        .tasneef-ticket-pdf-v207{font-family:Tahoma,Arial,sans-serif;color:#123b2f;background:#fff;width:1120px;padding:28px;box-sizing:border-box;direction:rtl}
        .tasneef-ticket-pdf-v207 *{box-sizing:border-box}
        .t-header{display:flex;align-items:center;justify-content:space-between;border-bottom:4px solid #0b5b49;padding-bottom:14px;margin-bottom:16px}
        .t-brand{display:flex;align-items:center;gap:12px}.t-logo{width:74px;height:74px;border:2px solid #d7e8e1;border-radius:18px;object-fit:contain;padding:8px;background:#fff}.t-title h1{font-size:30px;margin:0 0 6px;color:#0b5b49}.t-title p{margin:0;color:#5b7168;font-size:14px}.t-company{font-weight:900;color:#0b5b49;font-size:18px;text-align:left}.t-meta{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:12px 0 18px}.t-box{border:1px solid #d6e7e0;background:#f3faf7;border-radius:14px;padding:10px}.t-box small{display:block;color:#6a7a73;margin-bottom:5px}.t-box b{font-size:18px;color:#082f27}.super-section{page-break-inside:avoid;margin:18px 0 24px;border:1px solid #cadbd4;border-radius:15px;overflow:hidden}.super-head{background:#2779b7;color:#fff;padding:10px 13px;font-weight:900;font-size:17px;display:flex;justify-content:space-between}.ticket-table{width:100%;border-collapse:collapse;font-size:12px}.ticket-table th{background:#e9f1f5;color:#123b2f;border:1px solid #c8d6dc;padding:8px;text-align:right}.ticket-table td{border:1px solid #d7e0e4;padding:8px;vertical-align:top}.ticket-table tr:nth-child(even) td{background:#f6f9fb}.status{font-weight:800;border-radius:999px;padding:3px 8px;display:inline-block}.status.open{background:#ffe7e7;color:#9d2020}.status.processing{background:#fff3d2;color:#8a5c00}.status.closed{background:#e4f5ea;color:#0b7338}.desc{max-width:260px;white-space:pre-wrap;line-height:1.55}.footer{margin-top:18px;padding-top:10px;border-top:1px solid #d5e3dd;color:#71837b;text-align:center;font-size:12px}.empty{border:1px dashed #bdd4ca;border-radius:14px;padding:25px;text-align:center;color:#677b72}.page-break{page-break-after:always}@media print{.tasneef-ticket-pdf-v207{width:auto}.super-section{page-break-inside:avoid}}
      </style>
      <div class="t-header"><div class="t-brand"><img class="t-logo" src="tasneef_logo_print.png" onerror="this.style.display='none'"><div class="t-title"><h1>${E(title)}</h1><p>${E(filterSummaryTextV207())}</p><p>تاريخ التصدير: ${E(new Date().toLocaleString('ar-SA'))}</p></div></div><div class="t-company">شركة تصنيف لإدارة المرافق</div></div>
      <div class="t-meta"><div class="t-box"><small>إجمالي التكتات</small><b>${total}</b></div><div class="t-box"><small>مفتوح</small><b>${open}</b></div><div class="t-box"><small>تحت المعالجة</small><b>${processing}</b></div><div class="t-box"><small>مغلق</small><b>${closed}</b></div></div>
      ${groups.length?'':'<div class="empty">لا توجد تكتات مطابقة للفلاتر الحالية</div>'}
      ${groups.map(g=>`<section class="super-section"><div class="super-head"><span>المشرف: ${E(g.name || 'بدون مشرف')}</span><span>عدد التكتات: ${g.rows.length}</span></div><table class="ticket-table"><thead><tr><th>#</th><th>رقم التكت</th><th>المشروع</th><th>العنوان / الوصف</th><th>الحالة</th><th>الأولوية</th><th>رفع بواسطة</th><th>استلام / إغلاق</th><th>التاريخ</th></tr></thead><tbody>${g.rows.map((t,i)=>`<tr><td>${i+1}</td><td><b>${E(ticketNo(t))}</b></td><td>${E(projectLabel(t.project_id))}</td><td class="desc"><b>${E(t.title||'-')}</b><br>${E(t.description||'-')}</td><td><span class="status ${S(t.status||'open')}">${E(stLabel(t.status))}</span></td><td>${E(prLabel(t.priority))}</td><td>${E(raisedBy(t))}</td><td>استلم: ${E(t.claimed_by_name||'-')}<br>أغلق: ${E(t.closed_by_name||'-')}<br>${t.closure_note?`الحل: ${E(t.closure_note)}`:''}</td><td>${E(dayOnly(t.created_at))}<br><small>${E(durationText(t))}</small></td></tr>`).join('')}</tbody></table></section>`).join('')}
      <div class="footer">تم إنشاء التقرير آليًا من نظام تصنيف — حسب الفلاتر المختارة وقت التنزيل</div>
    `;
    return root;
  }
  function loadHtml2PdfV207(){
    return new Promise((resolve,reject)=>{
      if(window.html2pdf) return resolve(window.html2pdf);
      const old=document.querySelector('script[data-tasneef-html2pdf="1"]');
      if(old){ old.addEventListener('load',()=>resolve(window.html2pdf)); old.addEventListener('error',reject); return; }
      const s=document.createElement('script');
      s.src='https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js';
      s.async=true; s.dataset.tasneefHtml2pdf='1';
      s.onload=()=>window.html2pdf?resolve(window.html2pdf):reject(new Error('html2pdf not loaded'));
      s.onerror=()=>reject(new Error('تعذر تحميل مكتبة PDF'));
      document.head.appendChild(s);
    });
  }
  function printFallbackV207(node,title){
    const w=window.open('', '_blank');
    if(!w) return say('المتصفح منع فتح نافذة التقرير. اسمح بالنوافذ المنبثقة ثم حاول مرة أخرى.','err');
    w.document.open();
    w.document.write(`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${E(title)}</title></head><body>${node.outerHTML}<script>setTimeout(()=>window.print(),500)<\/script></body></html>`);
    w.document.close();
  }
  async function saveNodeAsPdfV207(node, filename){
    document.body.appendChild(node);
    node.style.position='fixed'; node.style.left='-10000px'; node.style.top='0'; node.style.zIndex='-1';
    try{
      const html2pdf = await loadHtml2PdfV207();
      await html2pdf().set({
        margin: 6,
        filename,
        image: {type:'jpeg', quality:0.98},
        html2canvas: {scale:2, useCORS:true, backgroundColor:'#ffffff', logging:false},
        jsPDF: {unit:'mm', format:'a4', orientation:'landscape'},
        pagebreak: {mode:['avoid-all','css','legacy']}
      }).from(node).save();
      say('تم تنزيل ملف PDF في التنزيلات');
    }catch(e){
      console.warn(e);
      say('تعذر التنزيل المباشر، فتحت لك نافذة الطباعة كحل بديل','err');
      printFallbackV207(node.cloneNode(true), filename.replace(/\.pdf$/,''));
    }finally{
      try{ node.remove(); }catch(_){ }
    }
  }
  window.ticketsDownloadPdfV207 = async function(){
    const list=ticketsForCurrentFilterV207();
    if(!list.length) return say('لا توجد تكتات مطابقة للفلاتر الحالية','err');
    const node=buildTicketsPdfNodeV207(list,'تقرير التكتات حسب المشرف');
    const d=new Date().toISOString().slice(0,10);
    await saveNodeAsPdfV207(node, `تقرير-التكتات-حسب-المشرف-${d}.pdf`);
  };
  window.ticketDownloadPdfV207 = async function(id){
    const t=arr(window.data?.tickets).find(x=>S(x.id)===S(id));
    if(!t) return say('التكت غير موجود','err');
    const node=buildTicketsPdfNodeV207([t], 'تقرير تكت واحد');
    await saveNodeAsPdfV207(node, `تكت-${ticketNo(t)}.pdf`);
  };
  // توافق مع أزرار النسخة السابقة V206 حتى تتحول إلى تنزيل مباشر بدل نافذة طباعة.
  window.ticketsDownloadPdfV206 = window.ticketsDownloadPdfV207;
  window.ticketDownloadPdfV206 = window.ticketDownloadPdfV207;
  function addDownloadButtonsV207(){
    [['ticketPdfBtnV206','ticketsBody'],['supTicketPdfBtnV206','supTicketsBody']].forEach(([btnId,bodyId])=>{
      const btn=$(btnId); if(btn){ btn.textContent='تنزيل PDF'; btn.onclick=window.ticketsDownloadPdfV207; }
      const body=$(bodyId); if(!body || $(btnId)) return;
      const host = body.closest('.card')?.querySelector('.filters') || body.closest('.card')?.querySelector('h2') || body.parentElement;
      const b=document.createElement('button'); b.id=btnId; b.type='button'; b.className='light ticket-pdf-v206 ticket-pdf-v207'; b.textContent='تنزيل PDF'; b.onclick=window.ticketsDownloadPdfV207;
      if(host?.classList?.contains('filters')) host.appendChild(b); else host?.insertAdjacentElement('afterend', b);
    });
  }
  function patchCardsRaisedByV207(){
    document.querySelectorAll('.smart-ticket-card').forEach(card=>{
      if(card.querySelector('.raised-by-v207')) return;
      const noText=S(card.querySelector('.smart-ticket-top strong')?.textContent);
      const t=arr(window.data?.tickets).find(x=>S(ticketNo(x))===noText);
      if(!t) return;
      const mini=card.querySelector('.smart-ticket-mini');
      const line=document.createElement('span'); line.className='raised-by-v207'; line.textContent='رفع بواسطة: '+raisedBy(t);
      if(mini) mini.prepend(line); else card.appendChild(line);
    });
  }
  const oldRender=window.renderTickets;
  window.renderTickets=function(){
    if(typeof oldRender==='function') oldRender.apply(this,arguments);
    setTimeout(()=>{ addDownloadButtonsV207(); patchCardsRaisedByV207(); },60);
  };
  function css(){
    if($('v207Css')) return;
    const st=document.createElement('style'); st.id='v207Css'; st.textContent='.ticket-pdf-v207{background:#07563d!important;color:#fff!important;border-color:#07563d!important}.raised-by-v207{display:inline-block;background:#eef7f3;border:1px solid #d5e7df;border-radius:999px;padding:4px 8px;margin:3px;color:#07563d;font-weight:700}'; document.head.appendChild(st);
  }
  function boot(){ css(); addDownloadButtonsV207(); patchCardsRaisedByV207(); }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(boot, ev==='load'?1000:350)));
  setTimeout(boot,1400);
  console.log('Tasneef V207 direct tickets PDF download loaded');
})();


/* ===================== V208 REAL FIX: PDF, DUPLICATE TABS, CONSUMPTION LABEL, PURCHASE PRICES =====================
   إصلاحات مباشرة للملاحظات:
   1) تنزيل PDF للتكتات لا يطلع فارغ.
   2) إزالة تكرار تبويبات المصروفات والمخزون.
   3) حذف خانة إجمالي الاستهلاك من الكروت وتفاصيل المنتج.
   4) دعم سعر دفعة إدخال المخزون وحفظه في حركة الإدخال، مع متوسط تكلفة مرجح، وإظهار دفعات الشراء.
=============================================================================================================== */
(function(){
  'use strict';
  window.TASNEEF_BUILD = 'V208_REAL_FIX_2026_05_20';
  const $ = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const N = v => { const x = Number(String(v ?? 0).replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const A = v => Array.isArray(v) ? v : [];
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const say = (t,type) => { try{ if(typeof msg==='function') msg(t,type); else alert(t); }catch(_){ alert(t); } };
  const money = v => { try{ return (Number(v)||0).toLocaleString('ar-SA',{minimumFractionDigits:2,maximumFractionDigits:2}) + ' ر.س'; }catch(_){ return (Number(v)||0).toFixed(2)+' ر.س'; } };
  const qty = v => { try{ return (Number(v)||0).toLocaleString('ar-SA',{maximumFractionDigits:2}); }catch(_){ return String(v||0); } };

  /* 1) إزالة التبويبات المكررة */
  function removeDuplicateFinanceTabsV208(){
    const tabs = [...document.querySelectorAll('.finance-tab, .finance-tabs button, button[onclick*="financeShowTab"]')];
    const seen = new Set();
    tabs.forEach(btn=>{
      const text = S(btn.textContent).replace(/\s+/g,' ');
      if(!text) return;
      const key = text;
      if(seen.has(key)) btn.remove();
      else seen.add(key);
    });
  }

  /* 2) حذف إجمالي الاستهلاك من أي كرت أو تفاصيل */
  function removeTotalConsumptionBlocksV208(){
    const bad = /إجمالي\s*(الاستهلاك|استهلاك|المستهلك)|Total\s*Consumed|Total\s*Consumption/i;
    document.querySelectorAll('.v180-stats > div, .smart-meta-v129 .m, .smart-meta-v129 > div, .m').forEach(el=>{
      const small = S(el.querySelector('small')?.textContent || el.textContent);
      if(bad.test(small)) el.remove();
    });
    document.querySelectorAll('th,td,div,span,small').forEach(el=>{
      if(el.children.length) return;
      if(bad.test(S(el.textContent))) {
        const parent = el.closest('.m') || el.closest('.v180-stats > div');
        if(parent) parent.remove();
      }
    });
  }

  /* 3) سعر دفعة الإدخال */
  function ensureMovementCostInputV208(){
    const type = $('inventoryMovementType');
    const qtyEl = $('inventoryMovementQty');
    if(!type || !qtyEl || $('inventoryMovementUnitCostV208')) return;
    const wrap = document.createElement('div');
    wrap.id = 'inventoryMovementUnitCostWrapV208';
    wrap.innerHTML = '<label>سعر هذه الدفعة</label><input type="number" id="inventoryMovementUnitCostV208" step="0.01" placeholder="اكتب سعر الشراء لهذه الكمية">';
    const parent = qtyEl.closest('.split') || qtyEl.parentElement;
    parent.insertAdjacentElement('afterend', wrap);
    function toggle(){
      const show = S(type.value)==='in';
      wrap.style.display = show ? '' : 'none';
      if(show){
        const item = A(window.data?.inventoryItems).find(i=>S(i.id)===S($('inventoryMovementItem')?.value));
        if(item && !N($('inventoryMovementUnitCostV208')?.value)) $('inventoryMovementUnitCostV208').value = N(item.unit_cost || item.unit_price || 0) || '';
      }
    }
    type.addEventListener('change', toggle);
    $('inventoryMovementItem')?.addEventListener('change', toggle);
    setTimeout(toggle,50);
  }

  function weightedCostV208(oldQty, oldCost, inQty, inCost){
    oldQty=N(oldQty); oldCost=N(oldCost); inQty=N(inQty); inCost=N(inCost);
    const totalQty = oldQty + inQty;
    if(totalQty<=0) return inCost || oldCost || 0;
    if(inCost<=0) return oldCost || 0;
    return ((oldQty*oldCost) + (inQty*inCost)) / totalQty;
  }

  // استبدال حفظ حركة المخزون حتى يقبل سعر الدفعة الجديدة، ولا يحسب كل شيء بالسعر القديم.
  window.inventorySaveMovement = async function(btn){
    try{
      if(btn) btn.disabled = true;
      const itemId = $('inventoryMovementItem')?.value;
      if(!itemId) throw new Error('اختر الصنف');
      const item = A(window.data?.inventoryItems).find(i=>S(i.id)===S(itemId));
      if(!item) throw new Error('الصنف غير موجود');
      const type = $('inventoryMovementType')?.value || 'out';
      const q = N($('inventoryMovementQty')?.value);
      if(q<=0) throw new Error('الكمية مطلوبة');
      const pid = $('inventoryMovementProject')?.value || null;
      const oldQty = N(item.quantity);
      const oldCost = N(item.unit_cost || item.unit_price || 0);
      const batchCost = type==='in' ? (N($('inventoryMovementUnitCostV208')?.value) || oldCost) : oldCost;
      const row = {
        item_id: Number(itemId),
        item_name: item.name || '',
        movement_type: type,
        quantity: q,
        movement_date: $('inventoryMovementDate')?.value || (typeof today==='function' ? today() : new Date().toISOString().slice(0,10)),
        project_id: pid ? Number(pid) : null,
        project_name: pid ? (typeof projectName==='function' ? projectName(pid) : '') : '',
        receiver: $('inventoryMovementReceiver')?.value || '',
        reason: $('inventoryMovementReason')?.value || '',
        notes: $('inventoryMovementNotes')?.value || '',
        product_code: (typeof v118ProductCode==='function' ? v118ProductCode(item) : (item.product_code || item.serial_number || '')),
        barcode: (typeof v118Barcode==='function' ? v118Barcode(item) : (item.barcode || item.supplier_barcode || '')),
        unit_cost: batchCost
      };
      const id = $('inventoryMovementId')?.value;
      if(id) throw new Error('تعديل حركة المخزون غير متاح؛ احذف الحركة وأضف حركة جديدة للحفاظ على دقة الرصيد');
      const res = await sb.from('inventory_movements').insert(row);
      if(res.error) throw res.error;
      let newQty = oldQty;
      let newCost = oldCost;
      if(type==='in'){
        newQty = oldQty + q;
        newCost = weightedCostV208(oldQty, oldCost, q, batchCost);
      }else if(type==='out' || type==='consume'){
        newQty = oldQty - q;
      }else if(type==='return'){
        newQty = oldQty + q;
      }else if(type==='adjust'){
        newQty = q;
      }
      const upd = type==='in' ? {quantity:newQty, unit_cost:newCost} : {quantity:newQty};
      const ures = await sb.from('inventory_items').update(upd).eq('id', itemId);
      if(ures.error) throw ures.error;
      say(type==='in' ? 'تم حفظ إدخال المخزون بسعر الدفعة وتحديث متوسط التكلفة' : 'تم حفظ حركة المخزون وتحديث الرصيد');
      try{ if(typeof inventoryClearMovementForm==='function') inventoryClearMovementForm(); }catch(_){ }
      try{ await financeLoadAll(); }catch(_){ }
      setTimeout(bootV208,300);
    }catch(e){ say(e.message || String(e), 'err'); }
    finally{ if(btn) btn.disabled = false; }
  };

  /* 4) إظهار دفعات الشراء داخل تفاصيل المنتج */
  function appendPurchaseBatchesToProductModalV208(){
    const modal = document.querySelector('.modal.show, .modal[style*="display: block"], .smart-modal, #productDetailModal, #smartModal');
    const holder = modal || document.body;
    if(!holder || holder.querySelector('#purchaseBatchesV208')) return;
    const titleText = S(holder.textContent);
    if(!/تفاصيل المنتج|ملخص الحركة|آخر الحركات/.test(titleText)) return;
    let itemId = '';
    const codeText = S(holder.textContent);
    const item = A(window.data?.inventoryItems).find(i => codeText.includes(S(i.name)) || codeText.includes(S(i.product_code||i.serial_number||'')));
    if(item) itemId = S(item.id);
    if(!itemId) return;
    const ins = A(window.data?.inventoryMovements).filter(m=>S(m.item_id)===itemId && S(m.movement_type)==='in')
      .sort((a,b)=>String(b.movement_date||b.created_at||'').localeCompare(String(a.movement_date||a.created_at||'')));
    if(!ins.length) return;
    const box = document.createElement('div');
    box.id = 'purchaseBatchesV208';
    box.innerHTML = `<h3>دفعات الشراء وأسعارها</h3><table><thead><tr><th>التاريخ</th><th>الكمية</th><th>سعر الدفعة</th><th>المورد</th><th>السبب / الفاتورة</th></tr></thead><tbody>${ins.map(m=>`<tr><td>${E(m.movement_date||String(m.created_at||'').slice(0,10)||'-')}</td><td>${qty(m.quantity)}</td><td>${money(m.unit_cost)}</td><td>${E(m.receiver||'-')}</td><td>${E(m.reason||m.notes||'-')}</td></tr>`).join('')}</tbody></table>`;
    const table = [...holder.querySelectorAll('table')].pop();
    if(table) table.insertAdjacentElement('afterend', box); else holder.appendChild(box);
  }

  /* 5) تنزيل PDF للتكتات بدون ملف فارغ: نترك العنصر ظاهرًا أثناء الالتقاط ثم نحذفه */
  function waitImagesV208(node){
    const imgs=[...node.querySelectorAll('img')];
    return Promise.all(imgs.map(img=>{
      if(img.complete) return Promise.resolve();
      return new Promise(resolve=>{ img.onload=resolve; img.onerror=resolve; setTimeout(resolve,1200); });
    }));
  }
  async function loadHtml2PdfV208(){
    if(window.html2pdf) return window.html2pdf;
    await new Promise((resolve,reject)=>{
      const old=document.querySelector('script[data-tasneef-html2pdf="1"]');
      if(old){ old.addEventListener('load',resolve,{once:true}); old.addEventListener('error',reject,{once:true}); return; }
      const s=document.createElement('script');
      s.src='https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js';
      s.async=true; s.dataset.tasneefHtml2pdf='1'; s.onload=resolve; s.onerror=reject; document.head.appendChild(s);
    });
    if(!window.html2pdf) throw new Error('تعذر تحميل مكتبة PDF');
    return window.html2pdf;
  }
  function printFallbackV208(node,title){
    const w=window.open('', '_blank');
    if(!w) return say('المتصفح منع فتح نافذة التقرير. اسمح بالنوافذ المنبثقة ثم حاول مرة أخرى.','err');
    w.document.open();
    w.document.write(`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${E(title)}</title><style>body{margin:0;background:#fff}</style></head><body>${node.outerHTML}<script>setTimeout(()=>window.print(),800)<\/script></body></html>`);
    w.document.close();
  }
  async function savePdfNodeV208(node, filename){
    node.style.position = 'absolute';
    node.style.top = '0';
    node.style.right = '0';
    node.style.left = 'auto';
    node.style.zIndex = '2147483647';
    node.style.background = '#fff';
    node.style.opacity = '1';
    node.style.transform = 'none';
    document.body.appendChild(node);
    try{
      await waitImagesV208(node);
      await new Promise(r=>setTimeout(r,350));
      const html2pdf = await loadHtml2PdfV208();
      await html2pdf().set({
        margin: 6,
        filename,
        image: {type:'jpeg', quality:0.98},
        html2canvas: {scale:2, useCORS:true, allowTaint:true, backgroundColor:'#ffffff', logging:false, scrollX:0, scrollY:0},
        jsPDF: {unit:'mm', format:'a4', orientation:'landscape'},
        pagebreak: {mode:['css','legacy']}
      }).from(node).save();
      say('تم تنزيل ملف PDF في التنزيلات');
    }catch(e){
      console.warn('V208 PDF direct failed', e);
      say('تعذر التنزيل المباشر، فتحت لك نافذة الطباعة كحل بديل','err');
      printFallbackV208(node.cloneNode(true), filename.replace(/\.pdf$/,''));
    }finally{
      try{ node.remove(); }catch(_){ }
    }
  }

  // نستخدم دالة بناء تقرير V207 إن كانت موجودة داخليًا غير متاحة؛ لذلك نبني تقريرًا مستقلًا هنا.
  function ticketNo(t){ return t?.ticket_number || ('T-' + S(t?.id || 0).padStart(4,'0')); }
  function statusText(s){ return s==='closed'?'مغلق':(s==='processing'?'تحت المعالجة':'مفتوح'); }
  function priorityText(p){ return p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي')); }
  function pName(id){ try{ return typeof projectName==='function' ? projectName(id) : '-'; }catch(_){ return '-'; } }
  function sName(id){ try{ return typeof supervisorName==='function' ? supervisorName(id) : '-'; }catch(_){ return '-'; } }
  function uName(id){ const u=A(window.data?.users).find(x=>S(x.id)===S(id)||S(x.username)===S(id)); return u ? S(u.full_name||u.name||u.username) : (S(id)||'-'); }
  function raisedBy(t){ return S(t.created_by_name||t.created_by_full_name||t.raised_by_name||t.created_user_name) || uName(t.created_by||t.raised_by||t.user_id||t.created_user_id); }
  function currentTicketListV208(){
    let list=A(window.data?.tickets).slice();
    const status = $('ticketFilterStatus')?.value || $('supTicketFilterStatus')?.value || '';
    const search = S($('ticketSearch')?.value || $('supTicketSearch')?.value).toLowerCase();
    const project = $('supTicketFilterProject')?.value || '';
    if(status) list=list.filter(t=>S(t.status||'open')===S(status));
    if(project) list=list.filter(t=>S(t.project_id)===S(project));
    if(search) list=list.filter(t=>[ticketNo(t),t.title,t.description,pName(t.project_id),sName(t.supervisor_id),raisedBy(t),statusText(t.status)].join(' ').toLowerCase().includes(search));
    return list.sort((a,b)=>S(sName(a.supervisor_id)).localeCompare(S(sName(b.supervisor_id)),'ar') || new Date(b.created_at||0)-new Date(a.created_at||0));
  }
  function buildTicketsNodeV208(list,title){
    const groups=new Map();
    list.forEach(t=>{ const k=S(t.supervisor_id)||'none'; if(!groups.has(k)) groups.set(k,{name:S(sName(t.supervisor_id))||'بدون مشرف',rows:[]}); groups.get(k).rows.push(t); });
    const root=document.createElement('div'); root.dir='rtl'; root.className='tasneef-ticket-pdf-v208';
    root.innerHTML=`<style>.tasneef-ticket-pdf-v208{font-family:Tahoma,Arial,sans-serif;color:#123b2f;background:#fff;width:1120px;padding:28px;direction:rtl;box-sizing:border-box}.tasneef-ticket-pdf-v208 *{box-sizing:border-box}.t-head{display:flex;justify-content:space-between;align-items:center;border-bottom:4px solid #0b5b49;padding-bottom:14px;margin-bottom:16px}.t-head h1{margin:0;color:#0b5b49;font-size:30px}.t-head p{margin:5px 0 0;color:#60726a}.brand{font-weight:900;color:#0b5b49}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:14px 0}.box{border:1px solid #d6e7e0;background:#f4faf7;border-radius:13px;padding:10px}.box small{display:block;color:#6a7a73}.box b{font-size:20px}.section{page-break-inside:avoid;margin:18px 0;border:1px solid #cadbd4;border-radius:14px;overflow:hidden}.section-h{background:#2779b7;color:#fff;padding:10px 13px;font-weight:900;display:flex;justify-content:space-between}.tbl{width:100%;border-collapse:collapse;font-size:12px}.tbl th{background:#e9f1f5;color:#123b2f}.tbl th,.tbl td{border:1px solid #d7e0e4;padding:8px;text-align:right;vertical-align:top}.tbl tr:nth-child(even) td{background:#f6f9fb}.desc{white-space:pre-wrap;line-height:1.5;max-width:270px}.footer{text-align:center;border-top:1px solid #d5e3dd;margin-top:18px;padding-top:10px;color:#71837b;font-size:12px}</style><div class="t-head"><div><h1>${E(title)}</h1><p>حسب الفلاتر الحالية — تاريخ التصدير: ${E(new Date().toLocaleString('ar-SA'))}</p></div><div class="brand">شركة تصنيف لإدارة المرافق</div></div><div class="meta"><div class="box"><small>إجمالي التكتات</small><b>${list.length}</b></div><div class="box"><small>مفتوح</small><b>${list.filter(t=>S(t.status||'open')==='open').length}</b></div><div class="box"><small>تحت المعالجة</small><b>${list.filter(t=>S(t.status)==='processing').length}</b></div><div class="box"><small>مغلق</small><b>${list.filter(t=>S(t.status)==='closed').length}</b></div></div>${[...groups.values()].map(g=>`<section class="section"><div class="section-h"><span>المشرف: ${E(g.name)}</span><span>عدد التكتات: ${g.rows.length}</span></div><table class="tbl"><thead><tr><th>#</th><th>رقم التكت</th><th>المشروع</th><th>العنوان / الوصف</th><th>الحالة</th><th>الأولوية</th><th>رفع بواسطة</th><th>استلام / إغلاق</th><th>التاريخ</th></tr></thead><tbody>${g.rows.map((t,i)=>`<tr><td>${i+1}</td><td><b>${E(ticketNo(t))}</b></td><td>${E(pName(t.project_id))}</td><td class="desc"><b>${E(t.title||'-')}</b><br>${E(t.description||'-')}</td><td>${E(statusText(t.status))}</td><td>${E(priorityText(t.priority))}</td><td>${E(raisedBy(t))}</td><td>استلم: ${E(t.claimed_by_name||'-')}<br>أغلق: ${E(t.closed_by_name||'-')}<br>${t.closure_note?`الحل: ${E(t.closure_note)}`:''}</td><td>${E(String(t.created_at||'').slice(0,10)||'-')}</td></tr>`).join('')}</tbody></table></section>`).join('') || '<div class="box">لا توجد تكتات مطابقة للفلاتر الحالية</div>'}<div class="footer">تم إنشاء التقرير آليًا من نظام تصنيف</div>`;
    return root;
  }
  async function ticketsDownloadPdfV208(){
    const list=currentTicketListV208();
    if(!list.length) return say('لا توجد تكتات مطابقة للفلاتر الحالية','err');
    const node=buildTicketsNodeV208(list,'تقرير التكتات حسب المشرف');
    await savePdfNodeV208(node, `تقرير-التكتات-حسب-المشرف-${new Date().toISOString().slice(0,10)}.pdf`);
  }
  async function ticketDownloadPdfSingleV208(id){
    const t=A(window.data?.tickets).find(x=>S(x.id)===S(id));
    if(!t) return say('التكت غير موجود','err');
    const node=buildTicketsNodeV208([t],'تقرير تكت واحد');
    await savePdfNodeV208(node, `تكت-${ticketNo(t)}.pdf`);
  }
  window.ticketsDownloadPdfV207 = ticketsDownloadPdfV208;
  window.ticketsDownloadPdfV206 = ticketsDownloadPdfV208;
  window.ticketDownloadPdfV207 = ticketDownloadPdfSingleV208;
  window.ticketDownloadPdfV206 = ticketDownloadPdfSingleV208;

  function patchPdfButtonsV208(){
    document.querySelectorAll('#ticketPdfBtnV206,#supTicketPdfBtnV206,.ticket-pdf-v206,.ticket-pdf-v207').forEach(b=>{ b.textContent='تنزيل PDF'; b.onclick=ticketsDownloadPdfV208; });
  }

  function bootV208(){
    ensureMovementCostInputV208();
    removeDuplicateFinanceTabsV208();
    removeTotalConsumptionBlocksV208();
    /* V230 disabled duplicate old purchase batch block */
    patchPdfButtonsV208();
  }
  window.tasneefBootV208 = bootV208;
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(bootV208, ev==='load'?1000:300)));
  setInterval(bootV208, 1200);
  setTimeout(bootV208, 500);
  setTimeout(bootV208, 2000);
  console.log('Tasneef V208 real fixes loaded');
})();

/* ===================== V209: REAL PDF FALLBACK + SMART ALERTS =====================
   1) إصلاح تنزيل تقرير التكتات حتى لا يطلع PDF فارغ.
   2) عرض التنبيهات بشكل ذكي مع زر حذف/إخفاء لكل تنبيه.
   3) إزالة أي خانة متبقية باسم إجمالي الاستهلاك/إجمالي المستهلك.
=============================================================================== */
(function(){
  'use strict';
  window.TASNEEF_BUILD = 'V209_REAL_PDF_SMART_ALERTS_2026_05_20';
  const $ = id => document.getElementById(id);
  const A = v => Array.isArray(v) ? v : [];
  const S = v => String(v ?? '').trim();
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const say = (t,type) => { try{ if(typeof msg === 'function') msg(t,type); else alert(t); }catch(_){ alert(t); } };
  const dateOnly = v => S(v).slice(0,10) || '-';
  const userName = id => {
    const u = A(window.data?.users || window.data?.appUsers).find(x => S(x.id)===S(id) || S(x.username)===S(id));
    return u ? S(u.full_name || u.name || u.username) : (S(id) || '-');
  };
  const projectText = id => { try{ return typeof projectName === 'function' ? projectName(id) : '-'; }catch(_){ return '-'; } };
  const supervisorText = id => { try{ return typeof supervisorName === 'function' ? supervisorName(id) : '-'; }catch(_){ return '-'; } };
  const ticketNo = t => S(t?.ticket_number) || ('T-' + S(t?.id || 0).padStart(4,'0'));
  const statusText = s => S(s)==='closed'?'مغلق':(S(s)==='processing'?'تحت المعالجة':'مفتوح');
  const priorityText = p => S(p)==='urgent'?'عاجل':(S(p)==='high'?'مهم':(S(p)==='low'?'منخفض':'عادي'));
  const raisedBy = t => S(t?.created_by_name || t?.created_by_full_name || t?.raised_by_name || t?.created_user_name || t?.creator_name || t?.created_by_username) || userName(t?.created_by || t?.raised_by || t?.user_id || t?.created_user_id || t?.created_by_id);

  function ticketListByCurrentFilters(){
    let list = A(window.data?.tickets).slice();
    const status = S($('ticketFilterStatus')?.value || $('supTicketFilterStatus')?.value || $('ticketStatusFilter')?.value || '');
    const project = S($('ticketFilterProject')?.value || $('supTicketFilterProject')?.value || '');
    const supervisor = S($('ticketFilterSupervisor')?.value || $('supTicketFilterSupervisor')?.value || '');
    const search = S($('ticketSearch')?.value || $('supTicketSearch')?.value || '').toLowerCase();
    const from = S($('ticketFilterFrom')?.value || $('supTicketFilterFrom')?.value || '');
    const to = S($('ticketFilterTo')?.value || $('supTicketFilterTo')?.value || '');
    if(status) list = list.filter(t => S(t.status || 'open') === status);
    if(project) list = list.filter(t => S(t.project_id) === project);
    if(supervisor) list = list.filter(t => S(t.supervisor_id) === supervisor);
    if(from) list = list.filter(t => dateOnly(t.created_at) >= from);
    if(to) list = list.filter(t => dateOnly(t.created_at) <= to);
    if(search){
      list = list.filter(t => [ticketNo(t), t.title, t.description, projectText(t.project_id), supervisorText(t.supervisor_id), raisedBy(t), statusText(t.status), priorityText(t.priority)].join(' ').toLowerCase().includes(search));
    }
    return list.sort((a,b)=> S(supervisorText(a.supervisor_id)).localeCompare(S(supervisorText(b.supervisor_id)),'ar') || new Date(b.created_at||0) - new Date(a.created_at||0));
  }

  function buildTicketsReportHtml(list, title){
    const groups = new Map();
    list.forEach(t => {
      const key = S(t.supervisor_id) || 'none';
      if(!groups.has(key)) groups.set(key,{name:S(supervisorText(t.supervisor_id)) || 'بدون مشرف', rows:[]});
      groups.get(key).rows.push(t);
    });
    const sections = [...groups.values()].map(g => `
      <section class="section">
        <div class="section-title"><span>المشرف: ${E(g.name)}</span><span>عدد التكتات: ${g.rows.length}</span></div>
        <table>
          <thead><tr><th>#</th><th>رقم التكت</th><th>المشروع</th><th>العنوان والوصف</th><th>الحالة</th><th>الأولوية</th><th>رفع بواسطة</th><th>استلام / إغلاق</th><th>التاريخ</th></tr></thead>
          <tbody>${g.rows.map((t,i)=>`
            <tr>
              <td>${i+1}</td>
              <td><b>${E(ticketNo(t))}</b></td>
              <td>${E(projectText(t.project_id))}</td>
              <td class="desc"><b>${E(t.title || '-')}</b><br>${E(t.description || '-')}</td>
              <td>${E(statusText(t.status))}</td>
              <td>${E(priorityText(t.priority))}</td>
              <td>${E(raisedBy(t))}</td>
              <td>استلم: ${E(t.claimed_by_name || userName(t.claimed_by) || '-')}<br>أغلق: ${E(t.closed_by_name || userName(t.closed_by) || '-')}<br>${t.closure_note ? 'الحل: '+E(t.closure_note) : ''}</td>
              <td>${E(dateOnly(t.created_at))}</td>
            </tr>`).join('')}</tbody>
        </table>
      </section>`).join('') || '<div class="empty">لا توجد تكتات مطابقة للفلاتر الحالية</div>';
    return `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${E(title)}</title>
      <style>
        @page{size:A4 landscape;margin:8mm} html,body{background:#fff;margin:0;padding:0;color:#153d32;font-family:Tahoma,Arial,sans-serif;direction:rtl;-webkit-print-color-adjust:exact;print-color-adjust:exact}.wrap{padding:20px;max-width:1320px;margin:0 auto}.head{display:flex;align-items:center;justify-content:space-between;border-bottom:4px solid #0b5b49;padding-bottom:12px;margin-bottom:14px}.head h1{margin:0;color:#0b5b49;font-size:28px}.head p{margin:5px 0 0;color:#647970}.brand{font-size:18px;font-weight:900;color:#0b5b49}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:14px 0}.box{border:1px solid #cfe2da;background:#f2faf6;border-radius:12px;padding:10px}.box small{display:block;color:#657972}.box b{font-size:21px}.section{break-inside:avoid;page-break-inside:avoid;border:1px solid #c9dcd4;border-radius:12px;overflow:hidden;margin:14px 0}.section-title{background:#2779b7;color:#fff;padding:9px 12px;font-weight:900;display:flex;justify-content:space-between}table{width:100%;border-collapse:collapse;font-size:11px}th,td{border:1px solid #d6e2df;padding:7px;text-align:right;vertical-align:top}th{background:#eaf2f6;color:#143d32}tr:nth-child(even) td{background:#f7fafb}.desc{white-space:pre-wrap;line-height:1.45;max-width:310px}.empty{border:1px solid #d6e2df;padding:22px;border-radius:12px;text-align:center}.footer{text-align:center;border-top:1px solid #d6e2df;margin-top:16px;padding-top:8px;color:#73847e;font-size:12px}.print-actions{position:fixed;left:10px;top:10px;z-index:9999;display:flex;gap:8px}.print-actions button{border:0;background:#0b5b49;color:#fff;padding:10px 14px;border-radius:10px;font-weight:700;cursor:pointer}@media print{.print-actions{display:none}.wrap{padding:0}}
      </style></head><body><div class="print-actions"><button onclick="window.print()">طباعة / حفظ PDF</button></div><div class="wrap"><div class="head"><div><h1>${E(title)}</h1><p>حسب الفلاتر الحالية — تاريخ التصدير: ${E(new Date().toLocaleString('ar-SA'))}</p></div><div class="brand">شركة تصنيف لإدارة المرافق</div></div><div class="meta"><div class="box"><small>إجمالي التكتات</small><b>${list.length}</b></div><div class="box"><small>مفتوح</small><b>${list.filter(t=>S(t.status||'open')==='open').length}</b></div><div class="box"><small>تحت المعالجة</small><b>${list.filter(t=>S(t.status)==='processing').length}</b></div><div class="box"><small>مغلق</small><b>${list.filter(t=>S(t.status)==='closed').length}</b></div></div>${sections}<div class="footer">تم إنشاء التقرير آليًا من نظام تصنيف</div></div></body></html>`;
  }

  function openPrintableTicketsReport(list, title){
    const html = buildTicketsReportHtml(list, title);
    const w = window.open('', '_blank');
    if(!w){
      const blob = new Blob([html], {type:'text/html;charset=utf-8'});
      const a = document.createElement('a');
      a.href = URL.createObjectURL(blob);
      a.download = `تقرير-التكتات-${new Date().toISOString().slice(0,10)}.html`;
      document.body.appendChild(a); a.click(); setTimeout(()=>{URL.revokeObjectURL(a.href); a.remove();}, 1500);
      return say('المتصفح منع نافذة PDF، تم تنزيل ملف HTML بديل افتحه واحفظه PDF','err');
    }
    w.document.open();
    w.document.write(html);
    w.document.close();
    w.focus();
    setTimeout(()=>{ try{ w.print(); }catch(_){ } }, 900);
  }

  async function loadScriptOnce(src, id){
    if(id && document.getElementById(id)) return new Promise((resolve,reject)=>{ const s=document.getElementById(id); if(s.dataset.loaded==='1') resolve(); else { s.addEventListener('load',resolve,{once:true}); s.addEventListener('error',reject,{once:true}); }});
    return new Promise((resolve,reject)=>{ const s=document.createElement('script'); if(id) s.id=id; s.src=src; s.async=true; s.onload=()=>{ s.dataset.loaded='1'; resolve(); }; s.onerror=reject; document.head.appendChild(s); });
  }

  async function directPdfAsImage(list, filename){
    /* هذه الطريقة أقوى من html2pdf: نلتقط التقرير كصورة ونضعها داخل jsPDF. */
    await loadScriptOnce('https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js','tasneef-html2canvas-v209');
    await loadScriptOnce('https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js','tasneef-jspdf-v209');
    if(!window.html2canvas || !window.jspdf?.jsPDF) throw new Error('تعذر تحميل مكتبات PDF');
    const html = buildTicketsReportHtml(list, 'تقرير التكتات حسب المشرف');
    const holder = document.createElement('div');
    holder.style.cssText='position:fixed;left:0;top:0;width:1320px;background:#fff;z-index:2147483647;opacity:1;transform:none;pointer-events:none;';
    holder.innerHTML = html.replace(/^[\s\S]*<body[^>]*>/i,'').replace(/<\/body>[\s\S]*$/i,'');
    const actions = holder.querySelector('.print-actions'); if(actions) actions.remove();
    document.body.appendChild(holder);
    await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)));
    const canvas = await window.html2canvas(holder, {backgroundColor:'#ffffff', scale:2, useCORS:true, logging:false, scrollX:0, scrollY:0, windowWidth:1320});
    holder.remove();
    if(canvas.width < 50 || canvas.height < 50) throw new Error('التقاط التقرير فشل');
    const { jsPDF } = window.jspdf;
    const pdf = new jsPDF({orientation:'landscape', unit:'mm', format:'a4'});
    const pageW = pdf.internal.pageSize.getWidth();
    const pageH = pdf.internal.pageSize.getHeight();
    const imgW = pageW;
    const imgH = canvas.height * imgW / canvas.width;
    let remaining = imgH;
    let y = 0;
    const imgData = canvas.toDataURL('image/jpeg', 0.98);
    pdf.addImage(imgData, 'JPEG', 0, y, imgW, imgH);
    remaining -= pageH;
    while(remaining > 2){
      y -= pageH;
      pdf.addPage();
      pdf.addImage(imgData, 'JPEG', 0, y, imgW, imgH);
      remaining -= pageH;
    }
    pdf.save(filename);
  }

  async function ticketsDownloadPdfV209(){
    const list = ticketListByCurrentFilters();
    if(!list.length) return say('لا توجد تكتات مطابقة للفلاتر الحالية','err');
    const filename = `تقرير-التكتات-حسب-المشرف-${new Date().toISOString().slice(0,10)}.pdf`;
    try{
      await directPdfAsImage(list, filename);
      say('تم تنزيل PDF في التنزيلات');
    }catch(e){
      console.warn('V209 direct PDF failed, opening printable report', e);
      say('فتحت لك التقرير جاهزًا للطباعة والحفظ PDF لأن التنزيل المباشر تعذر في المتصفح','err');
      openPrintableTicketsReport(list, 'تقرير التكتات حسب المشرف');
    }
  }

  async function singleTicketPdfV209(id){
    const t = A(window.data?.tickets).find(x => S(x.id) === S(id));
    if(!t) return say('التكت غير موجود','err');
    try{
      await directPdfAsImage([t], `تكت-${ticketNo(t)}.pdf`);
      say('تم تنزيل PDF في التنزيلات');
    }catch(e){
      openPrintableTicketsReport([t], `تقرير التكت ${ticketNo(t)}`);
    }
  }

  window.ticketsDownloadPdfV209 = ticketsDownloadPdfV209;
  window.ticketsDownloadPdfV208 = ticketsDownloadPdfV209;
  window.ticketsDownloadPdfV207 = ticketsDownloadPdfV209;
  window.ticketsDownloadPdfV206 = ticketsDownloadPdfV209;
  window.ticketDownloadPdfV209 = singleTicketPdfV209;
  window.ticketDownloadPdfV208 = singleTicketPdfV209;
  window.ticketDownloadPdfV207 = singleTicketPdfV209;
  window.ticketDownloadPdfV206 = singleTicketPdfV209;

  function patchTicketPdfButtons(){
    document.querySelectorAll('#ticketPdfBtnV206,#supTicketPdfBtnV206,.ticket-pdf-v206,.ticket-pdf-v207,.ticket-pdf-v208,[data-ticket-pdf]').forEach(b=>{ b.textContent='تنزيل PDF'; b.onclick=ticketsDownloadPdfV209; });
    document.querySelectorAll('button').forEach(b=>{
      if(S(b.textContent).includes('تصدير PDF') || S(b.textContent).includes('تنزيل PDF')){
        const area = b.closest('#ticketsPage,#ticketsSection,#supervisorTickets,#ticketsPanel,[id*="Ticket"],[id*="ticket"]');
        if(area) b.onclick = ticketsDownloadPdfV209;
      }
    });
  }

  const dismissedKey = 'tasneef_dismissed_alerts_v209';
  const getDismissed = () => { try{return JSON.parse(localStorage.getItem(dismissedKey)||'[]');}catch(_){return [];} };
  const setDismissed = arr => localStorage.setItem(dismissedKey, JSON.stringify(Array.from(new Set(arr))));
  window.dismissSmartAlertV209 = function(id){ const d=getDismissed(); d.push(S(id)); setDismissed(d); renderSmartAlertsV209(); };
  window.clearDismissedAlertsV209 = function(){ localStorage.removeItem(dismissedKey); renderSmartAlertsV209(); };

  function smartAlertsData(){
    const out=[];
    A(window.data?.projects).filter(p=>!p.supervisor_id).forEach(p=>out.push({id:`project-nosup-${p.id}`, level:'warn', group:'المشاريع', title:'مشروع بدون مشرف', text:p.name || '-', action:`openProjectManager&&openProjectManager(${Number(p.id)||0})`}));
    A(window.data?.workers).filter(w=>{ try{return !workerSupId(w);}catch(_){return !w.supervisor_id && !w.app_supervisor_id;} }).forEach(w=>out.push({id:`worker-nosup-${w.id}`, level:'warn', group:'العمال', title:'عامل بدون مشرف', text:w.name || '-', action:`editWorker&&editWorker(${Number(w.id)||0})`}));
    A(window.data?.logs).filter(l=>!l.check_out).forEach(l=>out.push({id:`log-open-${l.id}`, level:'danger', group:'الحضور', title:'دخول بدون خروج', text:`${projectText(l.project_id)} - ${supervisorText(l.supervisor_id)}`, action:''}));
    A(window.data?.tickets).filter(t=>S(t.status||'open')==='open').forEach(t=>out.push({id:`ticket-open-${t.id}`, level:S(t.priority)==='urgent'?'danger':'warn', group:'التكتات', title:'تكت مفتوح', text:`${t.title || '-'} - ${projectText(t.project_id)} — رفع بواسطة: ${raisedBy(t)}`, action:`viewTicketSmartV147&&viewTicketSmartV147(${Number(t.id)||0})`}));
    return out;
  }

  function renderSmartAlertsV209(){
    const div = $('alertsList'); if(!div) return;
    const dismissed = new Set(getDismissed());
    let alerts = smartAlertsData().filter(a=>!dismissed.has(S(a.id)));
    const danger = alerts.filter(a=>a.level==='danger').length;
    const warn = alerts.filter(a=>a.level!=='danger').length;
    if(!alerts.length){
      div.innerHTML = `<div class="smart-alerts-v209"><div class="smart-alert-head"><b>التنبيهات الذكية</b><button type="button" onclick="clearDismissedAlertsV209()">إظهار المحذوف</button></div><div class="alert-empty">لا توجد تنبيهات حالياً</div></div>`;
      return;
    }
    const groups = new Map();
    alerts.forEach(a=>{ if(!groups.has(a.group)) groups.set(a.group,[]); groups.get(a.group).push(a); });
    div.innerHTML = `<style>.smart-alerts-v209{display:grid;gap:12px}.smart-alert-head{display:flex;align-items:center;justify-content:space-between;gap:10px;background:#f4faf7;border:1px solid #d7e8e0;border-radius:16px;padding:12px}.smart-alert-head b{font-size:20px;color:#0b5b49}.smart-alert-head .counts{display:flex;gap:8px;flex-wrap:wrap}.pill-a{border-radius:999px;padding:6px 10px;font-weight:800;background:#eef7f3;color:#0b5b49}.pill-danger{background:#ffecec;color:#a22121}.pill-warn{background:#fff8de;color:#7a5a00}.alert-group{border:1px solid #d7e8e0;border-radius:16px;overflow:hidden;background:#fff}.alert-group-title{background:#0b5b49;color:#fff;padding:10px 12px;font-weight:900;display:flex;justify-content:space-between}.alert-card{display:grid;grid-template-columns:110px 1fr auto;gap:10px;align-items:center;padding:10px 12px;border-top:1px solid #edf3f0}.alert-card.danger{background:#fff8f8}.alert-card.warn{background:#fffdf5}.alert-title{font-weight:900;color:#123d31}.alert-text{color:#4e625a;font-size:13px;margin-top:3px}.alert-actions{display:flex;gap:6px}.alert-actions button{border:0;border-radius:10px;padding:7px 10px;cursor:pointer;font-weight:700}.alert-actions .open{background:#0b5b49;color:#fff}.alert-actions .del{background:#c0392b;color:#fff}.alert-empty{text-align:center;padding:18px;border:1px solid #d7e8e0;border-radius:14px;background:#f8fbfa;color:#62746d}@media(max-width:800px){.alert-card{grid-template-columns:1fr}.alert-actions{justify-content:flex-start}}</style><div class="smart-alerts-v209"><div class="smart-alert-head"><b>التنبيهات الذكية</b><div class="counts"><span class="pill-a pill-danger">حرجة: ${danger}</span><span class="pill-a pill-warn">تنبيه: ${warn}</span><span class="pill-a">الإجمالي: ${alerts.length}</span><button type="button" onclick="clearDismissedAlertsV209()">إظهار المحذوف</button></div></div>${[...groups.entries()].map(([name,items])=>`<section class="alert-group"><div class="alert-group-title"><span>${E(name)}</span><span>${items.length}</span></div>${items.map(a=>`<div class="alert-card ${E(a.level)}"><div><span class="pill-a ${a.level==='danger'?'pill-danger':'pill-warn'}">${a.level==='danger'?'حرج':'تنبيه'}</span></div><div><div class="alert-title">${E(a.title)}</div><div class="alert-text">${E(a.text)}</div></div><div class="alert-actions">${a.action?`<button type="button" class="open" onclick="${E(a.action)}">فتح</button>`:''}<button type="button" class="del" onclick="dismissSmartAlertV209('${E(a.id)}')">حذف</button></div></div>`).join('')}</section>`).join('')}</div>`;
  }
  window.renderAlerts = renderSmartAlertsV209;
  window.renderSmartAlertsV209 = renderSmartAlertsV209;

  function removeTotalConsumptionLeftovers(){
    const bad = ['إجمالي الاستهلاك','اجمالي الاستهلاك','إجمالي المستهلك','اجمالي المستهلك'];
    document.querySelectorAll('small,label,span,div,th,td').forEach(el=>{
      const txt = S(el.textContent);
      if(bad.some(x=>txt.includes(x))){
        const card = el.closest('.v180-stats > div,.stat-card,.metric-card,.info-box,.finance-card,.item-stat,.product-stat,td,th');
        if(card && card !== document.body) card.remove();
      }
    });
  }

  function bootV209(){ patchTicketPdfButtons(); renderSmartAlertsV209(); removeTotalConsumptionLeftovers(); }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(bootV209, ev==='load'?1200:300)));
  setTimeout(bootV209,500); setTimeout(bootV209,2000); setInterval(bootV209,2500);
  console.log('Tasneef V209 real PDF + smart alerts loaded');
})();

/* ===================== V210: CLEAN TICKETS PDF + WEIGHTED COST BATCHES =====================
   - PDF التكتات بتصميم أنظف وتقسيم صفحات حقيقي بدون صفحة بيضاء.
   - دعم إدخال نفس الصنف بأكثر من سعر في نفس الفاتورة كسطور منفصلة.
   - تحديث كرت الصنف بسعر متوسط تكلفة مرجح عند اختلاف أسعار الشراء.
=============================================================================== */
(function(){
  'use strict';
  window.TASNEEF_BUILD = 'V210_CLEAN_PDF_WEIGHTED_COST_2026_05_20';
  const $ = id => document.getElementById(id);
  const A = v => Array.isArray(v) ? v : [];
  const S = v => String(v ?? '').trim();
  const N = v => { const x = Number(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const say = (t,type) => { try{ if(typeof msg === 'function') msg(t,type); else alert(t); }catch(_){ alert(t); } };
  const todayIso = () => new Date().toISOString().slice(0,10);
  const money = v => N(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
  const qty = v => N(v).toLocaleString('en-US',{maximumFractionDigits:2});
  const dateOnly = v => S(v).slice(0,10) || '-';
  const pName = id => { try{ return typeof projectName === 'function' ? projectName(id) : '-'; }catch(_){ return '-'; } };
  const sName = id => { try{ return typeof supervisorName === 'function' ? supervisorName(id) : '-'; }catch(_){ return '-'; } };
  const userName = id => {
    const u=A(window.data?.users || window.data?.appUsers).find(x=>S(x.id)===S(id)||S(x.username)===S(id));
    return u ? S(u.full_name||u.name||u.username) : (S(id)||'-');
  };
  const ticketNo = t => S(t?.ticket_number) || ('T-' + S(t?.id||0).padStart(4,'0'));
  const statusText = s => S(s)==='closed'?'مغلق':(S(s)==='processing'?'تحت المعالجة':'مفتوح');
  const priorityText = p => S(p)==='urgent'?'عاجل':(S(p)==='high'?'مهم':(S(p)==='low'?'منخفض':'عادي'));
  const raisedBy = t => S(t?.created_by_name || t?.created_by_full_name || t?.raised_by_name || t?.created_user_name || t?.creator_name || t?.created_by_username) || userName(t?.created_by || t?.raised_by || t?.user_id || t?.created_user_id || t?.created_by_id);

  function currentTicketsV210(){
    let list=A(window.data?.tickets).slice();
    const status=S($('ticketFilterStatus')?.value || $('supTicketFilterStatus')?.value || $('ticketStatusFilter')?.value || '');
    const project=S($('ticketFilterProject')?.value || $('supTicketFilterProject')?.value || '');
    const supervisor=S($('ticketFilterSupervisor')?.value || $('supTicketFilterSupervisor')?.value || '');
    const search=S($('ticketSearch')?.value || $('supTicketSearch')?.value || '').toLowerCase();
    const from=S($('ticketFilterFrom')?.value || $('supTicketFilterFrom')?.value || '');
    const to=S($('ticketFilterTo')?.value || $('supTicketFilterTo')?.value || '');
    if(status) list=list.filter(t=>S(t.status||'open')===status);
    if(project) list=list.filter(t=>S(t.project_id)===project);
    if(supervisor) list=list.filter(t=>S(t.supervisor_id)===supervisor);
    if(from) list=list.filter(t=>dateOnly(t.created_at)>=from);
    if(to) list=list.filter(t=>dateOnly(t.created_at)<=to);
    if(search) list=list.filter(t=>[ticketNo(t),t.title,t.description,pName(t.project_id),sName(t.supervisor_id),raisedBy(t),statusText(t.status),priorityText(t.priority)].join(' ').toLowerCase().includes(search));
    return list.sort((a,b)=>S(sName(a.supervisor_id)).localeCompare(S(sName(b.supervisor_id)),'ar') || new Date(b.created_at||0)-new Date(a.created_at||0));
  }

  function buildCleanTicketsNodeV210(list,title){
    const groups=new Map();
    list.forEach(t=>{ const k=S(t.supervisor_id)||'none'; if(!groups.has(k)) groups.set(k,{name:S(sName(t.supervisor_id))||'بدون مشرف',rows:[]}); groups.get(k).rows.push(t); });
    const node=document.createElement('div');
    node.dir='rtl';
    node.className='tasneef-ticket-pdf-v210';
    node.innerHTML=`
      <style>
        .tasneef-ticket-pdf-v210{width:1240px;background:#fff;color:#173d32;font-family:Tahoma,Arial,sans-serif;direction:rtl;box-sizing:border-box;padding:26px;line-height:1.55}
        .tasneef-ticket-pdf-v210 *{box-sizing:border-box}
        .pdf-head{display:flex;justify-content:space-between;align-items:center;border-bottom:5px solid #0b5b49;padding-bottom:16px;margin-bottom:16px}
        .pdf-title h1{margin:0;color:#0b5b49;font-size:30px;font-weight:900}.pdf-title p{margin:6px 0 0;color:#637b72;font-size:13px}.pdf-brand{text-align:left;color:#0b5b49;font-weight:900;font-size:18px}.pdf-brand small{display:block;color:#6b8078;font-size:12px;font-weight:700}
        .pdf-stats{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:14px 0 20px}.pdf-box{border:1px solid #d3e4dd;background:#f4faf7;border-radius:14px;padding:12px}.pdf-box small{display:block;color:#6b8078;margin-bottom:5px}.pdf-box b{font-size:22px;color:#0b5b49}
        .pdf-section{break-inside:avoid;page-break-inside:avoid;border:1px solid #cbded6;border-radius:15px;overflow:hidden;margin:16px 0 22px;background:#fff}.pdf-section-title{background:#0b5b49;color:#fff;padding:11px 14px;font-weight:900;font-size:16px;display:flex;justify-content:space-between}.pdf-section-title span:last-child{font-size:13px;opacity:.95}
        .pdf-table{width:100%;border-collapse:collapse;font-size:11.5px}.pdf-table th{background:#edf5f2;color:#0b5b49;font-weight:900}.pdf-table th,.pdf-table td{border:1px solid #d7e3df;padding:8px;text-align:right;vertical-align:top}.pdf-table tr:nth-child(even) td{background:#f8fbfa}.pdf-desc{white-space:pre-wrap;line-height:1.45;max-width:300px}.pdf-pill{display:inline-block;border-radius:999px;padding:3px 8px;font-weight:800;background:#eef7f3;color:#0b5b49}.pdf-footer{text-align:center;border-top:1px solid #d8e5e0;margin-top:18px;padding-top:9px;color:#70847c;font-size:12px}.pdf-empty{border:1px dashed #c7d9d2;border-radius:14px;text-align:center;padding:24px;color:#667c73;background:#fbfdfc}
      </style>
      <div class="pdf-head"><div class="pdf-title"><h1>${E(title)}</h1><p>مرتب حسب المشرف — حسب الفلاتر الحالية — ${E(new Date().toLocaleString('ar-SA'))}</p></div><div class="pdf-brand">شركة تصنيف لإدارة المرافق<small>تقرير تكتات التشغيل</small></div></div>
      <div class="pdf-stats"><div class="pdf-box"><small>إجمالي التكتات</small><b>${list.length}</b></div><div class="pdf-box"><small>مفتوح</small><b>${list.filter(t=>S(t.status||'open')==='open').length}</b></div><div class="pdf-box"><small>تحت المعالجة</small><b>${list.filter(t=>S(t.status)==='processing').length}</b></div><div class="pdf-box"><small>مغلق</small><b>${list.filter(t=>S(t.status)==='closed').length}</b></div></div>
      ${[...groups.values()].map(g=>`<section class="pdf-section"><div class="pdf-section-title"><span>المشرف: ${E(g.name)}</span><span>عدد التكتات: ${g.rows.length}</span></div><table class="pdf-table"><thead><tr><th style="width:34px">#</th><th>رقم التكت</th><th>المشروع</th><th>العنوان والوصف</th><th>الحالة</th><th>الأولوية</th><th>رفع بواسطة</th><th>الاستلام والإغلاق</th><th>التاريخ</th></tr></thead><tbody>${g.rows.map((t,i)=>`<tr><td>${i+1}</td><td><b>${E(ticketNo(t))}</b></td><td>${E(pName(t.project_id))}</td><td class="pdf-desc"><b>${E(t.title||'-')}</b><br>${E(t.description||'-')}</td><td><span class="pdf-pill">${E(statusText(t.status))}</span></td><td>${E(priorityText(t.priority))}</td><td>${E(raisedBy(t))}</td><td>استلم: ${E(t.claimed_by_name||userName(t.claimed_by)||'-')}<br>أغلق: ${E(t.closed_by_name||userName(t.closed_by)||'-')}${t.closure_note?`<br>الحل: ${E(t.closure_note)}`:''}</td><td>${E(dateOnly(t.created_at))}</td></tr>`).join('')}</tbody></table></section>`).join('') || '<div class="pdf-empty">لا توجد تكتات مطابقة للفلاتر الحالية</div>'}
      <div class="pdf-footer">تم إنشاء التقرير آليًا من نظام تصنيف</div>`;
    return node;
  }

  async function loadScriptV210(src,id){
    if(window[id]) return;
    if(document.getElementById(id)) return new Promise((res,rej)=>{ const s=document.getElementById(id); if(s.dataset.loaded==='1') res(); else {s.addEventListener('load',res,{once:true});s.addEventListener('error',rej,{once:true});} });
    return new Promise((res,rej)=>{ const s=document.createElement('script'); s.id=id; s.src=src; s.async=true; s.onload=()=>{s.dataset.loaded='1';res();}; s.onerror=rej; document.head.appendChild(s); });
  }

  async function downloadNodePdfV210(node,filename){
    await loadScriptV210('https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js','html2canvas');
    await loadScriptV210('https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js','jspdf');
    if(!window.html2canvas || !window.jspdf?.jsPDF) throw new Error('تعذر تحميل مكتبات PDF');
    const holder=document.createElement('div');
    holder.style.cssText='position:absolute;left:0;top:0;width:1240px;background:#fff;z-index:-1;opacity:1;pointer-events:none;';
    holder.appendChild(node);
    document.body.appendChild(holder);
    await new Promise(r=>setTimeout(r,450));
    const canvas=await window.html2canvas(node,{backgroundColor:'#ffffff',scale:2,useCORS:true,logging:false,windowWidth:1240,scrollX:0,scrollY:0});
    holder.remove();
    if(canvas.width<100 || canvas.height<100) throw new Error('تعذر تكوين صورة التقرير');
    const {jsPDF}=window.jspdf;
    const pdf=new jsPDF({orientation:'landscape',unit:'mm',format:'a4'});
    const pageW=pdf.internal.pageSize.getWidth();
    const pageH=pdf.internal.pageSize.getHeight();
    const margin=5;
    const usableW=pageW-(margin*2);
    const usableH=pageH-(margin*2);
    const pxPerMm=canvas.width/usableW;
    const pagePxH=Math.floor(usableH*pxPerMm);
    let y=0, page=0;
    while(y<canvas.height){
      const sliceH=Math.min(pagePxH, canvas.height-y);
      const pageCanvas=document.createElement('canvas');
      pageCanvas.width=canvas.width; pageCanvas.height=sliceH;
      const ctx=pageCanvas.getContext('2d');
      ctx.fillStyle='#fff'; ctx.fillRect(0,0,pageCanvas.width,pageCanvas.height);
      ctx.drawImage(canvas,0,y,canvas.width,sliceH,0,0,canvas.width,sliceH);
      if(page>0) pdf.addPage();
      const imgData=pageCanvas.toDataURL('image/jpeg',0.96);
      const imgH=sliceH/pxPerMm;
      pdf.addImage(imgData,'JPEG',margin,margin,usableW,imgH);
      y+=sliceH; page++;
    }
    pdf.save(filename);
  }

  function openCleanPrintV210(list,title){
    const node=buildCleanTicketsNodeV210(list,title);
    const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${E(title)}</title><style>@page{size:A4 landscape;margin:8mm}body{margin:0;background:#fff}.print-actions{position:fixed;left:10px;top:10px;z-index:9999}.print-actions button{background:#0b5b49;color:#fff;border:0;border-radius:10px;padding:10px 14px;font-weight:800}@media print{.print-actions{display:none}.tasneef-ticket-pdf-v210{width:auto!important;padding:0!important}}</style></head><body><div class="print-actions"><button onclick="window.print()">طباعة / حفظ PDF</button></div>${node.outerHTML}</body></html>`;
    const w=window.open('','_blank');
    if(!w) return say('المتصفح منع فتح التقرير للطباعة','err');
    w.document.open(); w.document.write(html); w.document.close(); w.focus();
  }

  async function ticketsDownloadPdfV210(){
    const list=currentTicketsV210();
    if(!list.length) return say('لا توجد تكتات مطابقة للفلاتر الحالية','err');
    const node=buildCleanTicketsNodeV210(list,'تقرير التكتات حسب المشرف');
    try{
      await downloadNodePdfV210(node,`تقرير-التكتات-حسب-المشرف-${todayIso()}.pdf`);
      say('تم تنزيل PDF في التنزيلات');
    }catch(e){
      console.warn('V210 PDF failed, print fallback',e);
      say('تعذر التنزيل المباشر، فتحت لك تقرير مرتب للطباعة والحفظ PDF','err');
      openCleanPrintV210(list,'تقرير التكتات حسب المشرف');
    }
  }
  async function ticketDownloadPdfV210(id){
    const t=A(window.data?.tickets).find(x=>S(x.id)===S(id));
    if(!t) return say('التكت غير موجود','err');
    const node=buildCleanTicketsNodeV210([t],`تقرير التكت ${ticketNo(t)}`);
    try{ await downloadNodePdfV210(node,`تكت-${ticketNo(t)}.pdf`); say('تم تنزيل PDF في التنزيلات'); }
    catch(e){ openCleanPrintV210([t],`تقرير التكت ${ticketNo(t)}`); }
  }
  window.ticketsDownloadPdfV210=ticketsDownloadPdfV210;
  window.ticketsDownloadPdfV209=ticketsDownloadPdfV210;
  window.ticketsDownloadPdfV208=ticketsDownloadPdfV210;
  window.ticketsDownloadPdfV207=ticketsDownloadPdfV210;
  window.ticketsDownloadPdfV206=ticketsDownloadPdfV210;
  window.ticketDownloadPdfV210=ticketDownloadPdfV210;
  window.ticketDownloadPdfV209=ticketDownloadPdfV210;
  window.ticketDownloadPdfV208=ticketDownloadPdfV210;
  window.ticketDownloadPdfV207=ticketDownloadPdfV210;
  window.ticketDownloadPdfV206=ticketDownloadPdfV210;

  function itemCode(it){ return S(it?.product_code||it?.serial_number||it?.barcode||it?.supplier_barcode); }
  function findItemByCodeV210(code){ code=S(code); return A(window.data?.inventoryItems).find(i=>[i.product_code,i.serial_number,i.barcode,i.supplier_barcode].map(S).includes(code)); }
  function vatCalcV210(price,mode){ price=N(price); if(mode==='inclusive'){ const net=price/1.15; return {net,vat:price-net,gross:price}; } return {net:price,vat:price*.15,gross:price*1.15}; }
  function weightedCost(oldQty,oldCost,inQty,inCost){ oldQty=Math.max(0,N(oldQty)); inQty=Math.max(0,N(inQty)); if(inQty<=0) return N(oldCost); if(oldQty<=0) return N(inCost); return ((oldQty*N(oldCost))+(inQty*N(inCost)))/(oldQty+inQty); }
  async function batchTablesExist(){ if(!window.sb) return false; const r=await sb.from('inventory_batches').select('id').limit(1); return !r.error; }

  function overrideBatchAddLineV210(){
    if(!window.stockBatchAddLineV148 || window.stockBatchAddLineV148.v210Wrapped) return;
    window.stockBatchAddLineV148=function(){
      const code=S($('batchProductCodeV148')?.value); const name=S($('batchProductNameV148')?.value); const q=N($('batchQtyV148')?.value); const price=N($('batchUnitPriceV148')?.value); const unit=S($('batchUnitV148')?.value)||'حبة';
      if(!code) return say('كود المنتج مطلوب','err'); if(!name) return say('اسم الصنف مطلوب','err'); if(q<=0) return say('الكمية مطلوبة','err'); if(price<0) return say('سعر الوحدة غير صحيح','err');
      const arr=A(window.batchLinesV148);
      const existing=arr.find(l=>S(l.product_code)===code && Math.abs(N(l.unit_price)-price)<0.0001 && S(l.unit||'حبة')===unit);
      if(existing){ existing.quantity=N(existing.quantity)+q; existing.image_url=existing.image_url || (window.pendingLineImageV148||''); }
      else arr.push({product_code:code,item_name:name,category:S($('batchCategoryV148')?.value)||'أخرى',item_type:S($('batchItemTypeV148')?.value)||'مادة',unit,quantity:q,unit_price:price,image_url:window.pendingLineImageV148||''});
      window.batchLinesV148=arr;
      if(typeof stockBatchClearLineV148==='function') stockBatchClearLineV148();
      if(typeof stockBatchRenderLinesV148==='function') stockBatchRenderLinesV148();
      say('تمت إضافة السطر. إذا كان نفس الصنف بسعر مختلف سيبقى كسطر مستقل.');
    };
    window.stockBatchAddLineV148.v210Wrapped=true;
  }

  function overrideBatchSaveV210(){
    if(!window.stockBatchSaveV148 || window.stockBatchSaveV148.v210Wrapped) return;
    window.stockBatchSaveV148=async function(btn){
      try{
        if(btn) btn.disabled=true;
        const lines=A(window.batchLinesV148);
        if(!lines.length) throw new Error('أضف صنفًا واحدًا على الأقل داخل الفاتورة');
        const supplier=S($('batchSupplierV148')?.value); if(!supplier) throw new Error('اسم المورد مطلوب');
        const invoiceNo=S($('batchInvoiceV148')?.value) || ('ADD-'+Date.now());
        const date=S($('batchDateV148')?.value)||todayIso(); const mode=S($('batchVatModeV148')?.value)||'exclusive';
        let netTotal=0,vatTotal=0,grossTotal=0; const savedLines=[];
        for(const l of lines){
          const q=N(l.quantity); if(q<=0) continue;
          let item=findItemByCodeV210(l.product_code);
          const calc=vatCalcV210(l.unit_price,mode); const lineNet=calc.net*q, lineVat=calc.vat*q, lineGross=calc.gross*q;
          netTotal+=lineNet; vatTotal+=lineVat; grossTotal+=lineGross;
          if(item){
            const oldQty=N(item.quantity); const oldCost=N(item.unit_cost||item.unit_price); const newQty=oldQty+q; const newCost=weightedCost(oldQty,oldCost,q,calc.net);
            const upd={quantity:newQty,unit_cost:newCost,last_purchase_cost:calc.net,supplier,category:l.category||item.category,unit:l.unit||item.unit,product_code:l.product_code,serial_number:l.product_code,barcode:l.product_code,supplier_barcode:l.product_code,item_type:l.item_type||item.item_type,type:l.item_type||item.type};
            let res=await sb.from('inventory_items').update(upd).eq('id',item.id).select('*').single();
            if(res.error){ delete upd.last_purchase_cost; res=await sb.from('inventory_items').update(upd).eq('id',item.id).select('*').single(); }
            if(res.error) throw res.error; item=res.data;
          }else{
            const ins={name:l.item_name,serial_number:l.product_code,product_code:l.product_code,barcode:l.product_code,supplier_barcode:l.product_code,image_url:l.image_url||'',category:l.category||'أخرى',item_type:l.item_type||'مادة',type:l.item_type||'مادة',unit:l.unit||'حبة',quantity:q,min_quantity:0,unit_cost:calc.net,supplier,notes:'تم إنشاؤه من فاتورة إدخال '+invoiceNo};
            const res=await sb.from('inventory_items').insert(ins).select('*').single(); if(res.error) throw res.error; item=res.data;
          }
          const mv={item_id:item.id,item_name:item.name,movement_type:'in',quantity:q,movement_date:date,project_id:null,project_name:'',receiver:supplier,reason:'إدخال مخزون - فاتورة '+invoiceNo,notes:'batch:'+invoiceNo+' | سعر الدفعة: '+money(calc.net),product_code:l.product_code,barcode:l.product_code,unit_cost:calc.net,unit_price:calc.net,batch_unit_cost:calc.net};
          let mres=await sb.from('inventory_movements').insert(mv).select('*').single();
          if(mres.error){ delete mv.unit_price; delete mv.batch_unit_cost; mres=await sb.from('inventory_movements').insert(mv).select('*').single(); }
          if(mres.error) throw mres.error;
          savedLines.push({...l,item_id:item.id,unit_cost:calc.net,unit_vat:calc.vat,unit_gross:calc.gross,line_net:lineNet,line_vat:lineVat,line_gross:lineGross,movement_id:mres.data.id});
        }
        if(await batchTablesExist()){
          const bres=await sb.from('inventory_batches').insert({batch_date:date,supplier,invoice_no:invoiceNo,vat_mode:mode,total_before_vat:netTotal,total_vat:vatTotal,total_with_vat:grossTotal,notes:'فاتورة إدخال مخزون'}).select('*').single();
          if(bres.error) throw bres.error;
          const rows=savedLines.map(l=>({batch_id:bres.data.id,item_id:l.item_id,product_code:l.product_code,item_name:l.item_name,category:l.category,item_type:l.item_type,unit:l.unit,quantity:l.quantity,unit_price_before_vat:l.unit_cost,unit_vat:l.unit_vat,unit_price_with_vat:l.unit_gross,total_before_vat:l.line_net,total_vat:l.line_vat,total_with_vat:l.line_gross,movement_id:l.movement_id}));
          const li=await sb.from('inventory_batch_items').insert(rows); if(li.error) throw li.error;
        }
        say('تم حفظ الفاتورة وتحديث سعر الصنف بمتوسط تكلفة مرجح');
        if(typeof stockBatchPrintV148==='function') stockBatchPrintV148({invoice_no:invoiceNo,batch_date:date,supplier,vat_mode:mode,lines:savedLines,total_before_vat:netTotal,total_vat:vatTotal,total_with_vat:grossTotal});
        if(typeof stockBatchClearV148==='function') stockBatchClearV148();
        if(typeof financeLoadAll==='function') await financeLoadAll();
        if(typeof stockBatchLoadV148==='function') await stockBatchLoadV148(true);
      }catch(e){ say(e.message||String(e),'err'); }
      finally{ if(btn) btn.disabled=false; }
    };
    window.stockBatchSaveV148.v210Wrapped=true;
  }

  function cleanupProductCardsV210(){
    document.querySelectorAll('.inventory-item-card,.stock-card,.v129-card,.v178-product-card,article').forEach(card=>{
      if(!/سعر الحبة/.test(card.textContent||'')) return;
      card.querySelectorAll('small').forEach(s=>{ if(S(s.textContent)==='سعر الحبة') s.textContent='متوسط سعر التكلفة'; });
    });
    document.querySelectorAll('#inventoryProductModal small, .modal small').forEach(s=>{ if(S(s.textContent)==='سعر الحبة') s.textContent='متوسط سعر التكلفة'; });
  }

  function patchPdfButtonsV210(){
    document.querySelectorAll('#ticketPdfBtnV206,#supTicketPdfBtnV206,.ticket-pdf-v206,.ticket-pdf-v207,.ticket-pdf-v208,[data-ticket-pdf]').forEach(b=>{ b.textContent='تنزيل PDF'; b.onclick=ticketsDownloadPdfV210; });
    document.querySelectorAll('button').forEach(b=>{ if(/تنزيل PDF|تصدير PDF/.test(S(b.textContent)) && (b.closest('#ticketsPage')||b.closest('#supervisorTicketsPage')||b.closest('[id*="ticket" i]'))) b.onclick=ticketsDownloadPdfV210; });
  }
  function bootV210(){ overrideBatchAddLineV210(); overrideBatchSaveV210(); cleanupProductCardsV210(); patchPdfButtonsV210(); }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(bootV210,ev==='load'?1400:400)));
  setTimeout(bootV210,700); setTimeout(bootV210,2500); setInterval(bootV210,2500);
  console.log('Tasneef V210 clean PDF + weighted cost loaded');
})();

/* ===================== V211: SMART TICKET PDF WINDOW =====================
   - زر تنزيل PDF يفتح نافذة ذكية داخل النظام.
   - النافذة تعرض التقرير قبل التنفيذ.
   - يوجد زر طباعة / حفظ PDF وزر تنزيل PDF مباشر إن أمكن.
============================================================================ */
(function(){
  'use strict';
  window.TASNEEF_BUILD = 'V211_SMART_TICKET_PDF_WINDOW_2026_05_20';
  const S = v => String(v ?? '').trim();
  const A = v => Array.isArray(v) ? v : [];
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const $ = id => document.getElementById(id);
  const say = (t,type) => { try{ if(typeof msg === 'function') msg(t,type); else alert(t); }catch(_){ alert(t); } };
  const todayIso = () => new Date().toISOString().slice(0,10);
  const dateOnly = v => S(v).slice(0,10) || '-';
  const userName = id => {
    const u = A(window.data?.users || window.data?.appUsers).find(x => S(x.id)===S(id) || S(x.username)===S(id));
    return u ? S(u.full_name || u.name || u.username) : (S(id) || '-');
  };
  const projectText = id => { try{ return typeof projectName === 'function' ? projectName(id) : '-'; }catch(_){ return '-'; } };
  const supervisorText = id => { try{ return typeof supervisorName === 'function' ? supervisorName(id) : '-'; }catch(_){ return '-'; } };
  const ticketNo = t => S(t?.ticket_number) || ('T-' + S(t?.id || 0).padStart(4,'0'));
  const statusText = s => S(s)==='closed'?'مغلق':(S(s)==='processing'?'تحت المعالجة':'مفتوح');
  const priorityText = p => S(p)==='urgent'?'عاجل':(S(p)==='high'?'مهم':(S(p)==='low'?'منخفض':'عادي'));
  const raisedBy = t => S(t?.created_by_name || t?.created_by_full_name || t?.raised_by_name || t?.created_user_name || t?.creator_name || t?.created_by_username) || userName(t?.created_by || t?.raised_by || t?.user_id || t?.created_user_id || t?.created_by_id);

  function listByCurrentFiltersV211(){
    let list = A(window.data?.tickets).slice();
    const ids = name => S($(name)?.value || '');
    const status = ids('ticketFilterStatus') || ids('supTicketFilterStatus') || ids('ticketStatusFilter') || ids('ticketsStatusFilter');
    const project = ids('ticketFilterProject') || ids('supTicketFilterProject') || ids('ticketsProjectFilter');
    const supervisor = ids('ticketFilterSupervisor') || ids('supTicketFilterSupervisor') || ids('ticketsSupervisorFilter');
    const search = S($('ticketSearch')?.value || $('supTicketSearch')?.value || $('ticketsSearch')?.value || '').toLowerCase();
    const from = ids('ticketFilterFrom') || ids('supTicketFilterFrom') || ids('ticketsFromFilter');
    const to = ids('ticketFilterTo') || ids('supTicketFilterTo') || ids('ticketsToFilter');
    if(status) list = list.filter(t => S(t.status || 'open') === status);
    if(project) list = list.filter(t => S(t.project_id) === project);
    if(supervisor) list = list.filter(t => S(t.supervisor_id) === supervisor);
    if(from) list = list.filter(t => dateOnly(t.created_at) >= from);
    if(to) list = list.filter(t => dateOnly(t.created_at) <= to);
    if(search){
      list = list.filter(t => [ticketNo(t), t.title, t.description, projectText(t.project_id), supervisorText(t.supervisor_id), raisedBy(t), statusText(t.status), priorityText(t.priority)].join(' ').toLowerCase().includes(search));
    }
    return list.sort((a,b)=> S(supervisorText(a.supervisor_id)).localeCompare(S(supervisorText(b.supervisor_id)),'ar') || new Date(b.created_at||0) - new Date(a.created_at||0));
  }

  function filtersLabelV211(){
    const parts=[];
    const val = id => S($(id)?.value || '');
    const status = val('ticketFilterStatus') || val('supTicketFilterStatus') || val('ticketStatusFilter') || val('ticketsStatusFilter');
    const project = val('ticketFilterProject') || val('supTicketFilterProject') || val('ticketsProjectFilter');
    const supervisor = val('ticketFilterSupervisor') || val('supTicketFilterSupervisor') || val('ticketsSupervisorFilter');
    const from = val('ticketFilterFrom') || val('supTicketFilterFrom') || val('ticketsFromFilter');
    const to = val('ticketFilterTo') || val('supTicketFilterTo') || val('ticketsToFilter');
    if(status) parts.push('الحالة: '+statusText(status));
    if(project) parts.push('المشروع: '+projectText(project));
    if(supervisor) parts.push('المشرف: '+supervisorText(supervisor));
    if(from) parts.push('من: '+from);
    if(to) parts.push('إلى: '+to);
    return parts.length ? parts.join(' | ') : 'جميع التكتات حسب الفلاتر الحالية';
  }

  function reportBodyHtmlV211(list, title){
    const groups = new Map();
    list.forEach(t => {
      const key = S(t.supervisor_id) || 'none';
      if(!groups.has(key)) groups.set(key,{name:S(supervisorText(t.supervisor_id)) || 'بدون مشرف', rows:[]});
      groups.get(key).rows.push(t);
    });
    const sections = [...groups.values()].map(g => `
      <section class="v211-section">
        <div class="v211-section-title"><span>المشرف: ${E(g.name)}</span><span>${g.rows.length} تكت</span></div>
        <table class="v211-table">
          <thead><tr><th>#</th><th>رقم التكت</th><th>المشروع</th><th>العنوان / الوصف</th><th>الحالة</th><th>الأولوية</th><th>رفع بواسطة</th><th>استلام / إغلاق</th><th>التاريخ</th></tr></thead>
          <tbody>${g.rows.map((t,i)=>`
            <tr>
              <td>${i+1}</td>
              <td><b>${E(ticketNo(t))}</b></td>
              <td>${E(projectText(t.project_id))}</td>
              <td class="v211-desc"><b>${E(t.title || '-')}</b><br>${E(t.description || '-')}</td>
              <td><span class="v211-pill">${E(statusText(t.status))}</span></td>
              <td>${E(priorityText(t.priority))}</td>
              <td>${E(raisedBy(t))}</td>
              <td>استلم: ${E(t.claimed_by_name || userName(t.claimed_by) || '-')}<br>أغلق: ${E(t.closed_by_name || userName(t.closed_by) || '-')}<br>${t.closure_note ? 'الحل: '+E(t.closure_note) : ''}</td>
              <td>${E(dateOnly(t.created_at))}</td>
            </tr>`).join('')}</tbody>
        </table>
      </section>`).join('') || '<div class="v211-empty">لا توجد تكتات مطابقة للفلاتر الحالية</div>';
    return `
      <div class="v211-report" dir="rtl">
        <div class="v211-head">
          <div class="v211-brand"><img src="tasneef_logo_print.png" onerror="this.style.display='none'"><div><h1>${E(title)}</h1><p>${E(filtersLabelV211())}</p><p>تاريخ التصدير: ${E(new Date().toLocaleString('ar-SA'))}</p></div></div>
          <div class="v211-company">شركة تصنيف لإدارة المرافق</div>
        </div>
        <div class="v211-kpis">
          <div><small>إجمالي التكتات</small><b>${list.length}</b></div>
          <div><small>مفتوح</small><b>${list.filter(t=>S(t.status||'open')==='open').length}</b></div>
          <div><small>تحت المعالجة</small><b>${list.filter(t=>S(t.status)==='processing').length}</b></div>
          <div><small>مغلق</small><b>${list.filter(t=>S(t.status)==='closed').length}</b></div>
        </div>
        ${sections}
        <div class="v211-footer">تم إنشاء التقرير آليًا من نظام تصنيف — يعرض التكتات حسب الفلاتر المختارة</div>
      </div>`;
  }

  function reportStyleV211(){
    return `
      <style>
        @page{size:A4 landscape;margin:8mm}
        *{box-sizing:border-box} html,body{margin:0;background:#f3f6f5;color:#153d32;font-family:Tahoma,Arial,sans-serif;direction:rtl;-webkit-print-color-adjust:exact;print-color-adjust:exact}
        .v211-report{background:#fff;width:1320px;max-width:100%;margin:0 auto;padding:22px;color:#153d32}
        .v211-head{display:flex;justify-content:space-between;align-items:center;border-bottom:4px solid #0b5b49;padding-bottom:14px;margin-bottom:14px}
        .v211-brand{display:flex;align-items:center;gap:12px}.v211-brand img{width:72px;height:72px;border:1px solid #d7e6e1;border-radius:16px;object-fit:contain;padding:7px;background:#fff}.v211-brand h1{margin:0 0 5px;color:#0b5b49;font-size:28px}.v211-brand p{margin:3px 0;color:#647970;font-size:13px}.v211-company{font-weight:900;color:#0b5b49;font-size:18px;text-align:left}
        .v211-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:12px 0 16px}.v211-kpis div{border:1px solid #cfe2da;background:#f3faf7;border-radius:14px;padding:10px}.v211-kpis small{display:block;color:#657972;margin-bottom:5px}.v211-kpis b{font-size:22px;color:#062f27}
        .v211-section{break-inside:avoid;page-break-inside:avoid;border:1px solid #cadbd4;border-radius:13px;overflow:hidden;margin:14px 0}.v211-section-title{background:#2779b7;color:#fff;padding:10px 13px;font-weight:900;display:flex;justify-content:space-between;font-size:16px}
        .v211-table{width:100%;border-collapse:collapse;font-size:11px}.v211-table th,.v211-table td{border:1px solid #d6e2df;padding:7px;text-align:right;vertical-align:top}.v211-table th{background:#eaf2f6;color:#143d32}.v211-table tr:nth-child(even) td{background:#f7fafb}.v211-desc{white-space:pre-wrap;line-height:1.45;max-width:330px}.v211-pill{background:#eaf7f1;border:1px solid #cbe3d7;border-radius:999px;padding:3px 8px;font-weight:800;display:inline-block}.v211-empty{border:1px dashed #cbded6;border-radius:13px;padding:26px;text-align:center;color:#647970;background:#fff}.v211-footer{text-align:center;border-top:1px solid #d6e2df;margin-top:16px;padding-top:9px;color:#73847e;font-size:12px}
        @media print{html,body{background:#fff}.v211-report{width:auto;padding:0}.v211-section{page-break-inside:avoid}}
      </style>`;
  }

  function fullReportHtmlV211(list, title){
    return `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${E(title)}</title>${reportStyleV211()}</head><body>${reportBodyHtmlV211(list,title)}</body></html>`;
  }

  function ensureModalV211(){
    let modal = document.getElementById('ticketPdfSmartModalV211');
    if(modal) return modal;
    const style = document.createElement('style');
    style.id='ticketPdfSmartModalCssV211';
    style.textContent = `
      #ticketPdfSmartModalV211{position:fixed;inset:0;background:rgba(0,0,0,.55);z-index:2147483646;display:none;align-items:center;justify-content:center;padding:18px;direction:rtl}
      #ticketPdfSmartModalV211 .v211-modal-box{background:#fff;width:min(1200px,96vw);height:min(780px,94vh);border-radius:22px;box-shadow:0 24px 70px rgba(0,0,0,.28);display:flex;flex-direction:column;overflow:hidden;border:1px solid #d6e5df}
      #ticketPdfSmartModalV211 .v211-modal-head{background:#07563d;color:#fff;padding:14px 18px;display:flex;justify-content:space-between;align-items:center;gap:12px}
      #ticketPdfSmartModalV211 .v211-modal-head h3{margin:0;font-size:20px}#ticketPdfSmartModalV211 .v211-modal-head p{margin:4px 0 0;color:#d7eee5;font-size:12px}
      #ticketPdfSmartModalV211 .v211-modal-actions{display:flex;gap:8px;flex-wrap:wrap}#ticketPdfSmartModalV211 button{border:0;border-radius:12px;padding:10px 14px;font-weight:900;cursor:pointer}#ticketPdfSmartModalV211 .primary{background:#fff;color:#07563d}#ticketPdfSmartModalV211 .gold{background:#c7a24d;color:#fff}#ticketPdfSmartModalV211 .danger{background:#eaf4f0;color:#07563d}
      #ticketPdfSmartModalV211 iframe{border:0;flex:1;background:#fff;width:100%}
    `;
    document.head.appendChild(style);
    modal = document.createElement('div');
    modal.id='ticketPdfSmartModalV211';
    modal.innerHTML = `<div class="v211-modal-box"><div class="v211-modal-head"><div><h3>تقرير التكتات PDF</h3><p>راجع التقرير ثم اختر الطباعة أو التنزيل</p></div><div class="v211-modal-actions"><button class="primary" id="v211PrintBtn">طباعة / حفظ PDF</button><button class="gold" id="v211DownloadBtn">تنزيل PDF</button><button class="danger" id="v211CloseBtn">إغلاق</button></div></div><iframe id="v211PreviewFrame"></iframe></div>`;
    document.body.appendChild(modal);
    modal.querySelector('#v211CloseBtn').onclick=()=>{ modal.style.display='none'; };
    modal.addEventListener('click',e=>{ if(e.target===modal) modal.style.display='none'; });
    return modal;
  }

  async function loadHtml2PdfV211(){
    if(window.html2pdf) return window.html2pdf;
    await new Promise((resolve,reject)=>{
      const old = document.querySelector('script[data-tasneef-html2pdf-v211="1"],script[data-tasneef-html2pdf="1"]');
      if(old){ old.addEventListener('load',resolve,{once:true}); old.addEventListener('error',reject,{once:true}); if(window.html2pdf) resolve(); return; }
      const s=document.createElement('script');
      s.src='https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js';
      s.async=true; s.dataset.tasneefHtml2pdfV211='1';
      s.onload=resolve; s.onerror=reject; document.head.appendChild(s);
    });
    if(!window.html2pdf) throw new Error('تعذر تحميل مكتبة PDF');
    return window.html2pdf;
  }

  async function downloadPdfFromHtmlV211(html, filename){
    const html2pdf = await loadHtml2PdfV211();
    const wrap = document.createElement('div');
    wrap.style.cssText='position:fixed;left:0;top:0;width:1320px;background:#fff;z-index:2147483647;opacity:1;pointer-events:none;';
    wrap.innerHTML = html.replace(/^[\s\S]*<body[^>]*>/i,'').replace(/<\/body>[\s\S]*$/i,'');
    document.body.appendChild(wrap);
    try{
      const report = wrap.querySelector('.v211-report') || wrap;
      await html2pdf().set({
        margin: [6,6,6,6],
        filename,
        image: { type: 'jpeg', quality: 0.98 },
        html2canvas: { scale: 2, useCORS: true, backgroundColor:'#ffffff', logging:false, scrollX:0, scrollY:0 },
        jsPDF: { unit: 'mm', format: 'a4', orientation: 'landscape' },
        pagebreak: { mode: ['css','legacy'], avoid: ['.v211-section'] }
      }).from(report).save();
    } finally {
      setTimeout(()=>wrap.remove(), 800);
    }
  }

  function openSmartTicketPdfWindowV211(list, title){
    if(!list.length) return say('لا توجد تكتات مطابقة للفلاتر الحالية','err');
    const html = fullReportHtmlV211(list,title);
    const modal = ensureModalV211();
    const frame = modal.querySelector('#v211PreviewFrame');
    modal.style.display='flex';
    frame.srcdoc = html;
    modal.querySelector('#v211PrintBtn').onclick = () => {
      try{ frame.contentWindow.focus(); frame.contentWindow.print(); }
      catch(e){
        const w=window.open('','_blank'); if(w){ w.document.write(html); w.document.close(); setTimeout(()=>w.print(),600); }
      }
    };
    modal.querySelector('#v211DownloadBtn').onclick = async () => {
      try{
        await downloadPdfFromHtmlV211(html, `تقرير-التكتات-حسب-المشرف-${todayIso()}.pdf`);
        say('تم تنزيل ملف PDF في التنزيلات');
      }catch(e){
        console.warn('V211 direct download failed', e);
        say('تعذر التنزيل المباشر. استخدم زر طباعة / حفظ PDF من نفس النافذة.', 'err');
      }
    };
  }

  function ticketsDownloadPdfV211(){
    openSmartTicketPdfWindowV211(listByCurrentFiltersV211(), 'تقرير التكتات حسب المشرف');
  }
  function ticketDownloadPdfV211(id){
    const t=A(window.data?.tickets).find(x=>S(x.id)===S(id));
    if(!t) return say('التكت غير موجود','err');
    openSmartTicketPdfWindowV211([t], `تقرير التكت ${ticketNo(t)}`);
  }

  window.ticketsDownloadPdfV211 = ticketsDownloadPdfV211;
  window.ticketsDownloadPdfV210 = ticketsDownloadPdfV211;
  window.ticketsDownloadPdfV209 = ticketsDownloadPdfV211;
  window.ticketsDownloadPdfV208 = ticketsDownloadPdfV211;
  window.ticketsDownloadPdfV207 = ticketsDownloadPdfV211;
  window.ticketsDownloadPdfV206 = ticketsDownloadPdfV211;
  window.ticketDownloadPdfV211 = ticketDownloadPdfV211;
  window.ticketDownloadPdfV210 = ticketDownloadPdfV211;
  window.ticketDownloadPdfV209 = ticketDownloadPdfV211;
  window.ticketDownloadPdfV208 = ticketDownloadPdfV211;
  window.ticketDownloadPdfV207 = ticketDownloadPdfV211;
  window.ticketDownloadPdfV206 = ticketDownloadPdfV211;

  function patchButtonsV211(){
    document.querySelectorAll('#ticketPdfBtnV206,#supTicketPdfBtnV206,.ticket-pdf-v206,.ticket-pdf-v207,.ticket-pdf-v208,[data-ticket-pdf]').forEach(b=>{ b.textContent='تنزيل PDF'; b.onclick=ticketsDownloadPdfV211; });
    document.querySelectorAll('button').forEach(b=>{
      const txt=S(b.textContent);
      if(/تنزيل PDF|تصدير PDF/.test(txt) && (b.closest('#ticketsPage')||b.closest('#supervisorTicketsPage')||/ticket/i.test(S(b.closest('[id]')?.id)))){
        b.onclick=ticketsDownloadPdfV211;
      }
    });
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(patchButtonsV211,ev==='load'?1600:500)));
  setTimeout(patchButtonsV211,800); setTimeout(patchButtonsV211,2600); setInterval(patchButtonsV211,3000);
  console.log('Tasneef V211 smart ticket PDF window loaded');
})();

/* ===== V213: Clean Client Reports page + monthly merge + independent service gateways ===== */
(function(){
  if(window.__tasneefV213ClientReportsClean) return;
  window.__tasneefV213ClientReportsClean = true;
  const VERSION='V213';
  const $id = (id)=>document.getElementById(id);
  const esc = (v)=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const monthOf = (d)=>String(d||'').slice(0,7);
  const defaults = ['النظافة اليومية','إزالة النفايات','تنظيف الممرات','تنظيف المصاعد','تنظيف البيسمنت','تنظيف الأسطح','تنظيف المناور','تنظيف المكيفات','غسيل خزانات','مكافحة الحشرات','صيانة كهرباء','صيانة سباكة','صيانة مصعد','أعمال زراعة','تنظيف واجهات','أخرى'];
  let gateways = [];

  function getLocalGateways(){
    try{return JSON.parse(localStorage.getItem('tasneef_service_gateways_v213')||'[]').filter(Boolean);}catch(_){return [];}
  }
  function setLocalGateways(list){ try{ localStorage.setItem('tasneef_service_gateways_v213', JSON.stringify([...new Set(list.filter(Boolean))])); }catch(_){} }
  function allTypes(){ return [...new Set([...defaults, ...getLocalGateways(), ...gateways].map(x=>String(x||'').trim()).filter(Boolean))]; }
  async function loadGateways(){
    gateways = [];
    if(window.sb){
      try{
        const res = await sb.from('service_gateways').select('*').eq('is_active',true).order('sort_order',{ascending:true}).order('created_at',{ascending:true});
        if(!res.error && Array.isArray(res.data)) gateways = res.data.map(x=>x.service_name||x.name).filter(Boolean);
      }catch(_){ }
    }
    refreshGatewaySelects(); renderGatewayManager();
  }
  window.tasneefLoadGateways213 = loadGateways;
  function refreshGatewaySelects(){
    ['premiumAddServiceType','supClientReportServiceType'].forEach(id=>{
      const s=$id(id); if(!s) return;
      const current=s.value;
      s.innerHTML = allTypes().map(n=>`<option>${esc(n)}</option>`).join('');
      if(current && allTypes().includes(current)) s.value=current;
    });
  }
  async function saveGateway(name){
    name=String(name||'').trim(); if(!name) return;
    const list=[...new Set([...getLocalGateways(), name])]; setLocalGateways(list);
    if(window.sb){ try{ await sb.from('service_gateways').upsert({name, service_name:name, is_active:true, sort_order:allTypes().length+1},{onConflict:'name'}); }catch(_){} }
    await loadGateways();
  }
  async function deleteGateway(name){
    name=String(name||'').trim();
    setLocalGateways(getLocalGateways().filter(x=>x!==name));
    if(window.sb){ try{ await sb.from('service_gateways').delete().eq('name',name); }catch(_){} }
    await loadGateways();
  }
  window.tasneefSaveGateway213=saveGateway; window.tasneefDeleteGateway213=deleteGateway;

  function ensureStyles(){
    if($id('tasneefV213Style')) return;
    const st=document.createElement('style'); st.id='tasneefV213Style'; st.textContent=`
      #clientReports.client-reports-clean-v213 .report-premium-wrap{display:block!important;grid-template-columns:1fr!important}
      #clientReports.client-reports-clean-v213 #premiumReportFormCard{position:static!important;max-height:78vh;overflow:auto;margin:0!important;box-shadow:none!important;border:0!important}
      .client-report-toolbar-v213{display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;background:#fff;border:1px solid #dce9e3;border-radius:20px;padding:14px;margin:14px 0;box-shadow:0 10px 24px rgba(10,64,51,.05)}
      .client-report-toolbar-v213 .title h2{margin:0;color:#0A4033}.client-report-toolbar-v213 .title p{margin:4px 0 0;color:#60706a}.client-report-toolbar-v213 .actions{display:flex;gap:8px;flex-wrap:wrap}
      .modal-v213{position:fixed;inset:0;background:rgba(5,25,20,.45);z-index:99999;display:flex;align-items:center;justify-content:center;padding:18px}.modal-v213.hidden{display:none!important}.modal-card-v213{width:min(1180px,96vw);max-height:92vh;overflow:hidden;background:#fff;border-radius:24px;box-shadow:0 30px 90px rgba(0,0,0,.25);display:flex;flex-direction:column}.modal-head-v213{display:flex;justify-content:space-between;align-items:center;gap:12px;background:linear-gradient(135deg,#063f32,#0A5A49);color:#fff;padding:14px 18px}.modal-head-v213 h2{margin:0;color:#fff}.modal-body-v213{padding:16px;overflow:auto;background:#fbfdfc}.gateway-manager-v213 .add{display:grid;grid-template-columns:1fr auto;gap:8px;margin-bottom:14px}.gateway-list-v213{display:flex;gap:8px;flex-wrap:wrap}.gateway-chip-v213{display:inline-flex;align-items:center;gap:8px;background:#eef8f4;border:1px solid #cfe5dc;color:#0A4033;border-radius:999px;padding:8px 10px;font-weight:800}.gateway-chip-v213 button{background:#b83232;color:#fff;border-radius:999px;padding:3px 8px;font-size:11px}.edit-banner-v213{background:#fff8e6;border:1px solid #ead28d;color:#6d5100;border-radius:14px;padding:10px;margin:0 0 12px;font-weight:800}.monthly-note-v213{background:#eef8f4;border:1px solid #cfe5dc;color:#0A4033;border-radius:14px;padding:10px;margin:10px 0;font-weight:800}
      @media(max-width:760px){.modal-v213{align-items:flex-end;padding:0}.modal-card-v213{width:100%;max-height:94vh;border-radius:24px 24px 0 0}.client-report-toolbar-v213 .actions{width:100%}.client-report-toolbar-v213 button{flex:1}.gateway-manager-v213 .add{grid-template-columns:1fr}}
    `; document.head.appendChild(st);
  }

  function ensureCleanUI(){
    const sec=$id('clientReports'); if(!sec) return;
    ensureStyles(); sec.classList.add('client-reports-clean-v213');
    if(!$id('clientReportToolbarV213')){
      const toolbar=document.createElement('div'); toolbar.id='clientReportToolbarV213'; toolbar.className='client-report-toolbar-v213';
      toolbar.innerHTML=`<div class="title"><h2>تقارير العملاء</h2></div><div class="actions"><button type="button" onclick="tasneefOpenReportModal213('new')">+ إنشاء تقرير جديد</button><button type="button" class="light" onclick="tasneefOpenGatewayModal213()">إدارة خدمات التقارير</button><button type="button" class="light" onclick="loadPremiumReportsOnly(true).then(()=>renderPremiumReports())">تحديث</button></div>`;
      const kpis=sec.querySelector('.report-kpis') || sec.querySelector('.card') || sec.firstElementChild;
      (kpis?.parentNode||sec).insertBefore(toolbar, kpis?.nextSibling||sec.firstChild);
    }
    if(!$id('clientReportModalV213')){
      const modal=document.createElement('div'); modal.id='clientReportModalV213'; modal.className='modal-v213 hidden';
      modal.innerHTML=`<div class="modal-card-v213"><div class="modal-head-v213"><h2 id="clientReportModalTitleV213">تقرير العملاء</h2><button type="button" class="light" onclick="tasneefCloseReportModal213()">إغلاق</button></div><div class="modal-body-v213" id="clientReportModalBodyV213"></div></div>`;
      document.body.appendChild(modal);
    }
    if(!$id('gatewayModalV213')){
      const modal=document.createElement('div'); modal.id='gatewayModalV213'; modal.className='modal-v213 hidden';
      modal.innerHTML=`<div class="modal-card-v213" style="width:min(760px,96vw)"><div class="modal-head-v213"><h2>إدارة خدمات التقارير</h2><button type="button" class="light" onclick="tasneefCloseGatewayModal213()">إغلاق</button></div><div class="modal-body-v213 gateway-manager-v213"><div class="add"><input id="gatewayNameV213" placeholder="مثال: تنظيف الواجهات / فحص الدفاع المدني"><button type="button" onclick="tasneefSaveGateway213(document.getElementById('gatewayNameV213').value); document.getElementById('gatewayNameV213').value=''">+ إضافة الخدمة</button></div><div id="gatewayListV213" class="gateway-list-v213"></div><p style="color:#60706a;margin-top:12px">أي خدمة تضيفها هنا تظهر في قائمة الخدمات عند الإدارة والمشرفين.</p></div></div>`;
      document.body.appendChild(modal);
    }
    const form=$id('premiumReportFormCard'), body=$id('clientReportModalBodyV213');
    if(form && body && form.parentElement!==body){ body.appendChild(form); }
    const topButtons=[...sec.querySelectorAll('button')].filter(b=>/تقرير جديد|إنشاء تقرير/.test(b.textContent||'') && !b.closest('#clientReportToolbarV213'));
    topButtons.forEach(b=>{ b.style.display='none'; });
    const oldPanel=$id('serviceGatewayAdminV212'); if(oldPanel) oldPanel.remove();
    const noteId='monthlyNoteV213';
    const tableWrap=sec.querySelector('.table-wrap');
    if(tableWrap && !$id(noteId)){
      const n=document.createElement('div'); n.id=noteId; n.className='monthly-note-v213'; n.textContent='تنبيه: كل مشروع له تقرير واحد فقط في الشهر. عند رفع تقرير لنفس المشروع والشهر سيتم تحديث التقرير نفسه بدل إنشاء كرت جديد.';
      tableWrap.parentNode.insertBefore(n, tableWrap);
    }
    refreshGatewaySelects(); renderGatewayManager();
  }
  window.tasneefOpenReportModal213=function(mode){
    ensureCleanUI();
    if(mode==='new' && typeof clearPremiumReportForm==='function') clearPremiumReportForm();
    const m=$id('clientReportModalV213'); if(m) m.classList.remove('hidden');
    const title=$id('clientReportModalTitleV213'); if(title) title.textContent = mode==='new'?'إنشاء تقرير جديد':'تعديل تقرير العملاء';
    const card=$id('premiumReportFormCard');
    if(card && !$id('editBannerV213')){
      const b=document.createElement('div'); b.id='editBannerV213'; b.className='edit-banner-v213'; card.insertBefore(b, card.firstChild.nextSibling);
    }
    const banner=$id('editBannerV213'); if(banner){
      const id=($id('premiumReportId')?.value||'').trim();
      banner.textContent = id ? 'أنت تعدل تقريرًا موجودًا. سيتم تحديث نفس التقرير ولن يتم إنشاء كرت جديد.' : 'تقرير جديد. إذا كان لنفس المشروع ونفس الشهر سيتم دمجه تلقائيًا مع التقرير الموجود.';
    }
  };
  window.tasneefCloseReportModal213=()=>{ $id('clientReportModalV213')?.classList.add('hidden'); };
  window.tasneefOpenGatewayModal213=function(){ ensureCleanUI(); renderGatewayManager(); $id('gatewayModalV213')?.classList.remove('hidden'); };
  window.tasneefCloseGatewayModal213=()=>{ $id('gatewayModalV213')?.classList.add('hidden'); };
  function renderGatewayManager(){
    const box=$id('gatewayListV213'); if(!box) return;
    const custom=[...new Set([...getLocalGateways(), ...gateways])].filter(n=>!defaults.includes(n));
    box.innerHTML = custom.length ? custom.map(n=>`<span class="gateway-chip-v213">${esc(n)} <button type="button" onclick="tasneefDeleteGateway213('${String(n).replace(/'/g,"\\'")}')">حذف</button></span>`).join('') : '<small style="color:#60706a">لا توجد خدمات مخصصة بعد.</small>';
  }

  // Wrap edit button to open modal instead of scrolling and keeping page crowded.
  const oldEdit=window.editPremiumReport;
  if(typeof oldEdit==='function'){
    window.editPremiumReport=function(id){ oldEdit.apply(this,arguments); setTimeout(()=>window.tasneefOpenReportModal213('edit'),80); };
  }

  async function findMonthlyReport(projectId, reportDate, excludeId){
    const m=monthOf(reportDate || (typeof today==='function'?today():new Date().toISOString().slice(0,10)));
    let rows=(window.data?.clientReports||[]).filter(r=>String(r.project_id)===String(projectId) && String(r.report_date||'').startsWith(m));
    if(excludeId) rows=rows.filter(r=>String(r.id)!==String(excludeId));
    if(rows[0]) return rows[0];
    if(window.sb){
      try{
        const res=await sb.from('client_reports').select('*').eq('project_id',projectId).gte('report_date',m+'-01').lte('report_date',m+'-31').order('created_at',{ascending:true});
        if(!res.error && Array.isArray(res.data)) return res.data.find(r=>String(r.id)!==String(excludeId))||null;
      }catch(_){ }
    }
    return null;
  }

  // Admin save: same project + same month updates one report, not duplicate cards.
  window.savePremiumReport = async function(status='draft'){
    try{
      const btn=event?.target; if(btn){ btn.disabled=true; btn.dataset.oldText=btn.innerHTML; btn.innerHTML='جاري الحفظ...'; }
      const rawId=($id('premiumReportId')?.value||'').trim();
      const isUuid=v=>/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(v||'');
      let id=isUuid(rawId)?rawId:'';
      const base=collectPremiumReportBase(status);
      const existing=await findMonthlyReport(base.project_id, base.report_date, id);
      if(!id && existing?.id) id=existing.id;
      let reportId='', publicToken='';
      if(id){
        const current=(window.data?.clientReports||[]).find(r=>String(r.id)===String(id)) || existing || {};
        publicToken=current.public_token || genReportToken();
        const row={...base, public_token:status==='published'?publicToken:current.public_token, updated_at:new Date().toISOString()};
        const {data:updated,error}=await sb.from('client_reports').update(row).eq('id',id).select('id,public_token').single();
        if(error) throw error;
        reportId=updated.id; publicToken=updated.public_token||publicToken;
      }else{
        publicToken=genReportToken();
        const row={...base, report_no:genReportNo(), public_token:status==='published'?publicToken:null};
        const {data:ins,error}=await sb.from('client_reports').insert(row).select('id,public_token').single();
        if(error) throw error;
        reportId=ins.id; publicToken=ins.public_token||publicToken;
      }
      if($id('premiumReportId')) $id('premiumReportId').value=reportId;
      const {error:delErr}=await sb.from('client_report_services').delete().eq('report_id',reportId); if(delErr) throw delErr;
      const serviceRows=(window.premiumServicesState||premiumServicesState||[]).map((s,i)=>({report_id:reportId,sort_order:i+1,service_type:s.service_type||s.title||'خدمة',title:s.title||s.service_type||'خدمة',service_description:s.service_description||'',scope_work:s.scope_work||'',notes:s.notes||'',before_images:Array.isArray(s.before_images)?s.before_images:[],during_images:Array.isArray(s.during_images)?s.during_images:[],after_images:Array.isArray(s.after_images)?s.after_images:[]}));
      if(serviceRows.length){ const {error:se}=await sb.from('client_report_services').insert(serviceRows); if(se) throw se; }
      await loadPremiumReportsOnly(false); renderPremiumReports();
      msg(status==='published'?'تم اعتماد التقرير وتحديثه':'تم حفظ التقرير وتحديثه');
      window.tasneefCloseReportModal213();
      if(status==='published'){
        const url=clientReportUrl(publicToken); const phone=normalizePhoneForWa(base.chairman_phone);
        const text=`السيد / رئيس جمعية ${base.project_name} المحترم\n\nتم نشر ${base.title}.\nللاطلاع على التقرير:\n${url}\n\nشركة تصنيف لإدارة المرافق\n920015589`;
        if(phone && confirm('تم النشر. هل تريد فتح رسالة واتساب الآن؟')) window.open(`https://wa.me/${phone}?text=${encodeURIComponent(text)}`,'_blank');
      }
    }catch(e){ console.error('V213 savePremiumReport',e); msg(e.message||String(e),'err'); }
    finally{ const btn=event?.target; if(btn){ btn.disabled=false; if(btn.dataset.oldText) btn.innerHTML=btn.dataset.oldText; } }
  };

  // Supervisor save: append to the same monthly report for project instead of creating duplicate report cards.
  const oldSupSave=window.supClientSaveDailyReport;
  window.supClientSaveDailyReport = async function(btn){
    try{
      if(btn) btn.disabled=true;
      const pid=$id('supClientReportProject')?.value; if(!pid) throw new Error('اختر المشروع');
      const valid=((window.supClientServicesState||[])).filter(s=>{
        try{return (s.before_images?.length||0)+(s.during_images?.length||0)+(s.after_images?.length||0)>0;}catch(_){return false;}
      });
      if(!valid.length) throw new Error('أضف صورًا لخدمة واحدة على الأقل');
      const u=session()||{}, p=(typeof projectName==='function'?projectName(Number(pid)):'')||'', reportDate=$id('supClientReportDate')?.value||(typeof today==='function'?today():new Date().toISOString().slice(0,10));
      let existing=await findMonthlyReport(Number(pid), reportDate, null);
      let reportId='';
      if(existing?.id){
        reportId=existing.id;
        await sb.from('client_reports').update({title:`تقرير شهري - ${p}`,report_type:'تقرير شهري من المشرف',report_date:reportDate,executive_summary:$id('supClientReportSummary')?.value||'',updated_at:new Date().toISOString()}).eq('id',reportId);
      }else{
        const reportRow={report_no:genReportNo(),project_id:Number(pid),project_name:p,chairman_name:'',chairman_phone:'',title:`تقرير شهري - ${p}`,report_type:'تقرير شهري من المشرف',report_date:reportDate,executive_summary:$id('supClientReportSummary')?.value||'',status:'draft',public_token:null};
        const {data:rep,error}=await sb.from('client_reports').insert(reportRow).select('id').single(); if(error) throw error; reportId=rep.id;
      }
      let currentCount=0; try{ currentCount=(getReportServices(reportId)||[]).length; }catch(_){}
      const rows=valid.map((s,i)=>({report_id:reportId,sort_order:currentCount+i+1,service_type:s.service_type,title:s.title||s.service_type,service_description:'',scope_work:'',notes:`رفع بواسطة: ${u.full_name||u.username||''}`,before_images:s.before_images||[],during_images:s.during_images||[],after_images:s.after_images||[]}));
      const {error:se}=await sb.from('client_report_services').insert(rows); if(se) throw se;
      if(typeof v133Msg==='function') v133Msg('تم إرسال التقرير وتحديث تقرير الشهر نفسه'); else msg('تم تحديث تقرير الشهر');
      if(typeof supClientReportReset==='function') supClientReportReset(); await loadPremiumReportsOnly(false); if(typeof supClientRenderMyReports==='function') supClientRenderMyReports();
    }catch(e){ if(typeof v133Msg==='function') v133Msg(e.message||String(e),'err'); else msg(e.message||String(e),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };

  // Ratings: fill missing project/report/service names from linked report and service rows.
  const oldRatings=window.renderClientRatings;
  window.renderClientRatings=function(){
    const body=$id('clientRatingsBody'); if(!body){ if(oldRatings) return oldRatings(); return; }
    const q=($id('ratingSearch')?.value||'').trim(), v=$id('ratingFilterValue')?.value, f=$id('ratingFilterFollow')?.value;
    let rows=[...(window.data?.clientServiceRatings||[])];
    if(v) rows=rows.filter(r=>r.rating===v); if(f==='yes') rows=rows.filter(r=>r.followup_requested); if(f==='low') rows=rows.filter(r=>r.rating==='يحتاج تحسين');
    if(q) rows=rows.filter(r=>Object.values(r).join(' ').includes(q));
    body.innerHTML=rows.map(r=>{
      const rep=(window.data?.clientReports||[]).find(x=>String(x.id)===String(r.report_id))||{};
      const svc=(window.data?.clientReportServices||[]).find(x=>String(x.id)===String(r.service_id) || (String(x.report_id)===String(r.report_id) && String(x.title||x.service_type)===String(r.service_title||'')))||{};
      const rating=r.rating||'-';
      return `<tr class="${rating==='يحتاج تحسين'?'low-rating':''}"><td>${typeof fmt==='function'?fmt(r.created_at):esc(r.created_at||'')}</td><td>${esc(r.project_name||rep.project_name||(typeof projectName==='function'?projectName(rep.project_id):'')||'-')}</td><td>${esc(r.report_title||rep.title||'-')}</td><td>${esc(r.service_title||svc.title||svc.service_type||'-')}</td><td><span class="badge ${typeof ratingClass==='function'?ratingClass(rating):''}">${esc(rating)}</span></td><td>${esc(r.comment||'-')}</td><td>${r.followup_requested?'نعم':'لا'}</td></tr>`;
    }).join('')||'<tr><td colspan="7">لا توجد تقييمات حتى الآن</td></tr>';
  };

  function boot(){
    ensureCleanUI(); loadGateways(); refreshGatewaySelects();
    // make sure current visible title confirms new version without changing old content.
    [...document.querySelectorAll('h2,h3')].forEach(h=>{ if(/بوابة تقارير العملاء/.test(h.textContent||'') && !/V213/.test(h.textContent)) h.textContent='تقارير العملاء - V213'; });
  }
  const oldShow=window.showPage;
  if(typeof oldShow==='function'){
    window.showPage=function(page,btn){ const r=oldShow.apply(this,arguments); if(page==='clientReports') setTimeout(boot,80); return r; };
  }
  window.addEventListener('load',()=>setTimeout(boot,900));
})();


/* ===== V215: Client reports project filter fix + remove instructional notes ===== */
(function(){
  if(window.__tasneefV215ClientReportsProjectFilter) return;
  window.__tasneefV215ClientReportsProjectFilter = true;
  const $ = (id)=>document.getElementById(id);
  const S = (v)=>String(v??'');
  const E = (v)=>S(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const monthOf = (v)=>S(v).slice(0,7) || (new Date().toISOString().slice(0,7));
  const today214 = ()=> (typeof today==='function' ? today() : new Date().toISOString().slice(0,10));
  const projectLabel = (r)=> r?.project_name || (typeof projectName==='function' ? projectName(r?.project_id) : '') || '-';
  const reportTime = (r)=> Date.parse(r?.updated_at || r?.created_at || r?.report_date || '') || 0;
  const servicesForIds = (ids)=>{
    const set = new Set(ids.map(String));
    return (window.data?.clientReportServices||[]).filter(s=>set.has(String(s.report_id)));
  };
  const ratingsForIds = (ids)=>{
    const set = new Set(ids.map(String));
    return (window.data?.clientServiceRatings||[]).filter(s=>set.has(String(s.report_id)));
  };
  const statusText = (st)=> (typeof statusText159==='function'?statusText159(st):(typeof reportStatusText==='function'?reportStatusText(st):(st==='published'?'منشور':st==='draft'?'مسودة':S(st||'مسودة'))));
  const statusClass = (st)=> (typeof statusClass159==='function'?statusClass159(st):(typeof reportStatusClass==='function'?reportStatusClass(st):''));
  const ratingClass = (v)=> (typeof ratingClass159==='function'?ratingClass159(v):(typeof ratingClass==='function'?ratingClass(v):''));
  const reportUrl = (token)=> (typeof clientReportUrl==='function'?clientReportUrl(token):('client-report.html?token='+encodeURIComponent(token||'')));
  const serviceName = (s)=> S(s?.service_type || s?.title || 'خدمة').trim();
  const serviceNames = (ids)=> [...new Set(servicesForIds(ids).map(serviceName).filter(Boolean))];
  const ratingLabel = (ids)=>{
    const rs=ratingsForIds(ids);
    if(!rs.length) return 'لم يتم التقييم';
    if(rs.some(r=>r.rating==='يحتاج تحسين'||r.followup_requested)) return 'يحتاج تحسين';
    return rs[0].rating || 'تم التقييم';
  };
  function cleanClientReportNotes214(){
    document.querySelectorAll('.monthly-note-v213').forEach(n=>n.remove());
    document.querySelectorAll('#clientReportsPage .title p, .client-reports-page .title p').forEach(p=>{
      const t=S(p.textContent);
      if(t.includes('تقرير واحد لكل مشروع') || t.includes('بدون ازدحام الصفحة')) p.remove();
    });
    document.querySelectorAll('*').forEach(el=>{
      if(el.childElementCount===0){
        const t=S(el.textContent).trim();
        if(t==='تنبيه: كل مشروع له تقرير واحد فقط في الشهر. عند رفع تقرير لنفس المشروع والشهر سيتم تحديث التقرير نفسه بدل إنشاء كرت جديد.' || t==='تقرير واحد لكل مشروع في الشهر، مع تعديل وعرض مرتب بدون ازدحام الصفحة.') el.remove();
      }
    });
  }
  function groupedMonthlyReports214(reports){
    const map=new Map();
    reports.forEach(r=>{
      const key=`${S(r.project_id||projectLabel(r))}__${monthOf(r.report_date)}`;
      if(!map.has(key)) map.set(key, []);
      map.get(key).push(r);
    });
    return [...map.values()].map(group=>{
      group.sort((a,b)=>reportTime(b)-reportTime(a));
      const primary = group[0];
      const ids = group.map(r=>r.id).filter(Boolean);
      const published = group.find(r=>r.status==='published' && r.public_token);
      return { primary, group, ids, published };
    }).sort((a,b)=>{
      const ad=S(a.primary.report_date), bd=S(b.primary.report_date);
      return bd.localeCompare(ad) || projectLabel(a.primary).localeCompare(projectLabel(b.primary),'ar');
    });
  }
  window.tasneefDeleteMonthlyReportGroup214 = async function(idsCsv){
    const ids=S(idsCsv).split(',').filter(Boolean);
    if(!ids.length) return;
    if(!confirm('حذف تقرير هذا المشروع لهذا الشهر؟')) return;
    try{
      for(const id of ids){
        if(window.sb) await sb.from('client_reports').delete().eq('id', id);
      }
      if(typeof msg==='function') msg('تم حذف التقرير');
      if(typeof loadPremiumReportsOnly==='function') await loadPremiumReportsOnly(false);
      window.renderPremiumReports();
    }catch(e){ console.error(e); if(typeof msg==='function') msg(e.message||String(e),'err'); }
  };
  window.renderPremiumReports = function(){
    cleanClientReportNotes214();
    const body=$('premiumReportsBody'); if(!body) return;
    const reports=window.data?.clientReports||[];
    const ratings=window.data?.clientServiceRatings||[];
    const month=$('premiumReportFilterMonth')?.value || today214().slice(0,7);
    const mReports=reports.filter(r=>S(r.report_date).startsWith(month));
    const groupedAll = groupedMonthlyReports214(reports);
    const groupedMonth = groupedMonthlyReports214(mReports);
    if($('reportsTotalKpi')) $('reportsTotalKpi').textContent=groupedAll.length;
    if($('reportsMonthKpi')) $('reportsMonthKpi').textContent=groupedMonth.length;
    if($('reportsDraftKpi')) $('reportsDraftKpi').textContent=groupedAll.filter(g=>g.group.some(r=>r.status==='draft')).length;
    if($('reportsPublishedKpi')) $('reportsPublishedKpi').textContent=groupedAll.filter(g=>g.group.some(r=>r.status==='published')).length;
    if($('reportsFollowKpi')) $('reportsFollowKpi').textContent=ratings.filter(r=>r.rating==='يحتاج تحسين'||r.followup_requested).length;

    const q=($('premiumReportSearch')?.value||'').trim();
    const smart=($('premiumServiceSmartSearch')?.value||'').trim();
    const pid=$('premiumReportFilterProject')?.value||'';
    const st=$('premiumReportFilterStatus')?.value||'';
    const sv=$('premiumReportFilterService')?.value||'';
    const rt=$('premiumReportFilterRating')?.value||'';
    let rows=[...reports];
    if(q) rows=rows.filter(r=>[r.report_no,r.project_name,r.title,r.report_type,serviceNames([r.id]).join(' ')].join(' ').includes(q));
    if(smart) rows=rows.filter(r=>[r.report_no,r.project_name,r.title,r.report_type,serviceNames([r.id]).join(' ')].join(' ').includes(smart));
    if(pid){
      const sel=$('premiumReportFilterProject');
      const ptxt=S(sel?.selectedOptions?.[0]?.textContent).trim();
      rows=rows.filter(r=>{
        const rpid=S(r.project_id).trim();
        const rname=S(r.project_name || projectLabel(r)).trim();
        return rpid===S(pid).trim() || rname===ptxt || rname===S(pid).trim() || rpid===ptxt;
      });
    }
    if(st) rows=rows.filter(r=>(r.status||'unpublished')===st);
    if(month) rows=rows.filter(r=>S(r.report_date).startsWith(month));
    let groups=groupedMonthlyReports214(rows);
    if(sv) groups=groups.filter(g=>serviceNames(g.ids).some(n=>S(n).includes(sv)));
    if(rt) groups=groups.filter(g=> rt==='none' ? !ratingsForIds(g.ids).length : ratingsForIds(g.ids).some(x=>x.rating===rt));

    body.innerHTML = groups.map(g=>{
      const r=g.primary, names=serviceNames(g.ids), rating=ratingLabel(g.ids), pub=g.published || r, url=pub?.public_token?reportUrl(pub.public_token):'';
      const servicesHtml=names.length?names.map(n=>`<span class="service-chip">${E(n)}</span>`).join(''):'<span class="service-chip">غير محدد</span>';
      const monthLabel=monthOf(r.report_date);
      const countBadge=g.group.length>1?`<br><small style="color:#60706a">مدمج من ${g.group.length} تقارير</small>`:'';
      const status = g.group.some(x=>x.status==='published')?'published':(r.status||'draft');
      return `<tr data-report-id="${E(r.id)}"><td><b>${E(r.report_no||r.id)}</b><br><small>تقرير شهري - ${E(projectLabel(r))} - ${E(monthLabel)}</small>${countBadge}</td><td>${E(monthLabel)}</td><td>${E(projectLabel(r))}</td><td>${servicesHtml}</td><td><span class="badge ${statusClass(status)}">${statusText(status)}</span></td><td><span class="badge ${ratingClass(rating)}">${E(rating)}</span></td><td>${url?`<div class="client-copy">${E(url)}</div><button type="button" class="light" onclick="copyText('${encodeURIComponent(url)}')">نسخ</button>`:'غير منشور'}</td><td class="row-actions"><button type="button" onclick="editPremiumReport('${E(r.id)}')">تعديل</button>${url?`<button type="button" class="light" onclick="openClientReport('${E(pub.public_token)}')">عرض</button><button type="button" class="light" onclick="sendPremiumWhatsapp('${E(pub.id||r.id)}')">واتساب</button>`:''}<button type="button" class="danger" onclick="tasneefDeleteMonthlyReportGroup214('${g.ids.map(encodeURIComponent).join(',')}')">حذف</button></td></tr>`;
    }).join('') || `<tr><td colspan="8">${window.data?.clientReportsError?'شغّل ملف SQL الخاص بالتقارير في Supabase':'لا توجد تقارير'}</td></tr>`;
    cleanClientReportNotes214();
  };
  // Extra clean after V213 UI creation.
  const oldEnsure=window.tasneefOpenReportModal213;
  if(typeof oldEnsure==='function') window.tasneefOpenReportModal213=function(){ cleanClientReportNotes214(); return oldEnsure.apply(this,arguments); };
  const oldShow=window.showPage;
  if(typeof oldShow==='function') window.showPage=function(page,btn){ const r=oldShow.apply(this,arguments); if(page==='clientReports') setTimeout(()=>{cleanClientReportNotes214(); try{window.renderPremiumReports();}catch(_){ }},150); return r; };
  window.addEventListener('load',()=>setTimeout(()=>{ cleanClientReportNotes214(); try{window.renderPremiumReports();}catch(_){ } },1100));
  setTimeout(()=>{ cleanClientReportNotes214(); try{window.renderPremiumReports();}catch(_){ } },1800);
  console.log('Tasneef V215 client reports project filter fix loaded');
})();

/* ===== V217: Fix client reports empty table + robust monthly grouping/filter + update UI version ===== */
(function(){
  if(window.__tasneefV217ClientReportsVisible) return;
  window.__tasneefV217ClientReportsVisible = true;
  const $ = (id)=>document.getElementById(id);
  const S = (v)=>String(v??'').trim();
  const E = (v)=>S(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const isAll = (v)=>{ v=S(v); return !v || /^كل\s/.test(v) || v==='all' || v==='undefined' || v==='null'; };
  const getDate = (r)=>S(r?.report_date || r?.date || r?.created_at || r?.updated_at || '').slice(0,10);
  const getMonth = (r)=>{
    const d=getDate(r);
    if(/^\d{4}-\d{2}/.test(d)) return d.slice(0,7);
    return S($('premiumReportFilterMonth')?.value || new Date().toISOString().slice(0,7));
  };
  const projectNameSafe = (r)=>{
    let n=S(r?.project_name || r?.project || r?.project_title || '');
    if(!n && typeof projectName==='function') { try{ n=S(projectName(r?.project_id)); }catch(_){ } }
    return n || '-';
  };
  const reportTime = (r)=> Date.parse(r?.updated_at || r?.created_at || r?.report_date || '') || 0;
  const servicesByIds = (ids)=>{
    const set=new Set(ids.map(String));
    return (window.data?.clientReportServices||[]).filter(s=>set.has(String(s.report_id)));
  };
  const ratingsByIds = (ids)=>{
    const set=new Set(ids.map(String));
    return (window.data?.clientServiceRatings||[]).filter(s=>set.has(String(s.report_id)));
  };
  const serviceNames = (ids)=>[...new Set(servicesByIds(ids).map(s=>S(s.service_type||s.title||s.name||'خدمة')).filter(Boolean))];
  const ratingLabel = (ids)=>{
    const rs=ratingsByIds(ids);
    if(!rs.length) return 'لم يتم التقييم';
    if(rs.some(r=>S(r.rating)==='يحتاج تحسين'||r.followup_requested)) return 'يحتاج تحسين';
    return S(rs[0].rating || 'تم التقييم');
  };
  const statusTxt=(st)=>{
    st=S(st||'draft');
    if(st==='published') return 'منشورة';
    if(st==='draft') return 'مسودة';
    if(st==='unpublished') return 'غير منشورة';
    return st;
  };
  const badgeClass=(st)=> S(st)==='published'?'ok':(S(st)==='draft'?'warn':'');
  const reportUrl = (token)=>{
    try{ if(typeof clientReportUrl==='function') return clientReportUrl(token); }catch(_){ }
    return 'client-report.html?token='+encodeURIComponent(token||'');
  };
  function cleanTextNotes(){
    document.querySelectorAll('.monthly-note-v213').forEach(n=>n.remove());
    document.querySelectorAll('*').forEach(el=>{
      if(el.childElementCount!==0) return;
      const t=S(el.textContent);
      if(t.includes('كل مشروع له تقرير واحد فقط في الشهر') || t.includes('تقرير واحد لكل مشروع في الشهر') || t.includes('بدون ازدحام الصفحة')) el.remove();
    });
  }
  function updateClientReportsTitle(){
    document.querySelectorAll('#clientReports h2,#clientReports h3').forEach(h=>{
      const t=S(h.textContent);
      if(t.includes('بوابة تقارير العملاء') || t.includes('تقارير العملاء - V213')) h.textContent='تقارير العملاء - V217';
    });
  }
  function groupReports(list){
    const map=new Map();
    list.forEach(r=>{
      const p=projectNameSafe(r);
      const key=(p+'__'+getMonth(r)).toLowerCase();
      if(!map.has(key)) map.set(key,[]);
      map.get(key).push(r);
    });
    return [...map.values()].map(g=>{
      g.sort((a,b)=>reportTime(b)-reportTime(a));
      const primary=g[0];
      const ids=g.map(x=>x.id).filter(x=>x!==undefined&&x!==null);
      const pub=g.find(x=>S(x.status)==='published' && x.public_token);
      return {primary,group:g,ids,pub};
    }).sort((a,b)=> getDate(b.primary).localeCompare(getDate(a.primary)) || projectNameSafe(a.primary).localeCompare(projectNameSafe(b.primary),'ar'));
  }
  function matchesProject(r,pid,ptxt){
    if(isAll(pid)) return true;
    const vals=[r?.project_id,r?.project_name,projectNameSafe(r),r?.project].map(S).filter(Boolean);
    const p=S(pid), t=S(ptxt);
    return vals.some(v=>v===p || v===t || v.includes(p) || (t && v.includes(t)) || p.includes(v) || (t && t.includes(v)));
  }
  window.renderPremiumReports = function(){
    const body=$('premiumReportsBody'); if(!body) return;
    cleanTextNotes(); updateClientReportsTitle();
    const reports=(window.data?.clientReports||[]).filter(Boolean);
    const ratings=(window.data?.clientServiceRatings||[]).filter(Boolean);
    const selectedMonth=S($('premiumReportFilterMonth')?.value || new Date().toISOString().slice(0,7));
    const allGroups=groupReports(reports);
    const monthGroups=groupReports(reports.filter(r=>!selectedMonth || getMonth(r)===selectedMonth));
    if($('reportsTotalKpi')) $('reportsTotalKpi').textContent=allGroups.length;
    if($('reportsMonthKpi')) $('reportsMonthKpi').textContent=monthGroups.length;
    if($('reportsDraftKpi')) $('reportsDraftKpi').textContent=allGroups.filter(g=>g.group.some(r=>S(r.status)!=='published')).length;
    if($('reportsPublishedKpi')) $('reportsPublishedKpi').textContent=allGroups.filter(g=>g.group.some(r=>S(r.status)==='published')).length;
    if($('reportsFollowKpi')) $('reportsFollowKpi').textContent=ratings.filter(r=>S(r.rating)==='يحتاج تحسين'||r.followup_requested).length;

    const q=S($('premiumReportSearch')?.value);
    const smart=S($('premiumServiceSmartSearch')?.value);
    const pid=S($('premiumReportFilterProject')?.value);
    const ptxt=S($('premiumReportFilterProject')?.selectedOptions?.[0]?.textContent);
    const st=S($('premiumReportFilterStatus')?.value);
    const sv=S($('premiumReportFilterService')?.value);
    const rt=S($('premiumReportFilterRating')?.value);
    let rows=[...reports];
    if(selectedMonth) rows=rows.filter(r=>getMonth(r)===selectedMonth);
    if(!isAll(pid)) rows=rows.filter(r=>matchesProject(r,pid,ptxt));
    if(!isAll(st)) rows=rows.filter(r=>S(r.status||'draft')===st || (st==='unpublished' && S(r.status)!=='published'));
    const searchNeedle=(q+' '+smart).trim();
    if(searchNeedle){
      rows=rows.filter(r=>[r.report_no,r.title,r.report_type,r.project_name,projectNameSafe(r),serviceNames([r.id]).join(' ')].join(' ').includes(searchNeedle));
    }
    let groups=groupReports(rows);
    if(!isAll(sv)) groups=groups.filter(g=>serviceNames(g.ids).some(n=>n.includes(sv)));
    if(!isAll(rt)) groups=groups.filter(g=> rt==='none' ? !ratingsByIds(g.ids).length : ratingsByIds(g.ids).some(x=>S(x.rating)===rt));

    body.innerHTML = groups.map(g=>{
      const r=g.primary;
      const names=serviceNames(g.ids);
      const rating=ratingLabel(g.ids);
      const status=g.group.some(x=>S(x.status)==='published')?'published':S(r.status||'draft');
      const pub=g.pub||r;
      const url=pub?.public_token?reportUrl(pub.public_token):'';
      const servicesHtml=names.length?names.map(n=>`<span class="service-chip">${E(n)}</span>`).join(' '):'<span class="service-chip">غير محدد</span>';
      const merged=g.group.length>1?`<br><small style="color:#60706a">مدمج من ${g.group.length} تقارير</small>`:'';
      return `<tr data-report-id="${E(r.id)}">
        <td><b>${E(r.report_no||r.id||'-')}</b><br><small>تقرير شهري - ${E(projectNameSafe(r))} - ${E(getMonth(r))}</small>${merged}</td>
        <td>${E(getMonth(r))}</td>
        <td>${E(projectNameSafe(r))}</td>
        <td>${servicesHtml}</td>
        <td><span class="badge ${badgeClass(status)}">${E(statusTxt(status))}</span></td>
        <td><span class="badge">${E(rating)}</span></td>
        <td>${url?`<div class="client-copy">${E(url)}</div><button type="button" class="light" onclick="copyText('${encodeURIComponent(url)}')">نسخ</button>`:'غير منشور'}</td>
        <td class="row-actions"><button type="button" onclick="editPremiumReport('${E(r.id)}')">تعديل</button>${url?`<button type="button" class="light" onclick="openClientReport('${E(pub.public_token)}')">عرض</button><button type="button" class="light" onclick="sendPremiumWhatsapp('${E(pub.id||r.id)}')">واتساب</button>`:''}<button type="button" class="danger" onclick="tasneefDeleteMonthlyReportGroup214('${g.ids.map(encodeURIComponent).join(',')}')">حذف</button></td>
      </tr>`;
    }).join('') || `<tr><td colspan="8">${reports.length?'لا توجد تقارير مطابقة للفلاتر':'لا توجد تقارير'}</td></tr>`;
    cleanTextNotes(); updateClientReportsTitle();
  };
  const oldShow=window.showPage;
  if(typeof oldShow==='function') window.showPage=function(page,btn){ const r=oldShow.apply(this,arguments); if(page==='clientReports') setTimeout(()=>{try{renderPremiumReports();}catch(e){console.warn(e)}},200); return r; };
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(()=>{try{renderPremiumReports();}catch(e){}},ev==='load'?1300:500)));
  setTimeout(()=>{try{renderPremiumReports();}catch(e){}},2200);
  console.log('Tasneef V217 client reports table visibility fix loaded');
})();

/* ===== V217: Reports depend on CURRENT project supervisor only =====
   - Ticket PDF/report grouping and supervisor filter use project.supervisor_id now.
   - "رفع بواسطة" remains visible as information only.
   - Keeps client/project official names unchanged for customer-facing reports.
*/
(function(){
  'use strict';
  if(window.__tasneefV217CurrentSupervisorReports) return;
  window.__tasneefV217CurrentSupervisorReports = true;
  window.TASNEEF_BUILD = 'V217_CURRENT_PROJECT_SUPERVISOR_REPORTS_2026_05_20';
  const S=v=>String(v??'').trim();
  const A=v=>Array.isArray(v)?v:[];
  const $=id=>document.getElementById(id);
  const E=v=>S(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const say=(t,type)=>{ try{ if(typeof msg==='function') msg(t,type); else alert(t); }catch(_){ alert(t); } };
  const todayIso=()=>new Date().toISOString().slice(0,10);
  const dateOnly=v=>S(v).slice(0,10)||'-';
  const ticketNo=t=>S(t?.ticket_number)||('T-'+S(t?.id||0).padStart(4,'0'));
  const statusText=s=>S(s)==='closed'?'مغلق':(S(s)==='processing'?'تحت المعالجة':'مفتوح');
  const priorityText=p=>S(p)==='urgent'?'عاجل':(S(p)==='high'?'مهم':(S(p)==='low'?'منخفض':'عادي'));
  function findProjectById(id){ return A(window.data?.projects).find(p=>S(p.id)===S(id)); }
  function userName(id){
    const u=A(window.data?.users||window.data?.appUsers).find(x=>S(x.id)===S(id)||S(x.username)===S(id));
    return u?S(u.full_name||u.name||u.username):(S(id)||'-');
  }
  function projectOfficialName(pid){
    const p=findProjectById(pid);
    if(p) return S(p.official_name||p.client_name||p.display_name||p.name)||'-';
    try{ if(typeof projectName==='function') return S(projectName(pid))||'-'; }catch(_){ }
    return '-';
  }
  function supervisorNameSafe(id){
    try{ if(typeof supervisorName==='function') return S(supervisorName(id))||'-'; }catch(_){ }
    return userName(id);
  }
  function currentSupervisorIdForTicket(t){
    const p=findProjectById(t?.project_id);
    return S(p?.supervisor_id || t?.current_supervisor_id || t?.project_supervisor_id || t?.supervisor_id || '');
  }
  function currentSupervisorNameForTicket(t){
    const sid=currentSupervisorIdForTicket(t);
    return sid ? supervisorNameSafe(sid) : 'بدون مشرف حالي';
  }
  function raisedBy(t){
    return S(t?.created_by_name||t?.created_by_full_name||t?.raised_by_name||t?.created_user_name||t?.creator_name||t?.created_by_username) || userName(t?.created_by||t?.raised_by||t?.user_id||t?.created_user_id||t?.created_by_id);
  }
  function selectedVal(ids){ for(const id of ids){ const v=S($(id)?.value); if(v) return v; } return ''; }
  function listByCurrentFiltersV217(){
    let list=A(window.data?.tickets).slice();
    const status=selectedVal(['ticketFilterStatus','supTicketFilterStatus','ticketStatusFilter','ticketsStatusFilter']);
    const project=selectedVal(['ticketFilterProject','supTicketFilterProject','ticketsProjectFilter']);
    const supervisor=selectedVal(['ticketFilterSupervisor','supTicketFilterSupervisor','ticketsSupervisorFilter']);
    const from=selectedVal(['ticketFilterFrom','supTicketFilterFrom','ticketsFromFilter']);
    const to=selectedVal(['ticketFilterTo','supTicketFilterTo','ticketsToFilter']);
    const search=S($('ticketSearch')?.value||$('supTicketSearch')?.value||$('ticketsSearch')?.value||'').toLowerCase();
    if(status) list=list.filter(t=>S(t.status||'open')===status);
    if(project) list=list.filter(t=>S(t.project_id)===project);
    // المهم: فلتر المشرف يعتمد على المشرف الحالي للمشروع، وليس مشرف السجل القديم.
    if(supervisor) list=list.filter(t=>S(currentSupervisorIdForTicket(t))===S(supervisor));
    if(from) list=list.filter(t=>dateOnly(t.created_at)>=from);
    if(to) list=list.filter(t=>dateOnly(t.created_at)<=to);
    if(search){
      list=list.filter(t=>[
        ticketNo(t), t.title, t.description, projectOfficialName(t.project_id),
        currentSupervisorNameForTicket(t), raisedBy(t), statusText(t.status), priorityText(t.priority)
      ].join(' ').toLowerCase().includes(search));
    }
    return list.sort((a,b)=> currentSupervisorNameForTicket(a).localeCompare(currentSupervisorNameForTicket(b),'ar') || projectOfficialName(a.project_id).localeCompare(projectOfficialName(b.project_id),'ar') || (Date.parse(b.created_at||'')||0)-(Date.parse(a.created_at||'')||0));
  }
  function filtersLabelV217(){
    const parts=[];
    const status=selectedVal(['ticketFilterStatus','supTicketFilterStatus','ticketStatusFilter','ticketsStatusFilter']);
    const project=selectedVal(['ticketFilterProject','supTicketFilterProject','ticketsProjectFilter']);
    const supervisor=selectedVal(['ticketFilterSupervisor','supTicketFilterSupervisor','ticketsSupervisorFilter']);
    const from=selectedVal(['ticketFilterFrom','supTicketFilterFrom','ticketsFromFilter']);
    const to=selectedVal(['ticketFilterTo','supTicketFilterTo','ticketsToFilter']);
    if(status) parts.push('الحالة: '+statusText(status));
    if(project) parts.push('المشروع: '+projectOfficialName(project));
    if(supervisor) parts.push('المشرف الحالي: '+supervisorNameSafe(supervisor));
    if(from) parts.push('من: '+from);
    if(to) parts.push('إلى: '+to);
    return parts.length?parts.join(' | '):'جميع التكتات حسب الفلاتر الحالية';
  }
  function groupByCurrentSupervisor(list){
    const map=new Map();
    list.forEach(t=>{
      const key=currentSupervisorIdForTicket(t)||'none';
      if(!map.has(key)) map.set(key,{name:currentSupervisorNameForTicket(t),rows:[]});
      map.get(key).rows.push(t);
    });
    return [...map.values()].sort((a,b)=>a.name.localeCompare(b.name,'ar'));
  }
  function reportStyle(){ return `<style>
    @page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}html,body{margin:0;background:#f4f7f6;color:#153d32;font-family:Tahoma,Arial,sans-serif;direction:rtl;-webkit-print-color-adjust:exact;print-color-adjust:exact}.v217-report{background:#fff;width:1320px;max-width:100%;margin:0 auto;padding:22px;color:#153d32}.v217-head{display:flex;justify-content:space-between;align-items:center;border-bottom:4px solid #0b5b49;padding-bottom:14px;margin-bottom:14px}.v217-brand{display:flex;align-items:center;gap:12px}.v217-brand img{width:72px;height:72px;border:1px solid #d7e6e1;border-radius:16px;object-fit:contain;padding:7px;background:#fff}.v217-brand h1{margin:0 0 5px;color:#0b5b49;font-size:28px}.v217-brand p{margin:3px 0;color:#647970;font-size:13px}.v217-company{font-weight:900;color:#0b5b49;font-size:18px;text-align:left}.v217-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:12px 0 16px}.v217-kpis div{border:1px solid #cfe2da;background:#f3faf7;border-radius:14px;padding:10px}.v217-kpis small{display:block;color:#657972;margin-bottom:5px}.v217-kpis b{font-size:22px;color:#062f27}.v217-section{break-inside:avoid;page-break-inside:avoid;border:1px solid #cadbd4;border-radius:13px;overflow:hidden;margin:14px 0}.v217-section-title{background:#0b5b49;color:#fff;padding:10px 13px;font-weight:900;display:flex;justify-content:space-between;font-size:16px}.v217-table{width:100%;border-collapse:collapse;font-size:11px}.v217-table th,.v217-table td{border:1px solid #d6e2df;padding:7px;text-align:right;vertical-align:top}.v217-table th{background:#eaf2f6;color:#143d32}.v217-table tr:nth-child(even) td{background:#f7fafb}.v217-desc{white-space:pre-wrap;line-height:1.45;max-width:330px}.v217-pill{background:#eaf7f1;border:1px solid #cbe3d7;border-radius:999px;padding:3px 8px;font-weight:800;display:inline-block}.v217-empty{border:1px dashed #cbded6;border-radius:13px;padding:26px;text-align:center;color:#647970;background:#fff}.v217-footer{text-align:center;border-top:1px solid #d6e2df;margin-top:16px;padding-top:9px;color:#73847e;font-size:12px}@media print{html,body{background:#fff}.v217-report{width:auto;padding:0}.v217-section{page-break-inside:avoid}}
  </style>`; }
  function reportBody(list,title){
    const groups=groupByCurrentSupervisor(list);
    const sections=groups.map(g=>`<section class="v217-section"><div class="v217-section-title"><span>المشرف الحالي: ${E(g.name)}</span><span>${g.rows.length} تكت</span></div><table class="v217-table"><thead><tr><th>#</th><th>رقم التكت</th><th>المشروع</th><th>العنوان / الوصف</th><th>الحالة</th><th>الأولوية</th><th>رفع بواسطة</th><th>استلام / إغلاق</th><th>التاريخ</th></tr></thead><tbody>${g.rows.map((t,i)=>`<tr><td>${i+1}</td><td><b>${E(ticketNo(t))}</b></td><td>${E(projectOfficialName(t.project_id))}</td><td class="v217-desc"><b>${E(t.title||'-')}</b><br>${E(t.description||'-')}</td><td><span class="v217-pill">${E(statusText(t.status))}</span></td><td>${E(priorityText(t.priority))}</td><td>${E(raisedBy(t))}</td><td>استلم: ${E(t.claimed_by_name||userName(t.claimed_by)||'-')}<br>أغلق: ${E(t.closed_by_name||userName(t.closed_by)||'-')}${t.closure_note?`<br>الحل: ${E(t.closure_note)}`:''}</td><td>${E(dateOnly(t.created_at))}</td></tr>`).join('')}</tbody></table></section>`).join('') || '<div class="v217-empty">لا توجد تكتات مطابقة للفلاتر الحالية</div>';
    return `<div class="v217-report" dir="rtl"><div class="v217-head"><div class="v217-brand"><img src="tasneef_logo_print.png" onerror="this.style.display='none'"><div><h1>${E(title)}</h1><p>${E(filtersLabelV217())}</p><p>تاريخ التصدير: ${E(new Date().toLocaleString('ar-SA'))}</p></div></div><div class="v217-company">شركة تصنيف لإدارة المرافق</div></div><div class="v217-kpis"><div><small>إجمالي التكتات</small><b>${list.length}</b></div><div><small>مفتوح</small><b>${list.filter(t=>S(t.status||'open')==='open').length}</b></div><div><small>تحت المعالجة</small><b>${list.filter(t=>S(t.status)==='processing').length}</b></div><div><small>مغلق</small><b>${list.filter(t=>S(t.status)==='closed').length}</b></div></div>${sections}<div class="v217-footer">يعتمد ترتيب التقرير على المشرف الحالي للمشروع، مع إظهار من رفع التكت داخل التفاصيل.</div></div>`;
  }
  function fullHtml(list,title){ return `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>${E(title)}</title>${reportStyle()}</head><body>${reportBody(list,title)}</body></html>`; }
  function ensureModal(){
    let modal=document.getElementById('ticketPdfSmartModalV217'); if(modal) return modal;
    const style=document.createElement('style'); style.id='ticketPdfSmartModalCssV217'; style.textContent=`#ticketPdfSmartModalV217{position:fixed;inset:0;background:rgba(0,0,0,.48);z-index:999999;display:flex;align-items:center;justify-content:center;padding:18px;direction:rtl}#ticketPdfSmartModalV217.hidden{display:none}.v217-modal-box{width:min(1180px,98vw);height:min(860px,94vh);background:#fff;border-radius:22px;box-shadow:0 24px 80px rgba(0,0,0,.25);overflow:hidden;display:flex;flex-direction:column}.v217-modal-head{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:14px 16px;background:#0b5b49;color:#fff}.v217-modal-head h3{margin:0;font-size:20px}.v217-modal-head p{margin:4px 0 0;opacity:.9}.v217-modal-actions{display:flex;gap:8px;flex-wrap:wrap}.v217-modal-actions button{border:0;border-radius:12px;padding:10px 13px;font-weight:900;cursor:pointer}.v217-modal-actions .primary{background:#fff;color:#0b5b49}.v217-modal-actions .gold{background:#d5a72e;color:#fff}.v217-modal-actions .danger{background:#b3261e;color:#fff}#v217PreviewFrame{border:0;width:100%;flex:1;background:#fff}`; document.head.appendChild(style);
    modal=document.createElement('div'); modal.id='ticketPdfSmartModalV217'; modal.className='hidden'; modal.innerHTML=`<div class="v217-modal-box"><div class="v217-modal-head"><div><h3>تقرير التكتات PDF</h3><p>المشرف المعروض هو المشرف الحالي للمشروع</p></div><div class="v217-modal-actions"><button class="primary" id="v217PrintBtn">طباعة / حفظ PDF</button><button class="gold" id="v217DownloadBtn">تنزيل PDF</button><button class="danger" id="v217CloseBtn">إغلاق</button></div></div><iframe id="v217PreviewFrame"></iframe></div>`; document.body.appendChild(modal);
    document.getElementById('v217CloseBtn').onclick=()=>modal.classList.add('hidden');
    modal.addEventListener('click',e=>{ if(e.target===modal) modal.classList.add('hidden'); });
    return modal;
  }
  function openSmartTicketPdfWindowV217(list,title){
    const modal=ensureModal(); const frame=document.getElementById('v217PreviewFrame'); const html=fullHtml(list,title);
    modal.classList.remove('hidden'); frame.srcdoc=html;
    document.getElementById('v217PrintBtn').onclick=()=>{ try{ frame.contentWindow.focus(); frame.contentWindow.print(); }catch(e){ say('تعذر فتح الطباعة','err'); } };
    document.getElementById('v217DownloadBtn').onclick=async()=>{
      try{ await downloadPdfFromHtmlV217(html,`تقرير-التكتات-حسب-المشرف-الحالي-${todayIso()}.pdf`); say('تم تنزيل ملف PDF في التنزيلات'); }
      catch(e){ console.warn('V217 direct PDF failed',e); say('تعذر التنزيل المباشر. استخدم زر طباعة / حفظ PDF من نفس النافذة.','err'); }
    };
  }
  function loadHtml2Pdf(){ return new Promise((resolve,reject)=>{
    if(window.html2pdf) return resolve(window.html2pdf);
    const old=document.querySelector('script[data-tasneef-html2pdf-v217="1"],script[data-tasneef-html2pdf-v211="1"],script[data-tasneef-html2pdf="1"]');
    if(old){ old.addEventListener('load',()=>resolve(window.html2pdf),{once:true}); old.addEventListener('error',reject,{once:true}); if(window.html2pdf) resolve(window.html2pdf); return; }
    const s=document.createElement('script'); s.src='https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js'; s.async=true; s.dataset.tasneefHtml2pdfV217='1'; s.onload=()=>window.html2pdf?resolve(window.html2pdf):reject(new Error('تعذر تحميل مكتبة PDF')); s.onerror=reject; document.head.appendChild(s);
  }); }
  async function downloadPdfFromHtmlV217(html,filename){
    const html2pdf=await loadHtml2Pdf();
    const wrap=document.createElement('div'); wrap.style.position='fixed'; wrap.style.right='-99999px'; wrap.style.top='0'; wrap.style.background='#fff'; wrap.innerHTML=html.replace(/<!doctype html>[\s\S]*?<body>/i,'').replace(/<\/body>[\s\S]*?<\/html>/i,''); document.body.appendChild(wrap);
    try{ await html2pdf().set({margin:4,filename,html2canvas:{scale:2,useCORS:true,backgroundColor:'#ffffff'},jsPDF:{unit:'mm',format:'a4',orientation:'landscape'},pagebreak:{mode:['avoid-all','css','legacy']}}).from(wrap).save(); }
    finally{ wrap.remove(); }
  }
  window.ticketsDownloadPdfV217=function(){ openSmartTicketPdfWindowV217(listByCurrentFiltersV217(),'تقرير التكتات حسب المشرف الحالي'); };
  window.ticketDownloadPdfV217=function(id){ const t=A(window.data?.tickets).find(x=>S(x.id)===S(id)); if(!t) return say('التكت غير موجود','err'); openSmartTicketPdfWindowV217([t],`تقرير التكت ${ticketNo(t)}`); };
  // Override older PDF entry points.
  window.ticketsDownloadPdfV211=window.ticketsDownloadPdfV217; window.ticketsDownloadPdfV210=window.ticketsDownloadPdfV217; window.ticketsDownloadPdfV209=window.ticketsDownloadPdfV217; window.ticketsDownloadPdfV208=window.ticketsDownloadPdfV217; window.ticketsDownloadPdfV207=window.ticketsDownloadPdfV217; window.ticketsDownloadPdfV206=window.ticketsDownloadPdfV217;
  window.ticketDownloadPdfV211=window.ticketDownloadPdfV217; window.ticketDownloadPdfV210=window.ticketDownloadPdfV217; window.ticketDownloadPdfV209=window.ticketDownloadPdfV217; window.ticketDownloadPdfV208=window.ticketDownloadPdfV217; window.ticketDownloadPdfV207=window.ticketDownloadPdfV217; window.ticketDownloadPdfV206=window.ticketDownloadPdfV217;
  function patchButtons(){
    document.querySelectorAll('#ticketPdfBtnV206,#supTicketPdfBtnV206,.ticket-pdf-v206,.ticket-pdf-v207,.ticket-pdf-v208,[data-ticket-pdf]').forEach(b=>{ b.textContent='تنزيل PDF'; b.onclick=window.ticketsDownloadPdfV217; });
    document.querySelectorAll('button').forEach(b=>{ const txt=S(b.textContent); if(/تنزيل PDF|تصدير PDF/.test(txt) && (b.closest('#ticketsPage')||b.closest('#supervisorTicketsPage')||/ticket/i.test(S(b.closest('[id]')?.id)))) b.onclick=window.ticketsDownloadPdfV217; });
  }
  function updateVersionLabels(){ document.querySelectorAll('h1,h2,h3,.version,.app-version').forEach(el=>{ if(S(el.textContent).includes('V217')) el.textContent=el.textContent.replace('V217','V217'); }); }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(()=>{patchButtons(); updateVersionLabels();},ev==='load'?1600:500)));
  setTimeout(()=>{patchButtons(); updateVersionLabels();},1000); setInterval(patchButtons,3000);
  console.log('Tasneef V217 current project supervisor report logic loaded');
})();

/* ===== V218: Monthly reports use CURRENT project supervisor + de-duplicate workers ===== */
(function(){
  'use strict';
  function S(v){ return String(v ?? '').trim(); }
  function N(v){ return Number(v || 0); }
  function normArabicNameV218(name){
    return S(name)
      .replace(/[أإآ]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه')
      .replace(/[\u064B-\u0652]/g,'').replace(/\s+/g,' ').toLowerCase();
  }
  function projectByIdV218(projectId){
    return (window.data?.projects || []).find(p => S(p.id) === S(projectId));
  }
  function currentSupervisorIdForProjectV218(projectId, fallbackSupervisorId){
    const p = projectByIdV218(projectId);
    return p?.supervisor_id || fallbackSupervisorId || '';
  }
  function uniqueWorkersForProjectTextV218(projectId){
    const map = new Map();
    (window.data?.workers || []).forEach(w => {
      const status = S(w.status || 'active').toLowerCase();
      if(status === 'deleted' || status === 'inactive') return;
      const pid = (typeof workerProjectId === 'function') ? workerProjectId(w) : (w.project_id || w.assigned_project_id || '');
      if(S(pid) !== S(projectId)) return;
      const key = normArabicNameV218(w.name);
      if(key && !map.has(key)) map.set(key, S(w.name));
    });
    return [...map.values()].sort((a,b)=>a.localeCompare(b,'ar')).join('، ') || '-';
  }
  function monthlyRowsV218(){
    const month = document.getElementById('monthlyMonth')?.value || (typeof today === 'function' ? today().slice(0,7) : new Date().toISOString().slice(0,7));
    const selectedSupervisor = document.getElementById('monthlySupervisor')?.value || '';
    const logs = (window.data?.logs || []).filter(l => {
      const d = l.log_date || S(l.check_in).slice(0,10);
      if(!d || d.slice(0,7) !== month) return false;
      const currentSid = currentSupervisorIdForProjectV218(l.project_id, l.supervisor_id);
      return !selectedSupervisor || S(currentSid) === S(selectedSupervisor);
    });
    const map = new Map();
    logs.forEach(l => {
      const currentSid = currentSupervisorIdForProjectV218(l.project_id, l.supervisor_id);
      const projectId = l.project_id || '';
      const key = S(currentSid) + '_' + S(projectId);
      if(!map.has(key)) map.set(key,{s:currentSid,p:projectId,c:0,a:0,r:0,t:0});
      const row = map.get(key);
      row.c++;
      row.a += N((typeof logActualMinutes === 'function') ? logActualMinutes(l) : (l.duration_minutes || (typeof minutesBetween === 'function' ? minutesBetween(l.check_in,l.check_out) : 0)));
      row.r += N((typeof logRequiredMinutes === 'function') ? logRequiredMinutes(l) : l.required_minutes);
      row.t += N(l.travel_minutes);
    });
    const vals = [...map.values()];
    const supervisorTotals = {};
    vals.forEach(r => { const sid = S(r.s); supervisorTotals[sid] = (supervisorTotals[sid] || 0) + N(r.a); });
    return vals.map(r => {
      const supTotal = supervisorTotals[S(r.s)] || 0;
      const workPercent = supTotal ? (r.a / supTotal * 100) : 0;
      const commitmentPercent = r.r ? (r.a / r.r * 100) : 0;
      const diff = r.a - r.r;
      const st = (typeof monthlyStatusFromDiffV60 === 'function') ? monthlyStatusFromDiffV60(diff,r.r) : {text:(diff<0?'ناقص وقت':diff>0?'زيادة وقت':'ضمن الوقت'),cls:(diff<0?'bad':diff>0?'warn':'ok')};
      const ccls = (typeof monthlyCommitmentClassV60 === 'function') ? monthlyCommitmentClassV60(commitmentPercent,r.r) : st.cls;
      return {...r,supTotal,workers:uniqueWorkersForProjectTextV218(r.p),workPercent,commitmentPercent,ccls,diff,st:st.text,cls:st.cls};
    }).sort((a,b)=>{
      const s = (typeof supervisorName === 'function' ? supervisorName(a.s) : S(a.s)).localeCompare((typeof supervisorName === 'function' ? supervisorName(b.s) : S(b.s)),'ar');
      return s || (typeof projectName === 'function' ? projectName(a.p) : S(a.p)).localeCompare((typeof projectName === 'function' ? projectName(b.p) : S(b.p)),'ar');
    });
  }
  window.monthlyRowsV60 = monthlyRowsV218;
  window.monthlyReportRowsV58 = monthlyRowsV218;
  window.monthlyBaseRowsV59 = monthlyRowsV218;

  window.renderMonthly = function(){
    const body = document.getElementById('monthlyBody');
    if(!body) return;
    const table = body.closest('table');
    if(table && table.tHead) table.tHead.innerHTML = '<tr><th>المشرف الحالي</th><th>المشروع</th><th>أسماء العمال</th><th>الساعات المطلوبة</th><th>الساعات الفعلية</th><th>وقت الانتقال</th><th>نسبة العمل</th><th>نسبة الالتزام</th><th>حالة الأداء</th></tr>';
    const vals = monthlyRowsV218();
    body.innerHTML = vals.map(r=>`<tr><td>${esc(supervisorName(r.s))}</td><td>${esc(projectName(r.p))}</td><td>${esc(r.workers)}</td><td>${minsToText(r.r)}</td><td>${minsToText(r.a)}</td><td>${r.t} دقيقة</td><td><span class="badge green">${percentText(r.workPercent)}</span></td><td><span class="badge ${r.ccls}">${percentText(r.commitmentPercent)}</span></td><td><span class="badge ${r.cls}">${r.st}</span></td></tr>`).join('') || '<tr><td colspan="9">لا توجد بيانات</td></tr>';
    const total = vals.reduce((a,r)=>a+N(r.a),0), required = vals.reduce((a,r)=>a+N(r.r),0), travel = vals.reduce((a,r)=>a+N(r.t),0);
    const commitment = required ? total/required*100 : 0;
    const diff = total-required;
    const st = (typeof monthlyStatusFromDiffV60 === 'function') ? monthlyStatusFromDiffV60(diff,required) : {text:'-',cls:'neutral'};
    const summary = document.getElementById('monthlySummary');
    if(summary) summary.innerHTML = `<div class="kpi"><small>الساعات المطلوبة</small><b>${minsToText(required)}</b></div><div class="kpi"><small>الساعات الفعلية</small><b>${minsToText(total)}</b></div><div class="kpi"><small>فرق الوقت</small><b>${monthlyDiffTextV57(diff)}</b></div><div class="kpi"><small>وقت الانتقال</small><b>${travel} دقيقة</b></div><div class="kpi"><small>نسبة الالتزام</small><b>${percentText(commitment)}</b></div><div class="kpi"><small>حالة الأداء</small><b><span class="badge ${st.cls}">${st.text}</span></b></div>`;
  };
  window.exportMonthlyCSV = function(){
    const rows = [...document.querySelectorAll('#monthlyBody tr')].map(tr=>[...tr.children].map(td=>td.textContent.trim()));
    const csv = ['المشرف الحالي,المشروع,أسماء العمال,الساعات المطلوبة,الساعات الفعلية,وقت الانتقال,نسبة العمل,نسبة الالتزام,حالة الأداء',...rows.map(r=>r.map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))].join('\n');
    download('monthly-current-supervisor.csv',csv);
  };
  window.printMonthlyReportV57 = function(){
    const rows = monthlyRowsV218();
    if(!rows.length){ msg('لا توجد بيانات في الأوقات الشهرية للطباعة','err'); return; }
    const month = document.getElementById('monthlyMonth')?.value || (typeof today === 'function' ? today().slice(0,7) : '');
    const sup = document.getElementById('monthlySupervisor')?.value ? supervisorName(document.getElementById('monthlySupervisor').value) : 'الكل';
    let actualTotal=0, requiredTotal=0, over=0, under=0;
    rows.forEach(r=>{ actualTotal+=N(r.a); requiredTotal+=N(r.r); if(r.st==='زيادة وقت') over++; else if(r.st==='ناقص وقت') under++; });
    const commitmentTotal = requiredTotal ? actualTotal/requiredTotal*100 : 0;
    const groups = new Map();
    rows.forEach(r=>{ const sid=S(r.s); if(!groups.has(sid)) groups.set(sid,[]); groups.get(sid).push(r); });
    const groupCards = [...groups.entries()].map(([sid,items])=>{
      const sActual=items.reduce((a,r)=>a+N(r.a),0), sReq=items.reduce((a,r)=>a+N(r.r),0), sPct=sReq?sActual/sReq*100:0;
      const cards=items.map(r=>`<div class="project-card"><h3>${reportEscV52(projectName(r.p))}</h3><div class="sup-name">المشرف الحالي: ${reportEscV52(supervisorName(sid))}</div><div class="workers-list">${reportEscV52(r.workers||'-').split('،').map(x=>`<div>${x.trim()}</div>`).join('')}</div><div class="time-pill">${reportEscV52(minsToText(r.a))}</div></div>`).join('');
      return `<section class="sup-section"><div class="sup-title"><span>${reportEscV52(supervisorName(sid))}</span><b>${reportEscV52(percentText(sPct))}</b></div><div class="cards-grid">${cards}</div></section>`;
    }).join('');
    const detailRows = rows.map(r=>`<tr><td>${reportEscV52(supervisorName(r.s))}</td><td>${reportEscV52(projectName(r.p))}</td><td>${reportEscV52(r.workers||'-')}</td><td>${reportEscV52(minsToText(r.a))}</td><td>${reportEscV52(minsToText(r.r))}</td><td>${reportEscV52(monthlyDiffTextV57(r.diff))}</td><td>${reportEscV52(percentText(r.workPercent))}</td><td>${reportEscV52(percentText(r.commitmentPercent))}</td><td>${reportEscV52(r.st)}</td></tr>`).join('');
    const html = `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{margin:0;font-family:Tahoma,Arial,sans-serif;color:#153d32;background:#fff;font-size:11px}.page{min-height:100vh;padding:14px;border:2px solid #0b5b49;background:#fff}.top{display:flex;justify-content:space-between;align-items:center;border-bottom:3px solid #0b5b49;padding-bottom:10px;margin-bottom:10px}.brand{display:flex;align-items:center;gap:10px}.logo{width:54px;height:54px;border:2px solid #c7a24d;border-radius:50%;display:grid;place-items:center;font-weight:900;color:#0b5b49}.brand h2{margin:0;color:#0b5b49}.title{text-align:left}.title h1{margin:0;color:#0b5b49;font-size:26px}.muted{color:#68766e}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:10px 0}.box{border:1px solid #d9e6e1;border-radius:13px;padding:9px;text-align:center;background:#f8fbfa}.box b{display:block;color:#60756c}.box strong{font-size:16px;color:#0b5b49}.section-title{margin:12px 0 8px;text-align:center}.section-title span{background:#0b5b49;color:#fff;border-radius:999px;padding:7px 38px;font-weight:900}.sup-section{break-inside:avoid;margin:10px 0}.sup-title{display:flex;justify-content:space-between;background:#0b5b49;color:#fff;border-radius:10px 10px 0 0;padding:8px 12px;font-weight:900}.cards-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;border:1px solid #d9e6e1;border-top:0;padding:10px}.project-card{border:2px solid #333;border-radius:12px;background:#fff;overflow:hidden;text-align:center;break-inside:avoid}.project-card h3{margin:0;padding:9px;background:#454545;color:#fff;font-size:15px}.sup-name{padding:8px;border-bottom:1px solid #ddd;font-weight:900}.workers-list{padding:8px;line-height:1.9;min-height:58px}.workers-list div{border-bottom:1px dashed #e1e7e4}.time-pill{display:inline-block;background:#f2f2f2;border-radius:999px;padding:7px 22px;font-size:16px;font-weight:900;margin:8px}.details{width:100%;border-collapse:collapse;margin-top:10px}.details th{background:#0b5b49;color:#fff;padding:7px}.details td{border:1px solid #dfe8e4;padding:6px;text-align:center}.kpis{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin-top:10px}.kpi{border:1px solid #d9e6e1;border-radius:12px;padding:9px;text-align:center;background:#f8fbfa}.kpi strong{display:block;font-size:16px;color:#0b5b49}.footer{text-align:center;margin-top:10px;color:#68766e}@media print{body{print-color-adjust:exact;-webkit-print-color-adjust:exact}.page{border-radius:0}.project-card,.sup-section,.kpi{break-inside:avoid}}</style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h2>شركة تصنيف</h2><div class="muted">إدارة المرافق والتشغيل</div></div></div><div class="title"><h1>تقرير الأوقات الشهرية</h1><div class="muted">يعتمد التصنيف على المشرف الحالي للمشروع</div></div></div><div class="meta"><div class="box"><b>الشهر</b><strong>${reportEscV52(monthLabelV57(month))}</strong></div><div class="box"><b>المشرف</b><strong>${reportEscV52(sup)}</strong></div><div class="box"><b>عدد المشاريع</b><strong>${rows.length}</strong></div><div class="box"><b>نسبة الالتزام</b><strong>${reportEscV52(percentText(commitmentTotal))}</strong></div></div><div class="section-title"><span>ملخص المشرفين والمشاريع</span></div>${groupCards}<div class="section-title"><span>تفاصيل الأوقات الشهرية</span></div><table class="details"><thead><tr><th>المشرف الحالي</th><th>المشروع</th><th>أسماء العمال</th><th>الوقت الفعلي</th><th>الوقت المطلوب</th><th>الفرق</th><th>نسبة العمل</th><th>نسبة الالتزام</th><th>حالة الوقت</th></tr></thead><tbody>${detailRows}</tbody></table><div class="kpis"><div class="kpi"><strong>${reportEscV52(minsToText(actualTotal))}</strong><span>إجمالي الفعلي</span></div><div class="kpi"><strong>${reportEscV52(minsToText(requiredTotal))}</strong><span>إجمالي المطلوب</span></div><div class="kpi"><strong>${reportEscV52(monthlyDiffTextV57(actualTotal-requiredTotal))}</strong><span>فرق الوقت</span></div><div class="kpi"><strong>${over}</strong><span>زيادة وقت</span></div><div class="kpi"><strong>${under}</strong><span>ناقص وقت</span></div></div><div class="footer">هذا التقرير مولد آليًا من نظام تصنيف</div><script>window.onload=function(){setTimeout(function(){window.print()},450)}<\/script></div></body></html>`;
    const w = window.open('','_blank');
    if(!w){ msg('المتصفح منع فتح نافذة التقرير. اسمح بالنوافذ المنبثقة','err'); return; }
    w.document.open(); w.document.write(html); w.document.close();
  };
  function refreshIfMonthly(){ try{ if(document.getElementById('monthly') && !document.getElementById('monthly').classList.contains('hidden')) window.renderMonthly(); }catch(e){} }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(refreshIfMonthly, ev==='load'?1400:400)));
  setTimeout(refreshIfMonthly,1200);
  console.log('Tasneef V218 monthly current-supervisor grouping loaded');
})();


/* ===== V219: Monthly report first-style layout + daily/permanent sections + de-duplicate workers page ===== */
(function(){
  'use strict';
  if(window.__tasneefV219MonthlyWorkers) return;
  window.__tasneefV219MonthlyWorkers = true;
  const $id = (id)=>document.getElementById(id);
  const S = (v)=>String(v ?? '').trim();
  const N = (v)=>{ const x=Number(v||0); return Number.isFinite(x)?x:0; };
  const esc = (s)=>S(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const normName = (name)=>S(name).replace(/[أإآ]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه').replace(/[\u064B-\u0652]/g,'').replace(/\s+/g,' ').toLowerCase();
  const minuteText = (m)=>`${Math.round(N(m)).toLocaleString('en-US')} دقيقة`;
  const pctText = (p)=>`${N(p).toLocaleString('en-US',{minimumFractionDigits:1,maximumFractionDigits:1})}%`;
  const todayMonth = ()=> typeof today==='function' ? today().slice(0,7) : new Date().toISOString().slice(0,7);
  const projName = (id)=> typeof projectName==='function' ? projectName(id) : ((window.data?.projects||[]).find(p=>S(p.id)===S(id))?.name || '-');
  const supName = (id)=> typeof supervisorName==='function' ? supervisorName(id) : ((window.data?.supervisors||[]).find(s=>S(s.id)===S(id))?.full_name || (window.data?.users||[]).find(u=>S(u.id)===S(id))?.full_name || '-');
  const projectRow = (id)=> (window.data?.projects||[]).find(p=>S(p.id)===S(id)) || {};
  function currentSupervisor(projectId, fallback){ const p=projectRow(projectId); return p.supervisor_id || fallback || ''; }
  function logDate(l){ return S(l.log_date || l.date || l.check_in || l.created_at).slice(0,10); }
  function actualMins(l){ try{ if(typeof logActualMinutes==='function') return N(logActualMinutes(l)); }catch(_){ } return N(l.duration_minutes || (typeof minutesBetween==='function' ? minutesBetween(l.check_in,l.check_out) : 0)); }
  function requiredMins(l){ try{ if(typeof logRequiredMinutes==='function') return N(logRequiredMinutes(l)); }catch(_){ } return N(l.required_minutes || l.daily_required_minutes || l.required_daily_minutes); }
  function isPermanent(projectId, maxReq){
    const p=projectRow(projectId);
    const op=S(p.operation_type || p.visit_type_default || p.type || p.category || p.notes).toLowerCase();
    const name=S(p.name);
    const base=N(p.required_daily_minutes || p.required_minutes || p.daily_minutes || maxReq);
    return base>=540 || op.includes('full') || op.includes('permanent') || op.includes('24') || name.includes('دائم') || name.includes('دائمة') || name.includes('دوام') || name.includes('24');
  }
  function uniqueWorkerNamesForProject(projectId){
    const map=new Map();
    (window.data?.workers||[]).forEach(w=>{
      const status=S(w.status||'active').toLowerCase();
      if(status==='deleted' || status==='inactive') return;
      let pid='';
      try{ pid = (typeof workerProjectId==='function') ? workerProjectId(w) : (w.project_id || w.assigned_project_id || w.current_project_id || ''); }catch(_){ pid=w.project_id || w.assigned_project_id || ''; }
      if(S(pid)!==S(projectId)) return;
      const key=normName(w.name || w.full_name);
      if(key && !map.has(key)) map.set(key, S(w.name || w.full_name));
    });
    return [...map.values()].sort((a,b)=>a.localeCompare(b,'ar')).join('، ') || '-';
  }
  function monthRange(month){ const [y,m]=S(month).split('-').map(Number); const last=new Date(y,m,0).getDate(); return {start:`${month}-01`,end:`${month}-${String(last).padStart(2,'0')}`,days:last}; }
  function buildMonthlyV219(){
    const month=$id('monthlyMonth')?.value || todayMonth();
    const selectedSupervisor=$id('monthlySupervisor')?.value || '';
    const range=monthRange(month);
    let logs=(window.data?.logs||[]).filter(l=>{ const d=logDate(l); return d && d.slice(0,7)===month; });
    logs=logs.filter(l=>!selectedSupervisor || S(currentSupervisor(l.project_id,l.supervisor_id))===S(selectedSupervisor));
    const map=new Map();
    logs.forEach(l=>{
      const sid=currentSupervisor(l.project_id,l.supervisor_id);
      const pid=l.project_id || '';
      const key=S(sid)+'|'+S(pid);
      if(!map.has(key)) map.set(key,{s:sid,p:pid,actual:0,required:0,travel:0,records:0,maxReq:0,days:new Set()});
      const g=map.get(key); const req=requiredMins(l); const d=logDate(l);
      g.actual+=actualMins(l); g.required+=req; g.travel+=N(l.travel_minutes); g.records++; g.maxReq=Math.max(g.maxReq,req); if(d) g.days.add(d);
    });
    const all=[...map.values()].map(g=>({...g,workers:uniqueWorkerNamesForProject(g.p),projectName:projName(g.p),supervisorName:supName(g.s),permanent:isPermanent(g.p,g.maxReq)}));
    const daily=all.filter(x=>!x.permanent), permanent=all.filter(x=>x.permanent);
    const bySup={}; daily.forEach(r=>{ const k=S(r.s); (bySup[k]||(bySup[k]=[])).push(r); });
    Object.keys(bySup).forEach(sid=>{
      bySup[sid].sort((a,b)=>a.projectName.localeCompare(b.projectName,'ar'));
      const total=bySup[sid].reduce((a,r)=>a+N(r.actual),0); let used=0;
      bySup[sid].forEach((r,i)=>{ let pct= total ? (N(r.actual)/total*100) : 0; if(i<bySup[sid].length-1){ pct=Math.round(pct*10)/10; used+=pct; } else { pct=total?Math.max(0,Math.round((100-used)*10)/10):0; } r.percent=pct; r.supTotal=total; });
    });
    const dailySorted=Object.keys(bySup).sort((a,b)=>supName(a).localeCompare(supName(b),'ar')).flatMap(s=>bySup[s]);
    permanent.sort((a,b)=>{ const s=supName(a.s).localeCompare(supName(b.s),'ar'); return s || a.projectName.localeCompare(b.projectName,'ar'); });
    return {month,range,daily:dailySorted,permanent,bySup};
  }
  window.buildMonthlyV219 = buildMonthlyV219;
  window.monthlyRowsV60 = function(){ const r=buildMonthlyV219(); return [...r.daily,...r.permanent].map(x=>({s:x.s,p:x.p,a:x.actual,r:x.required,t:x.travel,workers:x.workers,workPercent:x.percent||0,commitmentPercent:x.required?x.actual/x.required*100:0,diff:x.actual-x.required,st:'مكتمل',cls:'ok',ccls:'green'})); };
  window.monthlyBaseRowsV59 = window.monthlyRowsV60;
  window.monthlyReportRowsV58 = window.monthlyRowsV60;
  window.renderMonthly = function(){
    const body=$id('monthlyBody'); if(!body) return;
    const table=body.closest('table');
    if(table?.tHead) table.tHead.innerHTML='<tr><th>القسم</th><th>المشرف الحالي</th><th>المشروع</th><th>أسماء العمال</th><th>الوقت بالدقائق</th><th>النسبة</th><th>مجموع الدقائق</th><th>مجموع النسبة</th></tr>';
    const res=buildMonthlyV219();
    let html=''; let last=null, total=0;
    res.daily.forEach(r=>{
      if(last!==null && S(last)!==S(r.s)){ html += `<tr style="background:#f4fbf8;font-weight:900"><td colspan="4">مجموع ${esc(supName(last))}</td><td>${minuteText(total)}</td><td>100.0%</td><td>${minuteText(total)}</td><td>100.0%</td></tr>`; total=0; }
      last=r.s; total+=N(r.actual);
      html += `<tr><td>زيارة يومية</td><td>${esc(r.supervisorName)}</td><td>${esc(r.projectName)}</td><td>${esc(r.workers)}</td><td>${minuteText(r.actual)}</td><td><span class="badge green">${pctText(r.percent)}</span></td><td>${minuteText(r.supTotal)}</td><td><span class="badge green">100.0%</span></td></tr>`;
    });
    if(last!==null) html += `<tr style="background:#f4fbf8;font-weight:900"><td colspan="4">مجموع ${esc(supName(last))}</td><td>${minuteText(total)}</td><td>100.0%</td><td>${minuteText(total)}</td><td>100.0%</td></tr>`;
    if(res.permanent.length){
      html += `<tr style="background:#0a5a49;color:#fff;font-weight:900"><td colspan="8">مشاريع دائمة</td></tr>`;
      res.permanent.forEach(r=>{ html += `<tr><td>مشروع دائم</td><td>${esc(r.supervisorName)}</td><td>${esc(r.projectName)}</td><td>${esc(r.workers)}</td><td>${minuteText(r.actual)}</td><td>—</td><td>${minuteText(r.actual)}</td><td>—</td></tr>`; });
    }
    body.innerHTML = html || '<tr><td colspan="8">لا توجد بيانات في هذا الشهر</td></tr>';
    const dailyTotal=res.daily.reduce((a,r)=>a+N(r.actual),0), permTotal=res.permanent.reduce((a,r)=>a+N(r.actual),0);
    const summary=$id('monthlySummary');
    if(summary) summary.innerHTML=`<div class="kpi"><small>مشاريع زيارة يومية</small><b>${res.daily.length}</b></div><div class="kpi"><small>مشاريع دائمة</small><b>${res.permanent.length}</b></div><div class="kpi"><small>مجموع دقائق اليومية</small><b>${minuteText(dailyTotal)}</b></div><div class="kpi"><small>مجموع نسبة اليومية</small><b>${res.daily.length?'100.0%':'0.0%'}</b></div><div class="kpi"><small>مجموع دقائق الدائمة</small><b>${minuteText(permTotal)}</b></div><div class="kpi"><small>النطاق</small><b>${esc(res.range.start)} إلى ${esc(res.range.end)}</b></div>`;
  };
  window.printMonthlyReportV57 = window.printMonthlyReportV219 = function(){
    const res=buildMonthlyV219();
    if(!res.daily.length && !res.permanent.length){ if(typeof msg==='function') msg('لا توجد بيانات للطباعة','err'); return; }
    const monthLabel=res.month;
    const dailyCards=Object.keys(res.bySup).sort((a,b)=>supName(a).localeCompare(supName(b),'ar')).map(sid=>{
      const rows=res.bySup[sid]; const total=rows.reduce((a,r)=>a+N(r.actual),0);
      const trs=rows.map(r=>`<tr><td>${esc(r.projectName)}</td><td>${minuteText(r.actual)}</td><td>${pctText(r.percent)}</td></tr>`).join('');
      const workerSet=new Map(); rows.forEach(r=>S(r.workers).split('،').map(x=>S(x)).filter(x=>x && x!=='-').forEach(n=>{const k=normName(n); if(!workerSet.has(k)) workerSet.set(k,n);}));
      const workers=[...workerSet.values()].join('، ') || '-';
      return `<div class="sup-card"><div class="sup-head"><b>${esc(supName(sid))}</b><span>المجموع: ${minuteText(total)} / 100%</span></div><table><thead><tr><th>المشروع</th><th>الوقت بالدقائق</th><th>النسبة</th></tr></thead><tbody>${trs}<tr class="total"><td>المجموع</td><td>${minuteText(total)}</td><td>100.0%</td></tr></tbody></table><div class="workers"><b>أسماء العمال بدون تكرار</b><p>${esc(workers)}</p></div></div>`;
    }).join('');
    const permCards=res.permanent.map(r=>`<div class="perm-card"><div class="perm-title">${esc(r.projectName)}</div><div class="perm-sup">المشرف الحالي: ${esc(r.supervisorName)}</div><div class="perm-workers">${esc(r.workers||'-')}</div><div class="perm-time">${minuteText(r.actual)}</div></div>`).join('');
    const dailyTotal=res.daily.reduce((a,r)=>a+N(r.actual),0), permTotal=res.permanent.reduce((a,r)=>a+N(r.actual),0);
    const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;color:#153d33;margin:0;background:#fff;font-size:11px}.page{padding:14px;border:2px solid #0a5a49;min-height:100vh}.top{display:flex;justify-content:space-between;align-items:center;border-bottom:3px solid #0a5a49;padding-bottom:10px}.brand{display:flex;align-items:center;gap:10px}.logo{width:58px;height:58px;border-radius:18px;border:2px solid #c7a24d;display:grid;place-items:center;font-weight:900;color:#0a5a49;background:#f7fbfa}.brand h1{margin:0;font-size:20px}.title{text-align:left}.title h2{font-size:26px;margin:0;color:#0a5a49}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:12px 0}.box{border:1px solid #d9e6e1;border-radius:13px;padding:10px;text-align:center;background:#fff}.box small{display:block;color:#697b74}.box b{font-size:16px;color:#0a5a49}.block{margin-top:12px}.block h2{margin:0 0 8px;text-align:center;color:#fff;background:#0a5a49;border:2px solid #c7a24d;border-radius:999px;padding:7px 35px}.sup-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.sup-card,.perm-card{border:2px solid #0a5a49;border-radius:12px;overflow:hidden;background:#fff;break-inside:avoid}.sup-head{display:flex;justify-content:space-between;padding:8px;background:#f7fbfa;color:#0a5a49;border-bottom:1px solid #d9e6e1}table{width:100%;border-collapse:collapse}th{background:#0a5a49;color:white;padding:6px}td{border:1px solid #e2ece8;padding:5px;text-align:center}.total{font-weight:900;background:#f4fbf8}.workers{padding:8px;text-align:center}.workers p{margin:4px 0 0;line-height:1.7}.perm-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.perm-card{text-align:center;padding-bottom:8px}.perm-title{background:#0a5a49;color:#fff;padding:10px;font-weight:900}.perm-sup{padding:7px;color:#0a5a49;font-weight:700}.perm-workers{min-height:55px;padding:7px;line-height:1.7}.perm-time{display:inline-block;border-radius:999px;background:#edf8f3;color:#0a5a49;padding:5px 15px;font-weight:900}.empty{grid-column:1/-1;text-align:center;border:1px dashed #aabdb6;border-radius:12px;padding:20px;color:#697b74}.foot{display:grid;grid-template-columns:1fr 1fr 1fr;gap:10px;margin-top:14px}.sign{border:1px solid #d9e6e1;border-radius:12px;padding:10px;min-height:70px}.note{margin-top:8px;text-align:center;color:#697b74}@media print{body{-webkit-print-color-adjust:exact;print-color-adjust:exact}.page{border-radius:0}.sup-card,.perm-card{break-inside:avoid}}</style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h1>شركة تصنيف لإدارة المرافق</h1><small>تقرير تشغيل شهري</small></div></div><div class="title"><h2>تقرير الأوقات الشهرية</h2><p>الشهر: ${esc(monthLabel)}</p></div></div><div class="meta"><div class="box"><small>مشاريع زيارة يومية</small><b>${res.daily.length}</b></div><div class="box"><small>مشاريع دائمة</small><b>${res.permanent.length}</b></div><div class="box"><small>مجموع دقائق اليومية</small><b>${minuteText(dailyTotal)}</b></div><div class="box"><small>مجموع دقائق الدائمة</small><b>${minuteText(permTotal)}</b></div></div><section class="block"><h2>مشاريع زيارة يومية</h2><div class="sup-grid">${dailyCards || '<div class="empty">لا توجد بيانات</div>'}</div></section><section class="block"><h2>مشاريع دائمة</h2><div class="perm-grid">${permCards || '<div class="empty">لا توجد مشاريع دائمة خلال هذا الشهر</div>'}</div></section><div class="foot"><div class="sign"><b>إعداد</b><br><br>الاسم: ____________</div><div class="sign"><b>مراجعة</b><br><br>الاسم: ____________</div><div class="sign"><b>اعتماد</b><br><br>الاسم: ____________</div></div><div class="note">ملاحظة: مشاريع الزيارة اليومية تُحسب نسبتها داخل كل مشرف بحيث يكون مجموع النسبة 100%. والتصنيف يعتمد على المشرف الحالي للمشروع.</div></div><script>window.onload=function(){setTimeout(function(){window.print()},450)}<\/script></body></html>`;
    const w=window.open('','_blank'); if(!w){ if(typeof msg==='function') msg('اسمح بالنوافذ المنبثقة للطباعة','err'); return; } w.document.open(); w.document.write(html); w.document.close();
  };
  window.exportMonthlyCSV = function(){ const res=buildMonthlyV219(); const csv=['القسم,المشرف الحالي,المشروع,أسماء العمال,الوقت بالدقائق,النسبة,مجموع دقائق المشرف,مجموع النسبة',...res.daily.map(r=>['زيارة يومية',r.supervisorName,r.projectName,r.workers,Math.round(N(r.actual)),pctText(r.percent),Math.round(N(r.supTotal)),pctText(100)].map(x=>'"'+S(x).replace(/"/g,'""')+'"').join(',')),...res.permanent.map(r=>['مشروع دائم',r.supervisorName,r.projectName,r.workers,Math.round(N(r.actual)),'',Math.round(N(r.actual)),''].map(x=>'"'+S(x).replace(/"/g,'""')+'"').join(','))].join('\n'); if(typeof download==='function') download('monthly-v219.csv',csv); };

  // Workers section: display without duplicate rows and prevent saving exact duplicate names for same project/supervisor.
  window.renderWorkers = function(){
    const b=$id('workersBody'); if(!b) return;
    const s=$id('workerFilterSupervisor')?.value, p=$id('workerFilterProject')?.value, st=$id('workerFilterStatus')?.value, tp=$id('workerFilterType')?.value, q=S($id('workerSearch')?.value);
    let rows=[...(window.data?.workers||[])];
    rows=rows.filter(w=>S(w.status||'active').toLowerCase()!=='deleted');
    const dedup=new Map();
    rows.forEach(w=>{
      let sid='', pid=''; try{ sid=(typeof workerSupId==='function')?workerSupId(w):(w.app_supervisor_id||w.supervisor_id||''); }catch(_){ sid=w.app_supervisor_id||w.supervisor_id||''; }
      try{ pid=(typeof workerProjectId==='function')?workerProjectId(w):(w.project_id||w.assigned_project_id||''); }catch(_){ pid=w.project_id||w.assigned_project_id||''; }
      const key=normName(w.name||w.full_name)+'|'+S(w.phone||'')+'|'+S(sid)+'|'+S(pid);
      if(!dedup.has(key)) dedup.set(key,w);
      else{
        const old=dedup.get(key);
        if(N(w.id)>N(old.id)) dedup.set(key,w);
      }
    });
    rows=[...dedup.values()];
    if(s) rows=rows.filter(w=>{try{return S(workerSupId(w))===S(s)}catch(_){return S(w.app_supervisor_id||w.supervisor_id)===S(s)}});
    if(p) rows=rows.filter(w=>{try{return S(workerProjectId(w))===S(p)}catch(_){return S(w.project_id||w.assigned_project_id)===S(p)}});
    if(st) rows=rows.filter(w=>S(w.status||'active')===S(st));
    if(tp) rows=rows.filter(w=>S(w.worker_type||'primary')===S(tp));
    if(q) rows=rows.filter(w=>[w.name,w.phone,supName((typeof workerSupId==='function'?workerSupId(w):w.supervisor_id)),projName((typeof workerProjectId==='function'?workerProjectId(w):w.project_id)),w.notes].join(' ').includes(q));
    b.innerHTML=rows.map(w=>{ const sid=(typeof workerSupId==='function'?workerSupId(w):(w.app_supervisor_id||w.supervisor_id)); const pid=(typeof workerProjectId==='function'?workerProjectId(w):(w.project_id||w.assigned_project_id)); return `<tr><td>${esc(w.name)}</td><td>${esc(supName(sid))}</td><td>${esc(projName(pid))}</td><td><span class="badge ${w.worker_type==='support'?'amber':'green'}">${typeof workerTypeText==='function'?workerTypeText(w.worker_type):'أساسي'}</span></td><td>${esc(w.phone)}</td><td>${w.salary||0}</td><td><span class="badge ${w.status==='inactive'?'red':'green'}">${w.status==='inactive'?'موقوف':'نشط'}</span></td><td>${esc(w.notes||'-')}</td><td class="row-actions"><button onclick="editWorker(${w.id})">تعديل</button><button class="light" onclick="toggleWorkerStatus(${w.id})">${w.status==='inactive'?'تفعيل':'إيقاف'}</button><button class="danger" onclick="deleteRow('workers',${w.id})">حذف</button></td></tr>`; }).join('') || '<tr><td colspan="9">لا توجد بيانات</td></tr>';
  };
  const oldSaveWorker=window.saveWorker;
  window.saveWorker = async function(){
    const id=S($id('workerId')?.value); const name=S($id('workerName')?.value); const sup=S($id('workerSupervisor')?.value); const pid=S($id('workerProject')?.value);
    const key=normName(name)+'|'+sup+'|'+pid;
    const exists=(window.data?.workers||[]).find(w=>S(w.id)!==id && S(w.status||'active').toLowerCase()!=='deleted' && normName(w.name||w.full_name)+'|'+S(w.app_supervisor_id||w.supervisor_id||'')+'|'+S(w.project_id||w.assigned_project_id||'')===key);
    if(exists){ if(typeof msg==='function') msg('هذا العامل موجود بالفعل لنفس المشرف والمشروع، تم منع التكرار','err'); return; }
    return oldSaveWorker ? oldSaveWorker.apply(this,arguments) : undefined;
  };
  function updateVersion(){ document.querySelectorAll('h1,h2,h3,.version,.app-version').forEach(el=>{ if(S(el.textContent).includes('V218')) el.textContent=el.textContent.replace('V218','V219'); if(S(el.textContent).includes('V217')) el.textContent=el.textContent.replace('V217','V219'); }); }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(()=>{try{updateVersion(); window.renderMonthly&&window.renderMonthly(); window.renderWorkers&&window.renderWorkers();}catch(e){}},ev==='load'?1200:400)));
  setTimeout(()=>{try{updateVersion(); window.renderMonthly&&window.renderMonthly();}catch(e){}},1600);
  console.log('Tasneef V219 monthly first-style report and worker de-dup loaded');
})();


/* ===== V220: Workers list grouped by worker name, with projects combined ===== */
(function(){
  if(window.__tasneefV220WorkersGrouped) return;
  window.__tasneefV220WorkersGrouped = true;
  const $id = (id)=>document.getElementById(id);
  const S = (v)=>String(v ?? '').trim();
  const N = (v)=>Number(v || 0);
  const esc2 = (v)=>{ try{ return (typeof esc==='function') ? esc(v) : S(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])); }catch(_){ return S(v); } };
  function normWorkerKey(name){
    return S(name).toLowerCase()
      .replace(/[\u064B-\u065F\u0670]/g,'')
      .replace(/[أإآا]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه')
      .replace(/\s+/g,' ')
      .trim();
  }
  function getSupId(w){ try{ return (typeof workerSupId==='function') ? workerSupId(w) : (w.app_supervisor_id||w.supervisor_id||''); }catch(_){ return w.app_supervisor_id||w.supervisor_id||''; } }
  function getProjId(w){ try{ return (typeof workerProjectId==='function') ? workerProjectId(w) : (w.project_id||w.assigned_project_id||''); }catch(_){ return w.project_id||w.assigned_project_id||''; } }
  function supText(id){ try{ return (typeof supervisorName==='function') ? supervisorName(id) : (typeof supName==='function'?supName(id):id); }catch(_){ return id||'-'; } }
  function projText(id){ try{ return (typeof projectName==='function') ? projectName(id) : (typeof projName==='function'?projName(id):id); }catch(_){ return id||'-'; } }
  function typeText(t){ try{ return (typeof workerTypeText==='function') ? workerTypeText(t) : (t==='support'?'بديل / مساند':'أساسي'); }catch(_){ return t==='support'?'بديل / مساند':'أساسي'; } }
  function uniquePush(map, key, value){ key=S(key); value=S(value); if(key && !map.has(key)) map.set(key,value||key); }
  function buildWorkerGroupsV220(){
    const s=$id('workerFilterSupervisor')?.value || '';
    const p=$id('workerFilterProject')?.value || '';
    const st=$id('workerFilterStatus')?.value || '';
    const tp=$id('workerFilterType')?.value || '';
    const q=S($id('workerSearch')?.value);
    let rows=[...(window.data?.workers||[])].filter(w=>S(w.status||'active').toLowerCase()!=='deleted');
    if(s) rows=rows.filter(w=>S(getSupId(w))===S(s));
    if(p) rows=rows.filter(w=>S(getProjId(w))===S(p));
    if(st) rows=rows.filter(w=>S(w.status||'active')===S(st));
    if(tp) rows=rows.filter(w=>S(w.worker_type||'primary')===S(tp));
    const groups=new Map();
    rows.forEach(w=>{
      const sid=getSupId(w), pid=getProjId(w);
      const name=S(w.name||w.full_name);
      const phone=S(w.phone||w.mobile||'');
      const key=[normWorkerKey(name), phone, S(sid)].join('|');
      if(!groups.has(key)){
        groups.set(key,{rep:w, ids:[], name:name, phone:phone, sid:sid, projects:new Map(), types:new Map(), statuses:new Map(), notes:[], salary:w.salary||0});
      }
      const g=groups.get(key);
      g.ids.push(w.id);
      if(N(w.id)>N(g.rep.id)) g.rep=w;
      if(!g.phone && phone) g.phone=phone;
      if(!g.salary && w.salary) g.salary=w.salary;
      uniquePush(g.projects, pid, projText(pid));
      uniquePush(g.types, S(w.worker_type||'primary'), typeText(w.worker_type||'primary'));
      uniquePush(g.statuses, S(w.status||'active'), w.status==='inactive'?'موقوف':'نشط');
      if(w.notes && !g.notes.includes(S(w.notes))) g.notes.push(S(w.notes));
    });
    let out=[...groups.values()];
    if(q){
      out=out.filter(g=>[g.name,g.phone,supText(g.sid),[...g.projects.values()].join(' '),g.notes.join(' ')].join(' ').includes(q));
    }
    out.sort((a,b)=>a.name.localeCompare(b.name,'ar') || supText(a.sid).localeCompare(supText(b.sid),'ar'));
    return out;
  }
  window.renderWorkers = function(){
    const b=$id('workersBody'); if(!b) return;
    const groups=buildWorkerGroupsV220();
    b.innerHTML=groups.map(g=>{
      const types=[...g.types.values()].join('، ') || 'أساسي';
      const projects=[...g.projects.values()].filter(Boolean).join('، ') || '-';
      const statusVals=[...g.statuses.keys()];
      const isInactive=statusVals.length===1 && statusVals[0]==='inactive';
      const statusText = isInactive ? 'موقوف' : 'نشط';
      const notes=g.notes.join(' / ') || (g.ids.length>1 ? `مرتبط بـ ${g.ids.length} مشاريع` : '-');
      const repId=g.rep?.id;
      return `<tr>
        <td>${esc2(g.name)}</td>
        <td>${esc2(supText(g.sid))}</td>
        <td>${esc2(projects)}</td>
        <td><span class="badge ${types.includes('بديل')?'amber':'green'}">${esc2(types)}</span></td>
        <td>${esc2(g.phone)}</td>
        <td>${g.salary||0}</td>
        <td><span class="badge ${isInactive?'red':'green'}">${statusText}</span></td>
        <td>${esc2(notes)}</td>
        <td class="row-actions"><button onclick="editWorker(${repId})">تعديل</button><button class="light" onclick="toggleWorkerStatus(${repId})">${isInactive?'تفعيل':'إيقاف'}</button><button class="danger" onclick="deleteRow('workers',${repId})">حذف</button></td>
      </tr>`;
    }).join('') || '<tr><td colspan="9">لا توجد بيانات</td></tr>';
  };
  const previousSaveWorkerV220 = window.saveWorker;
  window.saveWorker = async function(){
    const id=S($id('workerId')?.value);
    const name=S($id('workerName')?.value);
    const sup=S($id('workerSupervisor')?.value);
    const pid=S($id('workerProject')?.value);
    if(name && sup && pid){
      const key=normWorkerKey(name)+'|'+S(sup)+'|'+S(pid);
      const exists=(window.data?.workers||[]).find(w=>S(w.id)!==id && S(w.status||'active').toLowerCase()!=='deleted' && (normWorkerKey(w.name||w.full_name)+'|'+S(getSupId(w))+'|'+S(getProjId(w))===key));
      if(exists){ if(typeof msg==='function') msg('هذا العامل موجود بالفعل في نفس المشروع ونفس المشرف، لن يتم تكراره','err'); return; }
    }
    return previousSaveWorkerV220 ? previousSaveWorkerV220.apply(this,arguments) : undefined;
  };
  function updateVersionV220(){
    document.querySelectorAll('h1,h2,h3,.version,.app-version').forEach(el=>{ const t=S(el.textContent); if(t.includes('V219')) el.textContent=t.replace('V219','V220'); if(t.includes('V218')) el.textContent=t.replace('V218','V220'); if(t.includes('V217')) el.textContent=t.replace('V217','V220'); });
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(()=>{try{updateVersionV220(); window.renderWorkers&&window.renderWorkers();}catch(e){}},ev==='load'?1300:500)));
  setTimeout(()=>{try{updateVersionV220(); window.renderWorkers&&window.renderWorkers();}catch(e){}},1800);
  console.log('Tasneef V220 workers grouped list loaded');
})();

/* ===== V222: Smart Export / Import Center - one meeting Excel workbook ===== */
(function(){
  if(window.__tasneefExportImportV222) return;
  window.__tasneefExportImportV222 = true;
  const byId = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const N = v => { const n=Number(String(v??0).replace(/,/g,'')); return Number.isFinite(n)?n:0; };
  const dset = () => window.data || data || {};
  const safe = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&apos;'}[c]));
  const labelDate = () => new Date().toLocaleString('ar-SA');
  const todayStr = () => new Date().toISOString().slice(0,10);
  function pName(id){ try{return projectName(id)}catch(e){return (dset().projects||[]).find(p=>String(p.id)===String(id))?.name||'-';} }
  function sName(id){ try{return supervisorName(id)}catch(e){return (dset().users||[]).find(u=>String(u.id)===String(id))?.full_name||'-';} }
  function wProjectId(w){ try{return workerProjectId(w)}catch(e){return w.project_id || w.assigned_project_id || '';} }
  function wSupId(w){ try{return workerSupId(w)}catch(e){return w.supervisor_id || w.assigned_supervisor_id || '';} }
  function opText(t){ try{return projectOperationText(t)}catch(e){return t==='full_time'?'دائم':(t==='as_needed'?'حسب الحاجة':'زيارة يومية');} }
  function statusText(v){ v=String(v||'active'); return ['inactive','closed','disabled'].includes(v)?'متوقف':'نشط'; }
  function mins(a,b){ try{return minutesBetween(a,b)}catch(e){ if(!a||!b) return 0; return Math.max(0,Math.round((new Date(b)-new Date(a))/60000)); } }
  function fmtDate(v){ if(!v) return '-'; try{return new Date(v).toLocaleString('ar-SA')}catch(e){return S(v);} }
  function inMonth(dateVal, month){ if(!month) return true; const s=S(dateVal); if(s.startsWith(month)) return true; if(!s) return false; try{return new Date(dateVal).toISOString().slice(0,7)===month}catch(e){return false;} }
  function currentFilters(){ return {month: byId('meetingExportMonth')?.value || byId('monthlyMonth')?.value || todayStr().slice(0,7), supervisor: byId('meetingExportSupervisor')?.value || '', project: byId('meetingExportProject')?.value || ''}; }
  function filterProjects(){ const {supervisor, project}=currentFilters(); return (dset().projects||[]).filter(p=>(!project||String(p.id)===String(project)) && (!supervisor||String(p.supervisor_id)===String(supervisor))); }
  function allowedProjectIds(){ return new Set(filterProjects().map(p=>String(p.id))); }
  async function ensureExportData(){
    try{ if(typeof refreshAll==='function') await refreshAll(); }catch(e){}
    try{ if(typeof financeLoadAll==='function') await financeLoadAll(false); else if(window.financeLoadAll) await window.financeLoadAll(false); }catch(e){ console.warn('finance load export', e.message); }
    try{ if(window.loadPremiumReportsOnly) await window.loadPremiumReportsOnly(false); else if(typeof loadPremiumReportsOnly==='function') await loadPremiumReportsOnly(false); }catch(e){ console.warn('client reports load export', e.message); }
  }
  function hydrateExportCenterV222(){
    const month=byId('meetingExportMonth'); if(month && !month.value) month.value=(byId('monthlyMonth')?.value || todayStr().slice(0,7));
    const sup=byId('meetingExportSupervisor'); if(sup){ const cur=sup.value; sup.innerHTML='<option value="">كل المشرفين</option>'+(dset().supervisors||[]).map(u=>`<option value="${safe(u.id)}">${safe(u.full_name||u.username)}</option>`).join(''); sup.value=cur; }
    const pro=byId('meetingExportProject'); if(pro){ const cur=pro.value; pro.innerHTML='<option value="">كل المشاريع</option>'+(dset().projects||[]).map(p=>`<option value="${safe(p.id)}">${safe(p.name)}</option>`).join(''); pro.value=cur; }
  }
  window.hydrateExportCenterV222 = hydrateExportCenterV222;

  function filteredLogs(){ const f=currentFilters(), pids=allowedProjectIds(); return (dset().logs||[]).filter(l=>inMonth(l.log_date||l.check_in,f.month) && (!f.project || String(l.project_id)===String(f.project)) && (!f.supervisor || pids.has(String(l.project_id)))); }
  function filteredTickets(){ const f=currentFilters(), pids=allowedProjectIds(); return (dset().tickets||[]).filter(t=>inMonth(t.created_at||t.opened_at||t.date,f.month) && (!f.project || String(t.project_id)===String(f.project)) && (!f.supervisor || pids.has(String(t.project_id)))); }
  function filteredAttendance(){ const f=currentFilters(); const workers=dset().workers||[]; return (dset().attendance||[]).filter(a=>{ if(!inMonth(a.attendance_date||a.date,f.month)) return false; if(f.supervisor){ const w=workers.find(x=>String(x.id)===String(a.worker_id))||{}; const sid=a.supervisor_id || w.supervisor_id || w.assigned_supervisor_id; if(String(sid)!==String(f.supervisor)) return false; } return true; }); }
  function filteredExpenses(){ const f=currentFilters(), pids=allowedProjectIds(); return (dset().financeExpenses||[]).filter(e=>inMonth(e.expense_date||e.created_at,f.month) && (!f.project || String(e.project_id)===String(f.project)) && (!f.supervisor || pids.has(String(e.project_id||'')))); }
  function filteredMovements(){ const f=currentFilters(), pids=allowedProjectIds(); return (dset().inventoryMovements||[]).filter(m=>inMonth(m.movement_date||m.created_at,f.month) && (!f.project || String(m.project_id)===String(f.project)) && (!f.supervisor || !m.project_id || pids.has(String(m.project_id)))); }
  function filteredClientReports(){ const f=currentFilters(), pids=allowedProjectIds(); return (dset().clientReports||[]).filter(r=>inMonth(r.report_date||r.created_at,f.month) && (!f.project || String(r.project_id)===String(f.project)) && (!f.supervisor || pids.has(String(r.project_id||'')))); }
  function filteredRatings(){ const f=currentFilters(), reports=new Set(filteredClientReports().map(r=>String(r.id))), pids=allowedProjectIds(); return (dset().clientServiceRatings||[]).filter(r=>inMonth(r.created_at||r.rating_date,f.month) && (!f.supervisor || !r.project_id || pids.has(String(r.project_id))) && (!f.project || String(r.project_id)===String(f.project) || reports.has(String(r.report_id)))); }

  function sheetXml(name, rows, options={}){
    const title=safe(options.title || name), subtitle=safe(options.subtitle || ''), headers=rows[0]||[];
    const bodyRows=rows.slice(1);
    const colCount=Math.max(1, headers.length);
    let xml=`<Worksheet ss:Name="${safe(name).slice(0,31)}" ss:RightToLeft="1"><Table ss:ExpandedColumnCount="${colCount}" ss:ExpandedRowCount="${bodyRows.length+4}" x:FullColumns="1" x:FullRows="1">`;
    for(let i=0;i<colCount;i++) xml+=`<Column ss:AutoFitWidth="0" ss:Width="120"/>`;
    xml+=`<Row ss:Height="30"><Cell ss:MergeAcross="${Math.max(0,colCount-1)}" ss:StyleID="Title"><Data ss:Type="String">${title}</Data></Cell></Row>`;
    xml+=`<Row><Cell ss:MergeAcross="${Math.max(0,colCount-1)}" ss:StyleID="SubTitle"><Data ss:Type="String">${subtitle}</Data></Cell></Row>`;
    xml+=`<Row/>`;
    xml+=`<Row>${headers.map(h=>`<Cell ss:StyleID="Header"><Data ss:Type="String">${safe(h)}</Data></Cell>`).join('')}</Row>`;
    bodyRows.forEach((r,idx)=>{ xml+=`<Row>${headers.map((_,i)=>{ const v=r[i]; const isNum=typeof v==='number' && Number.isFinite(v); return `<Cell ss:StyleID="${idx%2?'DataAlt':'Data'}"><Data ss:Type="${isNum?'Number':'String'}">${safe(v)}</Data></Cell>`; }).join('')}</Row>`; });
    xml+=`</Table><WorksheetOptions xmlns="urn:schemas-microsoft-com:office:excel"><DisplayRightToLeft/><FreezePanes/><FrozenNoSplit/><SplitHorizontal>4</SplitHorizontal><TopRowBottomPane>4</TopRowBottomPane><ActivePane>2</ActivePane></WorksheetOptions></Worksheet>`;
    return xml;
  }
  function makeWorkbook(sheets){
    const styles=`<Styles>
      <Style ss:ID="Default" ss:Name="Normal"><Alignment ss:Vertical="Center" ss:Horizontal="Right" ss:WrapText="1"/><Font ss:FontName="Arial" ss:Size="10"/></Style>
      <Style ss:ID="Title"><Alignment ss:Horizontal="Center" ss:Vertical="Center"/><Font ss:FontName="Arial" ss:Size="16" ss:Bold="1" ss:Color="#FFFFFF"/><Interior ss:Color="#1F6B3A" ss:Pattern="Solid"/></Style>
      <Style ss:ID="SubTitle"><Alignment ss:Horizontal="Center" ss:Vertical="Center"/><Font ss:FontName="Arial" ss:Size="10" ss:Color="#335544"/><Interior ss:Color="#EAF5ED" ss:Pattern="Solid"/></Style>
      <Style ss:ID="Header"><Alignment ss:Horizontal="Center" ss:Vertical="Center" ss:WrapText="1"/><Font ss:FontName="Arial" ss:Size="10" ss:Bold="1" ss:Color="#FFFFFF"/><Interior ss:Color="#2E7D4F" ss:Pattern="Solid"/><Borders><Border ss:Position="Bottom" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#BBD7C4"/><Border ss:Position="Left" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#BBD7C4"/><Border ss:Position="Right" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#BBD7C4"/><Border ss:Position="Top" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#BBD7C4"/></Borders></Style>
      <Style ss:ID="Data"><Alignment ss:Horizontal="Right" ss:Vertical="Center" ss:WrapText="1"/><Interior ss:Color="#FFFFFF" ss:Pattern="Solid"/><Borders><Border ss:Position="Bottom" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#E5EFE8"/></Borders></Style>
      <Style ss:ID="DataAlt"><Alignment ss:Horizontal="Right" ss:Vertical="Center" ss:WrapText="1"/><Interior ss:Color="#F8FCF9" ss:Pattern="Solid"/><Borders><Border ss:Position="Bottom" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#E5EFE8"/></Borders></Style>
    </Styles>`;
    return `<?xml version="1.0" encoding="UTF-8"?><?mso-application progid="Excel.Sheet"?>`+
      `<Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:x="urn:schemas-microsoft-com:office:excel" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet" xmlns:html="http://www.w3.org/TR/REC-html40">${styles}${sheets.join('')}</Workbook>`;
  }
  function downloadXmlWorkbook(filename, sheets){
    const xml=makeWorkbook(sheets);
    const blob=new Blob([xml],{type:'application/vnd.ms-excel;charset=utf-8;'});
    const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=filename; document.body.appendChild(a); a.click(); setTimeout(()=>{URL.revokeObjectURL(a.href); a.remove();},1200);
  }
  function executiveRows(){
    const projects=filterProjects(), logs=filteredLogs(), tickets=filteredTickets(), exp=filteredExpenses(), items=dset().inventoryItems||[], reports=filteredClientReports(), ratings=filteredRatings();
    const open=tickets.filter(t=>!['closed','done','مغلق'].includes(String(t.status||'').toLowerCase())).length;
    const closed=tickets.length-open;
    const totalMins=logs.reduce((a,l)=>a+N(l.duration_minutes||mins(l.check_in,l.check_out)),0);
    const expTotal=exp.reduce((a,e)=>a+N(e.total||e.amount),0);
    const stockValue=items.reduce((a,i)=>a+N(i.quantity)*N(i.unit_cost||i.cost||i.price),0);
    return [['المؤشر','القيمة','ملاحظة'],['تاريخ التصدير',labelDate(),''],['الشهر',currentFilters().month||'كل الأشهر',''],['عدد المشاريع',projects.length,'حسب الفلتر الحالي'],['عدد المشرفين',(dset().supervisors||[]).length,''],['عدد العمال',(dset().workers||[]).length,'بدون تكرار في شاشة العمال'],['سجلات التشغيل',logs.length,''],['إجمالي دقائق التشغيل',Math.round(totalMins),''],['التكتات',tickets.length,`مفتوحة: ${open} / مغلقة: ${closed}`],['المصروفات',exp.length,expTotal],['أصناف المخزون',items.length,stockValue],['تقارير العملاء',reports.length,''],['تقييمات العملاء',ratings.length,'']];
  }
  function projectsRows(){ return [['اسم المشروع','المشرف الحالي','نوع التشغيل','الحالة','الدقائق اليومية','دقائق الجمعة','الموقع','ملاحظات'], ...filterProjects().map(p=>[p.name,sName(p.supervisor_id),opText(p.operation_type),statusText(p.status),N(p.required_daily_minutes||180),N(p.friday_minutes||90),p.location||'',p.notes||''])]; }
  function supervisorsRows(){ const projects=filterProjects(), tickets=filteredTickets(); return [['المشرف','عدد المشاريع الحالية','عدد العمال','التكتات المفتوحة','التكتات المغلقة'], ...(dset().supervisors||[]).filter(s=>!currentFilters().supervisor||String(s.id)===String(currentFilters().supervisor)).map(s=>{ const pids=projects.filter(p=>String(p.supervisor_id)===String(s.id)).map(p=>String(p.id)); const workers=(dset().workers||[]).filter(w=>pids.includes(String(wProjectId(w)))||String(wSupId(w))===String(s.id)); const ts=tickets.filter(t=>pids.includes(String(t.project_id))); const open=ts.filter(t=>!['closed','done','مغلق'].includes(String(t.status||'').toLowerCase())).length; return [s.full_name||s.username,pids.length,workers.length,open,ts.length-open]; })]; }
  function workersRows(){ const pids=allowedProjectIds(); const map=new Map(); (dset().workers||[]).forEach(w=>{ const pid=String(wProjectId(w)||''); if(currentFilters().project && pid!==String(currentFilters().project)) return; if(currentFilters().supervisor && !pids.has(pid) && String(wSupId(w))!==String(currentFilters().supervisor)) return; const key=S(w.name).toLowerCase()+'|'+S(w.phone); if(!map.has(key)) map.set(key,{name:w.name,phone:w.phone,supervisor:sName(wSupId(w)),projects:new Set(),status:w.status,notes:w.notes}); const rec=map.get(key); if(pid) rec.projects.add(pName(pid)); }); return [['اسم العامل','الجوال','المشرف','المشاريع','الحالة','ملاحظات'], ...[...map.values()].map(w=>[w.name,w.phone||'',w.supervisor,[...w.projects].join('، '),statusText(w.status),w.notes||''])]; }
  function logsRows(){ return [['التاريخ','المشروع','المشرف الحالي','وقت الدخول','وقت الخروج','الدقائق','وقت الانتقال','حالة الوقت','ملاحظات'], ...filteredLogs().map(l=>[l.log_date||S(l.check_in).slice(0,10),pName(l.project_id),sName((dset().projects||[]).find(p=>String(p.id)===String(l.project_id))?.supervisor_id||l.supervisor_id),fmtDate(l.check_in),fmtDate(l.check_out),N(l.duration_minutes||mins(l.check_in,l.check_out)),N(l.travel_minutes),l.time_status||'',l.notes||''])]; }
  function attendanceRows(){ const workers=dset().workers||[]; return [['التاريخ','العامل','المشرف','الحالة','ملاحظات'], ...filteredAttendance().map(a=>{ const w=workers.find(x=>String(x.id)===String(a.worker_id))||{}; const sid=a.supervisor_id || w.supervisor_id || w.assigned_supervisor_id; return [a.attendance_date||a.date, w.name||a.worker_name||'-', sName(sid), a.status==='present'?'حاضر':(a.status==='absent'?'غائب':(a.status||'')), a.notes||'']; })]; }
  function monthlyRows(){
    let rows=[['القسم','المشرف الحالي','المشروع','العمال','الدقائق','النسبة','مجموع دقائق المشرف','مجموع النسبة']];
    try{ if(typeof buildMonthlyV219==='function'){ const res=buildMonthlyV219(); [...(res.daily||[]),(res.permanent||[])].forEach(r=>{ if(currentFilters().supervisor && String((dset().projects||[]).find(p=>p.name===r.projectName)?.supervisor_id||'')!==String(currentFilters().supervisor)) return; rows.push([res.daily?.includes(r)?'زيارة يومية':'مشروع دائم',r.supervisorName,r.projectName,r.workers||'',Math.round(N(r.actual)),r.percent?String(Math.round(N(r.percent)*10)/10)+'%':'',Math.round(N(r.supTotal||r.actual)),r.percent?'100%':'']); }); return rows; } }catch(e){}
    const map={}; filteredLogs().forEach(l=>{ const p=(dset().projects||[]).find(x=>String(x.id)===String(l.project_id))||{}; const k=p.id||l.project_id; map[k]=map[k]||{project:p.name||pName(l.project_id),sup:sName(p.supervisor_id||l.supervisor_id),mins:0,type:p.operation_type==='full_time'?'مشروع دائم':'زيارة يومية'}; map[k].mins+=N(l.duration_minutes||mins(l.check_in,l.check_out)); }); Object.values(map).forEach(r=>rows.push([r.type,r.sup,r.project,'',Math.round(r.mins),'','',''])); return rows;
  }
  function ticketsRows(){ return [['رقم التكت','المشروع','المشرف الحالي','رفع بواسطة','العنوان','الحالة','الأولوية','تاريخ الفتح','تاريخ الإغلاق','ملاحظات'], ...filteredTickets().map(t=>{ const p=(dset().projects||[]).find(x=>String(x.id)===String(t.project_id))||{}; const creator=t.created_by_name||t.created_by||t.reported_by||t.requested_by||''; return [t.id,pName(t.project_id),sName(p.supervisor_id||t.supervisor_id),creator,t.title||t.subject||t.description||'',t.status||'',t.priority||'',fmtDate(t.created_at||t.opened_at),fmtDate(t.closed_at),t.notes||t.description||'']; })]; }
  function inventoryRows(){ return [['الصنف','الكود','التصنيف','المورد','الوحدة','الكمية الحالية','الحد الأدنى','متوسط التكلفة','قيمة المتبقي'], ...(dset().inventoryItems||[]).map(i=>[i.name,i.product_code||i.serial_number||i.barcode||'',i.category||i.item_type||'',i.supplier||'',i.unit||'',N(i.quantity),N(i.min_quantity),N(i.unit_cost||i.cost||i.price),N(i.quantity)*N(i.unit_cost||i.cost||i.price)])]; }
  function movementRows(){ return [['التاريخ','الصنف','نوع الحركة','الكمية','المشروع','المستلم','السبب','ملاحظات'], ...filteredMovements().map(m=>[m.movement_date||S(m.created_at).slice(0,10),m.item_name||((dset().inventoryItems||[]).find(i=>String(i.id)===String(m.item_id))?.name||''),m.movement_type,N(m.quantity),m.project_name||pName(m.project_id),m.receiver||'',m.reason||'',m.notes||''])]; }
  function expensesRows(){ return [['التاريخ','المشروع','التصنيف','المورد','المبلغ','طريقة الدفع','ملاحظات'], ...filteredExpenses().map(e=>[e.expense_date||S(e.created_at).slice(0,10),e.project_name||pName(e.project_id),e.category||e.expense_type||'',e.supplier||'',N(e.total||e.amount),e.payment_method||'',e.notes||''])]; }
  function clientReportsRows(){ return [['التاريخ','المشروع','الشهر','المشرف','عنوان التقرير','الحالة','الخدمات','التقييم'], ...filteredClientReports().map(r=>[r.report_date||S(r.created_at).slice(0,10),r.project_name||pName(r.project_id),S(r.report_date||r.created_at).slice(0,7),sName(r.supervisor_id),r.title||r.report_no||'',r.status||'',(dset().clientReportServices||[]).filter(s=>String(s.report_id)===String(r.id)).map(s=>s.service_name||s.service_type).filter(Boolean).join('، '), ''])]; }
  function ratingsRows(){ return [['التاريخ','المشروع','الخدمة','التقييم','ملاحظة العميل','تحتاج متابعة','الحالة'], ...filteredRatings().map(r=>[fmtDate(r.created_at||r.rating_date),r.project_name||pName(r.project_id),r.service_name||r.service_type||'',r.rating||r.score||'',r.comment||r.notes||'',r.need_followup||r.needs_follow_up?'نعم':'لا',r.status||''])]; }
  function alertsRows(){
    const projectsNoSup=(dset().projects||[]).filter(p=>!p.supervisor_id).map(p=>['مشروع بدون مشرف',p.name,'حرج','يحتاج ربط مشرف']);
    const workersNoSup=(dset().workers||[]).filter(w=>!wSupId(w)).map(w=>['عامل بدون مشرف',w.name,'عادي','يحتاج ربط مشرف']);
    const openLogs=(dset().logs||[]).filter(l=>l.check_in&&!l.check_out).map(l=>['دخول بدون خروج',pName(l.project_id),'حرج',sName(l.supervisor_id)]);
    const openTickets=filteredTickets().filter(t=>!['closed','done','مغلق'].includes(String(t.status||'').toLowerCase())).map(t=>['تكت مفتوح',pName(t.project_id),'عادي',t.title||t.subject||'']);
    return [['نوع التنبيه','المشروع/العنصر','الأهمية','الملاحظة'], ...projectsNoSup, ...workersNoSup, ...openLogs, ...openTickets];
  }
  function exportSheets(){ const sub=`شركة تصنيف لإدارة المرافق - تاريخ التصدير ${labelDate()} - الفلتر: ${currentFilters().month||'كل الأشهر'}`; return [
      sheetXml('الملخص التنفيذي', executiveRows(), {title:'الملخص التنفيذي للاجتماع', subtitle:sub}),
      sheetXml('المشاريع', projectsRows(), {title:'المشاريع', subtitle:sub}),
      sheetXml('المشرفون', supervisorsRows(), {title:'المشرفون', subtitle:sub}),
      sheetXml('العمال', workersRows(), {title:'العمال', subtitle:sub}),
      sheetXml('السجلات اليومية', logsRows(), {title:'السجلات اليومية', subtitle:sub}),
      sheetXml('الحضور والغياب', attendanceRows(), {title:'الحضور والغياب', subtitle:sub}),
      sheetXml('الأوقات الشهرية', monthlyRows(), {title:'الأوقات الشهرية', subtitle:sub}),
      sheetXml('التكتات', ticketsRows(), {title:'التكتات', subtitle:sub}),
      sheetXml('المخزون', inventoryRows(), {title:'المخزون', subtitle:sub}),
      sheetXml('حركة المخزون', movementRows(), {title:'حركة المخزون', subtitle:sub}),
      sheetXml('المصروفات', expensesRows(), {title:'المصروفات', subtitle:sub}),
      sheetXml('تقارير العملاء', clientReportsRows(), {title:'تقارير العملاء', subtitle:sub}),
      sheetXml('تقييمات العملاء', ratingsRows(), {title:'تقييمات العملاء', subtitle:sub}),
      sheetXml('التنبيهات', alertsRows(), {title:'التنبيهات', subtitle:sub})
    ]; }
  window.exportMeetingExcelV222 = async function(btn){
    try{
      if(btn) btn.disabled=true;
      await ensureExportData(); hydrateExportCenterV222();
      const f=currentFilters(); const name=`تقرير اجتماع تصنيف - ${f.month||todayStr().slice(0,7)}.xls`;
      downloadXmlWorkbook(name, exportSheets());
      if(typeof msg==='function') msg('تم تجهيز ملف Excel للاجتماع');
    }catch(e){ console.error(e); if(typeof msg==='function') msg(e.message||'تعذر تصدير ملف الاجتماع','err'); alert(e.message||'تعذر تصدير ملف الاجتماع'); }
    finally{ if(btn) btn.disabled=false; }
  };
  window.previewMeetingExportV222 = function(){
    hydrateExportCenterV222();
    const box=byId('meetingExportPreview'); if(!box) return;
    const p=filterProjects(), logs=filteredLogs(), tickets=filteredTickets(), exp=filteredExpenses();
    box.innerHTML=`<b>معاينة سريعة:</b><br>المشاريع: ${p.length} | سجلات التشغيل: ${logs.length} | التكتات: ${tickets.length} | المصروفات: ${exp.length}<br>سيتم إنشاء ملف Excel واحد يحتوي على تبويبات منظمة لكل أقسام النظام.`;
  };
  window.downloadImportTemplateV222 = function(){
    const sub='نموذج فارغ لتجهيز البيانات قبل الاستيراد - لا تغيّر أسماء الأعمدة';
    const sheets=[
      sheetXml('المشاريع', [['اسم المشروع','المشرف','نوع التشغيل','الدقائق اليومية','دقائق الجمعة','الموقع','الحالة','ملاحظات'],['مثال مشروع','اسم المشرف','زيارة يومية',180,90,'الرياض','نشط','']], {title:'نموذج استيراد المشاريع', subtitle:sub}),
      sheetXml('المشرفون', [['الاسم الكامل','اسم المستخدم','كلمة المرور','الحالة'],['اسم المشرف','username','123456','نشط']], {title:'نموذج استيراد المشرفين', subtitle:sub}),
      sheetXml('العمال', [['اسم العامل','الجوال','المشرف','المشروع','الراتب','الحالة','ملاحظات'],['اسم العامل','05xxxxxxxx','اسم المشرف','اسم المشروع',1500,'نشط','']], {title:'نموذج استيراد العمال', subtitle:sub}),
      sheetXml('المستخدمون', [['الاسم الكامل','اسم المستخدم','كلمة المرور','الدور','الحالة'],['اسم المستخدم','username','123456','supervisor','نشط']], {title:'نموذج استيراد المستخدمين', subtitle:sub}),
      sheetXml('التكتات', [['التاريخ','المشروع','العنوان','الوصف','الأولوية','رفع بواسطة','الحالة'],[todayStr(),'اسم المشروع','عنوان التكت','الوصف','عادي','اسم الرافع','مفتوح']], {title:'نموذج استيراد التكتات', subtitle:sub}),
      sheetXml('السجلات اليومية', [['التاريخ','المشروع','المشرف','وقت الدخول','وقت الخروج','ملاحظات'],[todayStr(),'اسم المشروع','اسم المشرف','08:00','17:00','']], {title:'نموذج استيراد السجلات اليومية', subtitle:sub}),
      sheetXml('المخزون', [['الصنف','الكود','التصنيف','المورد','الوحدة','الكمية','الحد الأدنى','سعر التكلفة'],['لمبة','L-001','كهرباء','اسم المورد','حبة',10,2,15]], {title:'نموذج استيراد المخزون', subtitle:sub}),
      sheetXml('المصروفات', [['التاريخ','المشروع','التصنيف','المورد','المبلغ','طريقة الدفع','ملاحظات'],[todayStr(),'اسم المشروع','مصروف عام','اسم المورد',100,'نقدي','']], {title:'نموذج استيراد المصروفات', subtitle:sub})
    ];
    downloadXmlWorkbook('نموذج استيراد شامل - تصنيف.xls', sheets);
  };
  const oldShowPageV222 = window.showPage || (typeof showPage==='function'?showPage:null);
  if(oldShowPageV222){
    window.showPage = function(id,btn){ const r=oldShowPageV222(id,btn); if(id==='export'){ setTimeout(()=>{ try{ hydrateExportCenterV222(); previewMeetingExportV222(); }catch(e){} },120); } return r; };
  }
  setTimeout(()=>{ try{ hydrateExportCenterV222(); }catch(e){} },800);
})();

/* ===== V222: Smart export slides + attendance without project relation ===== */
(function(){
  if(window.__tasneefV222AttendanceExportPatch) return;
  window.__tasneefV222AttendanceExportPatch = true;
  const $id = id => document.getElementById(id);
  const A = v => Array.isArray(v) ? v : [];
  const S = v => String(v ?? '').trim();
  const E = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const D = () => window.data || data || {};
  function workerSid(w){ try{return workerSupId(w)}catch(e){ return w?.supervisor_id || w?.assigned_supervisor_id || ''; } }
  function supNameV222(id){ try{return supervisorName(id)}catch(e){ return A(D().supervisors).concat(A(D().users)).find(u=>String(u.id)===String(id))?.full_name || '-'; } }
  function workerById(id){ return A(D().workers).find(w=>String(w.id)===String(id)) || {}; }
  function uniqueWorkersBySupervisor(sid){
    const seen = new Set();
    return A(D().workers).filter(w=>{
      if(sid && String(workerSid(w))!==String(sid)) return false;
      const key = String(w.id || w.name || '').trim();
      if(!key || seen.has(key)) return false;
      seen.add(key);
      return true;
    });
  }
  function todayV222(){ try{return today()}catch(e){ return new Date().toISOString().slice(0,10); } }
  function statusBadge(status){ return status==='present' ? '<span class="badge green">حاضر</span>' : '<span class="badge red">غائب</span>'; }

  window.renderAttendanceWorkersQuick = function(){
    const div=$id('attendanceQuick'); if(!div) return;
    const sid=$id('attendanceSupervisor')?.value || '';
    if(!sid){ div.innerHTML='<div class="quick-item">اختر المشرف لعرض العمال المرتبطين به فقط.</div>'; return; }
    const ws=uniqueWorkersBySupervisor(sid);
    div.innerHTML=ws.map(w=>`<div class="quick-item"><b>${E(w.name)}</b><div><button onclick="quickAttendance(${Number(w.id)},'present')">حاضر</button> <button class="danger" onclick="quickAttendance(${Number(w.id)},'absent')">غائب</button></div></div>`).join('') || '<div class="quick-item">لا يوجد عمال مرتبطون بهذا المشرف</div>';
  };

  window.saveAttendance = async function(){
    const id=$id('attendanceId')?.value || '';
    const workerId=Number($id('attendanceWorker')?.value)||0;
    if(!workerId) return (typeof msg==='function'?msg('اختر العامل','err'):alert('اختر العامل'));
    const w=workerById(workerId);
    const row={
      attendance_date:$id('attendanceDate')?.value || todayV222(),
      worker_id:workerId,
      supervisor_id:Number($id('attendanceSupervisor')?.value || workerSid(w)) || null,
      project_id:null,
      status:$id('attendanceStatus')?.value || 'present',
      notes:$id('attendanceNotes')?.value || '',
      created_by:(typeof session==='function'?session()?.id:null) || null
    };
    const res=id?await sb.from('attendance').update(row).eq('id',id):await sb.from('attendance').upsert(row,{onConflict:'attendance_date,worker_id'});
    if(res.error) return (typeof msg==='function'?msg(res.error.message,'err'):alert(res.error.message));
    if(typeof msg==='function') msg('تم حفظ الحضور بدون ربطه بمشروع');
    try{ clearAttendanceForm(); }catch(e){}
    try{ await refreshAll(); }catch(e){}
    try{ renderAttendance(); renderAttendanceWorkersQuick(); }catch(e){}
  };

  window.quickAttendance = async function(workerId,status){
    const w=workerById(workerId);
    if($id('attendanceWorker')) $id('attendanceWorker').value=workerId;
    if($id('attendanceSupervisor')) $id('attendanceSupervisor').value=workerSid(w)||$id('attendanceSupervisor').value||'';
    if($id('attendanceStatus')) $id('attendanceStatus').value=status;
    if($id('attendanceProject')) $id('attendanceProject').value='';
    await window.saveAttendance();
  };

  window.renderAttendance = function(){
    const b=$id('attendanceBody'); if(!b) return;
    const date=$id('attendanceFilterDate')?.value || '';
    const sid=$id('attendanceFilterSupervisor')?.value || '';
    const q=S($id('attendanceSearch')?.value);
    let rows=A(D().attendance);
    if(date) rows=rows.filter(a=>S(a.attendance_date)===date);
    if(sid) rows=rows.filter(a=>{ const w=workerById(a.worker_id); return String(a.supervisor_id || workerSid(w))===String(sid); });
    if(q) rows=rows.filter(a=>{ const w=workerById(a.worker_id); const sn=supNameV222(a.supervisor_id || workerSid(w)); return [w.name,a.worker_name,sn,a.status,a.notes].join(' ').includes(q); });
    b.innerHTML=rows.map(a=>{ const w=workerById(a.worker_id); const sid2=a.supervisor_id || workerSid(w); return `<tr><td>${E(a.attendance_date||'')}</td><td>${E(w.name||a.worker_name||'-')}</td><td>${E(supNameV222(sid2))}</td><td>${statusBadge(a.status)}</td><td>${E(a.notes||'')}</td><td class="row-actions"><button onclick="editAttendance(${Number(a.id)})">تعديل</button><button class="danger" onclick="deleteRow('attendance',${Number(a.id)})">حذف</button></td></tr>`; }).join('') || '<tr><td colspan="6">لا توجد بيانات</td></tr>';
    window.renderAttendanceWorkersQuick();
  };

  window.editAttendance = function(id){
    const a=A(D().attendance).find(x=>String(x.id)===String(id)); if(!a) return;
    const w=workerById(a.worker_id);
    if($id('attendanceId')) $id('attendanceId').value=a.id;
    if($id('attendanceDate')) $id('attendanceDate').value=a.attendance_date||todayV222();
    if($id('attendanceSupervisor')) $id('attendanceSupervisor').value=a.supervisor_id || workerSid(w) || '';
    if($id('attendanceProject')) $id('attendanceProject').value='';
    if($id('attendanceWorker')) $id('attendanceWorker').value=a.worker_id||'';
    if($id('attendanceStatus')) $id('attendanceStatus').value=a.status||'present';
    if($id('attendanceNotes')) $id('attendanceNotes').value=a.notes||'';
    if($id('attendanceFormTitle')) $id('attendanceFormTitle').textContent='تعديل حضور / غياب';
    window.renderAttendanceWorkersQuick();
  };

  window.renderSupervisorAttendanceList = function(){
    const div=$id('supervisorAttendanceList'); if(!div) return;
    const u=typeof session==='function'?session():null;
    const ws=uniqueWorkersBySupervisor(u?.id);
    div.innerHTML=ws.map(w=>`<div class="quick-item"><b>${E(w.name)}</b><select data-worker="${Number(w.id)}"><option value="present">حاضر</option><option value="absent">غائب</option></select></div>`).join('') || '<div class="quick-item">لا يوجد عمال مرتبطين بك</div>';
  };

  window.saveSupervisorAttendance = async function(){
    const u=typeof session==='function'?session():null;
    const date=$id('attendanceDate')?.value || todayV222();
    const rows=[...document.querySelectorAll('#supervisorAttendanceList select')].map(s=>({
      attendance_date:date,
      worker_id:Number(s.dataset.worker),
      supervisor_id:Number(u?.id)||null,
      project_id:null,
      status:s.value,
      created_by:Number(u?.id)||null
    })).filter(r=>r.worker_id);
    if(!rows.length) return (typeof msg==='function'?msg('لا يوجد عمال للتحضير','err'):alert('لا يوجد عمال للتحضير'));
    const {error}=await sb.from('attendance').upsert(rows,{onConflict:'attendance_date,worker_id'});
    if(error) return (typeof msg==='function'?msg(error.message,'err'):alert(error.message));
    if(typeof msg==='function') msg('تم حفظ التحضير بدون ربطه بمشروع');
    try{ await initSupervisor(); }catch(e){ try{ await refreshAll(); window.renderSupervisorAttendanceList(); }catch(_){} }
  };

  window.renderAttendanceMonthly = function(){
    const body=$id('attendanceMatrixBody'); const head=$id('attendanceMatrixHead'); if(!body||!head) return;
    const month=$id('attendanceMatrixMonth')?.value || todayV222().slice(0,7);
    const sid=$id('attendanceMatrixSupervisor')?.value || '';
    const q=S($id('attendanceMatrixSearch')?.value);
    const [y,m]=month.split('-').map(Number); const last=new Date(y,m,0).getDate();
    const days=Array.from({length:last},(_,i)=>String(i+1).padStart(2,'0'));
    head.innerHTML=`<tr><th>العامل</th><th>المشرف</th>${days.map(d=>`<th>${d}</th>`).join('')}<th>حضور</th><th>غياب</th><th>النسبة</th></tr>`;
    let workers=uniqueWorkersBySupervisor(sid);
    if(q) workers=workers.filter(w=>S(w.name).includes(q));
    let totalP=0,totalA=0;
    body.innerHTML=workers.map(w=>{
      let p=0,a=0;
      const cells=days.map(d=>{
        const ds=`${month}-${d}`;
        const rec=A(D().attendance).find(r=>String(r.worker_id)===String(w.id) && S(r.attendance_date)===ds);
        if(!rec) return '<td><span class="badge">-</span></td>';
        if(rec.status==='present'){p++; totalP++; return '<td><span class="badge green">ح</span></td>';}
        a++; totalA++; return '<td><span class="badge red">غ</span></td>';
      }).join('');
      const pct=(p+a)?(p/(p+a)*100):0;
      return `<tr><td>${E(w.name)}</td><td>${E(supNameV222(workerSid(w)))}</td>${cells}<td>${p}</td><td>${a}</td><td>${pct.toFixed(1)}%</td></tr>`;
    }).join('') || `<tr><td colspan="${days.length+5}">لا توجد بيانات</td></tr>`;
    const sum=$id('attendanceMatrixSummary'); if(sum){ const pct=(totalP+totalA)?(totalP/(totalP+totalA)*100):0; sum.innerHTML=`<div class="kpi"><small>عدد العمال</small><b>${workers.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalP}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalA}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`; }
  };

  // Compatibility aliases after V222 rename
  if(window.exportMeetingExcelV222) window.exportMeetingExcelV221 = window.exportMeetingExcelV222;
  if(window.previewMeetingExportV222) window.previewMeetingExportV221 = window.previewMeetingExportV222;
  if(window.downloadImportTemplateV222) window.downloadImportTemplateV221 = window.downloadImportTemplateV222;

  function bootV222(){
    try{ if($id('attendanceProject')) $id('attendanceProject').value=''; }catch(e){}
    try{ window.renderAttendanceWorkersQuick(); }catch(e){}
    try{ if($id('attendanceMatrixBody')) window.renderAttendanceMonthly(); }catch(e){}
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(bootV222, ev==='load'?900:250)));
  setTimeout(bootV222,1400);
  console.log('Tasneef V222 smart export slides and attendance without project loaded');
})();


/* ===== V223: منع تكرار أسماء العمال + شرائح تصدير ذكية حقيقية ===== */
(function(){
  const $id = id => document.getElementById(id);
  const A = v => Array.isArray(v) ? v : [];
  const S = v => String(v ?? '').trim();
  const E = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const D = () => window.data || (typeof data !== 'undefined' ? data : {});
  function normName(v){ return S(v).replace(/\s+/g,' ').replace(/[إأآا]/g,'ا').replace(/ى/g,'ي').toLowerCase(); }
  function workerSidV223(w){ try{return workerSupId(w)}catch(e){ return w?.supervisor_id || w?.assigned_supervisor_id || ''; } }
  function supNameV223(id){ try{return supervisorName(id)}catch(e){ return A(D().supervisors).concat(A(D().users)).find(u=>String(u.id)===String(id))?.full_name || '-'; } }
  function workerByIdV223(id){ return A(D().workers).find(w=>String(w.id)===String(id)) || {}; }
  function todayV223(){ try{return today()}catch(e){ return new Date().toISOString().slice(0,10); } }
  function statusBadgeV223(status){ return status==='present' ? '<span class="badge green">حاضر</span>' : '<span class="badge red">غائب</span>'; }
  function uniqueWorkersByNameV223(sid){
    const seen = new Map();
    A(D().workers).forEach(w=>{
      if(sid && String(workerSidV223(w))!==String(sid)) return;
      const key = normName(w.name || w.worker_name || w.full_name) || ('id:'+w.id);
      if(!key) return;
      const old = seen.get(key);
      // نختار أول عامل له مشروع/بيانات أو الأقل id لضمان الثبات، لكن العرض يكون مرة واحدة فقط
      if(!old) seen.set(key,w);
      else {
        const oldScore = (old.project_id?1:0)+(old.phone?1:0)+(old.notes?1:0);
        const newScore = (w.project_id?1:0)+(w.phone?1:0)+(w.notes?1:0);
        if(newScore>oldScore) seen.set(key,w);
      }
    });
    return [...seen.values()].sort((a,b)=>S(a.name).localeCompare(S(b.name),'ar'));
  }
  function refreshAttendanceWorkerSelectV223(sid, selected){
    const sel=$id('attendanceWorker'); if(!sel) return;
    const ws=uniqueWorkersByNameV223(sid||$id('attendanceSupervisor')?.value||'');
    const old=selected || sel.value || '';
    sel.innerHTML='<option value="">اختر العامل</option>'+ws.map(w=>`<option value="${Number(w.id)}">${E(w.name||'-')}</option>`).join('');
    if(old && [...sel.options].some(o=>String(o.value)===String(old))) sel.value=old;
  }

  window.renderAttendanceWorkersQuick = function(){
    const div=$id('attendanceQuick'); if(!div) return;
    const sid=$id('attendanceSupervisor')?.value || '';
    refreshAttendanceWorkerSelectV223(sid);
    if(!sid){ div.innerHTML='<div class="quick-item">اختر المشرف لعرض العمال بدون تكرار.</div>'; return; }
    const ws=uniqueWorkersByNameV223(sid);
    div.innerHTML=ws.map(w=>`<div class="quick-item"><b>${E(w.name)}</b><div><button onclick="quickAttendance(${Number(w.id)},'present')">حاضر</button> <button class="danger" onclick="quickAttendance(${Number(w.id)},'absent')">غائب</button></div></div>`).join('') || '<div class="quick-item">لا يوجد عمال مرتبطون بهذا المشرف</div>';
  };

  window.renderSupervisorAttendanceList = function(){
    const div=$id('supervisorAttendanceList'); if(!div) return;
    const u=typeof session==='function'?session():null;
    const ws=uniqueWorkersByNameV223(u?.id);
    div.innerHTML=ws.map(w=>`<div class="quick-item"><b>${E(w.name)}</b><select data-worker="${Number(w.id)}"><option value="present">حاضر</option><option value="absent">غائب</option></select></div>`).join('') || '<div class="quick-item">لا يوجد عمال مرتبطين بك</div>';
  };

  window.saveAttendance = async function(){
    const id=$id('attendanceId')?.value || '';
    const workerId=Number($id('attendanceWorker')?.value)||0;
    if(!workerId) return (typeof msg==='function'?msg('اختر العامل','err'):alert('اختر العامل'));
    const w=workerByIdV223(workerId);
    const row={attendance_date:$id('attendanceDate')?.value || todayV223(), worker_id:workerId, supervisor_id:Number($id('attendanceSupervisor')?.value || workerSidV223(w)) || null, project_id:null, status:$id('attendanceStatus')?.value || 'present', notes:$id('attendanceNotes')?.value || '', created_by:(typeof session==='function'?session()?.id:null) || null};
    const res=id?await sb.from('attendance').update(row).eq('id',id):await sb.from('attendance').upsert(row,{onConflict:'attendance_date,worker_id'});
    if(res.error) return (typeof msg==='function'?msg(res.error.message,'err'):alert(res.error.message));
    if(typeof msg==='function') msg('تم حفظ الحضور بدون تكرار وبدون ربطه بمشروع');
    try{ clearAttendanceForm(); }catch(e){}
    try{ await refreshAll(); }catch(e){}
    try{ renderAttendance(); renderAttendanceWorkersQuick(); }catch(e){}
  };

  window.quickAttendance = async function(workerId,status){
    const w=workerByIdV223(workerId);
    if($id('attendanceWorker')) refreshAttendanceWorkerSelectV223(workerSidV223(w), workerId);
    if($id('attendanceWorker')) $id('attendanceWorker').value=workerId;
    if($id('attendanceSupervisor')) $id('attendanceSupervisor').value=workerSidV223(w)||$id('attendanceSupervisor').value||'';
    if($id('attendanceStatus')) $id('attendanceStatus').value=status;
    if($id('attendanceProject')) $id('attendanceProject').value='';
    await window.saveAttendance();
  };

  window.renderAttendance = function(){
    const b=$id('attendanceBody'); if(!b) return;
    const date=$id('attendanceFilterDate')?.value || '';
    const sid=$id('attendanceFilterSupervisor')?.value || '';
    const q=S($id('attendanceSearch')?.value);
    let rows=A(D().attendance);
    if(date) rows=rows.filter(a=>S(a.attendance_date)===date);
    if(sid) rows=rows.filter(a=>{ const w=workerByIdV223(a.worker_id); return String(a.supervisor_id || workerSidV223(w))===String(sid); });
    if(q) rows=rows.filter(a=>{ const w=workerByIdV223(a.worker_id); const sn=supNameV223(a.supervisor_id || workerSidV223(w)); return [w.name,a.worker_name,sn,a.status,a.notes].join(' ').includes(q); });
    // في السجلات الفعلية نعرض السجلات المحفوظة، لكن نمنع تكرار نفس العامل في نفس اليوم والمشرف عند وجود صفوف قديمة مكررة
    const seen=new Set();
    rows=rows.filter(a=>{ const w=workerByIdV223(a.worker_id); const key=[S(a.attendance_date), normName(w.name||a.worker_name), String(a.supervisor_id || workerSidV223(w))].join('|'); if(seen.has(key)) return false; seen.add(key); return true; });
    b.innerHTML=rows.map(a=>{ const w=workerByIdV223(a.worker_id); const sid2=a.supervisor_id || workerSidV223(w); return `<tr><td>${E(a.attendance_date||'')}</td><td>${E(w.name||a.worker_name||'-')}</td><td>${E(supNameV223(sid2))}</td><td>${statusBadgeV223(a.status)}</td><td>${E(a.notes||'')}</td><td class="row-actions"><button onclick="editAttendance(${Number(a.id)})">تعديل</button><button class="danger" onclick="deleteRow('attendance',${Number(a.id)})">حذف</button></td></tr>`; }).join('') || '<tr><td colspan="6">لا توجد بيانات</td></tr>';
    window.renderAttendanceWorkersQuick();
  };

  window.editAttendance = function(id){
    const a=A(D().attendance).find(x=>String(x.id)===String(id)); if(!a) return;
    const w=workerByIdV223(a.worker_id);
    if($id('attendanceId')) $id('attendanceId').value=a.id;
    if($id('attendanceDate')) $id('attendanceDate').value=a.attendance_date||todayV223();
    if($id('attendanceSupervisor')) $id('attendanceSupervisor').value=a.supervisor_id || workerSidV223(w) || '';
    refreshAttendanceWorkerSelectV223(a.supervisor_id || workerSidV223(w), a.worker_id);
    if($id('attendanceProject')) $id('attendanceProject').value='';
    if($id('attendanceWorker')) $id('attendanceWorker').value=a.worker_id||'';
    if($id('attendanceStatus')) $id('attendanceStatus').value=a.status||'present';
    if($id('attendanceNotes')) $id('attendanceNotes').value=a.notes||'';
    if($id('attendanceFormTitle')) $id('attendanceFormTitle').textContent='تعديل حضور / غياب';
    window.renderAttendanceWorkersQuick();
  };

  window.saveSupervisorAttendance = async function(){
    const u=typeof session==='function'?session():null;
    const date=$id('attendanceDate')?.value || todayV223();
    const rows=[]; const seen=new Set();
    document.querySelectorAll('#supervisorAttendanceList select').forEach(s=>{
      const w=workerByIdV223(s.dataset.worker); const key=normName(w.name)||String(s.dataset.worker);
      if(seen.has(key)) return; seen.add(key);
      rows.push({attendance_date:date, worker_id:Number(s.dataset.worker), supervisor_id:Number(u?.id)||null, project_id:null, status:s.value, created_by:Number(u?.id)||null});
    });
    if(!rows.length) return (typeof msg==='function'?msg('لا يوجد عمال للتحضير','err'):alert('لا يوجد عمال للتحضير'));
    const {error}=await sb.from('attendance').upsert(rows,{onConflict:'attendance_date,worker_id'});
    if(error) return (typeof msg==='function'?msg(error.message,'err'):alert(error.message));
    if(typeof msg==='function') msg('تم حفظ التحضير بدون تكرار أسماء العمال');
    try{ await initSupervisor(); }catch(e){ try{ await refreshAll(); window.renderSupervisorAttendanceList(); }catch(_){} }
  };

  window.renderAttendanceMonthly = function(){
    const body=$id('attendanceMatrixBody'); const head=$id('attendanceMatrixHead'); if(!body||!head) return;
    const month=$id('attendanceMatrixMonth')?.value || todayV223().slice(0,7);
    const sid=$id('attendanceMatrixSupervisor')?.value || '';
    const q=S($id('attendanceMatrixSearch')?.value);
    const [y,m]=month.split('-').map(Number); const last=new Date(y,m,0).getDate();
    const days=Array.from({length:last},(_,i)=>String(i+1).padStart(2,'0'));
    head.innerHTML=`<tr><th>العامل</th><th>المشرف</th>${days.map(d=>`<th>${d}</th>`).join('')}<th>حضور</th><th>غياب</th><th>النسبة</th></tr>`;
    let workers=uniqueWorkersByNameV223(sid);
    if(q) workers=workers.filter(w=>S(w.name).includes(q));
    let totalP=0,totalA=0;
    body.innerHTML=workers.map(w=>{
      let p=0,a=0;
      const cells=days.map(d=>{
        const ds=`${month}-${d}`;
        const rec=A(D().attendance).find(r=>{ const rw=workerByIdV223(r.worker_id); return normName(rw.name||r.worker_name)===normName(w.name) && S(r.attendance_date)===ds && (!sid || String(r.supervisor_id || workerSidV223(rw))===String(sid)); });
        if(!rec) return '<td><span class="badge">-</span></td>';
        if(rec.status==='present'){p++; totalP++; return '<td><span class="badge green">ح</span></td>';}
        a++; totalA++; return '<td><span class="badge red">غ</span></td>';
      }).join('');
      const pct=(p+a)?(p/(p+a)*100):0;
      return `<tr><td>${E(w.name)}</td><td>${E(supNameV223(workerSidV223(w)))}</td>${cells}<td>${p}</td><td>${a}</td><td>${pct.toFixed(1)}%</td></tr>`;
    }).join('') || `<tr><td colspan="${days.length+5}">لا توجد بيانات</td></tr>`;
    const sum=$id('attendanceMatrixSummary'); if(sum){ const pct=(totalP+totalA)?(totalP/(totalP+totalA)*100):0; sum.innerHTML=`<div class="kpi"><small>عدد العمال</small><b>${workers.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalP}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalA}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`; }
  };

  window.showExportSlideV223 = function(index, btn){
    const slides=[...document.querySelectorAll('.export-slide-v222')];
    const buttons=[...document.querySelectorAll('.export-nav-v223 button')];
    slides.forEach((s,i)=>s.classList.toggle('active', i===index));
    buttons.forEach((b,i)=>b.classList.toggle('active', i===index));
    if(btn) btn.classList.add('active');
  };

  function bootV223(){
    try{ document.querySelector('.export-hero-badge-v222') && (document.querySelector('.export-hero-badge-v222').textContent='V223'); }catch(e){}
    try{ showExportSlideV223(0); }catch(e){}
    try{ refreshAttendanceWorkerSelectV223($id('attendanceSupervisor')?.value||''); }catch(e){}
    try{ renderAttendanceWorkersQuick(); }catch(e){}
    try{ renderSupervisorAttendanceList(); }catch(e){}
    try{ if($id('attendanceMatrixBody')) renderAttendanceMonthly(); }catch(e){}
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(bootV223, ev==='load'?900:250)));
  setTimeout(bootV223,1500);
  console.log('Tasneef V223 workers attendance dedupe + smart export slides loaded');
})();

/* ===== V224: Weighted average inventory cost + FIFO issuing + smart supplier/unit filters ===== */
(function(){
  const $id = id => document.getElementById(id);
  const A = v => Array.isArray(v) ? v : [];
  const S = v => String(v ?? '').trim();
  const N = v => { const x = Number(String(v ?? '').replace(/,/g,'')); return Number.isFinite(x) ? x : 0; };
  const E = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const D = () => window.data || (typeof data !== 'undefined' ? data : {});
  const VAT = 0.15;
  const UNITS = ['حبة','كرتون','علبة','كيس','رول','لتر','جالون','كيلو','جرام','متر','متر مربع','متر طولي','طقم','زوج','درزن','باكيت','عبوة','برميل','ساعة','زيارة','أخرى'];
  const M_IN = new Set(['in','return']);
  const M_OUT = new Set(['out','consume']);
  function moneyV224(v){ try { return (typeof money === 'function' ? money(N(v)) : N(v).toFixed(2)); } catch(e){ return N(v).toFixed(2); } }
  function todayV224(){ try{return today()}catch(e){ return new Date().toISOString().slice(0,10); } }
  function productCodeV224(i){ try{return v118ProductCode(i)}catch(e){ return i?.product_code || i?.serial_number || i?.barcode || ('INV-'+(i?.id||'')); } }
  function barcodeV224(i){ try{return v118Barcode(i)}catch(e){ return i?.barcode || i?.supplier_barcode || ''; } }
  function itemByIdV224(id){ return A(D().inventoryItems).find(i=>String(i.id)===String(id)); }
  function vatCalcV224(price, mode){ price=N(price); mode=mode||'exclusive'; if(mode==='inclusive'){ const net=price/(1+VAT); return {net, vat:price-net, gross:price}; } if(mode==='exempt') return {net:price, vat:0, gross:price}; return {net:price, vat:price*VAT, gross:price*(1+VAT)}; }
  function uniqueV224(arr){ return [...new Set(arr.map(S).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'ar')); }
  function supplierListV224(){
    return uniqueV224([
      ...A(D().inventoryItems).map(i=>i.supplier),
      ...A(D().inventoryMovements).map(m=>m.receiver),
      ...A(window.stockBatchesV148).map(b=>b.supplier)
    ]);
  }
  function ensureDatalistV224(id, values){
    let dl=$id(id); if(!dl){ dl=document.createElement('datalist'); dl.id=id; document.body.appendChild(dl); }
    dl.innerHTML = values.map(v=>`<option value="${E(v)}"></option>`).join('');
    return dl;
  }
  function enhanceBatchFiltersV224(){
    try{
      ensureDatalistV224('supplierOptionsV224', supplierListV224());
      ensureDatalistV224('unitOptionsV224', UNITS);
      ['batchSupplierV148','inventoryItemSupplier','financeExpenseSupplier'].forEach(id=>{ const el=$id(id); if(el) el.setAttribute('list','supplierOptionsV224'); });
      ['batchUnitV148','inventoryItemUnit'].forEach(id=>{ const el=$id(id); if(el) el.setAttribute('list','unitOptionsV224'); });
      const sup=$id('batchSupplierV148');
      if(sup && !sup.dataset.v224){ sup.dataset.v224='1'; sup.placeholder='اختر أو اكتب اسم المورد'; }
      const unit=$id('batchUnitV148');
      if(unit && !unit.dataset.v224){ unit.dataset.v224='1'; unit.placeholder='اختر الوحدة: حبة / كرتون / لتر...'; }
      const card=$id('stockBatchCardV148');
      if(card && !$id('batchHelperV224')){
        const helper=document.createElement('div'); helper.id='batchHelperV224'; helper.className='footer-note';
        helper.innerHTML=''; helper.style.display='none';
        card.insertBefore(helper, card.children[1] || null);
      }
    }catch(e){ console.warn('V224 filters warning', e); }
  }

  function inMovementsV224(itemId){
    return A(D().inventoryMovements)
      .filter(m=>String(m.item_id)===String(itemId) && M_IN.has(String(m.movement_type||'')))
      .map(m=>({id:m.id,date:S(m.movement_date||m.created_at||''),qty:N(m.quantity),cost:N(m.unit_cost),supplier:m.receiver||m.supplier||'',invoice:(S(m.notes).match(/batch:([^\s]+)/)||[])[1] || (S(m.reason).match(/فاتورة\s+(.+)$/)||[])[1] || ('MOV-'+(m.id||'')), raw:m}))
      .filter(x=>x.qty>0)
      .sort((a,b)=>S(a.date).localeCompare(S(b.date)) || N(a.id)-N(b.id));
  }
  function outMovementsV224(itemId){
    return A(D().inventoryMovements)
      .filter(m=>String(m.item_id)===String(itemId) && M_OUT.has(String(m.movement_type||'')))
      .map(m=>({id:m.id,date:S(m.movement_date||m.created_at||''),qty:N(m.quantity),raw:m}))
      .filter(x=>x.qty>0)
      .sort((a,b)=>S(a.date).localeCompare(S(b.date)) || N(a.id)-N(b.id));
  }
  function fifoLotsV224(itemId, excludeOutId){
    const lots=inMovementsV224(itemId).map(x=>({...x, remaining:x.qty}));
    outMovementsV224(itemId).forEach(o=>{
      if(excludeOutId && String(o.id)===String(excludeOutId)) return;
      let need=o.qty;
      for(const lot of lots){
        if(need<=0) break;
        const take=Math.min(N(lot.remaining), need);
        lot.remaining-=take; need-=take;
      }
    });
    return lots;
  }
  function fifoConsumeV224(itemId, qty, excludeOutId){
    let need=N(qty), value=0; const used=[];
    for(const lot of fifoLotsV224(itemId, excludeOutId)){
      if(need<=0) break;
      if(N(lot.remaining)<=0) continue;
      const take=Math.min(N(lot.remaining), need);
      used.push({...lot, taken:take}); value += take*N(lot.cost); need -= take;
    }
    return {value, avg: qty>0 ? value/N(qty) : 0, used, missing:need};
  }
  function weightedCostFromRemainingV224(itemId){
    const lots=fifoLotsV224(itemId);
    const qty=lots.reduce((a,l)=>a+N(l.remaining),0);
    const val=lots.reduce((a,l)=>a+N(l.remaining)*N(l.cost),0);
    const item=itemByIdV224(itemId);
    if(qty>0) return {qty, value:val, avg:val/qty};
    return {qty:N(item?.quantity), value:N(item?.quantity)*N(item?.unit_cost), avg:N(item?.unit_cost)};
  }
  async function updateItemWeightedCostV224(itemId){
    try{
      const item=itemByIdV224(itemId); if(!item || !window.sb) return;
      const wc=weightedCostFromRemainingV224(itemId);
      if(wc.avg>0 && Math.abs(N(item.unit_cost)-wc.avg)>.001){
        await sb.from('inventory_items').update({unit_cost:+wc.avg.toFixed(4)}).eq('id', itemId);
      }
    }catch(e){ console.warn('weighted cost update warning', e); }
  }

  // Same invoice: merge only if product code + price + unit are identical. Different price stays as a separate line.
  window.stockBatchAddLineV148 = function(){
    try{
      const code=S($id('batchProductCodeV148')?.value); const name=S($id('batchProductNameV148')?.value); const q=N($id('batchQtyV148')?.value); const price=N($id('batchUnitPriceV148')?.value); const unit=S($id('batchUnitV148')?.value)||'حبة';
      if(!code) return msg('كود المنتج مطلوب','err'); if(!name) return msg('اسم الصنف مطلوب','err'); if(q<=0) return msg('الكمية مطلوبة','err'); if(price<0) return msg('سعر الوحدة غير صحيح','err');
      window.batchLinesV148 = Array.isArray(window.batchLinesV148) ? window.batchLinesV148 : [];
      if(typeof batchLinesV148 !== 'undefined') batchLinesV148 = window.batchLinesV148;
      const keyPrice=price.toFixed(4);
      const existingLine = window.batchLinesV148.find(l=>S(l.product_code)===code && N(l.unit_price).toFixed(4)===keyPrice && S(l.unit||'حبة')===unit);
      if(existingLine){ existingLine.quantity = N(existingLine.quantity) + q; existingLine.image_url = existingLine.image_url || (typeof pendingLineImageV148 !== 'undefined' ? pendingLineImageV148 : ''); }
      else window.batchLinesV148.push({ product_code:code, item_name:name, category:S($id('batchCategoryV148')?.value)||'أخرى', item_type:S($id('batchItemTypeV148')?.value)||'مادة', unit, quantity:q, unit_price:price, image_url:(typeof pendingLineImageV148 !== 'undefined' ? pendingLineImageV148 : '') });
      if(typeof batchLinesV148 !== 'undefined') batchLinesV148 = window.batchLinesV148;
      if(typeof stockBatchClearLineV148==='function') stockBatchClearLineV148();
      if(typeof stockBatchRenderLinesV148==='function') stockBatchRenderLinesV148();
    }catch(e){ msg(e.message||String(e),'err'); }
  };

  // Weighted average on stock invoice save.
  window.stockBatchSaveV148 = async function(btn){
    try{
      if(btn) btn.disabled=true;
      const lines = Array.isArray(window.batchLinesV148) ? window.batchLinesV148 : (typeof batchLinesV148 !== 'undefined' ? batchLinesV148 : []);
      if(!lines.length) throw new Error('أضف صنفًا واحدًا على الأقل داخل الفاتورة');
      const supplier=S($id('batchSupplierV148')?.value); if(!supplier) throw new Error('اختر أو اكتب اسم المورد');
      const invoiceNo=S($id('batchInvoiceV148')?.value) || ('ADD-'+Date.now());
      const date=S($id('batchDateV148')?.value)||todayV224(); const mode=$id('batchVatModeV148')?.value||'exclusive';
      let netTotal=0, vatTotal=0, grossTotal=0; const savedLines=[];
      for(const l of lines){
        let item=(typeof findItemByCode==='function') ? findItemByCode(l.product_code) : A(D().inventoryItems).find(i=>[i.product_code,i.serial_number,i.barcode,i.supplier_barcode].map(S).includes(S(l.product_code)));
        if(!item && !l.image_url) throw new Error('الصنف الجديد '+l.item_name+' يحتاج صورة قبل الحفظ');
        const calc=vatCalcV224(l.unit_price, mode); const q=N(l.quantity); const lineNet=calc.net*q, lineVat=calc.vat*q, lineGross=calc.gross*q;
        netTotal+=lineNet; vatTotal+=lineVat; grossTotal+=lineGross;
        if(item){
          const oldQty=N(item.quantity), oldCost=N(item.unit_cost), newQty=oldQty+q;
          const avg = newQty>0 ? ((oldQty*oldCost) + (q*calc.net)) / newQty : calc.net;
          const upd={quantity:newQty, unit_cost:+avg.toFixed(4), supplier, category:l.category||item.category, unit:l.unit||item.unit, product_code:l.product_code, serial_number:l.product_code, barcode:l.product_code, supplier_barcode:l.product_code, item_type:l.item_type||item.item_type, type:l.item_type||item.type};
          const res=await sb.from('inventory_items').update(upd).eq('id', item.id).select('*').single(); if(res.error) throw res.error; item=res.data;
        }else{
          const ins={name:l.item_name, serial_number:l.product_code, product_code:l.product_code, barcode:l.product_code, supplier_barcode:l.product_code, image_url:l.image_url, category:l.category||'أخرى', item_type:l.item_type||'مادة', type:l.item_type||'مادة', unit:l.unit||'حبة', quantity:q, min_quantity:0, unit_cost:+calc.net.toFixed(4), supplier, notes:'تم إنشاؤه من فاتورة إدخال '+invoiceNo};
          const res=await sb.from('inventory_items').insert(ins).select('*').single(); if(res.error) throw res.error; item=res.data;
        }
        const mv={item_id:item.id,item_name:item.name,movement_type:'in',quantity:q,movement_date:date,project_id:null,project_name:'',receiver:supplier,reason:'إدخال مخزون - فاتورة '+invoiceNo,notes:'batch:'+invoiceNo,product_code:l.product_code,barcode:l.product_code,unit_cost:+calc.net.toFixed(4)};
        const mres=await sb.from('inventory_movements').insert(mv).select('*').single(); if(mres.error) throw mres.error;
        savedLines.push({...l,item_id:item.id,unit_cost:calc.net,unit_vat:calc.vat,unit_gross:calc.gross,line_net:lineNet,line_vat:lineVat,line_gross:lineGross,movement_id:mres.data.id});
      }
      try{
        if(typeof ensureBatchTablesExistV148 === 'function' && await ensureBatchTablesExistV148()){
          const bres=await sb.from('inventory_batches').insert({batch_date:date,supplier,invoice_no:invoiceNo,vat_mode:mode,total_before_vat:netTotal,total_vat:vatTotal,total_with_vat:grossTotal,notes:'فاتورة إدخال مخزون'}).select('*').single();
          if(bres.error) throw bres.error;
          const rows=savedLines.map(l=>({batch_id:bres.data.id,item_id:l.item_id,product_code:l.product_code,item_name:l.item_name,category:l.category,item_type:l.item_type,unit:l.unit,quantity:l.quantity,unit_price_before_vat:l.unit_cost,unit_vat:l.unit_vat,unit_price_with_vat:l.unit_gross,total_before_vat:l.line_net,total_vat:l.line_vat,total_with_vat:l.line_gross,movement_id:l.movement_id}));
          const li=await sb.from('inventory_batch_items').insert(rows); if(li.error) throw li.error;
        }
      }catch(e){ console.warn('batch tables optional warning', e); }
      msg('تم حفظ الفاتورة وتحديث متوسط تكلفة الصنف');
      if(typeof stockBatchPrintV148==='function') stockBatchPrintV148({invoice_no:invoiceNo,batch_date:date,supplier,vat_mode:mode,lines:savedLines,total_before_vat:netTotal,total_vat:vatTotal,total_with_vat:grossTotal});
      if(typeof stockBatchClearV148==='function') stockBatchClearV148();
      await financeLoadAll(); try{ await stockBatchLoadV148(true); }catch(e){}
    }catch(e){ msg(e.message||String(e),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };

  // FIFO cost on direct stock movement.
  window.inventorySaveMovement = async function(btn){
    try{
      if(btn) btn.disabled=true;
      const itemId=$id('inventoryMovementItem')?.value; if(!itemId) throw new Error('اختر الصنف');
      const item=itemByIdV224(itemId); const qty=N($id('inventoryMovementQty')?.value); if(qty<=0) throw new Error('الكمية مطلوبة');
      const type=$id('inventoryMovementType')?.value||'out'; const pid=$id('inventoryMovementProject')?.value;
      if(M_OUT.has(type) && N(item?.quantity)<qty) throw new Error('الكمية المتوفرة لا تكفي');
      let movementCost=N(item?.unit_cost);
      if(M_OUT.has(type)){
        const fifo=fifoConsumeV224(itemId, qty);
        movementCost = fifo.avg || N(item?.unit_cost);
      }
      const row={item_id:Number(itemId),item_name:item?.name||'',movement_type:type,quantity:qty,movement_date:$id('inventoryMovementDate')?.value||todayV224(),project_id:pid?Number(pid):null,project_name:pid?(typeof projectName==='function'?projectName(pid):''):'',receiver:$id('inventoryMovementReceiver')?.value||'',reason:$id('inventoryMovementReason')?.value||'',notes:$id('inventoryMovementNotes')?.value||'',product_code:productCodeV224(item),barcode:barcodeV224(item),unit_cost:+movementCost.toFixed(4)};
      const id=$id('inventoryMovementId')?.value; if(id) throw new Error('تعديل حركة المخزون غير متاح؛ احذف الحركة وأضف حركة جديدة للحفاظ على دقة الرصيد');
      const res=await sb.from('inventory_movements').insert(row).select('*').single(); if(res.error) throw res.error;
      let newQty=N(item?.quantity); if(type==='in'||type==='return') newQty+=qty; else if(type==='out'||type==='consume') newQty-=qty; else if(type==='adjust') newQty=qty;
      const newCost = M_OUT.has(type) ? weightedCostFromRemainingV224(itemId).avg || N(item?.unit_cost) : N(item?.unit_cost);
      const upd=await sb.from('inventory_items').update({quantity:newQty, unit_cost:+newCost.toFixed(4)}).eq('id',itemId); if(upd.error) throw upd.error;
      msg(M_OUT.has(type)?'تم حفظ الحركة بتكلفة FIFO من أقدم دفعة أولًا':'تم حفظ حركة المخزون وتحديث الرصيد');
      if(typeof inventoryClearMovementForm==='function') inventoryClearMovementForm(); await financeLoadAll();
    }catch(e){ msg(e.message||String(e),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };

  // Patch product card/table: show weighted average and batch count clearly.
  const oldInventoryRenderItemsV224 = window.inventoryRenderItems;
  window.inventoryRenderItems = function(){
    if(oldInventoryRenderItemsV224) oldInventoryRenderItemsV224.apply(this, arguments);
    try{
      document.querySelectorAll('#inventoryItemsBody tr').forEach(tr=>{});
      enhanceBatchFiltersV224();
      const supFilter=$id('inventorySupplierFilter');
      if(supFilter){ const old=supFilter.value; const vals=supplierListV224(); supFilter.innerHTML='<option value="">كل الموردين</option>'+vals.map(v=>`<option>${E(v)}</option>`).join(''); supFilter.value=old; }
    }catch(e){}
  };

  // Rich detail: FIFO lots and weighted cost.
  window.v118ShowProductDetail = function(itemId){
    const item=itemByIdV224(itemId); if(!item) return;
    const lots=fifoLotsV224(itemId);
    const wc=weightedCostFromRemainingV224(itemId);
    const addRows=lots.map(l=>`<tr><td>${E(l.date||'-')}</td><td>${E(l.invoice||'-')}</td><td>${E(l.supplier||'-')}</td><td>${N(l.qty)}</td><td>${N(l.remaining)}</td><td>${moneyV224(l.cost)}</td><td>${moneyV224(N(l.remaining)*N(l.cost))}</td></tr>`).join('') || '<tr><td colspan="7">لا توجد دفعات إدخال</td></tr>';
    const outs=outMovementsV224(itemId).map(o=>{ const f=fifoConsumeV224(itemId, o.qty, o.id); const m=o.raw||{}; return `<tr><td>${E(o.date||'-')}</td><td>${E(m.project_name||'-')}</td><td>${E(m.receiver||'-')}</td><td>${N(o.qty)}</td><td>${moneyV224(f.avg||m.unit_cost||item.unit_cost)}</td><td>${moneyV224(f.value || N(o.qty)*N(m.unit_cost||item.unit_cost))}</td><td>${E(m.reason||'-')}</td></tr>`; }).join('') || '<tr><td colspan="7">لا توجد عمليات صرف</td></tr>';
    const img=item.image_url?`<img src="${E(item.image_url)}" style="width:92px;height:92px;object-fit:contain;border:1px solid #d7e4df;border-radius:16px;background:#fff">`:'';
    const html=`<div class="modal-backdrop" onclick="if(event.target===this)this.remove()"><div class="modal-card" style="max-width:1050px"><div class="modal-head"><h2>تفاصيل المنتج</h2><button onclick="this.closest('.modal-backdrop').remove()">إغلاق</button></div><div class="grid two"><div class="card soft-card"><h2>${E(item.name)}</h2><p>كود المنتج: <b>${E(productCodeV224(item))}</b></p><p>المورد: <b>${E(item.supplier||'-')}</b> | الوحدة: <b>${E(item.unit||'-')}</b></p><p>المتوفر: <b>${N(item.quantity)}</b> | متوسط التكلفة الحالي: <b>${moneyV224(wc.avg||item.unit_cost)}</b></p></div><div class="card soft-card" style="text-align:center">${img}</div></div><h3>دفعات الشراء وأسعارها FIFO</h3><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>الفاتورة</th><th>المورد</th><th>كمية الدفعة</th><th>المتبقي من الدفعة</th><th>سعر الدفعة</th><th>قيمة المتبقي</th></tr></thead><tbody>${addRows}</tbody></table></div><h3>الصرف حسب أقدم دفعة أولًا</h3><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>المشروع</th><th>المستلم</th><th>الكمية</th><th>تكلفة FIFO للوحدة</th><th>قيمة الصرف</th><th>السبب</th></tr></thead><tbody>${outs}</tbody></table></div></div></div>`;
    document.body.insertAdjacentHTML('beforeend', html);
  };

  function bootV224(){
    try{ document.querySelector('.export-hero-badge-v222') && (document.querySelector('.export-hero-badge-v222').textContent='V224'); }catch(e){}
    try{ enhanceBatchFiltersV224(); }catch(e){}
    try{ if(typeof stockBatchRenderLinesV148==='function') stockBatchRenderLinesV148(); }catch(e){}
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(bootV224, ev==='load'?900:250)));
  setTimeout(bootV224,1500);
  console.log('Tasneef V224 weighted average inventory cost + FIFO issuing loaded');
})();


/* ===== V230: Stable inventory lots + correct batch prices + faster reports =====
   الهدف:
   - جدول دفعات الشراء يعرض سعر الفاتورة الحقيقي لكل دفعة، وليس متوسط الصنف.
   - متوسط التكلفة يحسب من المتبقي الحقيقي بعد FIFO: قيمة المتبقي ÷ كمية المتبقي.
   - الصرف يستخدم أقدم دفعة أولاً، ويؤثر على التقارير بنفس المنطق.
   - تحسين سرعة الاستجابة بتقليل إعادة الرسم الثقيلة، وتوحيد نافذة تفاصيل الصنف.
*/
(function(){
  'use strict';
  window.TASNEEF_BUILD='V230_INVENTORY_STABLE_FIFO';
  const $=id=>document.getElementById(id);
  const A=v=>Array.isArray(v)?v:[];
  const S=v=>String(v??'').trim();
  const N=v=>{ const x=Number(String(v??'').replace(/,/g,'')); return Number.isFinite(x)?x:0; };
  const E=v=>S(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const D=()=>window.data||(typeof data!=='undefined'?data:{});
  const VAT=0.15;
  const UNITS=['حبة','كرتون','علبة','كيس','رول','لتر','جالون','كيلو','جرام','متر','متر مربع','متر طولي','طقم','زوج','درزن','باكيت','عبوة','برميل','ساعة','زيارة','أخرى'];
  const IN_TYPES=new Set(['in','return']);
  const OUT_TYPES=new Set(['out','consume']);
  function msg2(t,type){ try{ if(typeof msg==='function') msg(t,type); else alert(t); }catch(e){ alert(t); } }
  function money2(v){ try{ return (typeof money==='function'?money(N(v)):N(v).toLocaleString('ar-SA',{minimumFractionDigits:2,maximumFractionDigits:2})+' ر.س'); }catch(e){ return N(v).toFixed(2)+' ر.س'; } }
  function qty2(v){ try{ return N(v).toLocaleString('ar-SA',{maximumFractionDigits:2}); }catch(e){ return String(N(v)); } }
  function today2(){ try{return today()}catch(e){ return new Date().toISOString().slice(0,10); } }
  function codeOf(i){ try{return v118ProductCode(i)}catch(e){ return i?.product_code||i?.serial_number||i?.barcode||('INV-'+(i?.id||'')); } }
  function barcodeOf(i){ try{return v118Barcode(i)}catch(e){ return i?.barcode||i?.supplier_barcode||''; } }
  function itemById(id){ return A(D().inventoryItems).find(i=>String(i.id)===String(id)); }
  function findItemByAnyCode(code){ code=S(code); return A(D().inventoryItems).find(i=>[i.id,i.product_code,i.serial_number,i.barcode,i.supplier_barcode].map(S).includes(code)); }
  function vatCalc(price,mode){ price=N(price); mode=mode||'exclusive'; if(mode==='inclusive'){ const net=price/(1+VAT); return {net,vat:price-net,gross:price}; } if(mode==='exempt') return {net:price,vat:0,gross:price}; return {net:price,vat:price*VAT,gross:price*(1+VAT)}; }
  function invFromText(m){ return (S(m.notes).match(/batch:([^\s]+)/)||[])[1] || (S(m.reason).match(/فاتورة\s+(.+)$/)||[])[1] || S(m.invoice_no||m.reference||''); }
  function normalizeDate(v){ return S(v||'').slice(0,10); }
  function batchLinesForItem(itemId){
    const out=[];
    const batches=A(window.stockBatchesV148);
    for(const b of batches){
      for(const l of A(b.lines)){
        if(String(l.item_id||'')===String(itemId) || String(findItemByAnyCode(l.product_code)?.id||'')===String(itemId)){
          out.push({
            movement_id:l.movement_id,
            batch_id:l.batch_id||b.id,
            invoice:S(b.invoice_no||b.id||l.invoice_no||'-'),
            date:normalizeDate(b.batch_date||l.batch_date||l.created_at),
            supplier:S(b.supplier||l.supplier||'-'),
            qty:N(l.quantity),
            cost:N(l.unit_price_before_vat||l.unit_cost||l.price||0),
            unit:S(l.unit||''),
            source:'batch',
            raw:l
          });
        }
      }
    }
    return out.filter(x=>x.qty>0 && x.cost>0);
  }
  function inMovements(itemId){
    const batchByMove=new Map();
    for(const bl of batchLinesForItem(itemId)){ if(bl.movement_id) batchByMove.set(String(bl.movement_id), bl); }
    const rows=A(D().inventoryMovements)
      .filter(m=>String(m.item_id)===String(itemId) && IN_TYPES.has(S(m.movement_type)))
      .map(m=>{
        const bm=batchByMove.get(String(m.id));
        return {
          id:m.id,
          date:normalizeDate(m.movement_date||m.created_at),
          qty:N(m.quantity),
          cost: bm ? N(bm.cost) : N(m.unit_cost),
          supplier: bm ? bm.supplier : S(m.receiver||m.supplier||'-'),
          invoice: bm ? bm.invoice : (invFromText(m)||('MOV-'+(m.id||''))),
          unit: bm ? bm.unit : '',
          raw:m,
          source: bm?'batch+movement':'movement'
        };
      }).filter(x=>x.qty>0 && x.cost>=0);
    const movementIds=new Set(rows.map(x=>String(x.id)));
    // If a batch line exists without a matching movement, keep it as an invoice lot so the report does not lose true invoice prices.
    for(const bl of batchLinesForItem(itemId)){
      if(bl.movement_id && movementIds.has(String(bl.movement_id))) continue;
      rows.push({id:'B-'+(bl.batch_id||bl.invoice),date:bl.date,qty:bl.qty,cost:bl.cost,supplier:bl.supplier,invoice:bl.invoice,unit:bl.unit,raw:bl.raw,source:'batch-only'});
    }
    return rows.sort((a,b)=>S(a.date).localeCompare(S(b.date)) || S(a.invoice).localeCompare(S(b.invoice)) || N(a.id)-N(b.id));
  }
  function outMovements(itemId){
    return A(D().inventoryMovements)
      .filter(m=>String(m.item_id)===String(itemId) && OUT_TYPES.has(S(m.movement_type)))
      .map(m=>({id:m.id,date:normalizeDate(m.movement_date||m.created_at),qty:N(m.quantity),raw:m}))
      .filter(x=>x.qty>0)
      .sort((a,b)=>S(a.date).localeCompare(S(b.date)) || N(a.id)-N(b.id));
  }
  function fifoLots(itemId, excludeOutId){
    const lots=inMovements(itemId).map(x=>({...x, originalQty:N(x.qty), remaining:N(x.qty), issued:0}));
    for(const out of outMovements(itemId)){
      if(excludeOutId && String(out.id)===String(excludeOutId)) continue;
      let need=N(out.qty);
      for(const lot of lots){
        if(need<=0) break;
        const take=Math.min(N(lot.remaining), need);
        lot.remaining=+(N(lot.remaining)-take).toFixed(6);
        lot.issued=+(N(lot.issued)+take).toFixed(6);
        need=+(need-take).toFixed(6);
      }
    }
    return lots;
  }
  function fifoConsume(itemId, amount, excludeOutId){
    let need=N(amount), value=0; const used=[];
    for(const lot of fifoLots(itemId,excludeOutId)){
      if(need<=0) break;
      if(N(lot.remaining)<=0) continue;
      const take=Math.min(N(lot.remaining),need);
      used.push({...lot,taken:take});
      value += take*N(lot.cost);
      need=+(need-take).toFixed(6);
    }
    return {value, avg:N(amount)>0?value/N(amount):0, used, missing:need};
  }
  function weightedCurrent(itemId){
    const lots=fifoLots(itemId);
    const qty=lots.reduce((a,l)=>a+N(l.remaining),0);
    const value=lots.reduce((a,l)=>a+N(l.remaining)*N(l.cost),0);
    const item=itemById(itemId);
    if(qty>0) return {qty,value,avg:value/qty,lots};
    return {qty:N(item?.quantity), value:N(item?.quantity)*N(item?.unit_cost), avg:N(item?.unit_cost), lots};
  }
  async function updateItemCostAndQty(itemId, qtyOverride){
    try{
      if(!window.sb) return;
      const item=itemById(itemId); if(!item) return;
      const wc=weightedCurrent(itemId);
      const upd={};
      if(wc.avg>0) upd.unit_cost=+wc.avg.toFixed(4);
      if(qtyOverride!==undefined) upd.quantity=N(qtyOverride);
      if(Object.keys(upd).length) await sb.from('inventory_items').update(upd).eq('id',itemId);
    }catch(e){ console.warn('V230 update item warning',e); }
  }
  function supplierList(){
    const set=new Set();
    A(D().inventoryItems).forEach(i=>{ if(S(i.supplier)) set.add(S(i.supplier)); });
    A(D().inventoryMovements).forEach(m=>{ if(S(m.receiver)) set.add(S(m.receiver)); });
    A(window.stockBatchesV148).forEach(b=>{ if(S(b.supplier)) set.add(S(b.supplier)); });
    return [...set].sort((a,b)=>a.localeCompare(b,'ar'));
  }
  function ensureDatalist(id, values){ let dl=$(id); if(!dl){ dl=document.createElement('datalist'); dl.id=id; document.body.appendChild(dl); } dl.innerHTML=values.map(v=>`<option value="${E(v)}"></option>`).join(''); }
  function enhanceInvoiceForm(){
    try{
      ensureDatalist('supplierOptionsV230', supplierList()); ensureDatalist('unitOptionsV230', UNITS);
      ['batchSupplierV148','inventoryItemSupplier','financeExpenseSupplier'].forEach(id=>{ const el=$(id); if(el) el.setAttribute('list','supplierOptionsV230'); });
      ['batchUnitV148','inventoryItemUnit'].forEach(id=>{ const el=$(id); if(el) el.setAttribute('list','unitOptionsV230'); });
      const sup=$('batchSupplierV148'); if(sup){ sup.placeholder='اختر المورد أو اكتب موردًا جديدًا'; }
      const unit=$('batchUnitV148'); if(unit){ unit.placeholder='اختر الوحدة أو اكتب وحدة جديدة'; }
      const card=$('stockBatchCardV148');
      if(card && !$('#batchHelperV230')){
        const d=document.createElement('div'); d.id='batchHelperV230'; d.className='footer-note';
        d.innerHTML=''; d.style.display='none';
        card.insertBefore(d, card.children[1]||null);
      }
    }catch(e){}
  }
  // Same invoice: merge only when code + price + unit are identical. Different price remains separate.
  window.stockBatchAddLineV148=function(){
    try{
      const code=S($('batchProductCodeV148')?.value); const name=S($('batchProductNameV148')?.value); const unit=S($('batchUnitV148')?.value)||'حبة';
      const q=N($('batchQtyV148')?.value); const price=N($('batchUnitPriceV148')?.value);
      if(!code) return msg2('كود المنتج مطلوب','err'); if(!name) return msg2('اسم الصنف مطلوب','err'); if(q<=0) return msg2('الكمية مطلوبة','err'); if(price<0) return msg2('سعر الوحدة غير صحيح','err');
      window.batchLinesV148=Array.isArray(window.batchLinesV148)?window.batchLinesV148:[];
      try{ if(typeof batchLinesV148!=='undefined') batchLinesV148=window.batchLinesV148; }catch(e){}
      const priceKey=price.toFixed(4);
      const line=window.batchLinesV148.find(l=>S(l.product_code)===code && N(l.unit_price).toFixed(4)===priceKey && S(l.unit||'حبة')===unit);
      if(line){ line.quantity=N(line.quantity)+q; }
      else window.batchLinesV148.push({product_code:code,item_name:name,category:S($('batchCategoryV148')?.value)||'أخرى',item_type:S($('batchItemTypeV148')?.value)||'مادة',unit,quantity:q,unit_price:price,image_url:(typeof pendingLineImageV148!=='undefined'?pendingLineImageV148:'')});
      try{ if(typeof batchLinesV148!=='undefined') batchLinesV148=window.batchLinesV148; }catch(e){}
      if(typeof stockBatchClearLineV148==='function') stockBatchClearLineV148();
      if(typeof stockBatchRenderLinesV148==='function') stockBatchRenderLinesV148();
    }catch(e){ msg2(e.message||String(e),'err'); }
  };
  window.stockBatchSaveV148=async function(btn){
    try{
      if(btn) btn.disabled=true;
      const lines=Array.isArray(window.batchLinesV148)?window.batchLinesV148:(typeof batchLinesV148!=='undefined'?batchLinesV148:[]);
      if(!lines.length) throw new Error('أضف صنفًا واحدًا على الأقل داخل الفاتورة');
      const supplier=S($('batchSupplierV148')?.value); if(!supplier) throw new Error('اختر أو اكتب اسم المورد');
      const invoiceNo=S($('batchInvoiceV148')?.value)||('ADD-'+Date.now());
      const date=S($('batchDateV148')?.value)||today2(); const mode=$('batchVatModeV148')?.value||'exclusive';
      let netTotal=0,vatTotal=0,grossTotal=0; const saved=[];
      for(const l of lines){
        let item=(typeof findItemByCode==='function')?findItemByCode(l.product_code):findItemByAnyCode(l.product_code);
        if(!item && !l.image_url) throw new Error('الصنف الجديد '+l.item_name+' يحتاج صورة قبل الحفظ');
        const calc=vatCalc(l.unit_price,mode); const q=N(l.quantity); const lineNet=calc.net*q,lineVat=calc.vat*q,lineGross=calc.gross*q;
        netTotal+=lineNet; vatTotal+=lineVat; grossTotal+=lineGross;
        if(item){
          const wc=weightedCurrent(item.id);
          const oldQty=wc.qty>0?wc.qty:N(item.quantity);
          const oldValue=wc.qty>0?wc.value:(N(item.quantity)*N(item.unit_cost));
          const newQty=N(item.quantity)+q;
          const newAvg=(oldValue+(q*calc.net))/(oldQty+q || q || 1);
          const upd={quantity:newQty,unit_cost:+newAvg.toFixed(4),supplier,category:l.category||item.category,unit:l.unit||item.unit,product_code:l.product_code,serial_number:l.product_code,barcode:l.product_code,supplier_barcode:l.product_code,item_type:l.item_type||item.item_type,type:l.item_type||item.type};
          const res=await sb.from('inventory_items').update(upd).eq('id',item.id).select('*').single(); if(res.error) throw res.error; item=res.data;
        }else{
          const ins={name:l.item_name,serial_number:l.product_code,product_code:l.product_code,barcode:l.product_code,supplier_barcode:l.product_code,image_url:l.image_url,category:l.category||'أخرى',item_type:l.item_type||'مادة',type:l.item_type||'مادة',unit:l.unit||'حبة',quantity:q,min_quantity:0,unit_cost:+calc.net.toFixed(4),supplier,notes:'تم إنشاؤه من فاتورة إدخال '+invoiceNo};
          const res=await sb.from('inventory_items').insert(ins).select('*').single(); if(res.error) throw res.error; item=res.data;
        }
        const mv={item_id:item.id,item_name:item.name,movement_type:'in',quantity:q,movement_date:date,project_id:null,project_name:'',receiver:supplier,reason:'إدخال مخزون - فاتورة '+invoiceNo,notes:'batch:'+invoiceNo,product_code:l.product_code,barcode:l.product_code,unit_cost:+calc.net.toFixed(4)};
        const mr=await sb.from('inventory_movements').insert(mv).select('*').single(); if(mr.error) throw mr.error;
        saved.push({...l,item_id:item.id,unit_cost:calc.net,unit_vat:calc.vat,unit_gross:calc.gross,line_net:lineNet,line_vat:lineVat,line_gross:lineGross,movement_id:mr.data.id});
      }
      try{
        if(typeof ensureBatchTablesExistV148==='function' && await ensureBatchTablesExistV148()){
          const br=await sb.from('inventory_batches').insert({batch_date:date,supplier,invoice_no:invoiceNo,vat_mode:mode,total_before_vat:netTotal,total_vat:vatTotal,total_with_vat:grossTotal,notes:'فاتورة إدخال مخزون'}).select('*').single();
          if(br.error) throw br.error;
          const rows=saved.map(l=>({batch_id:br.data.id,item_id:l.item_id,product_code:l.product_code,item_name:l.item_name,category:l.category,item_type:l.item_type,unit:l.unit,quantity:l.quantity,unit_price_before_vat:+N(l.unit_cost).toFixed(4),unit_vat:+N(l.unit_vat).toFixed(4),unit_price_with_vat:+N(l.unit_gross).toFixed(4),total_before_vat:+N(l.line_net).toFixed(4),total_vat:+N(l.line_vat).toFixed(4),total_with_vat:+N(l.line_gross).toFixed(4),movement_id:l.movement_id}));
          const li=await sb.from('inventory_batch_items').insert(rows); if(li.error) throw li.error;
        }
      }catch(e){ console.warn('V230 optional batch table warning',e); }
      msg2('تم حفظ الفاتورة بسعر كل دفعة وتحديث متوسط تكلفة الصنف','ok');
      if(typeof stockBatchPrintV148==='function') stockBatchPrintV148({invoice_no:invoiceNo,batch_date:date,supplier,vat_mode:mode,lines:saved,total_before_vat:netTotal,total_vat:vatTotal,total_with_vat:grossTotal});
      if(typeof stockBatchClearV148==='function') stockBatchClearV148();
      if(typeof financeLoadAll==='function') await financeLoadAll();
      try{ if(typeof stockBatchLoadV148==='function') await stockBatchLoadV148(true); }catch(e){}
      setTimeout(bootV230,250);
    }catch(e){ msg2(e.message||String(e),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };
  window.inventorySaveMovement=async function(btn){
    try{
      if(btn) btn.disabled=true;
      const itemId=$('inventoryMovementItem')?.value; if(!itemId) throw new Error('اختر الصنف');
      const item=itemById(itemId); if(!item) throw new Error('الصنف غير موجود');
      const type=$('inventoryMovementType')?.value||'out'; const q=N($('inventoryMovementQty')?.value); if(q<=0) throw new Error('الكمية مطلوبة');
      const pid=$('inventoryMovementProject')?.value;
      if(OUT_TYPES.has(type) && N(item.quantity)<q) throw new Error('الكمية المتوفرة لا تكفي');
      let cost=N(item.unit_cost);
      if(OUT_TYPES.has(type)){ const f=fifoConsume(itemId,q); cost=f.avg||N(item.unit_cost); }
      const row={item_id:Number(itemId),item_name:item.name||'',movement_type:type,quantity:q,movement_date:$('inventoryMovementDate')?.value||today2(),project_id:pid?Number(pid):null,project_name:pid?(typeof projectName==='function'?projectName(pid):''):'',receiver:$('inventoryMovementReceiver')?.value||'',reason:$('inventoryMovementReason')?.value||'',notes:$('inventoryMovementNotes')?.value||'',product_code:codeOf(item),barcode:barcodeOf(item),unit_cost:+cost.toFixed(4)};
      const id=$('inventoryMovementId')?.value; if(id) throw new Error('تعديل حركة المخزون غير متاح؛ احذف الحركة وأضف حركة جديدة للحفاظ على دقة الرصيد');
      const res=await sb.from('inventory_movements').insert(row).select('*').single(); if(res.error) throw res.error;
      let newQty=N(item.quantity); if(type==='in'||type==='return') newQty+=q; else if(type==='out'||type==='consume') newQty-=q; else if(type==='adjust') newQty=q;
      const wc=OUT_TYPES.has(type)?weightedCurrent(itemId):{avg:N(item.unit_cost)};
      const upd={quantity:newQty}; if(wc.avg>0) upd.unit_cost=+wc.avg.toFixed(4);
      const ur=await sb.from('inventory_items').update(upd).eq('id',itemId); if(ur.error) throw ur.error;
      msg2(OUT_TYPES.has(type)?'تم حفظ الصرف بتكلفة FIFO من أقدم دفعة':'تم حفظ حركة المخزون','ok');
      if(typeof inventoryClearMovementForm==='function') inventoryClearMovementForm();
      if(typeof financeLoadAll==='function') await financeLoadAll();
      setTimeout(bootV230,250);
    }catch(e){ msg2(e.message||String(e),'err'); }
    finally{ if(btn) btn.disabled=false; }
  };
  function productDetailHtml(itemId){
    const item=itemById(itemId); if(!item) return '';
    const lots=fifoLots(itemId); const wc=weightedCurrent(itemId);
    const insRows=lots.map(l=>`<tr><td>${E(l.date||'-')}</td><td>${E(l.invoice||'-')}</td><td>${E(l.supplier||'-')}</td><td>${qty2(l.originalQty)}</td><td>${qty2(l.issued)}</td><td>${qty2(l.remaining)}</td><td>${money2(l.cost)}</td><td>${money2(N(l.remaining)*N(l.cost))}</td></tr>`).join('')||'<tr><td colspan="8">لا توجد دفعات إدخال</td></tr>';
    const outRows=outMovements(itemId).map(o=>{ const f=fifoConsume(itemId,o.qty,o.id); const m=o.raw||{}; const used=f.used.map(u=>`${u.invoice}: ${qty2(u.taken)} × ${money2(u.cost)}`).join(' / '); return `<tr><td>${E(o.date||'-')}</td><td>${E(m.project_name||'-')}</td><td>${E(m.receiver||'-')}</td><td>${qty2(o.qty)}</td><td>${money2(f.avg||m.unit_cost||item.unit_cost)}</td><td>${money2(f.value||N(o.qty)*N(m.unit_cost||item.unit_cost))}</td><td>${E(used||'-')}</td><td>${E(m.reason||'-')}</td></tr>`; }).join('')||'<tr><td colspan="8">لا توجد عمليات صرف</td></tr>';
    const img=item.image_url?`<img src="${E(item.image_url)}" style="width:96px;height:96px;object-fit:contain;border:1px solid #d7e4df;border-radius:18px;background:#fff">`:'';
    return `<div class="modal-backdrop v225-product-modal" onclick="if(event.target===this)this.remove()"><div class="modal-card" style="max-width:1120px"><div class="modal-head"><h2>تفاصيل المنتج</h2><button onclick="this.closest('.modal-backdrop').remove()">إغلاق</button></div><div class="grid two"><div class="card soft-card"><h2>${E(item.name)}</h2><p>كود المنتج: <b>${E(codeOf(item))}</b></p><p>المورد: <b>${E(item.supplier||'-')}</b> | الوحدة: <b>${E(item.unit||'-')}</b></p><p>المتوفر: <b>${qty2(wc.qty||item.quantity)}</b></p></div><div class="card soft-card" style="text-align:center">${img}</div></div><h3>دفعات الشراء وأسعارها الحقيقية</h3><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>الفاتورة</th><th>المورد</th><th>كمية الدفعة</th><th>المصروف منها</th><th>المتبقي</th><th>سعر الدفعة</th><th>قيمة المتبقي</th></tr></thead><tbody>${insRows}</tbody></table></div><h3>الصرف حسب أقدم دفعة أولًا FIFO</h3><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>المشروع</th><th>المستلم</th><th>الكمية</th><th>تكلفة الوحدة</th><th>قيمة الصرف</th><th>تم الخصم من الدفعات</th><th>السبب</th></tr></thead><tbody>${outRows}</tbody></table></div></div></div>`;
  }
  function openProductDetail(itemId){ const html=productDetailHtml(itemId); if(html) document.body.insertAdjacentHTML('beforeend',html); }
  window.v118ShowProductDetail=openProductDetail;
  window.inventoryOpenItemSmart=openProductDetail;
  // Keep old report details synchronized when user is on reports tab.
  const oldFinanceRenderReports=window.financeRenderReports;
  window.financeRenderReports=function(){
    if(oldFinanceRenderReports) oldFinanceRenderReports.apply(this,arguments);
    try{
      const box=$('inventoryProductDetailBox'); const itemId=$('inventoryReportProduct')?.value;
      if(box && itemId){
        const item=itemById(itemId); if(!item) return;
        const lots=fifoLots(itemId); const wc=weightedCurrent(itemId);
        const rows=lots.map(l=>`<tr><td>${E(l.date||'-')}</td><td>${E(l.invoice||'-')}</td><td>${E(l.supplier||'-')}</td><td>${qty2(l.originalQty)}</td><td>${qty2(l.issued)}</td><td>${qty2(l.remaining)}</td><td>${money2(l.cost)}</td><td>${money2(N(l.remaining)*N(l.cost))}</td></tr>`).join('')||'<tr><td colspan="8">لا توجد دفعات إدخال</td></tr>';
        box.innerHTML=`<h3>تفاصيل ${E(item.name||'-')}</h3><div class="grid"><div class="metric"><small>المتوفر</small><b>${qty2(wc.qty||item.quantity)}</b></div></div><h3>دفعات الشراء وأسعارها الحقيقية</h3><table><thead><tr><th>التاريخ</th><th>الفاتورة</th><th>المورد</th><th>كمية الدفعة</th><th>المصروف منها</th><th>المتبقي</th><th>سعر الدفعة</th><th>قيمة المتبقي</th></tr></thead><tbody>${rows}</tbody></table>`;
      }
    }catch(e){ console.warn('V230 report detail warning',e); }
  };
  function updateVisiblePrices(){
    try{
      A(D().inventoryItems).forEach(it=>{
        const wc=weightedCurrent(it.id);
        if(wc.avg>0) it.unit_cost=+wc.avg.toFixed(4);
      });
      document.querySelectorAll('.export-hero-badge-v222').forEach(x=>x.textContent='V230');
      document.querySelectorAll('small').forEach(s=>{ if(S(s.textContent)==='سعر الحبة') s.textContent='متوسط تكلفة الوحدة'; });
    }catch(e){}
  }
  const oldRenderItems=window.inventoryRenderItems;
  window.inventoryRenderItems=function(){ if(oldRenderItems) oldRenderItems.apply(this,arguments); setTimeout(()=>{ enhanceInvoiceForm(); updateVisiblePrices(); },60); };
  function bootV230(){ enhanceInvoiceForm(); updateVisiblePrices(); try{ if(typeof financeRenderReports==='function') financeRenderReports(); }catch(e){} }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(bootV230,ev==='load'?1100:350)));
  setTimeout(bootV230,1500);
  console.log('Tasneef V230 stable inventory FIFO lots loaded');
})();

/* ===== Tasneef V230: fix product detail modal layout + stable attendance select ===== */
(function(){
  const VERSION='V230';
  function injectCss(){
    if(document.getElementById('tasneefV230Css')) return;
    const st=document.createElement('style');
    st.id='tasneefV230Css';
    st.textContent=`
      .modal-backdrop.v225-product-modal,.modal-backdrop.v226-product-modal{
        position:fixed!important; inset:0!important; width:100vw!important; height:100vh!important;
        z-index:2147483600!important; background:rgba(6,32,26,.55)!important;
        display:flex!important; align-items:center!important; justify-content:center!important;
        padding:18px!important; overflow:auto!important; direction:rtl!important;
      }
      .modal-backdrop.v225-product-modal>.modal-card,.modal-backdrop.v226-product-modal>.modal-card{
        width:min(1120px,96vw)!important; max-width:min(1120px,96vw)!important; max-height:92vh!important;
        overflow:auto!important; margin:auto!important; background:#fff!important; border:1px solid #d7e7e0!important;
        border-radius:24px!important; box-shadow:0 30px 90px rgba(0,0,0,.28)!important; padding:0!important;
      }
      .modal-backdrop.v225-product-modal .modal-head,.modal-backdrop.v226-product-modal .modal-head{
        position:sticky!important; top:0!important; z-index:5!important; display:flex!important; align-items:center!important;
        justify-content:space-between!important; gap:12px!important; background:linear-gradient(135deg,#064633,#0A7054)!important;
        color:#fff!important; padding:14px 18px!important; border-radius:24px 24px 0 0!important;
      }
      .modal-backdrop.v225-product-modal .modal-head h2,.modal-backdrop.v226-product-modal .modal-head h2{color:#fff!important;margin:0!important}
      .modal-backdrop.v225-product-modal .modal-head button,.modal-backdrop.v226-product-modal .modal-head button{background:#fff!important;color:#064633!important;border-radius:12px!important}
      .modal-backdrop.v225-product-modal .card,.modal-backdrop.v226-product-modal .card{box-shadow:none!important;margin:12px!important}
      .modal-backdrop.v225-product-modal h3,.modal-backdrop.v226-product-modal h3{margin:16px 18px 8px!important;color:#064633!important}
      .modal-backdrop.v225-product-modal .table-wrap,.modal-backdrop.v226-product-modal .table-wrap{margin:0 18px 18px!important;max-height:42vh!important}
      #attendanceMatrixSupervisor,#attendanceSupervisor,#attendanceFilterSupervisor{background:#fff!important;color:#10231d!important;box-shadow:none!important;outline:none!important}
      #attendanceMatrixSupervisor option,#attendanceSupervisor option,#attendanceFilterSupervisor option{background:#fff!important;color:#10231d!important}
    `;
    document.head.appendChild(st);
  }
  function removeProductModals(){
    document.querySelectorAll('.modal-backdrop').forEach(m=>{
      try{
        const txt=(m.textContent||'').trim();
        if(m.classList.contains('v225-product-modal') || m.classList.contains('v226-product-modal') || txt.includes('تفاصيل المنتج')) m.remove();
      }catch(e){}
    });
  }
  const oldOpen = window.inventoryOpenItemSmart || window.v118ShowProductDetail;
  function openFixedProduct(itemId){
    injectCss();
    removeProductModals();
    if(typeof oldOpen==='function') oldOpen(itemId);
    setTimeout(()=>{
      const modals=[...document.querySelectorAll('.modal-backdrop')].filter(m=>(m.textContent||'').includes('تفاصيل المنتج'));
      modals.slice(0,-1).forEach(m=>m.remove());
      const last=modals[modals.length-1];
      if(last){ last.classList.add('v226-product-modal'); last.classList.add('v225-product-modal'); last.scrollTop=0; }
    },30);
  }
  window.inventoryOpenItemSmart=openFixedProduct;
  window.v118ShowProductDetail=openFixedProduct;
  window.tasneefCloseProductDetailV230=removeProductModals;
  function boot(){
    injectCss();
    document.querySelectorAll('.export-hero-badge-v222').forEach(x=>x.textContent=VERSION);
    document.querySelectorAll('h1,h2,h3,p,small,span').forEach(el=>{
      if((el.textContent||'').includes('V230')) el.textContent=el.textContent.replace(/V230/g,VERSION);
    });
    removeProductModals();
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(boot,ev==='load'?900:200)));
  setTimeout(boot,1200);
  console.log('Tasneef V230 modal stability loaded');
})();


/* ===== Tasneef V230: Inventory detail cleanup + complete meeting Excel reports ===== */
(function(){
  if(window.__tasneefV230ReportsPatch) return;
  window.__tasneefV230ReportsPatch = true;
  const VERSION='V230';
  const A=v=>Array.isArray(v)?v:[];
  const S=v=>String(v??'').trim();
  const N=v=>{ const n=Number(String(v??0).replace(/,/g,'')); return Number.isFinite(n)?n:0; };
  const D=()=>window.data || (typeof data!=='undefined'?data:{}) || {};
  const E=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&apos;'}[c]));
  const today=()=>new Date().toISOString().slice(0,10);
  const fmtDate=v=>{ if(!v) return '-'; try{ return new Date(v).toLocaleString('ar-SA'); }catch(e){ return S(v)||'-'; } };
  const money=v=> (Math.round(N(v)*100)/100).toLocaleString('ar-SA',{minimumFractionDigits:2,maximumFractionDigits:2})+' ر.س';
  const qty=v=> Math.round(N(v)*100)/100;
  function byId(id){ return document.getElementById(id); }
  function pName(id){ try{ if(typeof projectName==='function') return projectName(id); }catch(e){} const p=A(D().projects).find(x=>S(x.id)===S(id)); return p?.name || S(id||'-'); }
  function sName(id){ try{ if(typeof supervisorName==='function') return supervisorName(id); }catch(e){} const u=A(D().supervisors).concat(A(D().users)).find(x=>S(x.id)===S(id)); return u?.full_name || u?.name || u?.username || S(id||'-'); }
  function wName(id){ const w=A(D().workers).find(x=>S(x.id)===S(id)); return w?.name || w?.full_name || S(id||'-'); }
  function itemName(id){ const i=A(D().inventoryItems).find(x=>S(x.id)===S(id)); return i?.name || S(id||'-'); }
  function itemCode(i){ return i?.product_code || i?.serial_number || i?.barcode || i?.code || ''; }
  function itemUnitCost(i){ return N(i?.unit_cost || i?.cost || i?.price); }
  function currentExportFilters(){
    const month=byId('meetingExportMonth')?.value || byId('monthlyMonth')?.value || today().slice(0,7);
    const supervisor=byId('meetingExportSupervisor')?.value || '';
    const project=byId('meetingExportProject')?.value || '';
    return {month,supervisor,project};
  }
  function inMonth(v,month){ if(!month) return true; const s=S(v); if(s.startsWith(month)) return true; try{ return new Date(v).toISOString().slice(0,7)===month; }catch(e){ return false; } }
  function projectMatches(pid){ const f=currentExportFilters(); if(f.project && S(pid)!==S(f.project)) return false; if(f.supervisor){ const p=A(D().projects).find(x=>S(x.id)===S(pid)); if(!p || S(p.supervisor_id)!==S(f.supervisor)) return false; } return true; }
  function filteredProjects(){ const f=currentExportFilters(); return A(D().projects).filter(p=>(!f.project||S(p.id)===S(f.project)) && (!f.supervisor||S(p.supervisor_id)===S(f.supervisor))); }
  function filteredLogs(){ const f=currentExportFilters(); return A(D().logs).filter(l=>inMonth(l.log_date||l.check_in||l.created_at,f.month) && projectMatches(l.project_id)); }
  function filteredAttendance(){ const f=currentExportFilters(); return A(D().attendance).filter(a=>inMonth(a.attendance_date||a.date||a.created_at,f.month) && (!f.supervisor || S(a.supervisor_id || (A(D().workers).find(w=>S(w.id)===S(a.worker_id))||{}).supervisor_id)===S(f.supervisor))); }
  function filteredTickets(){ const f=currentExportFilters(); return A(D().tickets).filter(t=>inMonth(t.created_at||t.opened_at||t.date,f.month) && projectMatches(t.project_id)); }
  function filteredMovements(){ const f=currentExportFilters(); return A(D().inventoryMovements).filter(m=>inMonth(m.movement_date||m.created_at,f.month) && (!m.project_id || projectMatches(m.project_id))); }
  function filteredExpenses(){ const f=currentExportFilters(); return A(D().financeExpenses).filter(e=>inMonth(e.expense_date||e.created_at,f.month) && (!e.project_id || projectMatches(e.project_id))); }
  function filteredClientReports(){ const f=currentExportFilters(); return A(D().clientReports).filter(r=>inMonth(r.report_date||r.created_at,f.month) && (!r.project_id || projectMatches(r.project_id))); }
  function filteredRatings(){ const f=currentExportFilters(); return A(D().clientServiceRatings).filter(r=>inMonth(r.rating_date||r.created_at,f.month) && (!r.project_id || projectMatches(r.project_id))); }
  function xmlType(v){ return (typeof v==='number' && Number.isFinite(v))?'Number':'String'; }
  function sheetXml(name, rows, opt={}){
    const title=E(opt.title||name), sub=E(opt.subtitle||''), headers=rows[0]||['البيان'];
    const body=rows.slice(1), cols=Math.max(1,headers.length), rowCount=body.length+4;
    let xml=`<Worksheet ss:Name="${E(name).slice(0,31)}" ss:RightToLeft="1"><Table ss:ExpandedColumnCount="${cols}" ss:ExpandedRowCount="${rowCount}" x:FullColumns="1" x:FullRows="1">`;
    for(let i=0;i<cols;i++) xml += `<Column ss:AutoFitWidth="0" ss:Width="135"/>`;
    xml+=`<Row ss:Height="32"><Cell ss:MergeAcross="${Math.max(0,cols-1)}" ss:StyleID="Title"><Data ss:Type="String">${title}</Data></Cell></Row>`;
    xml+=`<Row><Cell ss:MergeAcross="${Math.max(0,cols-1)}" ss:StyleID="SubTitle"><Data ss:Type="String">${sub}</Data></Cell></Row><Row/>`;
    xml+=`<Row>${headers.map(h=>`<Cell ss:StyleID="Header"><Data ss:Type="String">${E(h)}</Data></Cell>`).join('')}</Row>`;
    body.forEach((r,idx)=>{ xml+=`<Row>${headers.map((_,i)=>{ const v=r[i]??''; return `<Cell ss:StyleID="${idx%2?'DataAlt':'Data'}"><Data ss:Type="${xmlType(v)}">${E(v)}</Data></Cell>`; }).join('')}</Row>`; });
    xml+=`</Table><WorksheetOptions xmlns="urn:schemas-microsoft-com:office:excel"><DisplayRightToLeft/><FreezePanes/><FrozenNoSplit/><SplitHorizontal>4</SplitHorizontal><TopRowBottomPane>4</TopRowBottomPane><ActivePane>2</ActivePane></WorksheetOptions></Worksheet>`;
    return xml;
  }
  function workbookXml(sheets){
    const styles=`<Styles>
      <Style ss:ID="Default" ss:Name="Normal"><Alignment ss:Vertical="Center" ss:Horizontal="Right" ss:WrapText="1"/><Font ss:FontName="Arial" ss:Size="10"/></Style>
      <Style ss:ID="Title"><Alignment ss:Horizontal="Center" ss:Vertical="Center"/><Font ss:FontName="Arial" ss:Size="16" ss:Bold="1" ss:Color="#FFFFFF"/><Interior ss:Color="#064633" ss:Pattern="Solid"/></Style>
      <Style ss:ID="SubTitle"><Alignment ss:Horizontal="Center" ss:Vertical="Center"/><Font ss:FontName="Arial" ss:Size="10" ss:Color="#24473D"/><Interior ss:Color="#EAF5EF" ss:Pattern="Solid"/></Style>
      <Style ss:ID="Header"><Alignment ss:Horizontal="Center" ss:Vertical="Center" ss:WrapText="1"/><Font ss:FontName="Arial" ss:Size="10" ss:Bold="1" ss:Color="#FFFFFF"/><Interior ss:Color="#0A7054" ss:Pattern="Solid"/><Borders><Border ss:Position="Bottom" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#BBD7C4"/><Border ss:Position="Left" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#BBD7C4"/><Border ss:Position="Right" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#BBD7C4"/><Border ss:Position="Top" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#BBD7C4"/></Borders></Style>
      <Style ss:ID="Data"><Alignment ss:Horizontal="Right" ss:Vertical="Center" ss:WrapText="1"/><Interior ss:Color="#FFFFFF" ss:Pattern="Solid"/><Borders><Border ss:Position="Bottom" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#E5EFE8"/></Borders></Style>
      <Style ss:ID="DataAlt"><Alignment ss:Horizontal="Right" ss:Vertical="Center" ss:WrapText="1"/><Interior ss:Color="#F8FCF9" ss:Pattern="Solid"/><Borders><Border ss:Position="Bottom" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#E5EFE8"/></Borders></Style>
    </Styles>`;
    return `<?xml version="1.0" encoding="UTF-8"?><?mso-application progid="Excel.Sheet"?><Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:x="urn:schemas-microsoft-com:office:excel" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet" xmlns:html="http://www.w3.org/TR/REC-html40">${styles}${sheets.join('')}</Workbook>`;
  }
  function downloadFile(name,content){ const blob=new Blob([content],{type:'application/vnd.ms-excel;charset=utf-8'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=name; document.body.appendChild(a); a.click(); setTimeout(()=>{URL.revokeObjectURL(a.href); a.remove();},600); }
  function supWorkers(sid){ const seen=new Set(), out=[]; A(D().workers).forEach(w=>{ const key=S(w.name||w.full_name||w.id); if(sid && S(w.supervisor_id||w.assigned_supervisor_id)!==S(sid)) return; if(seen.has(key)) return; seen.add(key); out.push(w); }); return out; }
  function fifoLotsForItem(itemId){
    const ins=A(D().inventoryMovements).filter(m=>S(m.item_id)===S(itemId)&&S(m.movement_type)==='in').sort((a,b)=>String(a.movement_date||a.created_at||'').localeCompare(String(b.movement_date||b.created_at||'')));
    const outs=A(D().inventoryMovements).filter(m=>S(m.item_id)===S(itemId)&&['out','consume'].includes(S(m.movement_type))).sort((a,b)=>String(a.movement_date||a.created_at||'').localeCompare(String(b.movement_date||b.created_at||'')));
    const lots=ins.map(m=>({id:m.id,date:m.movement_date||String(m.created_at||'').slice(0,10),invoice:(m.reason||m.notes||'').replace(/^إدخال مخزون\s*-?\s*/,'')||'-',supplier:m.receiver||m.supplier||'-',original:N(m.quantity),issued:0,remaining:N(m.quantity),cost:N(m.unit_cost)}));
    outs.forEach(o=>{ let need=N(o.quantity); for(const lot of lots){ if(need<=0) break; const take=Math.min(lot.remaining,need); lot.remaining-=take; lot.issued+=take; need-=take; } });
    return lots;
  }
  function executiveRows(){
    const projects=filteredProjects(), logs=filteredLogs(), tickets=filteredTickets(), moves=filteredMovements(), expenses=filteredExpenses(), reports=filteredClientReports();
    return [['المؤشر','القيمة'],['الشهر',currentExportFilters().month||'كل الأشهر'],['المشاريع ضمن الفلتر',projects.length],['المشرفون',A(D().supervisors).length||A(D().users).filter(u=>S(u.role)==='supervisor').length],['العمال',A(D().workers).length],['السجلات اليومية',logs.length],['التكتات',tickets.length],['تكتات مفتوحة',tickets.filter(t=>!['closed','done','مغلق'].includes(S(t.status).toLowerCase())).length],['حركات المخزون',moves.length],['إجمالي المصروفات',expenses.reduce((s,e)=>s+N(e.total||e.amount),0)],['تقارير العملاء',reports.length]];
  }
  function projectsRows(){ return [['اسم المشروع','المشرف الحالي','نوع التشغيل','الدقائق اليومية','دقائق الجمعة','بداية العقد','نهاية العقد','الحالة','الموقع','ملاحظات'], ...filteredProjects().map(p=>[p.name,sName(p.supervisor_id),p.operation_type||p.type||'',N(p.daily_minutes||p.required_minutes||0),N(p.friday_minutes||0),p.contract_start||p.start_date||'',p.contract_end||p.end_date||'',p.status||'',p.location||'',p.notes||''])]; }
  function supervisorsRows(){ return [['المشرف','اسم المستخدم','عدد المشاريع الحالية','عدد العمال','الحالة','ملاحظات'], ...A(D().supervisors).concat(A(D().users).filter(u=>S(u.role)==='supervisor')).filter((u,i,arr)=>arr.findIndex(x=>S(x.id)===S(u.id))===i).map(u=>[u.full_name||u.name||u.username,u.username||'',A(D().projects).filter(p=>S(p.supervisor_id)===S(u.id)).length,supWorkers(u.id).length,u.status||'نشط',u.notes||''])]; }
  function workersRows(){ const f=currentExportFilters(); const seen=new Map(); A(D().workers).forEach(w=>{ const key=S(w.name||w.full_name||w.id); if(f.supervisor && S(w.supervisor_id||w.assigned_supervisor_id)!==S(f.supervisor)) return; if(!seen.has(key)) seen.set(key,{...w,projects:[]}); const r=seen.get(key); const pn=pName(w.project_id||w.assigned_project_id); if(pn && pn!=='-' && !r.projects.includes(pn)) r.projects.push(pn); }); return [['العامل','المشرف','المشاريع','الجوال','النوع','الراتب','الحالة','ملاحظات'], ...[...seen.values()].map(w=>[w.name||w.full_name,sName(w.supervisor_id||w.assigned_supervisor_id),w.projects.join('، '),w.phone||w.mobile||'',w.type||w.worker_type||'',N(w.salary),w.status||'نشط',w.notes||''])]; }
  function logsRows(){ return [['التاريخ','المشروع','المشرف الحالي','العامل','دخول','خروج','الدقائق','الحالة','ملاحظات'], ...filteredLogs().map(l=>{ const p=A(D().projects).find(x=>S(x.id)===S(l.project_id))||{}; return [l.log_date||String(l.check_in||'').slice(0,10),pName(l.project_id),sName(p.supervisor_id||l.supervisor_id),wName(l.worker_id)||l.worker_name||'',l.check_in||'',l.check_out||'',N(l.duration_minutes),l.status||'',l.notes||'']; })]; }
  function attendanceRows(){ return [['التاريخ','العامل','المشرف','الحالة','ملاحظة'], ...filteredAttendance().map(a=>[a.attendance_date||a.date||String(a.created_at||'').slice(0,10),wName(a.worker_id)||a.worker_name||'',sName(a.supervisor_id||a.supervisorId),a.status||'',a.notes||''])]; }
  function monthlyRows(){ const map={}; filteredLogs().forEach(l=>{ const p=A(D().projects).find(x=>S(x.id)===S(l.project_id))||{}; const k=S(l.project_id); map[k]=map[k]||{project:pName(l.project_id),sup:sName(p.supervisor_id||l.supervisor_id),mins:0,logs:0,type:p.operation_type||p.type||''}; map[k].mins+=N(l.duration_minutes); map[k].logs++; }); return [['المشروع','المشرف الحالي','نوع التشغيل','عدد السجلات','مجموع الدقائق','النسبة/ملاحظة'], ...Object.values(map).map(r=>[r.project,r.sup,r.type,r.logs,Math.round(r.mins),'حسب سجلات الشهر'])]; }
  function ticketsRows(){ return [['رقم التكت','المشروع','المشرف الحالي','رفع بواسطة','العنوان','الوصف','الحالة','الأولوية','تاريخ الفتح','تاريخ الإغلاق','طريقة الإغلاق','ملاحظات'], ...filteredTickets().map(t=>{ const p=A(D().projects).find(x=>S(x.id)===S(t.project_id))||{}; return [t.ticket_no||t.id,pName(t.project_id),sName(p.supervisor_id||t.supervisor_id),t.created_by_name||t.created_by||t.reported_by||'',t.title||t.subject||'',t.description||'',t.status||'',t.priority||'',fmtDate(t.created_at||t.opened_at),fmtDate(t.closed_at),t.close_method||t.resolution||'',t.notes||'']; })]; }
  function inventoryRows(){ return [['الصنف','الكود','التصنيف','المورد','الوحدة','الكمية الحالية','الحد الأدنى','متوسط تكلفة الوحدة','قيمة المخزون','الحالة'], ...A(D().inventoryItems).map(i=>[i.name,itemCode(i),i.category||i.item_type||'',i.supplier||'',i.unit||'',qty(i.quantity),qty(i.min_quantity||i.reorder_level),itemUnitCost(i),N(i.quantity)*itemUnitCost(i),N(i.quantity)<=N(i.min_quantity||i.reorder_level)?'منخفض':'متوفر'])]; }
  function movementRows(){ return [['التاريخ','الصنف','الكود','نوع الحركة','الكمية','تكلفة الوحدة المسجلة','المشروع','المستلم/المورد','السبب','ملاحظات'], ...filteredMovements().map(m=>[m.movement_date||String(m.created_at||'').slice(0,10),m.item_name||itemName(m.item_id),m.product_code||m.barcode||'',m.movement_type,qty(m.quantity),N(m.unit_cost),m.project_name||pName(m.project_id),m.receiver||'',m.reason||'',m.notes||''])]; }
  function fifoRows(){ const rows=[['الصنف','كود الصنف','تاريخ الدفعة','الفاتورة','المورد','كمية الدفعة','المصروف من الدفعة','المتبقي','سعر الدفعة','قيمة المتبقي']]; A(D().inventoryItems).forEach(i=>fifoLotsForItem(i.id).forEach(l=>rows.push([i.name,itemCode(i),l.date,l.invoice,l.supplier,qty(l.original),qty(l.issued),qty(l.remaining),N(l.cost),N(l.remaining)*N(l.cost)]))); return rows; }
  function expensesRows(){ return [['التاريخ','المشروع','التصنيف','المورد','المبلغ','طريقة الدفع','مركز التكلفة','ملاحظات'], ...filteredExpenses().map(e=>[e.expense_date||String(e.created_at||'').slice(0,10),e.project_name||pName(e.project_id),e.category||e.expense_type||'',e.supplier||'',N(e.total||e.amount),e.payment_method||'',e.cost_center||'',e.notes||''])]; }
  function clientReportsRows(){ const services=A(D().clientReportServices); return [['التاريخ','رقم التقرير','المشروع','الشهر','المشرف','العنوان','الحالة','الخدمات','النص/الملخص','ملاحظات'], ...filteredClientReports().map(r=>[r.report_date||String(r.created_at||'').slice(0,10),r.report_no||r.id,r.project_name||pName(r.project_id),String(r.report_date||r.created_at||'').slice(0,7),sName(r.supervisor_id),r.title||'',r.status||'',services.filter(s=>S(s.report_id)===S(r.id)).map(s=>s.service_name||s.service_type).filter(Boolean).join('، '),r.summary||r.description||r.report_text||'',r.notes||''])]; }
  function ratingsRows(){ return [['التاريخ','المشروع','الخدمة','التقرير المرتبط','التقييم','ملاحظة العميل','تحتاج متابعة','الحالة'], ...filteredRatings().map(r=>[fmtDate(r.rating_date||r.created_at),r.project_name||pName(r.project_id),r.service_name||r.service_type||'',r.report_id||'',r.rating||r.score||'',r.comment||r.notes||'',r.need_followup||r.needs_follow_up?'نعم':'لا',r.status||''])]; }
  function alertsRows(){ const rows=[['نوع التنبيه','العنصر','الأهمية','الملاحظة']]; A(D().projects).filter(p=>!p.supervisor_id).forEach(p=>rows.push(['مشروع بدون مشرف',p.name,'حرج','يحتاج ربط مشرف'])); A(D().workers).filter(w=>!w.supervisor_id&&!w.assigned_supervisor_id).forEach(w=>rows.push(['عامل بدون مشرف',w.name||w.full_name,'عادي','يحتاج ربط مشرف'])); A(D().logs).filter(l=>l.check_in&&!l.check_out).forEach(l=>rows.push(['دخول بدون خروج',pName(l.project_id),'حرج',sName(l.supervisor_id)])); filteredTickets().filter(t=>!['closed','done','مغلق'].includes(S(t.status).toLowerCase())).forEach(t=>rows.push(['تكت مفتوح',pName(t.project_id),'عادي',t.title||t.subject||''])); return rows; }
  function usersRows(){ return [['الاسم','اسم المستخدم','الدور','الحالة','المشرف المرتبط','ملاحظات'], ...A(D().users).map(u=>[u.full_name||u.name,u.username||'',u.role||'',u.status||'',sName(u.supervisor_id||u.linked_supervisor_id),u.notes||''])]; }
  function suppliersRows(){ return [['المورد','الجوال','العنوان','الحالة','ملاحظات'], ...A(D().suppliers||D().inventorySuppliers).map(s=>[s.name||s.supplier_name,s.phone||s.mobile||'',s.address||'',s.status||'',s.notes||''])]; }
  function exportSheetsV230(){ const f=currentExportFilters(); const sub=`شركة تصنيف لإدارة المرافق - تاريخ التصدير ${fmtDate(new Date())} - الشهر: ${f.month||'كل الأشهر'} - المشرف: ${f.supervisor?sName(f.supervisor):'كل المشرفين'} - المشروع: ${f.project?pName(f.project):'كل المشاريع'}`; return [
    sheetXml('الملخص التنفيذي',executiveRows(),{title:'الملخص التنفيذي للاجتماع',subtitle:sub}),
    sheetXml('المشاريع',projectsRows(),{title:'المشاريع - كل التفاصيل',subtitle:sub}),
    sheetXml('المستخدمون',usersRows(),{title:'المستخدمون والصلاحيات الأساسية',subtitle:sub}),
    sheetXml('المشرفون',supervisorsRows(),{title:'المشرفون',subtitle:sub}),
    sheetXml('العمال',workersRows(),{title:'العمال بدون تكرار',subtitle:sub}),
    sheetXml('السجلات اليومية',logsRows(),{title:'السجلات اليومية',subtitle:sub}),
    sheetXml('الحضور والغياب',attendanceRows(),{title:'الحضور والغياب',subtitle:sub}),
    sheetXml('الأوقات الشهرية',monthlyRows(),{title:'الأوقات الشهرية',subtitle:sub}),
    sheetXml('التكتات',ticketsRows(),{title:'التكتات',subtitle:sub}),
    sheetXml('المخزون',inventoryRows(),{title:'المخزون',subtitle:sub}),
    sheetXml('حركة المخزون',movementRows(),{title:'حركة المخزون',subtitle:sub}),
    sheetXml('دفعات FIFO',fifoRows(),{title:'دفعات الشراء وأسعارها الحقيقية FIFO',subtitle:sub}),
    sheetXml('الموردون',suppliersRows(),{title:'الموردون',subtitle:sub}),
    sheetXml('المصروفات',expensesRows(),{title:'المصروفات',subtitle:sub}),
    sheetXml('تقارير العملاء',clientReportsRows(),{title:'تقارير العملاء',subtitle:sub}),
    sheetXml('تقييمات العملاء',ratingsRows(),{title:'تقييمات العملاء',subtitle:sub}),
    sheetXml('التنبيهات',alertsRows(),{title:'التنبيهات',subtitle:sub})
  ]; }
  async function ensureExportData(){ try{ if(typeof refreshAll==='function') await refreshAll(); }catch(e){} try{ if(typeof financeLoadAll==='function') await financeLoadAll(false); }catch(e){} try{ if(typeof loadPremiumReportsOnly==='function') await loadPremiumReportsOnly(false); }catch(e){} }
  window.exportMeetingExcelV230 = async function(btn){ try{ if(btn) btn.disabled=true; await ensureExportData(); const f=currentExportFilters(); downloadFile(`تقرير اجتماع تصنيف الشامل - ${f.month||today().slice(0,7)}.xls`, workbookXml(exportSheetsV230())); if(typeof msg==='function') msg('تم تنزيل ملف الاجتماع الشامل بكل تفاصيل التبويبات','ok'); } catch(e){ console.error(e); if(typeof msg==='function') msg(e.message||'تعذر التصدير','err'); alert(e.message||'تعذر التصدير'); } finally{ if(btn) btn.disabled=false; } };
  window.exportMeetingExcelV223 = window.exportMeetingExcelV230;
  window.exportMeetingExcelV222 = window.exportMeetingExcelV230;
  window.exportMeetingExcelV221 = window.exportMeetingExcelV230;
  function cleanupDuplicateBatchBlocks(){ document.querySelectorAll('#purchaseBatchesV208').forEach(x=>x.remove()); }
  const oldOpen=window.inventoryOpenItemSmart || window.v118ShowProductDetail;
  if(typeof oldOpen==='function'){
    window.inventoryOpenItemSmart=function(id){ const r=oldOpen(id); setTimeout(cleanupDuplicateBatchBlocks,80); setTimeout(cleanupDuplicateBatchBlocks,500); return r; };
    window.v118ShowProductDetail=window.inventoryOpenItemSmart;
  }
  function boot(){ cleanupDuplicateBatchBlocks(); document.querySelectorAll('.export-hero-badge-v222').forEach(x=>x.textContent=VERSION); document.querySelectorAll('script[src*="app.js?v="]').forEach(()=>{}); }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(boot,ev==='load'?900:250)));
  setInterval(cleanupDuplicateBatchBlocks,1600);
  setTimeout(boot,1200);
  console.log('Tasneef V230 export complete + product detail cleanup loaded');
})();


/* ===== TASNEEF V230: attendance filter fix + inventory manager cleanup + speed guard ===== */
(function(){
  if(window.__tasneefV230FinalFix) return;
  window.__tasneefV230FinalFix = true;
  const VERSION='V230';
  const $id=id=>document.getElementById(id);
  const A=v=>Array.isArray(v)?v:[];
  const S=v=>String(v??'').trim();
  const E=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const D=()=>window.data || (typeof data!=='undefined'?data:{}) || {};
  function today228(){ try{return today()}catch(e){return new Date().toISOString().slice(0,10);} }
  function supIdOfWorker(w){ try{return workerSupId(w)}catch(e){return w?.supervisor_id||w?.assigned_supervisor_id||w?.supervisor||'';} }
  function supName228(id){ try{return supervisorName(id)}catch(e){ return A(D().supervisors).concat(A(D().users)).find(u=>String(u.id)===String(id))?.full_name || '-'; } }
  function uniqueByIdName(rows){
    const seen=new Set();
    return A(rows).filter(w=>{ const key=String(w.id||w.name||'').trim(); if(!key||seen.has(key)) return false; seen.add(key); return true; });
  }
  function supervisorRows228(){
    const rows=A(D().supervisors).length?A(D().supervisors):A(D().users).filter(u=>u.role==='supervisor'||u.role==='مشرف');
    return uniqueByIdName(rows).filter(x=>x.id!==undefined&&x.id!==null);
  }
  function fillMonthlySupervisor228(){
    const el=$id('attendanceMatrixSupervisor'); if(!el) return '';
    const old=el.value||'';
    const opts=supervisorRows228().map(s=>`<option value="${E(s.id)}">${E(s.full_name||s.username||s.name||'-')}</option>`).join('');
    el.innerHTML='<option value="">كل المشرفين</option>'+opts;
    if([...el.options].some(o=>o.value===old)) el.value=old; else el.value='';
    return el.value||'';
  }
  function ensureAttendanceMonth228(){
    const el=$id('attendanceMatrixMonth');
    if(el && (!el.value || el.value.includes('----'))) el.value=today228().slice(0,7);
    return el?.value || today228().slice(0,7);
  }
  function statusText228(status){ return status==='present'?'ح':(status==='absent'?'غ':'-'); }
  function statusClass228(status){ return status==='present'?'green':(status==='absent'?'red':''); }
  function recForDay228(workerId,ds){
    // إذا تكرر التحضير لنفس العامل في نفس اليوم نأخذ أحدث سجل متاح.
    const rows=A(D().attendance).filter(r=>String(r.worker_id)===String(workerId)&&S(r.attendance_date)===ds);
    return rows[rows.length-1]||null;
  }
  window.renderAttendanceMonthly=function(){
    const body=$id('attendanceMatrixBody'), head=$id('attendanceMatrixHead'); if(!body||!head) return;
    const month=ensureAttendanceMonth228();
    const sid=fillMonthlySupervisor228();
    const q=S($id('attendanceMatrixSearch')?.value);
    const [yy,mm]=month.split('-').map(Number); const daysCount=(!yy||!mm)?31:new Date(yy,mm,0).getDate();
    const days=Array.from({length:daysCount},(_,i)=>String(i+1).padStart(2,'0'));
    let workers=uniqueByIdName(A(D().workers));
    if(sid) workers=workers.filter(w=>String(supIdOfWorker(w))===String(sid));
    if(q) workers=workers.filter(w=>S(w.name).includes(q)||S(w.phone).includes(q));
    head.innerHTML=`<tr><th>العامل</th><th>المشرف</th>${days.map(d=>`<th>${d}</th>`).join('')}<th>حضور</th><th>غياب</th><th>النسبة</th></tr>`;
    let totalP=0,totalA=0;
    body.innerHTML=workers.map(w=>{
      let p=0,a=0;
      const cells=days.map(d=>{
        const rec=recForDay228(w.id,`${month}-${d}`);
        if(rec?.status==='present'){p++; totalP++;}
        else if(rec?.status==='absent'){a++; totalA++;}
        const st=statusText228(rec?.status);
        const cls=statusClass228(rec?.status);
        return `<td><span class="att-cell ${cls==='green'?'att-present':cls==='red'?'att-absent':'att-empty'}">${st}</span></td>`;
      }).join('');
      const pct=(p+a)?(p/(p+a)*100):0;
      return `<tr><td><b>${E(w.name||'-')}</b></td><td>${E(supName228(supIdOfWorker(w)))}</td>${cells}<td>${p}</td><td>${a}</td><td>${pct.toFixed(1)}%</td></tr>`;
    }).join('') || `<tr><td colspan="${days.length+5}">لا توجد بيانات حسب الفلتر المختار</td></tr>`;
    const summary=$id('attendanceMatrixSummary');
    if(summary){ const pct=(totalP+totalA)?(totalP/(totalP+totalA)*100):0; summary.innerHTML=`<div class="kpi"><small>عدد العمال</small><b>${workers.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalP}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalA}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`; }
  };

  function removeExactWarningText228(){
    const bad=['تنبيه تكلفة: عند إدخال نفس الصنف','سيتم حفظ سعر كل فاتورة كما هو','الصرف يعتمد FIFO','الصرف يعتمد على FIFO'];
    document.querySelectorAll('body *').forEach(el=>{
      if(!el || ['SCRIPT','STYLE','OPTION','SELECT','INPUT','TEXTAREA','TABLE','TBODY','TR','TD','TH'].includes(el.tagName)) return;
      const txt=S(el.textContent);
      if(!txt) return;
      if(bad.some(b=>txt.includes(b)) && txt.length<420){ el.remove(); }
    });
    const walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT);
    const nodes=[];
    while(walker.nextNode()){ const n=walker.currentNode; const txt=S(n.nodeValue); if(bad.some(b=>txt.includes(b))) nodes.push(n); }
    nodes.forEach(n=>{ try{ n.nodeValue=''; }catch(e){} });
  }
  function stripInventoryAdvancedBlocks228(root=document){
    if(!root) return;
    const titles=['دفعات الشراء وأسعارها الحقيقية','الصرف حسب أقدم دفعة أولًا FIFO','دفعات الشراء وأسعارها FIFO'];
    root.querySelectorAll('h3,h2').forEach(h=>{
      const t=S(h.textContent);
      if(titles.some(x=>t.includes(x))){
        let next=h.nextElementSibling;
        h.remove();
        let guard=0;
        while(next && guard++<3){
          const n=next.nextElementSibling;
          if(next.matches('.table-wrap,table,.card,div')) next.remove();
          next=n;
          if(next && /^H[23]$/.test(next.tagName)) break;
        }
      }
    });
    // remove duplicated old batch block if any.
    root.querySelectorAll('#purchaseBatchesV208').forEach(x=>x.remove());
  }
  const oldOpen228=window.inventoryOpenItemSmart || window.v118ShowProductDetail;
  if(typeof oldOpen228==='function'){
    window.inventoryOpenItemSmart=function(id){ const r=oldOpen228.apply(this,arguments); setTimeout(()=>{stripInventoryAdvancedBlocks228();removeExactWarningText228();},40); setTimeout(()=>{stripInventoryAdvancedBlocks228();removeExactWarningText228();},350); return r; };
    window.v118ShowProductDetail=window.inventoryOpenItemSmart;
  }
  const oldFinanceRenderReports228=window.financeRenderReports;
  if(typeof oldFinanceRenderReports228==='function'){
    window.financeRenderReports=function(){ const r=oldFinanceRenderReports228.apply(this,arguments); setTimeout(()=>{stripInventoryAdvancedBlocks228($id('inventoryProductDetailBox')||document);removeExactWarningText228();},60); return r; };
  }
  // Supervisor daily report safety: refresh data before rendering and avoid breaking the supervisor page.
  const oldSupClientReportInit228=window.supClientReportInit;
  window.supClientReportInit=function(){
    try{
      const u=typeof session==='function'?session():null;
      const projects=A(D().projects).filter(p=>!u?.id || String(p.supervisor_id||'')===String(u.id) || !p.supervisor_id);
      if($id('supClientReportProject') && projects.length){
        const old=$id('supClientReportProject').value||'';
        $id('supClientReportProject').innerHTML='<option value="">اختر المشروع</option>'+projects.map(p=>`<option value="${E(p.id)}">${E(p.name||'-')}</option>`).join('');
        if([...$id('supClientReportProject').options].some(o=>o.value===old)) $id('supClientReportProject').value=old;
      }
      if($id('supClientReportDate') && !$id('supClientReportDate').value) $id('supClientReportDate').value=today228();
      if(typeof oldSupClientReportInit228==='function') oldSupClientReportInit228.apply(this,arguments);
    }catch(e){ console.warn('V230 supervisor daily report safe init',e); }
  };
  function boot228(){
    document.querySelectorAll('script[src*="app.js?v="]').forEach(()=>{});
    document.querySelectorAll('.export-hero-badge-v222').forEach(x=>x.textContent=VERSION);
    removeExactWarningText228(); stripInventoryAdvancedBlocks228();
    if($id('attendanceMatrixBody')) window.renderAttendanceMonthly();
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(boot228,ev==='load'?900:250)));
  setTimeout(boot228,1300);
  console.log('Tasneef V230 final patch loaded');
})();


/* ===== TASNEEF V230: schema-safe inventory requests + hide warehouse manager cost summaries ===== */
(function(){
  if(window.__tasneefV230SchemaSafe) return;
  window.__tasneefV230SchemaSafe = true;
  const S=v=>String(v??'').trim();
  function cleanCostText(){
    try{
      const phrases=['متوسط تكلفة الوحدة','قيمة المتبقي','متوسط التكلفة الحالي'];
      document.querySelectorAll('.modal-card p,.modal-card .metric,.modal-card small,.modal-card b,.soft-card p').forEach(el=>{
        const t=S(el.textContent);
        if(phrases.some(p=>t.includes(p))){
          const available=t.match(/المتوفر\s*:\s*([^|]+)/);
          if(el.tagName==='P' && available){ el.innerHTML='المتوفر: <b>'+available[1].trim()+'</b>'; }
          else el.remove();
        }
      });
      document.querySelectorAll('#inventoryProductDetailBox .metric').forEach(el=>{
        const t=S(el.textContent); if(t.includes('متوسط تكلفة الوحدة')||t.includes('قيمة المتبقي')) el.remove();
      });
    }catch(e){}
  }
  // Strip unsupported requester_role from any remaining row before Supabase insert/update.
  function stripRequesterRole(row){
    try{
      if(row && typeof row==='object'){
        delete row.requester_role;
        if(Array.isArray(row)) row.forEach(stripRequesterRole);
      }
    }catch(e){}
    return row;
  }
  try{
    const oldFrom = window.sb && window.sb.from ? window.sb.from.bind(window.sb) : null;
    if(oldFrom && !window.sb.__tasneefV230FromPatched){
      window.sb.from = function(table){
        const q = oldFrom(table);
        if(String(table)==='inventory_requests'){
          ['insert','update','upsert'].forEach(fn=>{
            if(typeof q[fn]==='function'){
              const old=q[fn].bind(q);
              q[fn]=function(row){ return old(stripRequesterRole(row)); };
            }
          });
        }
        return q;
      };
      window.sb.__tasneefV230FromPatched=true;
    }
  }catch(e){}
  const oldInvOpen=window.inventoryOpenItemSmart || window.v118ShowProductDetail;
  if(typeof oldInvOpen==='function'){
    window.inventoryOpenItemSmart=function(){ const r=oldInvOpen.apply(this,arguments); setTimeout(cleanCostText,30); setTimeout(cleanCostText,300); return r; };
    window.v118ShowProductDetail=window.inventoryOpenItemSmart;
  }
  const oldReports=window.financeRenderReports;
  if(typeof oldReports==='function'){
    window.financeRenderReports=function(){ const r=oldReports.apply(this,arguments); setTimeout(cleanCostText,40); return r; };
  }
  function boot(){
    cleanCostText();
    document.querySelectorAll('.export-hero-badge-v222').forEach(x=>x.textContent='V230');
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(boot,ev==='load'?900:250)));
  setTimeout(boot,1200);
  console.log('Tasneef V230 schema safe inventory loaded');
})();

/* ===================== TASNEEF V231 - Abu Samer requested fixes =====================
   1) Contracts: show total buildings + total units.
   2) Tickets: sticky filters + project filter + title filter + priority filter.
   3) Today counters: use local date for today's logs/tickets so today's records appear correctly.
==================================================================================== */
(function(){
  if(window.__tasneefV231AbuSamerFix) return;
  window.__tasneefV231AbuSamerFix = true;

  const $ = id => document.getElementById(id);
  const A = v => Array.isArray(v) ? v : [];
  const S = v => String(v ?? '').trim();
  const N = v => { const n = Number(v ?? 0); return Number.isFinite(n) ? n : 0; };
  const E = v => S(v).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));

  function localYmd(d){
    const x = d instanceof Date ? d : new Date(d || Date.now());
    if(isNaN(x)) return '';
    return x.getFullYear() + '-' + String(x.getMonth()+1).padStart(2,'0') + '-' + String(x.getDate()).padStart(2,'0');
  }
  function todayLocal(){ return localYmd(new Date()); }
  function rowDate(row, fields){
    for(const f of fields){
      const v = row && row[f];
      if(!v) continue;
      if(/^\d{4}-\d{2}-\d{2}$/.test(S(v))) return S(v);
      const d = localYmd(v);
      if(d) return d;
    }
    return '';
  }
  function projectNameSafe(id){ try{ return typeof projectName==='function' ? projectName(id) : '-'; }catch(_){ return '-'; } }
  function supervisorNameSafe(id){ try{ return typeof supervisorName==='function' ? supervisorName(id) : '-'; }catch(_){ return '-'; } }
  function statusText(s){ s=S(s||'open'); return s==='closed'?'مغلق':(s==='processing'?'تحت المعالجة':'مفتوح'); }
  function priorityText(p){ p=S(p||'normal'); return p==='urgent'?'عاجل':(p==='high'?'مهم':(p==='low'?'منخفض':'عادي')); }
  function ticketNo(t){ return S(t.ticket_number) || ('T-' + S(t.id || 0).padStart(4,'0')); }
  function dateTimeLabel(v){
    if(!v) return '-';
    const d = new Date(v); if(isNaN(d)) return S(v);
    let h=d.getHours(); const m=String(d.getMinutes()).padStart(2,'0'); const ap=h>=12?'م':'ص'; h=h%12||12;
    return localYmd(d) + ' ' + String(h).padStart(2,'0') + ':' + m + ' ' + ap;
  }
  function durationText(t){
    const start = t.created_at || t.opened_at;
    const end = (S(t.status)==='closed' ? (t.closed_at || t.updated_at) : new Date().toISOString());
    const a = start ? new Date(start) : null, b = end ? new Date(end) : null;
    if(!a || !b || isNaN(a) || isNaN(b)) return '-';
    let mins=Math.max(0,Math.round((b-a)/60000));
    const d=Math.floor(mins/1440); mins%=1440; const h=Math.floor(mins/60); const m=mins%60;
    const parts=[]; if(d) parts.push(d+'ي'); if(h) parts.push(h+'س'); if(m || !parts.length) parts.push(m+'د'); return parts.join(' ');
  }
  function setOptions(select, rows, labelFn, allText){
    if(!select) return;
    const old = select.value;
    select.innerHTML = `<option value="">${E(allText || 'الكل')}</option>` + rows.map(r=>`<option value="${E(r.id)}">${E(labelFn(r))}</option>`).join('');
    if([...select.options].some(o=>o.value===old)) select.value = old;
  }

  function ensureContractTotalsUi(){
    if(!$('contractsActiveCount') || $('contractsBuildingsTotal')) return;
    const box = $('contractsActiveCount').closest('.kpis');
    if(!box) return;
    box.insertAdjacentHTML('beforeend',
      '<div class="kpi contract-total-v231"><small>مجموع العمائر</small><b id="contractsBuildingsTotal">0</b></div>'+
      '<div class="kpi contract-total-v231"><small>مجموع الشقق</small><b id="contractsUnitsTotal">0</b></div>'
    );
  }
  function updateContractTotals(){
    ensureContractTotalsUi();
    const projects = A(window.data && window.data.projects);
    const buildings = projects.reduce((a,p)=>a + N(p.buildings_count || p.buildings || p.building_count), 0);
    const units = projects.reduce((a,p)=>a + N(p.units_count || p.apartments_count || p.flats_count || p.units), 0);
    if($('contractsBuildingsTotal')) $('contractsBuildingsTotal').textContent = buildings.toLocaleString('ar-SA');
    if($('contractsUnitsTotal')) $('contractsUnitsTotal').textContent = units.toLocaleString('ar-SA');
  }

  function ensureTodayTicketsKpi(){
    if(!$('kpiTodayLogs') || $('kpiTodayTickets')) return;
    const box = $('kpiTodayLogs').closest('.kpis');
    if(!box) return;
    $('kpiTodayLogs').closest('.kpi').insertAdjacentHTML('afterend','<div class="kpi today-ticket-kpi-v231"><small>تكتات اليوم</small><b id="kpiTodayTickets">0</b></div>');
  }
  function updateTodayCounters(){
    const d = todayLocal();
    const logs = A(window.data && window.data.logs).filter(l => rowDate(l, ['log_date','check_in','created_at']) === d);
    const tickets = A(window.data && window.data.tickets).filter(t => rowDate(t, ['created_at','opened_at','date']) === d);
    if($('kpiTodayLogs')) $('kpiTodayLogs').textContent = logs.length.toLocaleString('ar-SA');
    ensureTodayTicketsKpi();
    if($('kpiTodayTickets')) $('kpiTodayTickets').textContent = tickets.length.toLocaleString('ar-SA');
    const div = $('todaySummary');
    if(div && A(window.data && window.data.supervisors).length){
      div.innerHTML = A(window.data.supervisors).map(s=>{
        const sLogs = logs.filter(l=>S(l.supervisor_id)===S(s.id));
        const mins = sLogs.reduce((a,l)=>a + N(l.duration_minutes || (typeof minutesBetween==='function' ? minutesBetween(l.check_in,l.check_out) : 0)),0);
        const time = typeof minsToText==='function' ? minsToText(mins) : (mins+' دقيقة');
        return `<div class="summary-item"><b>${E(s.full_name)}</b><br>عدد التسجيلات: ${sLogs.length}<br>إجمالي الوقت: ${E(time)}</div>`;
      }).join('') || '<div class="summary-item">لا توجد تسجيلات اليوم</div>';
    }
  }

  function ensureTicketFiltersUi(){
    const filters = $('ticketSearch')?.closest('.filters');
    if(filters){
      if(!$('ticketFilterProject')){
        filters.insertAdjacentHTML('afterbegin','<select id="ticketFilterProject" onchange="renderTickets()"><option value="">كل المشاريع</option></select>');
      }
      if(!$('ticketFilterPriority')){
        const status = $('ticketFilterStatus');
        (status || $('ticketSearch')).insertAdjacentHTML('afterend','<select id="ticketFilterPriority" onchange="renderTickets()"><option value="">كل الأولويات</option><option value="urgent">عاجل</option><option value="high">مهم</option><option value="normal">عادي</option><option value="low">منخفض</option></select>');
      }
      if(!$('ticketFilterTitle')){
        $('ticketSearch').insertAdjacentHTML('beforebegin','<input id="ticketFilterTitle" oninput="renderTickets()" placeholder="فلتر عنوان التكت">');
      }
      setOptions($('ticketFilterProject'), A(window.data && window.data.projects), p=>p.name || '-', 'كل المشاريع');
    }
    const supFilters = $('supTicketSearch')?.closest('.filters');
    if(supFilters){
      if(!$('supTicketFilterPriority')) $('supTicketSearch').insertAdjacentHTML('beforebegin','<select id="supTicketFilterPriority" onchange="renderTickets()"><option value="">كل الأولويات</option><option value="urgent">عاجل</option><option value="high">مهم</option><option value="normal">عادي</option><option value="low">منخفض</option></select>');
      if(!$('supTicketFilterTitle')) $('supTicketSearch').insertAdjacentHTML('beforebegin','<input id="supTicketFilterTitle" oninput="renderTickets()" placeholder="فلتر عنوان التكت">');
    }
  }
  function ticketFilters(){
    return {
      status:S($('ticketFilterStatus')?.value || $('supTicketFilterStatus')?.value || ''),
      project:S($('ticketFilterProject')?.value || $('supTicketFilterProject')?.value || ''),
      priority:S($('ticketFilterPriority')?.value || $('supTicketFilterPriority')?.value || ''),
      title:S($('ticketFilterTitle')?.value || $('supTicketFilterTitle')?.value || '').toLowerCase(),
      search:S($('ticketSearch')?.value || $('supTicketSearch')?.value || '').toLowerCase()
    };
  }
  function filteredTicketsV231(){
    const f = ticketFilters();
    let list = A(window.data && window.data.tickets).slice();
    if(f.status) list = list.filter(t=>S(t.status || 'open') === f.status);
    if(f.project) list = list.filter(t=>S(t.project_id) === f.project);
    if(f.priority) list = list.filter(t=>S(t.priority || 'normal') === f.priority);
    if(f.title) list = list.filter(t=>S(t.title).toLowerCase().includes(f.title));
    if(f.search){
      list = list.filter(t => [ticketNo(t), t.title, t.description, projectNameSafe(t.project_id), supervisorNameSafe(t.supervisor_id), statusText(t.status), priorityText(t.priority), t.claimed_by_name, t.closed_by_name, t.closure_note].join(' ').toLowerCase().includes(f.search));
    }
    return list.sort((a,b)=>(Date.parse(b.created_at||b.opened_at||'')||0)-(Date.parse(a.created_at||a.opened_at||'')||0));
  }
  function ticketSummaryHtml(list){
    const open=list.filter(t=>S(t.status||'open')==='open').length;
    const processing=list.filter(t=>S(t.status)==='processing').length;
    const closed=list.filter(t=>S(t.status)==='closed').length;
    const today=list.filter(t=>rowDate(t,['created_at','opened_at','date'])===todayLocal()).length;
    return `<div class="smart-ticket-kpi"><b>${list.length}</b><span>إجمالي التكتات</span></div><div class="smart-ticket-kpi red"><b>${open}</b><span>مفتوحة</span></div><div class="smart-ticket-kpi amber"><b>${processing}</b><span>تحت المعالجة</span></div><div class="smart-ticket-kpi green"><b>${closed}</b><span>مغلقة</span></div><div class="smart-ticket-kpi dark"><b>${today}</b><span>تكتات اليوم</span></div>`;
  }
  function ticketCard(t, mode){
    const id = Number(t.id)||0;
    const st = S(t.status||'open');
    const pr = S(t.priority||'normal');
    const stCls = st==='closed'?'closed':(st==='processing'?'processing':'open');
    const prCls = pr==='urgent'?'urgent':(pr==='high'?'high':'normal');
    const canDelete = mode === 'admin';
    const waFn = typeof window.sendTicketWhatsAppV43 === 'function' ? 'sendTicketWhatsAppV43' : (typeof window.sendTicketWhatsApp === 'function' ? 'sendTicketWhatsApp' : '');
    const viewBtn = typeof window.viewTicketSmartV147 === 'function' ? `<button type="button" onclick="viewTicketSmartV147(${id})">عرض</button>` : '';
    const pdfBtn = typeof window.ticketDownloadPdfV206 === 'function' ? `<button type="button" class="light" onclick="ticketDownloadPdfV206(${id})">PDF</button>` : '';
    const claimBtn = st!=='closed' && st!=='processing' && typeof window.claimTicket === 'function' ? `<button type="button" class="light" onclick="claimTicket(${id})">استلام</button>` : '';
    const closeBtn = st!=='closed' && typeof window.closeTicket === 'function' ? `<button type="button" onclick="closeTicket(${id})">إغلاق</button>` : '';
    const reopenBtn = st==='closed' && typeof window.setTicketStatus === 'function' ? `<button type="button" class="light" onclick="setTicketStatus(${id},'open')">إعادة فتح</button>` : '';
    const waBtn = waFn ? `<button type="button" class="ticket-wa-v147" onclick="${waFn}(${id})">واتساب</button>` : '';
    const delBtn = canDelete ? `<button type="button" class="danger" onclick="deleteRow('tickets',${id})">حذف</button>` : '';
    return `<article class="smart-ticket-card ${stCls} ${prCls}"><div class="smart-ticket-top"><div><strong>${E(ticketNo(t))}</strong><small>${E(dateTimeLabel(t.created_at||t.opened_at))}</small></div><span class="smart-ticket-status ${stCls}">${E(statusText(st))}</span></div><h3>${E(t.title||'-')}</h3><div class="smart-ticket-meta"><span>المشروع: <b>${E(projectNameSafe(t.project_id))}</b></span><span>المشرف: <b>${E(supervisorNameSafe(t.supervisor_id))}</b></span><span>الأولوية: <b>${E(priorityText(pr))}</b></span><span>مدة الفتح: <b>${E(durationText(t))}</b></span></div><p>${E(t.description||'لا يوجد وصف')}</p><div class="smart-ticket-mini"><span>استلم: ${E(t.claimed_by_name||'-')}</span><span>أغلق: ${E(t.closed_by_name||'-')}</span></div>${t.closure_note?`<div class="smart-ticket-note">الحل: ${E(t.closure_note)}</div>`:''}<div class="smart-ticket-actions">${viewBtn}${pdfBtn}<button type="button" class="light" onclick="editTicket(${id})">تعديل</button>${claimBtn}${closeBtn}${reopenBtn}${waBtn}${delBtn}</div></article>`;
  }

  const oldRenderTickets = window.renderTickets;
  window.renderTickets = function(){
    ensureTicketFiltersUi();
    const adminBody = $('ticketsBody');
    const supBody = $('supTicketsBody');
    if(!adminBody && !supBody){ if(typeof oldRenderTickets === 'function') return oldRenderTickets.apply(this, arguments); return; }
    const list = filteredTicketsV231();
    if($('ticketsSmartSummary')) $('ticketsSmartSummary').innerHTML = ticketSummaryHtml(list);
    if($('supTicketsSmartSummary')) $('supTicketsSmartSummary').innerHTML = ticketSummaryHtml(list);
    if(adminBody){ adminBody.classList.add('smart-ticket-grid'); adminBody.innerHTML = list.map(t=>ticketCard(t,'admin')).join('') || '<div class="empty-smart-ticket">لا توجد تكتات مطابقة للفلاتر الحالية</div>'; }
    if(supBody){ supBody.classList.add('smart-ticket-grid'); supBody.innerHTML = list.map(t=>ticketCard(t,'supervisor')).join('') || '<div class="empty-smart-ticket">لا توجد تكتات مطابقة للفلاتر الحالية</div>'; }
    setTimeout(()=>{ try{ if(typeof window.ticketsDownloadPdfV206 === 'function') document.querySelectorAll('#ticketPdfBtnV206,#supTicketPdfBtnV206').forEach(b=>b.onclick=window.ticketsDownloadPdfV206); }catch(_){} },50);
  };

  window.filterLogs = function(){
    let rows = A(window.data && window.data.logs).slice();
    const d = S($('dailyDate')?.value || $('supLogDate')?.value || '');
    const s = S($('dailySupervisor')?.value || '');
    const p = S($('dailyProject')?.value || $('logProjectFilter')?.value || '');
    const q = S($('dailySearch')?.value || '').toLowerCase();
    if(d) rows = rows.filter(l => rowDate(l, ['log_date','check_in','created_at']) === d);
    if(s) rows = rows.filter(l => S(l.supervisor_id) === s);
    if(p) rows = rows.filter(l => S(l.project_id) === p);
    if(q) rows = rows.filter(l => [supervisorNameSafe(l.supervisor_id), projectNameSafe(l.project_id), l.visit_type, l.time_status, l.notes].join(' ').toLowerCase().includes(q));
    return rows;
  };

  const oldRenderDashboard = window.renderDashboard;
  window.renderDashboard = function(){
    if(typeof oldRenderDashboard === 'function') oldRenderDashboard.apply(this, arguments);
    updateTodayCounters();
  };
  const oldRenderContracts = window.renderContracts;
  window.renderContracts = function(){
    if(typeof oldRenderContracts === 'function') oldRenderContracts.apply(this, arguments);
    updateContractTotals();
  };
  const oldRenderAll = window.renderAll;
  window.renderAll = function(){
    if(typeof oldRenderAll === 'function') oldRenderAll.apply(this, arguments);
    ensureTicketFiltersUi(); updateContractTotals(); updateTodayCounters();
  };

  function css(){
    if($('v231Css')) return;
    const st=document.createElement('style'); st.id='v231Css'; st.textContent = `
      #tickets .filters, #supTickets .filters { position: sticky; top: 0; z-index: 20; background: #fff; border: 1px solid var(--line,#dce6e2); border-radius: 16px; padding: 10px; box-shadow: 0 8px 20px rgba(10,64,51,.08); }
      #tickets .filters select, #tickets .filters input, #supTickets .filters select, #supTickets .filters input { min-width: 145px; }
      .contract-total-v231 b, .today-ticket-kpi-v231 b { color: var(--brand,#0A4033); }
      @media(max-width:900px){ #tickets .filters, #supTickets .filters{position:static} }
    `; document.head.appendChild(st);
  }
  function boot(){ css(); ensureTicketFiltersUi(); updateContractTotals(); updateTodayCounters(); try{ window.renderTickets(); }catch(_){} }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(boot, ev==='load'?900:250)));
  setTimeout(boot, 1200);
  window.TASNEEF_BUILD = 'V231_ABU_SAMER_FIXES_2026_05_31';
  console.log('Tasneef V231 Abu Samer fixes loaded');
})();

/* ===== V232 Abu Samer: إصلاح جذري لظهور تسجيلات اليوم ===== */
(function(){
  if(window.__tasneefV232TodayLogsFix) return;
  window.__tasneefV232TodayLogsFix = true;

  const LOG_COLS_V232 = 'id,user_id,supervisor_id,project_id,check_in,check_out,duration_minutes,travel_minutes,visit_type,required_minutes,time_difference_minutes,time_status,notes,created_at,updated_at,log_date';
  const $v232 = id => document.getElementById(id);
  const arr = v => Array.isArray(v) ? v : [];
  const str = v => String(v ?? '').trim();

  function pad2(n){ return String(n).padStart(2,'0'); }
  function localDate(d){
    const x = d instanceof Date ? d : new Date(d || Date.now());
    if(isNaN(x)) return '';
    return x.getFullYear() + '-' + pad2(x.getMonth()+1) + '-' + pad2(x.getDate());
  }
  function todayV232(){ return localDate(new Date()); }
  function addDaysV232(dateStr, days){
    const d = new Date(String(dateStr).slice(0,10) + 'T00:00:00');
    d.setDate(d.getDate() + Number(days||0));
    return localDate(d);
  }
  function monthStartV232(month){ return (month || todayV232().slice(0,7)) + '-01'; }
  function monthEndV232(month){
    const parts = (month || todayV232().slice(0,7)).split('-').map(Number);
    return localDate(new Date(parts[0], parts[1], 0));
  }
  function normalizeDateV232(v){
    if(!v) return '';
    const s = str(v);
    let m = s.match(/^(\d{4})-(\d{2})-(\d{2})/);
    if(m) return `${m[1]}-${m[2]}-${m[3]}`;
    m = s.match(/^(\d{1,2})[\/\-](\d{1,2})[\/\-](\d{4})/);
    if(m) return `${m[3]}-${pad2(m[2])}-${pad2(m[1])}`;
    const d = new Date(s);
    return isNaN(d) ? '' : localDate(d);
  }
  function logDateV232(l){ return normalizeDateV232(l?.log_date) || normalizeDateV232(l?.check_in) || normalizeDateV232(l?.created_at); }
  function mergeLogsV232(rows){
    window.data = window.data || {};
    const map = new Map(arr(window.data.logs).map(l => [String(l.id), l]));
    arr(rows).forEach(l => { if(l && l.id != null) map.set(String(l.id), l); });
    window.data.logs = [...map.values()].sort((a,b)=>(Date.parse(b.check_in||b.created_at||'')||0)-(Date.parse(a.check_in||a.created_at||'')||0));
  }
  async function qLogsV232(label, builder){
    try{
      const r = await builder;
      if(r.error){ console.warn('V232 تعذر تحميل سجلات '+label+':', r.error.message); return []; }
      return r.data || [];
    }catch(e){ console.warn('V232 تعذر تحميل سجلات '+label+':', e?.message || e); return []; }
  }

  window.fetchTimeLogsRangeV232 = async function(start, end, options={}){
    if(!start || !end || !window.sb) return arr(window.data && window.data.logs);
    const next = addDaysV232(end, 1);
    let q1 = sb.from('time_logs').select(LOG_COLS_V232).gte('log_date', start).lte('log_date', end).order('id',{ascending:false}).limit(options.limit || 3000);
    let q2 = sb.from('time_logs').select(LOG_COLS_V232).gte('check_in', start + 'T00:00:00').lt('check_in', next + 'T00:00:00').order('id',{ascending:false}).limit(options.limit || 3000);
    let q3 = sb.from('time_logs').select(LOG_COLS_V232).gte('created_at', start + 'T00:00:00').lt('created_at', next + 'T00:00:00').order('id',{ascending:false}).limit(500);
    if(options.supervisorId){ q1 = q1.eq('supervisor_id', options.supervisorId); q2 = q2.eq('supervisor_id', options.supervisorId); q3 = q3.eq('supervisor_id', options.supervisorId); }
    const [a,b,c] = await Promise.all([qLogsV232('log_date', q1), qLogsV232('check_in', q2), qLogsV232('created_at', q3)]);
    mergeLogsV232([...(a||[]), ...(b||[]), ...(c||[])]);
    window.data.logsLoadError = '';
    return arr(window.data.logs);
  };

  async function safeV232(name, builder, fallback=[]){
    try{
      const r = await builder;
      if(r.error){ console.warn('V232 تعذر تحميل '+name+':', r.error.message); return {data:fallback,error:r.error}; }
      return {data:r.data || fallback, error:null};
    }catch(e){ console.warn('V232 تعذر تحميل '+name+':', e?.message || e); return {data:fallback,error:e}; }
  }

  const oldLoadAllV232 = window.loadAll;
  window.loadAll = async function(){
    const d = todayV232();
    const month = d.slice(0,7);
    const start = monthStartV232(month);
    const end = monthEndV232(month);

    if(!window.sb){ if(typeof oldLoadAllV232 === 'function') return oldLoadAllV232(); return; }
    const usersP = safeV232('المستخدمين', sb.from('app_users').select('*').order('id'));
    const projectsP = safeV232('المشاريع', sb.from('projects').select('*').order('id'));
    const workersP = safeV232('العمال', sb.from('workers').select('*').order('id'));
    const attendanceP = safeV232('الحضور', sb.from('attendance').select('*').gte('attendance_date', addDaysV232(d,-45)).order('attendance_date',{ascending:false}).limit(3000));
    const ticketsP = safeV232('التكتات', sb.from('tickets').select('*').order('id',{ascending:false}).limit(3000));
    const servicesP = safeV232('الخدمات', sb.from('contract_services').select('*').order('id',{ascending:false}).limit(3000));

    const [users, projects, workers, attendance, tickets, contractServices] = await Promise.all([usersP,projectsP,workersP,attendanceP,ticketsP,servicesP]);
    window.data = window.data || {};
    data.users = users.data || [];
    data.supervisors = data.users.filter(u=>u.role==='supervisor' && u.is_active!==false);
    data.technicians = data.users.filter(u=>u.role==='technician' && u.is_active!==false);
    data.projects = projects.data || [];
    data.workers = workers.data || [];
    data.attendance = attendance.data || [];
    data.tickets = tickets.data || [];
    data.contractServices = contractServices.data || [];
    data.contractServicesError = contractServices.error ? contractServices.error.message : '';
    data.logs = [];
    await window.fetchTimeLogsRangeV232(start, end, {limit:5000});
    await window.fetchTimeLogsRangeV232(d, d, {limit:1500});
  };

  window.reloadTodayLogsV91 = async function(){
    const d = todayV232();
    await window.fetchTimeLogsRangeV232(d, d, {limit:1500});
    try{ renderAll(); }catch(e){ console.warn(e); }
  };
  window.reloadTodayLogsV232 = window.reloadTodayLogsV91;

  window.filterLogs = function(){
    let rows = arr(window.data && window.data.logs).slice();
    const d = str($v232('dailyDate')?.value || $v232('supLogDate')?.value || '');
    const s = str($v232('dailySupervisor')?.value || '');
    const p = str($v232('dailyProject')?.value || $v232('logProjectFilter')?.value || '');
    const q = str($v232('dailySearch')?.value || '').toLowerCase();
    if(d) rows = rows.filter(l => logDateV232(l) === d);
    if(s) rows = rows.filter(l => str(l.supervisor_id) === s);
    if(p) rows = rows.filter(l => str(l.project_id) === p);
    if(q){
      rows = rows.filter(l => [
        (typeof supervisorName==='function'?supervisorName(l.supervisor_id):''),
        (typeof projectName==='function'?projectName(l.project_id):''),
        l.visit_type,l.time_status,l.notes
      ].join(' ').toLowerCase().includes(q));
    }
    return rows;
  };

  function updateDashboardV232(){
    const d = todayV232();
    const logs = arr(window.data && window.data.logs).filter(l => logDateV232(l) === d);
    if($v232('kpiTodayLogs')) $v232('kpiTodayLogs').textContent = logs.length.toLocaleString('ar-SA');
    const box = $v232('todaySummary');
    if(box && arr(window.data && window.data.supervisors).length){
      box.innerHTML = arr(data.supervisors).map(s=>{
        const sLogs = logs.filter(l => str(l.supervisor_id) === str(s.id));
        const mins = sLogs.reduce((a,l)=>a + Number(l.duration_minutes || (typeof minutesBetween==='function'?minutesBetween(l.check_in,l.check_out):0) || 0),0);
        const txt = typeof minsToText==='function' ? minsToText(mins) : (mins + ' دقيقة');
        return `<div class="summary-item"><b>${String(s.full_name||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}</b><br>عدد التسجيلات: ${sLogs.length}<br>إجمالي الوقت: ${txt}</div>`;
      }).join('') || '<div class="summary-item">لا توجد تسجيلات اليوم</div>';
    }
  }

  const oldRenderDashboardV232 = window.renderDashboard;
  window.renderDashboard = function(){
    if(typeof oldRenderDashboardV232 === 'function') oldRenderDashboardV232.apply(this, arguments);
    updateDashboardV232();
  };

  const oldRenderTimeLogsV232 = window.renderTimeLogs;
  window.renderTimeLogs = async function(){
    const d = str($v232('dailyDate')?.value || $v232('supLogDate')?.value || '');
    if(d && window.fetchTimeLogsRangeV232) await window.fetchTimeLogsRangeV232(d, d, {limit:1500});
    if(typeof oldRenderTimeLogsV232 === 'function') return oldRenderTimeLogsV232.apply(this, arguments);
  };

  setTimeout(()=>{ try{ updateDashboardV232(); }catch(_){} }, 1500);
  console.log('Tasneef V232 today logs fix loaded');
})();


/* ===== V233: Orders section from Excel - رقم تلقائي + رقم القروب + كامل بيانات الإكسل ===== */
(function(){
  if(window.__tasneefOrdersV233) return;
  window.__tasneefOrdersV233 = true;

  const STORAGE_KEY = 'tasneef_orders_v233';
  const seed = window.TASNEEF_ORDERS_SEED || {headers:[],orders:[],lists:{}};
  const visibleHeaders = (seed.headers || []).filter(h => !String(h||'').startsWith('__excel_col_'));
  const hiddenHeaders = (seed.headers || []).filter(h => String(h||'').startsWith('__excel_col_'));
  const allHeaders = [...visibleHeaders, ...hiddenHeaders, '__excel_row'];
  const editableHeaders = visibleHeaders.filter(h => h !== 'رقم الطلب');

  function $(id){ return document.getElementById(id); }
  function esc2(s){ return String(s ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])); }
  function n2(v){ const x=Number(String(v ?? 0).replace(/,/g,'').trim()); return Number.isFinite(x) ? x : 0; }
  function money2(v){ return n2(v).toLocaleString('ar-SA',{minimumFractionDigits:2,maximumFractionDigits:2})+' ر.س'; }
  function localToday(){ const d=new Date(); d.setMinutes(d.getMinutes()-d.getTimezoneOffset()); return d.toISOString().slice(0,10); }
  function cloneRows(rows){ return JSON.parse(JSON.stringify(rows || [])); }

  function normalizeDateForInput(v){
    if(!v && v!==0) return '';
    if(v instanceof Date && !isNaN(v)) return v.toISOString().slice(0,10);
    let s=String(v).trim();
    if(!s || s==='0') return '';
    let m=s.match(/^(\d{1,2})[\/\-](\d{1,2})[\/\-](\d{4})$/);
    if(m) return `${m[3]}-${String(m[2]).padStart(2,'0')}-${String(m[1]).padStart(2,'0')}`;
    m=s.match(/^(\d{4})[\/\-](\d{1,2})[\/\-](\d{1,2})$/);
    if(m) return `${m[1]}-${String(m[2]).padStart(2,'0')}-${String(m[3]).padStart(2,'0')}`;
    return '';
  }
  function displayDate(v){
    const iso=normalizeDateForInput(v);
    if(!iso) return v ?? '';
    const [y,m,d]=iso.split('-');
    return `${d}/${m}/${y}`;
  }
  function dateForFilter(v){ return normalizeDateForInput(v); }
  function optionValues(header){
    const fromList = (seed.lists && seed.lists[header]) ? seed.lists[header] : [];
    const fromRows = getOrders().map(r => r[header]).filter(v => v!==null && v!==undefined && String(v).trim()!=='');
    return [...new Set([...fromList, ...fromRows].map(v=>String(v).trim()).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'ar'));
  }
  function getOrders(){
    try{
      const saved = JSON.parse(localStorage.getItem(STORAGE_KEY) || 'null');
      if(Array.isArray(saved) && saved.length) return saved;
    }catch(e){}
    return cloneRows(seed.orders || []);
  }
  function setOrders(rows){ localStorage.setItem(STORAGE_KEY, JSON.stringify(rows || [])); }
  function maxOrderNumber(rows){
    let max=0;
    (rows||[]).forEach(r=>{
      const s=String(r['رقم الطلب']||'');
      const nums=s.match(/\d+/g);
      if(nums && nums.length){ const x=Number(nums[nums.length-1]); if(Number.isFinite(x)) max=Math.max(max,x); }
    });
    return max;
  }
  function nextOrderNo(rows){
    const n=maxOrderNumber(rows)+1;
    return 'ORD'+String(n).padStart(4,'0');
  }
  function recalc(row){
    const inc=n2(row['السعر (شامل الضريبة)']);
    const cost=n2(row['التكلفة']);
    if(inc || cost){
      const before=+(inc/1.15).toFixed(2);
      const vat=+(inc-before).toFixed(2);
      row['السعر قبل الضريبة']=before;
      row['الضريبة 15%']=vat;
      row['الربح']=+(before-cost).toFixed(2);
    }
    return row;
  }
  function inputType(header){
    if(header.includes('تاريخ')) return 'date';
    if(header.includes('وقت')) return 'time';
    if(['السعر (شامل الضريبة)','الضريبة 15%','السعر قبل الضريبة','التكلفة','الربح','رقم الطلب بالجروب','رقم العميل'].includes(header)) return 'number';
    return 'text';
  }
  function isTextarea(header){ return ['التفاصيل','ملاحظات','كيفية التنفيذ'].includes(header); }
  function isDropdown(header){ return ['المشروع','نوع العقار','المنفذ','مرسل الطلب','تخص','حالة التنفيذ','تقرير','حالة السداد','فوترة بالسيستم'].includes(header); }
  function fieldId(header){ return 'orderFieldV233_'+btoa(unescape(encodeURIComponent(header))).replace(/=+$/,'').replace(/[^a-zA-Z0-9]/g,'_'); }

  function ensureStyle(){
    if(document.getElementById('ordersStyleV233')) return;
    const st=document.createElement('style'); st.id='ordersStyleV233';
    st.textContent=`
      .orders-layout-v233{grid-template-columns:430px 1fr!important;align-items:start}.orders-kpis-v233 .kpi b{font-size:24px}.orders-form-card-v233{position:sticky;top:12px;max-height:86vh;overflow:auto}.orders-form-grid-v233{display:grid;grid-template-columns:1fr 1fr;gap:8px 10px}.orders-form-grid-v233 .wide{grid-column:1/-1}.orders-filters-v233{position:sticky;top:0;z-index:5;background:#fff;border:1px solid var(--line);border-radius:18px;padding:10px}.orders-table-wrap-v233{max-height:72vh}.orders-table-wrap-v233 th{top:0}.orders-table-wrap-v233 td{max-width:280px;overflow:hidden;text-overflow:ellipsis}.orders-table-wrap-v233 td.details-cell-v233{white-space:normal;min-width:260px}.readonly-note-v233{background:#f6faf8}.orders-row-done-v233{background:#fbfffd}.orders-row-late-v233{background:#fffaf0}.orders-row-cancel-v233{background:#fff5f5}.orders-hidden-col-v233{opacity:.75;color:#60706a}.orders-form-card-v233 input[readonly]{background:#f4f8f6;color:#0a4033;font-weight:900}.orders-cards-v360{display:grid;grid-template-columns:repeat(auto-fill,minmax(310px,1fr));gap:12px;margin-top:12px}.order-card-v360{background:#fff;border:1px solid var(--line);border-radius:18px;padding:14px;box-shadow:0 8px 22px rgba(10,64,51,.06);display:grid;gap:10px;min-width:0}.order-card-v360.done{border-inline-start:5px solid #137a4b}.order-card-v360.due{border-inline-start:5px solid #9a6b00}.order-card-v360.cancel{border-inline-start:5px solid #b83232}.order-card-head-v360{display:flex;justify-content:space-between;gap:10px;align-items:flex-start}.order-card-head-v360 h3{margin:0;color:var(--brand);font-size:18px}.order-card-head-v360 small{display:block;color:var(--muted);margin-top:4px}.order-chip-row-v360{display:flex;gap:6px;flex-wrap:wrap}.order-chip-v360{border-radius:999px;padding:5px 9px;font-size:12px;font-weight:900;background:#eef6f3;color:var(--brand);border:1px solid #d8e8e2}.order-chip-v360.ops{background:#e8f4ee;color:#137a4b}.order-chip-v360.finance{background:#fff5da;color:#8a6700}.order-chip-v360.bad{background:#fde8e8;color:#9d2222}.order-meta-v360{display:grid;grid-template-columns:1fr 1fr;gap:7px}.order-meta-v360 div{background:#f8fbfa;border:1px solid #edf1ef;border-radius:12px;padding:8px;min-width:0}.order-meta-v360 small{display:block;color:var(--muted);font-size:11px;margin-bottom:3px}.order-meta-v360 b{display:block;color:#10231d;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.order-details-v360{line-height:1.7;color:#243b34;background:#fbfdfc;border:1px dashed #d8e8e2;border-radius:12px;padding:9px;min-height:64px}.order-card-actions-v360{display:flex;gap:7px;flex-wrap:wrap}.order-card-actions-v360 button{padding:8px 11px;border-radius:10px;font-size:12px}.orders-pager-v360{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-top:12px;background:#f8fbfa;border:1px solid var(--line);border-radius:14px;padding:10px}.orders-pager-v360 .pager-actions-v360{display:flex;gap:8px}.orders-pager-v360 button{padding:8px 12px;border-radius:10px}.orders-pager-v360 button:disabled{opacity:.45;cursor:not-allowed}.orders-empty-v360{grid-column:1/-1;background:#fff;border:1px dashed #b9d8ca;border-radius:16px;padding:22px;text-align:center;color:var(--muted)}@media(max-width:1100px){.orders-layout-v233{grid-template-columns:1fr!important}.orders-form-card-v233{position:static}.orders-form-grid-v233{grid-template-columns:1fr}.orders-pager-v360{flex-direction:column;align-items:stretch}}
    `;
    document.head.appendChild(st);
  }

  function hydrateOrdersForm(){
    const box=$('orderFormFieldsV233'); if(!box || box.dataset.ready==='1') return;
    box.dataset.ready='1';
    box.innerHTML = editableHeaders.filter(h=>h!=='رقم الطلب بالجروب').map(h=>{
      const id=fieldId(h); const wide=isTextarea(h)||h==='التفاصيل'||h==='ملاحظات'||h==='كيفية التنفيذ';
      let html='';
      if(isDropdown(h)){
        html = `<select id="${id}"><option value="">اختر</option>${optionValues(h).map(v=>`<option value="${esc2(v)}">${esc2(v)}</option>`).join('')}</select>`;
      }else if(isTextarea(h)){
        html = `<textarea id="${id}"></textarea>`;
      }else{
        html = `<input id="${id}" type="${inputType(h)}">`;
      }
      const readonly = ['الضريبة 15%','السعر قبل الضريبة','الربح'].includes(h) ? ' <span class="orders-hidden-col-v233">(تلقائي)</span>' : '';
      return `<div class="${wide?'wide':''}"><label>${esc2(h)}${readonly}</label>${html}</div>`;
    }).join('');
    ['السعر (شامل الضريبة)','التكلفة'].forEach(h=>{ const el=$(fieldId(h)); if(el) el.addEventListener('input', previewCalc); });
  }

  function hydrateOrdersFilters(){
    const map=[['orderProjectFilterV233','المشروع','كل المشاريع'],['orderExecutorFilterV233','المنفذ','كل المنفذين'],['orderSenderFilterV233','مرسل الطلب','كل مرسلي الطلب'],['orderStatusFilterV233','حالة التنفيذ','كل حالات التنفيذ'],['orderPaymentFilterV233','حالة السداد','كل حالات السداد'],['orderBillingFilterV233','فوترة بالسيستم','كل حالات الفوترة']];
    map.forEach(([id,h,label])=>{ const el=$(id); if(!el || el.dataset.ready==='1') return; el.dataset.ready='1'; el.innerHTML=`<option value="">${label}</option>`+optionValues(h).map(v=>`<option value="${esc2(v)}">${esc2(v)}</option>`).join(''); });
  }

  function previewCalc(){
    const inc=$(fieldId('السعر (شامل الضريبة)')); const cost=$(fieldId('التكلفة'));
    const vat=$(fieldId('الضريبة 15%')); const before=$(fieldId('السعر قبل الضريبة')); const profit=$(fieldId('الربح'));
    if(!inc || !cost) return;
    const row=recalc({'السعر (شامل الضريبة)':inc.value,'التكلفة':cost.value});
    if(vat) vat.value = row['الضريبة 15%'] || 0;
    if(before) before.value = row['السعر قبل الضريبة'] || 0;
    if(profit) profit.value = row['الربح'] || 0;
  }

  window.clearOrderFormV233 = function(){
    hydrateOrdersForm();
    if($('orderEditIndexV233')) $('orderEditIndexV233').value='';
    if($('orderNoV233')) $('orderNoV233').value='';
    if($('orderGroupNoV233')) $('orderGroupNoV233').value='';
    editableHeaders.forEach(h=>{ const el=$(fieldId(h)); if(el) el.value=''; });
    if($('orderFormTitleV233')) $('orderFormTitleV233').textContent='إضافة أوردر';
    const dateEl=$(fieldId('تاريخ الطلب')); if(dateEl && !dateEl.value) dateEl.value=localToday();
  };

  window.editOrderV233 = function(index){
    hydrateOrdersForm();
    const rows=getOrders(); const r=rows[index]; if(!r) return;
    $('orderEditIndexV233').value=String(index);
    $('orderNoV233').value=r['رقم الطلب']||'';
    $('orderGroupNoV233').value=r['رقم الطلب بالجروب']??'';
    editableHeaders.forEach(h=>{
      if(h==='رقم الطلب بالجروب') return;
      const el=$(fieldId(h)); if(!el) return;
      let v=r[h];
      if(inputType(h)==='date') v=normalizeDateForInput(v);
      if(inputType(h)==='time' && typeof v==='number') v='';
      el.value = v ?? '';
    });
    if($('orderFormTitleV233')) $('orderFormTitleV233').textContent='تعديل أوردر '+(r['رقم الطلب']||'');
    $('orders')?.scrollIntoView({behavior:'smooth',block:'start'});
  };

  window.saveOrderV233 = function(){
    hydrateOrdersForm();
    const rows=getOrders();
    const idxStr=$('orderEditIndexV233')?.value;
    const editing = idxStr!=='' && idxStr!==undefined;
    let row = editing ? Object.assign({}, rows[Number(idxStr)]||{}) : {};
    row['رقم الطلب'] = editing ? (row['رقم الطلب'] || $('orderNoV233')?.value || nextOrderNo(rows)) : nextOrderNo(rows);
    row['رقم الطلب بالجروب'] = $('orderGroupNoV233')?.value || '';
    editableHeaders.forEach(h=>{
      if(h==='رقم الطلب بالجروب') return;
      const el=$(fieldId(h)); if(!el) return;
      row[h]=el.value;
    });
    if(!row['تاريخ الطلب']) row['تاريخ الطلب']=localToday();
    row = recalc(row);
    if(editing) rows[Number(idxStr)] = row; else rows.unshift(row);
    setOrders(rows);
    try{ msg(editing?'تم تحديث الأوردر':'تم حفظ الأوردر'); }catch(e){}
    clearOrderFormV233();
    renderOrdersV233();
  };

  window.deleteCurrentOrderV233 = function(){
    const idxStr=$('orderEditIndexV233')?.value;
    if(idxStr==='' || idxStr===undefined) return alert('اختر أوردر أولًا من الجدول');
    const rows=getOrders(); const r=rows[Number(idxStr)];
    if(!r) return;
    if(!confirm('حذف الأوردر '+(r['رقم الطلب']||'')+'؟')) return;
    rows.splice(Number(idxStr),1); setOrders(rows); clearOrderFormV233(); renderOrdersV233();
  };

  window.deleteOrderV233 = function(index){
    const rows=getOrders(); const r=rows[index]; if(!r) return;
    if(!confirm('حذف الأوردر '+(r['رقم الطلب']||'')+'؟')) return;
    rows.splice(index,1); setOrders(rows); renderOrdersV233();
  };

  function filteredOrders(){
    const q=($('orderSearchV233')?.value||'').trim().toLowerCase();
    const project=$('orderProjectFilterV233')?.value||'';
    const exec=$('orderExecutorFilterV233')?.value||'';
    const sender=$('orderSenderFilterV233')?.value||'';
    const status=$('orderStatusFilterV233')?.value||'';
    const pay=$('orderPaymentFilterV233')?.value||'';
    const bill=$('orderBillingFilterV233')?.value||'';
    const from=$('orderFromDateV233')?.value||'';
    const to=$('orderToDateV233')?.value||'';
    return getOrders().map((r,i)=>({r,i})).filter(({r})=>{
      if(project && String(r['المشروع']||'').trim()!==project) return false;
      if(exec && String(r['المنفذ']||'').trim()!==exec) return false;
      if(sender && String(r['مرسل الطلب']||'').trim()!==sender) return false;
      if(status && String(r['حالة التنفيذ']||'').trim()!==status) return false;
      if(pay && String(r['حالة السداد']||'').trim()!==pay) return false;
      if(bill && String(r['فوترة بالسيستم']||'').trim()!==bill) return false;
      const d=dateForFilter(r['تاريخ الطلب']);
      if(from && d && d<from) return false;
      if(to && d && d>to) return false;
      if(q){
        const hay=[r['رقم الطلب'],r['رقم الطلب بالجروب'],r['المشروع'],r['اسم العميل'],r['رقم العميل'],r['التفاصيل'],r['ملاحظات'],r['رقم الفاتورة'],r['المنفذ']].join(' ').toLowerCase();
        if(!hay.includes(q)) return false;
      }
      return true;
    });
  }
  function rowClass(r){
    const st=String(r['حالة التنفيذ']||''); const pay=String(r['حالة السداد']||'');
    if(st.includes('لم ينفذ') || st.includes('ملغي')) return 'orders-row-cancel-v233';
    if(pay.includes('آجل') || pay.includes('جزئي')) return 'orders-row-late-v233';
    if(st.includes('تم التنفيذ')) return 'orders-row-done-v233';
    return '';
  }

  window.renderOrdersV233 = function(){
    ensureStyle(); hydrateOrdersForm(); hydrateOrdersFilters();
    const head=$('ordersHeadV233'), body=$('ordersBodyV233'); if(!head || !body) return;
    const list=filteredOrders();
    const tableHeaders=visibleHeaders;
    head.innerHTML = `<tr>${tableHeaders.map(h=>`<th>${esc2(h)}</th>`).join('')}<th>الإجراءات</th></tr>`;
    body.innerHTML = list.map(({r,i})=>{
      return `<tr class="${rowClass(r)}">${tableHeaders.map(h=>{
        let v=r[h];
        if(h.includes('تاريخ')) v=displayDate(v);
        const cls = ['التفاصيل','ملاحظات','كيفية التنفيذ'].includes(h) ? ' class="details-cell-v233"' : '';
        return `<td${cls}>${esc2(v ?? '')}</td>`;
      }).join('')}<td class="row-actions"><button onclick="editOrderV233(${i})">تعديل</button><button class="light" onclick="sendOrderWhatsappV233(${i})">واتساب</button><button class="danger" onclick="deleteOrderV233(${i})">حذف</button></td></tr>`;
    }).join('') || `<tr><td colspan="${tableHeaders.length+1}">لا توجد أوردرات حسب الفلتر الحالي</td></tr>`;
    renderOrdersSummaryV233(list.map(x=>x.r));
  };

  const ORDER_PAGE_SIZE_V360 = 12;
  window.__ordersPageV360 = window.__ordersPageV360 || 1;
  function orderStageClassV360(r){
    const st=String(r['ط­ط§ظ„ط© ط§ظ„طھظ†ظپظٹط°']||'');
    const pay=String(r['ط­ط§ظ„ط© ط§ظ„ط³ط¯ط§ط¯']||'');
    if(st.includes('ظ„ظ… ظٹظ†ظپط°') || st.includes('ظ…ظ„ط؛ظٹ')) return 'cancel';
    if(pay.includes('ط¢ط¬ظ„') || pay.includes('ط¬ط²ط¦ظٹ')) return 'due';
    if(st.includes('طھظ… ط§ظ„طھظ†ظپظٹط°')) return 'done';
    return '';
  }
  function shortOrderTextV360(v, n=150){ const s=String(v ?? '').trim(); return s.length>n ? s.slice(0,n)+'...' : s; }
  function orderCardHtmlV360(r,i){
    const no=r['ط±ظ‚ظ… ط§ظ„ط·ظ„ط¨']||'-', group=r['ط±ظ‚ظ… ط§ظ„ط·ظ„ط¨ ط¨ط§ظ„ط¬ط±ظˆط¨']||'-';
    const project=r['ط§ظ„ظ…ط´ط±ظˆط¹']||'-', client=r['ط§ط³ظ… ط§ظ„ط¹ظ…ظٹظ„']||'-', phone=r['ط±ظ‚ظ… ط§ظ„ط¹ظ…ظٹظ„']||'-';
    const executor=r['ط§ظ„ظ…ظ†ظپط°']||'-', sender=r['ظ…ط±ط³ظ„ ط§ظ„ط·ظ„ط¨']||'-', date=displayDate(r['طھط§ط±ظٹط® ط§ظ„ط·ظ„ط¨'])||'-';
    const status=r['ط­ط§ظ„ط© ط§ظ„طھظ†ظپظٹط°']||'-', pay=r['ط­ط§ظ„ط© ط§ظ„ط³ط¯ط§ط¯']||'-', bill=r['ظپظˆطھط±ط© ط¨ط§ظ„ط³ظٹط³طھظ…']||'-';
    const price=r['ط§ظ„ط³ط¹ط± (ط´ط§ظ…ظ„ ط§ظ„ط¶ط±ظٹط¨ط©)'], profit=r['ط§ظ„ط±ط¨ط­'], details=r['ط§ظ„طھظپط§طµظٹظ„']||'';
    const cls=orderStageClassV360(r), stBad=cls==='cancel'?' bad':' ops', payBad=cls==='due'?' finance':'';
    return `<article class="order-card-v360 ${cls}">
      <div class="order-card-head-v360"><div><h3>${esc2(no)}</h3><small>ط±ظ‚ظ… ط§ظ„ظ‚ط±ظˆط¨: ${esc2(group)} | ${esc2(date)}</small></div><span class="order-chip-v360">${esc2(project)}</span></div>
      <div class="order-chip-row-v360"><span class="order-chip-v360${stBad}">ط§ظ„طھط´ط؛ظٹظ„: ${esc2(status)}</span><span class="order-chip-v360${payBad}">ط§ظ„ظ…ط§ظ„ظٹط©: ${esc2(pay)}</span><span class="order-chip-v360">ط§ظ„ظپظˆطھط±ط©: ${esc2(bill)}</span></div>
      <div class="order-meta-v360"><div><small>ط§ظ„ط¹ظ…ظٹظ„</small><b>${esc2(client)}</b></div><div><small>ط§ظ„ط¬ظˆط§ظ„</small><b>${esc2(phone)}</b></div><div><small>ط§ظ„ظ…ظ†ظپط°</small><b>${esc2(executor)}</b></div><div><small>ظ…ط±ط³ظ„ ط§ظ„ط·ظ„ط¨</small><b>${esc2(sender)}</b></div><div><small>ط§ظ„ط³ط¹ط± ط´ط§ظ…ظ„ ط§ظ„ط¶ط±ظٹط¨ط©</small><b>${esc2(price!==''&&price!=null?money2(price):'-')}</b></div><div><small>ط§ظ„ط±ط¨ط­</small><b>${esc2(profit!==''&&profit!=null?money2(profit):'-')}</b></div></div>
      <div class="order-details-v360">${esc2(shortOrderTextV360(details)) || 'ظ„ط§ طھظˆط¬ط¯ طھظپط§طµظٹظ„'}</div>
      <div class="order-card-actions-v360"><button onclick="editOrderV233(${i})">طھط¹ط¯ظٹظ„</button><button class="light" onclick="sendOrderWhatsappV233(${i})">ظˆط§طھط³ط§ط¨</button><button class="danger" onclick="deleteOrderV233(${i})">ط­ط°ظپ</button></div>
    </article>`;
  }
  window.changeOrdersPageV360 = function(delta){
    window.__ordersPageV360 = Math.max(1, (window.__ordersPageV360||1) + Number(delta||0));
    renderOrdersV233();
  };
  window.renderOrdersV233 = function(){
    ensureStyle(); hydrateOrdersForm(); hydrateOrdersFilters();
    const cards=$('ordersCardsV360'), pager=$('ordersPagerV360');
    if(!cards) return;
    const list=filteredOrders();
    const pages=Math.max(1, Math.ceil(list.length/ORDER_PAGE_SIZE_V360));
    window.__ordersPageV360 = Math.min(Math.max(1, window.__ordersPageV360||1), pages);
    const start=(window.__ordersPageV360-1)*ORDER_PAGE_SIZE_V360;
    const pageRows=list.slice(start,start+ORDER_PAGE_SIZE_V360);
    cards.innerHTML = pageRows.map(({r,i})=>orderCardHtmlV360(r,i)).join('') || '<div class="orders-empty-v360">ظ„ط§ طھظˆط¬ط¯ ط£ظˆط±ط¯ط±ط§طھ ط­ط³ط¨ ط§ظ„ظپظ„طھط± ط§ظ„ط­ط§ظ„ظٹ</div>';
    if(pager){
      pager.innerHTML = `<div>ط¹ط±ط¶ ${list.length ? start+1 : 0}-${Math.min(start+ORDER_PAGE_SIZE_V360,list.length)} ظ…ظ† ${list.length} ط£ظˆط±ط¯ط± | طµظپط­ط© ${window.__ordersPageV360} ظ…ظ† ${pages}</div><div class="pager-actions-v360"><button class="light" onclick="changeOrdersPageV360(-1)" ${window.__ordersPageV360<=1?'disabled':''}>ط§ظ„ط³ط§ط¨ظ‚</button><button class="light" onclick="changeOrdersPageV360(1)" ${window.__ordersPageV360>=pages?'disabled':''}>ط§ظ„طھط§ظ„ظٹ</button></div>`;
    }
    renderOrdersSummaryV233(list.map(x=>x.r));
  };

  window.renderOrdersFirstPageV360 = function(){
    window.__ordersPageV360 = 1;
    renderOrdersV233();
  };

  function orderFieldV361(r, idx){
    const key = visibleHeaders[idx];
    return key ? r[key] : '';
  }
  function orderCardHtmlV361(r,i){
    const no=orderFieldV361(r,0)||'-', group=orderFieldV361(r,1)||'-', date=displayDate(orderFieldV361(r,2))||'-';
    const sender=orderFieldV361(r,4)||'-', project=orderFieldV361(r,5)||'-', client=orderFieldV361(r,8)||'-', phone=orderFieldV361(r,9)||'-';
    const executor=orderFieldV361(r,10)||'-', details=orderFieldV361(r,11)||'', status=orderFieldV361(r,16)||'-';
    const price=orderFieldV361(r,18), profit=orderFieldV361(r,22), pay=orderFieldV361(r,23)||'-', bill=orderFieldV361(r,25)||'-';
    const st=String(status), py=String(pay);
    const cls=(st.includes('لم ينفذ')||st.includes('ملغي'))?'cancel':((py.includes('آجل')||py.includes('جزئي'))?'due':(st.includes('تم التنفيذ')?'done':''));
    return `<article class="order-card-v360 ${cls}">
      <div class="order-card-head-v360"><div><h3>${esc2(no)}</h3><small>رقم القروب: ${esc2(group)} | ${esc2(date)}</small></div><span class="order-chip-v360">${esc2(project)}</span></div>
      <div class="order-chip-row-v360"><span class="order-chip-v360 ${cls==='cancel'?'bad':'ops'}">التشغيل: ${esc2(status)}</span><span class="order-chip-v360 ${cls==='due'?'finance':''}">المالية: ${esc2(pay)}</span><span class="order-chip-v360">الفوترة: ${esc2(bill)}</span></div>
      <div class="order-meta-v360"><div><small>العميل</small><b>${esc2(client)}</b></div><div><small>الجوال</small><b>${esc2(phone)}</b></div><div><small>المنفذ</small><b>${esc2(executor)}</b></div><div><small>مرسل الطلب</small><b>${esc2(sender)}</b></div><div><small>السعر شامل الضريبة</small><b>${esc2(price!==''&&price!=null?money2(price):'-')}</b></div><div><small>الربح</small><b>${esc2(profit!==''&&profit!=null?money2(profit):'-')}</b></div></div>
      <div class="order-details-v360">${esc2(shortOrderTextV360(details)) || 'لا توجد تفاصيل'}</div>
      <div class="order-card-actions-v360"><button onclick="editOrderV233(${i})">تعديل</button><button class="light" onclick="sendOrderWhatsappV233(${i})">واتساب</button><button class="danger" onclick="deleteOrderV233(${i})">حذف</button></div>
    </article>`;
  }
  window.renderOrdersV233 = function(){
    ensureStyle(); hydrateOrdersForm(); hydrateOrdersFilters();
    const cards=$('ordersCardsV360'), pager=$('ordersPagerV360');
    if(!cards) return;
    const list=filteredOrders();
    const pages=Math.max(1, Math.ceil(list.length/ORDER_PAGE_SIZE_V360));
    window.__ordersPageV360 = Math.min(Math.max(1, window.__ordersPageV360||1), pages);
    const start=(window.__ordersPageV360-1)*ORDER_PAGE_SIZE_V360;
    const pageRows=list.slice(start,start+ORDER_PAGE_SIZE_V360);
    cards.innerHTML = pageRows.map(({r,i})=>orderCardHtmlV361(r,i)).join('') || '<div class="orders-empty-v360">لا توجد أوردرات حسب الفلتر الحالي</div>';
    if(pager){
      pager.innerHTML = `<div>عرض ${list.length ? start+1 : 0}-${Math.min(start+ORDER_PAGE_SIZE_V360,list.length)} من ${list.length} أوردر | صفحة ${window.__ordersPageV360} من ${pages}</div><div class="pager-actions-v360"><button class="light" onclick="changeOrdersPageV360(-1)" ${window.__ordersPageV360<=1?'disabled':''}>السابق</button><button class="light" onclick="changeOrdersPageV360(1)" ${window.__ordersPageV360>=pages?'disabled':''}>التالي</button></div>`;
    }
    renderOrdersSummaryV233(list.map(x=>x.r));
  };

  window.renderOrdersSummaryV233 = function(rows){
    rows = rows || getOrders();
    const total=rows.length;
    const done=rows.filter(r=>String(r['حالة التنفيذ']||'').includes('تم التنفيذ')).length;
    const notDone=rows.filter(r=>String(r['حالة التنفيذ']||'').includes('لم ينفذ')).length;
    const paid=rows.filter(r=>String(r['حالة السداد']||'').includes('تم السداد')).length;
    const due=rows.filter(r=>String(r['حالة السداد']||'').includes('آجل') || String(r['حالة السداد']||'').includes('جزئي') || (!r['حالة السداد'] && n2(r['السعر (شامل الضريبة)'])>0)).length;
    const revenue=rows.reduce((a,r)=>a+n2(r['السعر (شامل الضريبة)']),0);
    const vat=rows.reduce((a,r)=>a+n2(r['الضريبة 15%']),0);
    const cost=rows.reduce((a,r)=>a+n2(r['التكلفة']),0);
    const profit=rows.reduce((a,r)=>a+n2(r['الربح']),0);
    if($('ordersTotalKpiV233')) $('ordersTotalKpiV233').textContent=total;
    if($('ordersDoneKpiV233')) $('ordersDoneKpiV233').textContent=done;
    if($('ordersDueKpiV233')) $('ordersDueKpiV233').textContent=due;
    if($('ordersProfitKpiV233')) $('ordersProfitKpiV233').textContent=money2(profit).replace(' ر.س','');
    const box=$('ordersSummaryV233');
    if(box) box.innerHTML = [
      ['إجمالي الإيرادات شامل الضريبة', money2(revenue)],
      ['إجمالي الضريبة 15%', money2(vat)],
      ['إجمالي التكلفة', money2(cost)],
      ['صافي الربح', money2(profit)],
      ['تم السداد', paid+' أوردر'],
      ['لم ينفذ / ملغي', notDone+' أوردر']
    ].map(x=>`<div class="summary-item"><b>${esc2(x[0])}</b><br>${esc2(x[1])}</div>`).join('');
  };

  window.renderOrdersSummaryV233 = function(rows){
    rows = rows || getOrders();
    const total=rows.length;
    const done=rows.filter(r=>String(orderFieldV361(r,16)||'').includes('تم التنفيذ')).length;
    const notDone=rows.filter(r=>String(orderFieldV361(r,16)||'').includes('لم ينفذ') || String(orderFieldV361(r,16)||'').includes('ملغي')).length;
    const paid=rows.filter(r=>String(orderFieldV361(r,23)||'').includes('تم السداد')).length;
    const due=rows.filter(r=>String(orderFieldV361(r,23)||'').includes('آجل') || String(orderFieldV361(r,23)||'').includes('جزئي') || (!orderFieldV361(r,23) && n2(orderFieldV361(r,18))>0)).length;
    const revenue=rows.reduce((a,r)=>a+n2(orderFieldV361(r,18)),0);
    const vat=rows.reduce((a,r)=>a+n2(orderFieldV361(r,19)),0);
    const cost=rows.reduce((a,r)=>a+n2(orderFieldV361(r,21)),0);
    const profit=rows.reduce((a,r)=>a+n2(orderFieldV361(r,22)),0);
    if($('ordersTotalKpiV233')) $('ordersTotalKpiV233').textContent=total;
    if($('ordersDoneKpiV233')) $('ordersDoneKpiV233').textContent=done;
    if($('ordersDueKpiV233')) $('ordersDueKpiV233').textContent=due;
    if($('ordersProfitKpiV233')) $('ordersProfitKpiV233').textContent=money2(profit).replace(' ر.س','');
    const box=$('ordersSummaryV233');
    if(box) box.innerHTML = [
      ['إجمالي الإيرادات شامل الضريبة', money2(revenue)],
      ['إجمالي الضريبة 15%', money2(vat)],
      ['إجمالي التكلفة', money2(cost)],
      ['صافي الربح', money2(profit)],
      ['تم السداد', paid+' أوردر'],
      ['لم ينفذ / ملغي', notDone+' أوردر']
    ].map(x=>`<div class="summary-item"><b>${esc2(x[0])}</b><br>${esc2(x[1])}</div>`).join('');
  };

  window.resetOrdersFiltersV233 = function(){
    ['orderSearchV233','orderProjectFilterV233','orderExecutorFilterV233','orderSenderFilterV233','orderStatusFilterV233','orderPaymentFilterV233','orderBillingFilterV233','orderFromDateV233','orderToDateV233'].forEach(id=>{ if($(id)) $(id).value=''; });
    renderOrdersV233();
  };

  window.sendOrderWhatsappV233 = function(index){
    const r=getOrders()[index]; if(!r) return;
    const msg = `*متابعة أوردر*%0Aرقم النظام: ${encodeURIComponent(r['رقم الطلب']||'')}%0Aرقم القروب: ${encodeURIComponent(r['رقم الطلب بالجروب']||'')}%0Aالمشروع: ${encodeURIComponent(r['المشروع']||'')}%0Aالعميل: ${encodeURIComponent(r['اسم العميل']||'-')}%0Aالجوال: ${encodeURIComponent(r['رقم العميل']||'-')}%0Aالتفاصيل: ${encodeURIComponent(r['التفاصيل']||'')}%0Aالسعر شامل الضريبة: ${encodeURIComponent(r['السعر (شامل الضريبة)']||0)}%0Aحالة التنفيذ: ${encodeURIComponent(r['حالة التنفيذ']||'')}%0Aحالة السداد: ${encodeURIComponent(r['حالة السداد']||'')}%0Aالمنفذ: ${encodeURIComponent(r['المنفذ']||'')}`;
    window.open('https://wa.me/?text='+msg,'_blank');
  };

  window.exportOrdersCSVV233 = function(){
    const rows=filteredOrders().map(x=>x.r);
    const headers=visibleHeaders;
    const csv=[headers.join(',')].concat(rows.map(r=>headers.map(h=>'"'+String(r[h]??'').replace(/"/g,'""')+'"').join(','))).join('\n');
    const blob=new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'});
    const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='tasneef_orders.csv'; a.click(); URL.revokeObjectURL(a.href);
  };

  window.resetOrdersToExcelSeedV233 = function(){
    if(!confirm('سيتم استبدال بيانات الأوردرات الحالية بنسخة الإكسل الأصلية. هل أنت متأكد؟')) return;
    localStorage.removeItem(STORAGE_KEY); renderOrdersV233();
  };

  const oldShowPageV233 = window.showPage;
  window.showPage = function(id, btn){
    if(typeof oldShowPageV233==='function') oldShowPageV233(id, btn);
    if(id==='orders') setTimeout(renderOrdersV233, 50);
  };
  const oldRenderAllV233 = window.renderAll;
  window.renderAll = function(){
    if(typeof oldRenderAllV233==='function') oldRenderAllV233();
    try{ renderOrdersSummaryV233(getOrders()); }catch(e){}
  };
  document.addEventListener('DOMContentLoaded', function(){ if($('orders')){ ensureStyle(); hydrateOrdersForm(); hydrateOrdersFilters(); renderOrdersSummaryV233(getOrders()); } });
})();

/* ===== TASNEEF V280 REAL SPEED + SAVE FIX ===== */
(function(){
  const FIX_VERSION = 'v280-fast-save-monthly';
  const PAGE_SIZE = 1000;
  const $x = (id)=>document.getElementById(id);
  function ymd(d){ return d.toISOString().slice(0,10); }
  function monthRange(month){
    month = month || ($x('monthlyMonth')?.value || $x('attendanceMatrixMonth')?.value || new Date().toISOString().slice(0,7));
    const start = month + '-01';
    const endDate = new Date(start + 'T00:00:00');
    endDate.setMonth(endDate.getMonth()+1);
    return {month,start,end:ymd(endDate)};
  }
  async function selectAllPaged(query){
    let out=[], from=0;
    while(true){
      const res = await query.range(from, from + PAGE_SIZE - 1);
      if(res.error) return res;
      const rows = res.data || [];
      out = out.concat(rows);
      if(rows.length < PAGE_SIZE) return {data:out,error:null};
      from += PAGE_SIZE;
      if(from > 20000) return {data:out,error:null};
    }
  }
  async function safeSelect(name, query){
    const r = await query;
    if(r.error) console.warn('[Tasneef '+FIX_VERSION+'] '+name, r.error.message);
    return r.data || [];
  }

  async function loadAllV280(){
    const r = monthRange();
    const [users, projects, workers, attendance, logs, tickets] = await Promise.all([
      safeSelect('app_users', sb.from('app_users').select('id,full_name,username,password,role,is_active,permissions').order('id')),
      safeSelect('projects', sb.from('projects').select('*').order('id')),
      safeSelect('workers', sb.from('workers').select('*').order('id')),
      selectAllPaged(sb.from('attendance').select('*').gte('attendance_date', r.start).lt('attendance_date', r.end).order('attendance_date',{ascending:false})),
      selectAllPaged(sb.from('time_logs').select('*').gte('log_date', r.start).lt('log_date', r.end).order('check_in',{ascending:false})),
      safeSelect('tickets', sb.from('tickets').select('*').order('created_at',{ascending:false}).limit(300))
    ]);
    let contractServices = {data:[],error:null};
    try{ contractServices = await sb.from('contract_services').select('*').order('id', { ascending:false }).limit(500); }catch(e){ console.warn(e); }
    data.users = users || [];
    data.supervisors = data.users.filter(u=>u.role==='supervisor' && u.is_active!==false);
    data.technicians = data.users.filter(u=>u.role==='technician' && u.is_active!==false);
    data.projects = projects || [];
    data.workers = workers || [];
    data.attendance = attendance.data || [];
    data.logs = logs.data || [];
    data.tickets = tickets || [];
    data.contractServices = contractServices.data || [];
    data.__loadedMonth = r.month;
    data.__fixVersion = FIX_VERSION;
  }

  try{ loadAll = loadAllV280; window.loadAll = loadAllV280; }catch(e){ window.loadAll = loadAllV280; }

  async function refreshAllV280(){ await loadAllV280(); hydrateForms(); renderAll(); showFixBadge(); }
  try{ refreshAll = refreshAllV280; window.refreshAll = refreshAllV280; }catch(e){ window.refreshAll = refreshAllV280; }

  function firstProjectForSupervisor(sid){
    return (data.projects||[]).find(p=>String(p.supervisor_id||'')===String(sid||''));
  }
  window.onWorkerSupervisorChange = function(){
    const sid=$x('workerSupervisor')?.value;
    const el=$x('workerProject');
    if(!el) return;
    let rows=(data.projects||[]).filter(p=>!sid || String(p.supervisor_id||'')===String(sid));
    fillSelect('workerProject', rows, 'name', 'اختر المشروع');
    if(sid && rows.length){ el.value = rows[0].id; }
  };
  try{ onWorkerSupervisorChange = window.onWorkerSupervisorChange; }catch(e){}

  window.saveWorker = async function(){
    const id=$x('workerId')?.value;
    const supId=Number($x('workerSupervisor')?.value)||null;
    let projectId=Number($x('workerProject')?.value)||null;
    if(supId && !projectId){ const p=firstProjectForSupervisor(supId); if(p) projectId=Number(p.id); }
    const row={
      name:($x('workerName')?.value||'').trim(),
      phone:($x('workerPhone')?.value||'').trim(),
      salary:Number($x('workerSalary')?.value||1500),
      supervisor_id:supId,
      app_supervisor_id:supId,
      project_id:projectId,
      worker_type:$x('workerType')?.value||'primary',
      status:$x('workerStatus')?.value||'active',
      notes:$x('workerNotes')?.value||''
    };
    if(!row.name) return msg('اسم العامل مطلوب','err');
    if(!row.supervisor_id) return msg('اختر المشرف المسؤول عن العامل','err');
    if(!row.project_id) return msg('لا يوجد مشروع مربوط بهذا المشرف. اربط مشروع بالمشرف أولاً','err');
    const res = id ? await sb.from('workers').update(row).eq('id',id).select('*').maybeSingle() : await sb.from('workers').insert(row).select('*').single();
    if(res.error) return msg(res.error.message,'err');
    const saved = res.data || Object.assign({id:Number(id)}, row);
    const i=(data.workers||[]).findIndex(w=>String(w.id)===String(saved.id));
    if(i>=0) data.workers[i]=Object.assign({}, data.workers[i], saved); else data.workers.unshift(saved);
    msg(id?'تم تحويل العامل وحفظ التعديل':'تم حفظ العامل');
    clearWorkerForm(); hydrateForms(); renderWorkers(); renderAttendanceWorkersQuick();
  };
  try{ saveWorker = window.saveWorker; }catch(e){}

  window.saveAttendance = async function(){
    const id=$x('attendanceId')?.value;
    const workerId=Number($x('attendanceWorker')?.value);
    const ds=$x('attendanceDate')?.value || new Date().toISOString().slice(0,10);
    const row={attendance_date:ds, worker_id:workerId, supervisor_id:Number($x('attendanceSupervisor')?.value)||null, project_id:Number($x('attendanceProject')?.value)||null, status:$x('attendanceStatus')?.value||'present', notes:$x('attendanceNotes')?.value||'', created_by:session()?.id||null};
    if(!row.worker_id) return msg('اختر العامل','err');
    let res;
    if(id){
      res = await sb.from('attendance').update(row).eq('id',id).select('*').maybeSingle();
    }else{
      const existing = await sb.from('attendance').select('id').eq('attendance_date', ds).eq('worker_id', workerId).maybeSingle();
      if(existing.error && existing.error.code!=='PGRST116') return msg(existing.error.message,'err');
      if(existing.data?.id) res = await sb.from('attendance').update(row).eq('id', existing.data.id).select('*').maybeSingle();
      else res = await sb.from('attendance').insert(row).select('*').single();
    }
    if(res.error) return msg(res.error.message,'err');
    const saved=res.data || row;
    const key = x=>String(x.worker_id) + '|' + String(x.attendance_date);
    data.attendance = (data.attendance||[]).filter(a=>key(a)!==key(saved));
    data.attendance.unshift(saved);
    msg('تم حفظ الحضور بدون تكرار'); clearAttendanceForm(); renderAttendance(); window.renderAttendanceMonthly && window.renderAttendanceMonthly();
  };
  try{ saveAttendance = window.saveAttendance; }catch(e){}

  const oldRenderAttendance = window.renderAttendance || (typeof renderAttendance==='function'?renderAttendance:null);
  window.renderAttendance = function(){
    if(Array.isArray(data.attendance)){
      const map=new Map();
      data.attendance.forEach(a=>{ const k=String(a.worker_id)+'|'+String(a.attendance_date); if(!map.has(k)) map.set(k,a); });
      data.attendance=[...map.values()];
    }
    if(typeof oldRenderAttendance==='function') return oldRenderAttendance();
  };
  try{ renderAttendance = window.renderAttendance; }catch(e){}

  async function reloadForSelectedMonth(){
    const btn=document.activeElement; if(btn) btn.disabled=true;
    try{ await refreshAllV280(); }
    finally{ if(btn) btn.disabled=false; }
  }
  document.addEventListener('change', function(e){
    if(e.target && (e.target.id==='monthlyMonth' || e.target.id==='attendanceMatrixMonth')) reloadForSelectedMonth();
  });

  function showFixBadge(){
    if(document.getElementById('tasneefFixBadgeV280')) return;
    const b=document.createElement('div'); b.id='tasneefFixBadgeV280';
    b.textContent='Tasneef '+FIX_VERSION;
    b.style.cssText='position:fixed;left:10px;bottom:10px;z-index:99999;background:#0a4033;color:#fff;padding:6px 10px;border-radius:12px;font:12px Arial;box-shadow:0 4px 12px #0002;opacity:.88';
    document.body.appendChild(b);
  }
  document.addEventListener('DOMContentLoaded', showFixBadge);
})();

/* ===== TASNEEF V281 MONTHLY LOGS REAL FIX: read by log_date OR check_in ===== */
(function(){
  const FIX_VERSION = 'v281-monthly-logdate-checkin';
  const PAGE_SIZE = 1000;
  const $id = id => document.getElementById(id);
  function todayMonth(){ return new Date().toISOString().slice(0,7); }
  function ymd(d){ return d.toISOString().slice(0,10); }
  function monthRange(month){
    month = month || ($id('monthlyMonth')?.value || $id('attendanceMatrixMonth')?.value || todayMonth());
    const start = month + '-01';
    const e = new Date(start + 'T00:00:00');
    e.setMonth(e.getMonth()+1);
    return {month,start,end:ymd(e)};
  }
  async function pageAll(q){
    let out=[], from=0;
    while(true){
      const r = await q.range(from, from + PAGE_SIZE - 1);
      if(r.error) return r;
      const rows = r.data || [];
      out = out.concat(rows);
      if(rows.length < PAGE_SIZE) return {data:out,error:null};
      from += PAGE_SIZE;
      if(from > 50000) return {data:out,error:null};
    }
  }
  async function safe(name, q){
    try{ const r=await q; if(r.error) console.warn('[Tasneef '+FIX_VERSION+'] '+name, r.error.message); return r.data || []; }
    catch(e){ console.warn('[Tasneef '+FIX_VERSION+'] '+name, e?.message||e); return []; }
  }
  function mergeById(a,b){
    const m = new Map();
    (a||[]).concat(b||[]).forEach(x=>{ if(x && x.id!=null) m.set(String(x.id), x); });
    return [...m.values()].sort((x,y)=>String(y.check_in||y.log_date||y.created_at||'').localeCompare(String(x.check_in||x.log_date||x.created_at||'')));
  }
  async function loadTimeLogsMonthV281(r){
    // بعض سجلاتكم محفوظة بتاريخ check_in وبعضها بتاريخ log_date، لذلك نقرأ الاثنين ونقوم بدمجهم بدون تكرار.
    const byLogDate = await pageAll(
      sb.from('time_logs').select('*')
        .gte('log_date', r.start)
        .lt('log_date', r.end)
        .order('check_in', {ascending:false})
    );
    const byCheckIn = await pageAll(
      sb.from('time_logs').select('*')
        .gte('check_in', r.start + 'T00:00:00')
        .lt('check_in', r.end + 'T00:00:00')
        .order('check_in', {ascending:false})
    );
    if(byLogDate.error) console.warn('[Tasneef '+FIX_VERSION+'] time_logs/log_date', byLogDate.error.message);
    if(byCheckIn.error) console.warn('[Tasneef '+FIX_VERSION+'] time_logs/check_in', byCheckIn.error.message);
    return mergeById(byLogDate.data || [], byCheckIn.data || []);
  }
  async function loadAllV281(){
    const r = monthRange();
    const [users, projects, workers, attendance, logs, tickets] = await Promise.all([
      safe('app_users', sb.from('app_users').select('id,full_name,username,password,role,is_active,permissions').order('id')),
      safe('projects', sb.from('projects').select('*').order('id')),
      safe('workers', sb.from('workers').select('*').order('id')),
      pageAll(sb.from('attendance').select('*').gte('attendance_date', r.start).lt('attendance_date', r.end).order('attendance_date',{ascending:false})),
      loadTimeLogsMonthV281(r),
      safe('tickets', sb.from('tickets').select('*').order('created_at',{ascending:false}).limit(300))
    ]);
    let contractServices = {data:[],error:null};
    try{ contractServices = await sb.from('contract_services').select('*').order('id', { ascending:false }).limit(500); }catch(e){ console.warn(e); }
    data.users = users || [];
    data.supervisors = data.users.filter(u=>u.role==='supervisor' && u.is_active!==false);
    data.technicians = data.users.filter(u=>u.role==='technician' && u.is_active!==false);
    data.projects = projects || [];
    data.workers = workers || [];
    data.attendance = attendance.data || [];
    data.logs = logs || [];
    data.tickets = tickets || [];
    data.contractServices = contractServices.data || [];
    data.__loadedMonth = r.month;
    data.__fixVersion = FIX_VERSION;
  }
  async function refreshAllV281(){
    await loadAllV281();
    try{ hydrateForms(); }catch(e){ console.warn(e); }
    try{ renderAll(); }catch(e){ console.warn(e); }
    showBadge();
  }
  window.loadAll = loadAllV281;
  window.refreshAll = refreshAllV281;
  try{ loadAll = loadAllV281; refreshAll = refreshAllV281; }catch(e){}

  async function reloadSelectedMonthV281(){
    const active = document.activeElement;
    if(active) active.disabled = true;
    try{ await refreshAllV281(); }
    finally{ if(active) active.disabled = false; }
  }
  document.addEventListener('change', function(e){
    if(e.target && (e.target.id==='monthlyMonth' || e.target.id==='attendanceMatrixMonth')){
      setTimeout(reloadSelectedMonthV281, 80);
    }
  });
  function showBadge(){
    const old=document.getElementById('tasneefFixBadgeV280'); if(old) old.remove();
    let b=document.getElementById('tasneefFixBadgeV281');
    if(!b){ b=document.createElement('div'); b.id='tasneefFixBadgeV281'; document.body.appendChild(b); }
    b.textContent='Tasneef '+FIX_VERSION;
    b.style.cssText='position:fixed;left:10px;bottom:10px;z-index:99999;background:#0a4033;color:#fff;padding:6px 10px;border-radius:12px;font:12px Arial;box-shadow:0 4px 12px #0002;opacity:.9';
  }
  document.addEventListener('DOMContentLoaded', function(){ showBadge(); if(!document.getElementById('supTitle')) setTimeout(()=>{ if(document.body) refreshAllV281(); }, 300); });
  console.log('Tasneef '+FIX_VERSION+' loaded');
})();


/* Tasneef v282 - worker multi-project assignments fix */
(function(){
  const FIX_VERSION='v282-worker-multi-project';
  function $(id){ return document.getElementById(id); }
  function esc(s){ return String(s ?? '').replace(/[&<>"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])); }
  function safeId(v){ return v==null ? '' : String(v); }
  async function safeQuery(name, q){
    try{ const r=await q; if(r.error){ console.warn('[Tasneef '+FIX_VERSION+'] '+name, r.error.message); return []; } return r.data||[]; }
    catch(e){ console.warn('[Tasneef '+FIX_VERSION+'] '+name, e?.message||e); return []; }
  }
  const oldLoadAll = window.loadAll;
  const oldRefreshAll = window.refreshAll;
  async function loadAssignments(){
    // هذا الجدول يجعل العامل يظهر في أكثر من مشروع. إذا لم يتم تشغيل SQL، يعمل النظام بالآلية القديمة مؤقتًا.
    return await safeQuery('worker_project_assignments',
      sb.from('worker_project_assignments').select('*').eq('is_active', true).order('id')
    );
  }
  async function loadAllV282(){
    if(typeof oldLoadAll === 'function') await oldLoadAll();
    data.workerAssignments = await loadAssignments();
    data.__fixVersion = FIX_VERSION;
  }
  async function refreshAllV282(){
    await loadAllV282();
    try{ hydrateForms(); }catch(e){ console.warn(e); }
    try{ renderAll(); }catch(e){ console.warn(e); }
    showBadgeV282();
  }
  window.loadAll = loadAllV282;
  window.refreshAll = refreshAllV282;
  try{ loadAll = loadAllV282; refreshAll = refreshAllV282; }catch(e){}

  function workerAssignments(wid){
    return (data.workerAssignments||[]).filter(a=>safeId(a.worker_id)===safeId(wid) && a.is_active!==false);
  }
  function workerAssignedProjectIds(w){
    const ids = new Set();
    const primary = (typeof workerProjectId === 'function') ? workerProjectId(w) : (w.project_id || w.assigned_project_id || '');
    if(primary) ids.add(safeId(primary));
    workerAssignments(w.id).forEach(a=>{ if(a.project_id) ids.add(safeId(a.project_id)); });
    return [...ids];
  }
  function workerAssignedSupervisorIds(w){
    const ids = new Set();
    const direct = (typeof workerSupId === 'function') ? workerSupId(w) : (w.app_supervisor_id || w.supervisor_id || '');
    if(direct) ids.add(safeId(direct));
    workerAssignedProjectIds(w).forEach(pid=>{
      const p=(data.projects||[]).find(x=>safeId(x.id)===safeId(pid));
      if(p?.supervisor_id) ids.add(safeId(p.supervisor_id));
    });
    return [...ids];
  }
  function workerBelongsToProject(w,pid){
    if(!pid) return true;
    return workerAssignedProjectIds(w).includes(safeId(pid));
  }
  function workerBelongsToSupervisor(w,sid){
    if(!sid) return true;
    return workerAssignedSupervisorIds(w).includes(safeId(sid));
  }
  function assignmentProjectNames(w){
    const ids=workerAssignedProjectIds(w);
    if(!ids.length) return '-';
    return ids.map(id=>typeof projectName==='function'?projectName(id):id).join('، ');
  }
  function assignmentSupervisorNames(w){
    const ids=workerAssignedSupervisorIds(w);
    if(!ids.length) return '-';
    return ids.map(id=>typeof supervisorName==='function'?supervisorName(id):id).join('، ');
  }

  window.renderProjectManager = function(){
    const b=$('projectWorkersBody'); if(!b) return;
    const pid=$('manageProjectId')?.value;
    if(!pid){ b.innerHTML='<tr><td colspan="5">اختر مشروع من زر إدارة المشروع</td></tr>'; return; }
    const rows=(data.workers||[]).filter(w=>workerBelongsToProject(w,pid));
    b.innerHTML=rows.map(w=>`<tr><td>${esc(w.name)}</td><td>${esc(assignmentSupervisorNames(w))}</td><td><span class="badge ${(w.worker_type||'primary')==='support'?'amber':'green'}">${typeof workerTypeText==='function'?workerTypeText(w.worker_type):'أساسي'}</span></td><td><span class="badge ${w.status==='inactive'?'red':'green'}">${w.status==='inactive'?'موقوف':'نشط'}</span></td><td class="row-actions"><button class="danger" onclick="removeWorkerFromProject(${w.id})">إزالة من هذا المشروع</button></td></tr>`).join('')||'<tr><td colspan="5">لا يوجد عمال مرتبطون بهذا المشروع</td></tr>';
  };
  try{ renderProjectManager = window.renderProjectManager; }catch(e){}

  window.addExistingWorkerToProject = async function(){
    const pid=Number($('manageProjectId')?.value), wid=Number($('manageWorkerSelect')?.value), type=$('manageWorkerType')?.value||'primary';
    if(!pid||!wid) return msg('اختر المشروع والعامل','err');
    const p=(data.projects||[]).find(x=>Number(x.id)===pid); const sid=p?.supervisor_id||null;
    // نضيف ربط جديد بدل تحديث project_id حتى لا يختفي العامل من مشروعه السابق.
    let res = await sb.from('worker_project_assignments').upsert({worker_id:wid, project_id:pid, worker_type:type, is_active:true},{onConflict:'worker_id,project_id'});
    if(res.error){
      // fallback إذا لم تشغّل ملف SQL بعد.
      res = await sb.from('workers').update({project_id:pid, supervisor_id:sid, app_supervisor_id:sid, worker_type:type}).eq('id',wid);
      if(res.error) return msg('شغّل ملف SQL الخاص بتعدد المشاريع أولاً: '+res.error.message,'err');
      msg('تم ربط العامل بالمشروع، لكن لتعدّد المشاريع شغّل ملف SQL v282','err');
    } else {
      await sb.from('workers').update({supervisor_id:sid, app_supervisor_id:sid}).eq('id',wid);
      msg('تم إضافة العامل لهذا المشروع بدون إزالته من المشاريع الأخرى');
    }
    await refreshAllV282(); openProjectManager(pid);
  };
  try{ addExistingWorkerToProject = window.addExistingWorkerToProject; }catch(e){}

  window.addWorkerInsideProject = async function(){
    const pid=Number($('manageProjectId')?.value); if(!pid) return msg('اختر المشروع أولاً','err');
    const name=$('manageNewWorkerName')?.value.trim(); if(!name) return msg('اسم العامل مطلوب','err');
    const p=(data.projects||[]).find(x=>x.id===pid); const sid=p?.supervisor_id||null;
    const row={name, phone:$('manageNewWorkerPhone')?.value.trim()||'', salary:Number($('manageNewWorkerSalary')?.value||1500), supervisor_id:sid, app_supervisor_id:sid, project_id:pid, worker_type:$('manageNewWorkerType')?.value||'primary', status:'active'};
    const ins=await sb.from('workers').insert(row).select('id').single();
    if(ins.error) return msg(ins.error.message,'err');
    await sb.from('worker_project_assignments').upsert({worker_id:ins.data.id, project_id:pid, worker_type:row.worker_type, is_active:true},{onConflict:'worker_id,project_id'});
    ['manageNewWorkerName','manageNewWorkerPhone'].forEach(id=>$(id)&&($(id).value=''));
    if($('manageNewWorkerSalary')) $('manageNewWorkerSalary').value=1500;
    msg('تم إضافة العامل وربطه بالمشروع');
    await refreshAllV282(); openProjectManager(pid);
  };
  try{ addWorkerInsideProject = window.addWorkerInsideProject; }catch(e){}

  window.removeWorkerFromProject = async function(wid){
    if(!confirm('إزالة العامل من هذا المشروع فقط؟')) return;
    const pid=Number($('manageProjectId')?.value);
    let res = await sb.from('worker_project_assignments').update({is_active:false}).eq('worker_id',wid).eq('project_id',pid);
    if(res.error){
      res = await sb.from('workers').update({project_id:null}).eq('id',wid).eq('project_id',pid);
      if(res.error) return msg(res.error.message,'err');
    }
    msg('تمت إزالة العامل من هذا المشروع فقط');
    await refreshAllV282(); if(pid) openProjectManager(pid);
  };
  try{ removeWorkerFromProject = window.removeWorkerFromProject; }catch(e){}

  window.renderWorkers = function(){
    const b=$('workersBody'); if(!b) return;
    const s=$('workerFilterSupervisor')?.value, p=$('workerFilterProject')?.value, st=$('workerFilterStatus')?.value, tp=$('workerFilterType')?.value, q=($('workerSearch')?.value||'').trim();
    let rows=data.workers||[];
    if(s) rows=rows.filter(w=>workerBelongsToSupervisor(w,s));
    if(p) rows=rows.filter(w=>workerBelongsToProject(w,p));
    if(st) rows=rows.filter(w=>(w.status||'active')===st);
    if(tp) rows=rows.filter(w=>(w.worker_type||'primary')===tp || workerAssignments(w.id).some(a=>(a.worker_type||'primary')===tp));
    if(q) rows=rows.filter(w=>[w.name,w.phone,assignmentSupervisorNames(w),assignmentProjectNames(w),w.notes].join(' ').includes(q));
    b.innerHTML=rows.map(w=>`<tr><td>${esc(w.name)}</td><td>${esc(assignmentSupervisorNames(w))}</td><td>${esc(assignmentProjectNames(w))}</td><td><span class="badge ${(w.worker_type||'primary')==='support'?'amber':'green'}">${typeof workerTypeText==='function'?workerTypeText(w.worker_type):'أساسي'}</span></td><td>${esc(w.phone)}</td><td>${w.salary||0}</td><td><span class="badge ${w.status==='inactive'?'red':'green'}">${w.status==='inactive'?'موقوف':'نشط'}</span></td><td>${esc(w.notes||'-')}</td><td class="row-actions"><button onclick="editWorker(${w.id})">تعديل</button><button class="light" onclick="toggleWorkerStatus(${w.id})">${w.status==='inactive'?'تفعيل':'إيقاف'}</button><button class="danger" onclick="deleteRow('workers',${w.id})">حذف</button></td></tr>`).join('')||'<tr><td colspan="9">لا توجد بيانات</td></tr>';
  };
  try{ renderWorkers = window.renderWorkers; }catch(e){}

  const oldInitSupervisor = window.initSupervisor;
  window.initSupervisor = async function(){
    const u=requireRole('supervisor'); if(!u) return;
    await loadAllV282();
    data.projects=(data.projects||[]).filter(p=>safeId(p.supervisor_id)===safeId(u.id));
    const supProjectIds = new Set(data.projects.map(p=>safeId(p.id)));
    data.workers=(data.workers||[]).filter(w=>workerBelongsToSupervisor(w,u.id) || workerAssignedProjectIds(w).some(pid=>supProjectIds.has(safeId(pid))));
    data.logs=(data.logs||[]).filter(l=>safeId(l.supervisor_id)===safeId(u.id) || supProjectIds.has(safeId(l.project_id)));
    data.tickets=(data.tickets||[]).filter(t=>safeId(t.supervisor_id)===safeId(u.id) || safeId(t.created_by)===safeId(u.id) || supProjectIds.has(safeId(t.project_id)));
    if($('supTitle')) $('supTitle').textContent=`لوحة المشرف - ${u.full_name}`;
    if(typeof fillSelect==='function'){
      fillSelect('logProject',data.projects,'name','اختر المشروع'); fillSelect('attendanceProject',data.projects,'name','اختر المشروع'); fillSelect('ticketProject',data.projects,'name','اختر المشروع');
    }
    if($('logDate')) $('logDate').value=(typeof today==='function'?today():new Date().toISOString().slice(0,10));
    if($('attendanceDate')) $('attendanceDate').value=(typeof today==='function'?today():new Date().toISOString().slice(0,10));
    try{ renderSupervisorAttendanceList(); renderTimeLogs(); }catch(e){ console.warn(e); }
    try{ await supervisorInventoryLoad(); }catch(e){}
    showBadgeV282();
  };
  try{ initSupervisor = window.initSupervisor; }catch(e){}

  function showBadgeV282(){
    ['tasneefFixBadgeV280','tasneefFixBadgeV281','tasneefFixBadgeV282'].forEach(id=>{ const old=document.getElementById(id); if(old) old.remove(); });
    const b=document.createElement('div'); b.id='tasneefFixBadgeV282'; document.body.appendChild(b);
    b.textContent='Tasneef '+FIX_VERSION;
    b.style.cssText='position:fixed;left:10px;bottom:10px;z-index:99999;background:#0a4033;color:#fff;padding:6px 10px;border-radius:12px;font:12px Arial;box-shadow:0 4px 12px #0002;opacity:.9';
  }
  document.addEventListener('DOMContentLoaded', function(){ showBadgeV282(); if(!document.getElementById('supTitle')) setTimeout(()=>refreshAllV282(), 500); });
  console.log('Tasneef '+FIX_VERSION+' loaded');
})();


/* Tasneef v283 - Smart Supervisor Projects & Workers Assignment Window */
(function(){
  const FIX_VERSION='v283-smart-supervisor-assignment';
  function $(id){ return document.getElementById(id); }
  function esc(s){ return String(s ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
  function sid(v){ return v==null ? '' : String(v); }
  function activeWorkers(){ return (window.data?.workers||[]).filter(w => (w.status||'active') !== 'inactive'); }
  function isAssigned(wid,pid){ return (window.data?.workerAssignments||[]).some(a => sid(a.worker_id)===sid(wid) && sid(a.project_id)===sid(pid) && a.is_active!==false); }
  function assignedProjectNames(wid){
    const ids = new Set();
    const w = (window.data?.workers||[]).find(x=>sid(x.id)===sid(wid));
    if(w?.project_id) ids.add(sid(w.project_id));
    (window.data?.workerAssignments||[]).forEach(a=>{ if(sid(a.worker_id)===sid(wid) && a.is_active!==false && a.project_id) ids.add(sid(a.project_id)); });
    const names=[...ids].map(id => (typeof projectName==='function'?projectName(id):id)).filter(Boolean);
    return names.length ? names.join('، ') : '-';
  }
  function workerMatchesSupervisor(w, supervisorId){
    if(!supervisorId) return true;
    if(sid(w.supervisor_id)===sid(supervisorId) || sid(w.app_supervisor_id)===sid(supervisorId)) return true;
    const supProjectIds = new Set((window.data?.projects||[]).filter(p=>sid(p.supervisor_id)===sid(supervisorId)).map(p=>sid(p.id)));
    if(w.project_id && supProjectIds.has(sid(w.project_id))) return true;
    return (window.data?.workerAssignments||[]).some(a => sid(a.worker_id)===sid(w.id) && a.is_active!==false && supProjectIds.has(sid(a.project_id)));
  }
  function ensureSmartModal(){
    if($('smartWorkerProjectModalV283')) return;
    const wrap=document.createElement('div');
    wrap.id='smartWorkerProjectModalV283';
    wrap.className='smart-modal-v283 hidden';
    wrap.innerHTML=`
      <div class="smart-card-v283" dir="rtl">
        <div class="smart-head-v283">
          <div><h2>ربط العمال بالمشاريع حسب المشرف</h2><p>اختر المشرف، ثم حدد المشاريع والعمال من نافذة واحدة.</p></div>
          <button type="button" class="light" onclick="closeSmartWorkerProjectModalV283()">إغلاق</button>
        </div>
        <div class="smart-top-v283">
          <div><label>المشرف</label><select id="smartSupervisorV283" onchange="renderSmartWorkerProjectV283()"></select></div>
          <div><label>نوع الربط</label><select id="smartWorkerTypeV283"><option value="primary">أساسي</option><option value="support">بديل / مساند</option></select></div>
          <div><label>بحث عامل</label><input id="smartWorkerSearchV283" oninput="renderSmartWorkerProjectV283()" placeholder="اكتب اسم العامل"></div>
          <label class="smart-check-v283"><input type="checkbox" id="smartShowAllWorkersV283" onchange="renderSmartWorkerProjectV283()"> عرض كل العمال</label>
        </div>
        <div class="smart-grid-v283">
          <div class="smart-panel-v283">
            <div class="smart-panel-title-v283"><h3>مشاريع المشرف</h3><button type="button" class="light" onclick="toggleSmartProjectsV283(true)">تحديد الكل</button></div>
            <div id="smartProjectsListV283" class="smart-list-v283"></div>
          </div>
          <div class="smart-panel-v283">
            <div class="smart-panel-title-v283"><h3>العمال</h3><button type="button" class="light" onclick="toggleSmartWorkersV283(true)">تحديد الكل</button></div>
            <div id="smartWorkersListV283" class="smart-list-v283"></div>
          </div>
        </div>
        <div class="smart-actions-v283">
          <button type="button" onclick="saveSmartWorkerProjectLinksV283()">حفظ الربط</button>
          <button type="button" class="danger" onclick="removeSmartWorkerProjectLinksV283()">إزالة الربط المحدد</button>
          <button type="button" class="light" onclick="toggleSmartProjectsV283(false);toggleSmartWorkersV283(false)">إلغاء التحديد</button>
        </div>
        <div class="smart-preview-title-v283">معاينة الربط الحالي</div>
        <div class="table-wrap smart-preview-v283"><table><thead><tr><th>العامل</th><th>مشاريعه الحالية</th></tr></thead><tbody id="smartPreviewBodyV283"></tbody></table></div>
      </div>`;
    document.body.appendChild(wrap);
    const st=document.createElement('style');
    st.textContent=`
      .smart-modal-v283{position:fixed;inset:0;z-index:99990;background:rgba(0,0,0,.42);display:flex;align-items:flex-start;justify-content:center;padding:24px;overflow:auto}.smart-modal-v283.hidden{display:none}
      .smart-card-v283{width:min(1120px,100%);background:#fff;border-radius:22px;padding:18px;box-shadow:0 20px 60px #0003;color:#123}.smart-head-v283,.smart-panel-title-v283,.smart-actions-v283{display:flex;align-items:center;justify-content:space-between;gap:10px}.smart-head-v283 h2{margin:0}.smart-head-v283 p{margin:4px 0 0;color:#667}
      .smart-top-v283{display:grid;grid-template-columns:1.2fr .8fr 1fr auto;gap:12px;margin:14px 0}.smart-top-v283 label{font-weight:700;font-size:13px}.smart-check-v283{display:flex;align-items:end;gap:6px;padding-bottom:10px;white-space:nowrap}
      .smart-grid-v283{display:grid;grid-template-columns:1fr 1fr;gap:14px}.smart-panel-v283{border:1px solid #e4ebe7;border-radius:18px;padding:12px;background:#fbfdfc}.smart-panel-title-v283 h3{margin:0 0 8px}
      .smart-list-v283{display:grid;gap:8px;max-height:330px;overflow:auto;padding:4px}.smart-item-v283{display:flex;gap:8px;align-items:flex-start;background:#fff;border:1px solid #e7eee9;border-radius:14px;padding:9px}.smart-item-v283 small{color:#667;display:block;margin-top:3px}.smart-actions-v283{justify-content:flex-start;margin:14px 0}.smart-preview-title-v283{font-weight:800;margin-top:6px}.smart-preview-v283{max-height:230px;overflow:auto}.smart-injected-btn-v283{margin-inline-start:8px}
      @media(max-width:800px){.smart-top-v283,.smart-grid-v283{grid-template-columns:1fr}.smart-modal-v283{padding:10px}.smart-card-v283{border-radius:16px}.smart-head-v283{align-items:flex-start}.smart-check-v283{align-items:center;padding:0}}
    `;
    document.head.appendChild(st);
  }
  function fillSmartSupervisor(){
    const sel=$('smartSupervisorV283'); if(!sel) return;
    const old=sel.value;
    sel.innerHTML='<option value="">اختر المشرف</option>'+(window.data?.supervisors||[]).map(s=>`<option value="${esc(s.id)}">${esc(s.full_name||s.username||s.id)}</option>`).join('');
    if(old) sel.value=old;
  }
  window.openSmartWorkerProjectModalV283 = async function(){
    ensureSmartModal();
    if(typeof refreshAll === 'function') { try{ await refreshAll(); }catch(e){ console.warn(e); } }
    fillSmartSupervisor();
    $('smartWorkerProjectModalV283').classList.remove('hidden');
    renderSmartWorkerProjectV283();
  };
  window.closeSmartWorkerProjectModalV283 = function(){ const m=$('smartWorkerProjectModalV283'); if(m) m.classList.add('hidden'); };
  window.renderSmartWorkerProjectV283 = function(){
    ensureSmartModal(); fillSmartSupervisor();
    const supervisorId=$('smartSupervisorV283')?.value||'';
    const q=($('smartWorkerSearchV283')?.value||'').trim();
    const showAll=$('smartShowAllWorkersV283')?.checked;
    const projects=(window.data?.projects||[]).filter(p=>!supervisorId || sid(p.supervisor_id)===sid(supervisorId));
    let workers=activeWorkers();
    if(supervisorId && !showAll) workers=workers.filter(w=>workerMatchesSupervisor(w,supervisorId));
    if(q) workers=workers.filter(w=>[w.name,w.phone,assignedProjectNames(w.id)].join(' ').includes(q));
    $('smartProjectsListV283').innerHTML = supervisorId ? (projects.map(p=>`<label class="smart-item-v283"><input type="checkbox" class="smartProjectChkV283" value="${esc(p.id)}"><span><b>${esc(p.name)}</b><small>${esc(p.location||'')} ${p.status==='inactive'?' - متوقف':''}</small></span></label>`).join('') || '<div class="footer-note">لا توجد مشاريع لهذا المشرف</div>') : '<div class="footer-note">اختر المشرف أولاً لعرض مشاريعه</div>';
    $('smartWorkersListV283').innerHTML = supervisorId ? (workers.map(w=>`<label class="smart-item-v283"><input type="checkbox" class="smartWorkerChkV283" value="${esc(w.id)}"><span><b>${esc(w.name)}</b><small>${esc(assignedProjectNames(w.id))}</small></span></label>`).join('') || '<div class="footer-note">لا يوجد عمال مطابقون</div>') : '<div class="footer-note">اختر المشرف أولاً لعرض العمال</div>';
    $('smartPreviewBodyV283').innerHTML = workers.slice(0,120).map(w=>`<tr><td>${esc(w.name)}</td><td>${esc(assignedProjectNames(w.id))}</td></tr>`).join('') || '<tr><td colspan="2">لا توجد بيانات</td></tr>';
  };
  window.toggleSmartProjectsV283 = function(on){ document.querySelectorAll('.smartProjectChkV283').forEach(x=>x.checked=!!on); };
  window.toggleSmartWorkersV283 = function(on){ document.querySelectorAll('.smartWorkerChkV283').forEach(x=>x.checked=!!on); };
  function selectedVals(cls){ return [...document.querySelectorAll(cls+':checked')].map(x=>Number(x.value)).filter(Boolean); }
  window.saveSmartWorkerProjectLinksV283 = async function(){
    const pids=selectedVals('.smartProjectChkV283'), wids=selectedVals('.smartWorkerChkV283'), type=$('smartWorkerTypeV283')?.value||'primary';
    if(!pids.length || !wids.length) return msg('حدد مشروع واحد وعامل واحد على الأقل','err');
    const rows=[]; wids.forEach(wid=>pids.forEach(pid=>rows.push({worker_id:wid,project_id:pid,worker_type:type,is_active:true,updated_at:new Date().toISOString()})));
    const res=await sb.from('worker_project_assignments').upsert(rows,{onConflict:'worker_id,project_id'});
    if(res.error) return msg('يجب تشغيل SQL v283 أولاً: '+res.error.message,'err');
    // توافق مع الشاشات القديمة: نضع مشرف أول مشروع كمرجع فقط، والربط الحقيقي يبقى في جدول worker_project_assignments.
    const firstProject=(window.data?.projects||[]).find(p=>Number(p.id)===pids[0]);
    if(firstProject?.supervisor_id){
      await sb.from('workers').update({supervisor_id:firstProject.supervisor_id,app_supervisor_id:firstProject.supervisor_id}).in('id',wids);
    }
    msg('تم ربط العمال بالمشاريع المحددة بنجاح');
    if(typeof refreshAll==='function') await refreshAll();
    renderSmartWorkerProjectV283();
  };
  window.removeSmartWorkerProjectLinksV283 = async function(){
    const pids=selectedVals('.smartProjectChkV283'), wids=selectedVals('.smartWorkerChkV283');
    if(!pids.length || !wids.length) return msg('حدد المشاريع والعمال المراد إزالة الربط بينهم','err');
    if(!confirm('سيتم إزالة الربط بين العمال والمشاريع المحددة فقط. هل أنت متأكد؟')) return;
    let ok=true, errMsg='';
    for(const wid of wids){
      const res=await sb.from('worker_project_assignments').update({is_active:false,updated_at:new Date().toISOString()}).eq('worker_id',wid).in('project_id',pids);
      if(res.error){ ok=false; errMsg=res.error.message; break; }
    }
    if(!ok) return msg(errMsg,'err');
    msg('تمت إزالة الربط المحدد');
    if(typeof refreshAll==='function') await refreshAll();
    renderSmartWorkerProjectV283();
  };
  function injectButton(){
    if($('openSmartWorkerProjectBtnV283')) return;
    const projectsSection=$('projects'); if(!projectsSection) return;
    const head=projectsSection.querySelector('.section-head'); if(!head) return;
    const btn=document.createElement('button'); btn.id='openSmartWorkerProjectBtnV283'; btn.className='smart-injected-btn-v283'; btn.textContent='ربط العمال بالمشاريع'; btn.type='button'; btn.onclick=()=>openSmartWorkerProjectModalV283();
    head.appendChild(btn);
  }
  function showBadge(){
    ['tasneefFixBadgeV280','tasneefFixBadgeV281','tasneefFixBadgeV282','tasneefFixBadgeV283'].forEach(id=>{ const old=$(id); if(old) old.remove(); });
    const b=document.createElement('div'); b.id='tasneefFixBadgeV283'; document.body.appendChild(b);
    b.textContent='Tasneef '+FIX_VERSION;
    b.style.cssText='position:fixed;left:10px;bottom:10px;z-index:99999;background:#0a4033;color:#fff;padding:6px 10px;border-radius:12px;font:12px Arial;box-shadow:0 4px 12px #0002;opacity:.9';
  }
  document.addEventListener('DOMContentLoaded', function(){ ensureSmartModal(); injectButton(); showBadge(); setTimeout(()=>{ injectButton(); showBadge(); }, 900); });
  console.log('Tasneef '+FIX_VERSION+' loaded');
})();

/* Tasneef v284 - Real Monthly Previous Months + Supervisor Worker Distribution Export */
(function(){
  'use strict';
  const FIX_VERSION='v284-monthly-history-distribution-export';
  const $ = id => document.getElementById(id);
  const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const sid = v => v==null ? '' : String(v);
  const num = v => { const n=Number(String(v ?? 0).replace(/,/g,'')); return Number.isFinite(n)?n:0; };
  const today = () => { const d=new Date(); return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0'); };
  const monthVal = () => $('monthlyMonth')?.value || today().slice(0,7);
  const monthStart = m => (m || monthVal()) + '-01';
  const monthEndExclusive = m => { const [y,mo]=String(m||monthVal()).split('-').map(Number); return new Date(y, mo, 1).toISOString().slice(0,10); };
  const dateOnly = v => String(v || '').slice(0,10).replace(/\//g,'-');
  const inMonth = (v,m) => dateOnly(v).slice(0,7) === String(m||monthVal()).slice(0,7);
  function supName(id){ return (typeof window.supervisorName==='function'?window.supervisorName(id):'') || (window.data?.users||[]).find(u=>sid(u.id)===sid(id))?.full_name || '-'; }
  function projName(id){ return (typeof window.projectName==='function'?window.projectName(id):'') || (window.data?.projects||[]).find(p=>sid(p.id)===sid(id))?.name || '-'; }
  function projectSup(pid){ return (window.data?.projects||[]).find(p=>sid(p.id)===sid(pid))?.supervisor_id || null; }
  function actualMinutes(l){
    if(num(l.duration_minutes)) return num(l.duration_minutes);
    if(l.check_in && l.check_out && typeof window.minutesBetween==='function') return num(window.minutesBetween(l.check_in,l.check_out));
    if(l.check_in && l.check_out){ const a=new Date(l.check_in), b=new Date(l.check_out); const d=(b-a)/60000; return Number.isFinite(d)&&d>0?Math.round(d):0; }
    return 0;
  }
  function minsText(m){ return (typeof window.minsToText==='function') ? window.minsToText(num(m)) : (Math.floor(num(m)/60)+' ساعة '+(num(m)%60)+' دقيقة'); }
  function pctText(v){ return (num(v).toFixed(1).replace(/\.0$/,'') + '%'); }
  function status(percent){ const p=num(percent); if(p>=90&&p<=110) return {text:'ممتاز',cls:'green'}; if((p>=70&&p<90)||(p>110&&p<=140)) return {text:'جيد',cls:'amber'}; return {text:'ضعيف',cls:'red'}; }
  function assignments(){ return (window.data?.workerAssignments||[]).filter(a=>a && a.is_active!==false); }
  function workerName(id){ return (typeof window.workerName==='function'?window.workerName(id):'') || (window.data?.workers||[]).find(w=>sid(w.id)===sid(id))?.name || ''; }
  function workersForProject(pid){
    const names = new Set();
    (window.data?.workers||[]).forEach(w=>{ if(sid(w.project_id)===sid(pid) && (w.status||'active')!=='inactive') names.add(w.name); });
    assignments().forEach(a=>{ if(sid(a.project_id)===sid(pid)){ const n=workerName(a.worker_id); if(n) names.add(n); } });
    return [...names].filter(Boolean).join('، ') || '-';
  }
  function mergeLogs(newRows){
    if(!window.data) window.data={};
    const map = new Map();
    (window.data.logs||[]).forEach(l=>{ if(l && l.id!=null) map.set(sid(l.id), l); });
    (newRows||[]).forEach(l=>{ if(l && l.id!=null) map.set(sid(l.id), l); });
    window.data.logs = [...map.values()];
  }
  window.fetchMonthlyLogsV284 = async function(month){
    month = month || monthVal();
    const start=monthStart(month), end=monthEndExclusive(month);
    const cacheKey='__monthly_v284_'+month;
    if(window.data?.[cacheKey]) return (window.data.logs||[]).filter(l=>inMonth(l.log_date||l.check_in||l.created_at,month));
    const results=[]; const errors=[];
    try{
      const r1 = await sb.from('time_logs').select('*').gte('log_date',start).lt('log_date',end).order('id',{ascending:false}).limit(10000);
      if(r1.error) errors.push(r1.error.message); else results.push(...(r1.data||[]));
    }catch(e){ errors.push(e?.message||String(e)); }
    try{
      const r2 = await sb.from('time_logs').select('*').gte('check_in',start).lt('check_in',end).order('id',{ascending:false}).limit(10000);
      if(r2.error) errors.push(r2.error.message); else results.push(...(r2.data||[]));
    }catch(e){ errors.push(e?.message||String(e)); }
    mergeLogs(results);
    window.data[cacheKey]=true;
    if(errors.length) window.data.monthlyV284Error=errors.join(' | '); else window.data.monthlyV284Error='';
    return (window.data.logs||[]).filter(l=>inMonth(l.log_date||l.check_in||l.created_at,month));
  };
  window.monthlyRowsV284 = function(){
    const month=monthVal(); const selectedSup=$('monthlySupervisor')?.value || '';
    let logs=(window.data?.logs||[]).filter(l=>inMonth(l.log_date||l.check_in||l.created_at,month));
    const map=new Map();
    logs.forEach(l=>{
      const pid=l.project_id; if(!pid) return;
      const sup=l.supervisor_id || projectSup(pid) || '';
      if(selectedSup && sid(sup)!==sid(selectedSup)) return;
      const key=sid(sup)+'_'+sid(pid);
      if(!map.has(key)) map.set(key,{s:sup,p:pid,a:0,c:0});
      const row=map.get(key); row.a += actualMinutes(l); row.c += 1;
    });
    const rows=[...map.values()];
    const supTotals={}; rows.forEach(r=>{ const s=sid(r.s); supTotals[s]=(supTotals[s]||0)+num(r.a); });
    return rows.map(r=>{ const total=supTotals[sid(r.s)]||0; const percent=total?num(r.a)/total*100:0; const st=status(percent); return {...r,total,percent,workers:workersForProject(r.p),st:st.text,cls:st.cls}; })
      .sort((a,b)=>String(supName(a.s)).localeCompare(String(supName(b.s)),'ar') || String(projName(a.p)).localeCompare(String(projName(b.p)),'ar'));
  };
  window.renderMonthly = async function(){
    const body=$('monthlyBody'); if(!body) return;
    const month=monthVal();
    body.innerHTML='<tr><td colspan="6">جاري تحميل سجلات الشهر من قاعدة البيانات...</td></tr>';
    await window.fetchMonthlyLogsV284(month);
    const table=body.closest('table');
    if(table?.tHead) table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>الساعات الفعلية</th><th>نسبة العمل</th><th>حالة الأداء</th></tr>';
    const rows=window.monthlyRowsV284();
    if(!rows.length){
      const err=window.data?.monthlyV284Error ? '<br><small style="color:#b00020">'+esc(window.data.monthlyV284Error)+'</small>' : '';
      body.innerHTML='<tr><td colspan="6">لا توجد بيانات لهذا الشهر في جدول time_logs'+err+'</td></tr>';
      if($('monthlySummary')) $('monthlySummary').innerHTML='<div class="kpi"><small>إجمالي الساعات الفعلية</small><b>0 دقيقة</b></div><div class="kpi"><small>عدد المشرفين</small><b>0</b></div><div class="kpi"><small>عدد المشاريع</small><b>0</b></div>';
      return;
    }
    let html='', last='__none__', groupTotal=0;
    const flush=()=>{ if(last!=='__none__'){ html += `<tr class="monthly-total-row" style="background:#f4fbf8;font-weight:900"><td colspan="3">مجموع ${esc(supName(last))}</td><td>${esc(minsText(groupTotal))}</td><td><span class="badge green">100%</span></td><td>—</td></tr>`; }};
    rows.forEach(r=>{ if(last!=='__none__' && sid(last)!==sid(r.s)){ flush(); groupTotal=0; } last=r.s; groupTotal+=num(r.a); html+=`<tr><td>${esc(supName(r.s))}</td><td>${esc(projName(r.p))}</td><td>${esc(r.workers||'-')}</td><td>${esc(minsText(r.a))}</td><td><span class="badge green">${esc(pctText(r.percent))}</span></td><td><span class="badge ${r.cls}">${esc(r.st)}</span></td></tr>`; });
    flush(); body.innerHTML=html;
    const grand=rows.reduce((a,r)=>a+num(r.a),0); const supCount=new Set(rows.map(r=>sid(r.s))).size;
    if($('monthlySummary')) $('monthlySummary').innerHTML=`<div class="kpi"><small>إجمالي الساعات الفعلية</small><b>${esc(minsText(grand))}</b></div><div class="kpi"><small>عدد المشرفين</small><b>${supCount}</b></div><div class="kpi"><small>عدد المشاريع</small><b>${rows.length}</b></div><div class="kpi"><small>الشهر</small><b>${esc(month)}</b></div>`;
  };
  window.exportMonthlyCSV = async function(){ await window.renderMonthly(); const rows=[...document.querySelectorAll('#monthlyBody tr')].map(tr=>[...tr.children].map(td=>td.textContent.trim())); const csv='\ufeff'+['المشرف,المشروع,أسماء العمال,الساعات الفعلية,نسبة العمل,حالة الأداء',...rows.map(r=>r.map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(','))].join('\n'); const blob=new Blob([csv],{type:'text/csv;charset=utf-8'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='monthly_time_'+monthVal()+'.csv'; a.click(); setTimeout(()=>URL.revokeObjectURL(a.href),1200); };
  window.exportSupervisorWorkerDistributionV284 = function(){
    const projects=(window.data?.projects||[]).slice().sort((a,b)=>String(supName(a.supervisor_id)).localeCompare(String(supName(b.supervisor_id)),'ar')||String(a.name||'').localeCompare(String(b.name||''),'ar'));
    const rows=[];
    projects.forEach(p=>{
      const workers=workersForProject(p.id);
      rows.push({supervisor:supName(p.supervisor_id), project:p.name||projName(p.id), workers, count: workers==='-'?0:workers.split('،').filter(Boolean).length});
    });
    const htmlRows=rows.map(r=>`<tr><td>${esc(r.supervisor)}</td><td>${esc(r.project)}</td><td>${esc(r.workers)}</td><td>${esc(r.count)}</td></tr>`).join('');
    const html=`<html dir="rtl" lang="ar"><head><meta charset="utf-8"><style>body{font-family:Tahoma,Arial}table{border-collapse:collapse;width:100%}th{background:#0a4033;color:white}td,th{border:1px solid #999;padding:8px;text-align:right}h2{color:#0a4033}</style></head><body><h2>توزيع المشرفين مع المشاريع والعمال</h2><table><thead><tr><th>المشرف</th><th>المشروع</th><th>العمال</th><th>عدد العمال</th></tr></thead><tbody>${htmlRows}</tbody></table></body></html>`;
    const blob=new Blob(['\ufeff'+html],{type:'application/vnd.ms-excel;charset=utf-8'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='supervisors_workers_distribution.xls'; a.click(); setTimeout(()=>URL.revokeObjectURL(a.href),1200);
  };
  function injectExportButton(){
    if($('exportSupervisorWorkerDistributionV284')) return;
    const projectsSection=$('projects') || $('workers') || document.body;
    const head=projectsSection.querySelector?.('.section-head') || projectsSection;
    const btn=document.createElement('button'); btn.id='exportSupervisorWorkerDistributionV284'; btn.type='button'; btn.textContent='تنزيل توزيع المشرفين والعمال'; btn.onclick=()=>window.exportSupervisorWorkerDistributionV284(); btn.style.marginInlineStart='8px';
    head.appendChild(btn);
  }
  function bindMonthlyChange(){ const m=$('monthlyMonth'), s=$('monthlySupervisor'); if(m && !m.dataset.v284Bound){ m.dataset.v284Bound='1'; m.addEventListener('change',()=>window.renderMonthly()); } if(s && !s.dataset.v284Bound){ s.dataset.v284Bound='1'; s.addEventListener('change',()=>window.renderMonthly()); } }
  function showBadge(){ ['tasneefFixBadgeV280','tasneefFixBadgeV281','tasneefFixBadgeV282','tasneefFixBadgeV283','tasneefFixBadgeV284'].forEach(id=>{ const old=$(id); if(old) old.remove(); }); const b=document.createElement('div'); b.id='tasneefFixBadgeV284'; b.textContent='Tasneef '+FIX_VERSION; b.style.cssText='position:fixed;left:10px;bottom:10px;z-index:99999;background:#0a4033;color:white;padding:6px 10px;border-radius:12px;font:12px Arial;box-shadow:0 4px 12px #0002;opacity:.9'; document.body.appendChild(b); }
  document.addEventListener('DOMContentLoaded',()=>{ injectExportButton(); bindMonthlyChange(); showBadge(); setTimeout(()=>{ injectExportButton(); bindMonthlyChange(); if($('monthlyBody')) window.renderMonthly(); showBadge(); },1200); });
  window.addEventListener('load',()=>setTimeout(()=>{ injectExportButton(); bindMonthlyChange(); showBadge(); },2200));
  console.log('Tasneef '+FIX_VERSION+' loaded');
})();

/* V299 - Contract smart window, annual services, client report table */
(function(){
  const LS_KEY='tasneef_contract_smart_v299';
  const CORE_SERVICES=[
    {key:'elevators', name:'المصاعد'},
    {key:'civilDefense', name:'الدفاع المدني'},
    {key:'pools', name:'المسابح'}
  ];
  const ANNUAL_OPTIONS=['غسيل الخزانات العلوية','غسيل الخزانات الأرضية','رش المبيدات','غسيل الأسطح','غسيل الممرات بالمكائن','غسيل الممر العام','غسيل المناور','غسيل المكيفات','تنظيف فلاتر المكيفات','تنظيف غرف المصاعد','غسيل المواقف','غسيل الأرصفة','تنظيف عدسات الكاميرات','التعطير','إعادة التشجير','صهريج مياه','صهريج صرف صحي','خدمة أخرى'];
  let smartCache={};
  let smartLoaded=false;
  let currentSmartProjectId=null;
  let currentSmartReadonly=false;

  function el(id){return document.getElementById(id)}
  function h(v){return String(v??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[m]))}
  function todayIso(){return new Date().toISOString().slice(0,10)}
  function defaultSmart(){
    const core={}; CORE_SERVICES.forEach(s=>core[s.key]={onUs:false, visits:0, done:[], company:'', phone:'', attachment:'', notes:''});
    return {association:{name:'', manager:'', phone:''}, core, annual:[], updated_at:null};
  }
  function mergeSmart(obj){
    const d=defaultSmart(); obj=obj||{};
    d.association={...d.association,...(obj.association||{})};
    CORE_SERVICES.forEach(s=>{d.core[s.key]={...d.core[s.key],...((obj.core||{})[s.key]||{})}; if(!Array.isArray(d.core[s.key].done)) d.core[s.key].done=[];});
    d.annual=Array.isArray(obj.annual)?obj.annual.map(a=>({id:a.id||('a'+Date.now()+Math.random().toString(16).slice(2)), name:a.name||'', visits:Number(a.visits)||1, done:Array.isArray(a.done)?a.done:[], notes:a.notes||''})):[];
    d.updated_at=obj.updated_at||null;
    return d;
  }
  function readLS(){try{return JSON.parse(localStorage.getItem(LS_KEY)||'{}')||{}}catch(e){return {}}}
  function writeLS(){try{localStorage.setItem(LS_KEY,JSON.stringify(smartCache));}catch(e){}}
  async function loadSmart(){
    if(smartLoaded) return;
    smartCache=readLS();
    try{
      if(typeof sb!=='undefined'){
        const r=await sb.from('project_contract_smart').select('*');
        if(!r.error && Array.isArray(r.data)){
          r.data.forEach(x=>{smartCache[String(x.project_id)]=mergeSmart(x.payload||{});});
          writeLS();
        }
      }
    }catch(e){/* fallback to localStorage */}
    smartLoaded=true;
  }
  async function persistSmart(projectId, payload){
    const clean=mergeSmart(payload); clean.updated_at=new Date().toISOString();
    smartCache[String(projectId)]=clean; writeLS();
    try{
      if(typeof sb!=='undefined'){
        const row={project_id:Number(projectId), payload:clean, updated_at:new Date().toISOString()};
        const r=await sb.from('project_contract_smart').upsert(row,{onConflict:'project_id'});
        if(r.error) console.warn('project_contract_smart fallback localStorage:',r.error.message);
      }
    }catch(e){console.warn(e.message||e)}
    return clean;
  }
  function getSmart(projectId){return mergeSmart(smartCache[String(projectId)]||{})}
  function projectById(id){return (window.data?.projects||data?.projects||[]).find(p=>String(p.id)===String(id))}
  function ensureAnnualSelect(){
    const s=el('csAnnualSelect'); if(!s || s.options.length) return;
    s.innerHTML=ANNUAL_OPTIONS.map(x=>`<option value="${h(x)}">${h(x)}</option>`).join('');
  }
  window.onAnnualServiceSelect=function(){
    const val=el('csAnnualSelect')?.value||'';
    if(val==='خدمة أخرى') el('csAnnualCustom')?.focus();
  };
  window.contractSmartTab=function(tab,btn){
    ['main','annual','report'].forEach(t=>el('contractSmart'+t.charAt(0).toUpperCase()+t.slice(1)+'Tab')?.classList.add('hidden'));
    el('contractSmart'+tab.charAt(0).toUpperCase()+tab.slice(1)+'Tab')?.classList.remove('hidden');
    document.querySelectorAll('.contract-smart-tabs button').forEach(b=>b.classList.remove('active'));
    btn?.classList.add('active');
    if(tab==='report') renderClientReportTable();
  };
  window.contractSmartBackdrop=function(e){ if(e.target && e.target.id==='contractSmartModal') closeContractSmartModal(); };
  window.closeContractSmartModal=function(){ el('contractSmartModal')?.classList.add('hidden'); document.body.style.overflow=''; currentSmartProjectId=null; };
  window.openContractSmartModal=async function(projectId, mode){
    await loadSmart(); ensureAnnualSelect();
    currentSmartProjectId=String(projectId); currentSmartReadonly=(mode==='view');
    const p=projectById(projectId); const d=getSmart(projectId);
    el('contractSmartTitle').textContent=(mode==='view'?'عرض العقد: ':'تعديل العقد: ')+(p?.name||'');
    el('contractSmartSub').textContent=`بداية العقد: ${p?.contract_start||'-'} | نهاية العقد: ${p?.contract_end||'-'}`;
    el('contractSmartProjectId').value=projectId;
    el('contractSmartSaveBtn').style.display=currentSmartReadonly?'none':'';
    el('csAssocName').value=d.association.name||''; el('csAssocManager').value=d.association.manager||''; el('csAssocPhone').value=d.association.phone||'';
    ['csAssocName','csAssocManager','csAssocPhone','csAnnualSelect','csAnnualCustom','csAnnualVisits'].forEach(id=>{if(el(id)) el(id).disabled=currentSmartReadonly;});
    renderCoreServices(d); renderAnnualServices(d); renderClientReportTable(d);
    document.querySelector('.contract-smart-tabs button')?.click();
    el('contractSmartModal')?.classList.remove('hidden'); document.body.style.overflow='hidden';
  };
  function renderCoreServices(d){
    const box=el('contractCoreServices'); if(!box) return;
    box.innerHTML=CORE_SERVICES.map(s=>{
      const x=d.core[s.key]||{}; const visits=Number(x.visits)||0; const done=Array.isArray(x.done)?x.done:[];
      const boxes=Array.from({length:visits},(_,i)=>`<button type="button" class="visit-chip ${done.includes(i+1)?'done':''}" ${currentSmartReadonly?'disabled':''} onclick="toggleCoreVisit('${s.key}',${i+1})">زيارة ${i+1}</button>`).join('') || '<small class="muted">حدد عدد الزيارات أولًا</small>';
      const alert=(x.onUs && !String(x.attachment||'').trim())?'<div class="smart-alert danger">تنبيه: هذه الخدمة علينا ولا يوجد مرفق عقد مسجل.</div>':'';
      return `<div class="core-service-card" data-core="${s.key}">
        <h3>${h(s.name)} <span class="badge ${x.onUs?'green':'gray'}">${x.onUs?'علينا':'ليست علينا'}</span></h3>
        <label class="toggle-row"><input type="checkbox" data-field="onUs" ${x.onUs?'checked':''} ${currentSmartReadonly?'disabled':''} onchange="renderCoreServiceVisitsFromInputs('${s.key}')"> الخدمة علينا</label>
        <label>عدد الزيارات</label><input type="number" min="0" data-field="visits" value="${h(visits)}" ${currentSmartReadonly?'disabled':''} onchange="renderCoreServiceVisitsFromInputs('${s.key}')">
        <label>مربعات الزيارات</label><div class="visit-boxes" data-visits="${s.key}">${boxes}</div>
        <label>اسم الشركة</label><input data-field="company" value="${h(x.company||'')}" placeholder="اسم شركة الصيانة / المورد" ${currentSmartReadonly?'disabled':''}>
        <label>رقم الشركة</label><input data-field="phone" value="${h(x.phone||'')}" placeholder="05xxxxxxxx" ${currentSmartReadonly?'disabled':''}>
        <label>مرفق العقد</label><input data-field="attachment" value="${h(x.attachment||'')}" placeholder="رابط العقد أو اسم الملف" ${currentSmartReadonly?'disabled':''}>
        <label>ملاحظات</label><textarea data-field="notes" ${currentSmartReadonly?'disabled':''}>${h(x.notes||'')}</textarea>
        ${alert}
      </div>`;
    }).join('');
  }
  function collectSmartFromModal(){
    const d=defaultSmart();
    d.association={name:el('csAssocName')?.value||'', manager:el('csAssocManager')?.value||'', phone:el('csAssocPhone')?.value||''};
    CORE_SERVICES.forEach(s=>{
      const card=document.querySelector(`[data-core="${s.key}"]`); const old=getSmart(currentSmartProjectId).core[s.key]||{};
      const visits=Number(card?.querySelector('[data-field="visits"]')?.value||0);
      const done=(Array.isArray(old.done)?old.done:[]).filter(n=>n<=visits);
      d.core[s.key]={
        onUs:!!card?.querySelector('[data-field="onUs"]')?.checked,
        visits,
        done,
        company:card?.querySelector('[data-field="company"]')?.value||'',
        phone:card?.querySelector('[data-field="phone"]')?.value||'',
        attachment:card?.querySelector('[data-field="attachment"]')?.value||'',
        notes:card?.querySelector('[data-field="notes"]')?.value||''
      };
    });
    d.annual=getSmart(currentSmartProjectId).annual||[];
    return d;
  }
  window.renderCoreServiceVisitsFromInputs=function(key){
    const d=collectSmartFromModal();
    smartCache[String(currentSmartProjectId)]=d;
    renderCoreServices(d);
  };
  window.toggleCoreVisit=function(key,no){
    if(currentSmartReadonly) return;
    const d=collectSmartFromModal();
    const arr=d.core[key].done||[]; const i=arr.indexOf(no); if(i>=0) arr.splice(i,1); else arr.push(no);
    d.core[key].done=arr.sort((a,b)=>a-b); smartCache[String(currentSmartProjectId)]=d; writeLS(); renderCoreServices(d); renderClientReportTable(d);
  };
  window.addContractAnnualService=function(){
    if(currentSmartReadonly) return;
    const d=collectSmartFromModal();
    let name=el('csAnnualSelect')?.value||''; const custom=(el('csAnnualCustom')?.value||'').trim(); if(name==='خدمة أخرى' || custom) name=custom||name;
    const visits=Math.max(1, Number(el('csAnnualVisits')?.value||1));
    if(!name) return msg ? msg('اختر أو اكتب اسم الخدمة','err') : alert('اختر أو اكتب اسم الخدمة');
    d.annual.push({id:'a'+Date.now()+Math.random().toString(16).slice(2), name, visits, done:[], notes:''});
    smartCache[String(currentSmartProjectId)]=d; writeLS(); if(el('csAnnualCustom')) el('csAnnualCustom').value=''; if(el('csAnnualVisits')) el('csAnnualVisits').value=1;
    renderAnnualServices(d); renderClientReportTable(d);
  };
  function renderAnnualServices(d){
    const body=el('csAnnualBody'); if(!body) return;
    const rows=d.annual||[];
    body.innerHTML=rows.map(a=>{
      const done=Array.isArray(a.done)?a.done:[]; const visits=Number(a.visits)||1; const boxes=Array.from({length:visits},(_,i)=>`<button type="button" class="visit-chip ${done.includes(i+1)?'done':''}" ${currentSmartReadonly?'disabled':''} onclick="toggleAnnualVisit('${a.id}',${i+1})">${i+1}</button>`).join('');
      return `<tr><td><b>${h(a.name)}</b></td><td>${visits}</td><td><div class="visit-boxes">${boxes}</div></td><td>${done.length}</td><td>${Math.max(0,visits-done.length)}</td><td>${currentSmartReadonly?'-':`<button class="danger" onclick="deleteAnnualService('${a.id}')">حذف</button>`}</td></tr>`;
    }).join('') || '<tr><td colspan="6">لا توجد خدمات سنوية بعد</td></tr>';
  }
  window.toggleAnnualVisit=function(id,no){
    if(currentSmartReadonly) return;
    const d=collectSmartFromModal(); const a=(d.annual||[]).find(x=>String(x.id)===String(id)); if(!a) return;
    if(!Array.isArray(a.done)) a.done=[]; const i=a.done.indexOf(no); if(i>=0) a.done.splice(i,1); else a.done.push(no); a.done=a.done.sort((x,y)=>x-y);
    smartCache[String(currentSmartProjectId)]=d; writeLS(); renderAnnualServices(d); renderClientReportTable(d);
  };
  window.deleteAnnualService=function(id){
    if(currentSmartReadonly) return;
    if(!confirm('حذف هذه الخدمة السنوية؟')) return;
    const d=collectSmartFromModal(); d.annual=(d.annual||[]).filter(a=>String(a.id)!==String(id)); smartCache[String(currentSmartProjectId)]=d; writeLS(); renderAnnualServices(d); renderClientReportTable(d);
  };
  function contractAlerts(d){
    const out=[]; CORE_SERVICES.forEach(s=>{const x=d.core[s.key]||{}; if(x.onUs && !String(x.attachment||'').trim()) out.push(`الخدمة ${s.name} علينا ولا يوجد مرفق عقد.`);}); return out;
  }
  function reportRows(d){
    return (d.annual||[]).map(a=>{const visits=Number(a.visits)||0; const done=(a.done||[]).length; return {name:a.name, visits, done, remain:Math.max(0,visits-done), status:done>=visits?'مكتمل':(done>0?'جاري':'لم يبدأ')};});
  }
  window.renderClientReportTable=function(optional){
    const d=optional||getSmart(currentSmartProjectId); const p=projectById(currentSmartProjectId); const alerts=contractAlerts(d);
    const rows=reportRows(d);
    const html=`${alerts.map(a=>`<div class="smart-alert danger">${h(a)}</div>`).join('')}
      <div class="smart-box"><b>المشروع:</b> ${h(p?.name||'-')} &nbsp; | &nbsp; <b>الجمعية:</b> ${h(d.association.name||'-')} &nbsp; | &nbsp; <b>مدير الجمعية:</b> ${h(d.association.manager||'-')} &nbsp; | &nbsp; <b>الجوال:</b> ${h(d.association.phone||'-')}</div>
      <table class="smart-report-table"><thead><tr><th>اسم الخدمة</th><th>عدد الزيارات السنوية</th><th>المنفذ منها</th><th>المتبقي</th><th>الحالة</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${h(r.name)}</td><td>${r.visits}</td><td>${r.done}</td><td>${r.remain}</td><td>${h(r.status)}</td></tr>`).join('')||'<tr><td colspan="5">لا توجد خدمات سنوية مسجلة</td></tr>'}</tbody></table>`;
    if(el('csClientReportBox')) el('csClientReportBox').innerHTML=html;
    return html;
  };
  window.copyContractClientTable=async function(){
    const html=renderClientReportTable(); const text=document.createElement('div'); text.innerHTML=html;
    try{await navigator.clipboard.writeText(text.innerText); if(typeof msg==='function') msg('تم نسخ جدول تقرير العميل');}
    catch(e){alert(text.innerText)}
  };
  window.printContractClientTable=function(){
    const p=projectById(currentSmartProjectId); const html=renderClientReportTable();
    const w=window.open('','_blank'); w.document.write(`<html dir="rtl"><head><title>تقرير خدمات العقد - ${h(p?.name||'')}</title><style>body{font-family:Tahoma,Arial;padding:24px;color:#10231d}h1{color:#0A4033}.smart-box{border:1px solid #dce8e4;border-radius:14px;padding:12px;margin:12px 0}.smart-report-table{width:100%;border-collapse:collapse}.smart-report-table th,.smart-report-table td{border:1px solid #dce8e4;padding:10px;text-align:center}.smart-report-table th{background:#0A4033;color:#fff}.smart-report-table td:first-child,.smart-report-table th:first-child{text-align:right}.smart-alert{background:#fdecec;border:1px solid #f2b7b7;color:#922;border-radius:12px;padding:10px;margin:8px 0}</style></head><body><h1>تقرير خدمات العقد</h1>${html}</body></html>`); w.document.close(); setTimeout(()=>w.print(),400);
  };
  window.saveContractSmartModal=async function(){
    if(!currentSmartProjectId) return;
    const d=collectSmartFromModal();
    await persistSmart(currentSmartProjectId,d);
    if(typeof msg==='function') msg('تم حفظ بيانات العقد والخدمات السنوية');
    renderContracts();
  };
  const oldRenderContractsV299=window.renderContracts;
  window.renderContracts=function(){
    const body=el('contractsBody');
    if(!body || typeof contractInfo!=='function') return oldRenderContractsV299&&oldRenderContractsV299();
    const q=(el('contractSearch')?.value||'').trim();
    const st=el('contractFilterStatus')?.value||'';
    let rows=[...(data.projects||[])];
    if(q) rows=rows.filter(p=>String(p.name||'').includes(q));
    if(st) rows=rows.filter(p=>contractInfo(p).key===st);
    rows.sort((a,b)=>{ const da=daysLeft(a.contract_end); const db=daysLeft(b.contract_end); return (da??999999)-(db??999999); });
    body.innerHTML=rows.map(p=>{ const c=contractInfo(p); return `<tr><td><b>${h(p.name)}</b></td><td>${p.buildings_count||0}</td><td>${p.units_count||0}</td><td>${h(isoDate(p.contract_start)||'-')}</td><td>${h(isoDate(p.contract_end)||'-')}</td><td>${c.days}</td><td><span class="badge ${c.cls}">${c.text}</span></td><td class="row-actions"><button class="light" onclick="openContractSmartModal(${p.id},'view')">عرض</button><button onclick="openContractSmartModal(${p.id},'edit')">تعديل</button></td></tr>`; }).join('') || '<tr><td colspan="8">لا توجد بيانات</td></tr>';
    if(el('contractsActiveCount')) el('contractsActiveCount').textContent=(data.projects||[]).filter(p=>contractInfo(p).key==='active').length;
    if(el('contractsSoonCount')) el('contractsSoonCount').textContent=(data.projects||[]).filter(p=>contractInfo(p).key==='soon').length;
    if(el('contractsExpiredCount')) el('contractsExpiredCount').textContent=(data.projects||[]).filter(p=>contractInfo(p).key==='expired').length;
    if(el('contractsMissingCount')) el('contractsMissingCount').textContent=(data.projects||[]).filter(p=>contractInfo(p).key==='missing').length;
  };
  const oldRefreshV299=window.refreshAll;
  if(typeof oldRefreshV299==='function') window.refreshAll=async function(){ const r=await oldRefreshV299.apply(this,arguments); await loadSmart(); return r; };
  window.__contractSmartV299={loadSmart,getSmart,persistSmart};
})();

/* ===== V305: No loading screen + supervisor fast mode ===== */
(function(){
  const FIX_VERSION = 'v305-no-loader-fast-mode';
  function hardRemoveLoader(){
    try{
      document.querySelectorAll('#tasneefSplashV154,#tasneefLoadingV154,.tasneef-splash-v154,.tasneef-loading-v154').forEach(function(n){ n.remove(); });
      if(!document.getElementById('tasneefNoLoaderV305')){
        const st=document.createElement('style'); st.id='tasneefNoLoaderV305';
        st.textContent='#tasneefSplashV154,#tasneefLoadingV154,.tasneef-splash-v154,.tasneef-loading-v154{display:none!important;opacity:0!important;visibility:hidden!important;pointer-events:none!important} *{scroll-behavior:auto!important}';
        document.head.appendChild(st);
      }
    }catch(e){}
  }
  window.showTasneefLoading=function(){};
  window.hideTasneefLoading=function(){};
  window.withTasneefLoading=async function(promiseOrFn){ return await (typeof promiseOrFn==='function'?promiseOrFn():promiseOrFn); };
  hardRemoveLoader();
  document.addEventListener('DOMContentLoaded', hardRemoveLoader);
  window.addEventListener('load', hardRemoveLoader);
  setInterval(hardRemoveLoader, 1200);

  function normId(v){ return v==null ? '' : String(v); }
  async function safeData(q){ try{ const r=await q; if(r.error){ console.warn('[Tasneef '+FIX_VERSION+']', r.error.message); return []; } return r.data||[]; }catch(e){ console.warn('[Tasneef '+FIX_VERSION+']', e?.message||e); return []; } }
  function dateBack(days){ const d=new Date(); d.setDate(d.getDate()-days); return d.toISOString().slice(0,10); }

  window.initSupervisor = async function(){
    const u = requireRole('supervisor'); if(!u) return;
    hardRemoveLoader();
    const since = dateBack(45);
    const [users, projects, workers, assignments, logs, tickets] = await Promise.all([
      safeData(sb.from('app_users').select('*').order('id')),
      safeData(sb.from('projects').select('*').eq('supervisor_id', u.id).order('name')),
      safeData(sb.from('workers').select('*').order('name')),
      safeData(sb.from('worker_project_assignments').select('*').eq('is_active', true).order('id')),
      safeData(sb.from('time_logs').select('*').gte('log_date', since).order('check_in',{ascending:false}).limit(250)),
      safeData(sb.from('tickets').select('*').order('created_at',{ascending:false}).limit(250))
    ]);
    data.users = users;
    data.supervisors = users.filter(x=>x.role==='supervisor' && x.is_active!==false);
    data.technicians = users.filter(x=>x.role==='technician' && x.is_active!==false);
    data.projects = projects;
    data.workerAssignments = assignments;
    const supProjectIds = new Set(projects.map(p=>normId(p.id)));
    data.workers = workers.filter(function(w){
      if(normId(w.app_supervisor_id||w.supervisor_id)===normId(u.id)) return true;
      const directProject = normId((typeof workerProjectId==='function') ? workerProjectId(w) : (w.project_id||w.assigned_project_id));
      if(directProject && supProjectIds.has(directProject)) return true;
      return assignments.some(a=>normId(a.worker_id)===normId(w.id) && supProjectIds.has(normId(a.project_id)));
    });
    data.logs = logs.filter(l=>normId(l.supervisor_id)===normId(u.id) || supProjectIds.has(normId(l.project_id)));
    data.tickets = tickets.filter(t=>normId(t.supervisor_id)===normId(u.id) || normId(t.created_by)===normId(u.id) || supProjectIds.has(normId(t.project_id)));
    if($('supTitle')) $('supTitle').textContent = `لوحة المشرف - ${u.full_name||u.username||''}`;
    if(typeof fillSelect==='function'){
      fillSelect('logProject', data.projects, 'name', 'اختر المشروع');
      fillSelect('attendanceProject', data.projects, 'name', 'اختر المشروع');
      fillSelect('ticketProject', data.projects, 'name', 'اختر المشروع');
      fillSelect('supTicketFilterProject', data.projects, 'name', 'كل المشاريع');
    }
    if($('logDate')) $('logDate').value=today();
    if($('attendanceDate')) $('attendanceDate').value=today();
    try{ renderSupervisorAttendanceList(); }catch(e){ console.warn(e); }
    try{ renderTimeLogs(); }catch(e){ console.warn(e); }
    try{ renderTickets(); }catch(e){ console.warn(e); }
    hardRemoveLoader();
  };
  try{ initSupervisor = window.initSupervisor; }catch(e){}

  // تحميل المخزون فقط عند فتح تبويب المخزون وليس مع بداية لوحة المشرف.
  const oldShowSupervisorWindow = window.showSupervisorWindow;
  window.showSupervisorWindow = function(id, btn){
    if(typeof oldShowSupervisorWindow === 'function') oldShowSupervisorWindow(id, btn);
    if(id==='supInventory' && typeof supervisorInventoryLoad==='function') setTimeout(supervisorInventoryLoad, 50);
  };
  try{ showSupervisorWindow = window.showSupervisorWindow; }catch(e){}

  // منع إعادة تحميل ثقيلة في صفحة المشرف.
  const oldRefreshAll = window.refreshAll;
  window.refreshAll = async function(){
    if(document.getElementById('supTitle')) return window.initSupervisor();
    if(typeof oldRefreshAll === 'function') return oldRefreshAll();
  };
  try{ refreshAll = window.refreshAll; }catch(e){}

  console.log('Tasneef '+FIX_VERSION+' loaded');
})();


/* ===== V306: remove expenses & inventory module, keep app lightweight ===== */
(function(){
  if(window.__tasneefFinanceProExclusive) return;
  const FIX_VERSION='v306-remove-finance-inventory';
  function removeFinanceInventoryUi(){
    try{
      document.querySelectorAll('button[onclick*=\"financeDashboard\"], #financeDashboard, #supInventory, button[onclick*=\"supInventory\"]').forEach(n=>n.remove());
      document.querySelectorAll('[data-perm=\"can_expenses_inventory\"],[data-perm=\"can_inventory_requests\"],[data-perm=\"can_manage_inventory\"]').forEach(function(inp){
        const wrap=inp.closest('label')||inp; wrap.remove();
      });
    }catch(e){}
  }
  removeFinanceInventoryUi();
  document.addEventListener('DOMContentLoaded', removeFinanceInventoryUi);
  window.addEventListener('load', removeFinanceInventoryUi);
  const oldShowPage=window.showPage;
  window.showPage=function(id,btn){
    if(id==='financeDashboard'){
      if(typeof msg==='function') msg('تم حذف قسم المصروفات والمخزون من هذه النسخة');
      return;
    }
    return typeof oldShowPage==='function'?oldShowPage(id,btn):undefined;
  };
  try{ showPage=window.showPage; }catch(e){}
  const oldShowSupervisorWindow=window.showSupervisorWindow;
  window.showSupervisorWindow=function(id,btn){
    if(id==='supInventory') return;
    return typeof oldShowSupervisorWindow==='function'?oldShowSupervisorWindow(id,btn):undefined;
  };
  try{ showSupervisorWindow=window.showSupervisorWindow; }catch(e){}
  window.supervisorInventoryLoad=async function(){ return []; };
  window.financeLoadAll=async function(){ return; };
  window.financeRenderAll=function(){};
  console.log('Tasneef '+FIX_VERSION+' loaded');
})();


/* V310: منع ظهور حضور وانصراف الفنيين داخل التسجيلات اليومية */
(function(){
  function isTechAttendanceLogV310(l){
    return String((l && l.visit_type) || '') === 'technician_attendance' || String((l && l.notes) || '').startsWith('فني:');
  }
  window.__tasneefHideTechAttendanceFromDailyV310 = true;
  const oldFilterLogsV310 = window.filterLogs;
  if(typeof oldFilterLogsV310 === 'function'){
    window.filterLogs = function(){
      const rows = oldFilterLogsV310.apply(this, arguments) || [];
      return rows.filter(l=>!isTechAttendanceLogV310(l));
    };
  }
})();

/* ===== TASNEEF V330 - Monthly previous months + editable percent/workers + export/import buttons fix ===== */
(function(){
  'use strict';
  const FIX_VERSION='v330-monthly-export-import-fix';
  const $ = id => document.getElementById(id);
  const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const S = v => String(v ?? '').trim();
  const N = v => { const n=Number(String(v ?? 0).replace(/,/g,'')); return Number.isFinite(n)?n:0; };
  const sid = v => v==null ? '' : String(v);
  const dataObj = () => window.data || (typeof data !== 'undefined' ? data : {});
  const todayMonth = () => new Date().toISOString().slice(0,7);
  const monthVal = () => $('monthlyMonth')?.value || todayMonth();
  const ymd = d => d.toISOString().slice(0,10);
  function monthRange(m){
    m = m || monthVal();
    const start = m + '-01';
    const [y,mo] = String(m).split('-').map(Number);
    const e = new Date(y, mo, 1);
    return {month:m,start,end:ymd(e)};
  }
  function inMonth(v,m){
    const s=String(v||'');
    if(!s) return false;
    if(s.slice(0,7)===m) return true;
    try{return new Date(s).toISOString().slice(0,7)===m;}catch(e){return false;}
  }
  function minsBetweenSafe(a,b){
    if(typeof window.minutesBetween==='function') return N(window.minutesBetween(a,b));
    if(!a||!b) return 0;
    const x=new Date(a), y=new Date(b), d=(y-x)/60000;
    return Number.isFinite(d)&&d>0?Math.round(d):0;
  }
  function actualMins(l){ return N(l.duration_minutes)||minsBetweenSafe(l.check_in,l.check_out)||0; }
  function minsTxt(m){ return (typeof window.minsToText==='function') ? window.minsToText(N(m)) : (Math.floor(N(m)/60)+' ساعة '+(N(m)%60)+' دقيقة'); }
  function supName(id){ try{return window.supervisorName(id)||'-';}catch(e){return (dataObj().users||[]).find(u=>sid(u.id)===sid(id))?.full_name || '-';} }
  function projName(id){ try{return window.projectName(id)||'-';}catch(e){return (dataObj().projects||[]).find(p=>sid(p.id)===sid(id))?.name || '-';} }
  function projectSup(pid){ return (dataObj().projects||[]).find(p=>sid(p.id)===sid(pid))?.supervisor_id || ''; }
  const LS='tasneef_monthly_manual_v330';
  function readManual(){ try{return JSON.parse(localStorage.getItem(LS)||'{}')||{};}catch(e){return {};} }
  function writeManual(o){ try{localStorage.setItem(LS, JSON.stringify(o||{}));}catch(e){} }
  function key(m,s,p){ return [m,sid(s),sid(p)].join('|'); }
  function getManual(m,s,p){ return readManual()[key(m,s,p)] || {}; }
  function setManual(m,s,p,patch){ const o=readManual(); const k=key(m,s,p); o[k]=Object.assign({}, o[k]||{}, patch||{}); writeManual(o); }
  function workersForProject(pid){
    const names=new Set();
    (dataObj().workers||[]).forEach(w=>{ if(sid(w.project_id)===sid(pid) && String(w.status||'active')!=='inactive') names.add(w.name); });
    (dataObj().workerAssignments||[]).forEach(a=>{ if(a && a.is_active!==false && sid(a.project_id)===sid(pid)){ const w=(dataObj().workers||[]).find(x=>sid(x.id)===sid(a.worker_id)); if(w?.name) names.add(w.name); }});
    return [...names].filter(Boolean).join('، ') || '-';
  }
  async function fetchSelectedMonthLogsV330(month){
    month=month||monthVal();
    const r=monthRange(month);
    const results=[];
    async function q1(){ try{ const res=await sb.from('time_logs').select('*').gte('log_date',r.start).lt('log_date',r.end).order('id',{ascending:false}).limit(10000); if(!res.error) results.push(...(res.data||[])); }catch(e){} }
    async function q2(){ try{ const res=await sb.from('time_logs').select('*').gte('check_in',r.start+'T00:00:00').lt('check_in',r.end+'T00:00:00').order('id',{ascending:false}).limit(10000); if(!res.error) results.push(...(res.data||[])); }catch(e){} }
    await Promise.all([q1(), q2()]);
    const map=new Map();
    (dataObj().logs||[]).filter(l=>!inMonth(l.log_date||l.check_in||l.created_at,month)).forEach(l=>{ if(l?.id!=null) map.set('old_'+l.id,l); });
    results.forEach(l=>{ if(l?.id!=null) map.set('new_'+l.id,l); });
    if(!window.data) window.data={};
    window.data.logs=[...map.values()].map(x=>{ const y=Object.assign({},x); return y; });
    window.data.__monthlyLoadedV330=month;
    return results;
  }
  function monthlyRowsV330(){
    const month=monthVal(); const selected=$('monthlySupervisor')?.value||'';
    const map=new Map();
    (dataObj().logs||[]).filter(l=>inMonth(l.log_date||l.check_in||l.created_at,month)).forEach(l=>{
      const pid=l.project_id; if(!pid) return;
      const sup=l.supervisor_id || projectSup(pid) || '';
      if(selected && sid(sup)!==sid(selected)) return;
      const k=sid(sup)+'|'+sid(pid);
      if(!map.has(k)) map.set(k,{s:sup,p:pid,a:0,c:0,t:0});
      const r=map.get(k); r.a+=actualMins(l); r.c+=1; r.t+=N(l.travel_minutes);
    });
    const rows=[...map.values()];
    const totals={}; rows.forEach(r=>{ totals[sid(r.s)] = (totals[sid(r.s)]||0) + N(r.a); });
    return rows.map(r=>{
      const manual=getManual(month,r.s,r.p);
      const calcPercent=(totals[sid(r.s)] ? r.a/totals[sid(r.s)]*100 : 0);
      const percent = manual.percent!==undefined && manual.percent!=='' ? N(manual.percent) : calcPercent;
      const workers = manual.workers!==undefined ? manual.workers : workersForProject(r.p);
      return Object.assign({}, r, {workers, percent, calcPercent});
    }).sort((a,b)=>String(supName(a.s)).localeCompare(String(supName(b.s)),'ar') || String(projName(a.p)).localeCompare(String(projName(b.p)),'ar'));
  }
  async function renderMonthlyV330(){
    const body=$('monthlyBody'); if(!body) return;
    const month=monthVal();
    body.innerHTML='<tr><td colspan="8">جاري تحميل بيانات الشهر المحدد...</td></tr>';
    await fetchSelectedMonthLogsV330(month);
    const table=body.closest('table');
    if(table?.tHead) table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>عدد السجلات</th><th>الساعات الفعلية</th><th>وقت الانتقال</th><th>نسبة العمل %</th><th>إجراء</th></tr>';
    const rows=monthlyRowsV330();
    if(!rows.length){
      body.innerHTML='<tr><td colspan="8">لا توجد بيانات لهذا الشهر. تأكد من اختيار الشهر الصحيح ثم اضغط تحديث البيانات.</td></tr>';
      if($('monthlySummary')) $('monthlySummary').innerHTML='<div class="kpi"><small>إجمالي الوقت</small><b>0 دقيقة</b></div><div class="kpi"><small>المشرفين</small><b>0</b></div><div class="kpi"><small>المشاريع</small><b>0</b></div>';
      return;
    }
    body.innerHTML=rows.map((r,i)=>{
      const k=key(month,r.s,r.p);
      return `<tr data-monthly-key="${esc(k)}" data-s="${esc(r.s)}" data-p="${esc(r.p)}">
        <td>${esc(supName(r.s))}</td>
        <td>${esc(projName(r.p))}</td>
        <td><input class="monthly-workers-edit-v330" value="${esc(r.workers||'')}" placeholder="أسماء العمال" style="min-width:240px"></td>
        <td>${N(r.c)}</td>
        <td>${esc(minsTxt(r.a))}</td>
        <td>${N(r.t)} دقيقة</td>
        <td><input class="monthly-percent-edit-v330" type="number" step="0.1" min="0" max="300" value="${Number(r.percent||0).toFixed(1).replace(/\.0$/,'')}" style="width:105px;text-align:center"></td>
        <td><button class="light" onclick="saveMonthlyManualV330('${esc(month)}','${esc(r.s)}','${esc(r.p)}')">حفظ</button></td>
      </tr>`;
    }).join('');
    const total=rows.reduce((a,r)=>a+N(r.a),0), supCount=new Set(rows.map(r=>sid(r.s))).size;
    if($('monthlySummary')) $('monthlySummary').innerHTML=`<div class="kpi"><small>الشهر</small><b>${esc(month)}</b></div><div class="kpi"><small>إجمالي الساعات الفعلية</small><b>${esc(minsTxt(total))}</b></div><div class="kpi"><small>عدد المشرفين</small><b>${supCount}</b></div><div class="kpi"><small>عدد المشاريع</small><b>${rows.length}</b></div><div class="kpi"><small>ملاحظة</small><b style="font-size:14px">يمكن تعديل العمال والنسبة يدويًا</b></div>`;
  }
  window.saveMonthlyManualV330=function(month,s,p){
    const tr=[...document.querySelectorAll('#monthlyBody tr')].find(x=>sid(x.dataset.s)===sid(s)&&sid(x.dataset.p)===sid(p));
    if(!tr) return;
    const workers=tr.querySelector('.monthly-workers-edit-v330')?.value||'';
    const percent=tr.querySelector('.monthly-percent-edit-v330')?.value||'';
    setManual(month,s,p,{workers, percent});
    if(typeof msg==='function') msg('تم حفظ تعديل الأوقات الشهرية'); else alert('تم الحفظ');
  };
  window.renderMonthly=renderMonthlyV330;
  window.monthlyRowsV60=monthlyRowsV330;
  window.monthlyRowsV284=monthlyRowsV330;
  window.monthlyBaseRowsV59=monthlyRowsV330;
  window.monthlyReportRowsV58=monthlyRowsV330;
  window.exportMonthlyCSV=async function(){
    await renderMonthlyV330();
    const rows=monthlyRowsV330();
    const headers=['الشهر','المشرف','المشروع','أسماء العمال','عدد السجلات','الساعات الفعلية','وقت الانتقال','نسبة العمل'];
    const lines=[headers].concat(rows.map(r=>[monthVal(),supName(r.s),projName(r.p),r.workers,N(r.c),minsTxt(r.a),N(r.t)+' دقيقة',String(r.percent).replace('.',',')+'%']));
    downloadFileV330('monthly_times_'+monthVal()+'.csv','\ufeff'+lines.map(row=>row.map(x=>'"'+String(x??'').replace(/"/g,'""')+'"').join(',')).join('\n'),'text/csv;charset=utf-8');
  };
  window.printMonthlyReportV57=async function(){
    await renderMonthlyV330();
    const rows=monthlyRowsV330();
    const tr=rows.map(r=>`<tr><td>${esc(supName(r.s))}</td><td>${esc(projName(r.p))}</td><td>${esc(r.workers)}</td><td>${N(r.c)}</td><td>${esc(minsTxt(r.a))}</td><td>${N(r.t)} دقيقة</td><td>${Number(r.percent||0).toFixed(1)}%</td></tr>`).join('');
    const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>الأوقات الشهرية</title><style>body{font-family:Tahoma,Arial,sans-serif;padding:20px;color:#103d32}h1{color:#0a4033}.head{display:flex;justify-content:space-between;border-bottom:3px solid #0a4033;margin-bottom:14px}.logo{font-weight:900;font-size:22px}table{width:100%;border-collapse:collapse}th{background:#0a4033;color:white}td,th{border:1px solid #dce6e2;padding:8px;text-align:center}tr:nth-child(even)td{background:#f7fbfa}@media print{button{display:none}}</style></head><body><button onclick="print()">طباعة</button><div class="head"><div class="logo">شركة تصنيف لإدارة المرافق</div><div><h1>تقرير الأوقات الشهرية</h1><p>الشهر: ${esc(monthVal())}</p></div></div><table><thead><tr><th>المشرف</th><th>المشروع</th><th>العمال</th><th>عدد السجلات</th><th>الساعات الفعلية</th><th>الانتقال</th><th>النسبة</th></tr></thead><tbody>${tr||'<tr><td colspan="7">لا توجد بيانات</td></tr>'}</tbody></table><script>setTimeout(()=>print(),500)</script></body></html>`;
    const w=window.open('','_blank'); if(w){ w.document.write(html); w.document.close(); }
  };
  function bindMonthly(){
    const m=$('monthlyMonth'), s=$('monthlySupervisor');
    if(m && !m.dataset.v330){ m.dataset.v330='1'; m.addEventListener('change',()=>renderMonthlyV330()); }
    if(s && !s.dataset.v330){ s.dataset.v330='1'; s.addEventListener('change',()=>renderMonthlyV330()); }
  }

  function downloadFileV330(filename, content, type){
    const blob=new Blob([content],{type:type||'application/octet-stream'});
    const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=filename; document.body.appendChild(a); a.click(); a.remove(); setTimeout(()=>URL.revokeObjectURL(a.href),1500);
  }
  window.showExportSlideV223=function(index,btn){
    const slides=[...document.querySelectorAll('.export-slide-v222')];
    const buttons=[...document.querySelectorAll('.export-nav-v223 button')];
    slides.forEach((s,i)=>s.classList.toggle('active',i===Number(index)));
    buttons.forEach((b,i)=>b.classList.toggle('active',i===Number(index)));
    hydrateExportV330();
  };
  function opt(rows,label){ return rows.map(r=>`<option value="${esc(r.id)}">${esc(r[label]||r.name||r.full_name||r.username||r.id)}</option>`).join(''); }
  function hydrateExportV330(){
    const d=dataObj();
    const mon=$('meetingExportMonth'); if(mon && !mon.value) mon.value=$('monthlyMonth')?.value || todayMonth();
    const sup=$('meetingExportSupervisor'); if(sup && !sup.dataset.v330Hydrated){ sup.innerHTML='<option value="">كل المشرفين</option>'+opt(d.supervisors||[], 'full_name'); sup.dataset.v330Hydrated='1'; }
    const pro=$('meetingExportProject'); if(pro && !pro.dataset.v330Hydrated){ pro.innerHTML='<option value="">كل المشاريع</option>'+opt(d.projects||[], 'name'); pro.dataset.v330Hydrated='1'; }
  }
  function csvRows(rows){ return '\ufeff'+rows.map(r=>r.map(x=>'"'+String(x??'').replace(/"/g,'""')+'"').join(',')).join('\n'); }
  window.exportTable=async function(table){
    try{
      const r=await sb.from(table).select('*').limit(10000);
      if(r.error) throw new Error(r.error.message);
      const rows=r.data||[]; if(!rows.length) return (typeof msg==='function'?msg('لا توجد بيانات للتصدير','err'):alert('لا توجد بيانات'));
      const heads=[...new Set(rows.flatMap(o=>Object.keys(o)))];
      downloadFileV330(table+'.csv', csvRows([heads].concat(rows.map(o=>heads.map(h=>o[h])))), 'text/csv;charset=utf-8');
    }catch(e){
      const localMap={app_users:dataObj().users,projects:dataObj().projects,workers:dataObj().workers,attendance:dataObj().attendance,time_logs:dataObj().logs,tickets:dataObj().tickets};
      const rows=localMap[table]||[]; if(!rows.length) return (typeof msg==='function'?msg('تعذر التصدير: '+(e.message||e),'err'):alert(e.message||e));
      const heads=[...new Set(rows.flatMap(o=>Object.keys(o)))];
      downloadFileV330(table+'.csv', csvRows([heads].concat(rows.map(o=>heads.map(h=>o[h])))), 'text/csv;charset=utf-8');
    }
  };
  function xlsXmlSheet(name, rows){
    const safe=v=>String(v??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
    const cols=Math.max(1,...rows.map(r=>r.length));
    return `<Worksheet ss:Name="${safe(name).slice(0,31)}" ss:RightToLeft="1"><Table>${rows.map((r,i)=>`<Row>${Array.from({length:cols},(_,j)=>`<Cell ss:StyleID="${i===0?'Header':'Data'}"><Data ss:Type="String">${safe(r[j]??'')}</Data></Cell>`).join('')}</Row>`).join('')}</Table></Worksheet>`;
  }
  function workbook(sheets){ return `<?xml version="1.0"?><Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet"><Styles><Style ss:ID="Header"><Font ss:Bold="1" ss:Color="#FFFFFF"/><Interior ss:Color="#0A4033" ss:Pattern="Solid"/></Style><Style ss:ID="Data"><Alignment ss:Horizontal="Right" ss:WrapText="1"/></Style></Styles>${sheets.join('')}</Workbook>`; }
  window.downloadImportTemplateV223=function(){
    const sheets=[
      xlsXmlSheet('المشاريع',[['اسم المشروع','المشرف','نوع التشغيل','بداية العقد','نهاية العقد','ملاحظات']]),
      xlsXmlSheet('المشرفون',[['الاسم','اسم المستخدم','كلمة المرور','الحالة']]),
      xlsXmlSheet('العمال',[['اسم العامل','المشرف','المشروع','الجوال','الراتب','الحالة']]),
      xlsXmlSheet('التكتات',[['التاريخ','المشروع','العنوان','الوصف','الحالة']]),
      xlsXmlSheet('السجلات اليومية',[['التاريخ','المشروع','المشرف','دخول','خروج','نوع الزيارة','ملاحظات']])
    ];
    downloadFileV330('tasneef_import_template.xls','\ufeff'+workbook(sheets),'application/vnd.ms-excel;charset=utf-8');
  };
  window.previewMeetingExportV223=function(){
    hydrateExportV330();
    const box=$('meetingExportPreview'); if(!box) return;
    const d=dataObj();
    box.innerHTML=`<div class="msg">معاينة التصدير: المشاريع ${N((d.projects||[]).length)}، المشرفين ${N((d.supervisors||[]).length)}، العمال ${N((d.workers||[]).length)}، السجلات اليومية ${N((d.logs||[]).length)}، الحضور ${N((d.attendance||[]).length)}.</div>`;
  };
  window.exportMeetingExcelV229=async function(btn){
    if(btn) btn.disabled=true;
    try{
      hydrateExportV330();
      const d=dataObj();
      const m=$('meetingExportMonth')?.value || $('monthlyMonth')?.value || todayMonth();
      await fetchSelectedMonthLogsV330(m);
      const sheets=[];
      sheets.push(xlsXmlSheet('الملخص', [['البند','العدد'],['المستخدمون',(d.users||[]).length],['المشاريع',(d.projects||[]).length],['العمال',(d.workers||[]).length],['سجلات الشهر',(d.logs||[]).filter(l=>inMonth(l.log_date||l.check_in,m)).length]]));
      sheets.push(xlsXmlSheet('المشاريع', [['المشروع','المشرف','بداية العقد','نهاية العقد'],...(d.projects||[]).map(p=>[p.name,supName(p.supervisor_id),p.contract_start||p.start_date||'',p.contract_end||p.end_date||''])]));
      sheets.push(xlsXmlSheet('العمال', [['العامل','المشرف','المشروع','الجوال','الحالة'],...(d.workers||[]).map(w=>[w.name,supName(w.supervisor_id||w.app_supervisor_id),projName(w.project_id),w.phone||'',w.status||''])]));
      const logs=(d.logs||[]).filter(l=>inMonth(l.log_date||l.check_in,m));
      sheets.push(xlsXmlSheet('السجلات اليومية', [['التاريخ','المشروع','المشرف','الدخول','الخروج','المدة','ملاحظات'],...logs.map(l=>[l.log_date||String(l.check_in||'').slice(0,10),projName(l.project_id),supName(l.supervisor_id),l.check_in||'',l.check_out||'',minsTxt(actualMins(l)),l.notes||''])]));
      sheets.push(xlsXmlSheet('الأوقات الشهرية', [['الشهر','المشرف','المشروع','العمال','عدد السجلات','الساعات الفعلية','النسبة'],...monthlyRowsV330().map(r=>[m,supName(r.s),projName(r.p),r.workers,r.c,minsTxt(r.a),Number(r.percent||0).toFixed(1)+'%'])]));
      downloadFileV330('tasneef_meeting_export_'+m+'.xls','\ufeff'+workbook(sheets),'application/vnd.ms-excel;charset=utf-8');
    } finally { if(btn) btn.disabled=false; }
  };
  function boot(){ bindMonthly(); hydrateExportV330(); if($('monthlyBody')) renderMonthlyV330(); }
  document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,700));
  window.addEventListener('load',()=>setTimeout(boot,1200));
  console.log('Tasneef '+FIX_VERSION+' loaded');
})();

/* ===== TASNEEF V331 - Monthly safe previous month + manual edit/add/delete rows ===== */
(function(){
  'use strict';
  const VERSION='v331-monthly-safe-manual';
  const $=id=>document.getElementById(id);
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const S=v=>String(v??'').trim();
  const sid=v=>v==null?'':String(v);
  const N=v=>{const n=Number(String(v??0).replace(/,/g,''));return Number.isFinite(n)?n:0;};
  const d=()=>window.data||{};
  const todayMonth=()=>new Date().toISOString().slice(0,7);
  const monthVal=()=>$('monthlyMonth')?.value||todayMonth();
  const LS='tasneef_monthly_manual_v331';
  const OLD_LS='tasneef_monthly_manual_v330';
  function getStore(){
    try{
      let v=JSON.parse(localStorage.getItem(LS)||'null');
      if(!v){ v={overrides:{}, rows:[]}; const old=JSON.parse(localStorage.getItem(OLD_LS)||'{}'); v.overrides=old||{}; localStorage.setItem(LS,JSON.stringify(v)); }
      v.overrides=v.overrides||{}; v.rows=Array.isArray(v.rows)?v.rows:[]; return v;
    }catch(e){return {overrides:{}, rows:[]};}
  }
  function setStore(v){try{localStorage.setItem(LS,JSON.stringify(v||{overrides:{},rows:[]}));}catch(e){}}
  function key(m,s,p){return [m,sid(s),sid(p)].join('|');}
  function override(m,s,p){return getStore().overrides[key(m,s,p)]||{};}
  function saveOverride(m,s,p,patch){const st=getStore(), k=key(m,s,p); st.overrides[k]=Object.assign({},st.overrides[k]||{},patch||{}); setStore(st);}
  function deleteOverride(m,s,p){const st=getStore(); delete st.overrides[key(m,s,p)]; st.rows=(st.rows||[]).filter(r=>!(r.month===m && sid(r.s)===sid(s) && sid(r.p)===sid(p))); setStore(st);}
  function supName(id){try{return window.supervisorName(id)||'-';}catch(e){return (d().users||[]).find(u=>sid(u.id)===sid(id))?.full_name||'-';}}
  function projName(id){try{return window.projectName(id)||'-';}catch(e){return (d().projects||[]).find(p=>sid(p.id)===sid(id))?.name||'-';}}
  function projectSup(pid){return (d().projects||[]).find(p=>sid(p.id)===sid(pid))?.supervisor_id||'';}
  function minsBetweenSafe(a,b){try{if(typeof window.minutesBetween==='function')return N(window.minutesBetween(a,b));}catch(e){} if(!a||!b)return 0; const x=new Date(a),y=new Date(b),z=(y-x)/60000; return Number.isFinite(z)&&z>0?Math.round(z):0;}
  function actualMins(l){return N(l.duration_minutes)||minsBetweenSafe(l.check_in,l.check_out)||0;}
  function minsTxt(m){try{if(typeof window.minsToText==='function')return window.minsToText(N(m));}catch(e){} return Math.floor(N(m)/60)+' ساعة '+(N(m)%60)+' دقيقة';}
  function pct(v){return (Number(v||0).toFixed(1).replace(/\.0$/,''))+'%';}
  function inMonth(v,m){const s=String(v||''); if(!s)return false; if(s.slice(0,7)===m)return true; try{return new Date(s).toISOString().slice(0,7)===m;}catch(e){return false;}}
  function ymd(dt){return dt.toISOString().slice(0,10);}
  function range(m){const [y,mo]=String(m).split('-').map(Number);return {start:m+'-01', end:ymd(new Date(y,mo,1))};}
  function workersForProject(pid){
    const set=new Set();
    (d().workers||[]).forEach(w=>{if(sid(w.project_id)===sid(pid) && String(w.status||'active')!=='inactive' && w.name)set.add(w.name);});
    (d().workerAssignments||[]).forEach(a=>{if(a&&a.is_active!==false&&sid(a.project_id)===sid(pid)){const w=(d().workers||[]).find(x=>sid(x.id)===sid(a.worker_id)); if(w?.name)set.add(w.name);}});
    return [...set].join('، ')||'-';
  }
  function timeoutPromise(ms){return new Promise((_,rej)=>setTimeout(()=>rej(new Error('timeout')),ms));}
  async function safeFetchMonthLogs(m){
    // Do not freeze the page. If the database is slow, keep local data and allow manual monthly edits.
    if(!window.sb || !m) return [];
    const r=range(m);
    try{
      const q=window.sb.from('time_logs').select('id,supervisor_id,project_id,log_date,check_in,check_out,duration_minutes,travel_minutes,required_minutes').gte('check_in',r.start+'T00:00:00').lt('check_in',r.end+'T00:00:00').limit(1500);
      const res=await Promise.race([q, timeoutPromise(6500)]);
      if(res && !res.error){
        const existing=(d().logs||[]).filter(l=>!inMonth(l.log_date||l.check_in||l.created_at,m));
        const map=new Map(); existing.forEach(l=>map.set('old_'+(l.id||Math.random()),l)); (res.data||[]).forEach(l=>map.set('new_'+(l.id||Math.random()),l));
        if(!window.data) window.data={}; window.data.logs=[...map.values()]; window.data.__monthlyLoadedV331=m;
        return res.data||[];
      }
    }catch(e){
      console.warn('Monthly safe fetch skipped:', e.message||e);
    }
    return [];
  }
  function monthlyRowsV331(){
    const m=monthVal(), selected=$('monthlySupervisor')?.value||'';
    const map=new Map();
    (d().logs||[]).filter(l=>inMonth(l.log_date||l.check_in||l.created_at,m)).forEach(l=>{
      const pid=l.project_id; if(!pid)return; const sup=l.supervisor_id||projectSup(pid)||''; if(selected&&sid(sup)!==sid(selected))return;
      const k=key(m,sup,pid); if(!map.has(k))map.set(k,{manualOnly:false,s:sup,p:pid,a:0,c:0,t:0});
      const r=map.get(k); r.a+=actualMins(l); r.c+=1; r.t+=N(l.travel_minutes);
    });
    const st=getStore();
    (st.rows||[]).filter(r=>r.month===m).forEach(r=>{if(selected&&sid(r.s)!==sid(selected))return; const k=key(m,r.s,r.p); if(!map.has(k))map.set(k,{manualOnly:true,s:r.s,p:r.p,a:N(r.actual_minutes),c:N(r.count),t:N(r.travel_minutes)});});
    const vals=[...map.values()]; const totals={}; vals.forEach(r=>totals[sid(r.s)]=(totals[sid(r.s)]||0)+N(r.a));
    return vals.map(r=>{
      const o=override(m,r.s,r.p); const calc=totals[sid(r.s)]?r.a/totals[sid(r.s)]*100:0;
      return Object.assign({},r,{workers:o.workers!==undefined?o.workers:workersForProject(r.p), percent:o.percent!==undefined&&o.percent!==''?N(o.percent):calc, calcPercent:calc});
    }).sort((a,b)=>String(supName(a.s)).localeCompare(String(supName(b.s)),'ar')||String(projName(a.p)).localeCompare(String(projName(b.p)),'ar'));
  }
  function ensureManualPanel(){
    const page=$('monthly'); if(!page||$('monthlyManualPanelV331'))return;
    const card=page.querySelector('.card')||page;
    const panel=document.createElement('div'); panel.id='monthlyManualPanelV331'; panel.className='card'; panel.style.margin='10px 0';
    panel.innerHTML=`<h3>تعديل يدوي للأوقات الشهرية</h3><div class="filters"><select id="monthlyAddSupervisorV331"><option value="">اختر المشرف</option></select><select id="monthlyAddProjectV331"><option value="">اختر المشروع</option></select><input id="monthlyAddWorkersV331" placeholder="أسماء العمال - يمكن الإضافة والحذف"><input id="monthlyAddPercentV331" type="number" step="0.1" placeholder="النسبة %"><input id="monthlyAddMinutesV331" type="number" placeholder="الدقائق الفعلية اختياري"><button onclick="addMonthlyManualRowV331()">إضافة / تحديث صف</button></div><div class="footer-note">هذا القسم يعطيك صلاحية إضافة مشروع يدويًا للشهر السابق أو تعديل أسماء العمال والنسبة بدون الاعتماد على السجلات اليومية إذا كانت قاعدة البيانات بطيئة.</div>`;
    const before=card.querySelector('.table-wrap'); card.insertBefore(panel,before||card.children[1]||null);
    fillMonthlyManualSelects();
  }
  function fillMonthlyManualSelects(){
    const s=$('monthlyAddSupervisorV331'), p=$('monthlyAddProjectV331');
    if(s&&!s.dataset.done){s.innerHTML='<option value="">اختر المشرف</option>'+(d().supervisors||d().users||[]).filter(u=>!u.role||u.role==='supervisor'||u.role==='operations_manager'||u.full_name).map(u=>`<option value="${esc(u.id)}">${esc(u.full_name||u.name||u.username||u.id)}</option>`).join(''); s.dataset.done='1';}
    if(p&&!p.dataset.done){p.innerHTML='<option value="">اختر المشروع</option>'+(d().projects||[]).map(x=>`<option value="${esc(x.id)}">${esc(x.name||x.id)}</option>`).join(''); p.dataset.done='1';}
  }
  window.addMonthlyManualRowV331=function(){
    const m=monthVal(), s=$('monthlyAddSupervisorV331')?.value||'', p=$('monthlyAddProjectV331')?.value||''; if(!s||!p) return alert('اختر المشرف والمشروع');
    const st=getStore(); st.rows=(st.rows||[]).filter(r=>!(r.month===m&&sid(r.s)===sid(s)&&sid(r.p)===sid(p)));
    st.rows.push({month:m,s,p,actual_minutes:N($('monthlyAddMinutesV331')?.value),count:N($('monthlyAddMinutesV331')?.value)?1:0,travel_minutes:0}); setStore(st);
    saveOverride(m,s,p,{workers:$('monthlyAddWorkersV331')?.value||workersForProject(p), percent:$('monthlyAddPercentV331')?.value||''});
    if(typeof msg==='function')msg('تم إضافة / تحديث الصف اليدوي'); renderMonthlyV331();
  };
  window.saveMonthlyManualV331=function(m,s,p){
    const tr=[...document.querySelectorAll('#monthlyBody tr')].find(x=>sid(x.dataset.s)===sid(s)&&sid(x.dataset.p)===sid(p)); if(!tr)return;
    saveOverride(m,s,p,{workers:tr.querySelector('.monthly-workers-edit-v331')?.value||'', percent:tr.querySelector('.monthly-percent-edit-v331')?.value||''});
    if(typeof msg==='function')msg('تم حفظ التعديل');
  };
  window.deleteMonthlyManualV331=function(m,s,p){
    if(!confirm('حذف التعديل اليدوي لهذا الصف؟'))return; deleteOverride(m,s,p); renderMonthlyV331();
  };
  async function renderMonthlyV331(){
    const body=$('monthlyBody'); if(!body)return; ensureManualPanel(); fillMonthlyManualSelects();
    const m=monthVal(); body.innerHTML='<tr><td colspan="9">جاري تجهيز بيانات الشهر...</td></tr>';
    await safeFetchMonthLogs(m);
    const table=body.closest('table'); if(table?.tHead)table.tHead.innerHTML='<tr><th>المشرف</th><th>المشروع</th><th>أسماء العمال</th><th>عدد السجلات</th><th>الساعات الفعلية</th><th>وقت الانتقال</th><th>النسبة %</th><th>مصدر</th><th>إجراء</th></tr>';
    const rows=monthlyRowsV331();
    if(!rows.length){body.innerHTML='<tr><td colspan="9">لا توجد بيانات لهذا الشهر. يمكنك إضافة صف يدوي من النموذج بالأعلى.</td></tr>'; updateSummary([]); return;}
    body.innerHTML=rows.map(r=>`<tr data-s="${esc(r.s)}" data-p="${esc(r.p)}"><td>${esc(supName(r.s))}</td><td>${esc(projName(r.p))}</td><td><input class="monthly-workers-edit-v331" value="${esc(r.workers||'')}" style="min-width:260px" placeholder="أضف أو احذف أسماء العمال"></td><td>${N(r.c)}</td><td>${esc(minsTxt(r.a))}</td><td>${N(r.t)} دقيقة</td><td><input class="monthly-percent-edit-v331" type="number" step="0.1" value="${Number(r.percent||0).toFixed(1).replace(/\.0$/,'')}" style="width:100px;text-align:center"></td><td>${r.manualOnly?'يدوي':'سجلات'}</td><td><button class="light" onclick="saveMonthlyManualV331('${esc(m)}','${esc(r.s)}','${esc(r.p)}')">حفظ</button><button class="danger" onclick="deleteMonthlyManualV331('${esc(m)}','${esc(r.s)}','${esc(r.p)}')">حذف التعديل</button></td></tr>`).join('');
    updateSummary(rows);
  }
  function updateSummary(rows){
    const total=rows.reduce((a,r)=>a+N(r.a),0), supCount=new Set(rows.map(r=>sid(r.s))).size;
    const box=$('monthlySummary'); if(box)box.innerHTML=`<div class="kpi"><small>إجمالي الوقت</small><b>${esc(minsTxt(total))}</b></div><div class="kpi"><small>عدد المشرفين</small><b>${supCount}</b></div><div class="kpi"><small>عدد المشاريع</small><b>${rows.length}</b></div><div class="kpi"><small>الشهر</small><b>${esc(monthVal())}</b></div>`;
  }
  window.renderMonthly=renderMonthlyV331; window.monthlyRowsV60=monthlyRowsV331; window.monthlyRowsV284=monthlyRowsV331; window.monthlyBaseRowsV59=monthlyRowsV331; window.monthlyReportRowsV58=monthlyRowsV331;
  function downloadFile(filename, content, type){const blob=new Blob([content],{type:type||'text/csv;charset=utf-8'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=filename; document.body.appendChild(a); a.click(); a.remove(); setTimeout(()=>URL.revokeObjectURL(a.href),1500);}
  window.exportMonthlyCSV=async function(){await renderMonthlyV331(); const rows=monthlyRowsV331(); const lines=[['الشهر','المشرف','المشروع','أسماء العمال','عدد السجلات','الساعات الفعلية','وقت الانتقال','النسبة','المصدر'],...rows.map(r=>[monthVal(),supName(r.s),projName(r.p),r.workers,N(r.c),minsTxt(r.a),N(r.t)+' دقيقة',pct(r.percent),r.manualOnly?'يدوي':'سجلات'])]; downloadFile('monthly_times_'+monthVal()+'.csv','\ufeff'+lines.map(row=>row.map(x=>'"'+String(x??'').replace(/"/g,'""')+'"').join(',')).join('\n'))};
  window.printMonthlyReportV57=async function(){await renderMonthlyV331(); const rows=monthlyRowsV331(); const trs=rows.map(r=>`<tr><td>${esc(supName(r.s))}</td><td>${esc(projName(r.p))}</td><td>${esc(r.workers)}</td><td>${N(r.c)}</td><td>${esc(minsTxt(r.a))}</td><td>${N(r.t)} دقيقة</td><td>${esc(pct(r.percent))}</td><td>${r.manualOnly?'يدوي':'سجلات'}</td></tr>`).join(''); const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>الأوقات الشهرية</title><style>body{font-family:Tahoma,Arial,sans-serif;padding:20px;color:#103d32}.head{display:flex;justify-content:space-between;border-bottom:3px solid #0a4033;margin-bottom:14px}.logo{font-weight:900;font-size:22px;color:#0a4033}table{width:100%;border-collapse:collapse}th{background:#0a4033;color:white}td,th{border:1px solid #dce6e2;padding:8px;text-align:center}tr:nth-child(even)td{background:#f7fbfa}@media print{button{display:none}}</style></head><body><button onclick="print()">طباعة</button><div class="head"><div class="logo">شركة تصنيف لإدارة المرافق</div><div><h1>تقرير الأوقات الشهرية</h1><p>الشهر: ${esc(monthVal())}</p></div></div><table><thead><tr><th>المشرف</th><th>المشروع</th><th>العمال</th><th>عدد السجلات</th><th>الساعات الفعلية</th><th>الانتقال</th><th>النسبة</th><th>المصدر</th></tr></thead><tbody>${trs||'<tr><td colspan="8">لا توجد بيانات</td></tr>'}</tbody></table><script>setTimeout(()=>print(),500)<\/script></body></html>`; const w=window.open('','_blank'); if(w){w.document.write(html); w.document.close();}};
  function boot(){const m=$('monthlyMonth'), s=$('monthlySupervisor'); if(m&&!m.dataset.v331){m.dataset.v331='1'; m.addEventListener('change',()=>renderMonthlyV331());} if(s&&!s.dataset.v331){s.dataset.v331='1'; s.addEventListener('change',()=>renderMonthlyV331());} if($('monthlyBody')) renderMonthlyV331();}
  document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,900)); window.addEventListener('load',()=>setTimeout(boot,1400));
  console.log('Tasneef '+VERSION+' loaded');
})();

/* ===== V338: Contracts/services view-only workflow + attendance supervisor filter + monthly print layout ===== */
(function(){
  'use strict';
  if(window.__tasneefV338ContractsAttendanceMonthly) return;
  window.__tasneefV338ContractsAttendanceMonthly = true;
  const $id = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const N = v => { const n=Number(v||0); return Number.isFinite(n)?n:0; };
  const escx = s => S(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const dset = () => window.data || data || {};
  const role = () => { try{return (session()?.role)||''}catch(_){return ''} };
  const isAdminUser = () => ['admin','general_manager','financial_manager','operations_manager','warehouse_manager'].includes(role());
  const msgx = (t,kind) => { try{ return msg(t,kind); }catch(_){ alert(t); } };
  const todayx = () => { try{return today()}catch(_){return new Date().toISOString().slice(0,10)} };
  const supText = id => { try{return supervisorName(id)}catch(_){return (dset().users||[]).find(u=>S(u.id)===S(id))?.full_name || '-'} };
  const projText = id => { try{return projectName(id)}catch(_){return (dset().projects||[]).find(p=>S(p.id)===S(id))?.name || '-'} };
  const workerText = id => { try{return workerName(id)}catch(_){return (dset().workers||[]).find(w=>S(w.id)===S(id))?.name || '-'} };
  function workerRow(id){ return (dset().workers||[]).find(w=>S(w.id)===S(id)) || {}; }
  function projectRow(id){ return (dset().projects||[]).find(p=>S(p.id)===S(id)) || {}; }
  function workerSup(w){ try{return workerSupId(w)}catch(_){return w.app_supervisor_id||w.supervisor_id||projectRow(w.project_id||w.assigned_project_id).supervisor_id||''} }
  function workerProj(w){ try{return workerProjectId(w)}catch(_){return w.project_id||w.assigned_project_id||w.current_project_id||''} }
  function attendanceEffectiveSupervisor(a){
    if(a.supervisor_id) return a.supervisor_id;
    const w=workerRow(a.worker_id);
    const ws=workerSup(w); if(ws) return ws;
    const ps=projectRow(a.project_id || workerProj(w)).supervisor_id; if(ps) return ps;
    return '';
  }
  function attendanceEffectiveProject(a){
    if(a.project_id) return a.project_id;
    const w=workerRow(a.worker_id);
    return workerProj(w) || '';
  }

  function ensureV338Style(){
    if($id('tasneefV338Style')) return;
    const st=document.createElement('style'); st.id='tasneefV338Style';
    st.textContent=`.modal-v338{position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:99999;display:flex;align-items:center;justify-content:center;padding:18px}.modal-v338.hidden{display:none!important}.modal-card-v338{width:min(980px,96vw);max-height:92vh;overflow:auto;background:#fff;border-radius:20px;box-shadow:0 25px 80px rgba(0,0,0,.25)}.modal-head-v338{display:flex;align-items:center;justify-content:space-between;background:#0a5a49;color:#fff;padding:14px 18px;border-radius:20px 20px 0 0}.modal-head-v338 h2{margin:0;color:#fff}.modal-body-v338{padding:16px}.service-view-grid-v338{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:10px 0}.service-box-v338{border:1px solid #d9e6e1;border-radius:14px;background:#f8fbfa;padding:10px;text-align:center}.service-box-v338 small{display:block;color:#68766e}.service-box-v338 b{display:block;color:#0a5a49;font-size:18px;margin-top:3px}.exec-number-v338{cursor:pointer;border:0;background:#0a5a49;color:#fff;border-radius:999px;padding:8px 18px;font-size:18px;font-weight:900}.exec-number-v338:hover{filter:brightness(.92)}.service-note-v338{background:#fff8e6;border:1px solid #ead28d;color:#6d5100;border-radius:12px;padding:10px;margin-top:10px;font-weight:700}.service-actions-v338{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}`;
    document.head.appendChild(st);
  }

  function svc(id){ try{return serviceById(id)}catch(_){return (dset().contractServices||[]).find(s=>S(s.id)===S(id))} }
  function svcProjectName(s){ try{return csProjectName(s)}catch(_){return s.project_name || projText(s.project_id)} }
  function svcSupervisorName(s){ try{return csSupervisorName(s)}catch(_){return s.supervisor_name || supText(s.supervisor_id)} }
  function svcName(s){ try{return csServiceName(s)}catch(_){return s.service_name || '-'} }
  function svcType(s){ try{return csServiceType(s)}catch(_){return s.service_type || s.service_name || '-'} }
  function svcFreq(s){ try{return csFrequency(s)}catch(_){return s.frequency || '-'} }
  function svcVisits(s){ try{return csVisits(s)}catch(_){return N(s.visit_count||s.visits_required||1)} }
  function svcDone(s){ try{return csDone(s)}catch(_){return N(s.executed_count||s.done_count||0)} }
  function svcRemaining(s){ try{return csRemaining(s)}catch(_){return Math.max(svcVisits(s)-svcDone(s),0)} }
  function svcLast(s){ try{return csLastDate(s)}catch(_){return S(s.last_execution_date||'')} }
  function svcDue(s){ try{return csDueDate(s)}catch(_){return S(s.next_due_date||'')} }
  function svcNotes(s){ try{return csNotes(s)}catch(_){return S(s.notes||'')} }
  function svcStatusKey(s){ try{return csStatusKey(s)}catch(_){return S(s.status)==='منجزة'?'done':'due'} }
  function svcStatusText(s){ try{return csStatusText(svcStatusKey(s))}catch(_){return S(s.status||'مستحقة')} }
  function svcBadge(s){ try{return csBadgeClass(svcStatusKey(s))}catch(_){return svcStatusKey(s)==='done'?'green':'amber'} }

  window.renderContractServices = function(){
    const body=$id('contractServicesBody'); if(!body) return;
    try{ hydrateServiceTypes && hydrateServiceTypes(); }catch(_){ }
    try{ financeHydrateForms && financeHydrateForms(); }catch(_){ }
    const rows = (typeof getFilteredContractServices==='function' ? getFilteredContractServices() : (dset().contractServices||[]));
    const all=dset().contractServices||[];
    const counts={total:all.length,due:0,soon:0,done:0,late:0,review:0};
    all.forEach(s=>{const k=svcStatusKey(s); if(k==='done')counts.done++; else if(k==='late')counts.late++; else if(k==='soon')counts.soon++; else if(k==='review')counts.review++; else counts.due++;});
    ['Total','Due','Done','Soon','Late','Review'].forEach(x=>{ const el=$id('services'+x+'Count'); if(el) el.textContent=counts[x.toLowerCase()]||0; });
    const emptyMsg = dset().contractServicesError ? ('تعذر تحميل الخدمات: '+escx(dset().contractServicesError)) : 'لا توجد خدمات مسجلة.';
    body.innerHTML = rows.slice(0,500).map(s=>`<tr>
      <td><b>${escx(svcProjectName(s))}</b></td><td>${escx(svcSupervisorName(s))}</td><td><b>${escx(svcName(s))}</b></td><td>${escx(svcType(s))}</td><td>${escx(svcFreq(s))}</td><td>${svcVisits(s)}</td><td>${escx(s.executor_name||'-')}</td><td>${svcDone(s)}</td><td>${svcRemaining(s)}</td><td>${escx(svcLast(s)||'-')}</td><td>${escx(svcDue(s)||'-')}</td><td><span class="badge ${svcBadge(s)}">${escx(svcStatusText(s))}</span></td><td>${escx(svcNotes(s)||'-')}</td><td class="row-actions">${isAdminUser()?`<button onclick="openContractServiceViewV338(${Number(s.id)||0})">عرض</button>`:'-'}</td>
    </tr>`).join('') || `<tr><td colspan="14">${emptyMsg}</td></tr>`;
    try{ renderSmartServicesList && renderSmartServicesList(); }catch(_){ }
    try{ showSupervisorServicesPreview && showSupervisorServicesPreview(false); }catch(_){ }
  };

  window.openContractServiceViewV338 = function(id){
    ensureV338Style();
    const s=svc(id); if(!s) return msgx('الخدمة غير موجودة','err');
    let modal=$id('contractServiceModalV338');
    if(!modal){
      modal=document.createElement('div'); modal.id='contractServiceModalV338'; modal.className='modal-v338 hidden';
      document.body.appendChild(modal);
    }
    modal.innerHTML=`<div class="modal-card-v338"><div class="modal-head-v338"><h2>عرض الخدمة التعاقدية</h2><button class="light" onclick="closeContractServiceViewV338()">إغلاق</button></div><div class="modal-body-v338">
      <h2>${escx(svcName(s))}</h2>
      <div class="service-view-grid-v338">
        <div class="service-box-v338"><small>المشروع</small><b>${escx(svcProjectName(s))}</b></div>
        <div class="service-box-v338"><small>المشرف</small><b>${escx(svcSupervisorName(s))}</b></div>
        <div class="service-box-v338"><small>نوع الخدمة</small><b>${escx(svcType(s))}</b></div>
        <div class="service-box-v338"><small>التكرار</small><b>${escx(svcFreq(s))}</b></div>
        <div class="service-box-v338"><small>عدد الزيارات</small><b>${svcVisits(s)}</b></div>
        <div class="service-box-v338"><small>اضغط الرقم لتسجيل تنفيذ</small><button class="exec-number-v338" onclick="markContractServiceExecutedV338(${Number(s.id)||0})">${svcDone(s)}</button></div>
        <div class="service-box-v338"><small>المتبقي</small><b>${svcRemaining(s)}</b></div>
        <div class="service-box-v338"><small>الحالة</small><b>${escx(svcStatusText(s))}</b></div>
        <div class="service-box-v338"><small>آخر تنفيذ</small><b>${escx(svcLast(s)||'-')}</b></div>
        <div class="service-box-v338"><small>الاستحقاق القادم</small><b>${escx(svcDue(s)||'-')}</b></div>
        <div class="service-box-v338"><small>المنفذ</small><b>${escx(s.executor_name||'-')}</b></div>
        <div class="service-box-v338"><small>ملاحظات</small><b>${escx(svcNotes(s)||'-')}</b></div>
      </div>
      <div class="service-note-v338">للإداريين: زر العرض فقط ظاهر في الجدول. عند الضغط على رقم المنفذ يتم تسجيل أن الخدمة نُفذت وزيادة العدد تلقائيًا.</div>
      <div class="service-actions-v338"><button onclick="markContractServiceExecutedV338(${Number(s.id)||0})">تسجيل تنفيذ الآن</button><button class="light" onclick="whatsappContractService(${Number(s.id)||0})">إرسال واتساب</button></div>
    </div></div>`;
    modal.classList.remove('hidden');
  };
  window.closeContractServiceViewV338 = function(){ $id('contractServiceModalV338')?.classList.add('hidden'); };
  window.markContractServiceExecutedV338 = async function(id){
    if(!isAdminUser()) return msgx('غير مسموح إلا للإداريين','err');
    const s=svc(id); if(!s) return msgx('الخدمة غير موجودة','err');
    const d=prompt('تاريخ التنفيذ', todayx()); if(!d) return;
    const executor=prompt('اسم المنفذ / الشركة المنفذة', S(s.executor_name||'')) || '';
    const extra=prompt('ملاحظة التنفيذ', '') || '';
    const done=svcDone(s)+1, visits=Math.max(svcVisits(s), done), remaining=Math.max(visits-done,0);
    let next=null; if(remaining>0) next=prompt('تاريخ الاستحقاق القادم YYYY-MM-DD', svcDue(s)||'') || null;
    const notes=[svcNotes(s), extra].filter(Boolean).join(' / ');
    const status=remaining>0?'مستحقة':'منجزة';
    const {error}=await sb.from('contract_services').update({last_execution_date:d, executed_count:done, remaining_count:remaining, visit_count:visits, executor_name:executor, notes, status, next_due_date:next, updated_at:new Date().toISOString()}).eq('id',id);
    if(error) return msgx(error.message,'err');
    msgx('تم تسجيل تنفيذ الخدمة');
    try{ await refreshAll(); }catch(_){ }
    setTimeout(()=>openContractServiceViewV338(id),150);
  };

  window.renderAttendance = function(){
    const b=$id('attendanceBody'); if(!b) return;
    const d=$id('attendanceFilterDate')?.value || '', s=$id('attendanceFilterSupervisor')?.value || '', q=S($id('attendanceSearch')?.value);
    let rows=[...(dset().attendance||[])];
    if(d) rows=rows.filter(a=>S(a.attendance_date)===S(d));
    if(s) rows=rows.filter(a=>S(attendanceEffectiveSupervisor(a))===S(s));
    if(q) rows=rows.filter(a=>[workerText(a.worker_id),supText(attendanceEffectiveSupervisor(a)),projText(attendanceEffectiveProject(a)),a.notes].join(' ').includes(q));
    const table=b.closest('table');
    if(table?.tHead) table.tHead.innerHTML='<tr><th>التاريخ</th><th>العامل</th><th>المشرف</th><th>المشروع</th><th>الحالة</th><th>ملاحظات</th><th>إجراء</th></tr>';
    b.innerHTML=rows.map(a=>`<tr><td>${escx(a.attendance_date)}</td><td>${escx(workerText(a.worker_id))}</td><td>${escx(supText(attendanceEffectiveSupervisor(a)))}</td><td>${escx(projText(attendanceEffectiveProject(a)))}</td><td><span class="badge ${a.status==='present'?'green':'red'}">${a.status==='present'?'حاضر':'غائب'}</span></td><td>${escx(a.notes||'')}</td><td class="row-actions"><button onclick="editAttendance(${a.id})">تعديل</button><button class="danger" onclick="deleteRow('attendance',${a.id})">حذف</button></td></tr>`).join('') || '<tr><td colspan="7">لا توجد بيانات</td></tr>';
    try{ renderAttendanceWorkersQuick && renderAttendanceWorkersQuick(); }catch(_){ }
  };

  function normName(name){ return S(name).replace(/[أإآ]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه').replace(/[\u064B-\u0652]/g,'').replace(/\s+/g,' ').toLowerCase(); }
  function minuteText(m){ return `${Math.round(N(m)).toLocaleString('en-US')} دقيقة`; }
  function pctText(p){ return `${N(p).toLocaleString('en-US',{minimumFractionDigits:1,maximumFractionDigits:1})}%`; }
  function logDate(l){ return S(l.log_date || l.date || l.check_in || l.created_at).slice(0,10); }
  function actualMins(l){ try{ if(typeof logActualMinutes==='function') return N(logActualMinutes(l)); }catch(_){ } try{return N(l.duration_minutes || minutesBetween(l.check_in,l.check_out));}catch(_){return N(l.duration_minutes)} }
  function requiredMins(l){ try{ if(typeof logRequiredMinutes==='function') return N(logRequiredMinutes(l)); }catch(_){ } return N(l.required_minutes || l.daily_required_minutes || l.required_daily_minutes); }
  function isPermanentProject(pid,maxReq){
    const p=projectRow(pid), op=S(p.operation_type || p.visit_type_default || p.type || p.category || p.notes).toLowerCase(), nm=S(p.name);
    const base=N(p.required_daily_minutes || p.required_minutes || p.daily_minutes || maxReq);
    return base>=540 || op.includes('full') || op.includes('permanent') || op.includes('24') || nm.includes('دائم') || nm.includes('دوام') || nm.includes('24');
  }
  function workersForProject(pid){
    const mp=new Map();
    (dset().workers||[]).forEach(w=>{
      if(['deleted','inactive'].includes(S(w.status||'active').toLowerCase())) return;
      if(S(workerProj(w))!==S(pid)) return;
      const name=S(w.name||w.full_name); const k=normName(name); if(k && !mp.has(k)) mp.set(k,name);
    });
    return [...mp.values()].sort((a,b)=>a.localeCompare(b,'ar'));
  }
  function buildMonthlyV338(){
    const month=$id('monthlyMonth')?.value || todayx().slice(0,7);
    const selected=$id('monthlySupervisor')?.value || '';
    let logs=(dset().logs||[]).filter(l=>logDate(l).slice(0,7)===month && S(l.visit_type||'')!=='technician_attendance');
    logs=logs.filter(l=>!selected || S(projectRow(l.project_id).supervisor_id || l.supervisor_id)===S(selected));
    const map=new Map();
    logs.forEach(l=>{
      const sid=projectRow(l.project_id).supervisor_id || l.supervisor_id || '';
      const pid=l.project_id || '';
      const key=S(sid)+'|'+S(pid);
      if(!map.has(key)) map.set(key,{s:sid,p:pid,actual:0,required:0,travel:0,records:0,maxReq:0});
      const g=map.get(key), req=requiredMins(l);
      g.actual+=actualMins(l); g.required+=req; g.travel+=N(l.travel_minutes); g.records++; g.maxReq=Math.max(g.maxReq,req);
    });
    const all=[...map.values()].map(g=>({...g,projectName:projText(g.p),supervisorName:supText(g.s),workers:workersForProject(g.p),permanent:isPermanentProject(g.p,g.maxReq)}));
    const daily=all.filter(r=>!r.permanent), permanent=all.filter(r=>r.permanent);
    function groupRows(rows, withPercent){
      const by={}; rows.forEach(r=>{ const k=S(r.s); (by[k]||(by[k]=[])).push(r); });
      Object.keys(by).forEach(sid=>{
        by[sid].sort((a,b)=>a.projectName.localeCompare(b.projectName,'ar'));
        const total=by[sid].reduce((a,r)=>a+N(r.actual),0); let used=0;
        by[sid].forEach((r,i)=>{ if(withPercent){ let pct=total?N(r.actual)/total*100:0; if(i<by[sid].length-1){pct=Math.round(pct*10)/10; used+=pct;} else pct=total?Math.max(0,Math.round((100-used)*10)/10):0; r.percent=pct; } r.supTotal=total; });
      });
      return by;
    }
    return {month,daily,permanent,byDaily:groupRows(daily,true),byPermanent:groupRows(permanent,false)};
  }
  window.buildMonthlyV338 = buildMonthlyV338;
  window.renderMonthly = function(){
    const body=$id('monthlyBody'); if(!body) return;
    const table=body.closest('table');
    if(table?.tHead) table.tHead.innerHTML='<tr><th>القسم</th><th>المشرف</th><th>المشروع</th><th>أسماء العمال بدون تكرار</th><th>الدقائق</th><th>النسبة</th><th>مجموع دقائق المشرف</th></tr>';
    const res=buildMonthlyV338(); let html='';
    Object.keys(res.byDaily).sort((a,b)=>supText(a).localeCompare(supText(b),'ar')).forEach(sid=>{
      res.byDaily[sid].forEach(r=>{ html+=`<tr><td>مشاريع الزيارة اليومية</td><td>${escx(r.supervisorName)}</td><td>${escx(r.projectName)}</td><td>${escx(r.workers.join('، ')||'-')}</td><td>${minuteText(r.actual)}</td><td><span class="badge green">${pctText(r.percent)}</span></td><td>${minuteText(r.supTotal)}</td></tr>`; });
    });
    Object.keys(res.byPermanent).sort((a,b)=>supText(a).localeCompare(supText(b),'ar')).forEach(sid=>{
      res.byPermanent[sid].forEach(r=>{ html+=`<tr><td>مشاريع الدوام الكامل</td><td>${escx(r.supervisorName)}</td><td>${escx(r.projectName)}</td><td>${escx(r.workers.join('، ')||'-')}</td><td>${minuteText(r.actual)}</td><td>—</td><td>${minuteText(r.supTotal)}</td></tr>`; });
    });
    body.innerHTML=html || '<tr><td colspan="7">لا توجد بيانات في هذا الشهر</td></tr>';
    const dt=res.daily.reduce((a,r)=>a+N(r.actual),0), pt=res.permanent.reduce((a,r)=>a+N(r.actual),0);
    const summary=$id('monthlySummary'); if(summary) summary.innerHTML=`<div class="kpi"><small>مشاريع الزيارة اليومية</small><b>${res.daily.length}</b></div><div class="kpi"><small>مشاريع الدوام الكامل</small><b>${res.permanent.length}</b></div><div class="kpi"><small>إجمالي دقائق الزيارة</small><b>${minuteText(dt)}</b></div><div class="kpi"><small>إجمالي دقائق الدوام الكامل</small><b>${minuteText(pt)}</b></div><div class="kpi"><small>الشهر</small><b>${escx(res.month)}</b></div>`;
  };
  window.printMonthlyReportV57 = window.printMonthlyReportV338 = function(){
    const res=buildMonthlyV338();
    if(!res.daily.length && !res.permanent.length) return msgx('لا توجد بيانات للطباعة','err');
    function workerListForRows(rows){ const mp=new Map(); rows.forEach(r=>r.workers.forEach(n=>{const k=normName(n); if(k&&!mp.has(k)) mp.set(k,n);})); return [...mp.values()].join('، ') || '-'; }
    function dailySection(){ return Object.keys(res.byDaily).sort((a,b)=>supText(a).localeCompare(supText(b),'ar')).map(sid=>{ const rows=res.byDaily[sid], total=rows.reduce((a,r)=>a+N(r.actual),0); return `<div class="sup-block"><h3>اسم المشرف: ${escx(supText(sid))}</h3><table><thead><tr><th>المشروع</th><th>الدقائق</th><th>النسبة</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${escx(r.projectName)}</td><td>${minuteText(r.actual)}</td><td>${pctText(r.percent)}</td></tr>`).join('')}<tr class="total"><td>المجموع</td><td>${minuteText(total)}</td><td>100.0%</td></tr></tbody></table><div class="workers"><b>أسماء العمال بدون تكرار:</b><p>${escx(workerListForRows(rows))}</p></div></div>`; }).join('') || '<div class="empty">لا توجد بيانات</div>'; }
    function permanentSection(){ return Object.keys(res.byPermanent).sort((a,b)=>supText(a).localeCompare(supText(b),'ar')).map(sid=>{ const rows=res.byPermanent[sid], total=rows.reduce((a,r)=>a+N(r.actual),0); return `<div class="sup-block"><h3>اسم المشرف: ${escx(supText(sid))}</h3><table><thead><tr><th>المشروع</th><th>الدقائق</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${escx(r.projectName)}</td><td>${minuteText(r.actual)}</td></tr>`).join('')}<tr class="total"><td>المجموع</td><td>${minuteText(total)}</td></tr></tbody></table><div class="workers"><b>أسماء العمال بدون تكرار:</b><p>${escx(workerListForRows(rows))}</p></div></div>`; }).join('') || '<div class="empty">لا توجد بيانات</div>'; }
    const dailyTotal=res.daily.reduce((a,r)=>a+N(r.actual),0), permTotal=res.permanent.reduce((a,r)=>a+N(r.actual),0);
    const html=`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الأوقات الشهرية</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;margin:0;color:#153d33;background:#fff;font-size:11px}.page{padding:14px;border:2px solid #0a5a49;min-height:100vh}.top{display:flex;justify-content:space-between;align-items:center;border-bottom:3px solid #0a5a49;padding-bottom:10px;margin-bottom:10px}.brand{display:flex;gap:10px;align-items:center}.logo{width:58px;height:58px;border:2px solid #c7a24d;border-radius:18px;display:grid;place-items:center;color:#0a5a49;font-weight:900}.brand h1,.title h2{margin:0;color:#0a5a49}.title{text-align:left}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:10px 0}.box{border:1px solid #d9e6e1;border-radius:12px;text-align:center;padding:9px;background:#f8fbfa}.box small{display:block;color:#68766e}.box b{font-size:16px;color:#0a5a49}.section-title{text-align:center;background:#0a5a49;color:#fff;border:2px solid #c7a24d;border-radius:999px;padding:8px 30px;font-weight:900;margin:14px 0 10px}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.sup-block{border:2px solid #0a5a49;border-radius:14px;overflow:hidden;break-inside:avoid;background:#fff}.sup-block h3{margin:0;background:#f4fbf8;color:#0a5a49;padding:9px;border-bottom:1px solid #d9e6e1}table{width:100%;border-collapse:collapse}th{background:#0a5a49;color:#fff;padding:7px}td{border:1px solid #e1ebe7;text-align:center;padding:6px}.total{font-weight:900;background:#f7fbfa}.workers{padding:9px;text-align:center}.workers p{margin:5px 0 0;line-height:1.8}.empty{grid-column:1/-1;border:1px dashed #9fb7ae;border-radius:12px;padding:18px;text-align:center;color:#68766e}.note{text-align:center;color:#68766e;margin-top:10px}.signs{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:14px}.sign{border:1px solid #d9e6e1;border-radius:12px;padding:10px;min-height:66px}@media print{body{-webkit-print-color-adjust:exact;print-color-adjust:exact}.sup-block{break-inside:avoid}}</style></head><body><div class="page"><div class="top"><div class="brand"><div class="logo">تصنيف</div><div><h1>شركة تصنيف لإدارة المرافق</h1><small>تقرير تشغيل شهري</small></div></div><div class="title"><h2>تقرير الأوقات الشهرية</h2><p>الشهر: ${escx(res.month)}</p></div></div><div class="meta"><div class="box"><small>مشاريع الزيارة اليومية</small><b>${res.daily.length}</b></div><div class="box"><small>مشاريع الدوام الكامل</small><b>${res.permanent.length}</b></div><div class="box"><small>إجمالي دقائق الزيارة</small><b>${minuteText(dailyTotal)}</b></div><div class="box"><small>إجمالي دقائق الدوام الكامل</small><b>${minuteText(permTotal)}</b></div></div><div class="section-title">مشاريع الزيارة اليومية</div><div class="grid">${dailySection()}</div><div class="section-title">مشاريع الدوام الكامل</div><div class="grid">${permanentSection()}</div><div class="signs"><div class="sign"><b>إعداد</b><br><br>الاسم: ____________</div><div class="sign"><b>مراجعة</b><br><br>الاسم: ____________</div><div class="sign"><b>اعتماد</b><br><br>الاسم: ____________</div></div><div class="note">تقرير الأوقات الشهرية: الزيارة اليومية تعرض النسب والدقائق، والدوام الكامل يعرض الدقائق، وأسماء العمال بدون تكرار تحت كل مشرف.</div></div><script>window.onload=function(){setTimeout(function(){window.print()},450)}<\/script></body></html>`;
    const w=window.open('','_blank'); if(!w) return msgx('اسمح بالنوافذ المنبثقة للطباعة','err'); w.document.open(); w.document.write(html); w.document.close();
  };
  window.exportMonthlyCSV = function(){
    const res=buildMonthlyV338();
    const lines=[['القسم','المشرف','المشروع','أسماء العمال بدون تكرار','الدقائق','النسبة','مجموع دقائق المشرف']];
    res.daily.forEach(r=>lines.push(['مشاريع الزيارة اليومية',r.supervisorName,r.projectName,r.workers.join('، '),Math.round(N(r.actual)),pctText(r.percent),Math.round(N(r.supTotal))]));
    res.permanent.forEach(r=>lines.push(['مشاريع الدوام الكامل',r.supervisorName,r.projectName,r.workers.join('، '),Math.round(N(r.actual)),'',Math.round(N(r.supTotal))]));
    const csv='\uFEFF'+lines.map(row=>row.map(c=>'"'+S(c).replace(/"/g,'""')+'"').join(',')).join('\n');
    try{ download('monthly-v338.csv',csv); }catch(_){ }
  };

  function initV338(){
    try{ hydrateServiceTypes && hydrateServiceTypes(); }catch(_){ }
    try{ if($id('contractServicesBody')) renderContractServices(); }catch(_){ }
    try{ if($id('attendanceBody')) renderAttendance(); }catch(_){ }
    try{ if($id('monthlyBody')) renderMonthly(); }catch(_){ }
  }
  ['DOMContentLoaded','load'].forEach(ev=>window.addEventListener(ev,()=>setTimeout(initV338, ev==='load'?1300:500)));
  setTimeout(initV338,1800);
  console.log('Tasneef V338 loaded');
})();

/* ===== V339: Contracts role buttons + visit-number save workflow ===== */
(function(){
  'use strict';
  if(window.__tasneefV339ContractsRoleVisits) return;
  window.__tasneefV339ContractsRoleVisits = true;
  const $ = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const N = v => { const n=Number(v||0); return Number.isFinite(n)?n:0; };
  const esc = s => S(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const ds = () => window.data || data || {};
  const current = () => { try{return (typeof session==='function' ? (session()||{}) : JSON.parse(localStorage.getItem('tasneef_user')||'null')) || {}; }catch(_){return {}; } };
  const role = () => S(current().role);
  const isAdminArea = () => ['admin','general_manager','financial_manager','operations_manager','warehouse_manager'].includes(role());
  const isSystemAdmin = () => role()==='admin';
  const msgx = (t,k) => { try{return msg(t,k)}catch(_){ alert(t); } };
  const today = () => { try{return window.today()}catch(_){return new Date().toISOString().slice(0,10)} };
  const hProject = id => { try{return projectName(id)}catch(_){ return (ds().projects||[]).find(p=>S(p.id)===S(id))?.name || '-'; } };
  const hSupervisor = id => { try{return supervisorName(id)}catch(_){ return (ds().users||[]).find(u=>S(u.id)===S(id))?.full_name || '-'; } };
  function ensureStyle(){
    if($('tasneefV339Style')) return;
    const st=document.createElement('style'); st.id='tasneefV339Style';
    st.textContent=`.visit-chip-v339{border:1px solid #bcd9d0;background:#f3faf8;color:#0a4033;border-radius:999px;padding:8px 14px;margin:3px;font-weight:900;cursor:pointer}.visit-chip-v339.done{background:#0a5a49;color:#fff;border-color:#0a5a49}.visit-chip-v339.selected{outline:3px solid #f0c66b}.visit-save-panel-v339{border:1px solid #dce8e4;border-radius:14px;padding:12px;background:#fbfdfc;margin-top:12px}.visit-save-panel-v339 .grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.visit-save-panel-v339 input{width:100%;box-sizing:border-box}.muted-v339{color:#68766e;font-size:12px}.sys-only-v339{display:inline-flex}`;
    document.head.appendChild(st);
  }

  /* عقود المشاريع: الإداري عرض فقط، مدير النظام عرض وتعديل */
  const oldRenderContracts = window.renderContracts;
  window.renderContracts = function(){
    const body=$('contractsBody');
    if(!body || typeof contractInfo!=='function') return oldRenderContracts && oldRenderContracts.apply(this,arguments);
    const q=S($('contractSearch')?.value);
    const st=S($('contractFilterStatus')?.value);
    let rows=[...(ds().projects||[])];
    if(q) rows=rows.filter(p=>S(p.name).includes(q));
    if(st) rows=rows.filter(p=>contractInfo(p).key===st);
    rows.sort((a,b)=>{ const da=typeof daysLeft==='function'?daysLeft(a.contract_end):999999; const db=typeof daysLeft==='function'?daysLeft(b.contract_end):999999; return (da??999999)-(db??999999); });
    body.innerHTML=rows.map(p=>{ const c=contractInfo(p); const view=`<button class="light" onclick="openContractSmartModal(${Number(p.id)||0},'view')">عرض</button>`; const edit=isSystemAdmin()?`<button onclick="openContractSmartModal(${Number(p.id)||0},'edit')">تعديل</button>`:''; return `<tr><td><b>${esc(p.name)}</b></td><td>${p.buildings_count||0}</td><td>${p.units_count||0}</td><td>${esc((typeof isoDate==='function'?isoDate(p.contract_start):p.contract_start)||'-')}</td><td>${esc((typeof isoDate==='function'?isoDate(p.contract_end):p.contract_end)||'-')}</td><td>${c.days}</td><td><span class="badge ${c.cls}">${c.text}</span></td><td class="row-actions">${isAdminArea()?view+edit:'-'}</td></tr>`; }).join('') || '<tr><td colspan="8">لا توجد بيانات</td></tr>';
    if($('contractsActiveCount')) $('contractsActiveCount').textContent=(ds().projects||[]).filter(p=>contractInfo(p).key==='active').length;
    if($('contractsSoonCount')) $('contractsSoonCount').textContent=(ds().projects||[]).filter(p=>contractInfo(p).key==='soon').length;
    if($('contractsExpiredCount')) $('contractsExpiredCount').textContent=(ds().projects||[]).filter(p=>contractInfo(p).key==='expired').length;
    if($('contractsMissingCount')) $('contractsMissingCount').textContent=(ds().projects||[]).filter(p=>contractInfo(p).key==='missing').length;
  };

  /* عند الإداريين: فتح العقد للعرض مع السماح فقط بتحديد أرقام الزيارات ثم حفظ */
  const originalOpenSmart = window.openContractSmartModal;
  window.openContractSmartModal = async function(projectId, mode){
    if(!isSystemAdmin()) mode='view';
    const r = await originalOpenSmart.apply(this,[projectId,mode]);
    if(isAdminArea() && !isSystemAdmin()) setTimeout(()=>enableContractVisitSaveOnlyV339(projectId),120);
    return r;
  };
  window.enableContractVisitSaveOnlyV339 = function(projectId){
    ensureStyle();
    const api=window.__contractSmartV299; if(!api || !$('contractSmartModal')) return;
    const original=api.getSmart(projectId);
    const draft=JSON.parse(JSON.stringify(original||{}));
    window.__contractVisitDraftV339={projectId:String(projectId),payload:draft};
    const save=$('contractSmartSaveBtn');
    if(save){ save.style.display=''; save.textContent='حفظ تنفيذ الزيارات'; save.onclick=saveContractVisitsOnlyV339; }
    document.querySelectorAll('#contractSmartModal input:not([type="checkbox"]),#contractSmartModal textarea,#contractSmartModal select').forEach(el=>{ el.disabled=true; });
    document.querySelectorAll('#contractSmartModal input[type="checkbox"]').forEach(el=>{ el.disabled=true; });
    document.querySelectorAll('#contractSmartModal button').forEach(btn=>{
      const oc=btn.getAttribute('onclick')||'';
      let m=oc.match(/toggleAnnualVisit\('([^']+)'\s*,\s*(\d+)\)/);
      if(m){ btn.disabled=false; btn.onclick=function(){ toggleAnnualDraftV339(m[1], Number(m[2]), this); }; btn.classList.add('visit-chip-v339'); return; }
      m=oc.match(/toggleCoreVisit\('([^']+)'\s*,\s*(\d+)\)/);
      if(m){ btn.disabled=false; btn.onclick=function(){ toggleCoreDraftV339(m[1], Number(m[2]), this); }; btn.classList.add('visit-chip-v339'); return; }
    });
    const note=document.createElement('div'); note.className='visit-save-panel-v339'; note.innerHTML='<b>وضع الإداري:</b> يمكنك فقط الضغط على أرقام الزيارات المنفذة ثم الضغط على <b>حفظ تنفيذ الزيارات</b>. لا يمكن تعديل بيانات العقد.';
    const target=$('contractSmartAnnualTab') || $('contractSmartMainTab'); if(target && !target.querySelector('.visit-save-panel-v339')) target.prepend(note);
  };
  window.toggleAnnualDraftV339=function(id,no,btn){
    const d=window.__contractVisitDraftV339?.payload; if(!d) return;
    const a=(d.annual||[]).find(x=>S(x.id)===S(id)); if(!a) return;
    if(!Array.isArray(a.done)) a.done=[];
    const i=a.done.indexOf(no); if(i>=0){ a.done.splice(i,1); btn?.classList.remove('done'); } else { a.done.push(no); btn?.classList.add('done'); }
    a.done=a.done.sort((x,y)=>x-y); btn?.classList.add('selected');
  };
  window.toggleCoreDraftV339=function(key,no,btn){
    const d=window.__contractVisitDraftV339?.payload; if(!d || !d.core || !d.core[key]) return;
    const a=d.core[key]; if(!Array.isArray(a.done)) a.done=[];
    const i=a.done.indexOf(no); if(i>=0){ a.done.splice(i,1); btn?.classList.remove('done'); } else { a.done.push(no); btn?.classList.add('done'); }
    a.done=a.done.sort((x,y)=>x-y); btn?.classList.add('selected');
  };
  window.saveContractVisitsOnlyV339=async function(){
    const d=window.__contractVisitDraftV339; if(!d) return msgx('لا توجد زيارات للحفظ','err');
    const api=window.__contractSmartV299; if(!api) return msgx('تعذر الوصول لبيانات العقد','err');
    await api.persistSmart(d.projectId,d.payload);
    msgx('تم حفظ تنفيذ الزيارات');
    if(typeof renderContracts==='function') renderContracts();
  };

  /* خدمات العقود العامة: الإداري عرض فقط، مدير النظام عرض وتعديل */
  function svcById(id){ try{return serviceById(id)}catch(_){return (ds().contractServices||[]).find(s=>S(s.id)===S(id));} }
  function svcProjectName(s){ try{return csProjectName(s)}catch(_){return s.project_name || hProject(s.project_id);} }
  function svcSupervisorName(s){ try{return csSupervisorName(s)}catch(_){return s.supervisor_name || hSupervisor(s.supervisor_id);} }
  function svcName(s){ try{return csServiceName(s)}catch(_){return s.service_name||'-';} }
  function svcType(s){ try{return csServiceType(s)}catch(_){return s.service_type||s.service_name||'-';} }
  function svcFreq(s){ try{return csFrequency(s)}catch(_){return s.frequency||'-';} }
  function svcVisits(s){ try{return csVisits(s)}catch(_){return N(s.visit_count||1)||1;} }
  function svcDone(s){ try{return csDone(s)}catch(_){return N(s.executed_count||0);} }
  function svcRemaining(s){ try{return csRemaining(s)}catch(_){return Math.max(svcVisits(s)-svcDone(s),0);} }
  function svcLast(s){ try{return csLastDate(s)}catch(_){return S(s.last_execution_date);} }
  function svcDue(s){ try{return csDueDate(s)}catch(_){return S(s.next_due_date);} }
  function svcNotes(s){ try{return csNotes(s)}catch(_){return S(s.notes);} }
  function svcStatusKey(s){ try{return csStatusKey(s)}catch(_){return svcRemaining(s)<=0?'done':'due';} }
  function svcStatusText(s){ try{return csStatusText(svcStatusKey(s))}catch(_){return S(s.status||'مستحقة');} }
  function svcBadge(s){ try{return csBadgeClass(svcStatusKey(s))}catch(_){return svcRemaining(s)<=0?'green':'amber';} }

  window.renderContractServices = function(){
    const body=$('contractServicesBody'); if(!body) return;
    try{ hydrateServiceTypes && hydrateServiceTypes(); }catch(_){ }
    try{ financeHydrateForms && financeHydrateForms(); }catch(_){ }
    const rows=(typeof getFilteredContractServices==='function'?getFilteredContractServices():(ds().contractServices||[]));
    const all=ds().contractServices||[]; const counts={total:all.length,due:0,soon:0,done:0,late:0,review:0};
    all.forEach(s=>{const k=svcStatusKey(s); if(k==='done')counts.done++; else if(k==='late')counts.late++; else if(k==='soon')counts.soon++; else if(k==='review')counts.review++; else counts.due++;});
    [['Total','total'],['Due','due'],['Done','done'],['Soon','soon'],['Late','late'],['Review','review']].forEach(([a,b])=>{const el=$('services'+a+'Count'); if(el) el.textContent=counts[b]||0;});
    const emptyMsg=ds().contractServicesError?('تعذر تحميل الخدمات: '+esc(ds().contractServicesError)):'لا توجد خدمات مسجلة.';
    body.innerHTML=rows.slice(0,500).map(s=>{ const id=Number(s.id)||0; const actions=isAdminArea()?`<button class="light" onclick="openContractServiceViewV339(${id})">عرض</button>${isSystemAdmin()?`<button onclick="editContractService(${id})">تعديل</button>`:''}`:'-'; return `<tr><td><b>${esc(svcProjectName(s))}</b></td><td>${esc(svcSupervisorName(s))}</td><td><b>${esc(svcName(s))}</b></td><td>${esc(svcType(s))}</td><td>${esc(svcFreq(s))}</td><td>${svcVisits(s)}</td><td>${esc(s.executor_name||'-')}</td><td>${svcDone(s)}</td><td>${svcRemaining(s)}</td><td>${esc(svcLast(s)||'-')}</td><td>${esc(svcDue(s)||'-')}</td><td><span class="badge ${svcBadge(s)}">${esc(svcStatusText(s))}</span></td><td>${esc(svcNotes(s)||'-')}</td><td class="row-actions">${actions}</td></tr>`; }).join('') || `<tr><td colspan="14">${emptyMsg}</td></tr>`;
    try{ renderSmartServicesList && renderSmartServicesList(); }catch(_){ }
    try{ showSupervisorServicesPreview && showSupervisorServicesPreview(false); }catch(_){ }
  };

  window.openContractServiceViewV339=function(id){
    ensureStyle();
    const s=svcById(id); if(!s) return msgx('الخدمة غير موجودة','err');
    let modal=$('contractServiceModalV339'); if(!modal){ modal=document.createElement('div'); modal.id='contractServiceModalV339'; modal.className='modal-v338 hidden'; document.body.appendChild(modal); }
    const visits=Math.max(1,svcVisits(s)), done=svcDone(s);
    const chips=Array.from({length:visits},(_,i)=>{ const no=i+1; return `<button type="button" class="visit-chip-v339 ${no<=done?'done':''}" data-no="${no}" onclick="selectServiceVisitV339(${id},${no},this)">${no}</button>`; }).join('');
    modal.innerHTML=`<div class="modal-card-v338"><div class="modal-head-v338"><h2>عرض الخدمة التعاقدية</h2><button class="light" onclick="closeContractServiceViewV339()">إغلاق</button></div><div class="modal-body-v338">
      <h2>${esc(svcName(s))}</h2>
      <div class="service-view-grid-v338">
        <div class="service-box-v338"><small>المشروع</small><b>${esc(svcProjectName(s))}</b></div>
        <div class="service-box-v338"><small>المشرف</small><b>${esc(svcSupervisorName(s))}</b></div>
        <div class="service-box-v338"><small>نوع الخدمة</small><b>${esc(svcType(s))}</b></div>
        <div class="service-box-v338"><small>التكرار</small><b>${esc(svcFreq(s))}</b></div>
        <div class="service-box-v338"><small>عدد الزيارات</small><b>${visits}</b></div>
        <div class="service-box-v338"><small>المنفذ حاليًا</small><b id="svcDonePreviewV339">${done}</b></div>
        <div class="service-box-v338"><small>المتبقي</small><b>${svcRemaining(s)}</b></div>
        <div class="service-box-v338"><small>الحالة</small><b>${esc(svcStatusText(s))}</b></div>
        <div class="service-box-v338"><small>آخر تنفيذ</small><b>${esc(svcLast(s)||'-')}</b></div>
        <div class="service-box-v338"><small>الاستحقاق القادم</small><b>${esc(svcDue(s)||'-')}</b></div>
      </div>
      <div class="visit-save-panel-v339">
        <b>أرقام الزيارات:</b><div style="margin:8px 0">${chips}</div>
        <div class="grid"><label>تاريخ التنفيذ<input id="svcExecDateV339" type="date" value="${today()}"></label><label>اسم المنفذ<input id="svcExecutorV339" value="${esc(s.executor_name||'')}"></label><label>ملاحظة<input id="svcNoteV339" value=""></label></div>
        <p class="muted-v339">اضغط رقم الزيارة المنفذة ثم اضغط حفظ. الإداري لا يظهر له تعديل بيانات الخدمة.</p>
        <button onclick="saveServiceVisitV339(${id})">حفظ</button>
      </div>
    </div></div>`;
    window.__serviceVisitDraftV339={id,done};
    modal.classList.remove('hidden');
  };
  window.closeContractServiceViewV339=function(){ $('contractServiceModalV339')?.classList.add('hidden'); };
  window.selectServiceVisitV339=function(id,no,btn){
    const d=window.__serviceVisitDraftV339||{id,done:0}; d.id=id; d.done=Math.max(N(d.done),N(no)); window.__serviceVisitDraftV339=d;
    document.querySelectorAll('#contractServiceModalV339 .visit-chip-v339').forEach(b=>{ const n=N(b.dataset.no); b.classList.toggle('done',n<=d.done); b.classList.toggle('selected',n===N(no)); });
    const pr=$('svcDonePreviewV339'); if(pr) pr.textContent=d.done;
  };
  window.saveServiceVisitV339=async function(id){
    if(!isAdminArea()) return msgx('غير مسموح','err');
    const s=svcById(id); if(!s) return msgx('الخدمة غير موجودة','err');
    const done=Math.max(N(window.__serviceVisitDraftV339?.done), svcDone(s));
    const visits=Math.max(svcVisits(s), done); const remaining=Math.max(visits-done,0);
    const d=$('svcExecDateV339')?.value || today(); const executor=$('svcExecutorV339')?.value || S(s.executor_name||''); const note=$('svcNoteV339')?.value || '';
    const notes=[svcNotes(s), note].filter(Boolean).join(' / '); const status=remaining>0?'مستحقة':'منجزة';
    const {error}=await sb.from('contract_services').update({last_execution_date:d,executed_count:done,remaining_count:remaining,visit_count:visits,executor_name:executor,notes,status,next_due_date:remaining>0?svcDue(s)||null:null,updated_at:new Date().toISOString()}).eq('id',id);
    if(error) return msgx(error.message,'err');
    msgx('تم حفظ تنفيذ الزيارة');
    try{ await refreshAll(); }catch(_){ }
    setTimeout(()=>openContractServiceViewV339(id),150);
  };

  window.TASNEEF_BUILD='V339_CONTRACT_ROLE_VISITS_2026_06_04';
  console.log('Tasneef V339 contract role/visit workflow loaded');
})();

/* ===== Tasneef v340 - Fix monthly attendance supervisor filter + supervisor attendance missing workers ===== */
(function(){
  const FIX_VERSION='v340-attendance-supervisor-filter-save';
  const $ = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const N = v => Number(v || 0);
  const esc = s => S(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const ds = () => window.data || {workers:[],projects:[],attendance:[],supervisors:[],users:[],workerAssignments:[]};
  const todayStr = () => (typeof today==='function' ? today() : new Date().toISOString().slice(0,10));
  const monthNow = () => todayStr().slice(0,7);
  const sid = v => v==null ? '' : String(v);
  function supName(id){ try{return supervisorName(id)||'-';}catch(_){ const s=(ds().supervisors||ds().users||[]).find(x=>sid(x.id)===sid(id)); return s?.full_name||s?.username||'-'; } }
  function projName(id){ try{return projectName(id)||'-';}catch(_){ const p=(ds().projects||[]).find(x=>sid(x.id)===sid(id)); return p?.name||'-'; } }
  function workerNameSafe(id){ try{return workerName(id)||'';}catch(_){ const w=(ds().workers||[]).find(x=>sid(x.id)===sid(id)); return w?.name||w?.full_name||''; } }
  function directWorkerSup(w){ try{return workerSupId(w)||'';}catch(_){ return w?.app_supervisor_id || w?.supervisor_id || ''; } }
  function directWorkerProject(w){ try{return workerProjectId(w)||'';}catch(_){ return w?.project_id || w?.assigned_project_id || ''; } }
  function projectRow(pid){ return (ds().projects||[]).find(p=>sid(p.id)===sid(pid)) || {}; }
  function activeWorker(w){ return !['inactive','deleted','موقوف'].includes(S(w?.status || 'active').toLowerCase()); }
  function assignmentsForWorker(wid){ return (ds().workerAssignments||[]).filter(a=>sid(a.worker_id)===sid(wid) && a.is_active!==false); }
  function workerProjectIds(w){
    const ids=new Set(); const primary=directWorkerProject(w); if(primary) ids.add(sid(primary));
    assignmentsForWorker(w?.id).forEach(a=>{ if(a.project_id) ids.add(sid(a.project_id)); });
    return [...ids];
  }
  function workerSupervisorIds(w){
    const ids=new Set(); const direct=directWorkerSup(w); if(direct) ids.add(sid(direct));
    workerProjectIds(w).forEach(pid=>{ const p=projectRow(pid); if(p.supervisor_id) ids.add(sid(p.supervisor_id)); });
    return [...ids];
  }
  function attendanceProject(a){ return a?.project_id || directWorkerProject((ds().workers||[]).find(w=>sid(w.id)===sid(a?.worker_id))) || ''; }
  function attendanceSupervisor(a){
    if(a?.supervisor_id) return a.supervisor_id;
    const w=(ds().workers||[]).find(x=>sid(x.id)===sid(a?.worker_id));
    if(w){ const d=directWorkerSup(w); if(d) return d; }
    const p=projectRow(attendanceProject(a));
    return p.supervisor_id || '';
  }
  function workerMatchesSupervisor(w, supervisorId){
    if(!supervisorId) return true;
    if(workerSupervisorIds(w).includes(sid(supervisorId))) return true;
    return (ds().attendance||[]).some(a=>sid(a.worker_id)===sid(w.id) && sid(attendanceSupervisor(a))===sid(supervisorId));
  }
  function firstProjectForWorkerAndSupervisor(w, supervisorId){
    const ids=workerProjectIds(w);
    const bySup=ids.find(pid=>sid(projectRow(pid).supervisor_id)===sid(supervisorId));
    return bySup || ids[0] || '';
  }
  function daysInMonth(month){ const [y,m]=S(month||monthNow()).split('-').map(Number); return new Date(y, m, 0).getDate(); }
  function dsOf(month,d){ return `${month}-${String(d).padStart(2,'0')}`; }
  function monthOf(a){ return S(a?.attendance_date).slice(0,7); }
  function setSupervisorOptionsKeep(){
    const el=$('attendanceMatrixSupervisor'); if(!el) return '';
    const old=el.value || '';
    const base=(ds().supervisors&&ds().supervisors.length?ds().supervisors:(ds().users||[]).filter(u=>u.role==='supervisor'))||[];
    const seen=new Set();
    const opts=base.filter(x=>x&&x.id!=null&&!seen.has(sid(x.id))&&(seen.add(sid(x.id)),true))
      .map(s=>`<option value="${esc(s.id)}">${esc(s.full_name||s.username||s.name||s.id)}</option>`).join('');
    el.innerHTML='<option value="">كل المشرفين</option>'+opts;
    if([...el.options].some(o=>o.value===old)) el.value=old;
    return el.value || '';
  }
  function monthlyWorkers(month, supervisorId, q){
    const map=new Map();
    (ds().workers||[]).filter(activeWorker).forEach(w=>{
      if(supervisorId && !workerMatchesSupervisor(w, supervisorId)) return;
      const name=S(w.name||w.full_name); if(q && !name.includes(q)) return;
      map.set(sid(w.id), {id:w.id, name:name||('عامل '+w.id), source:w});
    });
    (ds().attendance||[]).filter(a=>monthOf(a)===month).forEach(a=>{
      if(supervisorId && sid(attendanceSupervisor(a))!==sid(supervisorId)) return;
      const name=workerNameSafe(a.worker_id) || ('عامل رقم '+a.worker_id);
      if(q && !name.includes(q)) return;
      if(!map.has(sid(a.worker_id))){
        const w=(ds().workers||[]).find(x=>sid(x.id)===sid(a.worker_id));
        map.set(sid(a.worker_id), {id:a.worker_id, name, source:w||{id:a.worker_id,name}});
      }
    });
    return [...map.values()].sort((a,b)=>S(a.name).localeCompare(S(b.name),'ar'));
  }
  function recordFor(wid, date, supervisorId){
    const rows=(ds().attendance||[]).filter(a=>sid(a.worker_id)===sid(wid) && S(a.attendance_date)===S(date));
    return rows.find(a=>!supervisorId || sid(attendanceSupervisor(a))===sid(supervisorId)) || rows[0] || null;
  }

  window.renderAttendanceMonthly = function(){
    const body=$('attendanceMatrixBody'); if(!body) return;
    const monthEl=$('attendanceMatrixMonth'); if(monthEl && !monthEl.value) monthEl.value=monthNow();
    const month=monthEl?.value || monthNow();
    const supervisorId=setSupervisorOptionsKeep();
    const q=S($('attendanceMatrixSearch')?.value);
    const days=daysInMonth(month);
    const workers=monthlyWorkers(month, supervisorId, q);
    const head=$('attendanceMatrixHead');
    if(head){ head.innerHTML='<tr><th>الاسم</th><th>النوع</th><th>المشرف</th><th>'+Array.from({length:days},(_,i)=>String(i+1).padStart(2,'0')).join('</th><th>')+'</th><th>حضور</th><th>غياب</th><th>النسبة</th></tr>'; }
    let totalPresent=0,totalAbsent=0;
    body.innerHTML=workers.map(wr=>{
      const w=wr.source||{}; let present=0, absent=0; const cells=[];
      for(let d=1; d<=days; d++){
        const rec=recordFor(wr.id, dsOf(month,d), supervisorId);
        if(rec?.status==='present'){ present++; cells.push('<td><span class="att-cell att-present">حاضر</span></td>'); }
        else if(rec?.status==='absent'){ absent++; cells.push('<td><span class="att-cell att-absent">غائب</span></td>'); }
        else cells.push('<td><span class="att-cell att-empty">لم يسجل</span></td>');
      }
      totalPresent+=present; totalAbsent+=absent;
      const pct=(present+absent)?present/(present+absent)*100:0;
      const supId=supervisorId || attendanceSupervisor((ds().attendance||[]).find(a=>sid(a.worker_id)===sid(wr.id)&&monthOf(a)===month)) || workerSupervisorIds(w)[0] || '';
      const type=(w.worker_type||'primary')==='support'?'بديل / مساند':'العامل فقط';
      return `<tr><td><b>${esc(wr.name)}</b></td><td>${esc(type)}</td><td>${esc(supName(supId))}</td>${cells.join('')}<td><b>${present}</b></td><td><b>${absent}</b></td><td><b>${pct.toFixed(1)}%</b></td></tr>`;
    }).join('') || `<tr><td colspan="${days+6}">لا توجد بيانات حسب الفلاتر المختارة</td></tr>`;
    const recorded=totalPresent+totalAbsent; const pct=recorded?totalPresent/recorded*100:0;
    const sum=$('attendanceMatrixSummary');
    if(sum) sum.innerHTML=`<div class="kpi"><small>عدد الأسماء</small><b>${workers.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalPresent}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalAbsent}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`;
  };

  window.renderSupervisorAttendanceList = function(){
    const div=$('supervisorAttendanceList'); if(!div) return;
    const u=(typeof session==='function'?session():null) || {};
    const supId=u.id;
    const workers=(ds().workers||[]).filter(w=>activeWorker(w) && workerMatchesSupervisor(w, supId));
    const seen=new Set();
    const unique=workers.filter(w=>{ const k=sid(w.id); if(seen.has(k)) return false; seen.add(k); return true; }).sort((a,b)=>S(a.name).localeCompare(S(b.name),'ar'));
    div.innerHTML=unique.map(w=>`<div class="quick-item"><b>${esc(w.name||w.full_name)}</b><small>${esc(projName(firstProjectForWorkerAndSupervisor(w,supId)))}</small><select data-worker="${esc(w.id)}" data-project="${esc(firstProjectForWorkerAndSupervisor(w,supId))}"><option value="present">حاضر</option><option value="absent">غائب</option></select></div>`).join('') || '<div class="quick-item">لا يوجد عمال مرتبطين بك. راجع ربط العامل بالمشروع أو المشرف من الإدارة.</div>';
  };

  window.saveSupervisorAttendance = async function(){
    const u=(typeof session==='function'?session():null) || {};
    const date=$('attendanceDate')?.value || todayStr();
    const selectedProject=$('attendanceProject')?.value || '';
    const rows=[...document.querySelectorAll('#supervisorAttendanceList select')].map(s=>{
      const wid=N(s.dataset.worker);
      const w=(ds().workers||[]).find(x=>sid(x.id)===sid(wid)) || {};
      const pid=selectedProject || s.dataset.project || firstProjectForWorkerAndSupervisor(w,u.id) || null;
      return {attendance_date:date, worker_id:wid, supervisor_id:u.id, project_id:pid?N(pid):null, status:s.value, created_by:u.id};
    }).filter(r=>r.worker_id);
    if(!rows.length) return (typeof msg==='function'?msg('لا توجد أسماء للحفظ','err'):alert('لا توجد أسماء للحفظ'));
    const {error}=await sb.from('attendance').upsert(rows,{onConflict:'attendance_date,worker_id'});
    if(error) return (typeof msg==='function'?msg(error.message,'err'):alert(error.message));
    if(typeof msg==='function') msg('تم حفظ التحضير لكل الأسماء الظاهرة');
    try{ await loadAll(); }catch(_){ }
    try{ renderSupervisorAttendanceList(); }catch(_){ }
  };

  const oldInitSupervisorV340=window.initSupervisor;
  window.initSupervisor = async function(){
    const u=(typeof requireRole==='function'?requireRole('supervisor'):session()); if(!u) return;
    try{ await loadAll(); }catch(e){ console.warn(e); if(typeof oldInitSupervisorV340==='function') return oldInitSupervisorV340(); }
    const supProjectIds=new Set((ds().projects||[]).filter(p=>sid(p.supervisor_id)===sid(u.id)).map(p=>sid(p.id)));
    data.projects=(ds().projects||[]).filter(p=>supProjectIds.has(sid(p.id)));
    data.workers=(ds().workers||[]).filter(w=>activeWorker(w) && (workerMatchesSupervisor(w,u.id) || workerProjectIds(w).some(pid=>supProjectIds.has(sid(pid)))));
    data.logs=(ds().logs||[]).filter(l=>sid(l.supervisor_id)===sid(u.id) || supProjectIds.has(sid(l.project_id)));
    data.attendance=(ds().attendance||[]).filter(a=>sid(attendanceSupervisor(a))===sid(u.id) || supProjectIds.has(sid(attendanceProject(a))));
    data.tickets=(ds().tickets||[]).filter(t=>sid(t.supervisor_id)===sid(u.id) || sid(t.created_by)===sid(u.id) || supProjectIds.has(sid(t.project_id)));
    if($('supTitle')) $('supTitle').textContent=`لوحة المشرف - ${u.full_name||u.username||''}`;
    try{ if(typeof fillSelect==='function'){ fillSelect('logProject',data.projects,'name','اختر المشروع'); fillSelect('attendanceProject',data.projects,'name','اختر المشروع'); fillSelect('ticketProject',data.projects,'name','اختر المشروع'); } }catch(_){ }
    if($('logDate')) $('logDate').value=todayStr(); if($('attendanceDate')) $('attendanceDate').value=todayStr();
    try{ renderSupervisorAttendanceList(); }catch(e){ console.warn(e); }
    try{ renderTimeLogs(); }catch(e){ console.warn(e); }
    try{ await supervisorInventoryLoad(); }catch(_){ }
  };

  const oldShowPageV340=window.showPage;
  window.showPage=function(id,btn){ if(typeof oldShowPageV340==='function') oldShowPageV340(id,btn); if(id==='attendance') setTimeout(()=>window.renderAttendanceMonthly&&window.renderAttendanceMonthly(),80); };
  document.addEventListener('change', e=>{ if(e.target && ['attendanceMatrixMonth','attendanceMatrixSupervisor'].includes(e.target.id)) setTimeout(()=>window.renderAttendanceMonthly&&window.renderAttendanceMonthly(),20); });
  document.addEventListener('input', e=>{ if(e.target && e.target.id==='attendanceMatrixSearch') setTimeout(()=>window.renderAttendanceMonthly&&window.renderAttendanceMonthly(),20); });

  window.TASNEEF_BUILD='V340_ATTENDANCE_SUPERVISOR_FILTER_SAVE_2026_06_04';
  console.log('Tasneef '+FIX_VERSION+' loaded');
})();

/* ===== Tasneef v341 - Final attendance monthly supervisor filter + reliable supervisor attendance save ===== */
(function(){
  'use strict';
  if(window.__tasneefV341AttendanceFinal) return;
  window.__tasneefV341AttendanceFinal = true;
  const FIX_VERSION='V341_ATTENDANCE_FINAL_FILTER_SAVE_2026_06_04';
  const $id = id => document.getElementById(id);
  const S = v => String(v ?? '').trim();
  const sid = v => S(v);
  const N = v => { const n=Number(v||0); return Number.isFinite(n)?n:0; };
  const esc = s => S(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const ds = () => window.data || {workers:[],projects:[],attendance:[],supervisors:[],users:[],workerAssignments:[]};
  const todayStr = () => (typeof today==='function' ? today() : new Date().toISOString().slice(0,10));
  const monthNow = () => todayStr().slice(0,7);
  function norm(v){ return S(v).toLowerCase().replace(/[إأآا]/g,'ا').replace(/[ىي]/g,'ي').replace(/ة/g,'ه').replace(/[\u064B-\u0652]/g,'').replace(/\s+/g,' ').trim(); }
  function projectRow(pid){ return (ds().projects||[]).find(p=>sid(p.id)===sid(pid)) || {}; }
  function workerRow(wid){ return (ds().workers||[]).find(w=>sid(w.id)===sid(wid)) || {}; }
  function supervisorRows(){
    const base=[];
    (ds().supervisors||[]).forEach(x=>base.push(x));
    (ds().users||[]).filter(u=>S(u.role)==='supervisor').forEach(x=>base.push(x));
    const seen=new Set();
    return base.filter(x=>x && x.id!=null && !seen.has(sid(x.id)) && (seen.add(sid(x.id)), true));
  }
  function supRow(id){ return supervisorRows().find(s=>sid(s.id)===sid(id)) || (ds().users||[]).find(u=>sid(u.id)===sid(id)) || {}; }
  function supName(id){ try{return supervisorName(id)||'-';}catch(_){ const s=supRow(id); return s.full_name||s.name||s.username||'-'; } }
  function projName(id){ try{return projectName(id)||'-';}catch(_){ const p=projectRow(id); return p.name||'-'; } }
  function workerNameSafe(id){ try{return workerName(id)||'';}catch(_){ const w=workerRow(id); return w.name||w.full_name||''; } }
  function workerTypeTextSafe(w){ try{return workerTypeText(w.worker_type||'primary');}catch(_){ return (w.worker_type==='support'?'بديل / مساند':'عامل'); } }
  function activeWorker(w){ return !['inactive','deleted','موقوف','محذوف'].includes(norm(w?.status || 'active')); }
  function directWorkerSup(w){ try{return workerSupId(w)||'';}catch(_){ return w?.app_supervisor_id || w?.supervisor_id || w?.assigned_supervisor_id || ''; } }
  function directWorkerProject(w){ try{return workerProjectId(w)||'';}catch(_){ return w?.project_id || w?.assigned_project_id || w?.current_project_id || ''; } }
  function assignmentsForWorker(wid){ return (ds().workerAssignments||[]).filter(a=>sid(a.worker_id)===sid(wid) && a.is_active!==false); }
  function workerProjectIds(w){
    const ids=new Set();
    const primary=directWorkerProject(w); if(primary) ids.add(sid(primary));
    assignmentsForWorker(w?.id).forEach(a=>{ if(a.project_id) ids.add(sid(a.project_id)); });
    return [...ids];
  }
  function supervisorTokens(supervisorId){
    const s=supRow(supervisorId);
    return new Set([sid(supervisorId), norm(s.full_name), norm(s.name), norm(s.username)].filter(Boolean));
  }
  function sameSupervisorValue(v, supervisorId){
    if(!supervisorId || !v) return false;
    const tokens=supervisorTokens(supervisorId);
    return tokens.has(sid(v)) || tokens.has(norm(v));
  }
  function projectMatchesSupervisor(pid, supervisorId){
    if(!supervisorId || !pid) return !supervisorId;
    const p=projectRow(pid);
    return sameSupervisorValue(p.supervisor_id, supervisorId) || sameSupervisorValue(p.supervisor_name, supervisorId) || sameSupervisorValue(p.supervisor, supervisorId);
  }
  function workerMatchesSupervisor(w, supervisorId){
    if(!supervisorId) return true;
    if(sameSupervisorValue(directWorkerSup(w), supervisorId) || sameSupervisorValue(w.supervisor_name, supervisorId)) return true;
    if(workerProjectIds(w).some(pid=>projectMatchesSupervisor(pid, supervisorId))) return true;
    return (ds().attendance||[]).some(a=>sid(a.worker_id)===sid(w.id) && attendanceMatchesSupervisor(a, supervisorId));
  }
  function attendanceProject(a){
    if(a?.project_id) return a.project_id;
    const w=workerRow(a?.worker_id);
    return directWorkerProject(w) || workerProjectIds(w)[0] || '';
  }
  function attendanceMatchesSupervisor(a, supervisorId){
    if(!supervisorId) return true;
    if(sameSupervisorValue(a?.supervisor_id, supervisorId) || sameSupervisorValue(a?.supervisor_name, supervisorId) || sameSupervisorValue(a?.supervisor, supervisorId)) return true;
    const w=workerRow(a?.worker_id);
    if(w && w.id!=null && workerMatchesSupervisorNoAttendance(w, supervisorId)) return true;
    const pid=attendanceProject(a);
    return projectMatchesSupervisor(pid, supervisorId);
  }
  function workerMatchesSupervisorNoAttendance(w, supervisorId){
    if(!supervisorId) return true;
    if(sameSupervisorValue(directWorkerSup(w), supervisorId) || sameSupervisorValue(w.supervisor_name, supervisorId)) return true;
    return workerProjectIds(w).some(pid=>projectMatchesSupervisor(pid, supervisorId));
  }
  function effectiveSupervisorForWorker(w, preferredSupervisorId){
    if(preferredSupervisorId) return preferredSupervisorId;
    const direct=directWorkerSup(w); if(direct) return direct;
    const pid=workerProjectIds(w)[0]; if(pid) return projectRow(pid).supervisor_id || '';
    return '';
  }
  function firstProjectForWorkerAndSupervisor(w, supervisorId){
    const ids=workerProjectIds(w);
    if(supervisorId){
      const bySup=ids.find(pid=>projectMatchesSupervisor(pid, supervisorId));
      if(bySup) return bySup;
    }
    return ids[0] || '';
  }
  function daysInMonth(month){ const [y,m]=S(month||monthNow()).split('-').map(Number); return new Date(y, m, 0).getDate(); }
  function dsOf(month,d){ return `${month}-${String(d).padStart(2,'0')}`; }
  function monthOf(a){ return S(a?.attendance_date).slice(0,7); }
  function statusOf(a){ const st=norm(a?.status); if(['present','حاضر','حضور'].includes(st)) return 'present'; if(['absent','غائب','غياب'].includes(st)) return 'absent'; return S(a?.status||''); }
  function setSupervisorOptionsKeep(){
    const el=$id('attendanceMatrixSupervisor'); if(!el) return '';
    const old=el.value || '';
    const opts=supervisorRows().map(s=>`<option value="${esc(s.id)}">${esc(s.full_name||s.name||s.username||s.id)}</option>`).join('');
    const next='<option value="">كل المشرفين</option>'+opts;
    if(el.dataset.v341Html!==next){ el.innerHTML=next; el.dataset.v341Html=next; }
    if([...el.options].some(o=>o.value===old)) el.value=old;
    return el.value || '';
  }
  function monthlyWorkers(month, supervisorId, q){
    const map=new Map();
    (ds().workers||[]).filter(activeWorker).forEach(w=>{
      if(supervisorId && !workerMatchesSupervisor(w, supervisorId)) return;
      const name=S(w.name||w.full_name); if(q && !norm(name).includes(norm(q))) return;
      map.set(sid(w.id), {id:w.id, name:name||('عامل '+w.id), source:w});
    });
    // Add any names that were saved in attendance even if worker/project link is incomplete.
    (ds().attendance||[]).filter(a=>monthOf(a)===month).forEach(a=>{
      if(supervisorId && !attendanceMatchesSupervisor(a, supervisorId)) return;
      const name=workerNameSafe(a.worker_id) || S(a.worker_name) || ('عامل رقم '+a.worker_id);
      if(q && !norm(name).includes(norm(q))) return;
      if(!map.has(sid(a.worker_id))){
        const w=workerRow(a.worker_id);
        map.set(sid(a.worker_id), {id:a.worker_id, name, source:(w&&w.id!=null?w:{id:a.worker_id,name})});
      }
    });
    return [...map.values()].sort((a,b)=>S(a.name).localeCompare(S(b.name),'ar'));
  }
  function recordFor(wid, date, supervisorId){
    const rows=(ds().attendance||[]).filter(a=>sid(a.worker_id)===sid(wid) && S(a.attendance_date)===S(date));
    if(supervisorId){
      const exact=rows.find(a=>attendanceMatchesSupervisor(a, supervisorId));
      if(exact) return exact;
    }
    return rows[0] || null;
  }
  window.renderAttendanceMonthly = function(){
    const body=$id('attendanceMatrixBody'); if(!body) return;
    const monthEl=$id('attendanceMatrixMonth'); if(monthEl && !monthEl.value) monthEl.value=monthNow();
    const month=monthEl?.value || monthNow();
    const supervisorId=setSupervisorOptionsKeep();
    const q=S($id('attendanceMatrixSearch')?.value);
    const days=daysInMonth(month);
    const workers=monthlyWorkers(month, supervisorId, q);
    const head=$id('attendanceMatrixHead');
    if(head){ head.innerHTML='<tr><th>الاسم</th><th>النوع</th><th>المشرف</th>'+Array.from({length:days},(_,i)=>`<th>${String(i+1).padStart(2,'0')}</th>`).join('')+'<th>حضور</th><th>غياب</th><th>النسبة</th></tr>'; }
    let totalPresent=0,totalAbsent=0;
    body.innerHTML=workers.map(wr=>{
      const w=wr.source||{}; let present=0, absent=0; const cells=[];
      for(let d=1; d<=days; d++){
        const rec=recordFor(wr.id, dsOf(month,d), supervisorId);
        const st=statusOf(rec);
        if(st==='present'){ present++; cells.push('<td><span class="att-cell att-present">ح</span></td>'); }
        else if(st==='absent'){ absent++; cells.push('<td><span class="att-cell att-absent">غ</span></td>'); }
        else cells.push('<td><span class="att-cell att-empty">-</span></td>');
      }
      totalPresent+=present; totalAbsent+=absent;
      const pct=(present+absent)?present/(present+absent)*100:0;
      const supId=effectiveSupervisorForWorker(w, supervisorId) || (ds().attendance||[]).find(a=>sid(a.worker_id)===sid(wr.id)&&monthOf(a)===month)?.supervisor_id || '';
      const cls=pct>=90?'green':(pct>=70?'amber':'red');
      return `<tr><td><b>${esc(wr.name)}</b></td><td>${esc(workerTypeTextSafe(w))}</td><td>${esc(supName(supId))}</td>${cells.join('')}<td><span class="badge green">${present}</span></td><td><span class="badge red">${absent}</span></td><td><span class="badge ${cls}">${pct.toFixed(1)}%</span></td></tr>`;
    }).join('') || `<tr><td colspan="${days+6}">لا توجد بيانات حسب الفلاتر المختارة</td></tr>`;
    const recorded=totalPresent+totalAbsent; const pct=recorded?totalPresent/recorded*100:0;
    const sum=$id('attendanceMatrixSummary');
    if(sum) sum.innerHTML=`<div class="kpi"><small>عدد الأسماء</small><b>${workers.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalPresent}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalAbsent}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`;
  };

  window.renderSupervisorAttendanceList = function(){
    const div=$id('supervisorAttendanceList'); if(!div) return;
    const u=(typeof session==='function'?session():null) || {};
    const supId=u.id;
    const workers=(ds().workers||[]).filter(w=>activeWorker(w) && workerMatchesSupervisor(w, supId));
    const seen=new Set();
    const unique=workers.filter(w=>{ const k=sid(w.id); if(!k || seen.has(k)) return false; seen.add(k); return true; }).sort((a,b)=>S(a.name||a.full_name).localeCompare(S(b.name||b.full_name),'ar'));
    div.innerHTML=unique.map(w=>{
      const pid=firstProjectForWorkerAndSupervisor(w,supId);
      return `<div class="quick-item"><b>${esc(w.name||w.full_name)}</b><small>${esc(projName(pid))}</small><select data-worker="${esc(w.id)}" data-project="${esc(pid)}"><option value="present">حاضر</option><option value="absent">غائب</option></select></div>`;
    }).join('') || '<div class="quick-item">لا يوجد عمال مرتبطين بك. اربط العامل بالمشرف أو بالمشروع التابع للمشرف من الإدارة.</div>';
  };

  window.saveSupervisorAttendance = async function(){
    const u=(typeof session==='function'?session():null) || {};
    if(!u.id) return (typeof msg==='function'?msg('سجّل الدخول أولاً','err'):alert('سجّل الدخول أولاً'));
    const date=$id('attendanceDate')?.value || todayStr();
    const selectedProject=$id('attendanceProject')?.value || '';
    const selects=[...document.querySelectorAll('#supervisorAttendanceList select[data-worker]')];
    if(!selects.length) return (typeof msg==='function'?msg('لا توجد أسماء للحفظ','err'):alert('لا توجد أسماء للحفظ'));
    let ok=0, fail=0, lastErr='';
    for(const s of selects){
      const wid=N(s.dataset.worker); if(!wid) continue;
      const w=workerRow(wid);
      const pid=selectedProject || s.dataset.project || firstProjectForWorkerAndSupervisor(w,u.id) || null;
      const row={attendance_date:date, worker_id:wid, supervisor_id:N(u.id)||u.id, project_id:pid?N(pid):null, status:s.value, notes:'', created_by:N(u.id)||u.id};
      try{
        // تحديث الموجود أولاً، ثم إدخال جديد. هذا أكثر ثباتًا من upsert لأن بعض قواعد البيانات لا تملك unique constraint واضح.
        let q=sb.from('attendance').select('id').eq('attendance_date',date).eq('worker_id',wid).limit(1);
        const found=await q;
        if(found.error) throw found.error;
        if(found.data && found.data.length){
          const res=await sb.from('attendance').update(row).eq('id',found.data[0].id);
          if(res.error) throw res.error;
        }else{
          const res=await sb.from('attendance').insert([row]);
          if(res.error) throw res.error;
        }
        ok++;
      }catch(e){ fail++; lastErr=e?.message||String(e); console.error('attendance save failed', row, e); }
    }
    if(fail){
      const text=`تم حفظ ${ok} اسم، وفشل حفظ ${fail} اسم. السبب: ${lastErr}`;
      return (typeof msg==='function'?msg(text,'err'):alert(text));
    }
    if(typeof msg==='function') msg(`تم حفظ التحضير لعدد ${ok} اسم`); else alert(`تم حفظ التحضير لعدد ${ok} اسم`);
    try{ await loadAll(); }catch(_){ }
    try{ renderSupervisorAttendanceList(); }catch(_){ }
    try{ renderAttendanceMonthly(); }catch(_){ }
  };

  const oldShowPage=window.showPage;
  window.showPage=function(id,btn){ if(typeof oldShowPage==='function') oldShowPage(id,btn); if(id==='attendance') setTimeout(()=>window.renderAttendanceMonthly&&window.renderAttendanceMonthly(),120); };
  document.addEventListener('change', e=>{ if(e.target && ['attendanceMatrixMonth','attendanceMatrixSupervisor'].includes(e.target.id)) setTimeout(()=>window.renderAttendanceMonthly&&window.renderAttendanceMonthly(),30); });
  document.addEventListener('input', e=>{ if(e.target && e.target.id==='attendanceMatrixSearch') setTimeout(()=>window.renderAttendanceMonthly&&window.renderAttendanceMonthly(),30); });
  document.addEventListener('DOMContentLoaded',()=>setTimeout(()=>{ try{ if($id('attendanceMatrixBody')) renderAttendanceMonthly(); }catch(e){console.warn(e);} },1200));
  window.TASNEEF_BUILD=FIX_VERSION;
  console.log('Tasneef '+FIX_VERSION+' loaded');
})();

/* ===== Tasneef v342 - Attendance monthly must match attendance records exactly ===== */
(function(){
  'use strict';
  if(window.__tasneefV342AttendanceRecordsExact) return;
  window.__tasneefV342AttendanceRecordsExact = true;
  const BUILD='V342_ATTENDANCE_RECORDS_EXACT_2026_06_04';
  const $id=id=>document.getElementById(id);
  const A=v=>Array.isArray(v)?v:[];
  const S=v=>String(v ?? '').trim();
  const sid=v=>S(v);
  const esc=s=>S(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const norm=v=>S(v).toLowerCase().replace(/[إأآا]/g,'ا').replace(/[ىي]/g,'ي').replace(/ة/g,'ه').replace(/[\u064B-\u0652]/g,'').replace(/\s+/g,' ').trim();
  const ds=()=>window.data || {attendance:[],workers:[],projects:[],supervisors:[],users:[]};
  const todayStr=()=>{try{return today()}catch(_){return new Date().toISOString().slice(0,10)}};
  const monthNow=()=>todayStr().slice(0,7);
  function daysInMonth(month){const [y,m]=S(month||monthNow()).split('-').map(Number);return new Date(y,m,0).getDate();}
  function dateOf(month,d){return `${month}-${String(d).padStart(2,'0')}`;}
  function monthOf(a){return S(a.attendance_date || a.date || a.created_at).slice(0,7);}
  function workerRow(id){return A(ds().workers).find(w=>sid(w.id)===sid(id))||{};}
  function projectRow(id){return A(ds().projects).find(p=>sid(p.id)===sid(id))||{};}
  function workerNameExact(a){const w=workerRow(a.worker_id);return S(w.name||w.full_name||a.worker_name||a.name)||('عامل رقم '+S(a.worker_id||a.id||''));}
  function workerTypeExact(a){const w=workerRow(a.worker_id);try{return workerTypeText(w.worker_type||'primary')}catch(_){return w.worker_type==='support'?'بديل / مساند':'عامل'}}
  function projectIdExact(a){const w=workerRow(a.worker_id);let pid=a.project_id||w.project_id||w.assigned_project_id||w.current_project_id||'';return pid;}
  function projectNameExact(id){try{return projectName(id)||'-'}catch(_){return projectRow(id).name||'-'}}
  function allSupervisorRows(){
    const rows=[]; A(ds().supervisors).forEach(x=>rows.push(x)); A(ds().users).filter(u=>S(u.role)==='supervisor').forEach(x=>rows.push(x));
    const seen=new Set();
    return rows.filter(x=>x&&x.id!=null&&!seen.has(sid(x.id))&&(seen.add(sid(x.id)),true));
  }
  function supRow(id){return allSupervisorRows().find(s=>sid(s.id)===sid(id)) || A(ds().users).find(u=>sid(u.id)===sid(id)) || {};}
  function supNameExact(id){try{return supervisorName(id)||'-'}catch(_){const s=supRow(id);return s.full_name||s.name||s.username||'-'}}
  function supTokens(id){const s=supRow(id);return new Set([sid(id),norm(s.full_name),norm(s.name),norm(s.username)].filter(Boolean));}
  function sameSup(v,id){if(!id||!v)return false;const t=supTokens(id);return t.has(sid(v))||t.has(norm(v));}
  function recordSupervisorId(a){
    if(a.supervisor_id) return a.supervisor_id;
    const w=workerRow(a.worker_id);
    if(w.app_supervisor_id||w.supervisor_id||w.assigned_supervisor_id) return w.app_supervisor_id||w.supervisor_id||w.assigned_supervisor_id;
    const p=projectRow(projectIdExact(a));
    return p.supervisor_id||'';
  }
  function recordMatchesSupervisor(a,selected){
    if(!selected) return true;
    if(sameSup(a.supervisor_id,selected)||sameSup(a.supervisor_name,selected)||sameSup(a.supervisor,selected)) return true;
    const w=workerRow(a.worker_id);
    if(sameSup(w.app_supervisor_id||w.supervisor_id||w.assigned_supervisor_id,selected)||sameSup(w.supervisor_name,selected)) return true;
    const p=projectRow(projectIdExact(a));
    return sameSup(p.supervisor_id,selected)||sameSup(p.supervisor_name,selected)||sameSup(p.supervisor,selected);
  }
  function statusOf(a){const st=norm(a&&a.status); if(['present','حاضر','حضور','1','true'].includes(st))return 'present'; if(['absent','غائب','غياب','0','false'].includes(st))return 'absent'; return '';}
  function setSupervisorOptionsExact(){
    const el=$id('attendanceMatrixSupervisor'); if(!el)return '';
    const old=el.value||'';
    const attendanceSupIds=new Set(A(ds().attendance).map(recordSupervisorId).filter(Boolean).map(sid));
    const rows=allSupervisorRows().filter(s=>attendanceSupIds.size===0 || attendanceSupIds.has(sid(s.id)) || true);
    const opts=rows.map(s=>`<option value="${esc(s.id)}">${esc(s.full_name||s.name||s.username||s.id)}</option>`).join('');
    const html='<option value="">كل المشرفين</option>'+opts;
    if(el.dataset.v342Html!==html){el.innerHTML=html;el.dataset.v342Html=html;}
    if([...el.options].some(o=>o.value===old)) el.value=old;
    return el.value||'';
  }
  function latestRecordPerWorkerDate(records){
    const map=new Map();
    records.forEach(a=>{
      const wid=sid(a.worker_id || a.worker_name || a.name || a.id);
      const dt=S(a.attendance_date || a.date || '').slice(0,10);
      if(!wid||!dt)return;
      const key=wid+'|'+dt;
      const old=map.get(key);
      if(!old || Number(a.id||0)>=Number(old.id||0)) map.set(key,a);
    });
    return [...map.values()];
  }
  function groupRows(records,q){
    const map=new Map();
    records.forEach(a=>{
      const key=sid(a.worker_id || a.worker_name || a.name || a.id);
      if(!key)return;
      const name=workerNameExact(a);
      if(q && !norm(name).includes(norm(q))) return;
      if(!map.has(key)) map.set(key,{key, worker_id:a.worker_id, name, sample:a, records:[]});
      map.get(key).records.push(a);
    });
    return [...map.values()].sort((x,y)=>S(x.name).localeCompare(S(y.name),'ar'));
  }
  window.renderAttendanceMonthly=function(){
    const body=$id('attendanceMatrixBody'), head=$id('attendanceMatrixHead'); if(!body||!head)return;
    const monthEl=$id('attendanceMatrixMonth'); if(monthEl&&!monthEl.value) monthEl.value=monthNow();
    const month=monthEl?.value||monthNow();
    const selectedSup=setSupervisorOptionsExact();
    const q=S($id('attendanceMatrixSearch')?.value);
    const days=daysInMonth(month);
    const monthRecords=latestRecordPerWorkerDate(A(ds().attendance).filter(a=>monthOf(a)===month && recordMatchesSupervisor(a,selectedSup)));
    const groups=groupRows(monthRecords,q);
    head.innerHTML='<tr><th>الاسم</th><th>النوع</th><th>المشرف</th><th>المشروع</th>'+Array.from({length:days},(_,i)=>`<th>${String(i+1).padStart(2,'0')}</th>`).join('')+'<th>حضور</th><th>غياب</th><th>النسبة</th></tr>';
    let totalP=0,totalA=0;
    body.innerHTML=groups.map(g=>{
      let p=0,a=0; const byDate=new Map(g.records.map(r=>[S(r.attendance_date||r.date).slice(0,10),r]));
      const cells=[];
      for(let d=1;d<=days;d++){
        const rec=byDate.get(dateOf(month,d)); const st=statusOf(rec);
        if(st==='present'){p++; cells.push('<td title="حاضر"><span class="att-cell att-present">ح</span></td>');}
        else if(st==='absent'){a++; cells.push('<td title="غائب"><span class="att-cell att-absent">غ</span></td>');}
        else cells.push('<td title="لا يوجد سجل"><span class="att-cell att-empty">-</span></td>');
      }
      totalP+=p; totalA+=a; const pct=(p+a)?(p/(p+a))*100:0; const cls=pct>=90?'green':(pct>=70?'amber':'red');
      const supId=selectedSup || recordSupervisorId(g.sample); const pid=projectIdExact(g.sample);
      return `<tr><td><b>${esc(g.name)}</b></td><td>${esc(workerTypeExact(g.sample))}</td><td>${esc(supNameExact(supId))}</td><td>${esc(projectNameExact(pid))}</td>${cells.join('')}<td><span class="badge green">${p}</span></td><td><span class="badge red">${a}</span></td><td><span class="badge ${cls}">${pct.toFixed(1)}%</span></td></tr>`;
    }).join('') || `<tr><td colspan="${days+7}">لا توجد سجلات حضور مطابقة للفلاتر المختارة</td></tr>`;
    const recorded=totalP+totalA, pct=recorded?(totalP/recorded*100):0;
    const sum=$id('attendanceMatrixSummary');
    if(sum) sum.innerHTML=`<div class="kpi"><small>عدد الأسماء من سجلات الحضور</small><b>${groups.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalP}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalA}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`;
  };
  // حفظ حضور المشرف: حفظ واحد واحد مع المشروع والمشرف، ثم إعادة تحميل السجلات حتى يطابق الجدول القاعدة مباشرة.
  const previousSave=window.saveSupervisorAttendance;
  window.saveSupervisorAttendance=async function(){
    const u=(typeof session==='function'?session():null)||{};
    if(!u.id) return typeof msg==='function'?msg('سجّل الدخول أولاً','err'):alert('سجّل الدخول أولاً');
    const date=$id('attendanceDate')?.value||todayStr();
    const selectedProject=$id('attendanceProject')?.value||'';
    const selects=[...document.querySelectorAll('#supervisorAttendanceList select[data-worker]')];
    if(!selects.length) return typeof msg==='function'?msg('لا توجد أسماء للحفظ','err'):alert('لا توجد أسماء للحفظ');
    let ok=0,fail=0,lastErr='';
    for(const s of selects){
      const wid=Number(s.dataset.worker||0); if(!wid) continue;
      const w=workerRow(wid); const pid=selectedProject || s.dataset.project || w.project_id || w.assigned_project_id || '';
      const row={attendance_date:date,worker_id:wid,supervisor_id:Number(u.id)||u.id,project_id:pid?Number(pid):null,status:s.value||'present',notes:'',created_by:Number(u.id)||u.id};
      try{
        const found=await sb.from('attendance').select('id').eq('attendance_date',date).eq('worker_id',wid).limit(1);
        if(found.error) throw found.error;
        const res=(found.data&&found.data.length)?await sb.from('attendance').update(row).eq('id',found.data[0].id):await sb.from('attendance').insert([row]);
        if(res.error) throw res.error; ok++;
      }catch(e){fail++; lastErr=e?.message||String(e); console.error('V342 attendance save failed',row,e);}
    }
    if(fail) return typeof msg==='function'?msg(`تم حفظ ${ok} اسم، وفشل ${fail}. السبب: ${lastErr}`,'err'):alert(`تم حفظ ${ok} اسم، وفشل ${fail}. السبب: ${lastErr}`);
    if(typeof msg==='function') msg(`تم حفظ التحضير لعدد ${ok} اسم`);
    try{await loadAll();}catch(_){try{await refreshAll();}catch(__){}}
    try{window.renderSupervisorAttendanceList&&window.renderSupervisorAttendanceList();}catch(_){ }
    try{window.renderAttendanceMonthly&&window.renderAttendanceMonthly();}catch(_){ }
  };
  document.addEventListener('change',e=>{if(e.target&&['attendanceMatrixMonth','attendanceMatrixSupervisor'].includes(e.target.id))setTimeout(()=>window.renderAttendanceMonthly&&window.renderAttendanceMonthly(),20);});
  document.addEventListener('input',e=>{if(e.target&&e.target.id==='attendanceMatrixSearch')setTimeout(()=>window.renderAttendanceMonthly&&window.renderAttendanceMonthly(),20);});
  document.addEventListener('DOMContentLoaded',()=>setTimeout(()=>{try{window.renderAttendanceMonthly&&window.renderAttendanceMonthly();}catch(e){console.warn(e)}},1500));
  window.TASNEEF_BUILD=BUILD;
  console.log('Tasneef '+BUILD+' loaded');
})();

/* ===== V343: Beautiful attendance workflow - daily source of truth ===== */
(function(){
  const BUILD='v343-attendance-beautiful-daily-records';
  if(window.__tasneefV343AttendanceBeautiful) return;
  window.__tasneefV343AttendanceBeautiful = true;
  const S=v=>String(v??'').trim();
  const N=v=>{const n=Number(v);return Number.isFinite(n)?n:null};
  const A=v=>Array.isArray(v)?v:[];
  const $id=id=>document.getElementById(id);
  const E=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const ds=()=> (typeof data!=='undefined'?data:(window.data||{}));
  const todayStr=()=> (typeof today==='function'?today():new Date().toISOString().slice(0,10));
  const currentUser=()=>{try{return typeof session==='function'?(session()||{}):JSON.parse(localStorage.getItem('tasneef_user')||'{}')}catch(_){return {}}};
  function message(t,type){try{ if(typeof msg==='function') msg(t,type||'ok'); else alert(t); }catch(_){alert(t)}}
  function addStyle(){
    if($id('tasneefAttendanceV343Style')) return;
    const st=document.createElement('style'); st.id='tasneefAttendanceV343Style';
    st.textContent=`
      .att-v343-toolbar{display:grid;grid-template-columns:repeat(4,minmax(160px,1fr));gap:10px;align-items:end;margin:12px 0}
      .att-v343-actions{display:flex;gap:8px;flex-wrap:wrap;margin:10px 0}
      .att-v343-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:10px;margin-top:12px}
      .att-v343-card{border:1px solid var(--line,#dce6e2);background:#fff;border-radius:18px;padding:12px;box-shadow:0 6px 18px rgba(10,64,51,.04)}
      .att-v343-name{font-weight:900;color:var(--brand,#0A4033);font-size:16px;margin-bottom:6px}.att-v343-meta{color:var(--muted,#60706a);font-size:12px;line-height:1.7;margin-bottom:8px}
      .att-v343-row{display:grid;grid-template-columns:1fr;gap:7px}.att-v343-note{font-size:12px;padding:8px!important;border-radius:10px!important;min-height:38px!important}
      .att-v343-summary{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:12px 0}.att-v343-summary .kpi b{font-size:26px!important}
      .att-cell.att-leave{background:#fff8e4;color:#8a6700;border:1px solid #eed797}.att-cell.att-transfer{background:#eaf0ff;color:#2444a7;border:1px solid #c8d3ff}
      .att-v343-print-title{display:none}.attendance-matrix-v343 th,.attendance-matrix-v343 td{text-align:center}.attendance-matrix-v343 th:first-child,.attendance-matrix-v343 td:first-child{position:sticky;right:0;background:#fff;z-index:3;text-align:right;min-width:170px}
      @media(max-width:800px){.att-v343-toolbar,.att-v343-summary{grid-template-columns:1fr}.att-v343-grid{grid-template-columns:1fr}}
      @media print{body{background:#fff!important}.side,.nav,.actions,.filters button,.att-v343-actions,#globalMsg{display:none!important}.att-v343-print-title{display:block!important;text-align:center;margin:0 0 14px;color:#0A4033}.attendance-matrix-wrap{max-height:none!important;overflow:visible!important;border:0!important}.attendance-matrix-v343{font-size:10px!important}.attendance-matrix-v343 th,.attendance-matrix-v343 td{padding:5px!important}.card{box-shadow:none!important;border:0!important}.att-cell{width:20px!important;height:20px!important;border-radius:7px!important;font-size:10px!important}}
    `;
    document.head.appendChild(st);
  }
  function normId(v){ return S(v); }
  function workerRow(id){ return A(ds().workers).find(w=>normId(w.id)===normId(id)) || {}; }
  function projectRow(id){ return A(ds().projects).find(p=>normId(p.id)===normId(id)) || {}; }
  function userRow(id){ return A(ds().users).find(u=>normId(u.id)===normId(id)) || A(ds().supervisors).find(u=>normId(u.id)===normId(id)) || {}; }
  function wName(id,rec){ const w=workerRow(id); return w.name||w.full_name||rec?.worker_name||rec?.name||'-'; }
  function pName(id){ const p=projectRow(id); try{return (typeof projectName==='function'?projectName(id):'') || p.name || '-'}catch(_){return p.name||'-'} }
  function sName(id,rec){ const u=userRow(id); try{return (typeof supervisorName==='function'?supervisorName(id):'') || u.full_name || u.name || u.username || rec?.supervisor_name || '-'}catch(_){return u.full_name||u.name||u.username||rec?.supervisor_name||'-'} }
  function workerProjectIds(w){
    const out=[]; ['project_id','assigned_project_id','current_project_id'].forEach(k=>{if(w&&w[k]) out.push(w[k])});
    ['project_ids','assigned_project_ids','projects'].forEach(k=>{const v=w&&w[k]; if(Array.isArray(v)) v.forEach(x=>out.push(typeof x==='object'?(x.id||x.project_id):x)); else if(typeof v==='string') v.split(/[,،\s]+/).forEach(x=>x&&out.push(x));});
    return [...new Set(out.map(normId).filter(Boolean))];
  }
  function workerSupervisorIds(w){
    const out=[]; ['app_supervisor_id','supervisor_id','assigned_supervisor_id'].forEach(k=>{if(w&&w[k]) out.push(w[k])});
    workerProjectIds(w).forEach(pid=>{const p=projectRow(pid); if(p.supervisor_id) out.push(p.supervisor_id);});
    return [...new Set(out.map(normId).filter(Boolean))];
  }
  function mainProjectIdForWorker(w, selectedProject){
    const ids=workerProjectIds(w); if(selectedProject && ids.includes(normId(selectedProject))) return selectedProject;
    return ids[0] || w.project_id || w.assigned_project_id || '';
  }
  function statusInfo(st){
    st=S(st)||'not_recorded';
    if(['present','حاضر','حضور'].includes(st)) return {label:'حاضر',short:'ح',cls:'green',cell:'att-present'};
    if(['absent','غائب','غياب'].includes(st)) return {label:'غائب',short:'غ',cls:'red',cell:'att-absent'};
    if(['leave','vacation','إجازة','اجازة'].includes(st)) return {label:'إجازة',short:'ج',cls:'amber',cell:'att-leave'};
    if(['transferred','transfer','نقل','منقول'].includes(st)) return {label:'نقل',short:'ن',cls:'',cell:'att-transfer'};
    return {label:'لم يسجل',short:'-',cls:'',cell:'att-empty'};
  }
  function statusOptions(selected){
    const opts=[['present','حاضر'],['absent','غائب'],['leave','إجازة'],['transferred','نقل لمشروع آخر']];
    return opts.map(([v,l])=>`<option value="${v}" ${S(selected||'present')===v?'selected':''}>${l}</option>`).join('');
  }
  function recordDate(a){return S(a.attendance_date||a.date||a.created_at).slice(0,10)}
  function recordMonth(a){return recordDate(a).slice(0,7)}
  function daysInMonth(m){const [y,mo]=S(m||todayStr().slice(0,7)).split('-').map(Number); return new Date(y,mo,0).getDate();}
  function dateOf(m,d){return `${m}-${String(d).padStart(2,'0')}`;}
  function extractShift(note){ const m=S(note).match(/الفترة\s*:\s*([^|\n]+)/); return m?m[1].trim():''; }
  function cleanNote(note){ return S(note).replace(/الفترة\s*:\s*[^|\n]+\s*\|?\s*/,'').trim(); }
  function recordSupervisorId(a){
    if(a.supervisor_id) return a.supervisor_id;
    const w=workerRow(a.worker_id); const sids=workerSupervisorIds(w); if(sids[0]) return sids[0];
    const p=projectRow(a.project_id || mainProjectIdForWorker(w,'')); return p.supervisor_id||'';
  }
  function recordProjectId(a){ const w=workerRow(a.worker_id); return a.project_id || mainProjectIdForWorker(w,'') || ''; }
  function recordMatchesSupervisor(a, sid){
    if(!sid) return true;
    if(normId(a.supervisor_id)===normId(sid)) return true;
    if(normId(recordSupervisorId(a))===normId(sid)) return true;
    const w=workerRow(a.worker_id); if(workerSupervisorIds(w).includes(normId(sid))) return true;
    const p=projectRow(recordProjectId(a)); return normId(p.supervisor_id)===normId(sid);
  }
  function supervisorRows(){
    const rows=[...A(ds().supervisors),...A(ds().users).filter(u=>S(u.role)==='supervisor')]; const seen=new Set();
    return rows.filter(r=>r&&r.id!=null&&!seen.has(normId(r.id))&&(seen.add(normId(r.id)),true));
  }
  function setSupervisorSelect(id, keep=true){
    const el=$id(id); if(!el) return;
    const old=keep?el.value:'';
    el.innerHTML='<option value="">كل المشرفين</option>'+supervisorRows().map(s=>`<option value="${E(s.id)}">${E(s.full_name||s.name||s.username||s.id)}</option>`).join('');
    if(old && [...el.options].some(o=>o.value===old)) el.value=old;
  }
  function fillProjectsSelect(el, projects, allLabel){
    if(!el) return; const old=el.value;
    el.innerHTML=`<option value="">${E(allLabel||'كل المشاريع')}</option>`+A(projects).map(p=>`<option value="${E(p.id)}">${E(p.name||p.title||p.id)}</option>`).join('');
    if(old && [...el.options].some(o=>o.value===old)) el.value=old;
  }
  function setupSupervisorAttendanceUI(){
    const list=$id('supervisorAttendanceList'); if(!list) return; addStyle();
    const card=list.closest('.card'); if(!card || card.dataset.v343Ready) return;
    card.dataset.v343Ready='1';
    const oldDate=$id('attendanceDate')?.value || todayStr();
    card.innerHTML=`
      <h2>تحضير اليوم</h2>
      <div class="sup-help">اختر المشروع والفترة، ثم حدّد حالة كل عامل. عند الحفظ يتم تحديث السجل الموجود لنفس اليوم والعامل بدل التكرار.</div>
      <div class="att-v343-toolbar">
        <div><label>التاريخ</label><input type="date" id="attendanceDate" value="${E(oldDate)}" onchange="renderSupervisorAttendanceList()"></div>
        <div><label>المشروع</label><select id="attendanceProject" onchange="renderSupervisorAttendanceList()"><option value="">كل مشاريع المشرف</option></select></div>
        <div><label>الفترة</label><select id="attendanceShiftType"><option value="زيارة يومية">زيارة يومية</option><option value="دوام كامل">دوام كامل</option><option value="صباح">صباح</option><option value="مساء">مساء</option></select></div>
        <div><label>بحث العامل</label><input id="attendanceWorkerSearchV343" oninput="renderSupervisorAttendanceList()" placeholder="اسم العامل"></div>
      </div>
      <div class="att-v343-actions">
        <button type="button" class="light" onclick="attendanceMarkAllV343('present')">اعتماد الكل حاضر</button>
        <button type="button" class="light" onclick="attendanceMarkAllV343('absent')">اعتماد الكل غائب</button>
        <button type="button" class="light" onclick="renderSupervisorAttendanceList()">تحديث الأسماء</button>
      </div>
      <div id="supervisorAttendanceList" class="att-v343-grid"></div>
      <div class="actions"><button onclick="saveSupervisorAttendance()">حفظ تحضير اليوم</button></div>
    `;
  }
  function supervisorContext(){
    const u=currentUser(); const allProjects=A(ds().projects); const supProjects=allProjects.filter(p=>normId(p.supervisor_id)===normId(u.id)); const pids=new Set(supProjects.map(p=>normId(p.id)));
    const workers=A(ds().workers).filter(w=>workerSupervisorIds(w).includes(normId(u.id)) || workerProjectIds(w).some(pid=>pids.has(normId(pid))));
    return {u,projects:supProjects,workers};
  }
  window.attendanceMarkAllV343=function(st){ document.querySelectorAll('#supervisorAttendanceList select.att-status-v343').forEach(s=>s.value=st); };
  window.renderSupervisorAttendanceList=function(){
    setupSupervisorAttendanceUI(); addStyle();
    const list=$id('supervisorAttendanceList'); if(!list) return;
    const {projects,workers}=supervisorContext(); fillProjectsSelect($id('attendanceProject'),projects,'كل مشاريع المشرف');
    const selectedProject=$id('attendanceProject')?.value||''; const q=S($id('attendanceWorkerSearchV343')?.value).toLowerCase();
    const date=$id('attendanceDate')?.value||todayStr();
    const todays=A(ds().attendance).filter(a=>recordDate(a)===date); const recByWorker=new Map(todays.map(a=>[normId(a.worker_id),a]));
    let rows=workers.filter(w=>!selectedProject || workerProjectIds(w).includes(normId(selectedProject)) || normId(w.project_id)===normId(selectedProject));
    if(q) rows=rows.filter(w=>S(w.name||w.full_name).toLowerCase().includes(q));
    rows.sort((a,b)=>S(a.name||a.full_name).localeCompare(S(b.name||b.full_name),'ar'));
    list.innerHTML=rows.map(w=>{
      const rec=recByWorker.get(normId(w.id))||{}; const pid=rec.project_id || mainProjectIdForWorker(w,selectedProject); const st=rec.status||'present';
      return `<div class="att-v343-card" data-worker="${E(w.id)}" data-project="${E(pid)}">
        <div class="att-v343-name">${E(w.name||w.full_name||('عامل '+w.id))}</div>
        <div class="att-v343-meta">المشروع: <b>${E(pName(pid))}</b>${rec.id?`<br>آخر حالة محفوظة: <b>${E(statusInfo(rec.status).label)}</b>`:''}</div>
        <div class="att-v343-row"><select class="att-status-v343" data-worker="${E(w.id)}" data-project="${E(pid)}">${statusOptions(st)}</select><input class="att-v343-note" data-worker="${E(w.id)}" placeholder="ملاحظة اختيارية" value="${E(cleanNote(rec.notes||''))}"></div>
      </div>`;
    }).join('') || '<div class="sup-help">لا توجد أسماء مرتبطة بهذا المشروع أو المشرف. راجع ربط العمال بالمشرف أو المشروع.</div>';
  };
  window.saveSupervisorAttendance=async function(){
    const u=currentUser(); if(!u.id) return message('سجّل الدخول أولاً','err');
    const date=$id('attendanceDate')?.value||todayStr(); const shift=$id('attendanceShiftType')?.value||'زيارة يومية'; const selectedProject=$id('attendanceProject')?.value||'';
    const cards=[...document.querySelectorAll('#supervisorAttendanceList .att-v343-card')]; if(!cards.length) return message('لا توجد أسماء للحفظ','err');
    let ok=0, fail=0, last='';
    for(const c of cards){
      const wid=N(c.dataset.worker); if(!wid) continue;
      const st=c.querySelector('select.att-status-v343')?.value||'present'; const note= c.querySelector('.att-v343-note')?.value||'';
      const w=workerRow(wid); const pid=selectedProject || c.dataset.project || mainProjectIdForWorker(w,'');
      const row={attendance_date:date,worker_id:wid,supervisor_id:N(u.id)||u.id,project_id:pid?(N(pid)||pid):null,status:st,notes:`الفترة: ${shift}${note?' | '+note:''}`,created_by:N(u.id)||u.id};
      try{
        const found=await sb.from('attendance').select('id').eq('attendance_date',date).eq('worker_id',wid).limit(1);
        if(found.error) throw found.error;
        const res=(found.data&&found.data.length)? await sb.from('attendance').update(row).eq('id',found.data[0].id) : await sb.from('attendance').insert([row]);
        if(res.error) throw res.error; ok++;
      }catch(e){ fail++; last=e?.message||String(e); console.error('attendance v343 save failed',row,e); }
    }
    if(fail) return message(`تم حفظ ${ok} اسم وفشل ${fail}: ${last}`,'err');
    message(`تم حفظ تحضير اليوم لعدد ${ok} اسم`);
    try{ await loadAll(); }catch(e){ console.warn(e); }
    try{ window.renderSupervisorAttendanceList(); }catch(_){ }
    try{ window.renderAttendance && window.renderAttendance(); }catch(_){ }
    try{ window.renderAttendanceMonthly && window.renderAttendanceMonthly(); }catch(_){ }
  };
  const oldInitSupervisor=window.initSupervisor || (typeof initSupervisor==='function'?initSupervisor:null);
  window.initSupervisor=async function(){
    const u=(typeof requireRole==='function'?requireRole('supervisor'):currentUser()); if(!u) return;
    try{ await loadAll(); }catch(e){ console.error(e); }
    const allWorkers=[...A(ds().workers)], allProjects=[...A(ds().projects)], allLogs=[...A(ds().logs)], allTickets=[...A(ds().tickets)];
    const supProjects=allProjects.filter(p=>normId(p.supervisor_id)===normId(u.id)); const pids=new Set(supProjects.map(p=>normId(p.id)));
    const supWorkers=allWorkers.filter(w=>workerSupervisorIds(w).includes(normId(u.id)) || workerProjectIds(w).some(pid=>pids.has(normId(pid))));
    try{ data.projects=supProjects; data.workers=supWorkers; data.logs=allLogs.filter(l=>normId(l.supervisor_id)===normId(u.id)); data.tickets=allTickets.filter(t=>normId(t.supervisor_id)===normId(u.id)||normId(t.created_by)===normId(u.id)||pids.has(normId(t.project_id))); }catch(_){ }
    if($id('supTitle')) $id('supTitle').textContent=`لوحة المشرف - ${u.full_name||u.name||u.username||''}`;
    try{ if(typeof fillSelect==='function'){ fillSelect('logProject',data.projects,'name','اختر المشروع'); fillSelect('attendanceProject',data.projects,'name','كل مشاريع المشرف'); fillSelect('ticketProject',data.projects,'name','اختر المشروع'); fillSelect('supTicketFilterProject',data.projects,'name','كل المشاريع'); } }catch(_){ }
    if($id('logDate')&&!$id('logDate').value) $id('logDate').value=todayStr(); if($id('attendanceDate')&&!$id('attendanceDate').value) $id('attendanceDate').value=todayStr();
    setupSupervisorAttendanceUI();
    try{ renderSupervisorAttendanceList(); }catch(e){ console.warn(e); }
    try{ renderTimeLogs(); }catch(_){ }
    try{ renderTickets(); }catch(_){ }
    try{ if(typeof supervisorInventoryLoad==='function') await supervisorInventoryLoad(); }catch(_){ }
  };
  try{ initSupervisor=window.initSupervisor; }catch(_){ }
  window.renderAttendance=function(){
    addStyle();
    const b=$id('attendanceBody'); if(!b) return;
    const date=$id('attendanceFilterDate')?.value||''; const sid=$id('attendanceFilterSupervisor')?.value||''; const q=S($id('attendanceSearch')?.value).toLowerCase();
    setSupervisorSelect('attendanceFilterSupervisor');
    const table=b.closest('table'); const thead=table?.querySelector('thead'); if(thead) thead.innerHTML='<tr><th>التاريخ</th><th>العامل</th><th>المشرف</th><th>المشروع</th><th>الحالة</th><th>الفترة</th><th>ملاحظات</th><th>إجراء</th></tr>';
    let rows=A(ds().attendance).filter(a=>(!date||recordDate(a)===date) && recordMatchesSupervisor(a,sid));
    if(q) rows=rows.filter(a=>[wName(a.worker_id,a),sName(recordSupervisorId(a),a),pName(recordProjectId(a)),S(a.notes)].join(' ').toLowerCase().includes(q));
    rows.sort((a,b)=>S(recordDate(b)).localeCompare(S(recordDate(a))) || Number(b.id||0)-Number(a.id||0));
    b.innerHTML=rows.map(a=>{const st=statusInfo(a.status); const sid2=recordSupervisorId(a), pid=recordProjectId(a); return `<tr><td>${E(recordDate(a))}</td><td><b>${E(wName(a.worker_id,a))}</b></td><td>${E(sName(sid2,a))}</td><td>${E(pName(pid))}</td><td><span class="badge ${st.cls}">${E(st.label)}</span></td><td>${E(extractShift(a.notes)||'-')}</td><td>${E(cleanNote(a.notes||''))}</td><td class="row-actions"><button onclick="editAttendance(${Number(a.id)})">تعديل</button><button class="danger" onclick="deleteRow('attendance',${Number(a.id)})">حذف</button></td></tr>`}).join('') || '<tr><td colspan="8">لا توجد سجلات حضور حسب الفلتر المختار</td></tr>';
  };
  try{ renderAttendance=window.renderAttendance; }catch(_){ }
  window.renderAttendanceMonthly=function(){
    addStyle();
    const body=$id('attendanceMatrixBody'), head=$id('attendanceMatrixHead'); if(!body||!head) return;
    const mEl=$id('attendanceMatrixMonth'); if(mEl&&!mEl.value) mEl.value=todayStr().slice(0,7);
    const month=mEl?.value||todayStr().slice(0,7); setSupervisorSelect('attendanceMatrixSupervisor');
    const sid=$id('attendanceMatrixSupervisor')?.value||''; const q=S($id('attendanceMatrixSearch')?.value).toLowerCase(); const days=daysInMonth(month);
    let recs=A(ds().attendance).filter(a=>recordMonth(a)===month && recordMatchesSupervisor(a,sid));
    if(q) recs=recs.filter(a=>[wName(a.worker_id,a),pName(recordProjectId(a)),sName(recordSupervisorId(a),a)].join(' ').toLowerCase().includes(q));
    // group by actual attendance record identity: worker + project + supervisor. This keeps transfers/projects visible and matches records.
    const groups=new Map();
    recs.forEach(a=>{const key=[a.worker_id||wName(a.worker_id,a),recordProjectId(a),recordSupervisorId(a)].map(normId).join('|'); if(!groups.has(key)) groups.set(key,{sample:a,records:[]}); groups.get(key).records.push(a);});
    const arr=[...groups.values()].sort((x,y)=>S(wName(x.sample.worker_id,x.sample)).localeCompare(S(wName(y.sample.worker_id,y.sample),'ar')));
    head.innerHTML='<tr><th>الاسم</th><th>النوع</th><th>المشرف</th><th>المشروع</th><th>الفترة</th>'+Array.from({length:days},(_,i)=>`<th>${String(i+1).padStart(2,'0')}</th>`).join('')+'<th>حضور</th><th>غياب</th><th>إجازة</th><th>نقل</th><th>النسبة</th></tr>';
    let totalP=0,totalA=0,totalL=0,totalT=0;
    body.closest('table')?.classList.add('attendance-matrix-v343');
    body.innerHTML=arr.map(g=>{
      const latest=new Map(); g.records.forEach(r=>{const d=recordDate(r); const old=latest.get(d); if(!old||Number(r.id||0)>Number(old.id||0)) latest.set(d,r);});
      let p=0,a=0,l=0,t=0; const cells=[];
      for(let d=1; d<=days; d++){ const rec=latest.get(dateOf(month,d)); const inf=statusInfo(rec?.status); if(inf.short==='ح')p++; else if(inf.short==='غ')a++; else if(inf.short==='ج')l++; else if(inf.short==='ن')t++; cells.push(`<td title="${E(inf.label)}"><span class="att-cell ${inf.cell}">${E(inf.short)}</span></td>`); }
      totalP+=p; totalA+=a; totalL+=l; totalT+=t; const denom=p+a+l+t; const pct=denom?(p/denom*100):0; const cls=pct>=90?'green':(pct>=70?'amber':'red');
      const sample=g.sample, wid=sample.worker_id, sup=recordSupervisorId(sample), pid=recordProjectId(sample), shift=extractShift(sample.notes)||'-'; const w=workerRow(wid);
      return `<tr><td><b>${E(wName(wid,sample))}</b></td><td>${E(w.worker_type==='support'?'بديل / مساند':'عامل')}</td><td>${E(sName(sup,sample))}</td><td>${E(pName(pid))}</td><td>${E(shift)}</td>${cells.join('')}<td><span class="badge green">${p}</span></td><td><span class="badge red">${a}</span></td><td><span class="badge amber">${l}</span></td><td><span class="badge">${t}</span></td><td><span class="badge ${cls}">${pct.toFixed(1)}%</span></td></tr>`;
    }).join('') || `<tr><td colspan="${days+10}">لا توجد سجلات حضور مطابقة للفلاتر المختارة</td></tr>`;
    const denom=totalP+totalA+totalL+totalT; const pct=denom?(totalP/denom*100):0;
    const sum=$id('attendanceMatrixSummary'); if(sum) sum.innerHTML=`<div class="kpi"><small>عدد الصفوف من سجلات الحضور</small><b>${arr.length}</b></div><div class="kpi"><small>إجمالي الحضور</small><b>${totalP}</b></div><div class="kpi"><small>إجمالي الغياب</small><b>${totalA}</b></div><div class="kpi"><small>نسبة الحضور</small><b>${pct.toFixed(1)}%</b></div>`;
    const card=body.closest('.card'); if(card && !$id('attV343PrintTitle')){ const title=document.createElement('h2'); title.id='attV343PrintTitle'; title.className='att-v343-print-title'; title.textContent='تقرير الحضور والغياب الشهري'; card.prepend(title); }
  };
  try{ renderAttendanceMonthly=window.renderAttendanceMonthly; }catch(_){ }
  window.printAttendanceMonthlyV343=function(){ window.renderAttendanceMonthly&&window.renderAttendanceMonthly(); setTimeout(()=>window.print(),100); };
  window.exportAttendanceMatrixCSV=function(){
    const month=$id('attendanceMatrixMonth')?.value||todayStr().slice(0,7); const rows=[];
    const heads=[...document.querySelectorAll('#attendanceMatrixHead th')].map(th=>'"'+th.textContent.trim().replace(/"/g,'""')+'"').join(','); rows.push(heads);
    document.querySelectorAll('#attendanceMatrixBody tr').forEach(tr=>rows.push([...tr.children].map(td=>'"'+td.textContent.trim().replace(/"/g,'""')+'"').join(',')));
    try{ if(typeof download==='function') download(`attendance-${month}.csv`, rows.join('\n')); else { const a=document.createElement('a'); a.href=URL.createObjectURL(new Blob([rows.join('\n')],{type:'text/csv;charset=utf-8'})); a.download=`attendance-${month}.csv`; a.click(); } }catch(e){console.error(e)}
  };
  function enhanceAdminButtons(){
    const sum=$id('attendanceMatrixSummary'); if(!sum) return; const actions=sum.nextElementSibling; if(actions && !actions.querySelector('[data-v343-print]')){ const btn=document.createElement('button'); btn.className='light'; btn.dataset.v343Print='1'; btn.textContent='طباعة PDF'; btn.onclick=window.printAttendanceMonthlyV343; actions.appendChild(btn); }
  }
  document.addEventListener('DOMContentLoaded',()=>{ addStyle(); setTimeout(()=>{ try{setupSupervisorAttendanceUI();}catch(_){ } try{enhanceAdminButtons();}catch(_){ } try{ if($id('attendanceMatrixBody')) window.renderAttendanceMonthly(); }catch(e){console.warn(e)} },800); });
  document.addEventListener('change',e=>{ if(e.target && ['attendanceMatrixMonth','attendanceMatrixSupervisor','attendanceFilterSupervisor','attendanceFilterDate'].includes(e.target.id)){ setTimeout(()=>{try{window.renderAttendance(); window.renderAttendanceMonthly();}catch(_){ }},20); } });
  document.addEventListener('input',e=>{ if(e.target && ['attendanceMatrixSearch','attendanceSearch'].includes(e.target.id)){ setTimeout(()=>{try{window.renderAttendance(); window.renderAttendanceMonthly();}catch(_){ }},20); } });
  window.TASNEEF_ATTENDANCE_BUILD=BUILD;
  console.log('Tasneef '+BUILD+' loaded');
})();

/* V358: debounce expensive view refreshes. No stored data is changed. */
(function(){
  if(window.__tasneefRenderDebounceV358) return;
  window.__tasneefRenderDebounceV358 = true;

  function debounceFn(name, wait){
    const original = window[name];
    if(typeof original !== 'function' || original.__v358Debounced) return;
    let timer = null, lastThis = null, lastArgs = null;
    const wrapped = function(){
      lastThis = this;
      lastArgs = arguments;
      clearTimeout(timer);
      timer = setTimeout(function(){
        timer = null;
        try{ original.apply(lastThis, lastArgs); }catch(e){ console.warn(name + ' debounced render failed', e); }
      }, wait);
    };
    wrapped.__v358Debounced = true;
    wrapped.__v358Original = original;
    window[name] = wrapped;
  }

  function install(){
    debounceFn('renderAttendance', 70);
    debounceFn('renderAttendanceMonthly', 70);
    debounceFn('renderMonthly', 90);
    debounceFn('renderTickets', 70);
    debounceFn('financeRenderReports', 90);
    debounceFn('financeRenderAll', 90);

    const currentShowPage = window.showPage;
    if(typeof currentShowPage === 'function' && !currentShowPage.__v358Guarded){
      let lastPage = '', lastAt = 0;
      const guardedShowPage = function(id, btn){
        const now = Date.now();
        if(id === lastPage && now - lastAt < 250) return;
        lastPage = id;
        lastAt = now;
        return currentShowPage.apply(this, arguments);
      };
      guardedShowPage.__v358Guarded = true;
      window.showPage = guardedShowPage;
    }
  }

  if(document.readyState === 'loading') document.addEventListener('DOMContentLoaded', install, {once:true});
  else install();
  window.addEventListener('load', install, {once:true});
  setTimeout(install, 1200);
})();

/* V359: fast admin start. Shows the dashboard first, then warms the rest. */
(function(){
  if(window.__tasneefFastAdminV359) return;
  window.__tasneefFastAdminV359 = true;

  function safeRows(res){ return res && !res.error && Array.isArray(res.data) ? res.data : []; }
  function schedule(fn){
    if('requestIdleCallback' in window) requestIdleCallback(fn, {timeout: 1800});
    else setTimeout(fn, 700);
  }

  async function fastLoadAdminData(){
    const d = today();
    const [users, projects, workers, tickets, services, logs] = await Promise.all([
      sb.from('app_users').select('*').order('id'),
      sb.from('projects').select('*').order('id'),
      sb.from('workers').select('*').order('id'),
      sb.from('tickets').select('*').order('id', {ascending:false}).limit(300),
      sb.from('contract_services').select('*').order('id', {ascending:false}).limit(300),
      sb.from('time_logs').select('id,user_id,supervisor_id,project_id,check_in,check_out,duration_minutes,travel_minutes,visit_type,required_minutes,time_difference_minutes,time_status,notes,created_at,updated_at,log_date').gte('log_date', d).lte('log_date', d).order('id', {ascending:false}).limit(500)
    ]);

    data.users = safeRows(users);
    data.supervisors = data.users.filter(u=>u.role==='supervisor' && u.is_active!==false);
    data.technicians = data.users.filter(u=>u.role==='technician' && u.is_active!==false);
    data.projects = safeRows(projects);
    data.workers = safeRows(workers);
    data.tickets = safeRows(tickets);
    data.contractServices = safeRows(services);
    data.logs = safeRows(logs);
    if(!Array.isArray(data.attendance)) data.attendance = [];
  }

  const fullInitAdminV359 = window.initAdmin || (typeof initAdmin === 'function' ? initAdmin : null);
  let started = false;
  window.initAdmin = async function(){
    if(started) return window.__tasneefAdminWarmupPromise;
    started = true;
    const u = requireRole('admin');
    if(!u) return;
    try{
      await fastLoadAdminData();
      try{ hydrateForms(); }catch(e){ console.warn('fast hydrate failed', e); }
      try{ renderAll(); }catch(e){ console.warn('fast renderAll failed', e); try{ renderDashboard(); renderAlerts(); }catch(_e){} }
    }catch(e){
      console.warn('fast admin start failed, falling back to full start', e);
      if(typeof fullInitAdminV359 === 'function') return fullInitAdminV359.apply(this, arguments);
    }

    if(!window.__tasneefAdminWarmupPromise){
      schedule(function(){
        window.__tasneefAdminWarmupPromise = (window.loadAll ? window.loadAll() : loadAll())
          .then(function(){
            try{ hydrateForms(); }catch(_){}
            try{ renderAll(); }catch(_){ try{ renderDashboard(); renderAlerts(); }catch(__){} }
          })
          .catch(function(e){ console.warn('admin background warmup failed', e); });
      });
    }
  };
  try{ initAdmin = window.initAdmin; }catch(_){}
  if(document.getElementById('dashboard')){
    if(document.readyState === 'loading') document.addEventListener('DOMContentLoaded', function(){ window.initAdmin(); }, {once:true});
    else setTimeout(function(){ window.initAdmin(); }, 0);
  }
})();
